    cache merge tree socket socket delta buffer commit  # 0
    rebase parser token tree window  # 1
    handle rebase branch ref socket token  # 2
    pack header delta stream branch stream blob stream merge queue tree  # 3
    object merge pack worker cache queue commit  # 4
    commit cache remote window index  # 5
    header tree queue socket  # 6
    buffer ref window header index remote ref cache ref cache  # 7
    cache handle delta index branch merge commit  # 8
    blob header socket cache queue worker  # 9
    commit parser queue commit header queue window  # 10
    queue pack window token header delta buffer branch merge merge parser header  # 11
    handle delta rebase token window tree buffer index cache rebase pack  # 12
    queue window token branch cache stream header pack tree header rebase delta  # 13
    cache handle merge rebase branch parser tree merge blob remote queue index  # 14
    branch worker cache ref queue header queue delta remote  # 15
    ref pack stream tree index delta remote buffer tree branch queue  # 16
    window queue cache merge remote pack rebase handle  # 17
    parser index stream pack token rebase merge window parser commit  # 18
    ref tree header branch  # 19
    cache buffer tree window commit index merge socket  # 20
    queue handle handle branch handle pack queue blob socket  # 21
    ref branch pack pack window remote  # 22
    tree window rebase merge  # 23
    buffer handle parser ref  # 24
    socket ref index blob queue queue  # 25
    queue queue token token cache stream worker ref ref  # 26
    parser header stream queue remote parser socket blob tree parser  # 27
    pack header handle socket stream queue window  # 28
    remote remote remote window pack delta pack handle remote  # 29
    header tree pack pack queue commit window object object index rebase merge  # 30
    parser delta handle worker branch pack header ref cache worker queue  # 31
    rebase cache delta header remote branch  # 32
    tree ref handle rebase branch worker delta handle commit delta  # 33
    commit delta token index pack object header merge blob queue socket  # 34
    window worker header handle  # 35
    stream commit cache buffer object header  # 36
    pack pack blob merge tree merge worker handle cache  # 37
    index tree socket tree remote  # 38
    window remote header ref merge cache buffer queue tree commit header ref  # 39
    pack buffer tree tree blob delta handle delta  # 40
    commit branch object cache commit worker token delta object cache queue  # 41
    pack pack parser ref pack queue merge worker tree window worker remote  # 42
    pack branch token pack rebase queue index merge stream rebase  # 43
    handle merge cache parser branch window tree blob header commit parser  # 44
    header commit worker header queue socket delta header cache delta blob  # 45
    branch stream blob rebase object buffer stream delta merge  # 46
    parser window parser pack pack socket commit ref queue window index  # 47
    socket token header window branch delta token  # 48
    worker rebase remote delta branch delta worker remote  # 49
    delta rebase tree index window  # 50
    window socket pack handle delta merge queue header  # 51
    cache stream delta cache remote pack merge  # 52
    token cache delta stream remote buffer delta worker token  # 53
    buffer branch branch queue tree index ref socket cache handle object  # 54
    ref header handle worker worker queue blob merge token remote merge  # 55
    tree delta handle pack delta  # 56
    worker tree pack cache  # 57
    handle pack buffer header branch blob merge queue tree  # 58
    remote worker window token index object ref parser header  # 59
    ref queue blob pack queue  # 60
    remote header rebase parser object branch ref merge  # 61
    remote rebase ref remote object delta cache stream parser ref  # 62
    window socket rebase rebase  # 63
    index stream window remote tree token pack stream blob cache commit  # 64
    commit stream token commit index header delta parser window tree pack pack  # 65
    worker pack window ref handle  # 66
    token worker remote token stream buffer token  # 67
    tree socket commit stream cache parser worker token branch branch socket index  # 68
