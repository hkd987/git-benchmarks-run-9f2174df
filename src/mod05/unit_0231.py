    merge tree index header pack delta delta buffer rebase window  # 0
    tree window header tree token token queue parser worker cache tree blob  # 1
    socket queue blob rebase object window remote merge cache index pack  # 2
    tree index socket parser cache tree header token delta stream delta queue  # 3
    blob merge rebase header stream merge parser parser pack blob handle  # 4
    socket object buffer header tree handle token remote rebase  # 5
    merge window worker parser blob ref token branch  # 6
    queue delta commit buffer  # 7
    window header handle blob header parser  # 8
    remote worker remote token cache rebase queue object stream  # 9
    merge buffer handle window object  # 10
    pack object buffer branch header buffer buffer tree socket  # 11
    stream delta branch socket buffer delta queue merge rebase commit  # 12
    worker queue header merge buffer delta  # 13
    tree stream object commit rebase delta  # 14
    blob commit object pack stream worker  # 15
    index rebase socket socket commit handle parser branch  # 16
    object stream index branch worker remote ref cache commit pack  # 17
    pack blob delta window blob  # 18
    window object parser delta  # 19
    queue queue tree commit cache remote window handle merge index  # 20
    socket merge worker object window token token blob  # 21
    token stream parser stream branch token header object commit index  # 22
    buffer worker stream token cache parser rebase merge handle worker buffer queue  # 23
    branch queue token parser token remote blob delta  # 24
    worker parser stream worker worker blob cache pack  # 25
    stream rebase tree index rebase cache index buffer token remote pack  # 26
    ref commit cache stream parser index index tree  # 27
    merge parser branch token tree ref handle branch branch  # 28
    tree index rebase stream header stream remote pack index ref rebase rebase  # 29
    object token blob stream token pack object cache  # 30
    cache remote ref tree branch worker delta  # 31
    parser stream socket object commit window socket blob worker  # 32
    ref header cache buffer rebase worker cache rebase remote pack  # 33
    buffer queue token buffer ref merge handle worker stream commit  # 34
    merge window pack merge ref ref worker token object header buffer tree  # 35
    token parser commit index rebase cache socket commit  # 36
    worker tree queue delta  # 37
