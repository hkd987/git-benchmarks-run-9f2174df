    blob window index ref buffer commit  # 0
    handle commit merge delta pack blob  # 1
    delta worker commit blob rebase header cache  # 2
    blob object queue merge ref tree branch token remote  # 3
    pack stream buffer commit remote index cache  # 4
    window index handle index ref worker buffer queue ref  # 5
    worker pack object merge blob branch cache object rebase  # 6
    merge rebase token remote worker worker  # 7
    commit ref ref window index token delta commit socket remote queue  # 8
    ref remote socket buffer delta rebase buffer  # 9
    token stream worker token merge blob merge cache cache parser index  # 10
    handle window buffer token  # 11
    tree branch buffer delta ref token stream stream pack stream remote  # 12
    branch stream queue window merge  # 13
    object token pack cache pack  # 14
    stream ref queue header header merge object cache object pack  # 15
    blob window branch stream  # 16
    header cache parser remote buffer branch queue  # 17
    worker rebase index tree  # 18
    merge window stream remote merge stream socket queue rebase remote  # 19
    branch queue index handle  # 20
    cache stream index branch commit remote buffer ref blob object stream  # 21
    pack window commit commit token  # 22
    merge worker branch merge cache commit tree blob ref  # 23
    worker ref index buffer pack buffer stream token  # 24
    buffer ref worker merge token stream delta worker handle handle index  # 25
    handle handle cache buffer header tree token parser branch tree index object  # 26
    blob cache blob ref token commit pack merge merge stream tree branch  # 27
    cache merge token tree remote rebase window object  # 28
    delta buffer worker window window handle window ref buffer index remote  # 29
    worker window pack token buffer  # 30
