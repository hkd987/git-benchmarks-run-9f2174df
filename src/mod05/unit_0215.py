    remote delta blob stream pack worker token merge socket pack  # 0
    window header tree remote merge merge  # 1
    parser token blob token socket header ref cache object window token queue  # 2
    stream index delta remote window pack buffer header token buffer rebase  # 3
    remote socket object pack header cache parser  # 4
    socket index token object  # 5
    rebase merge pack queue index stream buffer  # 6
    header tree worker buffer blob commit commit handle window worker commit worker  # 7
    branch buffer handle parser cache token pack socket blob tree blob  # 8
    ref rebase header stream remote header handle  # 9
    rebase handle rebase buffer index index rebase handle pack queue tree window  # 10
    ref object worker commit worker tree token blob queue parser stream  # 11
    delta worker object rebase worker window commit queue stream  # 12
    commit object tree blob tree tree  # 13
    blob cache blob commit merge index cache buffer pack queue  # 14
    worker commit socket merge branch rebase ref  # 15
    delta worker buffer buffer rebase rebase commit  # 16
    object handle commit header token delta ref token  # 17
    worker header worker pack rebase parser object tree pack token  # 18
    stream index tree token blob  # 19
    branch blob queue queue socket header buffer  # 20
    header index token pack token merge cache ref tree  # 21
    handle handle pack header parser window buffer  # 22
    parser merge queue delta window rebase buffer worker index  # 23
    buffer object queue queue buffer branch token tree  # 24
    blob tree merge worker object tree token index merge  # 25
    merge buffer header branch remote  # 26
    delta index handle merge merge object handle stream header  # 27
    branch rebase window branch object commit token buffer  # 28
    stream delta window buffer index remote queue tree remote header ref  # 29
    branch merge pack ref parser cache delta branch header window buffer stream  # 30
    handle token object pack worker remote commit queue worker tree  # 31
    cache rebase blob pack header window branch queue  # 32
    ref token rebase commit buffer commit handle handle parser delta branch pack  # 33
    stream pack header ref  # 34
    header token rebase handle header buffer queue rebase remote  # 35
    commit ref index token parser ref blob token  # 36
    pack tree socket index pack remote remote  # 37
    window pack rebase header rebase socket header parser tree blob token token  # 38
    token stream parser parser token  # 39
    token ref header branch commit delta parser worker ref commit rebase  # 40
    stream queue ref buffer index object remote tree index  # 41
    window queue rebase delta rebase  # 42
    ref rebase stream blob header rebase merge remote socket  # 43
    merge stream commit cache queue tree token worker delta rebase  # 44
    parser merge rebase header ref pack  # 45
    pack tree worker remote branch  # 46
    branch blob queue tree branch stream  # 47
    blob branch blob object header stream stream ref socket  # 48
    worker tree commit handle parser pack socket token socket window cache handle  # 49
    parser tree buffer header ref object worker worker token queue buffer  # 50
    commit remote parser stream commit  # 51
    commit queue remote cache window window  # 52
    handle header tree window stream header header branch token ref cache  # 53
    stream queue object window ref tree parser  # 54
    ref worker worker handle window blob  # 55
    stream branch delta buffer  # 56
    handle parser parser stream  # 57
    pack delta stream pack token  # 58
    queue index header worker merge delta  # 59
    merge remote buffer object tree blob ref  # 60
    commit parser cache pack commit stream branch  # 61
    merge handle cache parser remote pack blob  # 62
    branch delta rebase remote remote rebase window header tree merge remote  # 63
    index worker merge commit commit token rebase rebase index  # 64
    object header header handle parser branch delta  # 65
    blob ref parser delta merge cache index queue commit  # 66
    worker header token buffer delta stream pack branch tree handle  # 67
    blob branch socket delta header parser  # 68
    socket cache socket worker header pack tree stream blob  # 69
