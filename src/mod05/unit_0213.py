    socket ref worker stream window  # 0
    blob pack merge merge rebase window window header worker  # 1
    queue ref window branch ref  # 2
    socket merge parser pack  # 3
    cache merge commit handle merge queue socket commit blob header  # 4
    stream commit token token socket header ref branch  # 5
    index worker parser queue delta tree buffer window  # 6
    token window delta stream socket merge  # 7
    object window handle queue parser token worker parser worker token  # 8
    merge pack token index worker remote stream header header  # 9
    index remote index object worker socket merge merge pack merge parser  # 10
    index object buffer branch handle queue object pack worker  # 11
    pack token object rebase merge rebase ref index  # 12
    commit merge stream buffer object delta queue queue  # 13
    commit header handle token blob  # 14
    index parser token remote token  # 15
    window blob buffer handle merge socket pack token handle socket delta  # 16
    delta buffer rebase ref header commit token object header merge pack  # 17
    token ref parser merge remote commit queue header blob worker  # 18
    rebase stream pack worker commit cache ref handle  # 19
    remote branch handle window index  # 20
    handle cache index buffer rebase  # 21
    parser merge stream merge handle ref branch queue queue branch blob pack  # 22
    branch queue queue socket branch header rebase merge header buffer object  # 23
    object rebase stream queue rebase header  # 24
    ref pack cache merge window  # 25
    ref rebase token parser rebase queue cache delta ref pack parser merge  # 26
    blob cache index branch window object handle buffer remote object  # 27
    rebase tree worker buffer remote ref worker token handle  # 28
    window index socket window ref  # 29
    rebase ref tree ref ref commit remote parser object delta socket handle  # 30
    merge queue commit worker cache rebase header cache worker  # 31
    socket buffer header object header commit object merge socket ref socket  # 32
    parser worker buffer object  # 33
    worker merge remote ref remote socket cache remote  # 34
    stream branch pack delta header parser buffer socket queue  # 35
    socket socket queue header cache object queue  # 36
    parser handle parser commit index merge blob worker index index object  # 37
    socket pack cache socket  # 38
    cache commit object handle parser merge token buffer header  # 39
    queue blob window remote remote cache index cache  # 40
    queue stream remote index branch handle  # 41
    socket worker tree cache buffer worker  # 42
    stream blob socket token queue rebase commit worker  # 43
    queue object ref header branch merge remote stream header remote rebase delta  # 44
    ref tree cache delta  # 45
    stream pack ref worker blob  # 46
    parser window merge header object handle commit index handle delta delta delta  # 47
    worker index rebase worker  # 0
    index pack object ref rebase worker tree ref parser worker  # 1
    token window blob window handle socket stream branch merge object  # 2
    buffer socket rebase branch rebase pack parser buffer  # 3
    tree token parser handle socket  # 4
    remote blob cache object  # 0
    remote merge pack pack tree index socket branch socket delta merge pack  # 1
    merge remote object token ref token blob cache remote tree object token  # 2
    merge handle rebase pack cache tree stream delta token  # 3
    rebase delta ref cache tree rebase rebase cache index  # 4
    pack blob socket token  # 5
    token tree object window window cache remote remote  # 6
    blob tree ref blob merge  # 7
    remote buffer remote tree merge handle  # 8
    worker socket commit index worker  # 9
    header merge pack blob rebase handle buffer token worker  # 10
