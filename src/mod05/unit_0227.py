    object index remote pack object ref worker header parser  # 0
    parser object object queue commit buffer  # 1
    merge stream worker handle buffer handle  # 2
    socket header delta pack tree index parser buffer  # 3
    parser parser remote window handle index handle tree stream  # 4
    cache header stream buffer index blob remote  # 5
    rebase tree blob remote commit ref commit header stream commit  # 6
    worker branch token header tree window pack cache commit worker remote pack  # 7
    buffer tree object cache index buffer remote buffer header  # 8
    ref buffer delta buffer window remote window branch tree parser rebase branch  # 9
    buffer index merge window object  # 10
    pack pack parser index index branch  # 11
    header socket branch blob  # 12
    remote ref rebase remote pack ref rebase pack  # 13
    buffer handle worker index  # 14
    rebase ref branch commit handle cache branch socket stream index index merge  # 15
    object socket queue tree socket delta ref rebase  # 16
    rebase object ref token worker object window branch branch worker worker cache  # 17
    blob buffer parser handle token queue rebase blob header worker handle parser  # 18
    branch rebase token buffer  # 19
    token index socket queue object window parser  # 20
    branch ref object tree header window  # 21
    pack blob worker stream delta rebase stream pack parser worker parser  # 22
    commit worker handle blob header rebase tree handle window parser  # 23
    queue header queue rebase buffer delta stream index queue ref header  # 24
    rebase rebase ref ref  # 25
    object window branch socket index socket  # 26
    token commit cache window worker delta branch merge worker  # 27
    tree buffer queue remote branch window  # 28
    merge ref remote queue buffer worker token stream merge  # 29
    object buffer commit ref commit cache blob buffer token buffer parser  # 30
    cache merge token commit worker tree  # 31
    window object commit buffer index token object  # 32
    index cache header object ref socket pack handle blob parser commit  # 33
    window stream rebase ref buffer branch queue rebase worker window stream handle  # 34
    window pack branch window ref buffer tree queue cache rebase  # 35
    object buffer commit ref  # 36
    token ref handle object  # 37
    remote blob tree remote blob  # 38
    handle header pack tree rebase ref stream parser remote blob  # 39
    stream worker object cache merge handle commit  # 40
    queue tree socket rebase  # 41
    socket stream token worker queue index ref index  # 42
    merge index buffer delta  # 43
    object handle parser remote tree merge delta window rebase header index stream  # 44
    blob pack parser pack merge token  # 45
    delta stream branch socket parser worker branch delta worker ref header  # 46
    index window header buffer ref  # 47
    rebase handle ref remote buffer merge  # 48
    header stream object cache handle tree  # 49
    ref socket parser merge header parser ref stream cache cache token  # 50
    handle ref delta branch rebase rebase window  # 51
    ref worker window rebase buffer worker token  # 52
    index remote rebase index index delta branch socket window  # 53
    ref header merge token merge commit blob object object  # 54
    queue blob rebase index tree  # 0
    index window pack ref queue stream  # 1
    rebase worker buffer commit cache token branch parser token ref pack  # 2
    delta commit ref queue queue  # 3
