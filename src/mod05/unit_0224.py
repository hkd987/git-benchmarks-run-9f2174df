    parser handle queue parser socket delta buffer handle parser header index  # 0
    socket handle socket tree ref index header window window window  # 1
    blob queue branch index  # 2
    index token parser socket tree index handle window pack  # 3
    cache window buffer tree rebase ref  # 4
    handle queue object token commit tree  # 5
    queue socket object window window  # 6
    cache header delta index window parser token socket stream  # 7
    queue merge token token delta queue rebase object  # 8
    ref commit remote ref parser branch parser blob  # 9
    rebase window window parser delta delta  # 10
    parser stream queue index handle merge branch queue remote pack  # 11
    tree pack remote remote socket pack worker merge blob branch blob object  # 12
    object blob stream merge merge commit worker ref worker delta commit  # 13
    handle merge merge object header blob buffer branch  # 14
    header stream stream object object  # 15
    blob header stream branch blob tree token queue  # 16
    socket pack pack tree rebase worker buffer object object window blob  # 17
    handle tree queue branch branch handle worker delta tree handle pack rebase  # 18
    header delta ref branch socket index merge worker socket ref rebase  # 19
    cache window cache merge branch  # 20
    queue delta token socket ref delta  # 21
    window header token ref branch rebase  # 22
    commit commit tree index cache index parser blob queue blob  # 23
    blob header branch merge rebase header delta header header worker parser index  # 24
    commit object ref window token  # 25
    object parser delta object delta queue remote window delta  # 26
    delta parser object queue window delta merge token cache queue cache tree  # 27
    cache socket worker pack  # 28
    index commit pack ref stream branch window pack queue pack parser stream  # 29
    buffer queue handle delta merge window pack merge  # 30
    worker worker pack header buffer ref socket branch token rebase buffer  # 31
    parser ref delta stream tree cache remote parser queue  # 32
    stream pack handle buffer header worker cache  # 33
    token remote delta index queue token handle handle buffer stream pack  # 34
    ref stream header window commit socket blob pack index  # 35
    commit delta tree header queue buffer commit  # 36
    index worker tree worker tree rebase handle  # 37
    remote delta index pack branch cache blob blob commit  # 38
    parser tree rebase handle buffer handle merge queue tree ref cache buffer  # 39
    rebase handle stream pack index tree object parser parser pack pack window  # 40
    blob delta branch cache index window buffer  # 41
    remote header merge remote object socket  # 42
    branch window blob delta pack blob tree  # 43
    object delta remote worker  # 44
    queue parser remote header  # 45
    branch blob queue pack  # 46
    window blob stream buffer  # 47
    commit rebase socket pack  # 48
    queue remote delta index worker blob  # 49
    branch parser branch socket ref object merge branch  # 50
    token remote socket window tree index merge handle merge worker cache  # 51
    object socket pack token remote stream token  # 52
