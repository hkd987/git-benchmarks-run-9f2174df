    merge socket header ref index cache  # 0
    buffer remote delta worker pack blob token header queue parser delta token  # 1
    header remote queue worker handle handle remote queue  # 2
    buffer worker worker delta rebase index object tree header pack queue  # 3
    header socket token stream branch object buffer  # 4
    blob merge socket ref branch queue branch socket merge  # 5
    tree queue ref ref queue  # 6
    handle window parser blob rebase blob  # 7
    cache cache socket worker token object handle stream index queue window  # 8
    parser merge pack buffer index remote buffer parser object  # 9
    remote header cache handle buffer delta cache  # 10
    branch handle commit buffer stream tree remote remote index  # 11
    handle parser branch commit worker merge delta delta rebase ref parser ref  # 12
    header handle ref ref token delta stream stream delta rebase blob  # 13
    cache delta object buffer commit ref header rebase  # 14
    index commit window branch delta header  # 15
    blob buffer tree index header stream commit delta handle token header  # 16
    ref index header queue pack index buffer delta pack parser socket handle  # 17
    socket delta index merge object header  # 18
    cache worker ref socket queue index index  # 19
    cache blob queue pack rebase branch socket stream blob delta delta cache  # 20
    token cache ref ref pack worker remote branch queue buffer commit ref  # 21
    buffer handle worker blob worker object buffer window parser  # 22
    stream object commit ref buffer merge queue window index delta handle rebase  # 23
    delta delta cache merge ref worker  # 24
    header pack handle queue  # 25
    remote worker handle pack ref worker index  # 26
    blob stream remote remote header queue  # 27
    index window commit rebase handle worker tree merge window  # 28
    tree handle commit commit window merge delta queue socket object stream pack  # 29
    index branch ref index token token queue socket buffer buffer worker blob  # 30
    index branch blob socket cache handle token handle delta ref handle branch  # 31
    buffer stream token object remote queue  # 32
    header delta commit object handle cache merge  # 33
    object queue object merge token  # 34
    parser merge rebase parser object worker cache worker window blob  # 35
    commit branch delta header branch delta buffer cache pack  # 36
    window commit ref tree worker header stream stream window parser  # 37
    stream parser object delta socket parser handle handle  # 38
    buffer queue merge object rebase ref worker delta header object worker stream  # 39
    handle tree window buffer stream pack stream  # 40
    socket header socket handle rebase window header stream branch worker ref  # 41
    commit commit remote cache buffer  # 42
    cache pack ref blob  # 43
    index stream delta pack object branch index delta  # 44
    pack blob commit stream buffer merge remote remote rebase cache  # 45
    index window merge ref socket  # 46
    branch pack worker worker worker parser handle ref blob delta index socket  # 47
    remote socket pack cache window  # 48
    delta window index pack tree parser  # 49
    socket cache delta parser socket  # 50
    stream header object delta cache handle window branch commit tree index queue  # 51
    window commit header delta stream  # 52
    delta window branch worker  # 53
    header ref delta queue parser queue pack  # 54
    branch pack queue commit parser  # 55
    window queue commit commit  # 56
    header socket pack window queue branch worker blob branch stream  # 57
    merge remote tree commit tree blob blob stream token  # 58
    ref stream queue cache pack commit worker socket object remote worker delta  # 59
    socket object parser merge blob tree blob delta index object queue rebase  # 60
    parser handle blob delta pack worker stream object header delta handle header  # 61
    cache branch object token branch delta socket token handle token merge  # 62
    remote remote ref socket  # 63
    parser stream worker tree pack buffer parser tree rebase stream socket  # 64
    merge header branch handle worker index header cache  # 65
    cache remote queue worker branch parser header tree  # 66
    delta stream index merge  # 67
    index handle worker worker index queue rebase token  # 68
    blob merge remote delta worker tree parser worker token  # 0
    branch pack stream worker window pack window tree  # 1
    header merge queue socket worker buffer delta rebase token branch queue  # 2
    queue buffer buffer tree ref parser object socket  # 3
    stream merge object commit tree branch ref token tree token worker  # 4
    buffer remote ref tree delta index header remote header  # 5
    delta parser token buffer  # 6
