    queue socket blob index cache commit ref rebase  # 0
    parser commit ref remote stream rebase queue commit  # 1
    token token header ref merge buffer handle cache object token  # 2
    rebase commit delta merge branch blob  # 3
    window remote delta rebase parser  # 4
    object socket ref worker  # 5
    header blob branch queue handle window socket  # 6
    commit window header branch delta handle  # 7
    window merge index cache rebase ref stream window  # 8
    parser ref parser buffer remote window rebase  # 9
    delta worker branch socket worker  # 10
    token cache queue stream parser handle  # 11
    buffer ref header object blob ref socket pack delta commit merge handle  # 12
    header parser rebase object ref queue branch blob  # 13
    handle socket tree header queue  # 14
    stream header buffer pack handle branch pack window  # 15
    buffer token remote ref remote token object parser handle socket pack  # 16
    worker pack tree parser blob queue window socket cache ref pack delta  # 17
    branch window tree token merge parser worker parser remote parser  # 18
    socket branch queue token object  # 19
    rebase token header blob worker queue queue branch header rebase cache worker  # 20
    socket worker rebase rebase tree delta index ref branch  # 21
    parser header socket commit token ref cache delta tree header index  # 22
    header pack object handle tree queue index cache branch tree index index  # 23
    branch branch remote merge tree parser  # 24
    ref object merge token  # 25
    remote commit token window blob handle remote  # 26
    socket branch header branch buffer header ref  # 27
    socket index token merge  # 28
    tree pack remote remote header pack window rebase  # 29
    queue stream rebase pack header index worker socket queue blob  # 30
    window commit index blob  # 31
    tree worker cache window header window ref object parser window buffer worker  # 32
    branch branch ref parser tree commit delta socket rebase delta commit delta  # 33
    blob ref window cache  # 34
    object remote blob buffer ref blob queue branch  # 35
    buffer token blob cache header worker merge  # 36
    socket commit tree header index  # 37
    parser worker parser queue header delta  # 38
