    queue rebase merge commit ref header  # 0
    socket cache rebase header rebase cache  # 1
    cache remote token ref delta handle tree delta pack cache merge  # 2
    queue tree delta object socket blob window socket  # 3
    delta queue handle window pack worker remote delta remote handle  # 4
    buffer blob worker parser tree  # 5
    parser window delta delta buffer branch rebase delta rebase  # 6
    object header worker delta handle remote parser commit merge tree  # 7
    header cache object buffer remote token  # 8
    remote buffer stream socket pack commit window object socket remote index commit  # 9
    remote ref commit token delta socket  # 10
    delta handle buffer blob index token socket  # 11
    pack parser rebase pack commit delta window object queue  # 12
    window delta merge remote branch ref  # 13
    index object remote handle parser worker header rebase object  # 14
    worker socket blob pack  # 15
    stream rebase queue window object commit blob token handle socket branch ref  # 16
    buffer commit token queue rebase merge branch remote object  # 17
    token ref worker pack handle branch commit handle  # 18
    stream stream queue socket remote queue  # 19
    cache parser stream delta tree  # 20
    window merge blob header  # 21
    socket merge worker stream handle merge  # 22
    index rebase commit stream  # 23
    rebase cache tree tree worker object rebase socket commit  # 24
    commit stream socket worker queue commit cache blob stream rebase rebase token  # 25
    parser handle socket merge  # 26
    rebase buffer commit window cache remote token  # 27
    remote handle blob window index blob cache  # 28
    remote handle branch worker handle ref object delta token parser  # 29
    handle index buffer window merge branch header delta branch tree commit branch  # 30
    header ref header ref delta  # 31
    branch pack tree handle stream merge buffer parser stream socket  # 32
    header queue parser stream branch tree handle parser ref worker  # 33
    delta buffer blob commit delta branch header  # 34
    ref pack window delta rebase delta token  # 35
    pack parser branch remote queue header delta stream worker index  # 36
    blob parser index buffer token remote buffer socket window worker window merge  # 37
    buffer ref tree blob window header handle ref  # 38
    window handle parser remote commit index socket delta tree worker  # 39
    index socket stream stream buffer rebase commit handle ref token  # 40
    handle ref index tree tree queue ref pack  # 41
    buffer blob parser ref cache stream pack handle header cache ref  # 42
    rebase stream buffer ref delta header  # 43
    parser worker parser blob merge tree branch  # 44
    header tree remote remote stream buffer pack tree cache cache delta  # 45
    blob header merge rebase cache commit stream stream delta token tree queue  # 46
    pack cache remote branch buffer  # 47
    cache commit buffer rebase token ref ref worker handle  # 48
    parser branch socket window  # 49
    header pack branch rebase blob  # 50
    commit queue token handle parser delta  # 51
    blob branch handle branch worker branch pack blob merge  # 52
    blob rebase window remote commit worker handle branch  # 53
    parser delta queue header buffer cache stream stream cache buffer  # 54
    token buffer parser merge merge socket blob header window tree window  # 55
    queue rebase header worker  # 56
    cache worker worker parser tree  # 57
    queue commit blob branch  # 58
    handle branch buffer window rebase  # 59
    socket branch socket header index pack queue buffer parser merge window  # 60
    cache ref blob header object header stream stream  # 61
    tree rebase blob cache merge token  # 62
    token delta parser rebase remote token ref parser queue window delta commit  # 63
    tree commit index handle merge  # 64
    handle delta worker window parser blob blob stream queue index cache pack  # 65
    parser window socket worker merge cache parser cache window socket handle  # 66
    pack blob header tree delta merge worker  # 67
    parser token window header remote delta  # 68
    cache branch ref socket worker remote socket tree handle  # 69
    commit queue rebase rebase blob header branch cache object window merge  # 70
