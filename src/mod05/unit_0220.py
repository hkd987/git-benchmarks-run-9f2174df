    handle object merge token  # 0
    commit rebase cache object header queue cache queue remote tree  # 1
    handle pack worker window handle  # 2
    cache socket header stream cache tree handle merge blob  # 3
    handle worker blob delta blob stream handle pack header tree socket buffer  # 4
    delta remote delta rebase object  # 5
    blob tree rebase object ref remote  # 6
    cache buffer queue remote  # 7
    window commit buffer parser ref queue  # 8
    token blob stream delta  # 9
    cache buffer remote object blob  # 10
    token worker ref header blob  # 11
    tree index tree merge parser  # 12
    token merge cache merge  # 13
    header blob object parser rebase header ref delta queue handle queue  # 14
    cache ref window object branch commit tree parser cache  # 15
    branch header worker window buffer index commit token  # 16
    buffer buffer cache parser window blob  # 17
    token index index handle  # 18
    branch header commit socket rebase queue delta  # 19
    branch merge buffer cache worker header merge worker queue parser branch  # 20
    handle index socket parser blob token worker queue  # 21
    window parser queue ref  # 22
    token parser delta buffer blob tree delta delta pack delta object  # 23
    header merge handle cache delta commit socket token rebase  # 24
    delta index object worker cache merge  # 25
    queue index stream header pack  # 26
    remote worker rebase tree tree pack buffer window queue  # 27
    remote worker header remote  # 28
    rebase object cache pack  # 29
    header commit remote cache queue tree  # 30
    header pack commit object pack branch handle  # 31
    worker commit delta branch buffer cache header commit token object queue  # 32
    worker remote socket token ref ref cache ref  # 33
    object delta pack cache token  # 34
    tree window object handle window  # 35
    merge handle queue rebase blob  # 36
    window ref parser buffer pack  # 37
    queue handle object buffer worker blob  # 38
    header index queue remote header commit window window handle branch stream window  # 39
    header stream worker merge queue merge  # 40
    worker rebase stream commit remote remote index parser commit  # 41
    delta merge ref cache header tree window header object cache pack remote  # 42
    blob index queue merge parser branch stream remote branch token queue  # 43
    branch branch cache cache header index branch remote window remote tree parser  # 44
    token blob tree handle ref  # 45
    delta remote stream ref parser  # 46
    pack delta socket merge stream blob blob pack tree commit remote delta  # 47
    header object object remote branch stream rebase queue ref commit commit stream  # 48
    queue header handle header pack  # 49
    socket ref header delta  # 50
    queue queue window merge commit  # 51
    object index merge pack token pack object pack pack stream socket ref  # 52
    delta commit delta queue tree ref commit parser cache remote  # 53
    cache remote socket commit ref  # 54
    ref delta remote stream worker token  # 55
    delta index blob parser  # 56
    pack rebase blob branch object commit remote branch commit object index merge  # 57
    ref stream object buffer tree buffer commit blob  # 58
    object tree cache object worker pack object  # 59
    parser merge merge cache token object stream  # 60
    cache worker merge queue cache index queue parser remote stream  # 0
    worker merge worker handle object socket handle index rebase object merge  # 1
    rebase queue window ref tree handle window header window  # 2
    delta token commit cache token  # 3
    header stream stream delta pack handle socket handle delta handle  # 4
    index handle branch branch index  # 0
    parser header branch rebase parser pack commit token merge branch rebase  # 1
    commit header stream commit rebase blob pack  # 2
