    cache object window merge object queue blob rebase  # 0
    blob buffer queue buffer  # 1
    header tree window ref header  # 2
    worker merge tree object parser delta parser  # 3
    header rebase rebase queue stream parser branch  # 4
    object stream rebase handle header merge stream tree window cache merge  # 5
    header index pack remote cache worker tree  # 6
    window stream cache delta pack  # 7
    branch token delta commit  # 8
    delta blob object worker delta remote tree handle socket  # 9
    token index token ref rebase object handle cache index object  # 10
    token queue branch merge  # 11
    parser parser cache ref remote rebase queue remote cache delta  # 12
    stream handle window remote commit delta blob token  # 13
    window queue commit blob delta  # 14
    cache header remote delta window cache worker parser buffer index cache queue  # 15
    commit rebase buffer index handle handle index handle parser  # 16
    tree branch index handle merge  # 17
    token ref commit rebase handle ref handle blob window window worker  # 18
    token blob rebase token remote tree socket  # 19
    stream worker socket blob index stream blob  # 20
    buffer pack commit token index pack queue  # 21
    delta cache stream socket commit stream worker stream object token  # 22
    header remote buffer parser token handle token branch index blob socket blob  # 23
    merge pack object index stream rebase parser tree cache remote  # 24
    object socket token rebase window  # 25
    handle token merge commit parser  # 26
    header cache remote index branch handle commit delta branch  # 27
    merge socket commit token pack tree handle rebase  # 28
    blob pack branch index handle  # 29
    delta blob ref branch token token header queue parser merge  # 30
    index cache commit branch header token commit remote object branch tree  # 31
    commit rebase cache worker object merge blob object branch branch  # 32
    ref branch object delta parser window pack socket handle window ref blob  # 33
    handle delta header token parser ref  # 34
    socket branch merge header object  # 35
    commit pack cache object pack ref queue header header pack queue  # 36
    pack buffer commit queue rebase handle  # 0
    rebase commit delta rebase  # 1
    delta blob merge buffer merge blob token object tree remote stream  # 2
    delta worker blob parser remote index token delta commit worker remote rebase  # 3
    handle handle merge tree buffer worker remote remote  # 4
    window rebase merge ref cache object remote stream  # 0
    cache object pack delta object tree  # 1
    index token parser buffer cache handle token token handle socket  # 2
    pack window header cache stream remote pack header buffer  # 3
    delta blob object branch token  # 4
    object pack socket buffer  # 5
    delta commit socket commit ref branch header buffer worker  # 0
    branch ref rebase parser token  # 1
    token handle branch commit  # 2
    cache handle queue index object commit  # 3
    blob delta merge worker rebase  # 4
    ref buffer branch parser blob header index delta cache pack socket  # 5
