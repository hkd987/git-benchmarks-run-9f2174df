    parser header queue branch parser worker  # 0
    object buffer rebase worker pack worker blob index  # 1
    worker header stream header  # 2
    commit commit pack index merge tree stream index  # 3
    socket merge cache queue header window pack delta pack cache  # 4
    tree socket branch object  # 5
    queue queue handle handle index pack token index socket branch socket  # 6
    handle queue rebase branch remote cache object  # 7
    header parser handle cache pack buffer index socket handle blob  # 8
    delta branch buffer buffer queue branch socket parser  # 9
    handle handle merge buffer queue  # 10
    remote ref token branch token  # 11
    window ref stream queue delta parser cache merge  # 12
    window commit window buffer commit  # 13
    cache cache commit branch socket pack object delta branch  # 14
    header pack header delta rebase delta commit  # 15
    merge pack worker pack header tree  # 16
    delta socket stream header tree remote socket worker parser rebase header  # 17
    parser pack remote remote merge blob cache window buffer  # 18
    buffer worker pack token window  # 19
    ref blob object socket blob buffer blob  # 20
    cache blob worker socket header buffer  # 21
    ref pack parser index branch  # 22
    window remote pack index  # 23
    merge handle handle stream merge window parser delta ref cache delta pack  # 24
    pack ref ref object tree parser branch queue  # 25
    commit buffer rebase token index  # 26
    delta socket worker handle window rebase stream buffer branch cache object worker  # 27
    cache token tree merge buffer parser  # 28
    token worker handle parser worker commit queue merge  # 29
    queue handle handle tree header branch socket remote window  # 30
    socket tree buffer delta token blob  # 31
    header handle buffer window  # 32
    window token token header object tree header parser parser rebase header object  # 33
    queue queue merge pack index token header stream window pack worker token  # 34
    ref commit buffer index  # 35
    parser merge ref window cache pack stream worker  # 36
    object commit ref window window parser blob rebase buffer  # 37
    header tree delta token blob pack header ref merge  # 38
    header remote blob parser commit  # 39
    window token index queue rebase blob object tree window index  # 40
    cache commit cache stream index  # 41
    buffer commit worker index blob delta token blob queue window tree header  # 42
    pack ref object blob  # 43
    object window pack header delta  # 44
    delta merge merge merge blob header branch socket  # 45
    token buffer handle tree window pack delta window  # 46
    pack buffer handle token stream pack worker object tree remote  # 47
    stream parser delta window parser branch queue  # 48
    socket branch tree cache queue tree stream worker handle delta  # 49
    cache branch branch ref socket header commit  # 50
    window object tree tree socket stream parser  # 51
    object header handle cache delta  # 52
    buffer stream parser parser  # 53
    socket ref tree merge buffer commit buffer parser  # 54
    ref worker cache buffer commit object blob stream remote window object worker  # 0
    remote delta parser handle object delta buffer  # 1
    header pack merge branch parser  # 2
    header object cache cache  # 3
    buffer remote header blob index object  # 4
    handle delta rebase object buffer  # 5
    merge index blob commit  # 6
    blob tree parser index window blob parser stream worker rebase  # 7
