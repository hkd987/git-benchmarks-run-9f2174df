    commit object worker cache rebase window worker merge tree queue index branch  # 0
    tree pack ref handle merge rebase  # 1
    commit delta window socket stream object tree  # 2
    ref cache worker handle delta socket merge buffer stream rebase  # 3
    parser merge pack parser parser  # 4
    rebase remote handle remote queue queue buffer parser ref  # 5
    tree handle branch worker remote tree buffer worker handle cache rebase socket  # 6
    queue token worker object object delta tree queue window token worker  # 7
    ref rebase stream blob index commit handle handle parser object blob  # 8
    remote index window socket worker token  # 9
    ref remote index queue window buffer stream token index merge stream object  # 10
    blob remote branch merge tree parser window blob buffer ref parser index  # 11
    delta commit branch branch ref object delta header token cache buffer  # 12
    window buffer rebase ref cache index ref stream  # 13
    tree header remote queue ref delta  # 14
    ref stream worker ref worker worker tree socket  # 15
    socket buffer buffer window ref  # 16
    remote blob ref index handle pack handle ref header branch commit header  # 17
    window header handle pack merge rebase cache blob worker buffer ref  # 18
    window buffer branch remote  # 19
    object commit token stream pack object cache ref pack rebase header  # 20
    worker worker header delta header header  # 21
    blob token branch header blob parser ref index window  # 22
    token worker rebase stream socket object blob commit parser branch merge  # 23
    rebase object ref window rebase stream merge commit stream  # 24
    index blob commit window  # 25
    parser header queue parser object delta commit blob parser header  # 26
    stream index ref blob handle queue buffer parser buffer index window  # 27
    cache commit commit worker cache buffer rebase merge buffer index header  # 28
    buffer socket stream delta commit delta blob tree worker  # 29
    socket header token remote stream stream window  # 30
    worker socket rebase object token queue pack buffer index pack  # 31
    remote pack ref cache queue merge tree object tree pack worker  # 32
    parser merge header ref socket worker header  # 33
    tree stream socket commit token commit branch  # 34
    stream window handle worker token header stream remote queue  # 35
    queue token handle branch remote  # 36
    index ref commit header object window  # 37
    tree socket worker index parser merge rebase  # 38
    commit header parser object cache  # 39
    parser commit stream parser merge ref buffer merge parser remote index header  # 40
    blob queue worker worker index worker socket  # 41
    handle window merge index branch header delta remote branch handle  # 42
    window ref header pack commit cache window  # 43
    branch buffer window remote object window header header pack  # 44
    stream worker window commit delta parser commit commit ref  # 45
    window window cache header  # 46
