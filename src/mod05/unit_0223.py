    pack pack queue parser commit pack merge  # 0
    worker ref branch pack header remote stream parser socket  # 1
    parser index ref merge worker tree index merge commit ref index parser  # 2
    object delta header pack rebase delta  # 3
    merge cache queue buffer header header remote commit cache  # 4
    cache buffer merge object cache queue merge tree commit header  # 5
    index remote window cache header socket delta  # 6
    ref window buffer blob socket  # 7
    branch header pack window worker handle handle token branch  # 8
    delta object ref delta  # 9
    queue socket object blob buffer delta  # 10
    cache pack object blob window index merge  # 11
    window commit window window worker  # 12
    token buffer cache window token pack cache token stream token  # 13
    rebase queue handle object  # 14
    header worker parser queue blob stream queue  # 15
    cache index handle worker rebase rebase parser merge commit remote  # 16
    parser cache delta window token token  # 17
    commit tree branch socket tree stream object  # 18
    branch stream token window ref buffer  # 19
    delta remote token queue window handle header index parser socket  # 20
    rebase window commit blob  # 21
    commit delta socket window pack  # 22
    pack ref token queue blob token  # 23
    handle remote parser buffer stream  # 24
    stream remote remote buffer window handle cache cache cache worker  # 25
    buffer header index branch remote window object worker branch  # 26
    rebase socket queue branch worker window index  # 27
    object merge delta queue ref buffer stream rebase  # 28
    handle stream object parser token object parser ref blob  # 29
    delta commit commit queue handle  # 30
    socket commit rebase delta parser  # 31
    cache branch delta window header socket pack queue stream queue object  # 32
    stream stream pack index commit delta rebase rebase remote queue parser  # 33
    ref handle branch token branch cache parser pack  # 34
    buffer branch tree header index branch rebase object tree delta tree handle  # 35
    stream queue header rebase socket merge  # 36
    pack queue pack handle cache queue ref buffer  # 37
    rebase handle commit object branch worker pack object  # 38
    rebase delta worker remote parser index cache socket worker stream token  # 39
    object remote pack stream commit merge index ref stream worker window  # 40
    window tree merge parser  # 41
    rebase branch buffer blob pack cache blob rebase merge queue  # 42
    header ref header branch commit object blob parser  # 43
    pack handle delta socket parser window pack commit  # 44
    pack worker delta worker rebase tree blob socket remote  # 45
    delta index stream delta socket ref tree rebase header delta window worker  # 46
    token tree handle ref merge handle ref merge buffer  # 47
    stream handle object window commit index object  # 48
    tree worker index rebase  # 49
    tree queue remote tree window  # 50
    ref branch branch blob header buffer object commit buffer  # 51
    merge queue handle index object  # 52
    window object blob index remote handle merge rebase blob ref delta buffer  # 53
    remote delta rebase window  # 54
    blob worker merge branch index object blob parser rebase branch  # 55
    cache remote cache blob merge delta  # 56
    merge commit token index queue parser merge stream  # 57
    pack ref merge commit  # 58
    parser handle merge cache socket branch  # 59
    commit handle ref ref commit ref handle delta  # 60
    queue index buffer window token queue commit queue object  # 61
    handle tree parser stream queue queue tree index buffer pack  # 62
    branch stream branch object queue delta handle pack remote commit delta  # 63
    object worker cache commit socket branch ref window queue queue cache  # 64
    index index blob object ref delta socket object rebase  # 65
    pack buffer stream pack handle remote blob  # 66
    parser pack header socket socket header handle ref commit window branch stream  # 67
    window queue window rebase remote delta parser blob ref cache  # 68
    branch rebase worker token  # 69
    stream worker stream delta stream handle rebase buffer object  # 70
    remote socket index handle delta  # 71
    queue ref stream token buffer index commit index socket pack tree  # 72
    branch socket remote commit pack cache cache remote blob pack merge commit  # 73
    handle buffer rebase buffer commit  # 74
    delta tree handle stream commit window buffer remote worker commit  # 75
    token branch blob merge handle branch token  # 76
    cache merge branch ref queue stream parser handle socket object stream  # 77
    remote stream blob header commit  # 78
