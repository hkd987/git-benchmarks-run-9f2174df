    worker delta commit buffer worker merge stream  # 0
    token commit remote buffer worker window pack commit socket tree pack pack  # 1
    queue parser socket queue stream stream worker cache  # 2
    tree delta cache rebase stream window branch index  # 3
    branch blob queue worker buffer merge rebase commit token delta ref pack  # 4
    stream remote index tree remote window window header  # 5
    blob object header rebase commit remote token parser  # 6
    window cache ref token commit  # 7
    queue parser window queue merge header merge ref index socket ref stream  # 8
    remote delta branch ref  # 9
    cache stream parser branch  # 10
    socket stream handle commit buffer delta header buffer object merge  # 11
    cache object buffer pack blob branch ref ref parser  # 12
    blob queue socket rebase handle window cache token object  # 13
    merge blob worker handle object token blob worker tree merge  # 14
    handle ref socket rebase commit  # 15
    queue delta blob socket  # 16
    index cache rebase header stream remote  # 17
    queue socket pack socket  # 18
    remote header stream parser  # 19
    branch object handle cache delta branch merge buffer socket buffer  # 20
    queue pack worker buffer merge queue token tree  # 21
    buffer stream window token  # 22
    header pack object worker rebase token parser queue  # 23
    header token object buffer token header rebase cache delta remote  # 24
    tree delta blob token parser remote socket commit socket token  # 25
    token worker parser blob cache blob tree  # 26
    queue socket stream stream parser merge window branch ref  # 27
    stream stream socket branch remote  # 28
    delta delta socket buffer delta branch rebase cache  # 29
    parser object header tree remote  # 30
    rebase rebase header blob ref worker queue socket cache commit  # 31
    buffer stream handle handle ref  # 32
    stream tree stream parser object  # 33
    tree index merge window  # 34
    token blob merge blob pack  # 35
    index window header object tree remote  # 36
    object worker tree queue rebase header token  # 37
    index rebase cache object delta stream parser parser merge queue  # 38
    socket ref remote tree  # 39
    merge token token parser commit token rebase object  # 40
    socket pack index rebase stream  # 41
    object delta object delta blob branch  # 42
    merge pack queue commit pack branch branch socket handle window object  # 43
    queue delta window branch object blob handle object window branch token rebase  # 44
    cache token delta socket worker ref object cache cache remote ref  # 45
    commit index socket cache tree object commit delta buffer stream  # 46
    tree parser delta delta  # 47
    merge buffer delta token cache commit queue object tree  # 48
    pack blob worker queue merge object parser  # 49
    handle commit socket queue tree worker ref cache  # 50
    cache commit window branch pack cache parser tree ref parser header blob  # 51
    queue buffer rebase pack branch token  # 52
    commit queue tree branch cache buffer branch index  # 53
    pack handle stream socket commit socket cache index pack worker stream parser  # 54
    remote tree delta delta worker delta  # 55
    rebase stream queue handle worker stream buffer merge parser stream blob  # 56
    commit queue object pack header cache  # 57
    buffer object buffer stream merge blob ref merge worker queue object commit  # 58
    blob queue ref worker  # 59
    cache cache cache object pack remote queue blob  # 0
    socket parser stream header merge  # 1
    window worker ref handle handle pack worker token header header queue worker  # 2
    index tree header window branch handle  # 3
    parser blob queue token delta token  # 4
    pack handle commit handle  # 5
