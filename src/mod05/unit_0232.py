    header merge ref rebase  # 0
    branch stream tree ref remote index delta queue index  # 1
    header ref commit token  # 2
    pack branch token commit cache buffer  # 3
    rebase rebase worker handle delta token blob parser ref rebase rebase  # 4
    header parser blob pack blob remote merge cache parser object buffer header  # 5
    branch cache parser delta index  # 6
    buffer index branch window window parser  # 7
    cache merge parser parser cache index window token socket parser parser pack  # 8
    delta token handle handle socket handle cache remote branch ref handle  # 9
    blob remote index tree window remote window worker buffer handle  # 10
    socket commit delta cache queue blob queue socket cache branch header  # 11
    queue window handle ref branch blob  # 12
    token window pack stream object pack queue branch worker delta blob worker  # 13
    token ref buffer delta window merge cache merge stream handle blob  # 14
    ref remote socket stream  # 15
    parser object rebase blob ref rebase buffer branch header  # 16
    ref socket merge commit cache worker tree  # 17
    stream remote window merge commit delta worker queue  # 18
    socket socket merge object  # 19
    buffer handle index branch branch queue  # 20
    handle buffer pack rebase cache queue pack parser header delta stream  # 21
    queue worker window branch socket branch parser object stream merge  # 22
    cache blob delta queue stream token branch socket blob buffer blob ref  # 23
    socket delta token buffer pack  # 24
    handle merge parser commit tree tree rebase token token tree  # 25
    queue stream cache worker queue merge remote  # 26
    object tree pack blob parser  # 27
    index pack worker socket ref stream  # 28
    index branch ref branch merge worker rebase delta  # 29
    ref token cache handle index buffer header delta cache  # 30
    socket commit ref cache pack  # 31
    worker socket tree buffer socket  # 32
    tree object worker object header  # 33
    commit pack blob object cache stream queue  # 34
    branch rebase merge rebase handle merge pack commit ref queue remote object  # 35
    pack index commit merge  # 36
    delta ref object pack worker worker delta parser  # 37
    rebase token token parser tree socket  # 38
    header handle cache ref branch merge  # 39
