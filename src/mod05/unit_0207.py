    object merge remote rebase  # 0
    handle ref header tree pack header index cache cache pack  # 1
    socket merge socket commit header  # 2
    worker branch tree ref blob merge buffer socket queue  # 3
    rebase blob tree parser cache  # 4
    blob branch ref window queue object merge branch queue ref  # 5
    stream handle tree window worker index header window blob tree stream header  # 6
    merge branch handle token parser branch parser parser blob stream socket merge  # 7
    socket delta merge stream remote rebase remote ref commit commit object cache  # 8
    socket rebase worker cache delta cache pack parser branch header merge blob  # 9
    queue stream window commit delta object delta buffer token  # 10
    remote object pack worker pack rebase socket commit  # 11
    object cache ref header buffer ref delta  # 12
    delta merge handle ref socket delta socket token ref socket stream rebase  # 13
    merge header worker object window stream index tree ref blob  # 14
    delta stream window queue ref  # 15
    remote merge header blob buffer remote  # 16
    worker merge tree pack tree tree rebase worker header worker delta cache  # 17
    object blob stream merge socket token stream remote branch token buffer  # 18
    header index window rebase buffer blob stream remote stream parser worker ref  # 19
    blob index socket pack ref merge cache  # 20
    handle branch pack pack commit socket rebase buffer worker cache ref  # 21
    token window buffer cache index buffer cache blob  # 22
    cache commit cache index blob rebase  # 23
    ref socket branch ref blob worker cache blob delta buffer rebase pack  # 24
    merge merge branch index commit merge index handle branch window window tree  # 25
    branch parser handle token tree rebase commit pack tree  # 26
    token pack branch merge  # 27
    ref pack pack remote queue delta parser stream  # 28
    cache blob branch branch delta  # 29
    ref index commit delta ref handle cache ref blob parser parser remote  # 30
    window merge cache parser header merge ref cache  # 31
    window delta header rebase socket window tree socket rebase  # 32
    handle pack header stream  # 33
    commit worker branch pack tree  # 34
    queue remote handle buffer worker queue handle remote worker commit delta handle  # 35
    commit remote queue tree  # 36
    parser object remote queue  # 37
    remote blob delta queue ref header header buffer delta remote stream worker  # 38
    token index remote merge header blob blob remote ref blob commit socket  # 39
    pack merge token pack blob index header header index  # 40
    pack socket delta pack index parser stream  # 41
    parser worker tree window blob remote header  # 42
    window delta tree worker stream tree parser queue branch  # 43
