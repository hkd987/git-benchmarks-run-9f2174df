    pack ref merge tree rebase branch socket token  # 0
    pack stream buffer delta pack handle tree parser blob parser blob  # 1
    window rebase ref rebase merge rebase blob remote blob  # 2
    socket object socket window index cache index token token socket blob index  # 3
    branch cache parser worker  # 4
    commit pack index worker remote branch rebase commit  # 5
    ref worker object stream rebase window commit merge parser queue window  # 6
    socket rebase ref merge  # 7
    commit worker blob cache  # 8
    socket object ref rebase delta window buffer ref object socket window stream  # 9
    buffer object socket rebase ref cache  # 10
    worker queue blob socket pack parser handle  # 11
    rebase cache object delta handle object commit stream socket remote rebase parser  # 12
    rebase blob parser queue  # 13
    token delta tree delta delta  # 14
    stream blob buffer cache remote branch  # 15
    buffer cache worker object handle ref index blob remote parser object  # 16
    parser worker ref handle parser index worker parser remote  # 17
    buffer tree cache index stream tree rebase buffer index window window  # 18
    parser socket tree stream socket cache token  # 19
    header worker blob index token header  # 20
    parser buffer remote stream token worker buffer blob handle cache worker  # 21
    stream pack pack window pack queue worker  # 22
    tree branch delta socket stream cache worker blob pack  # 23
    delta queue commit cache header object  # 24
    merge stream remote merge tree pack  # 25
    ref parser cache commit commit tree delta pack branch blob index  # 26
    tree index index ref parser ref buffer delta merge cache  # 27
    window worker handle index tree object pack handle remote header  # 28
    token rebase ref worker token worker buffer tree ref header  # 29
    delta buffer socket cache socket worker branch  # 30
    index tree parser merge commit rebase remote remote stream commit  # 31
    parser stream index pack tree  # 32
    stream handle remote buffer stream blob object  # 33
    buffer tree tree ref commit stream tree  # 34
    index handle index header window tree rebase index remote branch  # 35
    blob handle blob token handle  # 36
    remote queue merge object delta parser object  # 37
    queue rebase delta buffer remote tree merge index cache window queue parser  # 38
    branch parser token buffer buffer parser index header rebase token worker  # 39
    parser queue buffer ref remote ref delta pack  # 40
    cache header ref blob socket handle parser  # 41
    blob object window parser worker parser window header  # 42
    ref object pack remote remote queue index handle commit ref rebase remote  # 43
    socket object branch blob cache token remote window handle index worker buffer  # 44
    index commit tree remote object tree  # 45
    object object tree branch worker worker socket buffer  # 46
    commit socket blob handle stream  # 47
    parser merge merge header handle pack worker rebase ref window socket  # 48
