    window handle merge blob delta  # 0
    header parser merge header merge rebase worker object ref window  # 1
    branch header blob worker handle parser merge cache cache  # 2
    stream remote index delta index blob rebase  # 3
    token buffer remote object pack header object index  # 4
    worker object tree stream ref stream  # 5
    object buffer worker rebase branch branch worker  # 6
    ref cache branch parser header socket buffer pack handle blob merge  # 7
    stream ref object cache  # 8
    parser cache object handle merge cache ref remote socket index cache header  # 9
    header branch merge window header delta  # 10
    object remote rebase tree delta pack rebase stream rebase handle  # 11
    pack window index ref  # 12
    stream index header commit handle object worker socket  # 13
    stream buffer buffer ref commit object token queue socket pack blob  # 14
    rebase object worker header object tree rebase header pack commit  # 15
    commit window stream header pack rebase worker tree socket  # 16
    branch stream worker commit buffer parser cache tree  # 17
    tree cache header header socket branch cache  # 18
    index token commit remote token token  # 19
    ref branch cache queue parser index branch socket delta commit rebase stream  # 20
    pack pack stream ref  # 21
    buffer delta buffer tree tree stream tree window window blob  # 22
    remote window header socket index socket window token queue blob queue index  # 23
    blob pack object commit pack branch stream  # 24
    handle stream delta header branch handle  # 25
    socket blob socket rebase stream handle  # 26
    index handle delta object delta queue handle parser handle branch parser  # 27
    blob merge merge commit buffer token  # 28
    window header cache rebase buffer stream delta handle branch socket worker  # 29
    cache token pack merge rebase socket window delta branch  # 30
    delta parser worker index worker tree worker socket cache pack parser  # 31
    buffer tree index window token remote window remote tree branch tree ref  # 32
    blob tree parser worker window remote socket ref socket pack worker worker  # 33
    branch merge pack worker ref cache  # 34
    tree socket tree queue remote  # 35
    parser parser commit rebase parser worker remote  # 36
    worker object token worker branch socket worker buffer parser index header blob  # 37
    object cache ref branch branch ref token tree delta socket  # 38
    merge pack window stream buffer  # 39
    header index socket ref stream index branch blob object tree  # 40
    buffer merge token commit pack header  # 41
    commit header stream pack index pack buffer header blob rebase handle  # 42
    handle handle tree ref queue pack delta commit  # 43
    buffer header worker worker worker rebase merge  # 44
    commit pack token index  # 45
