    parser merge ref tree remote worker object remote remote header ref  # 0
    pack merge index branch branch  # 1
    cache tree rebase pack  # 2
    index stream socket blob handle branch  # 3
    rebase ref parser object merge queue header ref rebase  # 4
    queue window buffer ref commit tree worker queue socket remote buffer  # 5
    socket token remote delta object window worker  # 6
    tree token ref blob delta socket pack worker  # 7
    stream parser blob queue buffer worker stream ref header tree window  # 8
    delta remote index worker  # 9
    rebase parser header blob ref  # 10
    socket handle handle token index branch buffer blob delta handle merge delta  # 11
    handle delta blob commit pack merge worker parser header branch tree  # 12
    pack tree ref tree cache merge ref pack parser  # 13
    index buffer parser parser pack blob stream  # 14
    header commit index parser handle object  # 15
    header handle stream stream worker parser worker delta token cache window  # 16
    rebase rebase window merge pack header branch worker branch commit  # 17
    rebase commit handle commit remote branch  # 18
    token branch worker remote parser rebase handle blob  # 19
    pack branch handle remote object tree  # 20
    window commit stream token branch merge branch commit commit parser commit object  # 21
    window header blob rebase index index queue remote  # 22
    remote handle index stream delta delta parser tree delta tree parser  # 23
    handle cache remote header cache socket pack window header pack blob tree  # 24
    pack commit merge buffer  # 25
    cache remote queue buffer index  # 26
    buffer tree delta merge  # 27
    window tree handle merge remote  # 28
    window header index socket object  # 29
    branch blob queue delta stream delta  # 30
    branch socket delta rebase tree window commit window handle handle  # 31
    merge branch token rebase window commit cache socket merge  # 32
    handle header commit token worker buffer ref index object token merge  # 33
    remote ref header merge token  # 34
    blob handle worker rebase handle parser worker object buffer pack worker  # 35
    ref blob remote commit branch object worker rebase  # 36
    parser object cache cache merge blob worker  # 37
    parser delta remote parser buffer  # 38
    object delta parser stream buffer window queue handle  # 39
    window cache merge window  # 40
    delta token buffer buffer  # 41
    handle buffer header cache window branch  # 42
    pack window cache tree object delta tree  # 43
    worker queue ref header ref queue window commit  # 44
    remote header window remote merge branch merge  # 45
    commit stream buffer pack buffer commit token ref ref pack index  # 46
    ref index remote window index delta worker ref ref window handle  # 47
    ref remote window stream cache remote stream delta  # 48
    tree rebase rebase tree handle rebase delta ref  # 49
    pack rebase buffer handle  # 50
    rebase cache rebase token stream  # 51
    cache delta index worker ref merge  # 52
    window rebase queue window buffer  # 53
    buffer token parser token merge ref worker cache window parser  # 54
    parser blob parser rebase ref  # 55
    ref header socket branch queue socket rebase socket stream  # 56
    object token delta ref remote pack merge cache socket  # 57
    buffer buffer window parser remote window remote commit window branch queue  # 58
    commit socket buffer blob parser blob pack cache object parser tree cache  # 59
    ref worker merge rebase token index window  # 60
    token rebase header stream commit remote object worker handle rebase  # 61
    object rebase cache delta  # 62
    ref merge window rebase pack token delta cache pack socket  # 63
    rebase parser index ref commit queue stream parser queue header queue branch  # 64
    branch socket tree token  # 65
    ref delta socket stream token  # 66
    cache tree object header pack window remote  # 67
    object remote merge tree window cache delta worker  # 68
    queue blob window stream handle buffer blob index index parser branch delta  # 69
