    handle commit object token parser buffer index window pack  # 0
    window remote blob ref branch tree pack branch window  # 1
    merge ref tree worker  # 2
    merge rebase index tree delta ref header remote pack blob  # 3
    remote pack branch branch buffer token index window window branch commit  # 4
    ref ref remote queue stream  # 5
    cache blob blob index branch buffer  # 6
    stream rebase token queue stream cache object worker  # 7
    cache tree socket rebase buffer token handle pack commit pack  # 8
    tree delta object handle handle buffer commit commit token blob branch  # 9
    window remote blob cache cache  # 10
    token socket parser object cache socket socket token pack stream tree  # 11
    queue stream ref object delta remote window worker handle header rebase socket  # 12
    handle object cache index queue stream  # 13
    blob buffer worker queue stream  # 14
    queue remote commit merge blob rebase stream rebase header  # 15
    handle commit cache merge index index worker rebase  # 16
    ref worker socket rebase stream blob pack handle  # 17
    queue ref token remote parser  # 18
    stream handle merge socket parser pack pack queue  # 19
    rebase delta parser remote index delta window queue window buffer merge stream  # 20
    header index parser object pack branch blob  # 21
    parser remote index window tree object stream ref ref token  # 22
    merge cache commit cache rebase branch token token delta parser commit  # 23
    handle merge worker queue object  # 24
    parser handle branch delta stream pack  # 25
    merge delta remote window commit pack socket stream  # 26
    branch header delta worker ref commit pack  # 27
    rebase delta stream parser delta branch  # 28
    stream handle remote pack worker parser merge  # 29
    pack index ref socket branch blob tree object rebase  # 30
    socket token blob remote window commit  # 31
    pack rebase worker merge branch remote  # 32
    buffer branch commit ref parser branch delta socket  # 33
    object handle queue object stream rebase  # 34
    object object parser pack cache handle token rebase pack  # 35
    remote rebase parser header token  # 36
    pack commit stream commit stream cache ref ref socket blob handle remote  # 37
    socket delta header buffer branch token socket cache object  # 38
    branch cache handle object parser rebase queue header branch commit rebase  # 39
    stream handle header handle handle window  # 40
    tree header handle delta token remote window cache rebase handle stream  # 41
    remote stream cache rebase token branch socket buffer window token window tree  # 42
    token queue token merge rebase  # 43
