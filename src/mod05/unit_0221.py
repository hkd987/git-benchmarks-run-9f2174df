    object parser delta delta tree worker socket parser tree  # 0
    blob index handle worker socket  # 1
    queue worker commit buffer buffer  # 2
    socket queue cache rebase  # 3
    pack parser commit pack  # 4
    branch socket merge worker  # 5
    index window object merge pack ref token remote delta window socket  # 6
    merge parser worker index  # 7
    worker blob index buffer token worker  # 8
    token ref buffer handle stream delta buffer worker worker ref  # 9
    worker branch merge index  # 10
    queue window commit index blob rebase remote socket socket worker buffer  # 11
    header merge token tree buffer worker object  # 12
    merge socket stream branch blob worker blob worker pack  # 13
    index queue rebase window socket object remote index stream branch  # 14
    worker remote object token remote  # 15
    cache branch parser branch remote remote remote  # 16
    handle rebase blob token blob object remote worker blob cache  # 17
    merge remote worker remote pack  # 18
    cache remote parser socket index rebase cache tree branch  # 19
    handle window header object worker blob buffer cache tree pack  # 20
    blob rebase pack rebase branch token worker remote token  # 21
    delta commit worker rebase worker handle cache tree  # 22
    branch index stream worker  # 23
    window parser stream parser parser cache buffer blob tree index  # 24
    worker delta stream window token worker  # 25
    merge merge window blob ref blob socket tree  # 26
    rebase token parser object  # 27
    stream ref tree index tree pack index socket  # 28
    queue worker cache object  # 29
    cache merge remote delta pack buffer tree socket  # 30
    rebase token socket parser branch window pack delta buffer tree index  # 31
    worker token header parser branch branch remote buffer cache  # 32
    delta delta pack token window handle tree index header  # 33
    ref worker socket cache remote delta worker ref branch object  # 34
    delta branch socket header rebase stream rebase branch header object window rebase  # 35
    header cache merge parser token tree commit cache object  # 36
    commit worker handle tree delta stream cache  # 37
    merge pack branch merge parser tree  # 38
    branch blob token ref buffer window  # 39
    rebase worker object object queue buffer window stream blob object tree  # 40
    pack socket cache tree  # 41
    blob object index commit window remote queue pack index branch merge blob  # 42
    delta branch worker pack pack  # 43
    token cache remote rebase index pack buffer commit ref index index  # 44
    handle stream queue tree worker worker window commit window socket stream  # 45
    worker index socket token token cache token index buffer handle  # 46
    rebase socket queue socket remote parser merge index object rebase  # 47
    cache branch branch tree pack delta queue worker  # 48
    remote stream object merge remote window ref buffer ref header parser  # 49
    branch blob rebase window socket handle merge stream ref rebase merge tree  # 50
    pack buffer token merge worker delta branch ref  # 51
    handle tree merge branch token branch tree worker  # 52
