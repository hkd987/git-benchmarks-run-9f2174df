    window window remote header buffer index handle remote  # 0
    rebase remote object rebase parser merge ref ref handle branch delta object  # 1
    worker worker remote commit token branch branch branch branch  # 2
    socket buffer worker queue handle index index object tree parser  # 3
    window object commit delta buffer remote index pack socket rebase  # 4
    ref header buffer object stream window branch tree branch delta  # 5
    rebase worker ref ref worker  # 6
    blob ref parser worker handle stream stream header token  # 7
    socket blob delta pack rebase ref rebase index  # 8
    socket header rebase cache buffer header socket socket queue window  # 9
    window buffer worker cache rebase parser buffer rebase cache object  # 10
    queue queue stream delta branch index queue blob rebase  # 11
    header index blob blob remote remote commit remote token socket rebase delta  # 12
    branch remote queue stream stream header queue object delta ref queue  # 13
    handle stream handle token merge window socket merge  # 14
    commit blob delta rebase blob  # 15
    branch header index parser merge stream index queue index pack  # 16
    blob worker tree merge handle header queue index parser ref rebase pack  # 17
    remote buffer worker blob pack queue socket delta ref handle object  # 18
    buffer object ref worker ref remote index header ref  # 19
    socket blob merge window branch token header pack remote parser  # 20
    pack token pack socket header parser window queue blob socket delta  # 21
    parser stream cache window commit commit tree  # 22
    blob tree pack handle delta tree branch branch  # 23
    commit queue tree index commit handle rebase  # 24
    merge merge index parser  # 25
    index branch delta parser object index merge object pack  # 26
    stream worker commit pack blob index socket token buffer rebase buffer  # 27
    delta pack header pack pack  # 28
    queue worker branch merge stream cache pack remote  # 29
    ref merge merge ref object remote queue rebase index ref  # 30
    object pack commit commit  # 31
    index window cache branch rebase pack buffer  # 32
    index branch index tree window merge tree  # 33
    pack delta socket tree header  # 34
    parser queue stream ref token ref parser branch  # 35
    delta index object handle cache branch rebase parser token  # 36
    branch commit merge parser branch delta buffer socket merge branch handle  # 37
    buffer commit header blob header socket  # 38
    tree index stream handle remote queue blob queue tree  # 39
    remote stream tree queue branch object ref handle  # 40
    queue tree remote tree stream stream branch tree  # 41
    header token ref parser commit rebase commit worker parser  # 42
    tree queue worker header handle object index blob index  # 43
    socket parser ref tree blob queue header rebase buffer  # 44
    stream object window rebase buffer stream tree stream header tree  # 45
    stream handle rebase pack worker merge  # 46
    buffer worker socket handle delta  # 47
    parser merge parser handle socket window commit ref delta parser  # 48
    worker rebase delta token branch socket tree  # 49
    ref queue rebase socket  # 50
    merge tree index worker socket index buffer stream remote pack  # 51
    buffer socket queue rebase branch ref  # 52
    pack header object ref  # 53
    socket remote pack delta object stream remote stream  # 54
    cache cache token ref queue  # 55
    cache object commit rebase pack ref stream  # 56
    pack remote header header remote  # 57
    ref blob parser worker merge window index stream remote window handle rebase  # 58
    remote pack branch stream merge worker window handle  # 59
    object ref parser window remote parser blob ref socket  # 60
    worker window header window object  # 61
    socket commit object cache header blob rebase queue branch  # 62
    token blob remote tree tree header worker  # 63
    merge socket blob object commit object blob parser blob pack  # 64
    cache index token cache object object blob cache handle stream socket  # 65
    handle rebase window object token token header index  # 66
    token merge cache ref rebase commit buffer  # 67
    blob remote pack rebase commit  # 68
    worker handle buffer handle socket blob socket pack object handle  # 69
    window branch remote token parser blob ref stream cache queue remote delta  # 70
    ref commit header pack pack handle  # 71
    branch parser socket merge  # 72
    rebase socket merge parser queue header  # 73
    worker parser ref socket commit handle rebase window  # 74
    object socket commit object parser index queue  # 75
    branch rebase tree branch header index buffer worker rebase index rebase index  # 76
    branch commit tree cache branch token  # 77
    delta rebase socket index tree ref blob worker parser socket tree index  # 78
    handle worker buffer index buffer blob ref stream rebase  # 79
    token delta stream merge parser token  # 80
    socket worker object header stream queue  # 81
    header delta ref cache parser index header branch  # 82
    queue token window buffer token rebase rebase stream index blob  # 83
    token worker parser remote remote worker rebase commit ref delta socket  # 84
    parser stream rebase ref tree stream rebase  # 85
