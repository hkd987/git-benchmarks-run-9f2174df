    header token worker index blob ref stream  # 0
    merge commit delta stream socket object  # 1
    stream token commit delta pack stream  # 2
    window merge worker queue branch  # 3
    socket header stream handle  # 4
    header window tree worker parser pack buffer  # 5
    parser index blob cache queue worker window header tree branch handle rebase  # 6
    commit buffer rebase rebase cache buffer index socket  # 7
    token object pack object pack socket  # 8
    blob index ref handle blob tree blob object handle queue remote  # 9
    rebase socket commit delta window delta blob  # 10
    header ref merge buffer socket rebase pack  # 11
    ref header commit remote cache worker handle stream  # 12
    pack index header pack handle  # 13
    remote blob tree header commit tree  # 14
    merge cache ref remote rebase remote cache object  # 15
    stream parser header window rebase remote handle  # 16
    ref queue commit cache  # 17
    handle cache cache parser blob commit  # 18
    queue handle parser queue window delta window  # 19
    tree branch stream rebase handle  # 20
    pack handle tree worker  # 21
    ref pack stream blob commit worker buffer queue window  # 22
    delta header stream token tree window blob stream blob merge stream  # 23
    socket header pack socket branch cache cache  # 24
    commit queue rebase merge pack  # 25
    token buffer parser cache tree merge stream commit cache delta queue blob  # 26
    queue ref object merge tree  # 27
    parser parser ref queue delta  # 28
    handle buffer index socket cache buffer window index rebase  # 29
    header merge commit worker parser cache remote queue remote blob  # 30
