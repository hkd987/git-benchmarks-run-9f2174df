    rebase socket queue rebase buffer token object delta  # 0
    buffer merge parser commit branch remote cache index buffer ref  # 1
    rebase parser buffer queue handle rebase branch socket delta ref branch  # 2
    tree socket ref commit merge queue window  # 3
    branch index stream socket cache object merge  # 4
    merge ref token index worker  # 5
    ref queue header handle pack header header handle worker token rebase socket  # 6
    index rebase merge remote tree index queue index rebase window  # 7
    socket index tree cache header  # 8
    rebase index blob worker cache branch parser header remote cache delta delta  # 9
    merge tree ref stream  # 10
    worker object worker object pack pack handle object  # 11
    commit index object object window header buffer parser buffer socket parser  # 12
    branch window token merge index remote rebase tree delta  # 13
    handle blob window delta handle header merge delta queue merge blob  # 14
    blob object blob blob merge parser  # 15
    remote index cache rebase branch stream stream tree worker  # 16
    cache rebase handle delta stream  # 17
    blob blob branch index worker commit buffer branch  # 18
    merge cache queue object socket  # 19
    header worker commit stream commit tree  # 20
    tree buffer handle header branch delta  # 21
    window tree rebase index token handle remote handle branch blob  # 22
    delta merge remote cache socket pack queue header branch queue  # 23
    buffer merge branch handle  # 24
    buffer merge ref token handle handle cache merge rebase  # 25
    ref handle merge handle window merge socket ref  # 26
    blob pack rebase commit rebase branch commit blob tree queue window branch  # 27
    object worker window socket object branch worker  # 28
    socket socket index remote window merge token  # 29
    window queue window tree  # 30
    blob ref queue token pack stream parser delta index rebase  # 31
    stream parser rebase buffer delta merge  # 32
    delta object parser remote worker stream window ref socket  # 33
    delta handle rebase ref  # 34
    index handle branch cache object branch merge stream pack socket  # 35
    parser rebase remote handle blob commit pack token handle branch  # 36
    commit stream stream buffer  # 37
    rebase token object delta stream index buffer buffer parser pack  # 38
    delta commit handle socket merge ref parser blob delta  # 39
    buffer queue merge parser index  # 40
    handle header stream branch  # 41
    window window rebase token commit  # 42
    ref stream branch commit  # 43
    window tree branch window socket  # 44
    branch commit tree delta  # 45
    socket object pack window stream index handle buffer remote  # 46
    delta queue remote branch buffer  # 47
    index parser delta handle window socket handle parser ref blob  # 48
    queue queue rebase handle ref stream worker handle pack  # 49
    branch object commit pack tree object handle object merge  # 50
    parser socket ref index token worker index worker pack remote worker commit  # 51
    window tree queue remote delta delta rebase stream  # 52
    delta token delta branch object pack stream header buffer handle delta  # 0
    tree tree stream worker rebase stream  # 1
    object parser parser pack blob merge branch blob token  # 2
    window rebase pack cache queue  # 3
    tree remote branch queue delta  # 4
    worker commit stream window header  # 5
    pack buffer token object commit rebase rebase ref cache ref  # 6
    merge parser header ref worker  # 7
    cache parser merge buffer commit parser queue handle pack  # 8
    rebase socket ref header  # 9
