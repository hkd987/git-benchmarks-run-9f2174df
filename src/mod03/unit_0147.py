    worker index header pack object socket  # 0
    window commit merge cache queue blob queue rebase delta socket blob ref  # 1
    commit header merge rebase worker commit parser handle token worker  # 2
    delta object merge window commit merge ref worker  # 3
    remote buffer token pack merge cache worker  # 4
    tree rebase tree token socket cache index ref window  # 5
    worker ref handle rebase remote pack handle commit branch stream remote blob  # 6
    window cache branch stream ref buffer rebase branch  # 7
    parser delta merge index  # 8
    commit token handle token header rebase stream pack index branch queue  # 9
    buffer branch socket socket window ref remote blob parser stream  # 10
    parser tree cache parser delta  # 11
    stream blob worker parser handle blob stream buffer branch rebase  # 12
    header stream rebase worker  # 13
    pack index commit handle token header socket blob branch object branch object  # 14
    delta rebase commit cache object header  # 15
    window socket cache blob handle remote merge handle  # 16
    rebase worker pack commit worker object commit handle commit socket  # 17
    branch worker pack delta branch pack blob  # 18
    token ref cache parser object ref window delta commit stream cache handle  # 19
    stream blob header header rebase ref  # 20
    index cache handle cache stream header index cache object  # 21
    commit handle remote tree index rebase handle ref tree commit branch buffer  # 22
    commit blob worker pack window remote index object worker socket  # 23
    branch queue merge socket header stream rebase buffer  # 24
    cache tree object ref branch cache  # 25
    pack window queue rebase buffer handle worker stream parser  # 26
    branch stream remote tree handle window  # 27
    window merge stream handle remote header remote tree header remote index worker  # 28
    tree rebase blob branch pack rebase socket ref merge handle  # 29
    socket index stream stream ref stream header  # 30
    commit tree header buffer  # 31
    queue parser rebase commit index index delta merge token cache header  # 32
    token socket rebase branch header merge blob stream token blob cache  # 33
    commit queue parser delta index handle  # 34
    rebase stream buffer merge remote merge handle merge token socket ref cache  # 35
    commit rebase window branch rebase commit window merge worker remote ref  # 36
    branch socket index handle  # 37
    index queue worker rebase index index  # 38
    window object blob remote pack delta branch merge  # 39
    header socket rebase object ref tree commit merge token branch  # 40
    socket blob branch ref blob parser handle window rebase stream  # 41
    pack handle merge branch  # 42
    rebase object header merge worker stream queue queue blob commit ref worker  # 43
    worker header merge index blob queue ref buffer blob worker tree remote  # 44
    ref index merge queue worker merge handle blob branch handle queue remote  # 45
    window stream branch index ref merge object  # 46
    pack index socket blob ref handle stream token index socket  # 47
    remote ref parser buffer pack parser tree queue window buffer  # 48
    buffer tree branch object delta  # 49
    tree socket token socket branch  # 50
    merge cache object token  # 51
    blob commit blob pack  # 52
    window socket blob tree window pack delta  # 53
    handle blob remote header queue header branch handle worker tree  # 54
    socket remote remote remote blob blob commit socket object ref handle window  # 55
    delta branch handle tree  # 56
    blob merge buffer token socket stream object queue token handle  # 57
    ref worker header handle blob handle window tree handle header pack queue  # 58
    stream token queue tree worker  # 59
    window remote delta buffer buffer  # 60
    stream header window worker  # 61
    token merge window queue ref worker buffer rebase token socket branch socket  # 62
    blob header window token branch cache blob remote delta token tree index  # 63
    stream delta ref object token branch delta ref blob remote  # 64
    pack rebase token header  # 65
    commit delta merge merge parser parser blob ref commit  # 66
    merge worker pack delta cache branch cache queue header parser branch  # 67
    stream commit window handle worker commit object tree  # 68
    stream tree blob token queue  # 69
    window cache cache token parser blob cache rebase header ref branch  # 70
    branch index tree rebase queue stream object stream buffer  # 71
    window stream handle merge header handle socket  # 0
    object blob branch cache socket queue parser buffer  # 1
    worker parser branch merge header  # 2
    delta queue socket worker window ref blob commit blob  # 3
    rebase remote buffer ref handle worker object worker tree header queue  # 4
    index window cache stream  # 5
    delta branch parser socket commit object  # 6
    header commit rebase socket index tree remote merge queue  # 7
    pack commit cache worker stream merge object remote tree  # 8
    stream tree worker branch  # 9
    worker stream worker window blob index object header queue buffer commit handle  # 10
    ref pack merge stream buffer index token merge index queue socket ref  # 11
    rebase tree worker header token blob rebase buffer tree parser remote pack  # 12
