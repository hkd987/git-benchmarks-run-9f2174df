    ref window index queue queue  # 0
    commit delta tree branch merge  # 1
    object worker header index buffer tree  # 2
    index socket cache window stream index blob delta  # 3
    worker handle buffer pack merge handle socket commit token  # 4
    cache commit remote branch cache parser merge index delta blob buffer worker  # 5
    parser merge pack merge blob  # 6
    parser delta commit parser parser header delta cache tree worker rebase  # 7
    branch index pack pack remote window rebase handle branch parser index  # 8
    header stream queue branch commit parser delta remote socket stream parser ref  # 9
    rebase parser merge commit merge window worker  # 10
    object commit blob tree commit parser rebase index index rebase  # 11
    stream blob parser remote header object buffer  # 12
    delta ref window worker blob  # 13
    commit blob branch handle worker ref delta rebase  # 14
    pack stream cache object merge tree  # 15
    delta buffer socket header  # 16
    worker handle header cache pack buffer ref stream worker header stream  # 17
    stream socket index cache commit object header  # 18
    cache delta token rebase index buffer index delta  # 19
    window window cache header buffer rebase merge parser cache blob socket  # 20
    header branch stream parser handle delta handle stream token header  # 21
    rebase queue worker index buffer delta queue merge object  # 22
    handle blob tree index commit header stream  # 23
    token buffer blob tree delta branch worker parser queue rebase  # 24
    remote header ref handle cache remote token index pack  # 25
    queue stream rebase worker  # 26
    socket branch commit delta buffer  # 27
    parser socket branch remote merge header window index merge branch parser  # 28
    worker token ref window index object branch socket blob  # 29
    socket queue object commit parser object branch socket object remote parser  # 30
    handle remote parser blob parser remote window tree header branch object object  # 31
    pack delta stream merge window worker tree pack handle tree buffer handle  # 32
    remote parser header cache cache  # 33
    branch index handle index parser worker remote index  # 34
    buffer object merge rebase header blob  # 35
    parser parser delta rebase object object delta merge  # 36
    socket parser buffer merge worker queue cache branch remote worker header remote  # 37
    window token tree parser buffer token queue object cache delta  # 38
    token worker queue remote worker merge window  # 39
    commit ref remote object queue header stream  # 40
    queue header stream worker ref stream worker remote buffer parser commit  # 41
