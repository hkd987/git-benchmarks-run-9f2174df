    worker branch cache worker remote object worker merge cache  # 0
    blob commit tree buffer object index header parser  # 1
    tree object delta delta worker branch commit merge ref handle branch header  # 2
    socket window worker handle worker merge cache delta rebase branch  # 3
    pack cache tree queue commit merge window branch merge commit token token  # 4
    blob ref tree object remote window parser worker cache commit  # 5
    blob ref index ref commit pack window queue header token commit  # 6
    worker tree ref tree merge stream remote commit  # 7
    blob object queue token stream merge delta commit rebase cache  # 8
    handle tree merge rebase stream commit  # 9
    queue tree token object queue cache branch  # 10
    buffer handle window tree blob pack ref commit  # 11
    stream ref commit branch cache  # 12
    ref socket blob worker remote worker index branch handle  # 13
    blob token queue merge tree socket object  # 14
    index rebase ref queue queue merge window merge parser object parser blob  # 15
    delta object parser parser branch index delta  # 16
    window object blob window buffer pack index  # 17
    parser cache queue commit ref merge  # 18
    merge header merge token ref  # 19
    blob header rebase commit tree ref commit stream merge  # 20
    ref parser rebase tree token tree handle header  # 21
    worker rebase rebase window remote socket  # 22
    pack merge remote header delta ref merge cache handle rebase  # 23
    pack object socket merge handle window cache delta object  # 24
    blob cache commit object remote queue pack stream pack tree  # 25
    merge token delta worker delta socket parser ref  # 26
    cache merge ref window object tree index index token delta header socket  # 27
    token rebase handle delta blob socket branch socket socket socket stream index  # 28
    rebase parser ref merge token worker remote index delta window  # 29
    object buffer cache worker window parser queue  # 30
    delta commit parser remote socket header index ref rebase  # 31
    window parser header header ref parser parser commit commit cache  # 32
    commit ref index object pack  # 33
    merge window blob object blob worker stream index socket queue worker index  # 34
    stream rebase worker merge parser  # 35
    worker commit remote parser remote pack commit window token delta  # 36
    socket rebase tree blob  # 37
    remote pack cache merge  # 38
    ref parser window object merge  # 39
    window window handle header branch  # 40
    worker remote remote parser blob window ref  # 41
    worker commit remote token delta object token header window remote  # 42
    handle delta window header tree delta  # 43
    handle merge remote cache window cache  # 44
    branch token index merge worker commit tree commit ref header worker buffer  # 45
    rebase header window tree  # 46
    cache tree commit window commit socket cache queue header ref ref remote  # 47
    pack index commit remote token ref  # 48
    parser buffer cache index pack window queue handle window commit cache commit  # 49
    cache index window stream parser  # 50
    tree worker pack socket stream remote window handle blob  # 51
    header window buffer queue cache tree ref parser stream buffer  # 52
    token branch stream queue pack worker stream token buffer cache buffer  # 53
