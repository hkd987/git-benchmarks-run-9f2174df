    blob tree delta worker worker  # 0
    cache ref merge window  # 1
    handle object window queue rebase tree delta token handle index object queue  # 2
    worker ref stream index branch cache pack queue token object remote  # 3
    worker merge parser ref pack ref remote cache  # 4
    parser pack commit cache delta delta handle stream  # 5
    remote merge buffer socket merge tree handle blob  # 6
    stream blob commit window pack index cache remote  # 7
    buffer worker commit pack queue delta token merge tree parser index pack  # 8
    socket delta window parser queue ref pack worker ref remote window  # 9
    commit worker object stream commit ref window  # 10
    pack object cache index header handle branch index blob window  # 11
    buffer queue ref token remote ref  # 12
    index rebase blob index buffer remote branch  # 13
    index branch blob cache rebase buffer tree delta remote header index window  # 14
    blob token cache token  # 15
    token header cache cache commit delta  # 16
    cache ref parser object blob window cache  # 17
    pack cache delta rebase handle socket queue token delta handle pack  # 18
    branch pack object rebase branch  # 19
    delta socket rebase handle blob  # 20
    queue branch header token queue commit commit  # 21
    cache parser header merge branch merge  # 22
    merge handle merge stream rebase buffer parser header cache  # 23
    socket tree queue merge delta  # 24
    socket merge ref rebase  # 25
    cache remote delta queue handle handle  # 26
    merge worker remote tree ref ref worker ref object buffer remote  # 27
    branch delta merge object buffer stream blob delta rebase merge  # 28
    token branch socket rebase rebase object tree  # 29
    window rebase window merge  # 30
    stream remote merge socket merge  # 31
    header index merge cache stream queue  # 32
    cache pack object parser index  # 33
    stream stream pack delta branch queue branch rebase  # 34
    commit handle token queue pack merge parser  # 35
    index queue cache index buffer header index  # 36
    stream window cache blob queue  # 37
    ref index commit socket object merge commit  # 0
    parser parser stream rebase blob socket blob pack socket merge parser index  # 1
    pack tree index header merge  # 2
    queue branch socket header merge buffer index commit worker branch  # 3
    merge delta index socket cache pack index tree rebase  # 4
    merge merge branch ref handle handle tree worker object  # 5
    rebase object parser object object header cache queue  # 6
