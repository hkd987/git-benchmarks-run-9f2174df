    object index worker ref stream stream ref pack handle branch  # 0
    pack remote ref ref queue parser socket  # 1
    blob token rebase buffer cache token worker cache ref parser  # 2
    ref blob worker parser  # 3
    socket index merge remote stream token parser worker  # 4
    buffer delta queue window delta handle queue object handle object object  # 5
    rebase header ref tree worker remote object window queue  # 6
    parser window parser parser token rebase tree  # 7
    queue header handle buffer  # 8
    tree index branch window cache worker socket  # 9
    rebase blob token merge pack window tree token token branch  # 10
    socket stream socket remote header stream queue commit stream index  # 11
    commit parser branch remote pack parser queue rebase worker handle  # 12
    object object handle commit parser cache pack queue token socket  # 13
    pack stream window pack rebase buffer index blob index stream index delta  # 14
    remote handle token blob header queue commit index delta  # 15
    index delta window socket rebase pack buffer  # 16
    merge token parser delta merge queue socket tree remote buffer pack  # 17
    cache pack handle worker delta remote handle delta ref  # 18
    handle token handle queue  # 19
    buffer cache stream parser token  # 20
    remote index token pack merge queue blob header window  # 21
    commit object index blob header ref pack cache index header header queue  # 22
    rebase stream window delta rebase parser header branch pack buffer buffer token  # 23
    buffer handle object window merge parser header buffer window pack  # 24
    cache index parser commit commit socket token merge blob ref  # 25
    parser stream worker remote  # 26
    branch commit rebase worker worker delta object merge commit buffer  # 27
    merge buffer pack pack  # 28
    socket queue remote socket parser ref  # 29
    ref token object blob  # 30
    token cache window object handle token socket window object window header queue  # 31
    index ref queue handle blob queue socket  # 32
    header rebase tree branch merge window parser commit index tree header  # 33
    branch pack cache object ref tree commit parser object ref handle  # 34
    handle queue parser rebase object remote window ref tree delta  # 35
    index delta blob stream index merge window token buffer pack delta  # 36
    object index object branch  # 37
    window remote index blob tree header window buffer token object worker  # 38
    blob stream parser cache token pack pack window rebase worker object queue  # 39
    index remote worker handle queue buffer rebase parser  # 40
    tree object remote delta handle remote  # 41
    stream worker remote handle worker queue merge parser token socket pack  # 42
    ref parser blob blob window rebase cache merge  # 43
    header parser stream header  # 44
    handle socket delta merge token parser index pack cache  # 45
    parser window branch object pack parser rebase  # 46
    window buffer delta branch queue token  # 47
    handle object parser merge merge  # 48
    merge token stream commit rebase blob merge header ref  # 49
    rebase token parser window blob ref remote  # 50
    queue remote delta ref token remote delta  # 51
    ref worker commit delta blob merge queue  # 52
    blob rebase pack parser token cache token remote merge merge  # 53
    pack token parser index branch  # 54
    queue worker window tree window worker rebase tree  # 55
    queue parser branch worker tree delta branch header branch handle remote commit  # 56
    window queue delta rebase object header window token parser handle  # 57
    stream buffer buffer parser parser worker delta cache token merge handle branch  # 58
    pack queue handle worker worker blob ref token  # 59
    cache rebase window parser branch worker blob  # 60
    stream commit parser cache index header branch merge blob tree  # 61
    commit remote tree pack remote index delta tree cache object  # 62
    handle buffer queue window rebase pack rebase parser worker worker  # 63
    pack index window remote delta tree  # 64
    stream token stream delta cache socket commit worker pack handle ref branch  # 65
    ref delta header index merge  # 66
    header queue pack ref parser token remote parser merge  # 67
    worker tree socket cache token buffer window  # 68
    buffer queue tree window index header  # 69
    token branch token index  # 70
    merge ref merge socket commit socket merge worker pack  # 71
    window buffer header rebase remote blob ref handle  # 72
    stream parser worker handle cache branch buffer stream stream socket parser  # 73
    blob ref delta handle ref commit window object object header remote token  # 74
    merge handle remote header blob  # 75
    index branch parser object tree cache index  # 76
    tree branch worker token  # 77
    delta merge remote parser index pack blob object worker  # 78
    merge remote object merge pack buffer parser  # 79
    blob buffer remote remote blob queue ref delta cache  # 80
    merge object rebase remote cache buffer  # 81
    window object queue blob  # 82
    stream commit cache cache queue remote index  # 83
    cache branch index worker branch rebase  # 84
    socket socket blob cache header buffer handle  # 85
    branch remote index index merge rebase token window pack blob queue  # 0
    ref remote index queue  # 1
    window index rebase buffer  # 2
    stream token delta tree parser socket handle  # 3
    buffer queue worker delta  # 4
