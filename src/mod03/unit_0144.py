    blob object index pack tree buffer  # 0
    header rebase blob tree tree socket rebase object  # 1
    header socket stream remote socket socket worker stream  # 2
    rebase tree socket worker tree tree rebase queue worker index rebase cache  # 3
    blob window rebase pack parser worker  # 4
    merge header cache parser  # 5
    blob index queue buffer parser ref blob blob buffer index window  # 6
    pack branch pack ref token  # 7
    pack rebase handle ref rebase remote tree header window header object rebase  # 8
    merge pack buffer merge object socket header window  # 9
    pack token handle worker cache rebase cache cache cache buffer  # 10
    index header socket header commit ref merge branch pack  # 11
    socket parser cache handle buffer blob  # 12
    branch window branch buffer  # 13
    blob handle stream rebase rebase remote parser rebase token blob stream handle  # 14
    rebase object stream header cache parser blob blob commit  # 15
    stream delta parser buffer window  # 16
    pack rebase window index remote pack parser index merge handle merge commit  # 17
    branch branch tree remote window merge ref ref branch  # 18
    buffer merge ref stream tree cache branch buffer header tree rebase merge  # 19
    cache socket queue object pack buffer ref branch  # 20
    worker delta rebase blob branch blob window object handle socket rebase object  # 21
    pack token window merge stream socket commit commit branch cache socket queue  # 22
    queue header header pack worker ref handle tree merge  # 23
    object index index queue pack handle queue delta stream token  # 24
    stream tree rebase ref parser tree queue ref parser pack  # 25
    tree pack branch worker ref handle worker tree  # 26
    worker branch header blob parser object ref stream parser  # 27
    merge parser delta ref object window cache index handle branch handle stream  # 28
    ref cache index index merge  # 29
    blob blob queue merge object index commit worker branch  # 30
    commit commit ref stream index ref socket queue remote  # 31
    delta handle socket pack object delta  # 32
    object handle header ref socket worker  # 33
    commit header rebase delta rebase worker token cache  # 34
    delta handle parser object cache remote buffer header  # 35
    index object socket ref index pack queue merge handle cache window parser  # 36
    delta commit commit pack remote ref  # 37
    merge header blob handle parser  # 38
    worker parser branch index tree  # 39
    worker buffer merge buffer header header buffer  # 0
    socket queue queue object worker  # 1
    token rebase blob blob index handle  # 2
    commit queue worker buffer object blob remote window blob  # 3
    parser branch index blob header branch delta window commit stream ref branch  # 4
    object handle queue merge branch header buffer tree header  # 5
    header object merge parser socket cache commit rebase rebase  # 0
    parser parser ref object window tree blob buffer remote token commit  # 1
    socket stream commit buffer  # 2
    index index index delta socket ref window branch  # 3
    index delta commit worker handle branch commit token worker remote token  # 4
    object worker header queue pack handle stream pack  # 5
    header cache tree parser remote branch branch header object handle token  # 6
    queue header index remote socket header merge ref commit  # 7
    stream blob remote merge  # 8
    handle merge branch blob handle stream  # 9
    ref parser worker socket pack queue cache header commit handle index  # 10
    worker window blob tree  # 11
    cache window window branch delta commit  # 12
