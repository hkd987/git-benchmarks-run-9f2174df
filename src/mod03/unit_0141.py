    delta branch header worker  # 0
    delta buffer cache ref  # 1
    worker delta window buffer cache buffer token token queue worker tree queue  # 2
    parser remote handle pack  # 3
    socket remote stream delta merge  # 4
    delta pack blob branch handle pack branch token commit rebase header pack  # 5
    merge buffer cache blob blob cache buffer remote queue branch pack  # 6
    tree parser header index  # 7
    rebase cache commit branch token window pack  # 8
    cache window pack ref index handle window ref  # 9
    remote parser index tree buffer window remote merge index handle parser branch  # 10
    buffer token remote socket handle pack cache ref  # 11
    index delta tree buffer socket socket merge object pack window  # 12
    commit cache ref tree commit queue parser commit pack  # 13
    header tree handle rebase branch index socket tree cache worker worker  # 14
    window header blob handle worker delta  # 15
    header rebase object parser cache branch stream commit object cache buffer branch  # 16
    queue merge pack queue commit buffer handle remote window  # 17
    header buffer header cache worker ref queue merge rebase ref queue stream  # 18
    ref parser commit header ref cache object cache worker  # 19
    token tree buffer blob buffer window handle queue worker queue  # 20
    queue pack ref handle queue  # 21
    ref token stream index commit header socket ref  # 22
    branch delta window worker socket pack object window merge stream  # 23
    queue branch ref tree  # 24
    object queue handle tree  # 25
    socket cache index ref parser window token socket socket parser branch  # 26
    commit cache remote cache buffer window pack rebase  # 27
    header rebase remote stream queue handle cache socket window token blob cache  # 28
    socket remote object merge  # 29
    queue header handle parser queue ref commit pack parser delta  # 30
    tree merge blob object merge ref  # 31
    object rebase merge socket worker ref remote index pack remote tree  # 32
    window merge rebase tree socket index remote handle  # 33
    commit queue commit object stream tree parser branch stream header buffer  # 34
    handle cache branch stream socket index socket blob worker  # 35
    rebase merge cache cache  # 36
    buffer token pack blob stream  # 37
    rebase parser handle ref handle blob window buffer rebase tree pack cache  # 38
    blob worker blob cache header stream stream rebase  # 39
    header queue socket queue socket handle pack  # 40
    delta header window worker blob token header merge object branch token  # 41
    buffer index rebase parser  # 42
    token parser object token  # 43
    delta index rebase socket header blob  # 44
    branch rebase token commit blob index merge worker rebase  # 45
    buffer branch index worker window header parser blob  # 46
    buffer stream remote blob branch remote remote header parser header  # 47
    worker window queue window buffer token rebase handle socket index parser  # 48
    queue commit stream token index ref  # 49
    index pack handle pack parser ref  # 50
    index pack header queue  # 51
    queue parser delta cache socket index  # 52
    object handle object window pack socket index rebase  # 53
    cache handle index tree rebase worker queue blob token queue  # 54
    stream socket blob token pack rebase handle  # 55
    merge parser handle delta  # 56
    rebase blob delta worker parser remote window  # 57
    rebase blob branch header  # 58
    rebase cache window rebase buffer  # 59
    object window window window merge  # 60
    commit tree handle cache  # 61
    cache token commit merge cache parser worker branch rebase  # 62
    token window tree pack token cache socket remote token handle  # 63
    socket tree handle socket index buffer  # 0
    stream tree stream commit  # 1
    buffer cache pack header  # 2
    cache remote index token  # 3
    tree merge parser window tree branch ref window stream buffer queue handle  # 4
    socket blob socket remote remote merge  # 5
    header commit parser ref worker delta commit blob ref rebase  # 6
