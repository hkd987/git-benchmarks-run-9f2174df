    remote index delta rebase socket cache rebase  # 0
    header socket parser buffer branch window commit stream header  # 1
    stream parser ref buffer buffer stream pack ref header blob blob  # 2
    header parser delta ref handle socket  # 3
    window ref queue rebase  # 4
    merge stream branch token blob parser object handle  # 5
    socket delta commit handle delta  # 6
    ref blob blob branch object socket  # 7
    queue header blob header tree cache tree blob token parser remote cache  # 8
    index cache stream handle ref object cache token cache queue  # 9
    ref cache window handle handle rebase queue token token socket tree  # 10
    socket queue header handle tree tree ref cache header  # 11
    rebase handle remote handle commit worker remote buffer buffer window branch pack  # 12
    cache delta buffer remote index cache remote object delta remote  # 13
    cache ref header token header queue pack parser socket index token  # 14
    cache remote queue parser parser index worker branch parser remote tree  # 15
    socket pack rebase header  # 16
    token header branch object blob window  # 17
    window index object cache index  # 18
    token header header socket stream buffer merge  # 19
    queue commit window blob  # 20
    pack worker tree delta merge  # 21
    worker socket handle branch cache commit ref cache commit  # 22
    branch cache commit commit tree tree token  # 23
    commit window queue commit handle branch blob  # 24
    token header pack window socket handle window cache stream merge  # 25
    tree pack object object  # 26
    window token pack rebase buffer buffer  # 27
    queue merge parser blob  # 28
    delta merge worker ref tree stream index queue token handle  # 29
    branch worker parser socket handle cache stream token  # 30
    remote pack window delta socket blob rebase pack object  # 31
    merge window delta pack tree parser rebase remote rebase cache delta  # 32
    parser parser stream worker branch handle handle blob  # 33
    token worker buffer header cache merge  # 34
    branch buffer socket stream token token cache header branch  # 35
    branch worker blob remote stream  # 36
    object stream ref ref stream socket  # 37
    ref ref tree stream tree remote pack queue object object buffer  # 38
    tree handle socket ref delta commit worker remote cache  # 39
    commit rebase window index merge remote object stream blob pack  # 40
    merge token window ref delta tree window  # 41
    commit buffer token tree worker window queue window stream branch cache commit  # 42
    index handle handle branch branch worker header  # 43
    commit index socket remote blob branch stream pack  # 44
    index buffer tree handle worker token tree handle parser  # 45
    header ref object stream pack tree buffer handle ref queue ref  # 46
    header worker parser window rebase socket header stream cache stream  # 47
    window parser worker header merge tree token  # 48
    remote tree token commit handle token merge  # 49
    cache token stream window parser blob handle header object  # 50
    queue merge tree queue queue rebase queue token merge  # 51
    index index ref tree  # 52
    cache delta parser branch ref index  # 53
    handle blob index blob window  # 54
    merge worker socket cache cache  # 55
    stream rebase worker commit window cache worker blob cache  # 56
    buffer buffer cache object handle pack stream parser tree worker branch window  # 57
    stream commit rebase tree merge merge  # 58
    cache blob queue branch token window tree header  # 59
    commit merge rebase commit parser branch handle rebase window branch merge stream  # 60
    stream ref header worker worker buffer object parser stream buffer rebase delta  # 61
    token blob tree pack branch window  # 62
    delta delta pack stream window token merge tree  # 63
    pack header token rebase index window window object index object delta  # 64
    pack socket handle ref rebase  # 65
    remote delta commit delta token blob handle handle pack pack  # 66
    object rebase commit branch merge pack delta tree object socket  # 67
    index handle stream rebase queue header index merge  # 68
    blob socket buffer remote cache rebase rebase object index remote socket  # 69
    pack index socket token branch commit remote blob queue  # 70
    socket object buffer branch cache  # 71
    window handle stream remote socket rebase blob blob header parser blob parser  # 72
    window tree remote tree cache stream token handle worker token  # 73
    window parser delta token rebase buffer queue buffer token handle  # 74
    object pack ref pack object pack header branch commit remote window parser  # 75
    pack header merge delta object parser token remote rebase cache socket blob  # 76
    parser handle cache commit tree  # 77
    socket buffer handle stream  # 78
    pack delta index queue window header  # 79
    handle tree index stream worker delta blob commit object handle  # 80
    object token stream remote remote socket index token  # 0
    delta rebase worker rebase token  # 1
    queue remote rebase header  # 2
    token ref ref token window object  # 3
