    branch handle stream remote index merge queue pack  # 0
    rebase commit index merge buffer  # 1
    pack worker handle cache blob buffer rebase ref header queue blob blob  # 2
    ref window object delta tree worker ref remote delta commit socket  # 3
    socket remote cache worker commit remote  # 4
    remote commit pack object blob  # 5
    header commit rebase token commit tree delta parser  # 6
    tree socket index index cache window token  # 7
    index branch blob delta blob queue tree ref  # 8
    handle commit rebase object window window worker pack parser branch rebase buffer  # 9
    tree merge rebase socket stream window socket  # 10
    merge index pack remote parser  # 11
    commit worker token branch tree  # 12
    blob remote cache ref remote remote cache socket  # 13
    commit delta branch branch delta queue tree token commit handle ref  # 14
    ref remote socket rebase  # 15
    rebase window index branch socket branch queue buffer window  # 16
    object pack branch worker cache merge pack ref header pack  # 17
    window buffer index commit socket ref stream socket parser header  # 18
    buffer blob ref index delta remote parser buffer index ref pack  # 19
    blob pack commit delta commit worker socket object index  # 20
    stream cache handle pack header tree handle tree index handle  # 21
    remote header object queue header branch ref  # 22
    handle socket rebase token index  # 23
    branch merge merge rebase pack commit pack ref cache merge socket stream  # 24
    rebase buffer merge object blob cache  # 25
    socket queue window parser queue index stream index index  # 26
    remote stream stream commit object tree object object rebase tree  # 27
    pack window index token worker rebase queue header  # 28
    pack handle worker remote  # 29
    pack index merge rebase pack rebase rebase index window  # 30
    stream worker delta worker tree delta  # 31
    index token branch stream header  # 32
    object parser ref merge buffer delta delta cache object socket  # 33
    handle buffer rebase window buffer cache window  # 34
    header tree worker window tree queue worker handle pack object  # 35
    parser cache buffer socket ref remote queue parser  # 36
    rebase branch remote buffer blob rebase delta tree blob index window  # 37
    object ref ref socket pack merge queue worker queue worker socket  # 38
    tree cache token header rebase queue  # 39
    remote blob window parser branch parser remote tree socket socket  # 40
    delta token remote ref rebase window rebase  # 41
    buffer window stream tree remote merge  # 42
    parser delta index stream header tree worker index delta handle  # 43
    object header delta window merge window queue object parser  # 44
    object header rebase parser branch window pack remote window remote  # 45
    handle parser merge window tree blob index branch socket header blob  # 46
    handle tree socket object buffer header stream header  # 47
    object buffer tree stream queue delta tree delta header token blob ref  # 48
    object delta delta header ref  # 49
    branch object tree stream header queue token parser ref  # 50
    remote pack ref handle index token cache index  # 51
    ref merge ref header queue cache  # 52
    parser pack handle handle  # 53
    rebase pack branch worker handle tree cache  # 54
    parser blob window tree tree socket socket  # 55
    ref header window buffer merge queue  # 56
    commit remote blob token blob remote worker pack parser rebase  # 57
    commit buffer socket commit  # 58
    object queue socket blob remote  # 59
    header remote cache ref blob tree parser worker pack header  # 60
    queue ref branch queue header merge blob  # 61
    token worker worker delta parser delta  # 62
    branch merge rebase queue cache pack window branch  # 63
    token branch object index delta buffer parser window commit  # 64
    ref pack window header delta cache parser stream tree worker  # 65
    index index cache rebase branch index worker socket  # 66
    rebase token tree buffer object blob parser delta header window pack buffer  # 67
    cache queue cache commit  # 68
    remote window header index queue rebase rebase remote window  # 69
    delta blob cache delta index pack  # 70
    blob ref pack stream socket delta pack blob window blob object rebase  # 71
    delta parser delta stream merge tree branch  # 72
    window blob header cache  # 73
    header window delta cache merge window worker commit index worker  # 74
    cache delta rebase worker branch blob queue merge merge token window  # 75
    delta branch queue window window queue token cache  # 76
    rebase blob merge blob parser index cache remote  # 77
    rebase parser parser branch socket delta queue  # 78
    window commit header commit stream delta cache worker buffer socket  # 79
    branch socket pack delta object index pack  # 80
    header token socket token header header rebase window token parser queue pack  # 81
    token index buffer socket queue parser delta branch branch remote  # 82
