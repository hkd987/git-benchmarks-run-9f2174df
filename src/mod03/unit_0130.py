    object ref window index window branch commit delta blob handle token  # 0
    commit delta merge tree delta worker pack socket stream  # 1
    branch commit index worker buffer handle cache branch parser blob delta ref  # 2
    branch worker cache remote rebase socket window ref tree header header  # 3
    index remote branch token pack remote  # 4
    header queue stream queue object  # 5
    blob token index commit window index socket ref delta  # 6
    stream queue worker cache delta  # 7
    token window blob merge buffer socket  # 8
    index window header queue object window merge queue commit object  # 9
    socket pack merge socket tree  # 10
    header branch worker object object delta worker object ref socket header  # 11
    token rebase blob blob buffer pack stream delta  # 12
    object cache remote stream parser parser header object object stream merge  # 13
    pack tree socket delta object cache rebase parser index  # 14
    queue ref token merge cache object cache queue  # 15
    object merge remote delta ref rebase handle  # 16
    cache parser delta queue stream handle window remote cache blob branch  # 17
    rebase ref delta buffer stream rebase index parser merge stream delta  # 18
    token socket pack remote blob pack stream token branch branch commit  # 19
    rebase blob queue rebase remote  # 20
    delta blob branch blob queue token delta stream merge  # 21
    buffer index buffer commit commit stream commit cache pack worker handle  # 22
    queue index window stream remote rebase branch remote  # 23
    rebase parser branch cache stream buffer  # 24
    queue delta token tree delta window remote index buffer socket  # 25
    worker stream remote parser ref worker object  # 26
    parser socket queue header object delta blob stream  # 27
    queue tree buffer window  # 28
    merge tree handle parser queue  # 29
    ref remote socket header remote  # 30
    rebase token socket token blob cache remote rebase object buffer  # 31
    merge handle commit ref  # 32
    parser ref ref socket  # 33
    buffer commit branch stream header header tree  # 34
    rebase rebase branch cache  # 35
    queue parser blob stream commit merge merge queue  # 36
    blob buffer parser delta cache parser ref blob stream object  # 37
    buffer rebase socket remote ref tree delta handle parser object  # 38
    blob rebase remote ref stream handle blob branch buffer  # 39
    tree remote header worker pack token object commit  # 40
    cache handle delta handle  # 41
    rebase handle merge worker ref pack  # 42
    object token socket delta window  # 43
    token window pack stream buffer  # 44
    branch pack remote rebase commit index parser  # 45
    token token buffer parser remote tree  # 46
    object header stream delta pack rebase token  # 47
    header ref branch delta  # 48
    buffer parser merge queue tree tree socket remote  # 49
    rebase token cache worker cache blob handle  # 50
    tree queue pack merge rebase rebase  # 51
    pack token ref stream stream  # 52
    token tree header queue parser stream  # 53
    worker delta blob commit delta cache ref rebase buffer buffer  # 54
    handle merge cache queue buffer window worker delta  # 55
    object socket delta buffer index delta header delta cache  # 56
    buffer worker header object token branch buffer stream  # 57
    delta parser header rebase blob queue header  # 58
    tree branch object merge header parser  # 59
    token handle queue rebase  # 60
    window commit buffer rebase ref parser  # 61
    stream buffer worker ref queue queue queue tree  # 62
    parser pack header token  # 63
    tree remote header stream socket  # 64
    pack token socket worker handle header buffer window delta  # 65
    blob queue cache buffer merge token blob ref  # 0
    commit ref worker delta  # 1
    blob ref header merge worker socket remote tree header buffer  # 2
    token header pack cache queue  # 3
    blob pack pack stream buffer commit  # 4
    handle header stream window tree socket socket token queue branch delta  # 5
    commit remote delta blob  # 0
    window index stream object index handle  # 1
    window index object header header merge handle commit  # 2
    rebase window delta buffer pack tree delta object  # 3
    rebase tree delta rebase window commit remote  # 4
