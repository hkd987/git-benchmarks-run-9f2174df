    ref parser window index cache worker worker worker index  # 0
    delta ref buffer object buffer merge blob remote cache  # 1
    index window buffer queue token  # 2
    socket worker merge tree object object buffer cache  # 3
    branch index header blob commit header branch buffer  # 4
    delta parser window branch worker stream commit blob buffer remote rebase  # 5
    window index remote rebase tree cache tree index  # 6
    socket tree parser parser tree handle pack index window remote handle parser  # 7
    tree header parser remote branch cache stream token remote  # 8
    tree worker buffer token object handle header  # 9
    header branch socket blob worker branch handle merge queue tree delta  # 10
    parser tree window delta window queue  # 11
    pack blob stream stream worker delta commit cache worker cache window merge  # 12
    socket commit window ref worker blob handle commit merge tree merge remote  # 13
    socket window tree buffer blob token tree  # 14
    index pack parser handle object object blob remote worker queue handle tree  # 15
    tree ref handle blob socket socket delta tree token queue  # 16
    index index header token handle ref window  # 17
    cache rebase merge blob rebase tree merge  # 18
    branch ref worker rebase remote queue buffer index  # 19
    parser rebase tree branch rebase branch  # 20
    token socket blob queue merge socket commit tree merge  # 21
    token window tree buffer blob  # 22
    object cache merge queue delta object cache handle pack  # 23
    queue delta socket parser  # 24
    handle queue pack buffer parser parser tree blob rebase socket  # 25
    window tree branch cache branch cache object queue parser commit socket remote  # 26
    handle token ref buffer branch header  # 27
    blob ref window ref header branch remote branch blob worker  # 28
    index index ref buffer object handle window stream header worker  # 29
    queue cache merge branch parser handle worker ref  # 30
    worker delta commit buffer tree merge merge pack buffer queue  # 31
    header token socket buffer merge pack remote  # 32
    merge branch blob parser tree merge ref object cache  # 33
    token worker header tree tree token queue  # 34
    object merge stream window cache token merge remote delta blob token header  # 35
    object pack commit blob pack commit rebase handle queue object branch window  # 36
    tree pack ref window queue header commit rebase buffer handle  # 37
    parser rebase token token worker pack  # 38
    token queue branch worker delta ref  # 39
    object tree rebase handle tree worker header worker  # 40
    tree merge ref queue delta socket index header cache stream  # 41
    remote ref parser ref worker handle  # 42
    socket handle tree cache ref branch pack queue blob  # 43
