    queue parser cache stream stream blob delta blob stream handle  # 0
    token index tree pack cache window cache  # 1
    tree buffer pack window merge pack  # 2
    header index parser window pack tree pack worker tree socket  # 3
    tree remote rebase handle blob  # 4
    token worker delta tree commit blob index cache index rebase worker buffer  # 5
    blob index delta index object  # 6
    parser header delta remote parser remote index buffer ref commit token queue  # 7
    remote stream cache branch branch commit blob blob worker cache  # 8
    worker pack index header socket rebase merge window pack cache index  # 9
    header buffer worker token handle  # 10
    branch buffer token stream remote header branch index remote commit  # 11
    rebase branch blob tree handle cache window buffer object handle stream  # 12
    stream index commit pack rebase merge token  # 13
    queue tree rebase buffer object header socket parser merge window header queue  # 14
    tree commit token parser delta object ref ref pack branch  # 15
    stream parser merge tree window worker tree  # 16
    queue socket cache blob  # 17
    socket handle blob queue object worker window socket  # 18
    token queue delta stream merge token merge handle ref index  # 19
    rebase object branch merge parser worker  # 20
    handle stream window index buffer socket socket  # 21
    remote worker socket header pack object header rebase branch pack  # 22
    window merge object stream  # 23
    commit handle merge parser token index header queue  # 24
    delta rebase blob socket handle remote delta socket  # 25
    blob window commit stream commit socket worker pack  # 26
    index header buffer remote tree remote blob tree worker blob buffer  # 27
    queue queue rebase delta handle branch cache worker delta tree object  # 28
    rebase worker ref commit  # 29
    tree commit token tree object  # 30
    delta cache socket handle header stream merge stream worker  # 31
    index tree worker tree blob ref branch remote commit socket  # 32
    merge cache ref tree parser socket branch  # 33
    stream window buffer merge index socket worker window index worker  # 34
    buffer cache stream parser parser socket delta  # 35
    rebase parser delta socket  # 36
    commit cache blob parser  # 37
    parser worker pack ref queue buffer  # 38
    parser ref tree delta queue cache branch cache buffer queue handle  # 39
    stream buffer branch socket header  # 40
    remote socket socket merge commit object ref buffer queue remote  # 41
    remote index queue token remote handle  # 42
    cache commit merge tree window pack buffer queue branch  # 43
    rebase socket index blob worker object parser commit pack merge  # 44
    buffer branch socket remote socket socket remote pack socket buffer  # 45
    blob socket object queue worker merge ref parser  # 46
    queue ref index object parser index  # 47
    object blob pack tree parser object buffer blob branch socket  # 48
    branch pack worker cache blob window stream socket blob delta object header  # 49
    branch header buffer cache stream rebase buffer  # 50
    blob merge cache worker ref window merge rebase  # 51
    index token window stream  # 52
    pack worker commit remote worker worker stream merge blob cache window  # 53
    socket worker tree ref socket handle  # 54
    tree header index worker rebase parser rebase  # 0
    window queue ref handle header branch header ref window tree  # 1
    blob rebase ref socket  # 2
    blob buffer index tree buffer socket cache merge window  # 3
    object buffer blob index pack  # 4
    cache stream stream parser worker queue stream merge stream  # 5
    pack object delta socket window queue stream commit handle  # 6
    remote branch cache token  # 7
    index delta window worker commit commit commit  # 8
    stream delta handle blob queue stream pack pack branch ref  # 9
