    branch cache stream blob ref object window index  # 0
    object commit object token  # 1
    tree commit token tree object handle header blob  # 2
    parser ref handle worker window ref buffer parser tree token merge pack  # 3
    socket handle tree rebase rebase commit blob delta buffer branch window cache  # 4
    delta index worker index buffer header merge parser remote token parser tree  # 5
    index queue branch index  # 6
    cache ref parser pack socket branch window pack commit buffer handle  # 7
    index delta object header pack rebase tree branch merge token window  # 8
    object merge socket index stream rebase parser blob rebase index  # 9
    blob socket token object rebase queue queue socket  # 10
    header commit remote index remote worker merge tree delta ref  # 11
    object tree cache queue cache branch blob header commit blob  # 12
    token commit blob pack index cache delta tree header window merge  # 13
    delta blob handle socket queue  # 14
    queue tree remote cache blob tree  # 15
    stream object worker handle tree header pack queue  # 16
    remote index pack window remote stream  # 17
    branch queue remote window pack handle  # 18
    tree blob parser queue branch branch pack worker  # 19
    blob pack window merge object index token worker token  # 20
    commit merge delta socket blob handle  # 21
    merge commit header object ref socket stream  # 22
    commit buffer branch rebase parser  # 23
    socket ref header socket stream ref index delta  # 24
    delta socket remote commit  # 25
    handle queue socket commit cache window cache token buffer commit ref merge  # 26
    ref stream parser blob ref  # 27
    object parser ref ref delta commit merge parser branch pack remote  # 28
    ref worker window index worker buffer commit  # 29
    stream worker blob queue delta  # 30
    window ref index remote cache header commit  # 31
    commit window handle parser token  # 32
    stream handle handle window worker buffer handle  # 33
    index cache stream tree buffer delta queue rebase  # 34
    tree token delta buffer parser pack rebase  # 35
    blob parser blob ref handle window worker index handle  # 36
    parser pack pack cache blob merge queue queue  # 37
    parser delta delta blob token queue merge delta window  # 38
    header socket delta cache token  # 39
    object token worker blob  # 40
    header window window index  # 41
    socket branch delta token buffer rebase ref socket  # 42
    merge remote ref rebase token ref blob  # 43
    socket object rebase delta merge ref header parser blob handle  # 44
    token stream remote branch ref ref queue buffer object parser  # 45
    delta header index pack ref tree index handle branch branch blob merge  # 46
    delta stream pack pack cache handle pack branch tree  # 47
    blob cache header cache buffer window  # 48
    queue stream ref merge  # 49
    rebase pack merge queue remote commit parser token ref tree pack delta  # 50
    blob worker blob queue pack stream handle cache rebase  # 51
    merge window cache parser token cache window merge tree ref commit  # 52
    object window worker stream pack parser socket worker worker index  # 53
    cache ref buffer delta handle merge  # 54
    commit stream blob tree tree remote ref  # 55
    ref blob buffer stream  # 56
    queue object index header ref stream delta stream queue worker  # 57
    queue pack handle token handle stream rebase token buffer  # 58
    handle commit pack parser blob index  # 59
    token branch parser tree  # 60
    queue pack commit token socket branch handle delta merge  # 61
    worker stream cache queue merge handle branch parser  # 62
    handle branch commit pack handle tree object socket handle  # 63
    remote handle ref stream tree socket queue  # 64
    cache buffer delta tree handle ref blob blob parser header  # 65
    window header handle ref ref ref remote  # 66
    object parser cache index pack merge remote  # 67
    index stream handle index  # 68
    tree rebase blob window merge blob index commit remote commit  # 69
    index token worker remote tree  # 70
    merge header queue tree queue  # 71
    handle handle parser queue ref remote object index tree blob branch token  # 72
    branch stream index branch  # 73
    parser rebase token queue  # 74
