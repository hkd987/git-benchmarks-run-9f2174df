    remote commit index remote cache commit delta handle worker header delta handle  # 0
    cache queue merge stream stream delta merge worker buffer  # 1
    object socket token pack buffer ref window  # 2
    pack ref index object commit remote remote  # 3
    object blob blob buffer index queue rebase delta queue delta blob ref  # 4
    queue rebase tree remote stream buffer queue tree rebase branch ref  # 5
    cache blob merge commit token token commit pack object  # 6
    buffer handle ref buffer  # 7
    index stream stream handle merge blob  # 8
    remote window header stream delta header cache remote pack pack header  # 9
    stream worker pack merge object branch  # 10
    branch stream rebase merge branch parser ref queue  # 11
    queue remote rebase pack branch queue parser pack  # 12
    parser remote cache header remote cache delta parser handle window parser  # 13
    pack branch commit handle token merge  # 14
    queue delta blob index commit cache  # 15
    object buffer commit tree parser blob token remote token worker queue parser  # 16
    delta pack delta object object buffer branch header  # 17
    stream merge token queue token  # 18
    token rebase buffer worker tree tree handle handle branch delta index  # 19
    rebase delta delta index blob token socket parser  # 20
    worker index rebase socket buffer parser parser  # 21
    token tree header blob cache blob socket parser token parser  # 22
    parser blob queue tree buffer merge remote worker  # 23
    buffer socket window ref  # 24
    worker pack index stream cache object branch tree  # 25
    parser tree object worker object socket  # 26
    commit branch buffer rebase parser blob branch pack buffer stream  # 27
    merge socket cache remote header stream object stream  # 28
    worker commit socket token delta header blob ref ref object delta  # 29
    header merge rebase socket window blob branch  # 30
    parser window buffer worker  # 31
    stream token window window stream branch  # 32
    object window rebase commit buffer buffer  # 33
    tree token parser queue ref  # 34
    rebase rebase ref queue remote  # 35
    commit delta socket socket ref socket rebase worker header merge index  # 36
    remote commit window tree delta index blob cache worker handle  # 37
    ref header cache merge token commit cache  # 38
    object header handle delta  # 39
    pack stream buffer delta merge window merge commit merge stream  # 40
    cache queue queue tree handle index merge rebase header worker  # 41
    commit ref worker parser window branch handle queue  # 42
    merge ref socket index delta queue branch index pack delta merge cache  # 43
    blob buffer branch handle rebase remote remote tree blob  # 44
    rebase commit buffer queue pack blob  # 45
    remote window parser commit  # 46
    parser index delta queue handle object  # 47
    branch handle token stream tree handle cache worker branch branch pack delta  # 48
    blob cache index window window ref parser  # 49
    rebase handle handle stream blob tree socket pack handle queue  # 50
    commit branch object object handle ref commit cache handle delta  # 51
    blob delta handle remote  # 52
    pack rebase parser merge window branch commit  # 53
    stream index object ref commit header remote worker commit tree pack parser  # 54
    header index stream socket handle stream merge branch  # 55
    token remote socket handle worker rebase delta  # 56
    queue header pack window object index index window socket merge worker  # 57
    tree commit worker stream commit  # 58
    handle index parser queue queue  # 59
    tree blob stream parser object queue pack blob handle socket  # 60
    cache queue pack pack rebase blob socket token  # 61
    token commit stream parser pack commit index tree stream  # 62
    worker merge remote branch handle stream worker ref rebase rebase  # 63
    token stream parser delta  # 64
    delta window remote blob  # 65
    window handle worker delta  # 66
    window worker buffer parser queue header blob merge parser object socket  # 67
    cache tree socket token remote token  # 68
    window stream queue window handle tree rebase  # 69
