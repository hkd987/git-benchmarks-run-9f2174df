    handle buffer pack worker tree remote commit  # 0
    merge cache worker ref buffer handle merge header  # 1
    worker remote index blob handle  # 2
    pack commit parser tree parser worker worker branch stream object token  # 3
    remote window worker socket rebase window stream blob window pack  # 4
    commit remote pack blob window  # 5
    tree stream ref branch rebase rebase  # 6
    delta handle worker merge cache tree buffer parser worker queue buffer  # 7
    buffer tree remote handle blob delta pack ref  # 8
    branch parser ref token blob ref worker  # 9
    blob blob parser ref rebase delta buffer remote buffer  # 10
    window index merge remote header window  # 11
    commit commit window header socket delta header token buffer  # 12
    parser ref merge branch stream header socket token branch remote  # 13
    remote token buffer parser worker  # 14
    index parser stream commit socket branch stream object branch index worker cache  # 15
    ref blob delta window commit tree handle  # 16
    blob blob delta merge ref index handle  # 17
    pack token handle delta branch parser index socket window  # 18
    object buffer branch window merge queue merge parser handle worker parser socket  # 19
    parser branch commit commit stream remote branch  # 20
    token commit worker cache delta worker  # 21
    tree remote merge rebase pack handle worker remote  # 22
    buffer merge token rebase pack socket index branch socket queue pack  # 23
    header merge handle delta worker stream handle header blob stream  # 24
    merge parser tree worker merge buffer token remote rebase remote  # 25
    rebase cache window handle header blob commit merge cache  # 26
    header parser handle cache header token remote branch  # 27
    parser commit merge rebase  # 28
    header cache delta pack rebase token commit object worker branch remote  # 29
    index blob rebase rebase object blob blob object  # 30
    rebase tree window socket  # 31
    cache pack token commit merge  # 32
    commit object header worker tree remote rebase cache window delta  # 33
    blob pack tree ref queue  # 34
    window merge remote branch rebase queue ref window  # 35
    buffer parser object socket worker  # 36
    blob token cache blob  # 37
    socket commit branch delta  # 38
    commit pack buffer branch remote object delta  # 39
    branch queue remote tree  # 40
    buffer pack cache socket rebase queue rebase index pack tree  # 41
    queue ref token parser  # 42
    blob branch buffer branch pack header worker cache  # 43
    pack window window socket  # 44
    delta parser header header remote object parser  # 45
    queue commit branch ref blob branch delta queue branch ref handle  # 46
    handle handle stream index queue socket commit commit branch handle cache queue  # 47
    cache merge blob rebase rebase index commit tree parser socket pack merge  # 48
    socket pack commit blob remote  # 49
    ref handle buffer blob parser cache handle token  # 50
    rebase handle cache branch  # 51
    parser ref window buffer object queue remote object object window  # 52
    blob delta tree header branch branch merge object merge index tree pack  # 53
    ref branch tree commit blob ref commit rebase header  # 54
    cache cache header token parser  # 55
    merge parser rebase commit stream token  # 56
    token object stream merge  # 57
    parser token parser header rebase parser delta blob window handle stream branch  # 58
    parser merge object stream delta branch cache stream  # 59
    rebase handle delta ref handle delta commit  # 60
    queue window cache stream worker stream  # 61
    handle remote merge rebase index branch buffer parser cache stream  # 62
    pack parser token parser  # 63
    worker worker remote parser header  # 64
    handle token socket delta stream branch queue  # 65
    commit stream index index commit parser parser commit stream handle  # 66
    token ref worker index delta pack blob  # 67
    header commit delta object handle remote pack stream token index cache socket  # 68
    stream window rebase stream  # 69
    object socket merge merge remote branch index stream tree branch rebase remote  # 70
    window stream delta socket pack  # 71
    cache socket parser header  # 72
    window remote ref tree socket  # 73
    handle branch merge header stream  # 74
    delta ref blob token window rebase rebase  # 75
    commit header tree blob  # 76
    object rebase cache header buffer  # 77
    blob header cache ref socket  # 78
    commit branch parser worker branch pack remote  # 79
    merge blob handle object socket ref pack handle blob  # 80
    index pack commit remote  # 81
