    parser branch buffer merge ref queue socket remote buffer object pack  # 0
    branch commit ref delta branch delta branch socket  # 1
    window rebase stream token  # 2
    branch object ref buffer stream token stream commit commit blob socket  # 3
    remote rebase header stream cache pack delta ref index cache handle parser  # 4
    stream remote pack index handle merge stream pack header token  # 5
    buffer tree remote delta index ref  # 6
    socket object buffer blob worker  # 7
    window remote handle socket commit merge header token  # 8
    commit index window worker stream handle  # 9
    window object ref cache branch branch worker worker  # 10
    queue tree worker merge stream object handle parser  # 11
    index ref window header index token  # 12
    object cache tree cache blob delta remote  # 13
    queue buffer socket header token pack branch buffer rebase commit  # 14
    token stream stream pack merge buffer  # 15
    worker remote remote parser ref pack  # 16
    remote parser object index  # 17
    cache tree parser token  # 18
    index stream ref stream object delta  # 19
    index token handle header branch remote  # 20
    rebase token parser delta ref pack stream commit index parser delta tree  # 21
    handle parser commit remote pack  # 22
    window token handle cache blob handle blob  # 23
    remote window branch ref buffer token window  # 24
    parser parser tree stream socket header  # 25
    pack delta window merge window rebase worker blob merge parser  # 26
    parser index header queue buffer cache queue token queue  # 27
    header stream parser ref ref  # 28
    window parser commit commit commit socket rebase worker tree socket queue  # 29
