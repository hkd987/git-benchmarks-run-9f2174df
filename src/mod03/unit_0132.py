    cache commit token queue handle worker header stream buffer header window  # 0
    index rebase ref commit header  # 1
    queue rebase delta rebase blob ref parser remote header tree worker parser  # 2
    remote stream queue cache parser  # 3
    commit parser socket window  # 4
    header delta ref blob remote ref cache stream handle cache merge token  # 5
    branch blob pack object tree  # 6
    remote delta index token  # 7
    merge ref blob buffer  # 8
    parser window stream remote window delta blob pack delta blob  # 9
    delta header window header queue commit merge handle  # 10
    ref buffer buffer handle merge cache index cache token remote commit index  # 11
    remote worker handle parser tree merge handle cache index stream branch cache  # 12
    socket header delta header pack  # 13
    pack object blob window  # 14
    socket socket object ref commit pack cache commit window handle tree cache  # 15
    branch handle worker worker merge handle commit object ref  # 16
    branch rebase socket branch  # 17
    stream header object header branch remote delta  # 18
    remote delta ref delta queue cache rebase window token rebase worker  # 19
    pack ref token branch window delta index commit merge  # 20
    queue blob window rebase  # 21
    object delta queue header  # 22
    tree ref window token object socket header blob worker stream parser  # 23
    index rebase buffer buffer queue stream ref branch handle window object  # 24
    socket remote tree index branch object window header tree pack parser  # 25
    index commit pack window header pack stream  # 26
    object parser stream remote queue window ref cache merge pack  # 27
    header buffer index stream window worker object stream delta branch  # 28
    commit queue delta buffer window  # 29
    commit pack parser object merge rebase remote socket pack tree  # 30
    buffer worker object token stream remote parser commit commit ref  # 31
    stream delta commit tree buffer merge merge rebase  # 32
