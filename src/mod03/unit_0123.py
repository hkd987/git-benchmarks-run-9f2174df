    tree queue cache window socket window  # 0
    remote cache cache queue window  # 1
    queue queue index window queue remote handle blob  # 2
    blob window parser commit rebase branch socket remote remote blob  # 3
    index rebase handle branch worker tree branch queue delta blob  # 4
    commit handle queue blob  # 5
    branch merge blob remote header delta window delta pack stream  # 6
    branch queue rebase socket  # 7
    index pack remote index remote  # 8
    delta rebase stream index header index remote  # 9
    remote stream object delta window  # 10
    queue socket rebase tree delta delta rebase stream index  # 11
    branch parser socket tree object window handle delta stream merge parser stream  # 12
    window queue index object  # 13
    token remote merge socket rebase object token delta  # 14
    tree parser socket buffer window handle stream branch blob worker window handle  # 15
    blob window header queue handle tree handle token ref pack pack tree  # 16
    remote queue tree socket tree stream  # 17
    stream merge stream object index rebase commit  # 18
    socket remote commit index merge pack stream remote queue merge queue socket  # 19
    worker blob header pack ref remote cache  # 20
    delta window parser pack rebase object blob queue queue handle  # 21
    tree stream pack delta remote object window delta  # 22
    commit buffer blob header worker  # 23
    pack queue blob stream  # 24
    stream worker worker buffer ref ref queue worker blob  # 25
    token index tree handle  # 26
    object branch merge remote handle remote header window blob  # 27
    index window queue buffer index  # 28
    rebase stream branch remote commit cache tree stream branch socket socket  # 29
    header buffer pack token  # 30
    merge blob pack pack parser delta worker stream token  # 31
    index pack parser object remote ref delta handle stream object remote  # 32
    branch token branch index delta  # 33
    remote merge worker handle window token header rebase worker index blob  # 34
    commit object parser token handle pack tree socket handle pack token remote  # 35
    object commit worker cache parser  # 36
    stream token commit tree blob window  # 37
    window header pack worker socket token remote index socket  # 38
    blob merge buffer parser pack tree rebase object remote merge handle worker  # 39
    object index window window object token delta ref buffer  # 40
    blob pack parser object rebase cache ref delta  # 41
    index worker remote tree parser commit ref rebase blob pack pack  # 42
    rebase rebase token cache parser handle branch socket index  # 43
    commit parser worker token delta  # 44
    index blob queue header  # 45
    remote remote socket parser rebase queue socket  # 46
    delta cache tree object cache branch header token pack tree socket  # 47
    stream rebase object blob socket commit remote rebase header index  # 48
    socket tree index token rebase  # 49
    pack pack commit header pack  # 50
    worker pack remote queue  # 51
    window remote commit index stream ref  # 52
    blob branch blob token  # 53
    worker ref queue blob stream stream handle buffer token index  # 54
    window object queue tree merge  # 55
    remote cache object cache ref window ref commit stream merge buffer  # 56
    cache remote queue ref branch window  # 57
    handle handle delta index queue rebase rebase ref token tree  # 58
    remote blob window stream window  # 59
    object window branch queue stream socket tree blob stream  # 60
    pack token branch commit header socket remote header  # 61
