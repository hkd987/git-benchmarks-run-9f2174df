    socket window rebase commit rebase  # 0
    window header buffer stream  # 1
    parser parser parser branch buffer branch  # 2
    parser object socket queue branch remote queue window header parser  # 3
    delta commit tree token header merge object cache stream rebase buffer  # 4
    window index delta parser header queue parser blob  # 5
    rebase commit merge tree queue  # 6
    header buffer parser parser handle parser socket tree tree  # 7
    index window rebase remote handle window pack tree token worker  # 8
    pack header object blob pack token remote blob  # 9
    branch handle queue ref branch  # 10
    object pack tree tree stream token parser tree remote buffer  # 11
    index object index rebase stream pack stream token window ref  # 12
    cache merge remote stream rebase stream parser window cache parser ref  # 13
    stream buffer rebase window queue blob merge commit header handle buffer  # 14
    token stream ref socket token  # 15
    socket stream header rebase delta header pack cache  # 16
    stream branch delta object delta merge parser merge  # 17
    socket queue object remote tree socket commit  # 18
    commit worker merge rebase buffer token cache handle window tree cache blob  # 19
    queue stream delta handle  # 20
    worker header queue header parser remote buffer cache ref  # 21
    blob buffer parser stream handle object blob queue index commit object  # 22
    queue commit ref delta index window branch index remote ref commit parser  # 23
    merge blob window ref  # 24
    delta worker header header rebase cache branch  # 25
    ref remote rebase branch stream parser object  # 26
    merge header cache branch delta  # 27
    worker parser token blob socket blob object handle object merge merge  # 28
    header queue handle tree handle pack  # 29
    worker pack rebase tree queue remote delta handle branch buffer header worker  # 30
    delta worker remote handle ref buffer  # 31
    object worker remote object merge merge merge parser buffer index header  # 32
    parser remote delta socket ref  # 33
    window remote rebase pack worker worker remote  # 34
    header ref remote object parser window header buffer  # 35
    token cache worker tree object  # 36
    delta cache header merge buffer index header buffer  # 37
    ref stream worker queue branch handle tree cache  # 38
    window buffer rebase branch branch handle merge commit  # 39
    worker buffer worker pack commit  # 40
    window object parser parser window window  # 41
    remote remote token stream queue pack remote index object worker index branch  # 42
    stream header header handle pack header header remote pack object header branch  # 43
    window stream token ref parser queue commit stream  # 44
    buffer header stream blob rebase stream pack remote delta  # 45
    worker socket parser tree token tree  # 46
    index index tree queue handle merge pack socket worker  # 47
    socket buffer rebase pack cache ref cache queue  # 48
    ref branch buffer parser  # 49
    merge handle stream delta handle blob parser buffer remote  # 50
    branch merge remote worker remote buffer  # 51
    tree handle token remote remote ref cache window worker commit  # 52
    header delta remote stream socket remote branch index commit  # 53
    socket remote merge object socket ref handle token worker buffer parser  # 54
    ref remote merge merge ref remote  # 55
    worker branch token handle object  # 56
    queue merge ref blob ref socket index  # 57
    queue handle merge object stream cache pack parser cache stream  # 58
    tree pack cache rebase  # 59
    handle header worker object buffer blob worker parser token delta cache token  # 60
    parser remote window ref worker branch socket merge merge  # 61
    window object rebase buffer  # 62
    handle remote commit index buffer delta index ref remote  # 63
    handle object handle delta cache ref handle cache cache queue  # 64
    rebase stream cache tree parser merge worker merge  # 65
    handle stream index remote header commit cache remote pack buffer  # 66
    cache ref header rebase worker stream  # 67
    token token queue commit commit handle  # 68
    stream stream handle ref buffer delta remote handle blob window  # 69
    window handle blob token tree delta pack queue  # 70
    header socket remote buffer delta index queue  # 71
    handle branch remote queue stream handle buffer worker worker branch object  # 0
    ref queue pack object buffer handle socket branch delta index branch  # 1
    queue rebase token queue tree tree token  # 2
    header cache commit pack cache  # 3
    commit parser header cache tree token cache branch branch token socket index  # 4
    ref parser handle blob parser merge  # 5
    handle token token ref queue buffer stream  # 6
    queue blob ref tree branch tree index parser socket  # 7
    branch buffer parser remote index worker branch delta blob parser cache  # 8
