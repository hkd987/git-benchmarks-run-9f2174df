    remote index object remote header header header  # 0
    parser queue index merge window rebase token branch  # 1
    buffer handle blob index commit header branch merge parser queue  # 2
    handle branch branch pack commit header ref worker rebase handle header window  # 3
    branch delta cache handle window branch pack branch stream window header window  # 4
    delta ref header parser buffer handle token remote pack ref commit  # 5
    rebase index pack token  # 6
    merge parser stream parser socket cache cache buffer pack  # 7
    remote token rebase parser delta pack window pack  # 8
    handle blob branch commit blob stream buffer window merge  # 9
    pack buffer merge stream ref cache token remote  # 10
    remote queue stream buffer stream parser  # 11
    remote branch stream worker index header ref ref handle remote  # 12
    socket token cache buffer branch  # 13
    socket handle index queue remote token  # 14
    header delta handle object parser  # 15
    token parser index stream index worker blob queue stream tree delta  # 16
    index stream object merge window delta merge blob index  # 17
    index handle commit pack commit handle commit pack header cache  # 18
    merge queue cache queue remote handle pack parser tree  # 19
    blob queue parser parser index cache token header buffer delta rebase index  # 20
    blob pack cache rebase  # 21
    remote branch index object  # 22
    buffer parser merge index merge remote branch pack remote pack  # 23
    ref delta handle index delta socket commit window delta token  # 24
    commit delta object merge  # 25
    index remote token blob branch socket blob  # 26
    branch ref branch handle handle handle worker commit index branch  # 27
    parser object queue delta remote buffer pack stream blob  # 28
    window rebase header index ref worker  # 29
    remote rebase handle object  # 30
    worker ref worker tree ref socket parser delta socket  # 31
    ref token token pack worker socket  # 32
    remote stream socket object commit ref token ref header stream  # 33
    merge remote cache rebase parser stream  # 34
    ref window window buffer pack branch object delta  # 35
    index handle commit branch window  # 36
    queue socket merge handle window window cache tree  # 0
    parser buffer branch merge branch stream pack index socket  # 1
    buffer merge socket header object  # 2
    header delta handle cache queue parser  # 3
    header ref delta worker token stream buffer rebase  # 4
    worker tree pack header socket worker  # 5
    handle tree worker pack commit commit header  # 6
    index rebase commit token index blob object parser worker  # 7
    header buffer parser socket  # 8
    cache blob blob handle branch merge merge tree buffer merge merge header  # 9
    tree stream socket remote queue tree branch object  # 10
    merge stream header cache  # 11
