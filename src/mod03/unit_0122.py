    stream object socket header index remote object stream window  # 0
    merge object handle handle  # 1
    cache branch merge socket cache parser  # 2
    token header queue delta  # 3
    handle socket merge header socket queue  # 4
    buffer worker buffer delta cache tree  # 5
    token window tree queue index worker window object cache branch commit  # 6
    window blob header ref pack object remote  # 7
    remote header blob rebase remote blob blob handle blob socket  # 8
    index delta handle index  # 9
    index pack remote stream commit  # 10
    ref header stream rebase commit cache handle socket  # 11
    remote pack header delta  # 12
    cache handle stream branch queue handle pack rebase merge remote blob  # 13
    branch window queue header token token socket index queue pack  # 14
    socket branch merge object  # 15
    queue token rebase branch index stream header header commit tree merge handle  # 16
    header worker object branch commit object handle  # 17
    index rebase token object window delta cache  # 18
    remote handle cache stream cache blob merge handle pack blob tree buffer  # 19
    window remote stream socket stream pack commit cache commit socket queue ref  # 20
    stream buffer worker parser merge ref worker  # 21
    branch delta tree socket  # 22
    tree merge rebase index merge remote cache worker token window header  # 23
    object delta commit object stream worker pack  # 24
    token token parser blob delta  # 25
    socket queue blob buffer queue token buffer pack  # 26
    window window ref window tree handle window object tree pack  # 27
    merge handle branch cache queue cache blob index  # 28
    queue header socket index object token stream delta worker branch cache  # 29
    worker branch commit branch  # 30
    rebase rebase tree header socket cache worker remote parser  # 31
    merge header commit object handle merge branch parser  # 32
    queue token remote window delta worker buffer branch merge header index  # 33
    pack cache rebase blob merge merge header buffer rebase queue header  # 34
    ref pack object buffer blob object index tree rebase index  # 35
    token commit ref stream handle stream stream  # 36
    blob commit buffer tree handle branch merge tree pack branch commit delta  # 37
    branch commit worker remote merge remote merge branch tree  # 38
    branch parser cache parser remote parser commit queue ref branch cache stream  # 39
    branch remote handle pack delta cache parser ref index remote tree  # 40
    socket socket cache remote delta pack  # 41
    object delta buffer index  # 42
    merge queue handle pack  # 43
    rebase header delta worker header tree remote rebase blob ref stream worker  # 44
