    token buffer pack merge buffer ref header parser queue  # 0
    queue stream parser delta blob tree  # 1
    stream ref ref header index  # 2
    window parser rebase merge branch cache blob token cache stream cache  # 3
    object rebase cache header delta worker tree  # 4
    branch window pack ref worker pack  # 5
    ref stream window merge buffer remote object branch delta  # 6
    index object index blob worker branch window window socket handle cache handle  # 7
    queue token blob remote token object index index handle tree window  # 8
    delta delta remote stream token tree object pack parser  # 9
    object index commit pack object worker blob  # 10
    remote tree cache index worker merge rebase parser token  # 11
    ref handle cache worker queue parser header object  # 12
    branch buffer window parser pack rebase commit queue index branch ref handle  # 13
    handle pack socket cache worker cache  # 14
    cache buffer blob parser ref pack window remote ref delta worker object  # 15
    cache cache stream stream remote header  # 16
    remote tree delta ref merge header token  # 17
    index index pack object commit  # 18
    merge blob socket ref header index  # 19
    socket branch object stream  # 20
    queue ref buffer handle  # 21
    window stream merge cache object  # 22
    token object merge cache blob ref  # 23
    rebase handle merge queue socket rebase blob socket index socket header parser  # 24
    blob rebase pack merge window remote socket queue branch handle tree header  # 25
    worker queue parser window queue  # 26
    socket delta token buffer  # 27
    parser ref window object stream ref commit branch  # 28
    window socket index delta cache cache header stream header  # 29
    header window remote index stream rebase branch merge remote cache  # 30
    token cache tree header stream merge worker window stream merge  # 31
