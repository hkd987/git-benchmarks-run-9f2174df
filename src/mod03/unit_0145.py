    stream cache window ref  # 0
    ref delta cache branch delta ref branch handle index cache  # 1
    delta object token handle header cache  # 2
    object delta parser pack index cache blob header cache parser  # 3
    queue handle window commit queue handle tree merge token  # 4
    queue window merge cache index delta  # 5
    rebase cache buffer stream cache worker stream ref remote  # 6
    parser cache header buffer worker stream blob index delta handle  # 7
    blob blob window rebase remote merge queue remote blob  # 8
    socket queue cache stream queue blob parser handle pack remote tree  # 9
    blob worker index delta ref header cache  # 10
    queue pack blob merge stream merge merge parser token  # 11
    rebase remote index ref header worker buffer  # 12
    merge handle remote rebase index rebase index socket branch  # 13
    header buffer worker merge merge queue delta ref  # 14
    delta window token cache queue ref index remote header tree  # 15
    handle object merge merge socket socket rebase blob  # 16
    object header index token queue index stream remote socket tree blob buffer  # 17
    merge cache worker ref token window tree remote remote header  # 18
    stream rebase object cache remote pack  # 19
    delta buffer index stream buffer merge blob commit cache index object  # 20
    ref buffer pack rebase index cache tree pack cache ref  # 21
    pack delta remote commit queue merge  # 22
    commit parser merge tree  # 23
    worker branch cache remote socket header ref  # 24
    token worker index socket remote parser worker index header queue socket  # 25
    token delta ref queue  # 26
    tree buffer branch socket delta pack pack rebase header branch socket  # 27
    object tree pack handle branch commit commit queue branch cache  # 28
    queue merge delta handle header buffer merge ref header remote  # 29
    token index delta header  # 30
    header rebase buffer object commit blob merge pack commit  # 31
    ref delta ref pack rebase blob token ref delta handle  # 32
    buffer remote tree remote blob  # 33
    window worker socket commit index pack branch header stream delta  # 34
    parser stream commit worker tree buffer ref stream  # 35
    window queue socket branch delta buffer  # 36
    rebase stream queue worker socket  # 37
    token token index blob delta parser window buffer cache header token pack  # 38
    token index stream cache header  # 39
    pack token parser remote window handle handle delta  # 40
    rebase queue pack buffer commit  # 41
    window tree delta ref  # 42
    header tree parser delta stream pack remote  # 43
    rebase parser cache window delta ref stream ref object object rebase  # 44
    merge pack header header queue tree token buffer queue commit socket socket  # 45
    socket merge object merge header header parser  # 46
    worker blob rebase pack commit  # 47
    queue stream index ref socket  # 48
    token header rebase remote object socket rebase tree handle buffer tree  # 49
    stream rebase header header branch branch  # 50
    blob commit buffer queue delta stream  # 51
    window parser queue branch token commit pack handle queue blob blob cache  # 52
    delta stream delta queue window remote socket tree handle stream index  # 53
    merge remote queue cache worker window queue handle  # 54
    header header worker branch cache token stream cache remote token delta stream  # 55
    header tree blob remote window stream  # 56
    worker handle rebase handle object delta blob socket rebase header  # 57
    object remote tree queue token branch  # 58
    socket branch branch token pack object  # 59
    handle buffer object cache commit tree remote remote ref  # 60
    delta handle handle cache handle  # 61
    branch handle ref window parser stream remote tree ref object ref  # 62
    token branch cache header rebase cache  # 63
    buffer remote parser blob pack tree object merge stream queue  # 64
    blob header remote object commit header socket index  # 65
    object header cache index window parser index cache stream  # 66
    socket object handle socket remote handle queue handle stream header blob  # 67
    delta merge cache worker blob blob parser  # 68
    branch object index ref header object commit index  # 69
    stream buffer remote pack handle socket socket socket parser handle  # 70
    socket token rebase delta handle delta socket  # 71
    blob branch header remote  # 72
    socket cache index worker window rebase merge index tree tree header worker  # 73
    parser worker token ref rebase header token token blob commit parser rebase  # 74
    buffer token buffer parser queue queue branch blob header index  # 75
    buffer queue window header branch parser parser  # 76
    branch handle header ref remote worker merge index rebase  # 77
