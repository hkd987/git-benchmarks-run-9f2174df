    object token stream header buffer parser blob rebase rebase object  # 0
    socket delta ref delta stream ref rebase handle socket cache object parser  # 1
    window branch handle window socket blob branch ref object ref  # 2
    rebase socket queue index remote buffer  # 3
    buffer commit ref queue index queue blob  # 4
    commit worker blob buffer index pack  # 5
    worker parser window ref socket cache ref  # 6
    rebase ref index merge window  # 7
    cache header handle ref branch index pack index rebase commit commit queue  # 8
    stream token commit blob  # 9
    commit delta delta handle blob stream blob  # 10
    tree handle stream header object token rebase worker rebase parser window  # 11
    window remote index commit object commit branch pack queue  # 12
    token branch worker queue stream branch rebase  # 13
    window object window blob pack object queue blob  # 14
    buffer queue remote cache socket index merge socket  # 15
    tree parser pack object worker  # 16
    commit pack merge handle header queue rebase merge buffer blob  # 17
    blob rebase header index delta branch queue branch ref  # 18
    stream parser branch buffer commit pack index tree  # 19
    queue handle queue commit token  # 20
    tree commit ref socket branch token  # 21
    ref commit cache branch worker merge remote parser worker worker  # 22
    handle remote ref window header branch worker queue remote  # 23
    header index handle index tree delta window  # 24
    cache window remote merge rebase branch blob  # 25
    delta socket socket blob merge delta socket rebase  # 26
    commit stream socket rebase socket merge token remote pack index stream ref  # 27
    parser ref parser remote worker queue pack object queue window merge  # 28
    branch window cache remote object header remote ref delta queue buffer  # 29
    queue socket parser stream  # 30
    object branch pack header index worker delta remote delta blob queue commit  # 31
    remote pack worker rebase commit commit socket  # 32
    object window rebase queue object index queue index remote socket  # 33
    socket cache handle worker pack branch branch merge window token delta delta  # 34
    branch commit token commit worker worker stream object queue  # 35
    tree branch rebase index  # 36
    blob cache rebase index queue  # 37
    blob rebase worker commit parser stream  # 38
    worker delta token handle stream commit branch object header stream commit  # 39
    remote tree pack blob stream queue remote ref  # 40
    merge header ref blob stream buffer cache  # 41
    stream tree delta ref window queue delta blob window  # 42
    remote commit index queue window queue  # 43
    rebase tree worker parser rebase delta index pack blob  # 44
    pack ref socket rebase socket  # 45
    window remote branch socket branch parser stream commit parser  # 46
    pack parser window stream stream remote object worker blob  # 47
    handle blob socket pack header handle blob pack parser  # 48
    buffer header ref remote  # 49
    commit worker pack worker tree  # 50
    pack cache index tree commit  # 51
    socket pack buffer merge cache branch header  # 52
    delta cache index parser window  # 53
    header remote stream blob stream buffer header stream delta merge token  # 54
