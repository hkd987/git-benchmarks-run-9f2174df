    token pack blob tree blob token  # 0
    window parser socket merge window ref merge ref  # 1
    rebase merge header commit  # 2
    index index delta index merge tree remote  # 3
    worker rebase stream tree buffer buffer remote parser parser token  # 4
    handle pack tree branch remote worker delta delta blob header object queue  # 5
    branch branch header header  # 6
    rebase parser delta index  # 7
    worker tree buffer window worker  # 8
    queue header delta index buffer  # 9
    merge delta merge tree pack object  # 10
    header merge socket blob blob handle merge stream  # 11
    index ref token blob rebase handle ref  # 12
    tree tree socket cache cache pack rebase branch  # 13
    token rebase branch index tree branch tree handle window delta cache  # 14
    header branch object blob  # 15
    handle ref parser cache blob merge parser header queue tree  # 16
    commit stream worker remote tree merge header  # 17
    token commit branch handle window socket queue stream commit header socket  # 18
    socket blob header remote parser pack merge socket buffer socket pack remote  # 19
    pack buffer blob blob window delta window object header  # 20
    window cache stream ref ref  # 21
    commit parser worker remote delta parser commit parser rebase window  # 22
    delta blob queue pack buffer delta stream blob ref window window  # 23
    object merge socket object token index parser ref  # 24
    object object buffer blob delta  # 25
    buffer ref blob parser merge window delta blob  # 26
    delta window delta tree queue pack  # 27
    remote blob buffer stream ref cache  # 28
    queue window parser rebase parser header handle  # 29
    token window socket token window token queue ref cache rebase tree cache  # 30
    pack header tree parser handle ref commit  # 31
    rebase tree queue tree object socket socket queue window stream worker stream  # 32
    worker rebase branch rebase commit  # 33
    rebase remote branch buffer worker merge delta socket socket  # 34
    remote tree merge token handle index socket  # 35
    delta merge rebase handle parser commit buffer header object index ref  # 36
    parser blob token queue queue  # 37
    ref rebase handle blob buffer tree commit  # 38
    commit cache buffer token blob  # 39
    socket tree merge delta queue window branch  # 40
    branch buffer token pack socket stream index index branch socket buffer worker  # 41
    commit tree pack token pack handle  # 42
    object index cache pack delta pack parser pack  # 0
    index parser branch header delta worker object cache  # 1
    blob commit worker ref commit handle token queue index worker  # 2
    header blob buffer window object handle branch delta index merge  # 3
    cache token index worker token  # 4
    delta stream ref commit buffer ref  # 5
    rebase branch ref merge socket ref remote  # 6
    token delta merge window blob  # 7
    window buffer pack rebase object cache parser tree remote window pack header  # 8
    header handle delta header delta delta ref header  # 9
    commit window queue stream tree  # 10
