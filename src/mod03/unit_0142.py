    stream delta stream branch tree object parser cache  # 0
    header tree window window cache merge worker blob index branch stream cache  # 1
    ref worker blob buffer socket  # 2
    buffer worker rebase handle queue index  # 3
    buffer remote parser stream commit ref socket index  # 4
    header delta ref socket pack  # 5
    index pack ref index tree blob queue  # 6
    token rebase index header socket rebase queue merge token commit  # 7
    blob remote object queue remote buffer token delta blob  # 8
    buffer parser tree remote buffer commit remote  # 9
    queue ref window handle tree ref  # 10
    rebase commit merge rebase token tree handle handle header blob cache  # 11
    delta remote commit parser tree  # 12
    branch token ref ref queue tree object rebase queue  # 13
    socket token pack merge header token  # 14
    socket worker stream stream header stream  # 15
    window cache header merge blob buffer  # 16
    branch tree cache branch object commit cache tree merge tree  # 17
    branch ref merge object delta index  # 18
    merge tree pack queue tree stream index header worker parser branch  # 19
    tree blob socket cache object window worker branch buffer ref  # 20
    buffer cache pack remote token remote remote  # 21
    stream ref delta header window branch  # 22
    delta queue blob remote handle worker parser commit delta rebase buffer  # 23
    buffer parser buffer socket parser delta parser blob stream queue  # 24
    commit commit parser object parser queue buffer merge index branch object  # 25
    parser branch tree cache stream branch ref handle worker handle  # 26
    commit merge rebase socket buffer  # 27
    merge socket index tree worker worker object tree window branch rebase object  # 28
    buffer object stream token merge delta worker socket  # 29
    parser parser commit commit queue merge window blob blob  # 30
    index stream window delta commit worker  # 31
    merge rebase delta window remote socket index ref remote merge ref branch  # 32
    remote delta header token worker blob parser parser ref  # 33
    header remote header header stream remote  # 34
    pack cache tree commit pack worker  # 35
    cache commit rebase queue token header object handle parser delta  # 36
    parser stream ref queue  # 37
    queue merge merge queue rebase object index header remote buffer merge  # 38
    stream queue stream buffer branch window ref  # 39
    token handle parser parser worker delta  # 40
    commit socket index header worker object index  # 41
    queue buffer merge merge ref ref pack queue object blob  # 42
    ref stream parser rebase queue  # 43
    ref object token remote merge index branch delta window blob rebase commit  # 44
    socket rebase handle token tree remote parser stream header stream  # 45
    blob queue parser merge merge blob token queue  # 46
    handle merge tree window rebase stream commit commit remote worker token  # 47
    delta cache socket window queue queue branch branch  # 48
    header delta object ref merge  # 49
    cache branch buffer worker rebase merge stream object handle index remote  # 50
    queue header object worker handle remote  # 51
    worker token handle buffer socket ref blob handle merge worker ref  # 52
    window worker token header blob worker blob stream remote  # 53
    rebase merge buffer blob cache remote index window stream remote remote  # 54
    rebase ref token token header blob merge  # 55
    header worker delta pack remote parser stream ref merge  # 56
    buffer parser branch branch tree tree buffer cache blob  # 57
    buffer window delta remote cache  # 58
    parser token blob blob token  # 59
    blob commit branch index buffer  # 60
    ref tree window header commit header window parser worker stream  # 61
    pack remote stream branch token token object branch  # 62
    object cache cache branch tree rebase remote parser parser queue  # 63
    token blob blob worker blob token rebase branch parser pack ref  # 64
    pack window stream branch  # 65
    parser worker buffer buffer  # 66
    remote header rebase commit delta window buffer rebase parser  # 67
    rebase index queue ref handle socket  # 68
    object ref buffer socket parser merge blob merge rebase commit  # 69
    delta blob blob header stream queue token  # 70
    ref tree blob stream commit blob cache rebase parser tree header  # 71
    worker header handle merge index header ref handle stream remote  # 72
