    pack worker header pack ref handle handle buffer merge cache merge  # 0
    window socket rebase commit worker merge  # 1
    worker stream rebase socket worker parser remote buffer queue merge token  # 2
    tree cache ref rebase remote parser  # 3
    object merge token ref blob tree parser branch blob socket  # 4
    cache token queue header cache remote object socket worker pack  # 5
    ref window ref socket index pack worker  # 6
    branch socket cache window ref queue cache queue remote buffer  # 7
    remote remote blob merge worker  # 8
    merge stream worker delta queue ref  # 9
    queue commit queue token branch delta ref ref socket socket buffer  # 10
    queue socket blob window delta parser blob cache remote cache parser token  # 11
    socket pack worker delta ref tree remote worker remote merge  # 12
    object token rebase ref socket pack queue handle buffer ref rebase remote  # 13
    blob token parser header handle socket index blob token pack  # 14
    delta branch blob delta remote buffer queue buffer cache blob ref blob  # 15
    window commit queue window worker queue  # 16
    handle pack branch worker index header branch blob merge  # 17
    token parser socket header token index  # 18
    pack tree object parser socket parser  # 19
    cache socket handle header cache worker  # 20
    branch blob stream header merge pack window delta delta pack  # 21
    blob rebase worker queue window remote object token merge cache tree pack  # 22
    merge delta blob rebase  # 23
    index handle branch socket stream  # 24
    commit delta buffer blob buffer parser buffer buffer delta socket  # 25
    object window delta ref  # 26
    delta handle object queue token buffer  # 27
    rebase object worker remote parser branch index ref branch window parser branch  # 28
    header branch index queue window object socket ref delta blob branch parser  # 29
    parser window buffer window stream branch tree  # 30
    stream ref pack branch ref  # 31
    token token pack merge ref handle cache worker queue stream  # 32
    header cache branch tree  # 33
    blob window header stream remote worker ref socket buffer queue  # 34
    commit queue rebase pack buffer ref worker pack parser token  # 35
    handle rebase stream merge delta ref delta commit worker ref handle stream  # 36
    window stream buffer commit worker  # 37
    branch tree socket handle pack ref blob ref  # 38
    socket header blob index queue token buffer  # 39
    header blob rebase socket  # 40
    tree remote merge branch delta buffer stream buffer  # 41
    window remote parser window branch socket  # 42
    worker tree window merge object blob buffer object  # 43
    worker parser token index window remote remote  # 44
    buffer queue object parser handle pack tree delta branch  # 45
    ref queue merge blob cache index tree  # 46
    merge socket worker pack window stream stream pack object  # 47
    merge remote tree commit parser branch header commit handle index  # 48
