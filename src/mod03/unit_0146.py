    header parser token buffer parser  # 0
    ref tree header rebase object delta buffer cache ref header commit token  # 1
    commit tree branch ref blob  # 2
    branch socket branch window blob parser token pack commit token merge  # 3
    tree worker branch tree  # 4
    ref remote window tree blob delta rebase handle  # 5
    object handle socket queue  # 6
    rebase cache rebase worker stream object blob header token rebase header cache  # 7
    delta ref buffer ref index blob handle delta blob cache  # 8
    blob handle window index pack header  # 9
    worker object buffer rebase object  # 10
    parser blob cache index ref remote queue index remote stream parser  # 11
    index delta socket worker header stream delta merge merge cache stream  # 12
    remote cache worker socket merge window merge tree header  # 13
    window queue socket remote pack socket delta  # 14
    merge parser header branch cache branch remote worker pack  # 15
    delta buffer rebase socket buffer socket blob worker handle parser merge handle  # 16
    merge blob queue window merge branch commit tree stream queue stream ref  # 17
    index handle blob window merge branch  # 18
    commit socket cache socket  # 19
    buffer ref blob queue tree parser branch  # 20
    blob commit tree commit index handle object object  # 21
    token blob token pack window  # 22
    branch parser rebase queue window branch  # 23
    branch header tree blob window parser parser token buffer  # 24
    ref object rebase socket blob index worker blob branch worker  # 25
    blob merge blob ref  # 26
    blob index pack buffer parser worker window tree worker  # 27
    merge buffer buffer stream queue tree socket socket handle rebase index  # 28
    socket object index token stream  # 29
    cache rebase tree window handle stream queue handle tree  # 30
    rebase blob socket commit handle header worker  # 31
    cache stream token queue window cache delta  # 32
    stream header worker header stream socket buffer blob header delta branch  # 0
    window queue queue cache queue socket tree stream socket rebase tree buffer  # 1
    worker branch tree header  # 2
    remote blob blob branch worker delta  # 3
    header ref buffer stream index index  # 0
    token remote pack delta tree merge remote object header pack  # 1
    merge delta buffer object tree delta object worker stream delta delta  # 2
    rebase cache parser stream index object parser object  # 3
    handle window parser ref remote pack worker  # 4
    token window stream blob handle branch token remote cache  # 5
    buffer ref buffer index branch index header  # 6
    window tree cache rebase branch tree blob queue branch pack blob queue  # 7
    branch worker handle pack pack handle blob blob  # 8
