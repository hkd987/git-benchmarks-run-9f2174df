    tree branch socket index blob header socket index  # 0
    delta object window rebase remote handle handle object merge commit  # 1
    buffer buffer blob merge index cache  # 2
    delta cache token worker pack window merge rebase  # 3
    cache token token cache branch parser cache buffer parser  # 4
    index merge blob delta header index handle stream  # 5
    ref buffer index delta rebase branch socket  # 6
    tree rebase window window delta window remote tree parser remote header  # 7
    branch queue token header stream rebase  # 8
    buffer commit cache commit index parser blob window pack tree rebase  # 9
    blob buffer header worker rebase merge handle token header tree window  # 10
    branch index object window object branch  # 11
    buffer blob cache object worker pack branch tree  # 12
    delta remote handle index buffer buffer index queue delta  # 13
    stream worker header ref remote branch header remote tree ref ref  # 14
    branch header ref worker remote parser rebase  # 15
    buffer buffer merge rebase stream object worker header remote delta worker  # 16
    cache merge merge stream window delta cache  # 17
    index parser index object tree queue  # 18
    cache index delta queue socket  # 19
    parser parser ref merge remote merge header index worker parser pack tree  # 20
    object merge handle worker index header socket  # 21
    merge window queue stream header handle queue merge remote  # 22
    blob socket handle pack socket branch header token worker buffer blob  # 23
    delta parser merge socket rebase  # 24
    tree header index branch  # 25
    queue tree pack object index handle worker  # 26
    blob remote queue branch index handle buffer blob worker delta object  # 27
    token blob rebase parser parser handle queue queue stream rebase index  # 28
    commit rebase header tree index delta queue socket delta buffer buffer  # 29
    commit worker stream blob parser commit cache parser index pack parser  # 30
    commit token cache merge remote parser handle blob tree commit rebase socket  # 31
    header header delta pack header buffer stream window queue buffer cache  # 32
    delta token delta handle remote window  # 33
    remote rebase blob remote token  # 0
    queue worker token queue socket token delta delta remote socket  # 1
    commit token window commit token pack tree parser commit window handle  # 2
    merge remote tree commit object blob header worker index worker  # 3
    index ref ref tree  # 4
    ref rebase handle commit pack cache delta pack  # 5
    blob blob merge index header merge index tree pack handle  # 6
    cache object socket tree queue pack rebase parser parser  # 7
    socket queue header token window  # 8
    token pack commit object  # 9
    delta delta cache worker tree handle  # 10
    ref blob cache handle handle queue blob worker commit remote parser rebase  # 11
    rebase handle cache queue header worker window window buffer tree queue stream  # 12
    parser tree window window token object delta window  # 13
    socket handle object object worker  # 14
