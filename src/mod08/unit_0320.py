    remote token stream socket stream ref rebase index handle cache queue  # 0
    index tree rebase handle delta merge socket cache pack cache tree  # 1
    delta worker token socket branch worker parser worker  # 2
    index index buffer queue stream token commit pack cache remote tree  # 3
    blob tree socket token commit ref remote pack delta delta token buffer  # 4
    buffer stream stream object worker index cache remote object  # 5
    socket pack remote token parser header branch socket  # 6
    worker delta branch stream blob blob  # 7
    socket blob header stream object queue delta ref index  # 8
    worker token commit header stream window remote blob commit socket window  # 9
    merge tree delta handle cache  # 10
    cache buffer remote queue cache queue cache  # 11
    pack rebase object merge object socket handle delta ref window branch socket  # 12
    window tree stream token blob remote object  # 13
    remote window branch handle commit rebase stream worker stream  # 14
    ref window window object buffer commit ref blob merge  # 15
    buffer header remote stream blob delta socket cache  # 16
    blob tree worker merge commit ref  # 17
    parser queue queue token remote object  # 18
    pack branch pack worker index rebase pack  # 19
    branch token cache worker pack  # 20
    tree queue header ref parser commit commit index blob handle  # 21
    socket buffer header tree window ref  # 22
    branch stream parser handle queue token object  # 23
    rebase queue tree commit blob  # 24
    pack branch commit worker queue header ref  # 25
    parser window parser merge socket window tree object  # 26
    window buffer queue pack  # 27
    queue ref cache remote merge stream window tree  # 28
    ref commit commit remote  # 29
    handle commit header token header index  # 30
    blob stream worker socket index cache worker branch buffer window parser queue  # 31
    merge parser tree pack ref worker merge blob  # 32
    rebase merge queue tree object  # 33
    token tree buffer buffer handle remote parser remote pack socket socket  # 34
    merge delta branch cache pack header parser  # 35
    blob stream buffer pack index socket remote  # 36
    merge branch merge rebase  # 37
    cache cache window pack remote stream handle  # 38
    index delta object commit merge object merge branch commit  # 39
    worker delta handle handle socket remote handle tree blob handle  # 40
    merge buffer cache stream  # 41
    delta window ref branch queue cache branch  # 42
    buffer blob ref buffer queue parser commit parser branch rebase object cache  # 43
    queue queue handle cache worker socket blob buffer merge socket delta  # 44
    header index parser queue commit cache  # 45
    cache object cache socket rebase cache socket queue  # 46
    pack queue object pack remote  # 47
    handle buffer ref header commit handle remote  # 48
    ref object branch merge socket header ref  # 49
    token buffer delta commit queue delta window parser delta  # 50
    worker handle ref rebase handle header delta commit  # 51
    pack queue branch delta object handle tree header worker branch  # 52
    merge branch window token commit delta buffer worker buffer token  # 53
    worker rebase object socket rebase merge stream merge window  # 54
    socket token cache pack  # 55
    buffer tree token branch token ref  # 56
    queue delta cache pack buffer index index parser index cache  # 57
    rebase commit token tree socket branch ref rebase socket parser cache index  # 58
    object token parser object buffer object branch  # 59
    blob merge tree stream stream merge handle merge index object handle socket  # 60
    cache object stream handle index delta  # 61
    window buffer rebase parser stream blob worker worker worker  # 62
    handle stream queue merge remote pack remote remote  # 63
    worker tree merge token worker  # 64
    object worker merge stream pack ref branch object handle rebase  # 65
    ref stream ref delta merge ref rebase header merge pack  # 66
    tree commit cache ref pack buffer tree  # 67
    object buffer commit socket object window delta buffer branch ref rebase  # 68
    tree token ref token rebase commit remote header  # 69
    queue merge blob tree merge blob  # 70
