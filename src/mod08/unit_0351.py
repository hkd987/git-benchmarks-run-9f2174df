    delta branch ref tree queue ref pack  # 0
    buffer cache token header  # 1
    cache pack worker pack worker header blob ref index  # 2
    remote rebase merge rebase merge worker queue worker window ref  # 3
    tree delta branch object  # 4
    worker index buffer header delta  # 5
    window index pack index delta window  # 6
    ref socket delta window queue remote token queue header remote commit  # 7
    branch stream worker remote branch ref index token pack stream  # 8
    rebase delta object parser blob index cache  # 9
    commit window merge window index queue  # 10
    handle socket remote token pack  # 11
    worker remote ref socket tree index stream branch socket branch stream  # 12
    stream queue socket handle index cache remote index buffer merge cache header  # 13
    handle buffer remote delta  # 14
    parser merge worker queue header parser buffer object tree stream window buffer  # 15
    commit worker commit socket token pack parser socket ref cache token  # 16
    branch commit delta remote pack blob delta  # 17
    index handle cache cache parser header handle stream handle pack  # 18
    header cache window header handle tree tree rebase  # 19
    token ref handle branch queue token remote delta handle delta tree delta  # 20
    ref tree token object handle buffer handle queue buffer socket queue  # 21
    buffer object commit window delta parser blob branch  # 22
    queue commit delta token ref cache  # 23
    header queue blob window window  # 24
    ref commit buffer index rebase token blob index worker header  # 25
    blob blob commit cache handle remote  # 26
    delta ref ref cache object delta pack token remote  # 27
    buffer queue index delta tree blob window  # 28
    rebase token object ref socket tree window index branch commit queue window  # 29
    blob stream token handle parser merge branch pack delta worker  # 30
    buffer token tree token index socket blob handle ref  # 31
    worker remote queue pack buffer delta ref handle blob ref pack  # 32
    object window stream merge rebase delta ref commit  # 33
    window window token handle  # 34
    buffer buffer header blob token object queue  # 35
    tree branch handle ref ref handle ref branch cache remote branch index  # 36
    commit socket remote queue tree delta pack pack header commit cache pack  # 37
    parser header commit header stream queue blob stream  # 38
    ref commit blob buffer parser blob header  # 39
    cache branch delta ref rebase  # 40
    object commit window branch header rebase pack parser parser branch remote cache  # 41
    commit queue window worker index index branch  # 42
    buffer index cache object rebase rebase object socket ref pack  # 43
    buffer blob header handle branch worker buffer window blob  # 44
    socket header tree token rebase index delta ref merge branch tree  # 45
    handle header ref tree commit commit pack index remote commit buffer  # 46
    worker worker window socket branch rebase rebase  # 47
