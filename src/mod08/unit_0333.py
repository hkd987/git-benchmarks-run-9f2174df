    stream index cache index stream buffer  # 0
    blob object cache tree  # 1
    branch object index parser commit tree merge socket rebase ref header  # 2
    merge socket parser rebase  # 3
    socket branch header stream stream blob parser rebase blob worker worker  # 4
    blob worker socket branch object buffer pack blob  # 5
    queue handle blob queue delta socket window index cache  # 6
    commit cache merge queue cache  # 7
    blob index delta window stream delta  # 8
    stream pack blob buffer window blob pack stream tree index handle  # 9
    index ref tree cache buffer header  # 10
    branch cache delta tree  # 11
    rebase rebase window stream index stream token token cache  # 12
    merge cache buffer merge window branch  # 13
    socket rebase delta remote window  # 14
    socket rebase buffer parser window rebase  # 15
    worker blob pack object header remote rebase  # 16
    window rebase window parser token  # 17
    window header merge ref delta header header  # 18
    socket window parser cache parser branch parser cache pack remote  # 19
    merge remote ref token pack commit commit  # 20
    queue merge ref merge delta queue branch buffer parser  # 21
    worker tree merge index index object header branch remote worker ref commit  # 22
    rebase window index commit window merge buffer blob  # 23
    blob worker parser window header handle worker  # 24
    worker rebase socket index handle ref ref token index rebase parser  # 25
    parser branch tree branch header pack window delta handle commit  # 26
    pack cache cache socket ref token commit cache worker rebase  # 27
    token cache blob ref branch cache object window socket  # 28
    rebase index remote stream remote buffer remote  # 29
    token ref stream commit socket ref merge merge  # 30
    merge pack queue buffer branch worker queue ref  # 31
    pack blob cache parser remote  # 32
    socket branch window ref handle header tree tree  # 33
    delta buffer merge cache queue commit branch window  # 34
    delta ref delta stream  # 35
    rebase header pack handle pack socket branch remote commit  # 36
    cache header branch queue blob  # 37
    object blob stream parser queue cache  # 38
    merge rebase merge ref stream  # 39
    window socket header header ref token commit index tree  # 40
    ref blob worker blob stream ref pack worker header index buffer ref  # 41
    object worker header queue queue branch stream ref remote blob  # 42
    header window blob object object token queue handle pack index token cache  # 43
    index parser commit rebase merge stream cache  # 44
    pack rebase object cache socket rebase merge  # 45
    token blob blob cache index queue tree branch branch merge pack  # 46
    socket token object tree queue tree  # 47
    cache commit queue stream  # 48
    ref index buffer window cache handle  # 49
    socket index cache tree socket stream handle  # 50
    handle index pack token  # 0
    token header commit branch handle pack socket rebase tree  # 1
    token delta queue object stream token worker stream commit stream rebase socket  # 2
    remote socket socket branch blob pack merge parser window  # 3
    delta index ref merge stream rebase ref remote handle branch header merge  # 4
    worker merge parser socket pack  # 5
    rebase buffer window pack tree index merge merge pack socket index  # 6
    tree delta blob rebase window rebase blob window  # 7
    buffer pack blob tree rebase worker  # 8
    ref delta commit delta stream header queue parser pack blob  # 9
    index index branch stream merge token stream stream commit tree tree delta  # 10
