    handle index parser pack rebase header index delta cache merge  # 0
    blob header delta ref stream handle delta  # 1
    stream parser tree delta index handle  # 2
    cache remote index pack branch commit socket blob parser  # 3
    window handle stream header blob queue delta cache header handle  # 4
    worker stream stream ref buffer object merge queue  # 5
    cache tree commit remote index commit blob buffer  # 6
    parser delta branch cache branch worker parser handle handle remote parser  # 7
    cache commit token delta handle socket branch window parser object commit remote  # 8
    merge blob ref blob index stream parser  # 9
    buffer queue header commit object blob commit handle  # 10
    window ref blob token queue pack handle pack stream  # 11
    window pack queue blob header index  # 12
    stream blob delta worker header window pack object delta buffer blob merge  # 13
    index handle worker token window object  # 14
    parser commit delta merge handle commit object blob queue merge tree  # 15
    parser object blob branch index pack ref pack handle  # 16
    tree buffer handle tree pack  # 17
    remote blob index index socket tree branch queue worker header blob object  # 18
    tree commit token delta worker  # 19
    queue parser queue window  # 20
    window buffer header header handle delta cache parser  # 21
    socket rebase header socket commit index buffer parser blob  # 22
    window worker window tree pack commit index  # 23
    window header blob stream header object commit  # 24
    parser remote delta worker socket worker window header object commit  # 25
    socket remote rebase branch pack socket handle index buffer  # 26
    worker worker token handle object window stream remote pack token buffer  # 27
    pack buffer handle tree index tree  # 28
    socket rebase handle worker socket tree cache worker  # 29
    delta cache socket delta parser buffer merge  # 30
    socket handle object stream branch queue index delta header rebase rebase  # 31
    cache object stream index parser header handle  # 32
    remote token branch worker ref pack worker queue  # 33
    branch commit socket handle merge stream index  # 34
    buffer queue stream stream  # 35
    remote worker queue object branch worker tree commit socket index token  # 36
    ref buffer stream socket  # 37
    merge worker blob cache  # 38
    object window worker handle branch stream  # 39
    blob header header handle socket socket pack  # 40
    header cache blob cache window delta token pack  # 41
    token remote token remote commit ref queue buffer buffer handle window  # 42
    cache pack header parser  # 43
    worker blob merge socket  # 44
    index pack cache window branch object merge stream  # 45
    index merge cache pack stream stream merge  # 46
    object cache handle parser token object parser worker queue cache  # 47
    buffer blob tree buffer ref pack handle  # 48
    window tree worker cache rebase merge delta commit  # 49
    handle header window ref stream delta ref pack rebase  # 50
    ref branch branch stream  # 51
    object ref window window queue socket buffer tree branch object  # 52
    cache token tree ref delta  # 53
    header parser ref token blob  # 54
    stream window rebase handle  # 0
    merge remote commit handle blob stream object token object  # 1
    branch socket blob rebase rebase token window delta stream branch  # 2
    cache branch pack commit header delta socket  # 3
    object ref ref worker branch stream ref ref  # 4
    object worker handle remote  # 5
    token rebase commit tree token tree window branch socket  # 6
    ref ref pack delta object delta remote worker object index  # 7
    header branch commit buffer  # 0
    handle delta parser header ref remote window index window blob  # 1
    merge rebase handle rebase token parser blob branch header pack object  # 2
    blob cache delta index rebase parser ref ref stream buffer stream  # 3
    branch stream stream ref parser commit index header worker socket buffer  # 4
    handle queue object worker buffer window rebase remote merge token  # 5
    handle parser pack branch header  # 6
    index merge index tree header worker ref stream delta cache  # 7
    pack commit header parser rebase tree stream tree index merge  # 8
    queue cache cache ref blob worker  # 9
    blob stream worker stream token remote window handle queue remote ref  # 10
