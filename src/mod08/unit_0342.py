    token branch merge ref buffer  # 0
    blob branch handle tree blob  # 1
    commit ref commit worker  # 2
    token cache worker tree pack rebase object worker token stream branch tree  # 3
    header stream remote stream handle cache branch remote socket delta index  # 4
    token buffer token cache merge rebase remote delta  # 5
    header ref branch queue buffer delta  # 6
    worker header cache parser blob  # 7
    blob buffer parser object branch socket branch handle branch  # 8
    handle branch commit delta parser index queue handle  # 9
    merge object rebase stream commit buffer buffer pack  # 10
    token branch commit pack socket buffer handle commit tree merge  # 11
    header token ref object stream window blob commit rebase pack parser  # 12
    index queue commit ref index parser object queue blob  # 13
    handle handle ref remote header token tree window blob  # 14
    worker merge window rebase window rebase object index socket  # 15
    merge parser remote commit blob commit ref remote tree parser tree  # 16
    remote queue merge remote buffer  # 17
    merge pack branch stream  # 18
    branch remote window queue ref commit remote  # 19
    remote rebase remote window remote ref index index handle  # 20
    tree rebase commit blob handle delta token ref header queue tree branch  # 21
    ref window index worker object worker  # 22
    token branch merge ref index branch parser merge branch handle  # 23
    merge cache rebase queue  # 24
    object handle branch merge buffer handle ref branch blob buffer merge pack  # 25
    commit buffer parser token handle handle window window stream merge  # 26
    branch token stream merge branch window header window  # 27
    ref commit cache worker worker index ref queue branch tree  # 28
    pack token ref cache branch worker parser rebase remote handle window tree  # 29
    merge blob remote worker window pack remote blob tree cache  # 30
    stream branch delta cache token pack queue  # 31
    header object merge blob buffer worker window header  # 32
    merge cache index rebase pack token  # 33
    socket object ref window cache window handle worker queue queue commit  # 34
    pack stream index commit  # 35
    handle header object blob  # 36
    commit commit delta buffer object  # 37
    tree token handle socket remote tree worker worker merge rebase  # 38
    token ref object remote  # 39
    ref buffer rebase token merge buffer parser socket tree buffer worker rebase  # 40
    ref parser index merge  # 41
    token blob cache branch parser header worker  # 42
    commit token handle tree buffer merge delta  # 43
    worker object worker delta stream pack blob parser  # 44
    pack pack token parser rebase pack pack  # 45
    remote object index rebase cache window cache worker cache  # 46
    blob delta branch stream pack delta  # 47
    index window queue cache header worker header ref  # 48
    branch parser tree token queue buffer buffer merge  # 49
    blob worker handle pack merge buffer  # 50
    token stream object tree  # 51
    buffer worker merge branch handle handle remote cache stream  # 52
    window object cache socket object parser  # 53
    parser delta header object remote blob  # 54
    delta handle socket rebase buffer socket  # 55
    buffer rebase merge commit parser index parser ref buffer  # 56
    window object remote tree branch index object remote  # 57
    merge token delta queue stream pack socket commit buffer buffer tree commit  # 58
    index token socket queue  # 59
    token parser merge index remote queue worker header socket worker  # 60
    tree worker cache tree header ref  # 61
    parser window ref commit buffer delta  # 62
    commit header handle index cache queue delta window handle branch window index  # 63
    queue handle pack window branch parser window merge  # 64
    socket index stream merge object object buffer window rebase commit queue  # 65
    index remote handle branch worker stream ref  # 66
    queue merge queue socket buffer merge tree header window  # 67
    token cache window window blob window branch index merge  # 68
    object parser tree blob cache handle queue object branch cache  # 69
    merge ref remote delta window remote remote tree  # 70
    blob object window pack tree queue delta parser  # 71
    remote cache queue buffer merge socket header socket cache window  # 72
    socket rebase object index blob blob cache rebase pack object handle header  # 73
    pack window parser object token  # 74
    socket cache socket socket rebase parser  # 75
    stream branch parser rebase commit buffer merge blob buffer  # 76
    socket token window object rebase  # 77
    delta commit parser parser handle stream  # 78
    cache delta merge cache  # 0
    window rebase remote pack ref  # 1
    header commit merge parser stream window token index rebase delta delta  # 2
    buffer parser token handle worker window  # 3
    pack tree merge pack ref token token window commit handle window  # 4
    worker commit merge buffer merge remote parser blob index header index buffer  # 5
    cache pack socket cache branch queue pack ref handle remote  # 6
