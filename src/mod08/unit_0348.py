    cache merge handle window buffer cache cache stream  # 0
    buffer buffer pack worker delta ref merge parser index object merge  # 1
    buffer object blob tree ref queue queue token worker  # 2
    remote worker pack cache stream cache tree index stream branch index header  # 3
    worker pack remote object ref blob tree  # 4
    merge delta buffer header branch worker queue merge tree socket window  # 5
    delta tree token buffer remote buffer ref queue commit tree  # 6
    queue worker header delta tree commit handle commit object  # 7
    ref index pack window branch merge branch blob object stream parser  # 8
    socket remote tree object remote commit delta remote cache  # 9
    cache commit tree buffer  # 10
    delta remote buffer merge window  # 11
    ref stream cache socket rebase parser worker merge handle  # 12
    buffer delta handle queue parser object queue commit  # 13
    token delta tree commit window stream pack  # 14
    worker commit buffer socket window delta header handle buffer cache commit  # 15
    branch parser window header rebase queue rebase blob blob branch  # 16
    parser queue index buffer token cache delta window  # 17
    window index cache cache delta socket  # 18
    socket queue buffer header token rebase  # 19
    ref delta remote header buffer object commit  # 20
    header socket socket object branch commit index pack  # 21
    merge buffer header stream pack queue object object cache  # 22
    rebase delta stream remote index  # 23
    window remote parser tree handle buffer buffer blob header token  # 24
    queue tree window queue header handle buffer handle delta commit remote  # 25
    handle token cache remote cache ref  # 26
    parser tree socket window worker object cache header window parser  # 27
    branch socket socket stream pack delta merge delta ref remote worker token  # 28
    rebase commit branch index tree blob socket tree worker blob handle  # 29
    commit header index blob queue cache cache  # 30
    socket index remote window parser stream  # 31
    branch merge index socket parser ref  # 32
    token token socket queue object ref worker merge queue  # 33
    handle tree delta pack merge token commit index ref pack queue  # 34
    delta pack buffer token rebase delta queue remote  # 35
    handle ref queue index  # 36
    cache socket remote tree socket worker  # 37
    worker branch branch header branch remote buffer ref queue object  # 38
    window pack index commit buffer stream commit  # 39
