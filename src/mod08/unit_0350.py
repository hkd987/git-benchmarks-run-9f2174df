    token header index stream branch  # 0
    merge rebase tree parser  # 1
    remote buffer index parser cache token  # 2
    buffer ref window index ref delta delta merge index  # 3
    commit buffer cache commit merge blob branch pack worker stream  # 4
    branch rebase pack worker cache delta  # 5
    worker worker remote commit pack object branch merge commit header window  # 6
    worker tree window buffer header queue parser queue ref handle  # 7
    handle merge object tree window handle  # 8
    pack rebase object rebase blob pack index cache  # 9
    branch remote commit parser  # 10
    window object parser merge window merge  # 11
    rebase pack delta ref object ref tree ref  # 12
    token commit parser rebase parser token branch queue stream merge delta  # 13
    socket parser token merge tree merge token window ref buffer  # 14
    object commit commit stream remote socket object worker queue  # 15
    remote tree index cache delta tree index parser merge object rebase  # 16
    cache blob index branch ref queue token delta queue  # 17
    cache worker delta merge  # 18
    merge merge rebase pack parser rebase commit socket  # 19
    stream cache handle delta worker worker branch rebase branch window  # 20
    rebase header window delta index pack merge window socket  # 21
    window rebase index commit blob blob  # 22
    commit ref branch buffer object socket delta delta tree blob delta  # 23
    buffer delta commit index window token buffer window window  # 24
    rebase worker pack cache pack cache rebase index socket pack tree delta  # 25
    handle queue header buffer commit buffer branch header queue object branch  # 26
    parser pack parser cache cache commit worker rebase header remote worker remote  # 27
    header worker blob worker  # 28
    token token window worker tree pack stream  # 29
    ref handle blob pack buffer  # 30
    tree remote delta cache branch tree remote pack index socket  # 31
    blob branch parser socket stream ref index  # 32
    commit window tree queue  # 33
    blob window queue blob branch header blob cache worker  # 34
    token token header worker worker  # 35
    tree tree buffer merge header object blob  # 36
    queue queue ref worker handle remote cache remote token  # 37
    parser stream header delta delta queue delta delta stream queue ref  # 38
    ref tree commit branch  # 39
    queue cache header delta tree stream  # 40
    commit queue token buffer rebase remote window handle rebase buffer  # 41
    socket socket pack delta socket pack remote tree  # 42
    rebase remote worker cache branch worker merge  # 43
    merge stream stream ref tree remote buffer tree delta index object  # 44
