    socket token handle stream blob stream branch branch queue  # 0
    ref worker pack delta socket delta cache handle branch merge worker  # 1
    delta commit index blob token  # 2
    queue queue cache parser  # 3
    cache tree pack worker remote window queue ref tree  # 4
    commit handle worker token  # 5
    rebase object stream delta tree merge  # 6
    buffer parser remote merge tree commit stream commit delta worker  # 7
    pack tree socket index  # 8
    rebase cache ref object token delta commit header delta handle blob  # 9
    window index ref socket ref merge ref stream queue  # 10
    merge header rebase commit parser pack  # 11
    merge tree header ref queue parser  # 12
    merge merge cache handle buffer handle rebase merge pack tree  # 13
    handle header token cache worker cache token socket  # 14
    buffer window branch pack worker queue branch ref  # 15
    cache pack stream handle socket handle  # 16
    header stream buffer merge merge worker  # 17
    remote socket window buffer token token worker socket blob pack window cache  # 18
    queue ref socket handle delta  # 19
    remote stream index worker parser parser pack ref header queue  # 20
    parser buffer handle parser  # 21
    index window socket token cache tree window parser stream blob  # 22
    window pack delta window rebase window blob cache tree  # 23
    token header object blob window delta  # 24
    rebase rebase worker window tree stream remote queue remote buffer delta object  # 25
    cache header delta index buffer socket stream branch merge  # 26
    object parser merge token delta header window blob worker merge queue tree  # 27
    stream token worker tree  # 28
    tree index queue stream branch token cache commit index object object stream  # 29
    header token remote buffer tree remote merge worker object cache tree buffer  # 30
    remote object header rebase window socket ref window object  # 31
    commit pack cache blob  # 32
    blob window token blob pack handle cache buffer worker tree  # 33
    handle blob cache merge ref delta handle index tree delta header buffer  # 34
    object cache window header commit delta tree  # 35
    parser window parser cache token rebase socket merge tree merge  # 36
    merge remote parser delta token buffer queue queue rebase index queue branch  # 37
    merge tree merge buffer cache  # 38
    ref branch index stream tree index token object buffer delta blob parser  # 39
    worker pack socket window  # 40
    index object header blob worker window socket remote header  # 41
    buffer handle buffer window  # 42
    stream index token parser parser object header merge worker  # 43
    pack rebase token merge remote index tree blob remote socket pack ref  # 44
    cache handle delta ref cache queue rebase rebase commit queue object object  # 45
    ref blob queue buffer blob delta window branch window  # 46
    socket handle socket rebase  # 47
    queue parser delta pack blob branch branch window object  # 48
    window ref handle ref  # 0
    header worker rebase stream cache queue  # 1
    tree rebase remote queue index branch delta  # 2
    object parser commit merge buffer window  # 3
    remote worker remote worker  # 4
    socket queue socket remote worker window rebase parser socket  # 5
    index tree handle tree stream ref index token index  # 6
    worker delta blob ref delta parser  # 7
    buffer commit header merge parser object object window object pack  # 8
    parser buffer delta pack commit index delta  # 9
    blob delta handle rebase handle cache cache blob worker buffer  # 10
    queue stream buffer merge commit queue blob blob index  # 0
    header worker ref rebase ref tree worker  # 1
    buffer cache header socket ref merge  # 2
    stream queue handle branch queue commit cache stream merge buffer worker  # 3
