    ref header stream window window handle queue merge  # 0
    window blob buffer handle token header queue handle branch ref queue cache  # 1
    commit commit cache blob stream  # 2
    pack rebase header stream delta stream tree  # 3
    token window commit branch cache  # 4
    merge queue worker ref socket queue index remote  # 5
    token commit parser stream  # 6
    merge cache window rebase parser cache blob buffer  # 7
    merge delta buffer cache worker commit ref buffer parser commit  # 8
    pack merge remote buffer object handle header parser token tree  # 9
    object handle delta socket worker tree  # 10
    blob header delta handle delta worker header token  # 11
    handle parser cache rebase  # 12
    parser index stream branch header object index delta tree ref cache index  # 13
    window header token stream remote worker remote window object object queue branch  # 14
    cache handle parser worker index stream rebase  # 15
    remote blob queue worker ref handle header branch branch tree  # 16
    cache stream merge rebase index  # 17
    worker merge header header parser blob window branch delta token  # 18
    worker header header tree tree commit parser  # 19
    handle index branch ref pack pack rebase cache cache stream  # 20
    object ref remote object worker commit queue remote  # 21
    stream index parser pack ref window  # 22
    header ref index delta token branch header stream buffer  # 23
    socket delta delta window commit  # 24
    buffer commit buffer token tree index object ref  # 25
    delta rebase parser pack tree remote token index  # 26
    header cache pack parser header parser parser delta object  # 27
    commit blob index index  # 28
    window socket merge index  # 29
    remote commit token branch cache  # 30
    object delta handle index header pack cache  # 31
    tree header rebase token token ref  # 32
    commit buffer token merge branch index commit token buffer cache blob object  # 33
    delta blob header index object object remote index worker  # 34
    window header blob socket merge remote window  # 35
    object worker socket cache delta delta cache commit object  # 36
    stream window token remote worker cache socket remote handle  # 37
    stream token cache buffer queue blob ref window buffer remote header rebase  # 38
    index ref object token stream token delta rebase  # 39
    buffer pack handle parser tree merge parser  # 40
    delta worker rebase worker commit worker delta branch delta  # 41
    parser delta remote index object tree window handle rebase merge  # 42
    branch stream token parser  # 43
    branch pack queue merge rebase token  # 44
    header index remote worker cache window socket parser cache index queue commit  # 45
    window window worker commit merge branch ref worker worker blob handle delta  # 46
    socket index handle pack commit remote socket rebase remote ref header  # 47
    window merge socket queue blob  # 48
    parser worker socket index rebase  # 49
    index token remote parser token  # 50
    object buffer handle handle object socket  # 51
    header blob commit queue object buffer tree socket index tree parser commit  # 52
    remote parser remote worker header token buffer header delta worker queue  # 53
    worker rebase rebase ref window remote tree merge queue socket  # 54
    stream index stream commit commit header queue object branch index  # 55
    header worker window window queue delta  # 56
    parser pack queue worker  # 57
    object pack window remote handle  # 58
    token index delta socket object rebase branch delta index commit  # 59
