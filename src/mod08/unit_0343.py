    handle cache ref pack index merge merge  # 0
    rebase object delta socket worker ref branch socket merge buffer delta  # 1
    buffer pack stream buffer delta token worker commit window cache cache  # 2
    handle blob index pack object buffer delta commit index delta  # 3
    window tree parser tree branch branch pack parser queue object  # 4
    queue delta ref stream object remote window pack tree  # 5
    parser socket buffer branch token queue object worker rebase ref  # 6
    blob tree rebase parser rebase socket commit tree  # 7
    stream blob merge header  # 8
    handle parser commit branch ref socket tree delta merge  # 9
    token merge remote branch queue tree worker stream ref merge delta  # 10
    branch stream blob buffer pack stream buffer worker commit  # 11
    parser pack commit buffer worker stream socket window object merge buffer  # 12
    tree branch tree socket tree  # 13
    branch merge parser handle token stream cache header worker handle object  # 14
    blob merge branch remote cache object handle header stream cache pack  # 15
    merge object commit merge parser blob branch cache commit remote  # 16
    stream blob buffer stream branch window socket tree ref  # 17
    worker branch parser tree stream  # 18
    delta object commit remote index handle  # 19
    cache index commit delta parser remote  # 20
    buffer index buffer merge parser delta merge rebase token token  # 21
    rebase pack parser worker object merge ref commit cache token  # 22
    merge blob token stream queue delta buffer  # 23
    merge ref token cache rebase ref index object parser commit  # 24
    token header buffer object delta cache socket blob window  # 25
    buffer branch window token cache remote  # 26
    branch cache worker parser ref blob blob rebase delta remote index  # 27
    remote commit pack commit  # 28
    parser rebase parser blob header cache header blob queue stream  # 29
    branch blob delta stream stream delta handle pack blob rebase window  # 30
    tree delta ref stream  # 31
    index parser rebase delta worker pack rebase  # 32
    merge ref token branch  # 33
    cache ref stream buffer ref branch parser branch window merge ref  # 34
    tree stream socket commit merge header socket index worker  # 35
    stream blob queue pack buffer blob window object index pack object token  # 36
    window worker token object token index queue ref  # 37
    rebase buffer ref token index commit index ref remote blob buffer  # 38
    object handle tree header pack handle delta  # 39
    object handle header socket remote pack merge buffer branch object delta  # 40
    parser remote socket branch parser ref branch pack  # 41
    cache delta delta header pack token object object rebase delta buffer  # 42
    tree blob header buffer remote socket delta handle ref stream  # 43
    handle tree header token commit remote worker ref delta cache delta worker  # 44
    ref delta handle socket header ref delta pack remote remote  # 45
    worker delta ref commit worker pack merge socket ref pack merge  # 46
    handle ref worker header  # 47
    token queue commit delta buffer tree pack stream ref ref  # 48
    index cache buffer object parser ref token cache worker remote header  # 49
