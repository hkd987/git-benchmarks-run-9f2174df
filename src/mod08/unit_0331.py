    object branch queue header tree  # 0
    merge cache pack object rebase stream token queue cache header  # 1
    remote worker object blob merge blob cache handle handle handle worker  # 2
    tree token commit commit socket merge queue merge  # 3
    object token buffer branch stream  # 4
    parser queue buffer index pack socket  # 5
    header socket tree socket parser tree window merge merge handle  # 6
    buffer header index tree commit ref merge worker cache  # 7
    rebase ref queue stream stream cache token  # 8
    blob token branch rebase token buffer delta  # 9
    rebase index merge stream worker stream  # 10
    parser socket buffer remote merge socket parser delta header stream blob header  # 11
    remote pack commit remote index queue  # 12
    remote handle tree commit  # 13
    queue rebase cache ref cache token remote worker cache  # 14
    ref worker stream blob stream  # 15
    header object worker cache queue stream merge buffer tree pack buffer stream  # 16
    cache blob delta ref blob socket object worker header  # 17
    pack handle merge worker blob merge  # 18
    commit merge index worker blob tree index delta stream merge tree  # 19
    branch stream header blob merge ref rebase pack tree  # 20
    worker header object socket remote merge worker worker parser  # 21
    pack stream parser handle header window window header stream  # 22
    index commit header token stream  # 23
    stream handle ref rebase header  # 24
    rebase queue object parser delta tree pack parser remote buffer socket queue  # 25
    delta branch token window branch handle index header window  # 26
    pack blob worker socket branch ref merge branch branch  # 27
    branch ref stream object token cache ref branch header header index tree  # 28
    branch blob stream parser ref delta blob handle token branch  # 29
    queue commit index handle index  # 30
    index socket window cache worker header buffer  # 31
    merge tree worker ref ref merge tree  # 32
    branch stream parser blob object socket buffer index remote commit  # 33
    index pack commit delta tree blob parser header header  # 34
    cache token queue blob ref merge ref  # 35
    object buffer commit tree queue  # 36
    tree commit remote socket remote parser token token delta index delta  # 37
    worker pack delta index object blob queue parser  # 38
    queue token token commit  # 39
    index parser worker queue index branch commit blob handle blob  # 40
    rebase merge commit ref index handle merge cache worker index  # 41
    header rebase handle parser merge blob commit index cache blob header  # 42
    cache object commit window delta  # 43
    merge queue merge branch pack  # 44
    ref cache merge delta window delta buffer object socket handle window  # 45
    rebase worker blob window handle  # 46
    remote token cache socket  # 47
    cache parser merge buffer buffer branch  # 48
    token merge parser pack header tree  # 49
    delta header worker object tree token remote blob ref ref  # 50
    commit window cache token handle buffer cache cache parser merge cache  # 51
    worker buffer stream buffer  # 52
    index merge buffer branch index pack window ref branch ref header  # 53
    remote queue commit token merge queue  # 54
    header token merge stream parser handle token rebase merge  # 55
    worker blob rebase window stream  # 56
    worker worker delta token branch  # 57
    index stream stream header ref token rebase token  # 58
