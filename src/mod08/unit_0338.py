    cache token window token  # 0
    delta worker buffer rebase merge ref index token rebase worker merge remote  # 1
    delta header window queue parser buffer tree cache branch branch pack branch  # 2
    stream token token handle rebase worker object worker  # 3
    merge worker buffer queue token object pack queue remote commit tree blob  # 4
    socket worker buffer token index blob blob  # 5
    rebase index rebase delta object index  # 6
    cache tree index tree delta index remote worker  # 7
    window object blob token  # 8
    object merge stream tree buffer buffer stream  # 9
    tree ref parser header branch  # 10
    stream ref tree token worker ref  # 11
    handle commit pack cache commit remote  # 12
    blob rebase buffer parser tree buffer blob  # 13
    handle worker branch index rebase socket buffer socket  # 14
    merge window cache header header remote  # 15
    merge rebase socket header cache pack window  # 16
    rebase pack socket buffer  # 17
    queue handle rebase index  # 18
    cache branch index buffer delta header delta handle header buffer handle  # 19
    stream merge ref commit stream index cache merge stream  # 20
    pack worker parser parser worker index buffer  # 21
    handle remote stream pack header index token object window window  # 22
    delta branch rebase tree delta object cache buffer  # 23
    parser ref rebase socket stream queue worker ref header handle  # 24
    handle handle branch remote  # 25
    rebase branch worker window header socket index blob rebase handle queue  # 26
    parser worker rebase object tree merge pack socket parser socket worker rebase  # 27
    socket ref ref cache token blob ref header  # 28
    merge buffer delta window blob pack queue index blob window pack  # 29
    pack buffer cache remote pack queue token blob blob  # 30
    handle worker blob buffer  # 31
    commit merge merge stream branch merge index blob  # 32
    header index remote parser ref branch ref blob index  # 33
    ref queue commit ref buffer header socket tree header  # 34
    tree stream cache pack queue cache stream tree  # 35
    object object pack worker blob  # 36
    tree object worker handle ref branch  # 37
    token socket remote window commit ref merge worker pack stream  # 38
    tree tree branch commit parser socket tree cache commit cache window tree  # 39
    handle blob blob token merge commit delta  # 40
    header socket delta blob header socket token handle blob cache pack  # 41
    queue pack parser remote index buffer stream queue  # 42
    token handle blob queue remote header rebase worker worker  # 43
    buffer socket token blob tree queue parser commit cache  # 44
    queue index ref worker buffer  # 45
    ref ref blob handle merge token buffer pack tree window  # 46
    remote commit rebase handle queue index queue  # 47
    worker header commit token  # 48
    worker token buffer merge cache delta  # 49
    token pack buffer ref cache blob token branch merge merge  # 50
    blob object worker remote pack commit  # 51
    remote cache socket stream rebase remote branch parser buffer ref  # 52
    rebase socket buffer rebase  # 53
    ref pack window cache queue header window  # 54
    object queue token stream parser branch token parser buffer handle worker merge  # 55
    window blob merge object pack merge commit blob commit commit ref buffer  # 56
    window socket object queue token blob object handle index ref queue stream  # 57
    worker socket socket object buffer blob handle worker object  # 58
    worker blob remote remote delta index  # 59
    parser worker ref handle remote socket ref delta index window  # 60
