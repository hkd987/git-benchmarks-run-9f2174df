    index remote merge worker socket token delta delta window worker  # 0
    header stream ref tree header socket index commit window index stream  # 1
    tree object parser stream window queue blob  # 2
    object remote tree parser buffer cache queue socket socket worker blob header  # 3
    parser handle handle socket buffer delta  # 4
    parser rebase delta ref  # 5
    handle queue window delta  # 6
    header buffer tree cache tree merge socket buffer  # 7
    socket queue cache window index  # 8
    index stream remote queue object worker queue worker index  # 9
    index header header index parser delta header window remote handle object index  # 10
    parser index pack header buffer cache remote tree delta remote tree tree  # 11
    handle handle socket cache commit buffer  # 12
    worker object token stream index tree blob tree blob index index  # 13
    object parser object remote tree  # 14
    remote handle blob delta commit rebase index queue branch  # 15
    worker socket object branch buffer  # 16
    ref stream buffer header parser queue window delta worker buffer  # 17
    parser window remote cache pack token  # 18
    remote header object ref window socket handle merge blob worker index  # 19
    branch object stream rebase object pack buffer blob merge header object cache  # 20
    index delta branch cache remote object cache  # 21
    handle merge token buffer window rebase socket ref header  # 22
    delta object rebase object window token worker ref  # 23
    parser delta worker pack branch handle buffer  # 24
    remote queue delta blob tree commit blob delta queue branch  # 25
    worker index socket stream pack socket token  # 26
    pack cache worker delta worker  # 27
    branch object commit ref token commit ref window remote handle rebase  # 28
    object socket merge branch pack  # 29
    blob branch merge tree index branch blob ref remote window  # 30
    ref index commit merge object socket cache remote  # 31
    queue queue delta worker handle parser ref commit  # 32
    worker header window remote  # 33
    tree stream object branch delta commit ref tree  # 34
    index parser branch merge window header ref blob index blob merge  # 35
    ref pack index stream cache rebase buffer  # 36
    merge rebase queue ref branch index stream delta remote delta merge  # 0
    merge cache socket token window remote rebase  # 1
    delta worker socket merge index header object  # 2
    header pack token queue stream parser handle header  # 3
    token branch parser stream remote pack delta worker pack handle queue  # 4
    pack commit ref pack window ref cache window parser  # 5
    object branch queue window header object socket  # 6
    header window delta parser remote tree parser commit parser  # 7
    buffer commit tree header delta delta worker blob index pack  # 8
    branch pack merge cache queue  # 9
    object tree worker token tree header buffer  # 10
    blob window branch branch rebase blob ref rebase cache branch worker commit  # 11
    socket ref pack parser socket index tree token  # 12
    blob object queue socket stream pack blob pack tree socket  # 13
