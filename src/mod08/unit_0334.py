    handle blob handle branch handle window  # 0
    commit worker ref object remote index buffer delta ref  # 1
    blob object commit cache window stream index handle  # 2
    socket commit buffer window rebase buffer window header pack handle  # 3
    branch merge cache merge handle rebase remote cache ref remote  # 4
    blob blob window cache header  # 5
    pack buffer queue pack index buffer  # 6
    branch cache buffer rebase rebase branch object handle  # 7
    cache remote stream tree  # 8
    queue header handle remote  # 9
    window parser header socket socket pack buffer worker object window remote  # 10
    branch ref remote token  # 11
    blob header token rebase stream  # 12
    merge buffer pack buffer blob commit index object  # 13
    buffer socket cache index stream merge rebase commit stream header  # 14
    token rebase worker cache token merge  # 15
    handle worker pack handle cache remote delta index index header token window  # 16
    merge tree stream merge blob  # 17
    window index socket merge ref object blob blob  # 18
    socket remote tree window socket buffer index stream  # 19
    token parser worker queue delta ref ref stream window  # 20
    cache tree tree header handle worker ref socket parser buffer handle  # 21
    index remote branch socket tree parser tree handle token cache remote ref  # 22
    rebase rebase rebase merge remote  # 23
    stream pack header branch  # 24
    merge object tree pack worker worker parser pack commit index socket  # 25
    pack object merge token window  # 26
    pack object worker worker merge header index ref handle  # 27
    delta remote parser token tree queue token remote header pack  # 28
    worker commit window parser header  # 29
    buffer rebase token worker worker  # 30
    window merge delta ref merge window handle header rebase  # 31
    header window pack header token delta stream tree handle branch cache  # 32
    tree ref index commit buffer  # 33
    blob queue cache index rebase stream rebase branch tree  # 34
    rebase pack tree merge cache parser rebase  # 35
    ref parser queue index  # 36
    rebase delta window parser remote rebase cache cache buffer queue worker token  # 37
    branch tree object header  # 38
    queue index window ref index  # 39
    handle branch buffer token branch rebase remote index  # 40
    object blob ref tree branch pack header  # 41
    delta commit tree header blob blob pack worker cache blob parser merge  # 42
    buffer socket parser blob commit rebase stream commit index token object object  # 43
    handle window worker pack index header commit token socket header cache index  # 44
    blob buffer parser header index  # 45
    blob delta delta handle worker pack branch blob cache tree  # 46
    merge cache ref delta delta ref  # 47
    ref index worker header  # 48
    merge merge worker merge  # 49
    cache header handle cache tree worker header remote stream socket  # 50
    ref merge socket token merge object header token branch blob remote  # 51
    buffer handle header queue rebase blob ref pack worker  # 52
    parser index queue cache socket tree window  # 53
    branch handle blob index parser tree queue parser ref  # 54
    socket object merge buffer blob  # 55
