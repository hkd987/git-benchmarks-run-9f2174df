    branch branch index remote commit branch  # 0
    buffer blob rebase buffer worker tree object window  # 1
    branch commit cache merge tree parser worker queue  # 2
    handle worker merge merge  # 3
    queue token queue token  # 4
    remote worker window buffer worker pack pack object worker tree  # 5
    merge branch object ref rebase branch window ref  # 6
    parser ref cache pack object stream merge  # 7
    buffer index delta tree parser pack  # 8
    worker branch branch ref worker header delta object cache  # 9
    queue blob cache index  # 10
    pack remote window cache tree blob tree token merge token window  # 11
    header rebase cache header cache rebase handle  # 12
    handle tree parser commit queue handle delta socket  # 13
    commit rebase queue stream rebase buffer buffer parser rebase  # 14
    remote cache token cache  # 15
    window merge index window commit blob object  # 16
    index handle object token parser header worker ref cache  # 17
    object pack remote parser ref object pack pack  # 18
    rebase blob stream commit stream object buffer cache  # 19
    cache queue index delta handle stream cache queue handle worker object queue  # 20
    token handle rebase merge  # 21
    delta blob branch socket window token commit header remote blob index  # 22
    socket handle remote remote handle parser object token  # 23
    queue header worker token worker  # 24
    queue index ref socket tree ref header pack  # 25
    parser commit window object  # 26
    delta index parser delta header blob merge rebase socket branch  # 27
    window worker queue header window window handle worker stream buffer ref  # 28
    token commit socket worker pack stream header rebase queue  # 29
    branch branch pack merge stream parser handle  # 30
    object stream queue header worker tree socket worker queue  # 31
    rebase pack queue index header  # 32
    stream header token object ref delta object  # 33
    rebase parser socket window remote object rebase queue pack remote parser index  # 34
    ref socket ref rebase pack stream delta commit remote queue stream pack  # 35
    merge tree ref pack header blob handle pack index index parser blob  # 36
    commit remote worker token window parser queue parser  # 37
    merge handle window stream pack handle cache object parser buffer stream  # 38
    cache header index parser handle index remote ref handle buffer delta buffer  # 39
    parser tree socket stream merge branch blob  # 40
    tree pack pack worker merge cache handle remote window window delta blob  # 41
    queue commit worker token token socket window  # 42
