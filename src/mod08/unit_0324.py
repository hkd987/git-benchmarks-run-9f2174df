    pack queue token handle  # 0
    pack branch commit token stream socket  # 1
    rebase parser token socket  # 2
    queue buffer token buffer socket rebase blob ref merge rebase  # 3
    object window window cache cache object index buffer delta stream  # 4
    ref parser tree ref header rebase handle index window delta tree socket  # 5
    buffer buffer worker parser token ref commit buffer worker  # 6
    worker queue cache stream cache worker ref delta remote merge  # 7
    token pack buffer object pack buffer delta  # 8
    object index queue worker token handle object buffer parser blob  # 9
    tree delta rebase socket ref  # 10
    commit cache object queue handle pack  # 11
    ref ref stream object handle tree buffer worker rebase cache branch header  # 12
    window remote token object index index branch  # 13
    branch rebase remote blob merge  # 14
    pack header remote index object merge parser delta merge worker  # 15
    stream merge delta pack token stream  # 16
    socket stream parser object blob merge header header  # 17
    ref pack cache queue queue merge window  # 18
    queue blob header cache token token parser ref blob blob merge  # 19
    buffer queue worker remote window merge parser  # 20
    handle rebase index commit stream pack  # 21
    index parser ref branch worker stream merge rebase rebase remote delta  # 22
    index buffer window delta handle cache rebase  # 23
    remote stream handle worker branch tree header merge branch pack socket cache  # 24
    object window commit cache rebase queue  # 25
    commit buffer worker socket branch parser socket header  # 26
    merge delta commit object merge handle rebase merge  # 27
    socket delta merge branch branch  # 28
    parser object pack blob socket object socket header merge token  # 29
    branch ref branch window  # 30
    delta delta header index remote worker stream  # 31
    token parser commit ref token queue buffer commit  # 32
    header index header header rebase window  # 33
    handle remote object remote merge token worker commit  # 34
    merge remote branch remote token worker ref socket handle queue tree ref  # 0
    token window handle parser cache index cache token ref blob  # 1
    branch object parser worker queue commit ref ref stream branch buffer  # 2
    header header worker queue  # 3
    remote queue commit merge rebase socket rebase  # 4
    index blob tree index pack delta  # 5
