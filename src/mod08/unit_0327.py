    socket delta tree ref token cache socket delta rebase buffer ref object  # 0
    rebase buffer blob queue branch header ref commit index object queue  # 1
    parser buffer parser remote socket  # 2
    handle merge parser pack queue object pack stream cache remote commit  # 3
    queue index rebase buffer remote ref queue token blob tree socket  # 4
    remote buffer object branch window branch stream ref delta header blob  # 5
    header remote blob cache  # 6
    header tree merge commit ref commit remote ref branch tree header pack  # 7
    parser remote handle blob  # 8
    rebase delta header blob worker remote  # 9
    header remote tree branch  # 10
    merge socket remote ref parser queue stream remote  # 11
    stream merge token delta object cache queue merge cache handle  # 12
    worker pack window cache window socket ref rebase index delta parser  # 13
    index remote delta token handle worker parser handle socket buffer socket  # 14
    header buffer queue stream buffer branch delta pack worker  # 15
    commit socket handle blob ref branch token commit worker  # 16
    pack handle cache branch cache object merge window remote token handle  # 17
    token blob worker header handle rebase rebase commit pack index branch  # 18
    window blob ref worker rebase stream tree parser token queue header  # 19
    blob queue delta cache branch rebase merge header buffer handle tree worker  # 20
    tree branch blob object  # 21
    token commit socket ref pack merge index ref pack  # 22
    socket tree merge stream handle  # 23
    commit index cache pack handle blob delta merge worker stream tree  # 24
    delta header window merge handle handle blob queue blob merge  # 25
    parser object merge pack delta pack stream commit cache token stream queue  # 26
    stream buffer pack pack blob cache commit remote socket cache  # 27
    token remote remote parser socket branch worker handle ref index tree handle  # 28
    header worker ref parser remote window  # 29
    index window worker remote pack ref socket rebase stream handle  # 30
    window cache cache pack window index object cache window  # 31
    merge parser blob buffer cache tree delta worker tree  # 32
    pack worker commit pack header  # 33
    remote remote token object branch  # 34
    token window ref delta ref rebase tree queue branch buffer  # 35
    window window blob buffer socket merge token token blob worker  # 36
