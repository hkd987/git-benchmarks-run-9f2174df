    index blob tree remote delta rebase token tree merge merge  # 0
    window rebase remote branch merge  # 1
    pack pack header delta  # 2
    stream queue worker token rebase  # 3
    ref header rebase queue handle commit stream  # 4
    commit ref token queue index stream object tree parser object  # 5
    merge cache remote branch blob header token queue buffer window token commit  # 6
    handle header handle cache branch stream  # 7
    branch rebase buffer delta  # 8
    cache object queue tree blob branch rebase window  # 9
    blob header queue buffer commit object commit header  # 10
    cache header header branch blob cache stream  # 11
    delta worker queue object worker merge tree  # 12
    remote tree stream index commit  # 13
    merge object object token remote ref remote ref ref parser stream  # 14
    header socket delta stream ref socket pack queue object queue tree  # 15
    remote buffer delta commit merge tree merge parser ref  # 16
    branch remote worker cache branch remote header header blob window window  # 17
    socket merge object tree tree tree socket branch commit worker object header  # 18
    worker ref delta tree socket ref pack queue delta ref  # 19
    cache tree cache commit handle queue rebase worker  # 20
    cache worker queue queue branch cache  # 21
    branch merge stream blob stream merge cache pack tree pack  # 22
    ref pack index blob stream delta cache index tree  # 23
    ref delta cache delta  # 24
    rebase tree token index index ref delta tree buffer tree remote stream  # 25
    delta socket rebase remote rebase  # 26
    commit delta pack rebase cache parser branch window delta rebase  # 27
    buffer ref handle socket parser rebase remote header blob ref token  # 28
    worker commit branch object merge  # 29
    header branch token socket branch socket cache worker  # 30
    handle branch delta pack socket window rebase merge socket ref pack worker  # 31
    queue window tree worker branch delta rebase socket window tree socket socket  # 32
    branch window rebase token buffer blob rebase branch parser  # 33
    pack merge worker parser queue handle header window header stream  # 34
    remote ref window branch handle  # 35
    blob parser rebase blob object socket index  # 36
    index handle delta stream buffer blob delta tree parser socket  # 37
    buffer tree worker parser ref object rebase index delta ref object  # 38
    delta rebase merge remote object  # 39
    window header rebase socket branch worker window token  # 40
    ref worker index parser stream blob  # 41
    rebase parser queue buffer parser delta commit window handle  # 42
    object merge handle cache pack buffer  # 43
    pack commit buffer stream  # 44
    ref worker parser rebase worker  # 45
    object socket tree rebase object token merge handle  # 46
    token cache stream pack index window index socket  # 47
    worker header header remote queue window  # 48
    handle remote merge header stream window ref cache remote  # 49
    buffer handle buffer rebase rebase pack token remote rebase remote  # 50
    object worker worker parser handle  # 51
    worker parser handle object header handle handle queue blob rebase cache rebase  # 52
    worker queue handle delta index parser rebase parser delta merge  # 53
    token handle commit stream token  # 54
    index worker queue buffer handle  # 55
    token queue branch tree stream stream header handle remote token commit  # 56
    remote token window cache buffer index worker socket socket  # 57
    tree socket token queue window worker branch  # 58
    pack pack parser rebase queue object header  # 59
    queue index header commit branch remote handle remote object  # 60
    handle delta worker ref  # 61
    index buffer rebase cache cache  # 62
    header object stream blob object worker worker blob header index window cache  # 63
    buffer blob tree window object queue buffer cache branch  # 64
    remote worker buffer pack queue pack socket handle  # 65
    pack handle parser commit queue token remote  # 66
    socket branch tree header delta commit cache object  # 67
    pack buffer blob remote parser merge remote remote  # 68
    tree stream header tree pack window ref  # 69
    socket object worker header  # 70
    header object tree parser delta commit handle remote rebase  # 71
    remote pack header buffer worker header delta stream blob delta merge ref  # 0
    stream worker delta remote remote stream commit  # 1
    branch tree commit socket parser delta ref cache queue socket  # 2
    header blob window delta  # 3
    index token remote pack branch rebase header  # 4
    index parser parser rebase blob header worker  # 5
    window merge worker header ref parser tree window socket  # 6
    commit remote object worker ref queue index  # 7
    merge token object handle branch socket buffer  # 8
    index blob header object socket worker  # 9
    delta queue remote index commit merge token token merge merge stream  # 10
    token branch object merge socket delta branch blob handle blob ref token  # 0
    commit remote tree socket pack rebase  # 1
    window worker header tree  # 2
    branch remote handle pack handle token tree pack cache pack  # 0
    buffer merge tree cache  # 1
    header delta remote handle buffer ref queue stream socket pack buffer  # 2
    header merge ref merge queue worker token delta queue  # 3
    index socket cache rebase queue socket handle socket buffer  # 4
    parser remote remote window cache queue window ref token  # 5
    merge queue handle branch delta parser  # 6
    buffer socket index branch commit worker pack remote branch object token  # 0
    buffer buffer blob cache queue parser socket window header cache remote  # 1
    rebase window queue delta header merge  # 2
    stream rebase cache socket parser delta handle pack worker  # 3
