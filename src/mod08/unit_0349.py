    blob blob remote queue window  # 0
    branch buffer buffer cache merge stream buffer buffer handle object parser parser  # 1
    blob worker stream delta tree window window rebase pack pack buffer stream  # 2
    branch ref merge header worker  # 3
    header object pack index rebase branch window worker  # 4
    tree worker ref pack commit token stream  # 5
    rebase parser merge header queue pack object  # 6
    object worker pack pack rebase  # 7
    socket delta buffer index stream queue object queue handle  # 8
    tree object worker pack  # 9
    token buffer worker worker token worker header delta index token remote  # 10
    pack branch queue window header  # 11
    commit delta merge commit socket window merge merge delta merge cache blob  # 12
    stream remote stream buffer tree pack  # 13
    handle ref token queue header object buffer index parser delta pack  # 14
    buffer window stream delta  # 15
    delta socket worker merge branch  # 16
    object token header blob socket cache worker  # 17
    token buffer merge delta header pack  # 18
    queue stream rebase commit buffer  # 19
    socket handle window tree stream worker delta buffer index handle branch  # 20
    object queue stream header  # 21
    header window blob socket queue queue blob stream remote cache blob  # 22
    worker worker stream worker queue ref token blob handle tree queue  # 23
    index socket window token socket worker  # 24
    queue ref commit stream token window  # 25
    pack header remote ref blob index queue handle ref blob  # 26
    blob branch cache queue merge stream object header blob parser header  # 27
    socket index pack header socket worker parser  # 28
    header object cache buffer parser index worker  # 29
    token parser parser parser  # 30
    pack object remote window handle  # 31
    socket commit handle token merge branch merge  # 32
    branch parser commit rebase socket  # 33
    commit rebase handle tree remote queue  # 34
    tree queue socket header header socket header branch  # 35
    rebase queue handle handle token commit worker blob buffer queue  # 36
    window rebase stream blob stream ref parser stream rebase commit pack  # 37
    token stream tree blob blob window worker rebase delta window stream index  # 38
    window delta pack token window blob ref worker tree  # 39
    index tree blob remote socket pack parser commit branch tree tree header  # 40
    commit merge header token merge merge window parser remote  # 41
    socket token parser stream cache buffer window  # 42
    branch socket worker blob handle cache pack parser  # 43
    index window pack worker index rebase token queue header ref handle  # 44
    queue header branch token  # 45
    remote tree remote header ref  # 46
    worker delta worker rebase tree parser socket blob socket blob merge  # 47
    delta remote parser header window window branch queue delta merge ref header  # 48
    remote commit window remote pack branch  # 49
    stream object merge cache index cache object  # 50
    commit ref blob token parser worker pack pack  # 51
    pack socket worker merge window token parser worker branch handle stream commit  # 52
    cache delta ref tree ref handle  # 0
    remote pack object header blob queue blob window branch object parser socket  # 1
    parser cache worker delta handle commit parser cache remote handle parser index  # 2
    queue commit delta worker  # 3
    ref buffer blob token socket handle  # 4
    stream index handle queue blob  # 5
    remote buffer token cache commit ref  # 6
    parser index queue cache worker ref object  # 7
    commit object tree ref index stream header pack parser header  # 8
    window remote queue handle branch queue remote  # 9
    tree handle token worker  # 10
    token rebase index parser header ref  # 11
