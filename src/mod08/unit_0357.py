    worker header remote blob socket commit blob header delta  # 0
    tree cache pack socket index stream tree branch worker queue cache  # 1
    tree token socket ref branch commit tree  # 2
    object worker stream delta queue merge remote  # 3
    parser token worker branch ref parser tree delta pack cache buffer  # 4
    branch branch cache parser remote worker socket merge blob  # 5
    merge queue token worker queue  # 6
    worker remote commit rebase index blob delta cache  # 7
    handle handle cache cache handle delta object index parser cache handle tree  # 8
    queue object object parser tree rebase remote  # 9
    delta pack merge header merge  # 10
    merge remote object cache stream window token blob token delta  # 11
    queue header branch commit parser  # 12
    object stream object delta  # 13
    remote index ref rebase window  # 14
    tree ref tree pack delta buffer rebase commit window cache  # 15
    object tree remote stream  # 16
    tree branch rebase rebase  # 17
    tree rebase worker object buffer merge socket worker index queue index ref  # 18
    index token cache blob cache socket pack buffer  # 19
    parser window ref object token window merge worker pack ref queue  # 20
    token handle rebase branch queue branch  # 21
    pack window buffer blob parser branch  # 22
    parser rebase merge commit stream ref  # 23
    cache cache object object merge delta merge handle socket stream delta cache  # 24
    ref merge queue parser blob index queue token delta merge blob cache  # 25
    handle cache branch ref cache index remote  # 26
    window stream commit index queue  # 27
    rebase worker worker index branch branch token buffer queue token  # 28
    index branch branch blob  # 29
    worker header buffer header cache socket  # 30
    header window ref delta merge tree socket window pack tree rebase  # 31
    ref socket token rebase blob buffer ref delta rebase  # 32
    pack window blob index pack header handle rebase  # 33
    socket blob remote header  # 34
    cache handle stream rebase worker index buffer header commit stream  # 35
    merge branch merge object  # 36
    header window ref object delta queue commit rebase pack stream  # 37
    pack branch remote parser rebase buffer rebase window parser tree commit tree  # 38
    stream commit object commit queue header cache queue queue header  # 39
    window queue tree delta  # 40
    cache blob branch delta token ref merge parser  # 41
    ref index blob header rebase worker index  # 42
    rebase worker stream ref queue index object merge queue  # 43
    buffer parser stream remote socket merge window commit worker  # 44
    pack parser socket object tree socket tree queue pack queue tree worker  # 45
    worker object parser parser queue pack window blob  # 46
    cache queue ref object commit  # 47
    queue stream commit header remote object buffer  # 48
    pack remote window object  # 49
    delta socket ref socket commit  # 50
    index remote worker object blob object branch pack remote blob window delta  # 51
    handle parser commit header socket index parser rebase delta remote  # 52
    window buffer queue commit  # 53
    queue rebase commit header commit socket token blob worker worker handle cache  # 54
    queue remote rebase worker merge rebase parser remote tree  # 55
    index window cache window branch branch  # 56
    parser remote ref handle merge merge remote worker blob tree pack merge  # 57
    ref merge object worker socket delta handle queue merge pack index  # 58
    object merge cache merge token ref merge merge window buffer index  # 59
    header worker stream ref pack branch object worker object  # 60
    header object pack object pack branch parser pack object object  # 61
    header tree ref queue handle  # 62
    socket header parser worker delta merge header branch worker  # 63
    merge blob stream stream ref socket  # 64
    object delta remote merge branch header token token  # 65
    tree delta queue blob  # 66
    pack merge pack rebase cache index pack token pack remote  # 67
    token index rebase window object handle socket header  # 68
    buffer commit worker buffer  # 69
    remote merge branch parser cache worker object socket socket index stream parser  # 70
    cache parser branch cache commit token tree  # 71
    remote branch rebase socket rebase tree header ref  # 72
    window commit index merge ref stream header  # 73
    queue pack delta rebase ref stream queue window socket buffer  # 74
    parser cache branch buffer token queue  # 75
    worker cache buffer window commit rebase socket buffer stream parser window  # 76
    worker header worker merge ref  # 77
    branch buffer handle header  # 78
