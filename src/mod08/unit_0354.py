    pack worker commit rebase parser branch header remote header worker  # 0
    stream queue object ref buffer remote buffer remote worker remote  # 1
    branch ref branch queue index socket object  # 2
    tree cache delta token rebase blob merge parser buffer  # 3
    cache ref remote merge object socket queue token rebase  # 4
    branch stream socket parser  # 5
    object token stream handle  # 6
    merge parser ref token queue blob index object pack  # 7
    commit index stream buffer worker index  # 8
    stream branch commit ref socket rebase parser stream blob worker handle branch  # 9
    buffer worker remote object socket window worker pack branch header  # 10
    worker merge pack blob buffer header blob object remote worker  # 11
    handle index header merge  # 12
    commit buffer window remote blob window worker handle socket index ref  # 13
    commit ref tree token object ref handle pack tree  # 14
    window delta remote tree queue cache  # 15
    rebase handle worker commit rebase merge  # 16
    tree stream cache delta socket commit merge  # 17
    delta branch merge commit queue cache index branch  # 18
    ref tree object cache tree blob handle delta buffer blob parser  # 19
    header parser object commit tree ref token merge stream window cache  # 20
    blob worker stream queue handle header remote parser tree remote  # 21
    tree commit pack commit commit header window  # 22
    branch queue cache branch index buffer branch commit delta tree handle branch  # 23
    stream ref cache index branch socket  # 24
    blob pack header tree blob tree handle delta queue header stream parser  # 25
    socket window stream pack header index  # 26
    parser cache token stream commit queue header  # 27
    branch window rebase stream worker parser ref token cache rebase socket blob  # 28
    pack worker tree blob handle index pack header delta  # 29
    socket index pack stream  # 30
    rebase ref token tree token  # 31
    stream object rebase remote blob merge tree branch stream object handle parser  # 32
    cache queue stream queue tree queue pack blob  # 33
    branch object ref blob merge  # 34
    delta cache parser merge merge remote index blob  # 35
    commit handle commit remote remote tree blob cache cache  # 36
    delta delta commit blob token buffer cache branch commit branch stream  # 37
    worker worker stream commit merge object  # 38
    blob worker index buffer tree merge  # 39
    blob remote token index ref worker token remote handle index merge  # 40
    buffer delta merge object  # 41
    index tree socket remote  # 42
    pack stream ref token object ref buffer tree commit rebase  # 43
    tree stream cache pack branch worker rebase  # 44
    parser stream tree remote buffer object ref cache socket header  # 45
    object merge commit header window tree token  # 46
    queue pack commit header parser stream token commit object  # 47
    token window object branch rebase header token token  # 48
    commit remote delta index blob cache  # 49
    header remote remote pack tree  # 50
    token tree merge buffer header  # 51
    object merge stream merge socket header queue ref merge index tree  # 52
    merge parser index branch window window queue header commit delta  # 53
    token commit handle delta delta blob blob branch pack stream blob  # 54
    branch parser queue header merge stream queue  # 55
    blob window buffer socket object tree parser tree worker  # 56
    window blob handle token pack rebase  # 57
    buffer object socket header merge remote pack  # 58
    merge buffer parser worker cache window socket parser tree  # 59
    rebase blob branch remote worker merge index  # 60
    pack blob blob cache cache cache  # 61
    window index rebase queue blob tree ref commit delta pack  # 62
    handle handle remote socket  # 63
    socket tree commit window  # 64
    ref buffer pack handle header delta branch rebase stream worker stream  # 65
    header ref header handle cache tree  # 66
    cache buffer token commit blob rebase token remote object socket remote  # 67
    queue stream blob delta socket handle  # 68
    rebase header blob tree handle  # 69
    queue token handle socket object index worker buffer branch socket  # 70
    queue remote parser blob  # 71
    socket blob socket rebase token window commit parser merge  # 72
    queue tree worker buffer  # 73
