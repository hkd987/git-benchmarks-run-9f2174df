    worker branch remote parser ref pack  # 0
    socket window blob delta queue socket socket pack  # 1
    delta index index buffer commit parser delta handle  # 2
    tree remote delta handle commit  # 3
    queue commit ref ref parser  # 4
    parser remote index pack remote stream blob branch worker  # 5
    rebase stream remote branch pack ref commit ref  # 6
    cache header window stream parser stream cache  # 7
    object handle handle socket parser tree stream delta  # 8
    ref tree delta merge rebase header window tree  # 9
    merge remote delta blob queue stream handle  # 10
    commit merge window object remote merge index merge  # 11
    tree tree blob commit blob rebase index branch  # 12
    object branch commit stream  # 13
    commit cache object buffer handle header object buffer stream pack blob remote  # 14
    branch stream header buffer commit object worker merge delta index tree ref  # 15
    worker index buffer buffer rebase commit stream merge cache rebase  # 16
    worker tree window ref rebase ref remote blob ref ref remote blob  # 17
    rebase branch commit blob delta buffer  # 18
    commit buffer window handle merge  # 19
    ref remote worker handle index commit queue commit queue  # 20
    queue worker ref parser parser window ref tree header buffer  # 21
    queue tree ref remote tree branch stream  # 22
    window index blob header tree window cache  # 23
    object merge rebase branch blob handle rebase rebase remote worker worker pack  # 24
    handle tree handle pack delta ref delta header  # 25
    token pack header handle stream window stream queue merge worker cache queue  # 26
    remote object tree blob socket socket blob stream handle worker handle blob  # 27
    rebase handle pack branch ref buffer socket  # 28
    remote token rebase queue window merge parser blob pack delta commit  # 29
    merge window ref delta ref merge pack remote delta index  # 30
    delta commit window socket header pack commit  # 31
    branch worker stream window blob token  # 32
    commit rebase queue header cache pack stream cache  # 33
    remote window branch blob token rebase token remote  # 34
    queue delta token pack tree index pack rebase cache  # 35
    object rebase window rebase cache commit handle queue object worker token window  # 36
    branch worker worker token handle ref cache token pack  # 37
    worker token worker handle buffer blob remote rebase object cache socket socket  # 38
    queue queue stream rebase header merge tree commit pack blob buffer header  # 39
    window window token blob pack  # 40
    window pack cache remote tree branch stream token index window  # 41
    commit rebase socket header buffer  # 42
    tree cache merge buffer  # 43
    token buffer tree merge handle  # 44
    worker header window socket token cache blob  # 45
    remote remote tree delta  # 46
    cache remote commit parser queue buffer worker delta merge  # 47
    merge commit object stream socket remote  # 48
    merge commit merge remote queue object header commit handle remote  # 49
    commit merge queue queue worker delta object merge  # 0
    window cache index remote  # 1
    buffer window queue handle window tree  # 2
