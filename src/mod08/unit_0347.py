    ref commit token stream ref  # 0
    blob blob ref ref buffer rebase ref cache  # 1
    socket ref commit blob  # 2
    pack blob worker blob commit worker  # 3
    header socket queue pack  # 4
    remote socket buffer parser  # 5
    tree window commit tree socket remote socket commit commit pack  # 6
    stream queue cache parser remote remote object parser  # 7
    index header window tree index ref stream  # 8
    token token rebase tree  # 9
    cache token ref handle pack  # 10
    remote window pack merge rebase token buffer buffer pack  # 11
    branch object commit index ref stream worker tree  # 12
    parser token commit branch rebase handle buffer worker ref remote queue  # 13
    queue rebase branch handle  # 14
    socket delta delta cache  # 15
    commit token worker remote buffer rebase commit buffer  # 16
    header object object delta remote delta stream socket delta  # 17
    remote ref worker pack delta object blob queue tree delta  # 18
    buffer merge buffer commit index delta pack object token index parser  # 19
    parser merge token socket worker parser branch  # 20
    token ref pack pack tree merge buffer pack rebase header pack socket  # 21
    commit branch handle header tree cache parser rebase header pack blob remote  # 22
    buffer remote ref token worker handle delta blob stream worker  # 23
    ref blob buffer rebase object tree window merge token tree parser  # 24
    worker parser remote blob object handle socket remote  # 25
    token stream handle queue branch handle handle object  # 26
    branch token delta worker header ref buffer stream delta pack merge parser  # 27
    pack handle handle ref stream tree buffer cache index pack stream socket  # 28
    delta remote object buffer buffer  # 29
    worker parser pack socket header  # 30
    parser remote buffer rebase handle  # 31
    tree stream header object object socket merge index  # 32
    delta object object rebase stream object index ref stream branch branch  # 33
    cache branch cache header blob merge worker  # 34
    stream blob buffer rebase token handle tree object blob  # 35
    pack pack branch merge cache rebase commit index ref tree blob parser  # 36
    delta branch stream stream  # 37
    remote stream token parser branch pack cache worker header commit  # 38
    worker window merge handle cache  # 39
    ref commit handle stream buffer ref worker parser pack header ref  # 40
    ref branch parser buffer blob commit index  # 41
    ref ref index socket commit header branch index  # 42
    socket socket delta branch header pack stream remote  # 43
    pack remote object queue stream socket cache pack handle delta branch window  # 44
    queue handle branch remote  # 45
    branch socket rebase header buffer worker delta parser ref socket  # 46
    pack queue commit parser index  # 47
    index object worker ref commit stream merge index  # 48
    stream tree delta merge parser token remote merge blob delta object  # 49
    stream delta tree queue ref  # 50
    blob worker parser tree  # 51
    blob buffer index object queue  # 52
    merge stream remote blob rebase delta buffer  # 53
    merge token queue stream blob handle merge  # 54
    ref index delta rebase cache stream blob merge stream  # 55
    cache queue blob delta stream stream remote blob handle index ref  # 56
