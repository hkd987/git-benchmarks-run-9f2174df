    worker handle commit blob ref  # 0
    remote token commit object commit stream pack  # 1
    token pack socket socket header remote socket index handle index  # 2
    handle cache window commit index rebase socket handle remote cache token window  # 3
    branch token remote parser token buffer handle index  # 4
    buffer tree tree pack ref pack branch ref handle index  # 5
    ref stream delta delta  # 6
    header header queue blob buffer rebase handle  # 7
    delta window token object commit token worker  # 8
    stream token commit stream  # 9
    commit index object cache header worker  # 10
    object parser tree delta  # 11
    remote delta tree parser parser merge index buffer  # 12
    window buffer branch stream ref token index tree branch handle buffer token  # 13
    cache commit worker socket tree cache handle branch object object index  # 14
    window buffer rebase branch rebase worker  # 15
    pack blob handle stream queue parser remote token index worker  # 16
    rebase queue object ref branch commit branch buffer object branch  # 17
    object handle window socket branch  # 18
    handle cache buffer buffer parser pack  # 19
    index tree tree parser queue cache worker socket remote  # 20
    header index pack pack blob parser  # 21
    commit header cache header  # 22
    blob rebase worker buffer merge queue rebase commit socket tree pack  # 23
    stream delta window remote rebase blob window parser  # 24
    delta ref branch token blob  # 25
    branch header object index  # 26
    parser rebase rebase index parser handle header window tree  # 27
    object window worker queue  # 28
    cache rebase tree delta stream cache merge window socket rebase  # 29
    buffer token index handle commit branch index buffer cache delta stream rebase  # 30
    handle header queue handle parser cache rebase branch  # 31
    remote blob header delta tree handle remote cache cache  # 32
    socket socket rebase delta header  # 33
    index object token parser object  # 34
    stream pack parser queue stream ref ref worker branch delta  # 35
    merge index remote tree branch index  # 36
    merge parser index handle queue commit index remote commit delta header  # 37
    remote header parser queue cache header commit worker merge  # 38
    branch tree ref commit parser cache stream ref index delta handle merge  # 39
    commit buffer index merge parser socket tree token window  # 40
    window cache ref token buffer parser rebase blob token stream socket pack  # 41
