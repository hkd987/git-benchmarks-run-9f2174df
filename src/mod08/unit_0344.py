    branch blob worker merge window cache worker header  # 0
    socket queue cache socket cache parser ref branch delta socket socket  # 1
    worker tree tree branch delta branch  # 2
    handle socket queue tree handle  # 3
    window branch ref merge handle socket handle object blob  # 4
    pack ref commit queue queue  # 5
    queue parser tree index index socket  # 6
    window handle buffer token cache socket ref queue index queue merge index  # 7
    delta cache buffer socket queue rebase token socket worker buffer  # 8
    queue ref worker delta token header rebase handle cache handle window index  # 9
    token rebase handle window object token rebase token  # 10
    object merge index remote buffer  # 11
    pack queue socket token object ref remote merge queue remote ref blob  # 12
    header cache parser stream worker merge  # 13
    queue window socket header tree commit header blob rebase pack  # 14
    window parser remote delta buffer branch tree parser header socket ref tree  # 15
    tree delta worker tree worker branch merge blob cache  # 16
    blob commit window worker token header worker  # 17
    handle window rebase window commit socket parser commit remote  # 18
    cache parser buffer object  # 19
    remote parser header window  # 20
    stream merge branch buffer branch branch  # 21
    header tree cache index token buffer parser  # 22
    tree remote merge handle delta blob worker  # 23
    handle commit remote header worker parser delta index  # 24
    branch queue ref worker  # 25
    buffer blob ref pack commit  # 26
    index blob buffer token token window parser pack parser  # 27
    pack pack blob queue ref header  # 28
    delta tree tree socket branch queue  # 29
    remote commit handle socket parser commit pack ref pack queue pack handle  # 30
    stream token stream parser socket handle header queue buffer  # 31
    object object commit stream commit index delta delta  # 32
    token window rebase tree index window token branch pack index header  # 33
    rebase index tree socket token blob branch buffer object token blob  # 34
    token object socket pack  # 35
    socket commit buffer ref  # 36
    merge object header tree worker header object worker blob pack worker  # 37
    cache cache remote pack stream stream tree pack window tree header  # 38
    worker branch parser header buffer worker  # 39
    ref index blob blob  # 40
    handle rebase index ref  # 41
    object branch worker index handle  # 42
    worker remote buffer branch remote  # 43
    branch worker blob buffer socket buffer handle delta  # 44
    remote header branch worker handle header socket queue  # 45
    header socket socket object queue header parser merge commit delta delta  # 46
    worker header window socket merge worker window object parser index worker  # 47
    header tree header index header queue window cache parser  # 48
    buffer index buffer parser pack stream commit blob cache branch queue object  # 49
    blob header worker handle cache token remote  # 50
    handle header remote merge buffer token index pack ref commit blob window  # 51
    handle ref worker index object header index blob commit rebase worker worker  # 52
    tree socket buffer tree ref tree index tree commit tree socket  # 53
    rebase worker parser socket branch worker commit index pack branch queue  # 54
