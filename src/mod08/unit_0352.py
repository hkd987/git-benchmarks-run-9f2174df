    socket merge index merge token window parser merge stream commit socket blob  # 0
    remote cache header stream object cache object index handle index  # 1
    header delta pack commit queue token object rebase socket  # 2
    ref pack tree remote stream handle object branch remote branch  # 3
    commit stream commit parser object index index parser token stream blob  # 4
    ref delta blob buffer worker pack buffer worker queue parser stream ref  # 5
    cache ref parser commit ref index  # 6
    ref blob remote queue pack commit cache stream pack rebase header  # 7
    object stream window token rebase handle buffer parser pack  # 8
    buffer index socket worker merge worker  # 9
    index object queue handle handle queue header  # 10
    object index parser header cache cache ref object ref  # 11
    remote token parser pack object  # 12
    cache buffer queue cache  # 13
    blob cache worker commit remote  # 14
    stream blob cache index index socket cache branch token token worker  # 15
    window token stream blob cache token rebase  # 16
    stream rebase queue tree  # 17
    branch token buffer merge cache merge  # 18
    worker merge delta index tree window remote merge  # 19
    pack buffer branch merge cache rebase ref commit remote  # 20
    cache remote worker queue token  # 21
    parser pack buffer stream merge ref cache tree parser  # 22
    cache socket branch parser header token merge stream object index  # 23
    rebase commit blob pack stream index rebase object pack blob window  # 24
    object cache pack stream worker window queue queue ref tree remote  # 25
    delta branch worker pack header index  # 26
    merge token cache stream stream delta blob worker socket  # 27
    stream queue tree worker token rebase object delta  # 28
    token blob queue commit socket handle token rebase socket  # 29
    handle cache object buffer socket queue buffer commit window socket  # 30
    object cache queue rebase socket remote parser  # 31
    header queue tree commit buffer delta tree object ref header cache socket  # 32
    buffer queue window blob  # 33
    socket worker header tree tree window rebase queue cache queue parser worker  # 34
    parser header handle pack cache  # 35
    tree remote socket index object index  # 36
    token delta socket object socket remote merge index socket tree commit pack  # 37
    parser socket buffer window  # 38
    queue handle socket branch delta tree remote stream worker object remote socket  # 39
    remote delta delta worker commit branch blob branch stream  # 40
    header header rebase stream  # 41
    worker branch parser tree remote commit blob handle window  # 42
    queue worker remote rebase queue rebase socket  # 43
    index cache blob object  # 44
    remote ref tree index cache queue pack window  # 45
    branch blob pack object header worker cache index tree token  # 46
    cache stream index token index socket blob cache index header  # 47
    token socket merge rebase index pack stream cache  # 48
    socket index parser pack  # 49
    header delta delta parser worker socket tree merge socket  # 50
    merge pack remote branch header handle socket token cache remote cache merge  # 51
    commit queue remote ref delta remote branch remote tree stream  # 52
    branch window token object index merge remote branch buffer ref queue index  # 53
    object ref ref buffer header  # 54
    socket queue header cache object object merge ref index header pack  # 55
    socket stream ref ref ref ref tree  # 56
    pack handle handle window queue worker branch rebase object  # 57
    merge window remote window object remote header handle index blob blob  # 58
    delta index handle object index buffer rebase  # 59
    socket rebase delta queue handle  # 60
    queue handle object remote token remote stream delta  # 61
    worker socket tree cache cache  # 62
