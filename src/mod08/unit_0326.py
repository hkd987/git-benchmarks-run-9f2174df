    header token cache socket remote cache branch queue remote pack  # 0
    token blob stream remote parser parser socket delta pack tree commit  # 1
    parser stream header token cache  # 2
    parser rebase stream stream queue parser window branch ref handle index  # 3
    remote buffer stream object tree ref remote stream handle branch pack  # 4
    pack pack delta tree object worker ref pack commit  # 5
    token ref cache commit object handle object pack queue index  # 6
    socket window header merge object token cache parser index ref  # 7
    stream header remote parser merge header rebase pack object rebase blob window  # 8
    delta remote buffer worker remote  # 9
    object rebase socket parser window delta blob worker socket ref branch parser  # 10
    window handle index blob buffer merge commit commit parser window buffer  # 11
    object rebase stream parser index header token merge branch  # 12
    header tree buffer ref parser remote  # 13
    buffer merge worker worker worker remote merge object commit branch  # 14
    object rebase buffer queue branch worker socket stream cache tree  # 15
    rebase rebase socket header remote delta stream delta rebase branch socket  # 16
    buffer cache ref pack window parser branch  # 17
    stream header window delta  # 18
    branch remote commit tree tree branch handle parser parser stream  # 19
    tree parser index window  # 20
    parser index header commit  # 21
    branch parser cache branch stream handle  # 22
    object merge delta parser  # 23
    token token handle queue socket pack  # 24
    socket object token blob worker pack parser cache buffer buffer branch branch  # 25
    object token socket buffer remote cache remote remote remote object ref handle  # 26
    rebase index handle blob tree window tree  # 27
    socket handle blob ref window blob delta parser tree tree remote  # 28
    rebase buffer index blob commit handle delta stream blob worker delta socket  # 29
    socket merge cache socket  # 30
    merge rebase queue delta token commit window blob ref remote  # 31
    commit header handle worker parser  # 32
    merge header header queue ref rebase cache header window rebase pack  # 33
    branch window stream window remote socket index  # 34
    queue parser socket handle rebase remote header  # 35
    rebase index branch blob merge ref handle branch merge token worker  # 36
    queue pack commit cache worker object delta  # 37
    header commit blob ref blob index handle remote worker remote worker  # 38
    object window handle handle commit rebase handle index remote cache header  # 39
    window handle token header object tree branch stream remote handle  # 40
    parser rebase window pack object remote worker index  # 41
    token tree blob index token worker worker  # 42
    cache parser stream handle object branch blob merge header handle  # 43
    parser commit buffer rebase token branch blob  # 44
    object remote queue handle rebase delta  # 45
    pack remote cache index ref ref  # 46
    rebase tree remote branch rebase header buffer stream rebase socket tree  # 47
    queue pack rebase parser buffer  # 48
    worker ref rebase tree index stream worker stream queue  # 49
