    handle rebase rebase ref queue stream worker object rebase buffer  # 0
    header delta socket branch  # 1
    header tree token window branch  # 2
    socket worker header tree object handle rebase branch  # 3
    object ref branch object token rebase  # 4
    ref commit worker queue remote blob ref remote  # 5
    stream socket tree socket  # 6
    tree parser socket stream  # 7
    pack cache delta socket worker commit remote merge queue  # 8
    remote queue index index ref cache merge tree handle pack  # 9
    branch parser index parser socket tree handle parser remote blob branch pack  # 10
    commit pack cache worker  # 11
    socket pack object tree worker remote  # 12
    branch remote stream worker remote header worker parser stream rebase blob  # 13
    queue tree worker handle branch  # 14
    parser handle delta merge stream  # 15
    blob buffer ref object object  # 16
    ref merge merge stream socket index worker commit  # 17
    stream object branch handle  # 18
    ref object commit remote blob handle merge object buffer remote  # 19
    parser remote index object branch index delta  # 20
    header remote window parser blob buffer handle queue delta buffer commit stream  # 21
    object object worker branch parser commit socket object token merge blob  # 22
    tree remote handle parser stream parser socket pack delta rebase  # 23
    header blob socket queue rebase commit window object merge branch handle merge  # 24
    remote tree stream blob  # 25
    rebase socket index token blob rebase buffer remote  # 26
    header window remote stream delta ref object worker tree  # 27
    index worker socket pack token object cache window ref blob merge window  # 28
    queue buffer ref commit  # 29
    stream header socket pack socket buffer rebase merge parser delta ref header  # 30
    merge delta pack window branch remote object pack merge  # 31
    tree merge worker worker buffer token merge handle  # 32
    index rebase socket cache token worker stream merge  # 33
    pack header buffer token ref merge tree tree commit tree merge  # 34
    index object handle window header buffer delta index branch index  # 35
    worker cache blob merge stream stream cache rebase  # 36
    stream branch cache remote commit pack tree pack object queue branch blob  # 37
    buffer header handle index parser worker handle blob index blob window  # 38
    parser delta object remote token pack tree window blob ref merge stream  # 39
    rebase commit rebase branch tree queue ref socket ref handle  # 40
    delta buffer pack ref object merge worker tree ref object  # 41
    window blob stream stream branch window index parser pack token tree commit  # 42
    merge ref merge merge parser cache  # 43
    queue cache remote blob header tree handle window rebase ref token cache  # 44
    handle window index index token buffer branch  # 45
    rebase blob ref delta  # 46
    branch stream handle header delta stream merge worker commit  # 47
    buffer worker socket blob index object ref  # 48
    ref parser rebase index  # 49
    branch worker tree branch object worker parser queue token  # 50
    pack parser remote handle commit  # 51
    ref parser merge blob blob buffer token ref  # 52
    cache delta socket cache commit index index header ref worker stream  # 0
    ref merge merge queue  # 1
    buffer merge rebase window  # 2
    worker blob delta merge token blob branch merge merge index  # 3
    tree commit socket window blob ref handle branch queue  # 4
    delta parser merge remote tree stream tree tree rebase commit header commit  # 5
    handle ref cache worker token cache branch worker ref pack queue token  # 6
    rebase cache socket buffer commit parser token commit commit object commit  # 7
    delta index window commit queue rebase stream queue handle remote  # 8
    handle stream ref handle header handle blob stream blob token worker queue  # 9
    tree pack worker tree token socket cache  # 10
    window rebase parser ref header ref delta merge commit token branch worker  # 11
    delta branch handle queue merge window ref socket rebase  # 12
    delta merge branch object socket worker window  # 13
    merge queue queue parser token window ref queue token  # 0
    blob queue window window ref index parser pack merge blob ref queue  # 1
    pack commit handle socket  # 2
    header window pack queue worker tree worker tree  # 3
    cache branch blob socket rebase merge socket socket  # 4
