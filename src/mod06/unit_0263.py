    cache branch tree rebase stream handle cache  # 0
    socket stream blob header token remote rebase blob queue blob cache window  # 1
    commit cache object commit  # 2
    token object ref commit blob merge  # 3
    commit stream rebase delta tree commit token buffer  # 4
    window ref merge object handle socket ref socket merge  # 5
    rebase pack token pack worker  # 6
    index remote merge merge socket buffer stream delta delta stream delta  # 7
    queue stream worker stream blob object queue  # 8
    remote stream queue remote queue commit remote blob  # 9
    delta blob tree handle pack stream buffer blob  # 10
    parser queue merge ref stream index object  # 11
    commit branch buffer object commit  # 12
    token queue commit parser  # 13
    tree index commit cache parser commit buffer  # 14
    delta merge buffer pack buffer rebase  # 15
    stream ref merge rebase worker header cache pack remote token  # 16
    pack token socket branch commit commit branch handle parser remote parser remote  # 17
    cache delta commit delta delta token ref index  # 18
    socket buffer object blob window tree  # 19
    index branch delta branch  # 20
    parser worker blob blob index delta worker blob branch  # 21
    token branch window header parser socket buffer index object rebase  # 22
    ref socket blob commit handle buffer index pack  # 23
    worker queue blob branch delta rebase queue queue pack  # 24
    token branch branch stream header object remote pack handle  # 25
    branch index cache token buffer branch buffer pack object parser branch  # 26
    pack socket remote tree index remote handle  # 27
    worker ref ref socket ref branch stream branch  # 28
    cache pack ref token handle commit remote  # 29
    remote tree tree header parser token pack index  # 30
    queue stream token blob  # 31
    index branch token merge socket remote queue handle remote parser object  # 32
