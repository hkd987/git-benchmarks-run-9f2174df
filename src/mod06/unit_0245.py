    cache parser blob buffer socket socket socket header worker  # 0
    commit token branch worker socket  # 1
    token blob remote blob  # 2
    worker buffer queue token parser socket buffer stream buffer queue  # 3
    cache worker rebase queue socket queue  # 4
    index remote index cache remote window  # 5
    worker worker object handle  # 6
    rebase socket commit ref parser object blob branch index blob cache  # 7
    worker handle token delta pack window worker socket  # 8
    socket socket handle queue blob branch ref  # 9
    socket buffer socket header buffer socket pack remote parser tree cache index  # 10
    header queue object queue tree tree branch socket buffer  # 11
    rebase delta parser parser buffer index worker  # 12
    remote pack worker ref worker  # 13
    rebase token tree pack  # 14
    buffer merge blob commit merge window window parser stream socket socket index  # 15
    buffer delta token socket commit  # 16
    commit blob tree branch object header index remote branch  # 17
    commit parser branch delta rebase tree commit rebase stream cache token  # 18
    worker branch pack ref index  # 19
    rebase branch stream index rebase delta header  # 20
    buffer tree delta window merge delta index header branch branch cache  # 21
    window cache branch merge buffer ref window queue socket  # 22
    commit queue merge worker rebase handle  # 23
    header handle worker worker token worker socket rebase header stream  # 24
    pack token commit index ref commit  # 25
    header window queue object queue  # 26
    rebase buffer pack ref branch pack rebase rebase buffer object  # 27
    index parser parser merge blob  # 28
    rebase header delta stream socket rebase object  # 29
    queue object tree stream stream buffer  # 30
    socket ref rebase stream pack token handle blob merge  # 31
    delta branch handle remote pack tree branch buffer worker  # 32
    delta queue delta window ref pack token  # 33
    remote cache stream cache merge object pack commit pack stream delta  # 34
    tree token index branch commit branch ref index buffer  # 35
    pack branch pack pack branch handle branch remote  # 36
    header header token worker buffer buffer cache queue  # 37
    cache blob remote commit parser rebase commit merge  # 38
    socket ref tree window pack cache remote  # 39
    remote ref stream object parser cache socket blob remote handle tree parser  # 40
    pack commit stream parser handle  # 41
    commit window object ref blob  # 42
    index parser socket stream worker merge cache merge window ref tree  # 43
    parser ref blob window commit queue merge  # 44
    commit parser handle socket  # 45
    blob object merge rebase rebase tree blob blob  # 46
    parser branch handle merge merge ref queue  # 47
    ref ref delta merge header token blob index index pack tree  # 48
    worker window index index buffer token tree ref  # 49
    branch buffer header header ref pack window branch  # 50
    object index buffer remote handle  # 51
    queue handle handle stream  # 52
    merge cache socket socket branch parser socket merge  # 53
    socket token buffer stream rebase handle queue queue object  # 54
    rebase object buffer remote pack header blob worker token cache  # 55
    token parser handle commit branch header buffer pack socket  # 56
    rebase ref pack rebase merge delta stream  # 57
    object pack stream buffer tree socket queue blob object remote tree  # 58
    header cache rebase worker  # 59
    header commit merge queue pack  # 60
    window object tree tree  # 61
    buffer index index ref worker rebase window object queue buffer remote branch  # 62
    ref worker parser handle pack ref socket worker rebase  # 63
    branch window header ref parser tree branch  # 0
    branch parser window token cache buffer parser  # 1
    branch remote parser branch tree worker ref buffer cache delta handle  # 2
    header branch parser index worker branch  # 3
    branch stream remote header cache handle object ref window queue merge cache  # 4
    commit index remote header worker window buffer index merge queue socket  # 5
