    stream token cache blob object socket  # 0
    buffer branch parser commit commit queue  # 1
    pack ref parser queue worker socket remote ref parser  # 2
    header handle ref header index  # 3
    queue header buffer pack blob worker header stream  # 4
    index merge object window  # 5
    branch stream delta remote remote delta delta tree  # 6
    branch ref pack branch branch  # 7
    socket parser cache socket socket handle handle commit header  # 8
    cache header queue branch merge object stream  # 9
    merge pack rebase window header token tree  # 10
    blob pack handle branch pack object buffer worker blob merge remote parser  # 11
    remote worker queue parser ref buffer window branch remote parser remote ref  # 12
    index stream pack handle header socket token branch header  # 13
    parser queue delta blob commit delta handle header object commit index worker  # 14
    pack blob pack parser object delta queue cache  # 15
    queue socket merge handle worker ref buffer token commit ref header index  # 16
    token branch stream branch parser buffer stream parser remote  # 17
    object rebase delta tree window buffer commit  # 18
    object handle object rebase stream worker  # 19
    merge cache branch cache cache  # 20
    stream window cache window header  # 21
    header worker blob delta merge buffer  # 22
    queue rebase object ref ref remote parser pack merge  # 23
    commit delta token window token remote branch handle  # 24
    delta blob ref token rebase window worker ref pack ref  # 25
    delta header parser header window blob merge blob handle index socket  # 26
    header stream remote index  # 27
    socket handle socket ref commit commit commit token  # 28
    delta header stream socket  # 29
    object tree remote ref handle queue buffer merge cache  # 30
    buffer delta object delta blob buffer stream index  # 31
    branch handle merge ref blob  # 32
    handle header merge commit remote handle pack pack queue queue  # 33
    socket blob blob tree queue token  # 0
    pack socket merge stream remote handle ref merge token ref index cache  # 1
    tree remote buffer window blob handle merge token remote window  # 2
    window pack merge object cache index worker blob handle worker handle socket  # 3
    branch worker handle header queue object buffer stream socket index queue  # 4
    branch delta commit commit worker ref delta remote index index tree pack  # 5
    ref buffer merge queue tree tree ref window object blob pack cache  # 6
    tree queue rebase commit  # 7
    ref commit branch worker stream rebase index object header header queue tree  # 8
    commit branch ref token commit object merge token commit  # 9
