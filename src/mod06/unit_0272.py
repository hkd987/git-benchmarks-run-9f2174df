    branch worker commit tree tree tree parser index blob merge commit  # 0
    worker handle blob rebase queue rebase  # 1
    ref merge tree cache cache index blob worker blob worker merge queue  # 2
    delta handle token delta commit  # 3
    ref parser index cache parser pack  # 4
    branch cache buffer worker window object commit rebase rebase token rebase  # 5
    token commit buffer queue handle branch index pack  # 6
    handle index merge branch blob  # 7
    cache branch socket window merge branch blob blob buffer  # 8
    tree buffer token window pack commit object window buffer remote  # 9
    branch parser stream merge delta blob blob buffer parser header object commit  # 10
    blob worker tree cache commit queue stream window commit  # 11
    branch stream window branch merge buffer  # 12
    tree buffer branch header handle handle  # 13
    cache window rebase handle  # 14
    handle parser queue token handle socket parser delta stream ref  # 15
    merge buffer buffer merge stream buffer  # 16
    handle buffer queue ref ref rebase tree worker worker socket  # 17
    commit blob blob worker  # 18
    handle worker cache ref remote parser branch object header  # 19
    branch delta socket blob pack ref cache window buffer parser stream index  # 20
    tree rebase socket window pack index queue token  # 21
    queue cache commit branch buffer parser  # 22
    index merge token merge merge delta buffer worker delta merge ref  # 23
    buffer window rebase header socket  # 24
    blob pack buffer delta socket object delta rebase  # 25
    window blob buffer cache remote cache delta rebase worker rebase  # 26
    socket object stream stream branch socket remote branch token  # 27
    index handle branch handle ref cache tree tree  # 28
    merge cache object header token branch blob parser stream  # 29
    header cache tree blob index parser  # 30
    blob cache socket merge commit socket parser merge window branch merge  # 31
    queue merge handle pack rebase tree cache window parser blob window  # 32
    branch merge handle socket blob handle pack buffer parser index cache cache  # 33
    tree stream branch blob index commit object worker ref object index stream  # 34
    rebase buffer branch stream object handle queue  # 35
    branch rebase stream handle cache handle cache merge  # 36
    window pack branch branch merge ref parser rebase token object  # 37
    commit object commit merge socket merge  # 38
    rebase token object commit header header blob parser branch handle parser  # 39
    pack window header tree handle handle header window header  # 40
    socket parser delta commit worker tree parser commit token stream  # 41
    commit index queue parser index rebase parser rebase remote buffer window  # 42
    window commit branch branch  # 43
    branch header delta socket window handle worker handle buffer merge  # 44
    rebase queue socket cache buffer blob buffer stream delta header  # 45
    worker token header queue header cache rebase branch socket delta stream  # 46
    parser header commit header merge handle handle pack header cache  # 47
    index object ref parser branch header remote  # 48
    blob tree parser handle ref header buffer window merge object index socket  # 49
    pack object buffer header branch merge queue token merge parser remote handle  # 50
    remote parser queue window worker  # 51
    cache object cache index rebase merge tree  # 52
    blob window blob blob worker socket worker remote merge  # 53
    parser object blob ref stream commit branch queue cache merge pack  # 54
    merge rebase window object ref worker socket pack  # 55
    merge handle remote stream socket ref branch ref handle parser worker  # 56
    rebase socket pack worker blob branch header worker queue remote  # 57
    cache stream socket parser tree object socket index parser queue index  # 58
    branch merge ref blob index window stream  # 59
    worker queue queue rebase delta commit  # 60
    merge worker buffer index index socket commit  # 61
    buffer merge tree stream  # 62
    delta blob object branch  # 63
    queue ref stream socket queue delta parser parser rebase  # 64
    tree ref header pack parser parser commit blob handle remote pack parser  # 65
