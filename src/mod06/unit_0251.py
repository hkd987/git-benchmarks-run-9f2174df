    blob cache pack rebase worker token index buffer branch cache  # 0
    index socket ref commit queue socket handle delta  # 1
    merge token ref pack buffer  # 2
    header buffer tree delta token queue branch remote handle queue merge worker  # 3
    window remote ref cache  # 4
    object remote remote pack worker branch  # 5
    socket token header socket remote merge socket blob  # 6
    pack parser handle socket rebase pack handle  # 7
    socket cache queue object delta delta commit queue buffer  # 8
    blob parser cache remote index worker tree queue rebase  # 9
    merge header parser stream tree rebase queue  # 10
    blob worker queue parser remote queue buffer header  # 11
    socket buffer tree commit queue remote window  # 12
    index worker parser queue handle merge socket index queue  # 13
    delta header blob rebase tree  # 14
    blob tree window object object delta index commit  # 15
    object tree pack rebase rebase token buffer index queue  # 16
    rebase parser object remote object token handle tree  # 17
    index object ref cache blob window socket  # 18
    handle remote queue parser cache header object worker  # 19
    object object delta commit remote socket merge  # 20
    pack branch pack pack pack  # 21
    commit index tree token stream worker remote index queue stream ref token  # 22
    remote merge window stream index index commit handle object  # 23
    stream branch rebase ref ref delta merge remote tree  # 24
    remote rebase remote worker window socket  # 25
    ref buffer merge branch delta header rebase cache  # 26
    remote header buffer window stream cache buffer  # 27
    tree header parser cache queue delta  # 28
    index queue blob header  # 29
    delta branch stream queue ref buffer delta stream window tree rebase  # 30
    queue ref queue worker commit header  # 31
    cache commit rebase rebase buffer pack  # 32
    parser delta handle stream stream merge queue pack token socket rebase tree  # 33
    commit blob cache cache  # 34
    rebase worker token parser  # 35
    merge branch remote parser rebase parser parser  # 36
    parser tree ref branch delta buffer branch blob  # 37
    index object token merge parser rebase token rebase  # 38
    delta commit parser delta commit ref  # 39
    commit merge rebase pack delta  # 40
    commit branch handle cache branch index cache  # 41
    buffer pack handle tree pack parser branch index remote commit pack window  # 42
    ref ref cache queue remote delta branch branch tree rebase header socket  # 43
    blob blob branch parser socket remote blob socket commit pack rebase  # 44
    ref socket index blob tree buffer buffer blob  # 45
    parser commit index buffer buffer worker  # 46
    pack cache blob index stream remote remote buffer stream index pack  # 47
    buffer header queue parser parser handle window stream pack  # 48
    remote socket pack commit pack remote  # 49
    cache merge index blob rebase blob rebase  # 50
    buffer remote token queue pack index commit  # 51
    queue queue header pack pack cache cache header tree queue buffer  # 52
    index remote stream queue remote rebase branch parser stream ref object queue  # 53
    worker stream branch worker branch cache index branch stream  # 54
    queue stream index window rebase rebase  # 55
    object stream queue delta token socket  # 56
    tree buffer remote worker buffer branch handle buffer token  # 57
    buffer token pack rebase parser buffer merge tree index token window header  # 58
    branch rebase rebase handle handle pack merge header blob stream header  # 59
    header worker cache cache delta merge  # 60
