    parser parser pack merge index  # 0
    pack commit branch buffer branch  # 1
    remote socket window branch queue blob object worker tree remote  # 2
    stream buffer window merge tree stream  # 3
    parser stream token index tree remote buffer  # 4
    remote queue worker stream stream delta parser token delta remote pack handle  # 5
    tree branch header pack  # 6
    index tree window commit  # 7
    pack index index window object remote cache merge window  # 8
    pack stream remote buffer rebase pack index delta  # 9
    commit remote branch header branch delta handle remote pack socket  # 10
    window delta remote merge  # 11
    stream socket merge rebase window token  # 12
    worker object commit cache stream cache  # 13
    stream branch window rebase merge pack socket commit rebase pack  # 14
    buffer buffer tree merge pack commit handle socket merge pack blob parser  # 15
    delta object header handle queue handle  # 16
    socket parser delta branch worker blob  # 17
    merge handle blob blob buffer commit cache queue handle commit branch cache  # 18
    buffer buffer pack header cache merge  # 19
    rebase pack object buffer cache delta  # 20
    blob stream worker handle stream stream window blob branch  # 21
    queue commit queue pack ref stream header cache branch worker tree  # 22
    parser delta buffer tree rebase commit worker buffer queue  # 23
    parser commit remote merge object handle  # 24
    branch branch ref handle rebase  # 25
    socket remote parser socket index buffer rebase commit  # 26
    token header window merge merge commit token token delta object ref rebase  # 27
    ref token commit header ref  # 28
    header cache object merge branch ref tree blob rebase  # 29
    parser buffer object window token remote merge queue ref  # 30
    delta index worker merge socket  # 31
    queue pack worker queue cache rebase object parser branch delta parser parser  # 32
    stream blob queue merge worker tree handle parser  # 33
    remote tree header window cache branch branch index merge worker commit  # 34
    cache queue buffer delta socket  # 35
    ref token pack blob object window cache handle  # 36
    queue handle stream token cache ref worker  # 37
    commit branch cache window  # 38
    cache delta merge tree token header worker queue ref header  # 39
    pack buffer index index branch socket remote stream  # 40
    branch queue object merge worker token queue index branch cache  # 41
    stream index index tree socket branch ref stream merge queue rebase buffer  # 42
    ref socket delta stream tree rebase delta object blob worker window tree  # 43
    rebase branch window branch queue pack merge header rebase commit stream  # 44
    commit cache branch index rebase worker branch  # 45
    worker merge queue blob commit ref index header parser header branch ref  # 46
