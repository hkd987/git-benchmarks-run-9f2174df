    branch object queue socket branch socket buffer queue  # 0
    remote window buffer remote commit  # 1
    worker merge tree object delta socket parser commit handle stream  # 2
    index blob commit handle index window worker queue remote merge index  # 3
    index index object rebase rebase branch branch remote  # 4
    merge branch remote header commit stream tree  # 5
    merge handle queue worker  # 6
    object handle buffer ref blob merge stream header branch  # 7
    merge object queue buffer merge pack ref  # 8
    buffer handle window ref pack blob rebase index object remote  # 9
    worker ref blob delta blob parser worker cache window window  # 10
    rebase cache handle rebase blob merge buffer buffer  # 11
    worker branch token index worker parser blob pack  # 12
    worker window queue object tree parser object pack  # 13
    cache merge header tree rebase tree delta remote  # 14
    merge rebase worker pack commit  # 15
    stream tree parser worker buffer  # 16
    merge blob blob token token token buffer  # 17
    token buffer parser delta socket parser window header window  # 18
    cache rebase pack header socket  # 19
    pack handle tree window ref worker object commit rebase socket  # 20
    buffer merge remote index header remote buffer delta remote tree  # 21
    merge rebase token ref socket object branch  # 22
    remote worker cache tree cache blob socket branch buffer  # 23
    pack worker window queue branch worker handle cache socket ref index  # 24
    tree commit header branch index window  # 25
    branch parser ref rebase  # 26
    parser ref stream buffer blob pack ref remote stream rebase  # 27
    worker header branch ref index index stream index  # 28
    remote tree handle rebase branch pack ref queue  # 29
    worker buffer handle delta delta handle rebase index  # 30
    tree merge commit branch  # 31
    commit ref remote window cache blob branch queue  # 32
    pack pack worker token worker blob delta queue tree  # 33
    delta merge handle pack  # 34
    window queue index parser  # 35
    pack parser handle cache header cache socket tree  # 36
    stream token index buffer rebase index  # 37
    window parser handle socket blob  # 38
    branch delta window merge handle token worker ref rebase token header  # 39
    window merge ref index window parser  # 40
    handle tree index stream commit socket  # 41
    queue branch queue blob queue handle header object header merge merge queue  # 42
    commit worker window merge header object tree branch index worker socket socket  # 43
    pack worker remote stream header ref pack buffer tree token object  # 44
    rebase handle parser tree  # 45
    ref cache stream handle window handle blob ref delta branch  # 46
    handle token stream queue blob commit stream remote delta  # 47
    branch header remote commit cache  # 48
    header handle worker branch buffer index queue cache rebase pack  # 49
    parser socket index branch blob index rebase handle  # 50
    window commit remote index remote remote parser header buffer window index  # 51
    buffer tree stream object parser cache  # 52
    header token merge tree pack worker remote  # 53
    delta index cache queue  # 54
    delta object stream branch header tree tree blob blob header rebase  # 55
    branch ref stream stream delta socket  # 56
    header worker commit commit  # 57
    delta pack commit queue delta token rebase worker blob tree tree handle  # 58
    commit object merge remote queue token parser handle ref blob worker  # 59
