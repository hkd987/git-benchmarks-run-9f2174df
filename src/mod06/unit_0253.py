    ref queue rebase socket branch buffer handle token  # 0
    stream cache parser merge window stream rebase window tree buffer header  # 1
    token branch pack header  # 2
    pack worker branch branch parser handle  # 3
    parser branch ref branch buffer  # 4
    stream pack window handle rebase header delta worker queue branch  # 5
    header worker stream delta object cache rebase  # 6
    buffer remote worker socket tree delta branch stream stream index object tree  # 7
    window commit index queue branch  # 8
    socket remote socket merge commit remote socket  # 9
    token merge tree delta object header commit header header header  # 10
    index index stream remote delta ref handle pack rebase buffer handle  # 11
    stream stream branch blob commit object object token token ref  # 12
    parser window commit socket index  # 13
    token delta buffer remote commit pack token rebase  # 14
    stream token delta remote parser  # 15
    remote cache delta queue tree remote tree merge  # 16
    socket ref worker socket worker worker window remote  # 17
    index merge commit queue  # 18
    buffer delta object ref header token  # 19
    tree pack socket handle buffer merge branch cache parser  # 20
    ref window header token parser object worker rebase object merge  # 21
    parser window blob branch window  # 22
    merge worker stream remote socket  # 23
    rebase stream object buffer merge queue rebase object commit queue  # 24
    socket merge window stream parser cache stream delta header  # 25
    window ref branch tree commit buffer remote delta tree worker blob  # 26
    buffer header stream header buffer  # 27
    merge commit cache buffer blob commit remote handle delta buffer rebase  # 28
    blob handle commit branch socket rebase pack buffer stream ref index buffer  # 29
    socket parser window remote object ref pack socket object socket  # 30
    branch stream ref window parser queue merge header  # 31
    rebase rebase delta token parser header window handle buffer worker worker  # 32
    buffer index parser commit branch parser blob window cache tree queue branch  # 33
    branch delta remote index buffer cache worker delta  # 34
    remote worker socket commit blob merge rebase header commit commit pack  # 35
    queue object object rebase tree commit ref  # 36
    parser merge object remote rebase  # 37
    parser remote socket index header buffer  # 38
    pack worker window branch commit  # 39
    branch socket commit header worker  # 40
    ref worker token window  # 41
    remote handle pack worker  # 42
    rebase rebase commit merge socket socket object  # 43
    queue tree commit rebase stream buffer cache  # 44
    merge remote token object worker parser worker pack delta ref pack  # 45
    commit window buffer parser remote cache merge blob ref  # 46
    parser object remote handle delta cache buffer ref pack window  # 47
    commit window rebase cache ref delta worker window ref  # 48
    stream rebase merge queue blob worker remote rebase commit  # 49
    branch blob worker blob cache ref parser tree cache delta stream branch  # 50
    stream parser header socket  # 51
    commit header object cache worker stream merge handle pack  # 52
    socket queue commit header object delta header merge branch buffer branch object  # 53
    header commit socket window delta commit buffer merge object branch  # 54
    remote tree worker ref handle queue socket stream ref window parser  # 55
    merge remote delta buffer index token  # 56
    pack header delta stream socket queue tree tree branch token pack  # 57
    parser socket window delta pack worker merge  # 58
    header parser cache parser delta  # 59
    rebase queue header handle stream branch object handle  # 60
    buffer delta ref cache remote object ref rebase token stream pack rebase  # 61
    commit rebase tree ref token header object worker window branch cache  # 62
    cache branch branch stream  # 63
    cache index remote cache index parser blob pack  # 64
    token merge cache object commit rebase worker blob handle branch window buffer  # 65
