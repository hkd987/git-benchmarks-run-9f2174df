    socket window queue buffer delta index stream ref cache socket  # 0
    socket tree ref index socket  # 1
    index object commit buffer delta queue blob merge stream branch rebase  # 2
    socket queue remote index commit socket parser handle  # 3
    stream token cache cache pack window index window  # 4
    branch object index blob delta remote pack window queue tree delta  # 5
    remote header buffer commit object stream worker cache cache worker pack object  # 6
    queue index window pack index rebase commit buffer pack merge  # 7
    header queue queue buffer commit cache index socket header token  # 8
    index stream buffer cache delta remote  # 9
    queue tree blob merge buffer stream tree parser  # 10
    ref remote queue buffer rebase branch delta queue rebase cache blob cache  # 11
    handle worker parser buffer queue token tree queue commit index  # 12
    ref rebase branch delta cache worker object cache parser  # 13
    worker ref stream buffer index object  # 14
    handle delta handle queue merge index  # 15
    tree header queue remote  # 16
    token buffer window socket  # 17
    socket worker rebase pack stream window queue index buffer  # 18
    socket index queue remote delta header pack blob socket stream queue branch  # 19
    merge index remote index queue tree blob socket remote  # 20
    window parser handle token buffer rebase ref ref cache remote cache  # 21
    branch header stream parser queue token  # 22
    remote window remote delta  # 23
    pack index socket stream merge handle token index merge  # 24
    cache index queue socket rebase pack remote token socket pack delta stream  # 25
    ref queue worker queue parser header handle  # 26
    cache parser object queue object worker worker remote socket blob rebase worker  # 27
    index tree handle commit commit  # 28
    rebase socket token remote  # 29
    merge stream parser commit commit commit  # 30
    queue remote remote queue branch merge tree  # 31
    buffer cache parser index ref  # 32
    ref blob commit ref branch handle parser stream worker  # 33
    cache object ref header header ref socket blob worker  # 34
    queue ref index header delta delta branch tree cache branch socket  # 35
    pack worker tree blob queue remote token  # 36
    rebase merge handle window pack header stream header commit  # 37
    index token branch ref queue pack buffer  # 38
    worker commit branch queue object  # 39
    handle blob ref remote object worker window header  # 40
    merge ref queue parser blob  # 41
    object pack cache stream handle  # 42
    stream object branch queue rebase worker cache delta pack  # 43
    socket header header socket ref rebase  # 44
    token token window pack queue rebase buffer object socket delta token  # 45
    socket buffer merge blob  # 46
    ref pack index window socket remote index  # 47
    branch parser remote delta object ref remote buffer buffer token merge stream  # 48
    stream queue header stream tree pack queue ref socket buffer  # 49
    index index queue ref rebase tree buffer commit  # 50
    ref socket cache merge parser delta socket cache remote pack delta  # 51
    parser parser index delta index delta queue token pack queue  # 52
    blob commit handle ref object remote  # 0
    cache pack handle branch blob buffer worker header rebase token  # 1
    delta token handle rebase  # 2
    window socket worker tree branch socket  # 3
    merge socket ref branch index queue tree header index queue buffer parser  # 4
    queue commit socket parser remote worker  # 5
    ref pack token socket branch tree  # 6
    token parser ref buffer cache stream token ref stream  # 7
    commit header object merge commit stream header commit socket remote remote  # 8
    token delta pack object merge socket rebase token handle buffer  # 9
    queue stream object cache socket header pack window parser merge delta  # 10
    ref queue merge tree buffer queue buffer object cache cache branch  # 0
    branch handle parser socket buffer parser remote cache buffer blob socket parser  # 1
    tree index token commit object  # 2
    token pack tree worker cache  # 3
    pack merge header pack parser ref header tree delta window pack  # 4
