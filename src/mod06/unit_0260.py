    parser object rebase cache merge socket merge buffer branch header socket  # 0
    token object remote delta buffer  # 1
    ref handle merge queue remote token  # 2
    buffer rebase worker rebase parser tree index queue  # 3
    stream cache cache delta  # 4
    tree tree blob tree window remote socket rebase  # 5
    queue header blob remote  # 6
    object handle commit token commit pack queue merge stream  # 7
    window worker worker header buffer delta pack object  # 8
    socket object queue delta tree parser remote remote branch buffer  # 9
    merge ref tree object parser stream parser pack  # 10
    merge parser buffer tree tree merge  # 11
    remote window commit stream worker queue object merge socket blob remote socket  # 12
    buffer handle delta worker window remote queue index pack  # 13
    remote header stream blob parser handle rebase parser cache cache  # 14
    cache window branch index commit cache object queue index  # 15
    token pack tree token parser stream  # 16
    stream queue commit queue worker object  # 17
    pack pack blob queue  # 18
    object queue rebase cache parser merge blob cache parser  # 19
    cache header socket handle branch queue socket tree tree tree stream cache  # 20
    tree pack parser worker worker  # 21
    blob buffer ref header token index token ref  # 22
    handle delta rebase rebase tree tree object rebase index  # 23
    delta queue pack socket parser object commit branch pack handle window merge  # 24
    object remote merge cache remote index header index rebase header index  # 25
    cache handle blob branch branch token delta commit stream pack  # 26
    queue delta buffer remote index blob object handle blob object  # 27
    rebase ref merge handle  # 28
    delta header blob index cache delta rebase  # 29
    queue commit pack merge rebase parser socket object  # 30
    worker branch merge ref token pack ref commit  # 31
    tree merge tree remote object cache socket token merge  # 32
    parser parser index pack merge branch  # 33
    parser window buffer window rebase queue  # 34
    ref stream stream pack merge ref commit object commit branch delta handle  # 35
    tree worker remote socket stream object rebase branch parser remote queue stream  # 36
    branch rebase merge pack commit queue ref pack blob tree token rebase  # 37
    parser stream rebase window queue commit blob rebase  # 38
    branch ref remote index commit object  # 39
    socket ref branch queue pack socket header blob cache  # 40
    token blob queue buffer header blob delta header ref queue cache  # 41
    token ref window queue socket blob branch commit delta commit branch blob  # 42
    handle delta commit handle cache delta merge remote commit blob cache  # 43
    blob token parser blob tree socket ref  # 44
    handle cache rebase cache handle tree object stream tree  # 45
    branch delta header window parser  # 46
    token object token tree header cache object  # 47
    buffer pack rebase socket parser merge  # 48
    buffer cache header merge pack pack rebase token header pack  # 49
    cache worker ref commit header worker worker handle  # 50
