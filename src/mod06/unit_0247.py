    ref parser merge socket parser ref socket worker stream  # 0
    token branch token token rebase ref handle  # 1
    socket token commit buffer stream cache buffer  # 2
    object parser handle worker worker index cache merge stream  # 3
    header object token pack rebase cache object  # 4
    token pack queue commit commit ref commit socket index  # 5
    delta rebase commit token merge index object blob socket  # 6
    buffer pack blob pack window rebase  # 7
    worker remote socket token  # 8
    header stream object queue worker header blob branch window merge object index  # 9
    pack remote branch parser remote buffer ref ref handle commit socket rebase  # 10
    buffer handle branch remote  # 11
    commit tree object cache merge commit rebase branch  # 12
    rebase branch handle index index merge window parser  # 13
    object index header window index  # 14
    handle cache rebase cache merge cache parser socket window  # 15
    delta merge object rebase pack tree stream worker stream window cache  # 16
    remote queue tree commit branch ref stream tree window  # 17
    stream commit rebase object  # 18
    remote tree branch worker worker token blob object queue socket window rebase  # 19
    queue tree socket rebase worker parser pack index worker blob  # 20
    token commit commit cache delta queue header  # 21
    merge remote tree worker parser header  # 22
    header remote token commit handle branch socket tree object  # 23
    branch index remote token commit buffer queue  # 24
    worker blob stream handle  # 25
    cache delta token header cache stream pack header cache worker buffer  # 26
    merge worker merge rebase buffer merge socket  # 27
    worker window ref token window  # 28
    parser buffer parser queue object remote remote ref  # 29
    commit merge cache buffer  # 30
    cache blob branch tree buffer object window queue ref worker  # 31
    blob queue handle object rebase tree queue  # 32
    header tree tree remote merge socket commit rebase remote merge pack  # 33
    blob parser queue buffer commit commit ref buffer parser buffer commit  # 34
    header token window socket worker merge buffer stream queue  # 35
    merge remote delta delta pack rebase delta delta worker worker handle merge  # 36
    worker token rebase worker  # 37
    queue header object remote blob parser header  # 38
    parser stream socket parser queue pack window branch worker queue  # 39
    stream branch object header rebase socket branch commit blob pack  # 40
    queue cache tree rebase commit blob header object token  # 41
    buffer cache token socket rebase socket parser  # 42
    object rebase index merge token stream branch commit window token handle  # 43
    pack rebase delta cache buffer handle tree delta cache window  # 44
    parser token remote cache commit token worker delta pack  # 45
    socket worker pack stream socket tree header worker index queue delta  # 46
    handle parser branch tree stream worker pack worker cache  # 47
    rebase window tree rebase worker index  # 48
    blob blob blob merge  # 49
    ref handle tree index header object  # 50
    commit worker delta buffer ref  # 51
    worker handle ref rebase branch blob index  # 52
    handle handle merge worker token socket merge cache blob token  # 53
    branch ref blob merge branch queue blob tree branch  # 54
    header blob tree token  # 55
    window window parser header branch  # 56
    branch queue delta handle pack index index handle ref queue remote  # 57
    cache stream rebase blob object parser queue blob handle blob ref  # 58
    worker remote commit token rebase handle ref object merge pack  # 59
    pack commit tree buffer rebase index ref delta  # 60
    parser stream rebase branch stream handle  # 61
    queue pack cache merge  # 62
    branch buffer merge index rebase parser socket object delta queue  # 63
    tree handle remote merge socket queue  # 64
    commit ref blob remote object token  # 65
    buffer worker socket stream blob branch index buffer parser  # 66
    socket delta delta branch buffer branch branch commit cache queue  # 67
    blob window commit ref window header buffer remote blob token  # 68
    buffer blob rebase token branch ref stream pack queue object parser  # 69
    rebase object header buffer  # 70
    merge rebase parser blob cache delta tree header  # 71
    merge rebase rebase window parser merge  # 72
