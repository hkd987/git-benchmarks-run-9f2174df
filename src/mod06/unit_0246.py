    stream worker branch ref ref parser branch remote pack  # 0
    stream merge object object header cache token  # 1
    socket branch index parser queue window  # 2
    object header merge cache commit tree cache queue delta commit  # 3
    merge header merge buffer queue blob  # 4
    parser buffer object remote token  # 5
    branch handle worker rebase buffer header branch  # 6
    handle window queue queue queue delta  # 7
    index handle stream token index token stream  # 8
    remote cache token index pack rebase handle parser  # 9
    branch remote rebase blob pack  # 10
    blob parser ref ref  # 11
    header merge index object socket  # 12
    tree index tree worker object  # 13
    remote handle blob branch  # 14
    socket ref blob remote blob header buffer pack remote index tree  # 15
    rebase socket tree merge remote remote  # 16
    tree merge commit header  # 17
    tree cache delta ref  # 18
    buffer window header parser buffer parser worker ref rebase remote  # 19
    blob merge parser commit  # 20
    parser pack index handle index parser  # 21
    index blob remote tree cache socket  # 22
    socket object token merge remote parser delta commit queue  # 23
    handle pack queue rebase stream tree header handle blob  # 24
    socket ref blob tree delta pack  # 25
    handle stream ref socket header blob  # 26
    tree buffer header stream commit handle  # 27
    parser socket ref worker buffer  # 28
    token pack stream delta index remote  # 29
    ref worker header merge index index socket commit  # 30
    branch socket blob buffer header socket index worker token queue object  # 31
    pack pack parser ref branch worker  # 32
    buffer rebase cache delta parser buffer commit blob pack tree buffer  # 33
    index queue queue header parser remote index token header  # 34
    buffer commit blob blob commit blob cache branch commit parser tree  # 35
    object blob buffer rebase header  # 36
    header cache buffer cache index cache pack tree tree pack buffer token  # 37
    cache window rebase index index delta delta  # 38
    branch cache ref stream ref buffer commit index delta  # 39
    merge parser rebase parser stream token blob token rebase queue  # 40
    buffer pack branch remote buffer tree commit  # 41
    commit commit worker blob pack  # 42
    cache commit object merge buffer pack  # 43
    tree worker merge object object tree socket cache delta queue object  # 44
    merge pack ref cache buffer token parser window index  # 45
    queue remote buffer pack object merge merge pack  # 46
    queue stream delta merge delta token handle handle pack parser handle  # 47
    index handle pack commit stream cache socket branch queue remote handle  # 48
    index tree pack handle  # 49
    object cache branch handle ref queue socket stream pack  # 50
    ref branch pack stream cache ref queue header token  # 51
    rebase branch buffer worker  # 52
    ref handle token tree window ref merge ref window handle  # 53
    tree pack pack ref  # 54
