    parser token commit index branch  # 0
    tree index rebase pack worker  # 1
    commit stream tree object parser branch  # 2
    ref buffer commit object blob ref token branch object  # 3
    branch delta cache pack object  # 4
    rebase ref queue ref stream  # 5
    blob parser header cache parser  # 6
    queue worker cache ref tree buffer ref queue  # 7
    delta merge index blob queue  # 8
    pack object index merge tree delta blob tree parser worker commit ref  # 9
    worker merge ref token pack  # 10
    pack commit remote branch delta object  # 11
    window handle commit header header  # 12
    parser remote object pack blob parser window  # 13
    cache parser delta queue buffer header token parser ref branch  # 14
    handle branch index object rebase remote index blob stream  # 15
    handle index commit stream remote remote stream  # 16
    branch buffer buffer window blob  # 17
    index rebase commit cache ref tree rebase commit  # 18
    commit stream header delta branch object stream commit  # 19
    stream handle blob socket  # 20
    rebase socket remote stream parser socket commit commit parser cache window remote  # 21
    header handle delta blob  # 22
    parser commit queue merge header tree  # 23
    queue handle index tree object ref cache  # 24
    token object worker worker  # 25
    blob index branch commit merge tree  # 26
    delta header worker blob socket merge  # 27
    ref pack stream index window ref pack  # 28
    merge parser delta socket blob pack index  # 29
    commit token header tree queue buffer delta  # 30
    header index rebase delta window branch worker  # 31
    pack rebase buffer handle token pack  # 32
    index token index branch blob stream remote  # 33
    queue blob merge merge branch merge  # 34
    branch ref handle buffer commit  # 35
    pack parser index header merge cache merge buffer delta  # 36
    stream remote pack index window tree  # 37
    buffer window remote parser stream header worker branch blob  # 38
    window socket object handle  # 39
    parser handle header ref header token commit ref blob rebase  # 40
    blob header rebase rebase tree  # 41
    header remote header worker window index branch remote stream pack delta  # 42
    parser buffer socket branch parser branch header stream window tree  # 43
    merge cache ref rebase index cache parser buffer rebase window parser parser  # 44
    token socket rebase stream header  # 45
    index blob socket pack  # 46
    queue cache handle cache branch token token parser branch  # 47
    queue queue delta object parser merge queue parser branch delta index  # 48
    branch parser worker index index rebase socket  # 49
    merge blob pack worker handle branch stream header stream  # 50
    window index stream pack  # 51
    handle queue commit worker  # 52
    pack handle worker branch remote pack stream header index blob  # 53
    header window rebase stream remote  # 54
    handle commit queue remote delta header  # 55
    blob cache stream socket remote  # 56
    cache cache header delta commit window pack  # 57
    worker merge stream index commit merge rebase rebase  # 58
    window index window stream remote index queue header index commit  # 59
    rebase ref worker header queue pack  # 60
    ref commit ref header tree header  # 61
    token blob token token merge pack  # 62
    remote parser rebase commit branch commit blob parser stream index object window  # 63
    ref rebase object handle socket branch socket cache branch cache header stream  # 0
    ref parser delta commit commit worker worker buffer remote branch parser  # 1
    rebase token window merge  # 2
    delta rebase delta socket handle object blob window header  # 3
    worker queue branch window object window index socket blob parser branch  # 4
    tree buffer object merge commit parser index commit stream buffer blob ref  # 5
    ref commit stream index branch merge index ref object parser socket commit  # 6
    parser stream branch stream  # 7
