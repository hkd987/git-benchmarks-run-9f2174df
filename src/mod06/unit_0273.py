    ref socket object index queue buffer object merge commit delta rebase  # 0
    window stream object buffer rebase delta  # 1
    cache token index rebase rebase stream  # 2
    remote cache queue pack tree header object remote window index branch  # 3
    token socket worker commit worker  # 4
    socket socket cache merge tree buffer  # 5
    parser stream socket token pack blob  # 6
    header cache buffer window merge delta commit branch blob header  # 7
    stream ref ref buffer queue socket rebase  # 8
    commit handle remote handle  # 9
    buffer window header commit cache index tree delta rebase window remote commit  # 10
    pack index stream tree buffer rebase ref rebase header  # 11
    stream cache branch merge  # 12
    worker token tree queue  # 13
    socket remote worker commit handle remote object ref  # 14
    merge blob worker ref remote parser commit object delta  # 15
    blob parser buffer socket merge parser queue queue parser  # 16
    socket socket handle parser token merge pack buffer  # 17
    remote token queue window branch index pack  # 18
    worker tree rebase branch blob worker token blob socket parser branch object  # 19
    handle merge stream object window parser handle cache  # 20
    delta merge branch remote socket token socket window  # 21
    stream delta rebase worker window object window cache blob  # 22
    tree socket ref ref rebase stream token  # 23
    window commit index header header header parser  # 24
    buffer rebase socket index handle handle socket delta  # 25
    blob delta buffer socket worker rebase parser parser pack  # 26
    window queue ref delta buffer parser parser blob commit object rebase  # 27
    pack commit tree rebase stream delta  # 28
    blob buffer tree token handle parser socket buffer parser remote delta merge  # 29
    window buffer tree delta blob handle object merge  # 30
    token rebase ref handle socket handle index delta merge  # 31
    handle stream object queue  # 32
    object commit worker stream index header remote header cache commit  # 33
    stream index merge handle socket parser token delta  # 34
    merge buffer token commit socket socket worker index  # 35
    remote index delta parser delta delta rebase window worker cache buffer  # 36
    token pack socket blob socket  # 37
    ref window commit ref worker  # 38
    remote cache handle token merge blob delta blob window ref  # 39
    header branch merge stream rebase tree window header rebase  # 40
    pack header delta blob handle index worker pack delta token queue branch  # 0
    object rebase index window token merge header tree queue pack  # 1
    stream pack index blob handle index blob commit worker token cache parser  # 2
    header header parser handle socket cache header stream ref branch stream  # 3
    merge header object pack delta pack index merge token cache merge blob  # 4
    index index parser tree object  # 5
    merge tree ref stream ref parser  # 6
    remote token ref blob branch blob branch header pack buffer parser branch  # 7
    remote cache stream commit branch stream index socket header remote blob  # 8
