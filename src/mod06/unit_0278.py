    queue branch index delta commit object token pack pack worker buffer  # 0
    ref commit parser ref branch buffer index cache object queue blob token  # 1
    delta commit rebase ref stream delta cache handle  # 2
    handle ref cache token  # 3
    delta delta header pack queue parser remote ref  # 4
    queue window pack cache header  # 5
    window object parser ref pack  # 6
    pack handle queue index handle rebase handle object tree buffer rebase token  # 7
    ref delta buffer blob rebase cache branch socket window stream  # 8
    handle remote merge commit queue index  # 9
    delta window cache remote  # 10
    queue handle commit branch commit header blob cache header header index remote  # 11
    window blob parser index tree buffer  # 12
    worker header rebase blob blob queue branch object cache  # 13
    pack pack tree queue index commit rebase  # 14
    parser ref handle merge delta tree worker  # 15
    delta worker queue rebase rebase tree delta socket header  # 16
    pack queue worker rebase merge buffer  # 17
    parser header ref tree window cache buffer remote remote handle  # 18
    buffer blob object token  # 19
    stream tree handle commit blob tree remote object cache commit blob  # 20
    socket token tree object stream tree parser  # 21
    ref merge ref parser delta queue window merge window tree remote delta  # 22
    worker branch socket index delta window stream window window ref rebase  # 23
    index commit merge delta remote header ref index commit buffer token index  # 24
    object remote index handle buffer stream header merge blob ref tree  # 25
    object rebase window index stream queue header delta pack queue  # 26
    object parser index header  # 27
    pack header worker rebase handle worker header  # 28
    buffer branch merge stream stream merge token  # 29
    worker rebase index queue delta  # 30
    socket index window remote worker blob  # 31
    header index object object remote buffer stream  # 32
    stream stream token worker queue rebase stream parser index buffer  # 33
    token pack socket commit tree rebase header  # 34
    commit buffer remote pack stream header rebase  # 35
    parser queue header stream header tree header blob window  # 36
    socket merge window ref pack pack parser window  # 37
    object token object pack rebase merge queue buffer index token  # 38
    merge header parser token buffer token tree  # 39
    queue queue buffer parser token  # 40
    window socket token commit index worker commit handle buffer tree ref  # 41
    handle blob token object queue ref header handle cache socket stream blob  # 42
    window buffer delta commit delta  # 43
    socket cache merge window parser parser  # 44
    delta socket cache commit parser tree queue remote rebase header socket index  # 45
    object ref pack handle rebase buffer parser ref pack  # 46
    remote parser remote blob delta handle window socket  # 47
    cache buffer merge object delta handle window  # 48
    commit parser token parser merge  # 49
    delta socket buffer pack parser stream object socket header  # 50
    handle blob rebase index remote  # 51
    queue object object header commit handle buffer  # 52
    cache tree worker stream commit blob queue queue rebase blob branch header  # 53
    tree worker parser window queue  # 54
    blob parser blob buffer rebase branch window pack token ref  # 55
    tree stream header queue queue rebase object tree  # 56
    rebase rebase window delta socket header object delta token stream  # 57
    rebase tree ref window handle object object remote merge object token remote  # 58
    tree queue socket header cache rebase blob socket delta cache cache queue  # 59
    pack worker ref rebase parser rebase socket buffer object delta  # 60
    commit index token handle window queue worker token parser commit index  # 61
    delta object buffer window cache handle header queue delta buffer rebase  # 62
    header rebase header delta  # 63
    ref blob commit index parser buffer ref object pack blob cache tree  # 64
    tree queue delta index commit index delta index delta parser  # 65
    window handle tree branch merge cache remote cache  # 66
    worker queue delta object  # 67
    remote window handle commit branch socket  # 68
    header delta rebase socket remote blob remote buffer  # 69
    header merge delta socket commit cache header branch buffer stream header  # 70
    ref branch window socket window pack tree  # 71
    object branch parser delta remote merge stream index queue handle worker merge  # 72
    index socket remote commit rebase window  # 73
    pack parser worker delta socket handle blob delta rebase header  # 74
    header parser branch branch token parser  # 75
    pack pack parser buffer socket delta buffer  # 76
    header window cache commit pack queue ref commit delta  # 77
    handle handle delta token  # 0
    merge handle handle ref window worker remote branch remote index parser  # 1
    header tree object commit blob  # 2
    handle token rebase tree commit parser tree object tree cache  # 3
    object header stream index  # 4
    blob rebase window tree index window rebase  # 5
    window index commit pack queue parser cache ref header object parser header  # 6
    object socket blob pack header header  # 7
