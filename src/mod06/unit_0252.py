    token tree delta tree rebase  # 0
    token ref object cache parser index token  # 1
    object remote window rebase commit cache  # 2
    object window rebase window handle handle header remote rebase stream branch header  # 3
    socket parser handle socket delta object object  # 4
    socket rebase handle index blob cache merge rebase  # 5
    object delta branch commit window rebase socket ref handle socket commit branch  # 6
    token window rebase commit rebase cache  # 7
    pack commit blob queue rebase pack cache ref  # 8
    window buffer branch queue index token  # 9
    index commit rebase window delta delta handle token cache stream window object  # 10
    window rebase socket parser  # 11
    blob merge blob parser  # 12
    header remote blob ref object pack worker socket blob token buffer  # 13
    remote branch pack object delta socket queue commit  # 14
    rebase remote window branch delta cache parser window  # 15
    ref object window blob index window header queue handle rebase  # 16
    queue rebase buffer branch  # 17
    index window object parser  # 18
    window parser cache stream rebase stream commit worker commit buffer  # 19
    queue socket index cache index handle header worker  # 20
    tree tree cache tree queue  # 21
    parser merge ref parser index  # 22
    handle socket blob branch index delta merge merge object  # 23
    branch branch blob index  # 24
    ref parser socket token branch blob parser  # 25
    delta object rebase parser parser tree buffer branch window blob merge  # 26
    commit token pack worker  # 27
    rebase token parser handle stream blob  # 28
    socket remote object handle parser remote merge ref remote pack object socket  # 29
    commit stream remote parser handle cache parser handle tree stream token  # 30
    stream commit socket worker  # 31
    remote cache delta delta merge ref token header  # 32
    rebase stream object token branch delta socket token merge handle  # 33
    object token blob delta token pack parser stream ref  # 34
    object parser blob index window cache stream branch remote merge pack  # 35
    delta pack header header  # 36
    rebase stream pack delta parser token  # 37
    handle tree stream stream merge worker parser branch header socket  # 38
