    buffer merge pack tree ref socket buffer cache delta tree  # 0
    blob handle remote index socket buffer  # 1
    ref rebase queue object window index stream buffer window cache commit  # 2
    commit socket ref token queue token ref  # 3
    ref tree stream remote tree pack  # 4
    worker remote parser parser blob pack object remote  # 5
    window tree rebase remote  # 6
    token queue blob delta socket socket ref rebase  # 7
    handle merge worker commit remote header commit  # 8
    remote buffer token tree cache queue stream parser worker  # 9
    parser pack stream ref commit object branch object  # 10
    parser index socket cache branch branch delta pack rebase tree ref merge  # 11
    merge handle commit rebase object index delta  # 12
    commit commit stream ref header cache  # 13
    socket token token window handle token worker socket branch blob  # 14
    delta buffer socket ref window queue queue tree header token rebase  # 15
    blob commit object rebase  # 16
    commit window cache rebase commit branch ref blob window parser socket  # 17
    delta cache handle pack blob  # 18
    socket tree blob remote header commit header commit pack socket stream  # 19
    tree delta stream object  # 20
    index tree buffer handle object tree object socket remote  # 21
    header delta merge branch index  # 22
    index worker stream delta rebase token queue  # 23
    ref remote ref header branch delta  # 24
    object buffer delta merge stream buffer buffer cache index object  # 25
    header queue tree handle socket blob handle stream object  # 26
    parser token parser socket buffer stream queue buffer delta buffer  # 27
    branch tree token blob handle  # 28
    branch delta queue handle buffer handle  # 29
    tree pack blob index rebase parser socket tree  # 30
    parser header handle blob buffer worker cache rebase remote remote  # 31
    buffer buffer parser commit  # 32
    queue header pack parser buffer window buffer remote  # 33
    delta index rebase cache  # 34
    queue pack window pack queue socket cache  # 35
    merge cache cache stream parser token blob queue handle  # 0
    delta branch object parser buffer  # 1
    window stream worker delta blob index token merge merge handle merge  # 2
    branch header window rebase tree blob socket pack worker  # 3
    window delta branch handle  # 4
