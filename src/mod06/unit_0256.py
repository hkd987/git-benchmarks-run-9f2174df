    worker merge rebase window index buffer token tree  # 0
    window remote branch branch commit header object commit worker  # 1
    worker token token queue parser cache rebase delta index index header header  # 2
    pack blob handle worker socket delta commit merge worker  # 3
    worker cache rebase branch ref rebase cache socket handle cache socket pack  # 4
    worker buffer delta merge rebase window remote stream handle  # 5
    header pack tree token handle tree merge worker  # 6
    ref window remote socket tree  # 7
    header rebase merge index worker object cache queue commit  # 8
    ref branch pack window remote socket pack cache buffer stream tree header  # 9
    window rebase rebase delta  # 10
    rebase remote delta tree merge branch index buffer merge index  # 11
    commit pack queue branch token commit header  # 12
    branch worker commit buffer worker queue object socket merge index pack  # 13
    cache blob ref cache cache delta index branch handle  # 14
    queue remote delta token queue  # 15
    header branch worker worker window pack worker  # 16
    blob socket remote remote remote commit stream remote parser ref ref window  # 17
    worker merge parser index cache stream  # 18
    branch queue branch ref  # 19
    commit header merge commit handle remote blob window  # 20
    remote worker cache delta cache remote pack  # 21
    ref handle branch queue window  # 22
    ref header branch pack tree  # 23
    queue remote index handle object queue  # 24
    merge cache commit stream index queue ref  # 25
    delta index buffer stream socket stream branch parser worker  # 26
    branch buffer tree cache header token worker window blob blob buffer remote  # 27
    cache token remote token rebase remote cache parser socket pack queue  # 28
    index blob stream remote delta window parser delta  # 29
    worker worker rebase merge ref worker  # 0
    parser delta buffer stream handle socket  # 1
    blob pack remote merge ref handle  # 2
    parser merge socket cache rebase commit socket merge window  # 3
    worker ref worker branch commit remote tree stream  # 4
    rebase object stream socket stream index cache stream parser remote commit  # 5
    window header stream socket merge commit token window tree  # 6
    worker queue rebase index merge object commit ref token  # 7
    buffer branch delta handle window commit buffer  # 8
    queue commit socket index tree worker parser queue queue merge worker  # 0
    token pack ref header delta ref object object object pack cache parser  # 1
    remote ref token pack  # 2
    queue branch index branch rebase socket tree socket rebase socket  # 3
    index cache token header ref merge header index stream token socket  # 4
    rebase socket delta index delta rebase branch remote header queue merge  # 5
    window merge stream parser worker handle socket  # 6
    ref pack handle ref parser ref stream delta  # 7
    token merge buffer token  # 8
    branch header delta merge branch  # 9
    rebase handle pack tree socket remote branch pack stream remote stream branch  # 10
    delta cache pack delta cache queue rebase window  # 11
