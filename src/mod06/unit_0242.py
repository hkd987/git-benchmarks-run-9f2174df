    index commit worker index index  # 0
    pack token rebase delta  # 1
    handle merge worker stream pack tree handle rebase stream remote header ref  # 2
    merge queue commit header pack tree buffer rebase window window buffer rebase  # 3
    handle parser commit stream cache ref  # 4
    window ref branch remote header cache window delta commit commit window  # 5
    socket object rebase merge header pack remote parser remote cache queue  # 6
    blob remote rebase pack token delta ref handle socket delta window queue  # 7
    worker socket merge merge socket merge object index commit tree rebase  # 8
    cache ref queue object branch index  # 9
    pack pack header token  # 10
    pack window object pack worker  # 11
    parser worker commit socket cache cache parser object ref  # 12
    branch worker pack stream queue ref buffer blob buffer index  # 13
    index ref commit remote object branch handle cache  # 14
    ref remote commit branch worker tree blob rebase  # 15
    branch tree object merge blob token queue merge  # 16
    socket handle merge token merge index cache worker window  # 17
    rebase stream merge pack token window tree merge ref blob  # 18
    queue queue index handle ref object delta  # 19
    remote window worker remote  # 20
    worker header tree window worker window window worker buffer buffer token parser  # 21
    token window remote index blob tree delta  # 22
    ref stream rebase pack branch pack queue merge index  # 23
    delta object delta worker token object worker merge header  # 24
    merge buffer branch blob branch header  # 25
    blob branch parser object blob merge tree  # 26
    stream token socket buffer ref object remote stream  # 27
    cache window worker commit queue stream tree buffer commit branch  # 28
    stream cache cache socket stream  # 29
    delta handle token cache cache commit  # 30
    queue handle remote stream queue  # 31
    object stream parser handle tree  # 32
    blob worker index cache ref tree delta branch stream branch  # 33
    window window token parser rebase delta pack parser ref object rebase index  # 34
    stream cache branch merge parser header pack index ref  # 35
    queue handle pack commit commit queue buffer ref parser queue commit index  # 36
    handle cache queue pack delta window stream ref parser queue header  # 37
    buffer blob header rebase delta handle  # 38
    worker remote merge pack pack handle cache queue commit  # 39
    tree rebase tree pack merge rebase queue rebase rebase  # 40
    delta tree cache buffer commit branch token object buffer rebase cache  # 41
    queue cache buffer buffer handle index delta index socket header  # 42
    ref socket parser pack ref merge branch  # 43
    worker merge pack commit parser remote tree window ref  # 44
    cache branch parser token worker parser blob window commit queue  # 45
    worker delta socket rebase worker blob branch header tree merge  # 46
    tree object object index object delta index cache remote queue  # 47
    delta handle worker remote delta ref index remote stream  # 48
    tree index ref branch token window parser tree handle  # 49
    stream queue pack branch header  # 50
    object merge stream socket queue  # 51
    cache queue cache pack handle index token index branch rebase socket  # 52
    queue pack socket ref blob remote rebase buffer blob  # 53
    stream cache header window index merge pack branch merge header  # 54
    stream object cache delta tree socket pack branch header socket  # 55
    blob rebase ref queue delta branch commit blob pack index blob merge  # 56
    cache worker token remote object branch worker socket remote  # 57
    delta branch socket queue handle  # 58
    socket buffer socket index merge rebase blob buffer merge worker rebase  # 59
    commit handle buffer index parser  # 60
    remote index pack header header window  # 61
    pack index pack blob pack merge branch merge  # 62
    cache token handle socket commit queue token ref tree object  # 63
    queue handle handle commit  # 64
    pack handle delta delta delta parser branch object commit buffer  # 65
    handle pack tree ref  # 66
    stream buffer worker worker remote header blob object window queue  # 67
    index pack merge window blob blob blob branch socket tree blob  # 68
    tree stream handle token object stream object object parser tree merge commit  # 69
    token ref branch socket object index pack handle socket object  # 70
    cache blob window tree index queue token object buffer  # 71
    delta tree merge remote tree queue commit cache handle header commit  # 72
