    index object stream branch branch parser blob window remote  # 0
    merge object ref blob  # 1
    index tree buffer tree stream object stream socket delta  # 2
    socket parser object worker handle  # 3
    index socket pack commit socket token window pack stream branch ref  # 4
    window object tree delta merge  # 5
    socket cache index tree queue tree socket pack token socket  # 6
    rebase commit cache merge branch token branch worker handle  # 7
    branch stream queue rebase handle queue handle tree blob branch stream stream  # 8
    index merge socket index window queue queue window tree worker stream blob  # 9
    blob remote commit ref pack commit object window tree pack merge  # 10
    window branch merge queue branch window buffer blob pack  # 11
    queue queue socket remote token parser commit object merge queue window index  # 12
    header blob rebase merge remote socket tree branch token  # 13
    delta delta header merge index header handle remote parser parser object  # 14
    merge branch header commit handle window buffer token queue object  # 15
    pack ref branch parser  # 16
    cache token header stream rebase queue cache delta  # 17
    queue window rebase rebase branch worker commit header token ref delta  # 18
    object blob queue delta commit buffer ref  # 19
    queue socket token queue index cache commit window  # 20
    object branch tree delta delta cache stream socket tree buffer token index  # 21
    queue blob window window pack socket remote stream  # 22
    queue delta tree buffer rebase rebase  # 23
    ref delta stream object rebase merge tree  # 24
    token commit stream remote queue object remote index  # 25
    header buffer blob parser window token  # 26
    socket socket stream handle blob object queue remote index commit  # 27
    rebase socket delta rebase parser ref  # 28
    branch handle ref buffer queue handle remote pack index cache tree  # 29
    pack header parser delta pack token cache  # 30
    commit header stream index pack socket delta socket object token cache  # 31
    remote delta buffer token  # 32
    buffer worker remote tree header commit object blob buffer tree commit  # 33
    pack handle socket branch header index token remote queue token  # 34
    index stream merge token ref blob worker  # 35
    remote ref handle merge tree worker delta pack merge object  # 36
    commit object tree remote merge index buffer  # 37
    branch remote worker queue token buffer header rebase queue tree  # 38
    handle tree index queue window handle token ref handle  # 39
    cache buffer merge tree  # 40
    queue buffer commit blob queue tree rebase cache parser handle handle merge  # 41
    queue pack parser window tree tree delta cache commit token  # 42
    stream blob merge index branch  # 43
    handle parser token object  # 44
    pack index ref handle header handle token  # 45
    remote ref pack tree handle socket socket stream parser handle  # 46
    parser ref buffer worker object token parser remote commit rebase pack  # 47
    branch pack ref object branch ref socket merge header  # 48
    parser token delta index blob handle remote tree blob parser  # 49
    worker branch parser handle header object handle ref socket  # 50
    commit worker branch tree commit stream rebase branch token worker  # 51
    parser token object ref parser tree blob  # 0
    queue pack commit index object index handle pack  # 1
    rebase object index blob rebase rebase header object branch handle rebase window  # 2
    token handle queue queue  # 3
    commit window socket header socket stream remote cache tree object index  # 4
    cache branch ref window branch rebase commit index merge queue socket buffer  # 5
