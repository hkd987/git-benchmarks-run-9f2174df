    window rebase merge ref ref  # 0
    cache branch queue commit branch commit parser  # 1
    socket buffer cache socket stream header pack remote handle commit rebase  # 2
    object socket handle commit blob remote commit commit buffer tree commit  # 3
    index pack tree parser worker tree  # 4
    header queue merge blob token tree merge delta remote branch index  # 5
    token buffer pack tree object worker merge rebase commit stream  # 6
    queue branch delta cache  # 7
    object header stream rebase stream  # 8
    buffer header token cache ref pack window ref ref pack blob queue  # 9
    handle commit rebase branch blob commit delta tree  # 10
    parser cache header socket buffer  # 11
    stream commit window header worker tree buffer ref tree stream queue commit  # 12
    handle remote object token worker cache commit commit window parser header  # 13
    socket merge object rebase buffer worker stream commit  # 14
    object socket window buffer merge commit object  # 15
    ref delta socket queue socket worker handle delta cache  # 16
    merge header blob branch delta window cache branch  # 17
    worker socket stream cache window pack rebase queue merge socket token pack  # 18
    stream header queue delta queue  # 19
    parser handle parser pack ref handle  # 20
    branch socket commit commit object  # 21
    ref handle merge branch parser tree remote remote  # 22
    rebase handle queue window window index merge header  # 23
    stream worker handle header buffer stream header window index merge merge pack  # 24
    stream header cache token queue  # 25
    token index rebase parser header branch worker handle remote  # 26
    object window blob header branch socket buffer cache object merge  # 27
    blob branch tree index header ref token rebase branch ref buffer ref  # 28
    socket socket handle worker token token buffer branch  # 29
    commit socket blob blob handle ref stream remote parser handle  # 30
    token cache stream cache commit remote token handle blob  # 31
    stream token rebase rebase handle worker token tree object blob  # 32
    stream parser delta index delta  # 33
    window merge commit object ref socket queue socket  # 34
    commit index blob commit socket ref buffer object  # 35
    tree window rebase index branch remote socket handle window remote pack merge  # 36
    rebase blob index commit worker parser socket header delta delta  # 37
    object merge index queue delta object blob token  # 38
    blob pack ref merge cache worker commit commit rebase handle parser  # 39
    cache index rebase handle  # 40
    worker window stream object tree branch parser queue window cache parser  # 41
    rebase merge branch window delta commit queue commit remote commit  # 42
    index index window merge merge rebase handle window cache  # 43
    queue stream queue remote pack delta delta  # 44
    pack merge delta token  # 45
    object merge remote header window blob remote pack stream  # 46
    token pack ref cache socket buffer window delta  # 47
    handle socket branch merge  # 48
    socket branch blob stream worker pack  # 49
    stream delta ref pack rebase delta header window parser  # 50
    rebase tree cache window buffer handle token header pack ref delta buffer  # 51
    worker pack object ref handle  # 52
    stream branch delta handle  # 53
    blob rebase parser remote  # 54
    worker ref cache rebase token index buffer branch commit cache  # 55
    token header branch queue merge buffer branch  # 56
    remote stream index index handle queue merge ref parser  # 57
    object commit token object commit  # 58
    token pack buffer merge branch cache stream rebase  # 59
    index stream socket index remote handle token remote remote  # 60
    ref object pack commit buffer commit  # 61
    window queue blob rebase buffer worker commit index pack commit  # 62
    queue merge delta object index header queue queue handle remote merge rebase  # 63
    worker branch cache object parser tree  # 64
    parser branch remote stream  # 65
    socket worker worker remote index window pack cache  # 66
    remote index buffer remote remote worker tree pack delta  # 67
    branch rebase remote commit remote worker worker  # 68
    worker worker pack object token pack cache token cache header  # 69
    rebase header remote cache handle ref  # 70
    index ref merge ref ref branch worker  # 71
    buffer merge rebase header  # 72
    pack cache cache rebase object window  # 73
    commit stream merge queue pack commit commit index branch blob remote ref  # 74
    commit socket parser pack tree queue pack remote window rebase rebase  # 75
    token index commit handle rebase socket handle commit socket  # 76
    cache cache delta merge  # 77
    object object tree worker merge delta delta  # 0
    tree pack merge merge pack  # 1
    commit object index ref  # 2
    object branch buffer buffer worker delta parser  # 3
    parser socket branch buffer merge buffer socket buffer merge commit  # 4
    remote buffer queue commit queue buffer window  # 5
    tree tree rebase header index worker object socket  # 6
    worker queue stream handle token blob parser  # 7
    rebase pack handle queue parser ref object commit commit worker queue merge  # 0
    tree window commit remote  # 1
    branch rebase pack pack object cache index blob stream index token  # 2
    index cache delta merge rebase branch stream  # 3
    cache remote pack commit branch header header handle  # 4
    pack worker cache pack branch blob merge  # 5
    socket socket parser object index ref ref  # 6
    commit branch commit merge stream  # 7
    rebase ref rebase pack merge delta merge pack tree tree index blob  # 8
    object pack socket window  # 9
    parser remote worker socket merge cache index pack index object tree  # 10
    tree delta cache worker pack pack token object  # 11
