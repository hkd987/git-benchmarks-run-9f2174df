    queue handle socket merge cache ref socket index token branch buffer remote  # 0
    queue queue socket token header branch header ref commit window  # 1
    stream object ref index tree remote  # 2
    branch delta cache token rebase handle tree blob buffer pack token rebase  # 3
    commit delta index merge queue index stream handle remote remote commit index  # 4
    ref branch handle delta parser cache  # 5
    merge socket handle index socket header queue header object cache  # 6
    remote delta branch remote token rebase branch header object window  # 7
    object parser handle pack tree window index delta index token rebase cache  # 8
    tree object commit delta delta  # 9
    index buffer index merge tree tree token tree delta  # 10
    commit window queue pack cache ref object object window token  # 11
    pack delta queue pack rebase ref  # 12
    handle merge blob stream window delta merge object buffer delta  # 13
    buffer commit token remote buffer delta socket buffer delta  # 14
    branch rebase delta commit remote worker ref pack  # 15
    socket token object queue worker pack blob blob worker object  # 16
    tree cache merge stream index  # 17
    pack queue header handle  # 18
    header stream object header index blob index buffer  # 19
    merge socket branch queue object tree blob blob ref worker  # 20
    ref ref blob delta branch merge cache rebase  # 21
    token branch blob ref pack  # 22
    pack pack parser worker  # 23
    branch parser buffer header  # 24
    delta socket queue commit index header handle commit stream index blob handle  # 25
    commit handle token index worker rebase index header parser  # 26
    socket queue buffer socket commit queue window cache header window delta  # 27
    handle header blob blob stream socket stream  # 28
    ref tree blob pack commit  # 29
    pack remote rebase rebase buffer  # 30
    token queue cache socket ref object delta pack header  # 31
    commit buffer queue commit merge ref window remote header queue branch  # 32
    blob remote merge index socket  # 33
    window pack branch worker stream rebase ref ref window  # 34
    object merge branch buffer blob remote cache token blob  # 35
    tree buffer merge branch stream  # 36
    delta token socket branch rebase blob commit tree commit parser ref pack  # 37
    buffer window token rebase pack object merge  # 38
    cache blob tree token socket  # 39
    token header window window delta stream buffer token merge object  # 40
    window tree delta delta  # 41
    parser token cache ref delta delta ref buffer remote stream rebase rebase  # 42
    handle object blob window ref worker ref delta token stream buffer queue  # 43
    rebase worker ref handle cache  # 44
    object handle blob buffer worker  # 45
    merge merge token worker pack commit delta blob pack  # 46
    object object branch delta object worker token remote header cache tree  # 47
    parser commit commit delta  # 48
    window queue tree commit remote branch pack  # 49
    tree ref merge queue socket cache socket remote queue object ref blob  # 50
    rebase buffer object stream  # 51
    stream delta stream rebase remote window branch tree  # 52
    stream worker commit merge buffer  # 53
    ref object queue ref buffer index window commit ref worker object parser  # 54
    stream worker socket pack blob stream buffer handle socket rebase delta cache  # 55
    handle socket header window merge tree queue token pack rebase parser blob  # 56
    ref handle queue index  # 57
    blob merge window rebase blob commit tree blob pack  # 58
    worker token object window header cache ref header cache cache queue  # 59
    queue remote buffer cache ref handle blob tree parser commit  # 60
    header merge parser branch index header stream rebase commit queue token object  # 61
    buffer branch stream socket token window ref queue pack delta header  # 0
    token header blob remote delta commit ref rebase  # 1
    handle parser blob handle header merge merge  # 2
    remote header parser header index queue delta token delta queue object delta  # 3
    commit buffer blob remote worker  # 4
    delta worker queue cache branch branch  # 5
    stream blob header buffer blob remote ref pack window parser socket rebase  # 6
    queue handle object buffer  # 7
