    commit object token ref handle queue token token buffer delta merge  # 0
    branch index token tree commit token commit parser socket tree rebase  # 1
    blob cache index header delta object parser merge handle buffer rebase blob  # 2
    branch tree worker branch  # 3
    worker header pack tree delta remote handle  # 4
    object branch remote header socket branch blob ref stream  # 5
    branch stream ref handle index socket queue token branch window ref pack  # 6
    ref merge delta blob parser queue parser rebase delta rebase blob worker  # 7
    stream tree ref handle handle  # 8
    object handle index ref stream buffer  # 9
    parser delta parser branch handle blob delta cache queue buffer buffer  # 10
    object window stream parser worker  # 11
    merge worker commit rebase token object  # 12
    commit branch parser header branch tree ref merge delta window  # 13
    buffer cache blob window  # 14
    rebase remote window rebase socket stream commit delta cache handle delta  # 15
    merge tree socket handle worker  # 16
    window token ref delta parser token delta parser window  # 17
    socket stream token delta delta index rebase parser  # 18
    index merge index rebase buffer ref blob blob token parser window  # 19
    window window object cache pack token index index worker pack queue  # 20
    index buffer window socket branch merge stream buffer socket blob queue window  # 21
    delta remote branch merge delta worker commit parser blob cache cache  # 22
    worker queue commit buffer tree stream queue cache socket header commit  # 23
    object ref commit merge  # 24
    branch remote rebase delta queue commit queue object window  # 25
    remote window commit branch delta tree token window  # 26
    tree ref parser queue handle socket cache blob worker merge  # 27
    queue merge buffer buffer  # 28
    handle queue cache blob header tree parser index object tree index queue  # 29
    rebase branch header branch index delta tree window  # 30
    token ref queue cache stream queue commit commit header parser parser tree  # 31
    queue cache window header ref header cache cache merge cache  # 32
    delta rebase blob pack pack header queue stream token header  # 33
    window buffer merge queue tree cache delta  # 34
    commit pack object index blob socket header  # 35
    merge worker buffer header stream delta parser  # 36
    worker blob token index tree pack handle token token merge merge  # 37
    parser branch branch socket ref ref tree header header handle index branch  # 38
    branch handle handle worker commit queue parser  # 39
    remote rebase socket cache handle token parser header  # 40
    handle object merge remote commit rebase  # 41
    index stream stream index  # 42
    pack worker header token branch parser handle merge blob token  # 43
    branch parser merge stream socket  # 44
    worker ref window header  # 45
    ref worker window delta  # 46
    parser ref pack token buffer cache  # 47
    worker ref commit ref token  # 48
    cache worker object window  # 49
