    token queue cache pack index header  # 0
    object tree merge rebase commit branch blob  # 1
    stream parser pack remote cache stream window queue header handle  # 2
    buffer remote delta socket index  # 3
    handle ref remote parser worker token remote object queue stream queue  # 4
    branch rebase pack cache pack  # 5
    commit token delta tree socket pack buffer window stream merge buffer queue  # 6
    ref rebase worker pack blob blob pack rebase buffer  # 7
    ref worker socket index pack branch  # 8
    queue commit remote token queue blob  # 9
    blob delta socket token cache remote worker blob  # 10
    token index merge buffer stream  # 11
    rebase header remote worker queue remote worker remote socket cache  # 12
    handle blob blob token ref  # 13
    socket index merge stream remote delta commit remote ref ref parser  # 14
    merge buffer index handle pack pack object blob rebase header  # 15
    blob token socket object  # 16
    parser remote delta handle  # 17
    object header merge pack token token token token worker merge cache queue  # 18
    handle commit index blob  # 19
    parser remote ref blob rebase  # 20
    pack buffer token buffer  # 21
    handle worker blob buffer stream rebase stream commit object object  # 22
    window worker delta index index  # 23
    commit object token cache  # 24
    queue branch rebase index window queue pack blob cache  # 25
    merge header socket queue pack queue merge  # 26
    header object header worker  # 27
    buffer socket socket rebase stream remote window cache  # 28
    remote commit header worker index  # 29
    ref ref parser handle  # 30
    index parser pack token socket stream window index worker parser  # 31
    token tree buffer ref window index  # 32
    rebase stream worker parser branch delta window branch ref object  # 33
    delta header tree socket branch remote  # 34
    index tree handle tree delta  # 35
    object merge delta stream remote object branch header rebase token  # 36
    commit buffer cache merge blob blob  # 37
    handle parser rebase delta cache token commit window ref commit  # 38
    parser index pack blob remote worker cache cache  # 39
    cache parser socket worker worker stream window object remote merge tree buffer  # 40
    header object branch buffer  # 41
    worker delta header merge header pack  # 42
    tree tree socket cache object commit window  # 43
    handle tree ref delta buffer tree window  # 44
    queue stream cache object merge socket rebase rebase cache socket tree header  # 45
    stream commit cache token  # 46
    index blob worker worker  # 47
    header socket branch blob ref socket parser worker  # 48
    index pack tree parser worker  # 49
    object socket buffer tree rebase window buffer  # 50
    window rebase commit socket delta tree header merge object  # 51
    branch worker object delta handle index handle  # 52
    index queue token buffer delta index cache window cache handle commit  # 53
    buffer handle remote ref header branch queue blob stream  # 54
    object worker rebase tree object rebase object  # 55
    remote socket remote socket queue merge commit  # 56
    header stream tree object delta buffer queue  # 57
    ref buffer ref cache  # 58
    socket tree index delta token rebase delta remote  # 59
    pack merge branch commit branch object token  # 60
    worker object window blob remote socket pack branch merge header cache  # 61
    header stream remote delta cache  # 62
    tree stream delta delta worker parser header window ref header cache index  # 63
    header stream delta index stream index header tree object worker index pack  # 64
    handle worker stream worker socket merge commit  # 65
    window header buffer object  # 66
    tree tree branch queue  # 67
    merge buffer object window parser commit  # 68
    merge handle parser worker  # 69
    cache branch object object pack handle blob index merge pack header  # 70
    window object rebase handle  # 71
    delta stream stream header index buffer  # 72
    cache handle tree cache rebase buffer object delta cache token parser  # 73
    index header header ref token remote token  # 74
    delta buffer header buffer parser tree merge handle  # 75
