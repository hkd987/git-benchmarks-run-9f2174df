    buffer pack merge object worker parser socket index token worker  # 0
    cache object socket object object object branch cache parser index branch header  # 1
    index token remote stream buffer handle remote commit object rebase ref worker  # 2
    pack parser stream window merge rebase tree object worker worker delta object  # 3
    merge merge parser worker token remote cache  # 4
    handle stream socket queue buffer delta window socket  # 5
    header parser queue handle socket ref branch branch stream queue handle ref  # 6
    merge handle index index  # 7
    ref index parser socket pack  # 8
    queue commit ref blob index delta window blob ref remote rebase header  # 9
    branch parser delta remote rebase stream stream index  # 10
    header rebase handle branch rebase remote tree cache  # 11
    queue cache blob cache rebase handle worker delta  # 12
    token remote merge index remote remote branch commit remote  # 13
    remote remote handle delta delta commit socket  # 14
    pack window window blob object queue worker stream delta blob  # 15
    ref queue buffer rebase object queue ref handle cache worker  # 16
    object tree commit cache queue socket token queue  # 17
    branch delta stream window token buffer  # 18
    blob socket worker index tree merge ref token blob  # 19
    buffer merge delta cache remote  # 20
    pack header worker rebase header tree  # 21
    cache object window header remote cache buffer stream commit token ref parser  # 22
    window queue object blob blob cache object parser branch  # 23
    merge header window remote ref remote delta tree delta  # 24
    tree handle header blob branch header object pack index ref  # 25
    header merge tree token worker pack branch buffer tree header stream  # 26
    cache ref worker parser object header pack stream socket queue  # 27
    index buffer token commit buffer remote blob buffer index token  # 28
    ref buffer index cache token branch window  # 29
    handle branch handle pack object branch  # 30
    blob cache tree object parser header ref cache tree  # 31
    header socket index handle merge  # 32
    object header stream rebase  # 33
    worker remote object tree merge commit rebase parser remote token tree parser  # 34
    pack object header branch worker buffer stream cache buffer  # 35
    blob blob merge parser rebase delta  # 36
    merge socket pack stream handle worker cache remote commit merge parser  # 37
    worker handle parser buffer tree token pack tree merge  # 38
    stream remote cache handle delta merge parser  # 39
    buffer handle object queue index delta  # 40
    socket merge worker handle merge  # 41
    socket queue parser remote merge  # 42
    header ref commit merge object branch pack object index worker  # 43
    object window remote commit tree merge merge object cache blob commit header  # 44
    worker commit handle remote ref object worker parser merge tree socket worker  # 45
    object blob rebase worker cache stream index header delta  # 46
    branch parser rebase buffer pack socket buffer branch parser  # 47
    worker window merge buffer token blob branch cache index  # 48
    stream socket stream socket remote commit blob branch buffer  # 49
    blob window token commit tree stream header ref tree merge socket  # 50
    pack tree socket delta object blob tree window commit tree object remote  # 51
    tree window object tree index commit  # 52
    branch remote index queue ref tree queue handle  # 53
    header token pack token handle merge  # 54
    object commit tree rebase rebase  # 55
    handle worker remote cache cache rebase token pack token cache merge  # 56
    index merge tree stream object ref cache  # 57
    ref parser stream worker commit ref pack ref buffer rebase  # 58
    parser tree merge socket merge worker branch parser  # 59
    commit handle socket branch parser socket cache buffer  # 60
    index stream handle remote socket  # 61
    object stream token pack  # 62
    ref object tree commit stream  # 63
    token worker rebase worker tree worker commit worker index pack index window  # 64
    window rebase delta branch socket cache object blob stream commit index  # 65
    rebase blob object blob parser rebase cache pack stream merge tree  # 66
    tree parser parser object window ref remote queue cache worker  # 67
    ref handle index token queue parser commit tree tree index  # 68
    window rebase index header index blob handle token ref merge socket  # 69
    remote index worker rebase window cache parser commit commit tree  # 70
    stream queue rebase tree ref token  # 71
    cache parser branch ref ref  # 72
    rebase rebase blob remote branch socket delta  # 73
    delta commit merge stream object blob buffer object buffer handle  # 74
    header commit window rebase rebase rebase parser  # 75
    rebase index parser stream object  # 76
    cache commit handle window parser blob remote cache  # 77
    remote parser parser blob header tree window cache  # 78
    cache commit worker ref token  # 79
    token token blob socket worker cache ref window  # 80
    worker cache merge pack buffer ref worker branch parser buffer handle window  # 81
    blob ref remote socket commit handle header window  # 0
    object parser remote socket ref remote handle cache object buffer  # 1
    object rebase header cache index parser commit branch blob delta window cache  # 2
    buffer commit branch queue blob handle delta  # 3
    delta buffer branch parser  # 4
    rebase queue index ref  # 5
    worker rebase commit blob ref stream window buffer pack  # 6
