    remote stream index merge buffer socket  # 0
    worker worker delta queue commit parser remote commit rebase ref stream  # 1
    tree merge branch cache window socket object handle  # 2
    index handle queue rebase parser ref  # 3
    branch rebase delta handle tree blob tree  # 4
    tree token delta blob window window buffer ref blob cache  # 5
    pack queue rebase rebase stream remote pack handle ref cache socket  # 6
    index pack token stream cache  # 7
    parser header ref cache worker header window remote  # 8
    merge queue tree object handle object window buffer merge  # 9
    queue blob delta window branch blob merge merge  # 10
    pack pack stream socket rebase socket rebase cache index queue  # 11
    queue pack header cache commit handle worker object index buffer tree  # 12
    cache commit cache ref merge cache  # 13
    merge cache tree merge pack cache delta object delta  # 14
    cache pack parser remote buffer delta handle index token merge  # 15
    ref cache buffer queue blob  # 16
    commit pack window delta header index index delta  # 17
    handle parser handle tree worker commit ref  # 18
    cache window index remote pack stream tree blob delta object socket  # 19
    worker blob blob handle token ref  # 20
    branch parser remote branch socket ref tree commit token  # 21
    cache handle buffer index token index handle merge worker  # 22
    object handle window worker tree tree cache socket branch  # 23
    socket branch ref commit parser object rebase tree queue buffer remote parser  # 24
    worker remote merge token  # 25
    cache handle rebase worker cache index tree  # 26
    merge blob remote queue ref parser index branch socket  # 27
    socket parser index token queue window blob object header  # 28
    index parser token remote branch cache rebase remote socket window socket  # 29
    token handle blob blob worker pack delta  # 30
    branch header blob header ref branch buffer delta  # 31
    blob parser parser delta  # 32
    cache blob worker object  # 33
    merge object parser merge remote window ref  # 34
    merge tree stream queue commit merge remote commit delta socket buffer branch  # 0
    worker merge worker index socket tree  # 1
    tree queue window commit rebase branch ref stream queue  # 2
    index index window ref stream  # 3
    buffer remote ref ref  # 4
    object queue cache header window stream stream stream  # 5
