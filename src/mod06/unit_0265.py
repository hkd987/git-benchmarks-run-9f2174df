    token branch parser header object cache socket cache branch stream  # 0
    ref window branch stream tree tree parser object window object  # 1
    window stream rebase window commit handle window handle  # 2
    window token merge buffer  # 3
    delta object delta stream commit merge branch cache object index  # 4
    tree remote queue object merge buffer merge remote worker  # 5
    merge rebase worker cache worker blob  # 6
    rebase merge stream token queue object socket  # 7
    cache remote object handle remote socket queue  # 8
    cache index branch parser object object  # 9
    ref parser merge commit handle object delta header stream window  # 10
    pack parser ref header branch branch pack commit rebase header  # 11
    queue parser cache commit index buffer delta blob rebase rebase  # 12
    pack header rebase socket merge ref delta tree parser merge  # 13
    cache pack header handle header token  # 14
    socket queue rebase token tree queue blob header remote worker  # 15
    queue blob merge header token  # 16
    stream window tree commit remote header worker window delta index ref object  # 17
    merge window worker handle object tree index  # 18
    queue queue window token window tree commit socket cache queue socket  # 19
    handle cache parser index parser stream object  # 20
    buffer buffer worker worker merge cache cache object remote token commit  # 21
    commit index rebase object blob stream pack cache cache commit  # 22
    buffer delta token tree merge handle header merge commit  # 23
    blob handle queue socket stream ref  # 24
    commit buffer cache stream pack ref cache tree pack  # 25
    window pack window merge ref pack merge tree stream branch parser  # 26
    parser tree blob merge blob cache merge ref  # 27
    header socket stream index object header delta object object token branch commit  # 28
    header ref handle object cache ref ref parser worker token ref  # 29
    queue worker merge cache ref header handle handle queue branch  # 30
    stream pack cache queue worker handle merge stream  # 31
    index ref pack remote branch socket  # 32
    object worker branch blob delta buffer branch ref buffer blob buffer branch  # 33
    ref cache header object cache  # 34
    blob buffer delta rebase blob  # 35
    remote pack worker object handle buffer  # 36
    handle tree delta commit handle queue delta  # 37
    commit handle queue branch socket  # 38
    merge branch blob token merge socket token blob buffer buffer  # 39
    worker delta handle window worker merge window rebase remote  # 40
    handle stream delta parser  # 41
    object worker rebase blob stream buffer branch header  # 42
    object object index worker parser cache  # 43
    ref stream stream parser  # 44
    header cache stream cache commit branch pack remote  # 45
    blob ref parser socket branch window handle cache branch queue token  # 46
    parser index ref worker object  # 47
    blob index parser merge window branch  # 48
    tree rebase blob stream merge pack socket  # 49
    remote blob index pack object  # 50
    index queue parser handle stream tree  # 51
    blob index queue branch rebase object index header stream  # 0
    blob branch handle remote merge object  # 1
    tree tree window tree window pack merge branch  # 2
    handle rebase token pack branch remote commit header buffer buffer stream  # 3
    worker cache tree tree tree stream cache header socket queue index blob  # 4
    parser merge parser handle queue  # 5
    object buffer buffer window parser remote object cache  # 6
    stream ref index merge  # 7
    remote buffer handle parser blob merge stream  # 8
    object cache parser tree branch index socket buffer  # 0
    buffer window blob branch header commit rebase blob parser  # 1
    window branch window stream blob parser rebase  # 2
    remote merge ref header token stream parser window window  # 3
    merge socket cache tree stream queue handle delta handle  # 4
