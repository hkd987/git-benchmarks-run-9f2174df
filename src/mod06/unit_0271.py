    token queue rebase pack buffer merge  # 0
    header branch rebase rebase socket window delta queue remote ref header  # 1
    branch branch index token worker pack merge  # 2
    buffer header token object worker buffer index parser socket cache commit rebase  # 3
    window merge parser buffer remote commit handle worker remote  # 4
    commit buffer pack rebase  # 5
    buffer rebase tree queue delta buffer worker parser  # 6
    token stream socket blob blob handle header handle branch header  # 7
    pack parser socket pack header header branch index tree pack worker  # 8
    token header cache pack buffer delta handle branch blob  # 9
    stream buffer branch ref branch remote  # 10
    header handle queue header window remote buffer  # 11
    index pack pack ref rebase delta handle stream  # 12
    header buffer cache cache branch token object parser handle index window rebase  # 13
    tree buffer cache branch socket parser remote parser  # 14
    rebase delta handle object tree token blob queue remote  # 15
    cache worker index delta commit  # 16
    index rebase index worker remote  # 17
    ref token token merge rebase buffer  # 18
    worker rebase ref rebase object  # 19
    socket merge pack merge stream worker remote remote index pack  # 20
    window queue header rebase  # 21
    cache handle pack header buffer object rebase blob worker token  # 22
    blob queue queue pack socket header  # 23
    parser token buffer header header token delta remote tree commit  # 24
    delta object handle branch delta stream  # 25
    index pack blob blob worker tree cache branch tree delta worker  # 26
    cache window socket object socket commit  # 27
    window rebase token ref rebase ref window merge branch object  # 28
    handle worker delta commit branch token queue remote handle  # 29
    blob buffer object window cache commit index stream blob  # 30
    stream commit stream commit stream cache ref header pack  # 31
    stream handle buffer window token handle stream remote commit  # 32
    ref buffer delta token delta worker cache buffer delta blob window handle  # 33
    remote socket branch ref worker branch delta branch tree parser  # 34
    object blob branch remote rebase object blob  # 35
    worker pack rebase token delta branch  # 36
    buffer worker buffer socket rebase commit window delta  # 37
    commit merge parser parser merge queue buffer  # 38
    token buffer delta ref  # 39
    rebase buffer stream header handle  # 40
    parser commit header socket blob token merge index delta rebase ref cache  # 41
    object stream blob buffer token handle  # 42
    worker commit window socket cache ref blob rebase merge  # 43
    handle worker object stream worker tree merge  # 44
    branch rebase merge token socket pack token  # 45
    rebase tree buffer parser tree  # 46
    merge object branch tree remote  # 47
    parser commit branch window handle pack ref tree rebase tree merge merge  # 48
    queue stream object object object pack delta delta rebase rebase  # 49
    stream ref remote remote queue token object delta header branch index worker  # 50
    branch socket socket remote index  # 51
    window token socket header remote token header worker token handle index  # 52
    stream pack pack cache worker header window delta worker remote rebase  # 53
    remote handle index worker tree index tree merge delta header branch  # 54
    blob commit commit socket header window buffer tree  # 55
    stream pack delta ref buffer queue  # 56
