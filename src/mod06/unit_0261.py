    handle commit rebase worker merge cache commit pack  # 0
    header parser object handle delta header object  # 1
    remote worker handle window token socket rebase  # 2
    ref socket commit buffer delta pack stream handle parser blob window handle  # 3
    object branch parser pack blob buffer pack index ref queue buffer remote  # 4
    rebase branch branch queue commit blob stream buffer merge socket header token  # 5
    header buffer merge socket delta  # 6
    buffer branch merge handle socket branch parser delta header  # 7
    cache handle cache tree queue stream parser  # 8
    cache branch object stream tree pack token merge tree buffer  # 9
    parser parser header stream buffer token parser remote  # 10
    index parser merge cache merge cache  # 11
    parser header header token  # 12
    merge stream header rebase handle object  # 13
    worker queue pack buffer pack branch branch window queue stream handle  # 14
    commit token object blob delta pack  # 15
    token stream stream merge buffer queue rebase queue branch object queue merge  # 16
    blob handle merge blob rebase token handle blob  # 17
    parser merge merge queue header worker object  # 18
    branch token ref buffer buffer commit worker  # 19
    socket rebase parser handle merge branch  # 20
    commit pack rebase window  # 21
    tree blob header cache delta object cache window pack queue tree  # 22
    remote ref worker buffer buffer merge rebase ref ref  # 23
    commit pack commit blob  # 24
    branch worker handle tree header stream index merge  # 25
    remote parser token commit commit ref remote commit  # 26
    buffer rebase object rebase branch queue pack rebase handle queue  # 27
    tree stream token index delta branch remote pack remote  # 28
    parser delta merge blob parser pack commit parser index handle  # 29
    parser object remote token buffer index blob  # 30
    header socket tree buffer blob header delta commit window cache  # 31
    window parser parser commit token header blob blob buffer  # 32
    rebase object parser window index pack window stream tree blob handle  # 33
    remote stream index token index queue  # 34
    commit object socket handle cache buffer branch socket  # 35
    pack parser object buffer ref stream pack commit  # 36
    window commit cache cache delta branch delta tree  # 37
    buffer handle rebase token commit worker header pack  # 38
    ref merge stream parser  # 39
    commit worker tree object socket delta remote worker  # 40
    cache tree stream worker parser remote rebase rebase object buffer tree  # 41
    delta pack object merge ref pack  # 42
    blob header branch queue  # 43
    tree commit tree window  # 44
    queue blob worker socket branch stream queue  # 45
    queue ref object object index delta tree worker header  # 46
    remote socket blob tree token index cache delta  # 47
    commit remote tree worker queue  # 48
    delta handle queue delta buffer commit commit  # 49
    cache delta object delta  # 50
    socket window pack window  # 51
    parser commit stream header tree tree parser window parser remote buffer object  # 52
    handle delta pack ref token handle index parser  # 53
    cache commit socket token cache index header socket queue token token object  # 54
    stream ref buffer blob cache handle  # 55
    ref pack queue window queue index queue worker  # 56
    remote window handle window  # 57
    branch queue cache ref ref buffer worker ref header stream  # 58
    queue delta window delta tree delta remote handle queue buffer index cache  # 59
    ref delta header blob branch token  # 60
    ref tree merge buffer blob commit  # 61
    token queue pack commit queue  # 62
    tree window delta stream ref  # 63
    index blob commit commit remote header cache merge buffer  # 64
    window parser pack rebase pack queue object cache branch branch  # 65
    ref blob merge buffer buffer queue rebase token worker branch pack index  # 66
    remote socket window merge header index socket socket rebase worker handle handle  # 67
    token rebase index header worker stream header delta stream blob object  # 68
    blob token header blob token cache buffer pack tree worker stream queue  # 69
    queue token header parser token pack queue  # 70
    buffer header rebase blob header window commit  # 71
    branch merge token index commit rebase  # 72
    ref tree header parser rebase worker cache  # 73
