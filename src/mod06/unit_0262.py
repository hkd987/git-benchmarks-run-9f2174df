    worker token stream parser  # 0
    token worker merge handle  # 1
    cache worker blob pack branch delta index object object parser cache  # 2
    pack parser window delta remote  # 3
    header rebase cache index remote delta stream worker socket commit buffer  # 4
    handle buffer rebase pack object header  # 5
    handle rebase buffer cache tree token stream merge  # 6
    ref worker branch remote socket stream window delta delta header commit socket  # 7
    blob tree index handle cache cache header header buffer queue merge rebase  # 8
    pack rebase queue worker branch header window cache remote  # 9
    parser parser tree remote stream pack cache socket window branch tree blob  # 10
    index commit tree remote  # 11
    merge socket merge delta queue socket blob tree buffer commit delta  # 12
    rebase rebase branch index commit blob parser tree rebase buffer  # 13
    parser index delta window  # 14
    object merge socket token commit rebase tree blob branch  # 15
    remote commit index merge pack object delta delta queue socket merge  # 16
    tree handle handle ref object stream worker ref queue worker  # 17
    merge worker socket buffer branch object object queue stream  # 18
    ref handle index cache queue worker remote socket parser remote blob  # 19
    commit handle cache ref worker stream blob ref branch stream  # 20
    queue queue index remote  # 21
    object branch buffer branch parser window parser tree  # 22
    ref remote object worker queue stream stream remote token object  # 23
    merge commit rebase token remote buffer buffer header socket  # 24
    branch worker rebase tree socket  # 25
    branch merge header index  # 26
    blob buffer cache queue parser queue  # 27
    handle rebase delta socket stream parser handle handle merge socket cache blob  # 28
    commit token queue ref  # 29
    worker remote rebase worker blob window index remote parser merge  # 30
    remote buffer parser parser socket queue tree socket token pack socket merge  # 31
    handle object handle buffer object socket buffer pack socket handle  # 32
    merge merge ref delta delta window commit worker  # 33
    header queue blob commit queue  # 34
    worker delta delta delta rebase  # 35
    worker pack cache blob  # 36
    queue parser blob pack index parser stream remote rebase queue merge  # 37
    parser merge object parser queue buffer rebase tree tree  # 38
    branch buffer commit window  # 39
    tree window queue buffer object window cache header ref  # 40
    header object worker buffer delta ref stream delta  # 41
    ref buffer worker token  # 42
    header pack handle merge buffer rebase handle pack window  # 43
    queue remote header parser handle window token  # 44
    rebase parser buffer handle token rebase stream commit  # 45
    token cache worker parser object index ref remote pack  # 46
    cache branch queue merge queue ref worker token delta pack  # 47
    header stream index header rebase merge handle stream handle token delta  # 48
    index blob remote window buffer header header  # 49
    index commit buffer cache handle object  # 50
    commit index commit window  # 51
    remote rebase delta ref ref remote ref cache  # 52
    remote index window merge remote  # 53
    pack worker merge tree  # 54
    ref rebase queue index index merge blob socket rebase  # 55
    ref token buffer index handle  # 56
    ref remote index stream worker delta object token  # 57
    buffer socket buffer remote object token header pack pack socket  # 58
    branch commit remote commit worker handle branch  # 59
    cache pack header commit branch index buffer delta stream header  # 60
    header delta socket token token window branch remote  # 61
    rebase header branch merge window parser index rebase branch cache  # 62
    remote delta object remote blob rebase buffer  # 0
    index tree buffer buffer rebase object queue blob delta socket socket  # 1
    worker tree cache blob index buffer socket tree handle tree pack tree  # 2
    object handle delta ref  # 3
    cache worker merge ref branch delta pack  # 4
    socket token commit tree socket buffer  # 5
    pack worker worker queue header index socket token ref merge cache  # 6
    header delta queue buffer  # 7
    index cache buffer socket  # 8
    remote index token branch rebase parser stream rebase buffer branch  # 9
    handle remote window handle index buffer remote blob rebase blob worker  # 10
    pack delta stream blob  # 11
