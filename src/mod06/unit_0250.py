    stream buffer stream queue window merge  # 0
    queue header handle parser buffer remote tree blob  # 1
    ref index blob pack queue  # 2
    commit branch tree rebase tree parser window parser  # 3
    branch object worker index cache window rebase token merge  # 4
    index tree handle commit  # 5
    remote tree pack branch queue socket index branch merge socket  # 6
    worker index tree object header index object stream merge  # 7
    commit window tree stream remote header worker worker rebase  # 8
    stream remote remote header merge buffer commit token token window  # 9
    stream commit commit blob queue socket cache handle worker  # 10
    worker rebase rebase merge  # 11
    commit parser branch blob pack cache buffer index rebase handle  # 12
    buffer handle parser pack parser header stream  # 13
    branch object token commit  # 14
    branch commit ref blob merge stream  # 15
    buffer parser rebase merge token worker index index  # 16
    buffer index tree remote buffer handle token handle delta buffer header handle  # 17
    header stream queue object  # 18
    socket object index rebase rebase buffer commit worker stream index remote  # 19
    socket worker commit remote stream ref queue index  # 20
    object branch worker tree stream branch  # 21
    commit ref pack commit index window pack buffer object queue header  # 22
    buffer merge commit tree  # 23
    pack header index blob tree queue stream queue tree header commit  # 24
    parser queue remote worker window header merge  # 25
    cache parser pack delta header  # 26
    delta branch object branch worker pack worker window blob index  # 27
    token token handle index  # 28
    parser stream commit ref buffer window delta stream index merge rebase  # 29
    parser ref rebase queue index blob queue worker queue  # 30
    commit worker parser delta ref worker pack buffer ref  # 31
    header merge cache rebase merge queue blob  # 32
    branch remote remote handle  # 33
    buffer delta blob tree stream  # 34
    window ref buffer worker worker  # 35
    socket rebase stream object parser tree delta rebase  # 36
    tree worker queue rebase header branch  # 37
    delta blob object blob header stream rebase token  # 38
    stream merge stream cache worker  # 39
    merge object rebase cache stream parser token header tree  # 40
    delta queue worker merge token buffer ref object queue parser  # 41
    commit worker handle object header merge branch stream socket blob index queue  # 42
    cache buffer pack branch socket ref header merge  # 43
    index handle rebase blob remote pack  # 44
    merge pack commit token  # 45
    header branch cache header commit merge pack stream token  # 46
    commit token cache object stream buffer delta rebase  # 47
    index branch tree delta handle token pack header parser delta queue  # 48
    buffer token branch queue ref object rebase blob branch  # 49
    remote ref tree tree tree  # 50
    buffer buffer handle queue index rebase worker commit delta header  # 51
    pack parser parser buffer header  # 52
    commit socket buffer token token window token rebase rebase header delta  # 53
    window buffer cache header cache merge token remote parser index  # 54
    tree header buffer socket queue remote object queue socket commit stream  # 55
    parser blob branch header remote branch merge stream tree index commit stream  # 56
    handle ref cache buffer remote header rebase merge cache  # 57
    branch pack parser socket token branch  # 58
    rebase token merge socket stream branch ref buffer handle cache branch  # 59
