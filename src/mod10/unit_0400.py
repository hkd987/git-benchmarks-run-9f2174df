    stream ref remote blob blob window  # 0
    queue branch header ref  # 1
    handle buffer merge header branch branch  # 2
    blob queue buffer socket  # 3
    header remote delta tree merge window worker cache ref merge window blob  # 4
    pack worker remote cache cache header delta cache queue  # 5
    merge remote delta token rebase queue index buffer window parser rebase socket  # 6
    handle handle branch rebase header delta remote stream cache blob stream buffer  # 7
    rebase blob remote window merge cache  # 8
    window parser worker ref  # 9
    remote cache header worker merge rebase remote tree worker branch  # 10
    merge merge stream socket index queue  # 11
    pack worker ref buffer  # 12
    tree delta handle tree index branch parser  # 13
    parser token merge ref window header header pack index handle tree token  # 14
    socket object pack window ref parser  # 15
    header stream merge window merge blob worker blob blob  # 16
    queue branch parser rebase blob  # 17
    header index handle pack header rebase rebase parser remote  # 18
    object worker rebase branch cache commit tree index queue window pack  # 19
    window parser handle remote delta  # 20
    pack merge queue remote ref tree rebase parser window pack token  # 21
    index ref stream handle remote window pack token merge queue header commit  # 22
    pack queue merge merge header delta cache  # 23
    blob tree header buffer merge commit worker commit  # 24
    commit blob object tree merge window stream object  # 25
    ref blob parser ref rebase queue  # 26
    commit commit stream pack commit header branch index buffer window blob  # 27
    blob buffer worker parser branch tree token token  # 28
    ref cache branch pack stream socket socket socket tree  # 29
    stream index token parser ref worker handle tree  # 30
    merge delta socket parser  # 31
    object object object index delta socket buffer  # 32
    remote worker queue stream handle buffer index buffer merge commit ref delta  # 33
    handle merge tree ref  # 34
    parser index cache stream cache stream  # 35
    tree handle cache index socket rebase token blob rebase blob  # 36
    branch remote stream socket pack queue merge socket blob  # 37
    parser tree buffer socket stream  # 38
    buffer delta ref token queue index remote window  # 39
    socket delta pack window token socket cache queue index rebase  # 40
    tree window buffer object cache  # 41
    stream stream ref socket ref  # 42
    buffer branch header buffer cache  # 43
    tree window buffer socket socket window stream window  # 44
    branch pack header remote  # 45
    handle ref merge pack blob worker stream object  # 46
    worker queue rebase worker object tree object  # 47
    stream delta pack rebase window rebase tree buffer cache token  # 48
    worker merge index buffer rebase ref index pack blob  # 49
    pack header socket token index tree merge blob delta ref blob worker  # 50
    stream handle pack remote remote delta cache queue object token  # 51
    ref object socket socket token parser  # 52
    delta tree socket worker header buffer buffer branch tree  # 53
    blob remote cache pack  # 54
    rebase stream tree header delta  # 55
    commit header header buffer branch header token queue tree  # 56
    branch window tree cache socket header blob rebase queue handle delta  # 57
    ref commit ref delta  # 58
    header delta object ref  # 59
    tree ref worker header buffer remote remote buffer stream  # 60
    parser object header ref index  # 61
    window window object delta worker buffer  # 62
    rebase buffer branch blob cache object  # 63
    rebase cache remote rebase buffer handle commit handle rebase stream socket merge  # 64
    buffer buffer socket tree  # 65
    worker worker worker worker delta  # 66
    worker buffer tree blob remote remote  # 67
    rebase pack socket worker commit parser ref blob  # 68
    rebase remote delta ref socket parser index handle remote rebase socket merge  # 69
    window ref token ref ref rebase remote  # 70
    token ref worker ref window pack queue handle delta  # 71
    branch object stream commit queue pack commit branch  # 72
    blob handle buffer parser branch rebase commit  # 73
    socket tree tree tree rebase index parser  # 74
    object object header object merge token branch rebase blob socket  # 75
    stream cache queue remote socket ref window handle  # 76
    parser object index stream tree socket commit delta index ref merge commit  # 77
    queue header window socket pack socket ref handle tree delta remote  # 78
    branch branch index remote handle object stream remote socket stream object  # 79
    token handle header token remote  # 80
    stream branch index ref merge delta blob ref ref  # 81
    pack remote ref pack  # 0
    branch rebase tree delta index object pack ref tree  # 1
    delta object tree buffer header  # 2
    merge branch worker worker ref remote tree stream parser  # 3
    blob worker stream buffer window object  # 4
    stream stream stream worker buffer index cache ref  # 5
    rebase index merge cache queue delta blob delta  # 6
    merge parser remote commit worker rebase token blob buffer  # 7
    remote token token delta index blob remote queue ref ref header remote  # 8
    socket rebase branch tree index header  # 9
    stream index queue remote  # 10
    window index rebase index branch ref object  # 11
    branch delta ref handle buffer object blob queue  # 12
    stream parser socket handle queue worker ref merge object commit branch  # 13
    delta branch branch rebase remote socket stream socket token  # 14
