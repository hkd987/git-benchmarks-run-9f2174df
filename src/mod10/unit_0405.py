    index window queue header token merge worker tree branch  # 0
    index blob tree cache buffer stream delta parser  # 1
    worker queue parser stream branch tree queue branch tree remote worker ref  # 2
    stream remote ref branch parser parser tree delta stream worker index object  # 3
    blob remote tree rebase object rebase merge socket stream index remote  # 4
    queue pack pack cache cache index pack queue index rebase blob  # 5
    ref blob remote parser rebase buffer  # 6
    tree merge buffer object blob rebase rebase queue  # 7
    rebase rebase branch object commit pack token cache  # 8
    object index token cache stream  # 9
    worker token tree queue header parser branch delta delta remote window tree  # 10
    handle parser delta cache window stream worker worker tree  # 11
    pack header tree stream remote pack delta index pack buffer queue window  # 12
    blob pack token worker  # 13
    window index worker tree pack  # 14
    index queue buffer blob delta window  # 15
    index header branch socket  # 16
    handle worker commit token queue buffer  # 17
    buffer delta branch handle branch stream  # 18
    merge pack stream socket parser blob worker  # 19
    token commit delta queue token ref branch branch  # 20
    blob stream header cache branch  # 21
    parser object header remote  # 22
    buffer buffer cache parser handle token branch tree merge  # 23
    tree ref index index tree buffer blob  # 24
    tree ref remote delta ref worker blob rebase blob pack queue  # 25
    window remote parser object header header worker socket  # 26
    tree object branch commit pack handle delta window handle  # 27
    merge buffer queue commit parser socket rebase  # 28
    blob index ref cache stream pack worker buffer commit  # 29
    tree delta object branch worker commit remote socket index  # 30
    index index buffer worker  # 31
    merge delta queue rebase tree parser window merge tree commit header object  # 32
    queue ref index worker stream tree queue branch buffer header branch merge  # 33
    rebase object tree object stream delta queue cache  # 34
    pack stream ref tree  # 35
    blob delta ref worker branch branch header worker handle cache branch merge  # 36
    branch handle commit parser cache merge delta delta socket commit blob worker  # 37
    pack queue cache remote token ref pack pack token  # 38
    rebase branch object pack remote header parser stream token socket parser tree  # 39
    token index commit window rebase token tree branch pack  # 40
    worker rebase socket header commit window  # 41
    window window queue buffer pack pack  # 42
    blob tree rebase window  # 43
    window ref stream window rebase  # 44
    tree header header socket commit header branch tree blob  # 45
    delta ref stream parser tree tree window cache handle rebase branch commit  # 46
    queue socket window rebase blob tree token buffer stream rebase parser  # 47
    merge rebase buffer buffer socket stream worker index  # 48
    branch token delta stream header buffer tree handle tree stream blob delta  # 49
    commit handle object rebase index buffer tree worker merge window window cache  # 50
    rebase header remote parser  # 51
    ref stream buffer rebase token delta merge  # 52
    commit ref remote cache cache remote delta  # 53
