    worker pack worker commit handle stream  # 0
    commit worker object pack window cache index header header commit  # 1
    merge window branch rebase branch header delta  # 2
    socket tree blob header worker  # 3
    pack index queue blob tree socket cache branch cache object stream  # 4
    delta pack ref socket pack window queue header worker  # 5
    merge stream cache stream index blob cache merge window delta queue worker  # 6
    worker parser merge parser pack parser branch rebase worker delta header worker  # 7
    window cache blob socket worker pack worker ref  # 8
    token delta queue cache blob token delta  # 9
    merge commit token commit  # 10
    stream remote token remote window blob stream pack header index worker queue  # 11
    queue queue header index commit blob cache branch rebase delta remote  # 12
    blob merge handle buffer object  # 13
    merge object merge merge object commit socket  # 14
    window index tree blob pack  # 15
    header delta window parser parser handle queue socket parser ref commit  # 16
    parser commit header ref tree header commit header  # 17
    ref rebase window socket commit tree ref commit object index delta  # 18
    pack tree merge commit index window commit branch stream buffer rebase  # 19
    ref tree queue handle index  # 20
    tree blob pack rebase socket  # 21
    delta worker branch cache delta window  # 22
    rebase ref pack token ref buffer commit tree cache merge  # 23
    token rebase window stream merge  # 24
    ref buffer buffer delta header branch  # 25
    cache worker stream buffer stream rebase branch ref handle header remote parser  # 26
    worker buffer buffer buffer stream cache index object rebase ref object remote  # 27
    header parser commit cache ref pack rebase token  # 28
    blob token stream remote delta window rebase worker commit window blob parser  # 29
    blob branch stream tree tree index  # 30
