    delta header socket header ref delta stream rebase cache  # 0
    worker object index merge stream branch rebase delta index ref pack  # 1
    delta ref branch handle delta merge index commit  # 2
    remote stream remote handle queue  # 3
    handle rebase worker merge tree delta  # 4
    tree delta token token worker token delta  # 5
    tree window index index remote token  # 6
    queue token branch merge  # 7
    ref window remote cache parser branch  # 8
    queue remote socket merge rebase ref worker object ref cache stream  # 9
    queue blob handle ref parser blob  # 10
    pack delta buffer blob rebase queue merge delta  # 11
    blob branch delta handle blob parser parser parser cache object cache  # 12
    pack socket object rebase header stream parser index branch ref socket  # 13
    token worker ref worker stream tree merge delta  # 14
    tree buffer branch branch merge pack branch merge  # 15
    buffer queue worker index buffer window token header object object  # 16
    worker merge pack delta remote index token worker parser  # 17
    object handle socket blob parser handle window index socket tree parser  # 18
    window commit stream cache delta  # 19
    socket ref buffer token branch buffer window window merge queue merge remote  # 20
    worker ref handle blob window blob tree  # 21
    blob pack window parser index ref index stream parser  # 22
    branch pack stream remote object branch  # 23
    ref index merge ref pack pack socket cache blob ref token branch  # 24
    handle worker branch tree rebase delta delta commit worker  # 25
    rebase index remote buffer header index branch object commit rebase queue token  # 26
    cache stream socket queue blob merge parser parser remote  # 27
    worker header queue delta blob branch  # 28
    socket object handle rebase pack  # 29
    blob merge handle blob handle  # 30
    object delta stream index parser parser ref  # 31
    pack socket parser delta socket merge stream parser  # 32
    worker object buffer branch ref  # 33
    worker commit window header token handle delta queue buffer  # 34
    object header cache handle queue ref ref index  # 35
    object object queue delta delta header handle delta pack object pack  # 36
    header index branch parser blob  # 37
    branch delta cache cache blob pack  # 38
    cache merge merge header token stream object rebase blob rebase ref socket  # 39
    queue tree pack pack merge object worker socket  # 40
    object socket index handle window  # 41
    handle object cache blob cache token handle pack token object  # 42
    worker window worker parser branch ref blob  # 43
    tree buffer branch queue index delta blob  # 44
    queue token pack token commit queue blob  # 45
    cache delta remote handle pack queue parser remote buffer  # 46
    cache branch pack object object delta token  # 47
    branch remote window token ref tree pack queue  # 48
    worker stream handle merge rebase blob delta blob ref ref  # 49
    ref cache delta remote window  # 50
    pack header rebase index handle commit header remote delta branch  # 51
    blob delta token blob pack token rebase worker  # 52
    socket window pack buffer  # 53
    handle stream token remote queue queue  # 54
    handle token window queue ref  # 55
    blob buffer parser blob  # 56
    merge rebase cache buffer header  # 57
    token cache token ref cache  # 58
    buffer remote branch buffer stream queue stream commit tree remote  # 59
    tree remote rebase parser remote  # 60
    queue merge ref token blob blob object  # 61
    parser worker buffer parser  # 62
    remote merge tree queue rebase cache window blob blob parser socket  # 63
    stream handle object queue handle tree  # 64
