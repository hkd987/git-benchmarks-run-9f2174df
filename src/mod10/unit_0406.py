    ref parser rebase blob socket pack rebase header worker  # 0
    blob delta tree blob branch ref socket ref  # 1
    window handle delta stream socket  # 2
    stream delta ref token cache header token pack merge object  # 3
    index handle remote pack branch stream  # 4
    tree queue branch object window merge parser index handle  # 5
    handle rebase stream delta merge parser branch token merge cache stream  # 6
    handle cache window ref token ref queue socket remote queue window  # 7
    cache token index token pack ref socket window handle queue  # 8
    pack object worker rebase  # 9
    stream ref cache stream blob window remote  # 10
    pack window cache object  # 11
    remote ref ref rebase  # 12
    socket index buffer commit cache branch rebase object handle  # 13
    queue handle parser pack worker cache merge token object socket tree  # 14
    delta socket cache header merge merge commit parser  # 15
    queue rebase ref commit  # 16
    branch buffer merge window delta tree object ref handle  # 17
    delta rebase worker header  # 18
    queue handle commit index rebase handle merge handle window buffer token ref  # 19
    header token merge worker merge parser header worker commit blob merge delta  # 20
    parser header branch window commit delta commit socket merge window branch stream  # 21
    cache buffer cache commit worker token header socket parser  # 22
    handle socket stream blob object buffer ref header  # 23
    stream stream index merge merge  # 24
    branch header queue ref window branch header ref branch  # 25
    merge window window index object window cache rebase parser remote rebase stream  # 26
    parser token window rebase index tree blob cache  # 27
    rebase object stream pack header remote rebase commit  # 28
    object token branch parser index header window commit worker delta worker delta  # 29
    merge tree blob rebase window rebase  # 30
    blob window delta remote token remote queue  # 31
    index rebase cache commit remote stream remote merge  # 32
    cache socket object handle pack remote stream worker cache  # 33
    stream delta stream pack pack delta  # 34
    object buffer cache token token worker parser object parser  # 35
    header rebase merge window  # 36
    header handle rebase commit ref worker cache header merge cache commit  # 37
    token object queue commit pack parser blob remote token  # 38
    cache parser branch worker buffer remote pack remote buffer token queue worker  # 39
    rebase buffer merge rebase remote  # 40
    parser branch ref handle parser index blob remote header  # 41
    pack cache buffer delta pack rebase index parser blob buffer  # 42
    tree token tree header ref queue socket stream tree header  # 43
    commit remote parser remote worker  # 44
    socket commit socket blob  # 45
    parser tree tree rebase token  # 46
    remote branch remote stream object parser socket branch merge parser branch  # 47
    stream remote parser tree token commit socket cache tree rebase blob  # 48
    handle tree tree ref stream ref window  # 49
    tree worker remote branch  # 50
    stream object merge queue blob  # 51
    cache window object ref ref branch token tree tree  # 52
    branch branch object socket  # 53
    rebase ref parser delta buffer object commit handle token  # 54
    header handle queue tree tree cache merge branch stream  # 55
    queue token buffer index commit merge  # 56
    object header cache cache parser stream blob branch  # 57
    handle object branch remote header rebase stream worker  # 58
    delta token commit remote  # 59
    merge branch stream pack commit parser  # 60
    blob buffer header worker  # 61
    queue token handle tree parser remote commit  # 62
    tree branch blob handle worker branch  # 63
    parser ref parser pack worker  # 64
    header ref stream delta cache socket tree tree blob index  # 65
    header cache cache object worker  # 66
    object token header socket branch branch  # 67
    stream blob stream tree parser ref blob commit blob tree delta  # 68
    delta branch queue remote commit  # 69
    cache blob parser commit merge ref merge delta cache pack handle buffer  # 70
    rebase queue token worker handle window header buffer pack  # 71
    tree merge index pack merge commit  # 72
    ref worker queue remote blob  # 73
