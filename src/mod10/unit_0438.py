    pack header remote header  # 0
    worker buffer parser rebase  # 1
    object object parser buffer commit window  # 2
    ref rebase commit index rebase header delta  # 3
    object merge branch worker delta worker  # 4
    queue parser remote object remote delta merge branch merge stream stream commit  # 5
    delta index rebase worker  # 6
    index header window tree  # 7
    token token handle ref ref queue socket ref worker  # 8
    worker cache merge tree index socket pack merge  # 9
    merge pack tree token token index pack branch merge rebase token  # 10
    blob socket tree pack socket branch buffer blob  # 11
    parser buffer commit worker token parser handle parser parser header stream delta  # 12
    buffer index remote blob commit worker handle token socket index queue ref  # 13
    worker buffer token blob index header tree  # 14
    rebase buffer ref tree stream rebase delta  # 15
    window worker worker branch  # 16
    cache remote pack blob socket remote header ref  # 17
    header token parser buffer token remote tree tree parser tree commit  # 18
    worker merge ref blob delta socket buffer window rebase commit socket  # 19
    socket pack ref queue rebase pack  # 20
    handle cache stream tree buffer remote queue handle stream merge  # 21
    worker commit window worker index buffer delta pack handle merge buffer  # 22
    stream window tree delta parser blob window  # 23
    header ref branch tree token ref  # 24
    parser buffer stream buffer queue commit ref buffer stream handle stream  # 25
    token ref socket parser socket stream stream window commit token socket queue  # 26
    index queue stream cache rebase blob buffer buffer branch delta stream  # 27
    socket index token token worker branch worker queue  # 28
    socket blob header branch rebase tree rebase  # 29
    object socket delta branch buffer pack remote index pack socket cache socket  # 30
    stream merge index object blob socket stream parser window  # 31
    parser blob cache pack branch parser window handle  # 32
    cache branch queue delta  # 33
    stream parser queue token merge token  # 34
    cache handle socket blob commit header window  # 35
    commit socket queue pack  # 36
    object tree worker ref stream tree merge object  # 37
    index ref buffer pack index object blob blob tree tree index  # 38
    parser window stream stream worker  # 39
    rebase blob rebase window queue tree window parser handle  # 40
    branch rebase header worker window blob object queue pack window token merge  # 41
    worker cache delta stream worker branch object parser delta  # 42
    object socket handle commit blob branch object worker handle  # 43
    object buffer buffer cache header tree rebase window cache delta token  # 44
    remote buffer parser commit blob token worker remote commit remote header branch  # 45
    remote delta pack commit commit socket rebase handle tree  # 46
    stream header header pack ref handle object  # 47
    buffer commit queue token handle socket index branch  # 48
    branch rebase branch object tree tree stream worker merge tree  # 49
    branch rebase rebase handle object cache worker buffer  # 50
    worker branch commit commit branch rebase blob cache buffer branch  # 51
    handle rebase worker window  # 52
    object window window worker  # 53
    branch branch object remote pack  # 54
    window queue buffer merge delta worker window  # 55
    socket index token queue cache token socket rebase  # 56
    worker buffer branch remote worker header stream window branch branch buffer  # 57
    merge blob worker branch queue ref queue index  # 58
    index stream socket tree cache rebase queue  # 59
    object ref worker tree object delta  # 60
    token worker buffer parser socket delta blob cache token delta commit  # 61
    rebase pack delta tree object  # 62
    merge tree delta queue  # 63
    socket stream rebase header cache index window commit ref commit remote  # 64
    tree queue index token  # 65
    ref object cache remote object  # 66
    socket header tree rebase rebase tree tree cache  # 67
    object merge remote pack branch rebase cache  # 68
    remote token tree blob pack token cache handle  # 69
    delta socket index ref worker handle rebase index delta merge blob  # 70
    object commit queue commit merge  # 71
    merge ref tree branch object rebase  # 72
    delta rebase index handle socket  # 73
    object handle socket branch index header pack  # 74
    branch index commit ref parser branch header branch parser window  # 75
    blob object token ref parser parser rebase ref delta stream stream tree  # 76
    pack parser remote merge parser cache commit window  # 77
    buffer token queue commit buffer rebase remote token handle queue  # 78
    queue branch queue delta cache buffer delta window  # 79
    stream handle pack blob merge index  # 80
    commit parser buffer pack worker  # 81
    window header object branch cache socket header remote header handle  # 82
    index worker pack pack branch  # 83
    pack index window queue socket pack  # 84
