    commit pack parser buffer branch remote parser remote  # 0
    cache remote cache parser token index ref buffer token header worker  # 1
    worker index remote delta index  # 2
    index tree window token window branch commit branch buffer tree  # 3
    branch remote window rebase socket  # 4
    header buffer object worker queue pack  # 5
    header delta handle queue cache handle blob remote ref cache remote  # 6
    socket handle token worker remote index token socket blob stream  # 7
    pack object socket blob remote token ref tree  # 8
    object ref worker socket socket branch  # 9
    branch token cache header header delta token pack index  # 10
    tree stream parser socket header  # 11
    delta queue blob blob handle blob  # 12
    token tree merge index object queue token commit delta  # 13
    stream branch blob tree cache stream  # 14
    delta worker rebase delta  # 15
    token buffer blob worker  # 16
    merge blob handle remote worker buffer delta blob pack blob remote commit  # 17
    buffer worker cache header window  # 18
    index blob rebase stream tree socket  # 19
    merge pack branch tree  # 20
    tree header socket queue tree tree window merge tree delta header  # 21
    cache pack handle delta rebase delta stream queue socket pack branch  # 22
    delta object commit merge cache index cache remote window object  # 23
    pack branch merge header blob blob  # 24
    ref tree merge index handle index socket socket header stream  # 25
    parser remote stream ref tree  # 26
    delta commit delta index index remote socket handle header ref pack  # 27
    ref queue buffer branch  # 28
    tree parser ref merge ref socket blob rebase  # 29
    queue cache rebase window merge merge rebase buffer pack parser stream  # 30
    pack parser stream tree branch  # 31
    pack handle branch blob cache  # 32
    buffer branch buffer cache merge merge worker index header tree  # 33
    object ref delta handle pack index commit header socket token merge merge  # 34
    window handle tree header parser stream  # 35
    pack handle rebase merge window remote remote  # 36
    merge commit index handle token branch merge window queue index object delta  # 37
    pack buffer object object delta buffer socket socket commit socket branch token  # 38
    pack parser pack token object socket commit branch branch  # 39
    stream socket index parser object header  # 40
    branch branch window merge object  # 41
    object branch merge object socket buffer window  # 42
    rebase commit socket pack  # 43
    handle rebase ref remote remote queue  # 44
    pack buffer tree tree remote index cache buffer header tree ref  # 45
    worker header object window cache cache rebase merge  # 46
    commit handle parser rebase rebase window blob branch parser commit token rebase  # 47
    worker cache object parser index merge parser pack  # 48
