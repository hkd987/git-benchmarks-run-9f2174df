    buffer worker stream handle handle commit commit index window commit cache parser  # 0
    delta ref pack handle window handle commit window  # 1
    socket cache delta object worker remote handle header branch object  # 2
    delta worker header tree tree worker rebase token  # 3
    merge parser cache stream pack ref object pack tree  # 4
    ref delta window object  # 5
    rebase window delta cache token socket delta stream stream merge  # 6
    delta cache merge merge branch stream buffer ref buffer blob merge  # 7
    remote window token remote cache socket pack stream buffer handle  # 8
    ref delta worker cache remote  # 9
    queue pack stream object index pack parser pack worker  # 10
    token socket buffer parser merge commit queue ref rebase  # 11
    index parser object tree window handle ref header parser remote object worker  # 12
    merge ref cache ref  # 13
    header cache parser window merge ref index index pack  # 14
    tree object cache branch branch blob  # 15
    stream rebase object blob buffer blob branch worker socket  # 16
    window rebase queue pack branch worker blob  # 17
    delta queue ref blob remote header object  # 18
    queue parser window blob window header stream commit  # 19
    stream remote ref index ref  # 20
    blob index buffer buffer rebase ref rebase buffer remote blob  # 21
    stream cache header token token handle stream merge cache  # 22
    tree index object pack  # 23
    queue remote index tree tree cache cache  # 24
    header index socket remote commit branch  # 25
    handle window branch handle  # 26
    tree buffer header buffer merge header  # 27
    rebase delta commit delta tree socket rebase delta commit  # 28
    socket index handle commit worker  # 29
    delta worker parser branch queue buffer rebase  # 30
    merge stream ref handle parser parser socket socket handle  # 31
    ref parser parser rebase cache buffer blob cache  # 32
