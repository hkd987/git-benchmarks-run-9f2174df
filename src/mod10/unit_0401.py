    header rebase ref header window cache worker merge cache object ref worker  # 0
    buffer ref delta stream blob cache object token pack pack pack merge  # 1
    remote header blob ref queue ref worker cache  # 2
    index index buffer blob buffer worker window delta handle merge token remote  # 3
    rebase tree handle blob rebase tree worker worker queue rebase remote  # 4
    queue worker remote blob  # 5
    queue merge socket delta  # 6
    tree remote queue queue queue merge ref branch window delta  # 7
    commit branch commit pack header queue header tree remote  # 8
    token rebase cache pack  # 9
    blob queue queue remote  # 10
    index commit window index handle ref queue queue  # 11
    worker ref tree blob blob window parser branch object socket branch queue  # 12
    remote stream parser delta  # 13
    commit pack queue buffer commit remote worker handle merge commit  # 14
    socket stream remote worker pack commit tree index  # 15
    queue queue cache header  # 16
    queue stream object index stream  # 17
    worker delta index socket rebase merge handle handle cache queue  # 18
    header object rebase rebase token handle  # 19
    index parser parser buffer socket blob parser  # 20
    index branch queue queue stream queue socket  # 21
    window worker window ref cache header queue stream merge header branch parser  # 22
    parser handle delta tree queue delta  # 23
    window header merge commit pack  # 24
    stream commit worker handle cache blob pack tree object socket merge  # 25
    tree merge ref token delta merge window  # 26
    token object header remote blob worker parser object tree tree queue  # 27
    blob socket queue header parser pack commit remote header worker merge  # 28
    remote cache window token window  # 29
    branch pack cache socket socket stream blob window socket pack parser  # 30
    tree worker remote remote ref tree header  # 31
    rebase parser tree remote queue tree object ref blob remote  # 32
    cache rebase object ref remote handle ref branch socket merge remote  # 33
    tree parser remote socket queue commit window commit buffer blob ref  # 34
    window cache object worker cache socket rebase blob rebase queue parser merge  # 35
    delta delta branch buffer socket cache cache ref window index commit branch  # 36
    worker delta queue token worker commit tree rebase commit handle  # 37
    index pack socket object  # 38
    remote branch socket header header rebase cache socket queue socket socket  # 39
    parser delta object parser header queue blob  # 40
    ref cache object ref header queue token object window window window  # 41
    cache tree commit pack cache ref stream remote pack buffer  # 42
    cache commit rebase delta stream ref handle buffer merge header commit branch  # 43
    merge object header commit parser object  # 44
    worker parser parser tree merge  # 45
    ref object parser rebase merge queue buffer worker merge header token token  # 46
    token blob tree commit pack index socket stream queue stream pack worker  # 47
    merge branch worker handle merge pack merge queue  # 48
    stream merge parser parser token header cache object  # 49
    remote blob cache tree  # 50
    socket tree handle index  # 51
    blob cache commit cache stream pack object cache stream  # 52
    header window cache header object window merge buffer worker  # 53
    handle pack parser index  # 54
    handle socket window handle socket  # 55
    handle merge branch cache tree window buffer header tree tree index handle  # 56
    index index header worker delta  # 57
    cache stream parser branch pack ref delta blob remote queue  # 58
    ref buffer socket parser tree merge  # 59
    queue object parser token buffer handle worker  # 60
    index branch blob buffer socket header queue  # 61
    object header window object  # 62
    worker remote tree ref remote remote index  # 63
    blob window object pack commit header cache rebase token buffer blob  # 64
    stream pack pack delta header header object queue  # 65
    stream tree parser handle token index ref parser window parser rebase  # 66
    parser pack cache object delta branch delta  # 0
    worker parser stream index index worker parser  # 1
    delta blob remote tree buffer rebase socket worker  # 2
    branch worker pack socket blob delta queue pack window branch window parser  # 3
    tree socket cache merge token commit branch worker pack blob commit  # 4
    merge commit tree blob stream object remote handle ref  # 5
    handle header remote commit delta pack socket handle queue buffer pack  # 6
    tree queue index blob stream worker blob stream parser object window tree  # 7
    commit handle buffer handle blob rebase tree ref rebase tree socket pack  # 8
    worker queue merge stream header  # 9
    token buffer cache socket handle worker blob  # 10
    merge commit cache worker object rebase token header ref handle  # 11
    branch stream worker worker window token rebase blob handle delta merge worker  # 12
