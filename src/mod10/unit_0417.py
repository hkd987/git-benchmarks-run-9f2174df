    queue ref rebase header rebase  # 0
    queue parser tree rebase object pack merge object  # 1
    header worker index blob  # 2
    blob stream stream rebase header token blob index commit queue  # 3
    socket index stream parser index  # 4
    branch object delta stream tree  # 5
    window window remote parser ref handle token branch index merge blob token  # 6
    parser index cache merge object pack queue delta  # 7
    cache rebase pack object worker merge  # 8
    buffer queue index token parser worker commit branch blob  # 9
    tree delta pack stream commit  # 10
    blob commit delta index pack object  # 11
    rebase tree token handle stream queue  # 12
    branch merge worker window delta token cache blob merge cache  # 13
    index delta pack delta  # 14
    merge parser stream ref socket  # 15
    cache queue buffer handle window token parser merge token stream  # 16
    buffer socket buffer pack pack object object  # 17
    merge branch merge delta queue  # 18
    object header queue pack header  # 19
    rebase tree token blob  # 20
    queue object tree window  # 21
    index parser window commit tree  # 22
    cache tree object cache socket handle  # 23
    worker token buffer delta queue  # 24
    merge cache merge branch merge  # 25
    delta tree tree worker commit  # 26
    merge index cache cache window header branch socket branch  # 27
    buffer queue token index handle tree parser socket  # 28
    buffer header header header token merge handle handle merge  # 29
    tree branch rebase parser merge branch remote tree object remote  # 30
    header window header buffer  # 31
    cache tree parser queue index token socket  # 32
    token parser delta blob worker index stream buffer commit remote worker socket  # 33
    handle buffer tree blob handle rebase rebase worker blob index stream  # 34
    rebase delta merge blob tree ref  # 35
    blob queue socket branch pack  # 36
    delta remote socket queue token parser window commit object cache  # 37
    worker queue socket token ref socket delta header  # 38
    parser merge header window queue index cache index  # 39
    queue stream handle cache blob object commit header pack worker  # 40
    rebase stream cache ref header worker buffer stream socket commit handle  # 41
    remote cache branch socket window token header  # 42
    cache tree branch token  # 43
    tree queue blob branch parser object  # 44
    cache token blob commit index merge socket object object  # 45
    pack remote stream worker delta remote socket tree tree parser cache  # 46
    header queue worker rebase pack rebase header handle token window parser  # 47
    blob cache parser remote header rebase branch  # 48
    blob window token socket  # 49
    commit pack pack remote window socket branch  # 50
    remote buffer pack queue  # 51
    cache blob ref commit rebase  # 52
    index rebase object delta header header worker header socket  # 53
    pack merge token buffer rebase object  # 54
    index window object buffer queue parser token  # 55
    branch pack merge rebase branch pack token  # 56
    remote stream parser blob tree  # 57
    ref merge merge blob tree branch branch pack buffer worker  # 58
    index delta handle worker tree header header commit  # 59
    window commit rebase branch  # 60
    queue commit header commit branch  # 61
    queue buffer pack handle commit object blob handle  # 62
    window parser branch queue queue socket queue rebase  # 63
    token parser rebase worker merge delta blob cache delta object handle pack  # 64
    queue rebase pack index worker blob buffer  # 65
    cache stream parser parser  # 66
    token delta handle buffer index worker index commit blob  # 67
    stream token merge queue  # 68
    cache socket stream index worker window object socket  # 69
    window token remote worker header socket delta worker remote cache  # 70
    cache tree object handle parser socket header  # 71
    tree blob ref cache  # 72
    tree worker header window queue window stream commit rebase  # 73
    blob branch window parser branch parser worker index object parser cache  # 74
    tree cache commit window  # 75
    ref queue tree worker commit pack remote ref delta  # 76
    delta ref header tree handle  # 77
    blob ref index delta  # 78
    parser tree commit pack worker tree tree  # 79
    branch token object socket branch branch object rebase rebase worker window  # 0
    delta socket rebase ref commit pack stream handle commit  # 1
    merge commit commit buffer worker queue parser index buffer  # 2
    branch object handle stream commit stream header parser tree delta handle header  # 3
    header tree rebase pack merge parser rebase pack commit  # 4
    delta ref remote buffer  # 5
    stream tree buffer delta pack token tree  # 6
    merge handle token handle index handle stream socket header  # 7
    parser token index commit branch ref parser commit  # 8
    stream queue cache socket worker queue token handle  # 9
    blob tree cache socket token parser  # 10
    branch object token socket buffer cache index branch header  # 11
