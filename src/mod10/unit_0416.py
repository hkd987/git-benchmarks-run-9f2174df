    tree parser socket window buffer stream index tree branch remote header commit  # 0
    commit index delta window object merge cache branch pack window  # 1
    branch worker token handle queue object handle stream ref  # 2
    object ref buffer buffer token merge  # 3
    queue token queue parser queue blob header blob branch delta  # 4
    window object queue branch branch blob  # 5
    queue index delta header delta worker ref branch token  # 6
    handle branch remote window token  # 7
    stream buffer cache blob worker index object queue object delta buffer  # 8
    object merge rebase parser buffer header stream  # 9
    handle branch blob rebase parser  # 10
    pack index parser queue handle blob socket remote parser commit  # 11
    token tree commit blob header pack  # 12
    rebase tree header tree  # 13
    delta commit blob remote  # 14
    pack delta pack branch  # 15
    window header rebase tree stream parser token blob  # 16
    queue ref socket header header buffer index branch remote  # 17
    pack buffer remote rebase tree tree ref tree queue handle cache  # 18
    queue stream rebase stream  # 19
    socket token delta stream delta  # 20
    delta parser socket handle  # 21
    delta window handle queue parser  # 22
    ref parser object index worker tree worker merge token socket  # 23
    blob delta delta buffer commit window worker queue handle worker  # 24
    handle tree token merge delta merge merge rebase stream remote  # 25
    blob index header stream rebase pack  # 26
    branch token buffer branch object merge index window handle parser tree ref  # 27
    worker blob rebase index merge cache  # 28
    pack delta socket tree blob commit buffer  # 29
    parser handle object token index object window  # 30
    header branch stream cache object blob parser  # 31
    window queue blob object  # 32
    header parser blob ref delta socket delta buffer  # 33
    remote commit queue commit parser remote ref header index delta queue header  # 34
    socket index pack branch rebase blob handle queue index remote  # 35
    ref cache token tree header cache rebase parser  # 36
    parser socket remote worker worker socket  # 37
    parser index commit handle commit index blob remote header ref header  # 38
    object buffer cache rebase branch merge queue index socket ref merge  # 39
    handle cache stream parser object buffer  # 40
    object cache header blob window ref handle cache token merge pack worker  # 41
    cache queue token socket token cache buffer header pack tree buffer  # 42
    commit token worker token object cache header header queue tree  # 43
    token socket tree queue socket window handle remote remote  # 44
    queue ref pack queue window ref branch header blob blob  # 45
    commit pack merge queue parser window buffer tree index merge  # 46
    merge pack tree window buffer commit worker  # 47
    parser parser window delta ref tree stream tree queue  # 48
    queue blob cache worker pack  # 49
    tree tree tree parser pack commit handle delta queue  # 50
    handle merge token buffer header window index buffer remote buffer  # 51
    object parser merge parser blob  # 52
    branch tree pack queue handle index handle  # 53
    parser stream ref delta worker object delta branch blob  # 54
    window index pack stream pack handle rebase pack  # 55
    tree header rebase tree  # 56
    cache blob object ref header handle object  # 57
    branch token socket queue merge branch commit ref parser parser buffer blob  # 58
    pack cache rebase window header socket blob merge blob header ref  # 59
    blob commit header object pack buffer buffer token  # 60
    window parser socket object index remote rebase worker commit blob pack rebase  # 61
    token branch socket buffer header cache object delta  # 62
    pack index ref object rebase header worker header window  # 63
    stream worker ref rebase parser commit buffer worker window  # 64
    handle branch delta commit pack commit stream queue index  # 65
    delta socket rebase buffer rebase delta  # 66
    window stream remote handle buffer handle stream token  # 67
    queue token token buffer stream commit  # 68
    rebase token commit blob  # 69
    socket rebase merge tree worker queue window commit socket  # 70
    socket buffer commit object ref commit  # 71
