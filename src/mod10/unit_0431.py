    pack ref socket index index cache tree blob  # 0
    index parser rebase cache delta delta handle token  # 1
    buffer cache handle index stream queue socket  # 2
    rebase parser blob ref pack remote commit  # 3
    stream cache remote blob cache remote  # 4
    header handle blob token cache rebase blob queue pack pack index  # 5
    buffer merge merge worker merge queue header worker  # 6
    branch stream tree cache object index remote  # 7
    parser token merge worker tree ref pack queue commit  # 8
    merge window header delta pack branch  # 9
    merge branch cache handle worker object window blob token  # 10
    stream object commit queue ref token index object header index  # 11
    delta token branch branch object worker buffer buffer index token buffer  # 12
    header blob branch pack  # 13
    header branch remote queue branch commit merge  # 14
    worker worker queue rebase object rebase merge tree tree header  # 15
    commit socket rebase queue merge ref cache worker queue header  # 16
    handle cache window header delta blob rebase buffer tree  # 17
    pack index token pack parser blob  # 18
    cache cache stream queue delta index blob window window ref delta tree  # 19
    blob branch tree commit worker  # 20
    branch rebase branch handle  # 21
    object cache ref rebase  # 22
    object ref object remote stream commit worker queue delta branch handle  # 23
    parser object tree index tree  # 24
    branch object remote branch socket parser header merge rebase  # 25
    branch tree queue cache  # 26
    queue buffer socket object delta  # 27
    stream branch tree header  # 28
    queue pack handle token cache commit  # 29
