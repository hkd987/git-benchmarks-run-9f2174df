    parser socket window merge remote branch  # 0
    header token ref ref rebase header branch handle pack merge  # 1
    blob tree pack ref  # 2
    cache branch merge commit tree handle parser delta blob window ref  # 3
    object rebase tree delta token buffer buffer branch  # 4
    pack index commit branch parser pack buffer header object handle remote object  # 5
    branch delta handle object index rebase  # 6
    worker object buffer pack handle stream buffer commit  # 7
    window rebase cache stream socket pack parser pack rebase commit header window  # 8
    branch header queue handle handle handle parser  # 9
    handle blob merge rebase  # 10
    tree object handle socket merge  # 11
    tree remote merge queue delta rebase cache  # 12
    buffer stream socket stream index blob object  # 13
    buffer cache merge branch remote window delta object queue  # 14
    parser branch header cache worker socket blob branch stream ref remote  # 15
    object tree header delta stream merge pack delta remote remote worker handle  # 16
    parser ref handle commit blob buffer pack commit  # 17
    handle index merge commit ref rebase buffer index  # 18
    parser ref token merge token queue token  # 19
    pack ref cache queue merge delta merge worker index delta header object  # 20
    remote remote tree stream pack pack pack merge pack commit  # 21
    rebase window commit merge  # 22
    blob parser blob rebase remote commit blob  # 23
    ref branch parser ref worker commit  # 24
    handle worker merge cache cache ref parser  # 25
    blob tree commit tree handle index token queue tree  # 26
    delta index header ref index socket object  # 27
    remote parser remote commit header window tree rebase buffer cache buffer  # 28
    parser commit ref pack rebase token pack ref worker commit  # 29
    socket index queue commit  # 30
    worker worker commit cache stream socket remote commit handle cache blob cache  # 31
    index handle header remote tree branch token queue header socket blob handle  # 32
    worker remote commit remote commit ref commit  # 33
    cache index blob blob merge branch parser  # 34
    delta merge stream ref parser pack rebase stream buffer cache buffer parser  # 35
    parser token object branch cache  # 36
    cache pack ref merge worker stream branch worker rebase ref  # 37
    window handle blob pack parser stream object worker merge queue worker  # 38
    socket branch token commit parser remote index header handle  # 39
    tree index handle branch socket branch pack  # 40
    cache tree header tree queue  # 41
    socket stream socket pack worker worker ref  # 42
    index worker parser commit commit tree index tree  # 43
    tree branch cache stream tree object  # 44
    object window blob ref queue ref rebase blob header delta remote  # 45
    pack header ref branch socket index socket object tree handle  # 46
    token rebase ref parser parser commit buffer pack blob token header delta  # 47
    commit window worker parser  # 48
    stream rebase index buffer stream  # 49
    pack parser queue worker tree header tree cache  # 50
    token queue socket buffer merge merge  # 51
    delta commit rebase tree cache buffer ref stream header object tree rebase  # 52
    stream queue index tree merge token cache  # 53
    window token remote queue ref buffer  # 54
    index parser ref pack object pack header object token  # 55
    cache rebase token parser blob worker header parser handle  # 56
    header tree worker socket window commit index stream handle  # 57
    pack stream merge ref  # 58
    queue rebase parser tree index index  # 59
    window handle buffer rebase stream  # 60
    blob tree socket token delta parser socket index pack  # 61
    worker socket branch pack merge ref worker merge queue blob branch  # 62
    stream index remote handle delta  # 63
    ref delta pack handle buffer stream token queue parser merge branch branch  # 64
    stream socket header cache delta handle index  # 65
    header socket pack worker socket socket parser remote cache blob header  # 66
    pack handle pack parser blob stream blob worker ref commit  # 67
    pack buffer token parser cache commit commit ref  # 68
    rebase parser cache cache pack  # 69
    handle header rebase remote tree stream worker socket commit header worker  # 70
    header token pack pack buffer merge tree buffer remote pack remote commit  # 71
    object cache window handle tree object socket blob tree tree branch  # 72
