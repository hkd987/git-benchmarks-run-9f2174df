    rebase handle window queue merge branch header handle  # 0
    token handle header window delta parser merge blob tree  # 1
    object stream socket delta  # 2
    cache cache header parser  # 3
    queue blob parser pack tree commit delta ref blob window rebase  # 4
    rebase window worker commit remote header socket  # 5
    parser index ref handle remote handle cache rebase  # 6
    parser ref pack remote worker rebase object buffer branch  # 7
    object header ref header token tree tree tree token stream delta window  # 8
    blob branch handle delta  # 9
    socket index handle pack blob pack  # 10
    blob buffer queue queue ref  # 11
    ref socket index rebase blob remote index pack delta blob object  # 12
    branch stream cache worker parser  # 13
    handle worker buffer pack merge object socket parser socket queue commit pack  # 14
    branch cache tree index window  # 15
    object blob cache object buffer window branch delta delta window commit  # 16
    pack merge buffer remote rebase index socket socket  # 17
    tree commit window pack blob commit ref token merge worker stream socket  # 18
    delta parser queue token remote queue worker cache object window tree handle  # 19
    socket delta worker pack handle worker ref merge  # 20
    pack delta cache socket delta cache buffer pack rebase token  # 21
    object remote commit token token delta header rebase rebase socket buffer handle  # 22
    remote window stream header delta header worker ref branch object tree  # 23
    worker merge blob header  # 24
    merge buffer worker queue  # 25
    cache merge queue delta index index  # 26
    parser socket handle pack delta  # 27
    cache commit handle object tree  # 28
    blob rebase cache rebase stream pack  # 29
    header delta merge cache header buffer tree branch index delta  # 30
    parser pack rebase window token branch buffer window  # 31
    parser object ref object tree index window queue merge  # 32
