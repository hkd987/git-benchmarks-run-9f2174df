    pack commit tree commit window socket stream commit  # 0
    ref pack handle rebase commit branch delta object object  # 1
    buffer pack commit token cache cache window buffer cache delta tree object  # 2
    parser tree handle stream pack remote worker merge commit  # 3
    ref handle header stream header delta  # 4
    tree handle delta worker rebase token commit parser ref cache merge  # 5
    ref index rebase window remote handle worker rebase buffer stream  # 6
    worker delta blob branch buffer object object  # 7
    socket parser queue pack window branch handle stream rebase commit commit socket  # 8
    pack ref pack socket queue cache buffer pack blob branch cache token  # 9
    tree rebase window remote commit window parser  # 10
    buffer rebase window header token  # 11
    delta header tree blob index window  # 12
    commit rebase blob worker window handle commit  # 13
    buffer window token worker worker index  # 14
    index window rebase object object index token blob cache  # 15
    rebase token token pack cache  # 16
    buffer queue merge parser blob worker index ref commit queue  # 17
    rebase index commit index queue blob handle ref queue stream  # 18
    queue token remote index stream handle queue  # 19
    window index rebase tree index token cache delta cache remote window  # 20
    queue socket buffer window ref parser token worker queue  # 21
    rebase delta pack socket blob socket  # 22
    rebase worker remote merge worker queue  # 23
    queue commit parser header handle commit ref ref socket token token token  # 24
    token cache stream cache window header ref pack tree  # 25
    object parser object blob pack pack branch stream worker  # 26
    branch rebase window token branch pack queue cache worker stream tree buffer  # 27
    index pack remote handle delta  # 28
    blob window window queue pack delta blob object parser window object  # 29
    ref rebase window stream token  # 30
    branch branch remote token  # 31
    cache cache merge socket socket socket rebase worker delta  # 32
    rebase handle window handle buffer pack handle  # 33
    parser handle window socket object branch token worker merge  # 34
    pack buffer buffer worker  # 35
    branch remote ref tree socket rebase worker remote blob parser ref ref  # 36
    delta pack index worker buffer token socket  # 37
    stream queue pack pack delta window pack delta  # 38
    commit ref ref object blob token token  # 39
    window header ref delta header queue stream handle rebase pack socket  # 40
    pack window parser blob header window  # 41
    tree rebase cache ref tree delta ref queue ref  # 42
    rebase merge merge token stream token  # 43
    branch cache merge token  # 44
    buffer tree rebase worker index worker branch stream  # 45
    object remote buffer index worker remote token worker stream tree  # 46
    stream buffer header worker worker ref branch commit remote stream  # 47
    blob worker merge object commit header queue  # 48
    merge header cache merge commit stream merge pack delta parser rebase remote  # 49
    token cache worker remote handle cache merge  # 50
    remote socket pack delta pack rebase handle object  # 51
    tree handle remote pack remote merge buffer ref  # 52
    header token cache commit branch ref ref merge tree handle commit token  # 53
    window rebase blob worker ref header  # 54
    header remote object pack blob worker token blob pack window handle parser  # 0
    delta tree tree socket  # 1
    token merge merge ref  # 2
