    pack window merge cache parser window queue handle token parser merge worker  # 0
    pack merge delta object blob worker queue token token  # 1
    branch token header header index token object window  # 2
    remote commit socket commit  # 3
    index merge stream socket  # 4
    window window branch window commit parser stream cache token commit cache cache  # 5
    pack socket window handle handle buffer delta stream header socket tree  # 6
    buffer pack delta object branch cache header  # 7
    commit buffer worker worker buffer worker branch  # 8
    socket ref window handle  # 9
    ref ref rebase tree header pack header  # 10
    window buffer branch worker  # 11
    header index branch socket merge queue handle handle  # 12
    tree stream handle blob cache ref handle commit socket handle remote cache  # 13
    rebase object token commit header  # 14
    worker cache window stream buffer cache merge queue  # 15
    header socket ref handle header buffer branch  # 16
    socket index rebase token index token  # 17
    token commit tree buffer socket parser parser cache parser buffer rebase worker  # 18
    blob socket commit blob handle  # 19
    blob delta tree remote pack commit parser stream merge blob  # 20
    buffer branch delta parser ref branch pack header pack stream  # 21
    header ref socket ref remote worker buffer commit  # 22
    window worker token tree tree header object stream index  # 23
    handle buffer merge commit header socket buffer token index queue buffer remote  # 24
    token header worker rebase blob tree blob parser remote  # 25
    stream worker stream pack queue merge blob blob queue parser  # 26
    remote queue object buffer  # 27
    tree ref blob header pack merge blob token tree socket  # 28
    token worker handle ref pack remote ref  # 29
    worker buffer merge worker  # 30
    token handle header buffer ref stream worker parser commit token object index  # 31
    queue branch token ref cache  # 32
    rebase pack window handle tree stream window index rebase  # 33
    blob queue blob worker merge blob  # 34
    branch pack object remote  # 35
    pack pack buffer cache  # 36
    blob stream cache ref rebase rebase stream  # 37
    token pack branch handle parser  # 38
    remote handle rebase branch window handle branch token token rebase object object  # 39
    branch worker delta ref rebase cache rebase remote branch stream header  # 40
    commit token parser window header object commit stream  # 41
    worker remote index branch tree remote  # 42
    cache window token stream delta branch window object header parser cache worker  # 43
    header header window socket tree stream window object object  # 44
    window token index handle object  # 45
    object delta tree commit stream branch stream  # 46
    tree merge handle rebase commit object delta stream handle  # 47
    ref index rebase socket window handle  # 48
    branch socket worker branch ref object commit handle buffer  # 49
    tree ref handle ref queue object worker socket remote delta socket stream  # 50
    remote remote handle branch worker window branch window parser socket  # 51
    buffer cache rebase handle handle queue index token tree socket header  # 52
    rebase parser object rebase rebase  # 53
    pack blob index object delta  # 54
    parser parser ref ref branch worker object pack cache index queue  # 55
    handle commit rebase rebase  # 56
    delta rebase parser window merge  # 57
    handle socket tree queue remote handle tree commit  # 58
    window remote index socket blob parser commit tree stream  # 59
    tree window buffer ref object token worker socket  # 60
    ref blob buffer index  # 61
    rebase stream blob ref cache merge  # 62
    branch tree worker index branch delta  # 63
    parser object rebase buffer tree ref pack remote remote  # 64
    handle commit delta index window ref stream parser object commit commit cache  # 65
    socket header parser rebase ref socket branch  # 66
    cache pack delta buffer merge  # 67
    object tree stream tree handle blob socket window merge ref stream rebase  # 68
    ref pack remote remote  # 69
    handle buffer remote merge stream object header stream parser  # 70
    ref queue queue branch index parser blob delta  # 71
    remote token buffer socket ref cache worker header  # 72
    branch worker queue branch window worker index rebase object branch stream  # 73
    blob stream blob index parser socket  # 74
    delta queue socket index header  # 75
    stream object ref object branch stream index queue merge parser  # 76
    delta commit handle remote object ref blob  # 77
    branch stream blob pack object rebase  # 78
    cache blob rebase index queue queue handle header tree  # 79
    worker remote remote stream branch  # 80
    handle socket pack header stream stream header  # 81
    stream header object index commit handle token socket header pack tree  # 82
    delta pack rebase buffer rebase remote object worker stream index object branch  # 83
