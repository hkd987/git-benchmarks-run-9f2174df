    ref ref handle socket window queue stream  # 0
    parser worker worker index socket ref stream cache socket blob branch header  # 1
    window socket cache stream queue handle object header buffer branch  # 2
    parser buffer parser queue branch socket blob blob  # 3
    header header branch cache handle merge  # 4
    object blob parser blob stream tree ref branch stream  # 5
    tree cache pack commit rebase parser tree  # 6
    queue object token window stream  # 7
    buffer object token index cache window merge header  # 8
    index handle header handle parser index socket object stream  # 9
    cache merge blob token token ref parser commit remote tree  # 10
    index cache queue object  # 11
    cache pack window header handle pack  # 12
    socket pack merge queue ref tree worker stream worker queue rebase buffer  # 13
    blob queue merge commit tree ref object  # 14
    tree cache worker cache ref pack  # 15
    pack parser stream parser pack parser socket ref  # 16
    queue index cache rebase header remote  # 17
    index pack socket delta worker rebase worker  # 18
    window socket token stream  # 19
    remote handle pack parser buffer rebase branch cache socket  # 20
    delta commit worker parser  # 21
    socket parser branch worker  # 22
    index merge tree queue pack worker delta parser buffer window  # 23
    delta index branch queue ref handle cache ref cache delta  # 24
    stream handle cache stream header parser pack merge delta  # 25
    object pack remote object  # 26
    handle queue stream queue remote window ref  # 27
    worker window cache tree buffer  # 28
    parser branch commit worker merge buffer commit  # 29
    object blob remote token token blob branch remote  # 30
    ref tree delta index delta socket stream branch ref worker ref  # 31
    blob ref remote branch worker window header tree  # 32
    rebase queue parser stream remote  # 33
    delta merge merge object worker rebase ref stream tree parser  # 34
    socket token remote window handle merge worker header tree merge rebase remote  # 35
    window token delta index socket commit branch rebase merge tree  # 36
    pack pack delta ref tree handle commit worker window token tree  # 37
    branch rebase handle socket remote token buffer  # 38
    branch window socket socket stream parser  # 39
    merge parser token parser object  # 40
    header commit pack queue merge ref  # 41
    pack index window tree pack blob stream commit  # 42
    pack merge handle pack worker window queue commit worker index tree  # 43
    header header stream ref branch object stream parser tree object index delta  # 44
    ref worker merge branch worker buffer merge merge  # 45
    header ref cache stream  # 46
    ref parser handle tree buffer index cache worker rebase token stream  # 47
    socket index worker socket stream delta window  # 48
    merge header buffer handle remote object object remote  # 49
    blob tree window queue header remote  # 50
    merge pack queue branch parser ref header index rebase tree  # 51
    branch blob buffer tree header index stream rebase tree ref buffer buffer  # 52
    blob worker handle merge window ref blob  # 53
    queue window tree remote pack parser pack header  # 54
    merge rebase remote window  # 55
    parser buffer handle handle header handle buffer branch cache cache remote remote  # 56
    cache index window cache stream  # 57
    cache branch remote socket  # 58
    delta window commit index window stream pack worker branch token token stream  # 59
    object pack token window remote tree queue socket handle tree handle header  # 60
    window buffer token socket window ref blob  # 61
    remote blob blob tree  # 62
    commit worker token commit token window  # 63
    token buffer header queue token cache queue window  # 64
    object rebase rebase blob tree stream pack tree header queue branch index  # 65
    worker branch stream parser handle remote stream stream merge token stream  # 66
    rebase ref stream branch  # 67
    index buffer token window  # 68
    pack token merge object socket remote stream buffer index header  # 69
    stream window pack rebase index delta index merge buffer  # 70
    header object remote ref stream header tree cache merge  # 71
    queue buffer socket commit window ref branch socket rebase cache remote  # 72
    index window pack tree token branch token index header window window index  # 73
    object buffer pack commit  # 74
    window branch tree queue buffer cache  # 75
    merge tree stream cache  # 76
    remote stream merge blob ref  # 77
    window buffer parser pack pack merge handle worker parser handle  # 78
    index socket window remote parser stream object cache  # 79
    window queue worker socket parser header handle merge queue object cache cache  # 80
    queue delta commit parser delta token remote branch handle branch branch stream  # 81
    socket socket pack header queue tree window index ref parser  # 82
    rebase worker cache rebase delta  # 83
    branch socket handle branch  # 84
    window commit index index  # 85
    buffer blob tree header merge queue object  # 86
    window delta pack cache cache pack buffer  # 87
    blob worker tree object delta token  # 88
    header queue ref pack branch token delta pack buffer stream  # 89
    stream buffer merge branch merge tree header cache object commit tree window  # 0
    delta socket parser socket branch token  # 1
    handle header stream worker cache branch stream buffer index header  # 2
    stream worker merge tree parser parser window header branch merge remote token  # 3
    delta delta delta rebase merge branch  # 4
    token parser commit stream ref token token merge index commit  # 5
    object parser queue delta  # 6
    socket queue buffer socket parser cache commit  # 7
    remote buffer ref merge handle queue pack  # 8
    window socket commit pack index pack socket parser pack merge queue  # 9
    rebase header token commit stream stream index  # 10
    worker index merge branch rebase  # 0
    commit branch parser parser tree  # 1
    blob remote branch branch tree index ref window stream object token blob  # 2
    delta branch window cache commit remote socket delta index  # 3
    cache window cache window delta ref handle handle  # 4
    worker cache rebase window parser header remote queue  # 5
