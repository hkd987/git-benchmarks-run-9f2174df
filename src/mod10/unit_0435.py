    blob remote cache rebase index remote  # 0
    remote commit handle cache  # 1
    remote window commit remote pack stream remote stream  # 2
    stream token queue branch index socket token window branch socket header  # 3
    ref index worker branch object handle cache rebase  # 4
    socket queue remote blob queue socket ref commit  # 5
    branch branch window socket blob delta buffer rebase  # 6
    rebase commit remote socket branch parser index delta index blob  # 7
    ref index cache object remote stream handle commit rebase  # 8
    pack ref object parser commit stream socket parser token tree token socket  # 9
    window rebase object branch queue pack  # 10
    window ref handle handle stream ref branch pack cache  # 11
    index blob token commit window  # 12
    merge socket pack rebase remote tree  # 13
    delta commit buffer socket  # 14
    branch blob commit window branch handle commit  # 15
    header delta parser merge remote  # 16
    delta window socket branch  # 17
    rebase branch blob buffer stream  # 18
    blob handle socket blob header remote pack window index socket stream  # 19
    window delta token stream ref pack delta cache object remote pack  # 20
    socket queue object stream object window header blob tree worker  # 21
    buffer cache header pack delta index pack stream ref  # 22
    handle merge cache commit ref token  # 23
    window pack branch index socket cache header blob token window worker  # 24
    handle ref remote window remote index object buffer index window  # 25
    header delta handle remote merge socket cache handle branch remote commit  # 26
    buffer parser buffer buffer cache handle branch object index handle  # 27
    worker parser tree object ref object  # 28
    commit header window merge token object  # 29
    handle cache remote ref merge branch cache header cache pack  # 30
    stream object blob stream cache remote  # 31
    parser stream object merge rebase delta window header rebase  # 32
    token cache commit stream  # 33
    rebase index socket stream cache tree remote blob  # 34
    commit ref blob delta stream queue header merge buffer buffer merge  # 35
    parser ref worker pack index commit rebase window object queue  # 36
    header rebase socket blob branch pack object  # 37
    remote delta tree token worker  # 38
    ref stream window ref stream cache branch merge stream tree pack  # 39
    blob remote pack pack index branch commit buffer ref handle socket index  # 0
    commit parser delta token cache index buffer delta index delta token  # 1
    pack socket tree remote header worker token blob stream  # 2
    ref cache token merge token buffer tree rebase  # 3
    window remote stream worker socket  # 4
    handle tree header parser cache queue branch blob cache tree socket object  # 5
    pack queue pack socket object pack blob  # 6
