    buffer object cache index pack index pack parser pack delta object  # 0
    stream token merge branch buffer token tree object rebase branch  # 1
    branch queue window delta window token pack delta header  # 2
    tree branch rebase window  # 3
    header branch merge header merge  # 4
    branch header index socket handle remote delta  # 5
    handle header queue blob cache stream remote merge remote tree stream  # 6
    handle index object buffer parser token  # 7
    queue blob socket branch buffer  # 8
    commit remote rebase index window index  # 9
    tree queue blob worker header socket window queue branch  # 10
    object stream object worker  # 11
    handle remote handle remote queue remote object remote  # 12
    socket queue branch worker cache socket remote handle header branch  # 13
    remote socket buffer delta socket handle worker ref tree commit branch  # 14
    handle rebase stream pack  # 15
    rebase ref cache branch branch worker header window  # 16
    object blob blob ref token ref merge rebase token  # 17
    handle object merge parser ref rebase header tree window index worker  # 18
    buffer index commit window index parser socket header merge cache header  # 19
    blob object remote object buffer remote  # 20
    socket token worker merge stream branch buffer object parser pack  # 21
    remote object socket commit merge  # 22
    rebase stream header window delta cache token  # 23
    ref buffer rebase window  # 24
    cache window worker stream worker handle buffer merge header commit socket  # 25
    window buffer tree header buffer object delta socket index  # 26
    handle token buffer blob ref branch blob cache ref parser token  # 27
    object merge index blob tree worker pack buffer object rebase  # 28
    delta remote ref parser header blob stream  # 29
    blob buffer index remote  # 30
    stream parser socket remote queue rebase cache worker token tree cache  # 31
    token buffer socket remote branch delta buffer ref parser  # 32
    socket parser parser branch socket  # 33
    ref remote pack handle pack window remote commit ref ref commit ref  # 34
    remote handle handle index object ref  # 35
    parser worker queue token cache header pack pack worker  # 36
    cache handle handle delta window remote blob  # 37
    index token queue socket socket ref queue header ref buffer parser  # 38
    queue branch cache header token token window tree worker  # 39
    remote index rebase socket merge remote pack branch  # 40
    object worker delta rebase handle  # 41
    pack tree cache blob ref window queue window token stream ref token  # 42
    ref handle delta queue buffer socket branch  # 43
    blob worker commit rebase buffer  # 44
    cache delta cache rebase rebase  # 45
    merge branch socket header commit  # 46
    window parser rebase blob  # 47
    parser pack worker handle  # 48
    remote commit tree remote window delta  # 49
    window header worker rebase index header queue remote blob header  # 50
    ref stream queue pack  # 51
    blob token merge socket index window  # 52
    ref window header rebase  # 53
    tree worker token worker queue header token merge  # 54
    merge cache token stream pack blob branch commit blob header handle  # 55
    delta stream rebase token  # 56
    remote object worker header buffer stream buffer ref  # 57
    rebase ref merge remote rebase ref object object  # 58
    queue rebase branch handle header delta socket remote cache  # 59
    delta index buffer tree  # 60
    index buffer blob tree  # 61
    object blob ref branch object token buffer handle delta commit  # 62
