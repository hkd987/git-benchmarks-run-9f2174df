    delta blob index object blob parser parser  # 0
    merge cache window index queue blob  # 1
    token remote token stream delta ref merge object  # 2
    handle pack blob worker  # 3
    remote object parser worker  # 4
    object worker ref delta parser branch pack branch parser worker index  # 5
    parser cache cache object  # 6
    delta ref handle handle blob stream blob ref commit object queue header  # 7
    object buffer window worker window branch window window window header handle  # 8
    stream socket merge tree handle rebase queue delta  # 9
    blob branch window header tree remote  # 10
    branch ref socket branch rebase ref socket queue parser stream commit  # 11
    pack tree buffer worker  # 12
    pack token remote ref delta tree  # 13
    window tree stream rebase pack queue pack branch object  # 14
    ref handle token buffer pack rebase queue stream socket  # 15
    handle tree rebase index  # 16
    cache socket tree handle index header buffer header parser ref  # 17
    stream commit index commit  # 18
    delta buffer parser pack commit worker queue handle window remote  # 19
    parser blob socket ref window index delta commit buffer object  # 20
    object header ref rebase commit rebase handle buffer token stream  # 21
    tree rebase socket socket  # 22
    ref token rebase worker branch object branch header object merge  # 23
    pack parser branch merge object ref rebase ref socket  # 24
    token pack merge pack buffer worker delta worker header header merge parser  # 25
    cache commit rebase parser parser  # 26
    queue commit tree worker handle header parser merge ref pack worker window  # 27
    remote pack delta branch buffer ref parser delta  # 28
    ref blob parser queue handle blob delta tree commit  # 29
    delta token worker branch stream token parser  # 30
    pack header window parser index index delta cache buffer socket  # 31
    branch merge rebase buffer remote ref  # 32
    tree commit merge remote index token branch branch  # 33
    commit parser pack handle  # 34
    parser remote remote blob merge buffer parser queue  # 35
    ref ref delta socket index stream index  # 36
    stream window token pack remote parser  # 37
    merge handle worker stream socket pack  # 38
    header index remote worker stream pack cache handle parser handle cache  # 39
    buffer delta socket object cache remote stream pack queue  # 40
    rebase rebase buffer blob buffer cache  # 41
    blob tree cache tree handle remote index  # 42
    branch worker commit socket queue delta ref handle  # 43
    handle object delta delta index object header worker queue  # 44
    window socket blob buffer rebase header queue stream handle merge  # 45
    merge header merge object remote object commit index  # 46
    remote commit parser tree remote  # 47
    handle object queue header parser buffer ref  # 48
    ref worker stream remote branch  # 49
    commit socket commit remote tree merge ref  # 50
    ref index object cache  # 51
    cache object branch blob rebase index cache object ref merge  # 52
    branch pack remote commit queue pack token rebase rebase window  # 53
    worker handle header commit token  # 54
    commit blob stream buffer socket index window parser queue blob queue  # 55
    queue merge branch worker blob parser  # 56
    socket pack tree cache stream ref rebase handle stream ref token  # 57
    cache handle worker ref object rebase parser  # 58
    worker ref pack index  # 59
