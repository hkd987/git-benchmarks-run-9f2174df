    commit queue buffer cache pack remote socket remote object token blob  # 0
    remote token stream socket blob worker header queue buffer rebase  # 1
    handle branch buffer tree stream blob header handle object  # 2
    blob token blob tree  # 3
    socket index header queue handle branch branch handle  # 4
    merge worker ref stream tree token token cache  # 5
    token merge cache stream worker delta rebase stream parser  # 6
    remote rebase delta worker socket commit queue delta parser stream object branch  # 7
    buffer buffer commit index pack  # 8
    blob queue merge commit token pack stream delta  # 9
    tree window stream cache buffer delta  # 10
    worker blob header queue  # 11
    commit worker stream delta remote pack queue queue  # 12
    worker token remote pack ref tree stream parser index header commit  # 13
    commit socket header socket worker pack socket  # 14
    merge worker delta stream rebase token tree remote  # 15
    blob blob index merge stream header worker  # 16
    stream worker branch branch blob cache commit blob socket worker  # 17
    window token remote socket  # 18
    index header commit stream  # 19
    window buffer blob merge  # 20
    ref handle handle cache cache ref branch rebase worker tree queue  # 21
    cache cache tree rebase parser cache ref pack commit  # 22
    window delta merge header worker blob  # 23
    socket buffer tree index tree header merge cache tree token  # 24
    pack index parser rebase blob merge  # 25
    tree stream parser blob index queue ref stream stream  # 26
    stream index header header tree token rebase stream window object delta object  # 27
    tree token commit queue cache socket cache index rebase object  # 28
    parser buffer object remote remote blob commit merge  # 29
    merge remote rebase parser pack object parser rebase socket branch index token  # 30
    stream header branch merge queue window object buffer  # 31
    queue buffer stream delta queue commit merge socket tree worker  # 32
    token queue window handle commit object token parser header header object  # 0
    window index ref blob buffer ref pack  # 1
    delta ref queue commit commit worker ref object  # 2
    rebase queue worker merge rebase index branch ref socket parser  # 3
    commit token index remote token pack ref window index token pack token  # 4
    queue branch ref remote parser window ref cache parser tree remote  # 5
    tree remote window remote  # 6
    pack window pack ref rebase blob worker remote worker  # 7
    merge stream index window blob parser branch stream delta buffer pack  # 8
