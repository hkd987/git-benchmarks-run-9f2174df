    window worker tree token tree delta  # 0
    header tree worker object buffer commit object cache ref cache  # 1
    cache delta ref pack queue token index window  # 2
    delta window commit cache delta ref handle  # 3
    delta delta delta index merge object queue  # 4
    parser cache pack rebase cache blob  # 5
    header merge pack rebase queue object header stream merge pack  # 6
    ref commit token merge index  # 7
    worker handle cache parser pack window remote  # 8
    queue handle pack rebase merge commit rebase remote handle  # 9
    token tree commit window pack  # 10
    socket commit commit parser tree  # 11
    delta header stream worker tree merge  # 12
    header window pack delta handle worker  # 13
    window cache stream token header parser index token object  # 14
    handle pack pack ref cache header cache merge handle object  # 15
    window merge token branch stream object delta remote ref  # 16
    cache rebase tree delta handle queue  # 17
    branch rebase worker branch blob window  # 18
    branch branch ref parser window stream socket buffer parser  # 19
    socket object pack delta tree pack socket pack pack  # 20
    blob ref blob socket rebase parser branch handle stream handle  # 21
    header header branch remote header object queue pack token blob  # 22
    delta rebase commit queue window commit pack stream ref pack remote handle  # 23
    ref merge rebase window index socket cache header  # 24
    queue window commit ref parser stream window blob worker object delta  # 25
    rebase stream remote branch window worker ref commit rebase  # 26
    queue commit ref buffer delta rebase object  # 27
    tree socket socket remote object ref blob remote stream token delta  # 28
    window tree commit stream handle ref object rebase tree commit window worker  # 29
    socket pack token delta queue socket handle  # 30
    remote merge rebase token delta  # 31
    header commit handle cache ref buffer merge commit  # 32
    header worker pack blob cache header header parser header tree  # 33
    parser rebase remote branch blob branch socket  # 34
    branch stream window blob  # 35
    header rebase object remote header  # 36
    queue branch buffer cache socket rebase object worker  # 37
    handle ref object index index delta merge buffer tree branch  # 38
    stream merge parser parser window ref ref remote commit  # 39
    blob commit queue commit rebase window stream pack buffer ref parser  # 40
    pack token rebase token header rebase buffer rebase pack merge commit  # 41
    index window blob commit window cache token queue remote index commit  # 42
    handle socket header index  # 43
    window remote remote handle handle object ref buffer cache tree commit  # 44
    merge token handle commit index queue commit  # 45
    stream cache ref handle merge merge token  # 46
    commit remote token blob tree buffer merge  # 47
    commit branch window handle pack branch  # 48
