    commit parser pack stream buffer tree queue  # 0
    cache parser handle parser tree token object  # 1
    parser header commit cache queue blob delta pack rebase  # 2
    remote merge handle socket object queue  # 3
    branch pack cache cache tree  # 4
    ref handle blob pack commit worker  # 5
    merge queue handle remote branch cache merge  # 6
    tree blob blob index token token blob worker  # 7
    pack stream socket pack worker  # 8
    cache cache pack branch token  # 9
    token delta blob token header tree parser ref worker commit  # 10
    branch socket ref buffer header commit rebase delta  # 11
    blob tree header commit ref rebase handle  # 12
    blob buffer index handle branch buffer pack  # 13
    buffer socket blob delta delta cache rebase buffer header  # 14
    blob handle header index cache branch worker cache header parser window blob  # 15
    pack worker socket parser index pack tree merge remote parser  # 16
    buffer cache buffer object worker  # 17
    window ref token queue delta branch object  # 18
    rebase header worker rebase merge remote window  # 19
    pack socket socket socket remote handle object worker merge commit  # 20
    remote commit token delta parser pack socket delta tree merge pack  # 21
    window blob branch remote blob handle object socket window ref  # 22
    commit merge ref queue stream tree branch object  # 23
    delta worker ref handle branch delta  # 24
    pack queue cache index  # 25
    parser rebase stream token worker branch tree  # 26
    remote pack worker window rebase window buffer index window merge socket tree  # 27
    queue commit index remote blob commit token tree buffer rebase ref stream  # 28
    index token branch branch merge delta object stream delta queue window tree  # 29
    object parser handle rebase tree buffer blob  # 30
    worker worker index socket socket  # 31
    handle header delta header parser merge  # 32
    token delta worker ref worker remote index window buffer  # 33
    worker parser remote parser merge ref stream queue merge index socket  # 34
    cache remote commit socket queue handle object blob worker buffer  # 35
    parser pack token parser pack cache window object header  # 36
    commit blob blob index cache token  # 37
    branch handle delta handle worker worker delta header  # 38
    commit window socket commit handle  # 39
    commit cache blob header pack object buffer delta  # 40
    cache queue remote handle delta buffer token header branch pack  # 41
    object window pack commit queue stream remote rebase index  # 42
    object index index object rebase blob tree socket queue token handle parser  # 43
    stream header header merge socket merge handle remote header buffer buffer index  # 44
    rebase token commit queue window  # 45
    stream buffer token stream socket cache object queue  # 46
    delta handle index worker stream index token parser  # 47
    remote pack token commit header branch worker  # 48
    rebase delta remote branch object ref socket blob queue  # 49
    worker ref stream remote worker queue  # 50
    rebase buffer object rebase parser queue ref tree object cache object  # 0
    header blob pack index branch  # 1
    merge index ref header stream object handle stream ref merge  # 2
    rebase worker tree merge worker tree rebase  # 3
    header stream branch remote  # 4
    delta branch buffer parser token pack branch  # 5
    delta ref worker parser handle token window commit pack  # 6
    token worker token commit cache handle queue rebase  # 7
    merge blob delta stream blob remote token cache  # 8
    merge cache pack object worker delta cache window worker handle  # 9
    cache commit socket worker window token queue worker token queue cache buffer  # 10
    pack merge header socket handle window merge blob worker delta  # 0
    merge cache parser pack handle rebase commit header header cache index handle  # 1
    blob ref worker tree worker merge tree handle ref ref tree token  # 2
