    cache branch branch stream worker branch  # 0
    commit object parser branch socket token cache stream tree  # 1
    header handle token index queue stream header stream pack delta cache  # 2
    header delta merge ref commit  # 3
    pack parser index handle remote stream tree token pack handle  # 4
    delta handle cache blob commit blob remote remote rebase header buffer buffer  # 5
    socket remote object window stream blob header window commit worker rebase  # 6
    cache token buffer cache handle pack remote merge handle header token  # 7
    pack handle ref token remote object token branch worker index object remote  # 8
    branch index remote ref queue commit window branch parser remote merge  # 9
    ref commit rebase remote rebase buffer commit parser  # 10
    merge window token cache object  # 11
    branch queue token handle header remote  # 12
    window ref window rebase  # 13
    index rebase pack rebase buffer merge blob  # 14
    parser pack tree worker object stream parser blob delta branch  # 15
    queue delta tree rebase ref  # 16
    commit ref cache worker token  # 17
    pack commit worker buffer worker delta index socket remote index commit ref  # 18
    stream queue rebase branch window  # 19
    object ref commit rebase object  # 20
    index remote token parser stream rebase tree remote token blob socket  # 21
    cache remote delta rebase ref object ref  # 22
    delta branch socket branch object object commit worker  # 23
    worker commit stream buffer delta buffer commit rebase ref tree socket queue  # 24
    token socket pack queue commit parser stream socket header  # 25
    rebase socket pack queue worker  # 26
    ref token cache blob stream index  # 27
    buffer worker index blob queue  # 28
    blob remote commit token header header remote blob  # 29
    header token branch branch cache tree object cache  # 30
    tree buffer branch token handle  # 31
    rebase pack blob rebase queue rebase delta queue handle queue  # 32
    blob token stream token cache stream handle token cache socket  # 33
    socket stream branch buffer remote  # 34
    ref merge branch parser token token  # 35
    queue merge rebase rebase object stream  # 36
    header parser window cache worker window header worker  # 37
    delta handle cache delta index branch buffer branch object socket merge  # 38
    header tree pack remote merge queue commit queue delta pack delta ref  # 39
    buffer cache rebase worker socket parser rebase delta cache blob header ref  # 40
    header parser window rebase commit  # 41
    merge handle rebase worker queue socket handle remote tree queue parser  # 42
    token index stream worker blob queue pack  # 43
    cache parser worker rebase index ref cache commit  # 44
    handle index remote socket worker worker pack pack remote queue remote  # 45
    token header remote pack  # 46
    parser socket index socket ref  # 47
    merge cache merge handle stream socket blob index header  # 48
    index remote window header stream index ref remote stream token window handle  # 49
    pack cache ref buffer buffer cache branch buffer handle window object tree  # 50
    worker cache rebase cache  # 51
    buffer blob buffer blob  # 52
    remote stream blob delta header blob tree socket pack worker worker  # 53
    object index pack header window branch parser worker ref  # 54
    remote blob window queue object pack parser handle blob worker pack token  # 55
    token queue token blob ref tree worker  # 56
    delta branch socket socket pack blob rebase delta worker  # 57
    buffer index stream queue blob tree parser stream stream worker rebase  # 58
    tree blob remote window pack header rebase  # 59
    window index window ref  # 60
    rebase rebase worker object buffer  # 61
    window parser header parser remote ref commit branch branch blob commit blob  # 62
    tree cache index pack stream delta remote index queue pack  # 63
    tree buffer window header stream header cache  # 64
    blob remote commit branch delta token queue ref ref parser  # 65
    commit worker tree queue queue ref  # 66
    merge handle index token merge commit index  # 67
    blob handle handle object commit socket tree parser object delta header buffer  # 68
    socket delta queue branch  # 69
    token buffer window parser window branch pack stream rebase header cache blob  # 70
    commit queue remote stream  # 71
    cache socket blob pack queue worker delta  # 72
    cache object pack parser stream remote object commit socket tree index  # 73
    parser commit tree object ref window handle  # 74
    socket parser worker window queue pack queue worker handle cache header  # 75
    index worker blob remote merge pack queue window blob window stream pack  # 76
    cache tree merge commit header token tree object queue  # 77
    blob parser rebase worker object  # 78
    commit blob merge remote buffer socket delta handle parser socket blob  # 79
    pack pack blob tree rebase object token worker ref  # 80
    handle pack commit handle parser merge window tree  # 81
    tree handle branch ref remote socket branch blob  # 82
    branch tree merge window queue queue pack branch object  # 83
    window remote blob merge handle handle queue rebase socket tree blob tree  # 84
    parser merge object ref blob rebase  # 85
    remote tree tree handle ref token  # 86
    worker tree queue header ref queue index  # 0
    blob tree index blob  # 1
    handle window worker remote token merge parser object object pack delta  # 2
    handle blob pack handle object pack window  # 3
    parser token worker socket pack cache tree pack object  # 4
    queue cache window delta token parser commit merge pack worker index branch  # 5
    cache header commit token tree  # 6
    queue buffer buffer object token  # 7
    queue handle commit parser pack ref token  # 8
    parser handle parser socket merge  # 9
    cache pack branch merge object header  # 10
    socket window worker handle parser  # 11
    rebase socket branch buffer branch  # 12
    stream header worker socket delta rebase commit ref worker  # 13
    parser object branch delta branch worker tree tree  # 14
