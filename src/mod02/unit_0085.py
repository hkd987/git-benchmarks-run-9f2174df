    merge merge commit worker rebase delta delta remote merge  # 0
    remote pack queue stream header header window remote parser  # 1
    queue stream merge commit cache worker commit header  # 2
    delta commit window blob commit ref  # 3
    stream window commit stream cache index merge tree delta socket blob buffer  # 4
    remote pack commit window handle header  # 5
    blob header commit token tree pack handle commit remote pack stream  # 6
    worker rebase merge delta object commit buffer  # 7
    buffer remote stream queue header rebase buffer pack rebase  # 8
    worker branch parser parser socket queue  # 9
    cache stream buffer commit tree  # 10
    ref window index branch index branch ref cache  # 11
    remote tree commit remote blob handle rebase object parser remote tree handle  # 12
    handle commit queue blob  # 13
    cache tree pack commit token queue rebase index cache tree ref handle  # 14
    object parser cache header index handle parser  # 15
    worker branch ref pack  # 16
    worker object index socket  # 17
    stream pack remote remote rebase commit stream object token worker delta ref  # 18
    index buffer rebase remote merge pack branch  # 19
    commit parser worker blob rebase merge blob ref stream merge stream  # 20
    ref rebase parser handle index branch  # 21
    token commit window parser worker  # 22
    tree window delta merge  # 23
    ref branch branch socket delta token pack ref commit  # 24
    branch socket tree socket ref cache stream stream token stream blob  # 25
    tree window remote queue rebase branch merge  # 26
    queue cache handle blob cache window queue branch token worker delta  # 27
    parser object delta header window index stream  # 28
    header token handle token ref parser commit  # 29
    remote ref merge rebase tree ref parser blob token  # 30
    buffer handle worker parser stream handle token object window header  # 31
    rebase buffer rebase parser parser  # 32
    remote rebase remote commit header tree buffer pack cache token branch rebase  # 33
    parser delta cache cache tree tree cache queue commit  # 34
    blob token worker ref ref buffer  # 35
    queue header pack index commit  # 36
    queue commit tree branch queue token header index index  # 37
    blob branch socket parser buffer remote commit blob index blob header stream  # 38
    cache delta blob cache queue queue  # 39
    delta handle stream remote socket object  # 40
    stream index header ref object object delta pack branch parser merge  # 41
    pack parser buffer pack  # 42
    stream blob commit socket handle  # 43
    remote pack cache worker delta ref header blob rebase cache commit  # 44
    branch queue worker token worker  # 45
    commit tree queue socket ref  # 46
    worker socket cache remote pack delta ref handle  # 47
    handle blob buffer blob socket pack commit socket rebase stream  # 48
    parser pack buffer token branch branch queue queue blob  # 49
    delta socket stream header handle blob  # 50
    window queue pack index buffer commit  # 51
    cache index delta commit stream queue branch queue ref remote window rebase  # 52
    object cache ref blob buffer  # 53
    cache worker token header tree  # 54
    cache rebase tree branch queue blob pack header  # 55
    delta pack buffer tree stream queue commit pack pack index parser token  # 56
    stream socket delta cache  # 57
    commit pack remote parser ref commit cache blob queue  # 58
    token pack parser buffer  # 59
    stream socket blob blob  # 60
    blob socket branch pack commit token parser tree commit parser queue  # 61
    commit delta queue index merge pack rebase handle queue commit rebase buffer  # 62
    branch index object pack parser tree token ref commit  # 63
    handle remote queue window worker  # 64
    pack object pack socket buffer remote window  # 65
    worker blob branch delta ref object remote  # 66
    tree branch stream index blob merge header token  # 67
    cache token tree window buffer remote remote tree delta remote blob buffer  # 68
    commit handle stream queue branch cache buffer  # 69
    remote parser socket handle token worker parser merge rebase  # 70
    branch branch object cache  # 71
    merge index tree handle  # 72
    socket merge worker token handle queue buffer worker rebase pack window delta  # 73
    handle blob commit token buffer stream ref queue stream token  # 74
    delta delta index ref queue  # 75
    handle buffer commit cache buffer  # 76
