    header object branch queue merge header  # 0
    delta stream commit rebase window handle delta header rebase handle  # 1
    queue handle pack buffer  # 2
    rebase worker object parser buffer  # 3
    pack object cache worker object header rebase window socket pack worker  # 4
    pack remote object handle index token delta  # 5
    buffer rebase ref index window index delta ref worker token buffer  # 6
    blob header tree queue worker worker tree merge header blob commit commit  # 7
    queue socket queue worker branch tree commit  # 8
    socket socket handle queue delta queue delta rebase object buffer parser commit  # 9
    socket header socket remote header handle blob merge  # 10
    cache branch window worker pack index rebase cache cache delta  # 11
    stream tree parser branch worker buffer ref rebase socket stream pack  # 12
    pack merge delta delta remote commit commit token pack stream index  # 13
    commit object cache window  # 14
    socket handle commit remote queue parser ref handle stream commit  # 15
    socket window token handle branch delta token blob cache branch queue  # 16
    header worker ref worker  # 17
    object stream rebase commit delta handle blob stream cache buffer  # 18
    object remote object buffer  # 19
    blob worker merge remote socket worker worker index rebase token pack  # 20
    token socket tree token worker rebase rebase tree handle merge handle  # 21
    window blob socket tree queue token commit ref parser buffer commit  # 22
    ref delta object index buffer remote rebase rebase handle  # 23
    token rebase index object merge stream remote merge  # 24
    window header token handle  # 25
    stream tree worker tree header branch object object parser blob stream window  # 26
    window buffer rebase branch header  # 27
    delta object merge merge parser index cache handle  # 28
    window delta token rebase  # 29
    header socket merge tree branch socket pack stream blob ref stream commit  # 30
    queue token pack merge queue delta buffer worker index branch worker  # 31
    ref merge token ref commit  # 32
    stream index socket parser branch merge object cache socket socket parser header  # 33
    rebase rebase tree ref queue branch blob  # 34
    queue queue parser handle parser socket delta token cache  # 35
    merge buffer parser index queue tree branch merge merge remote rebase  # 36
    merge delta object branch rebase branch cache delta object index  # 37
    branch handle cache window tree object commit queue rebase delta tree merge  # 38
    stream parser window cache stream stream pack handle blob header handle  # 39
    delta stream cache queue branch delta rebase remote buffer object commit tree  # 40
    pack tree socket tree delta stream queue  # 41
    commit handle stream rebase window commit window remote blob  # 42
    header stream ref commit queue queue merge socket queue pack  # 43
    parser branch rebase merge cache  # 44
    window handle socket cache handle blob window handle queue tree  # 45
    pack header tree queue object window stream  # 46
    stream socket merge cache window tree  # 47
    queue handle commit tree parser buffer  # 48
    ref cache blob handle  # 49
    commit delta delta index remote merge blob blob stream remote tree  # 50
    index index branch branch buffer header handle index buffer branch  # 51
    handle tree merge cache socket tree worker merge merge object  # 52
    rebase handle queue cache worker merge  # 53
    parser header parser rebase parser rebase pack object  # 54
    remote commit remote queue blob handle  # 55
    stream delta branch socket index pack object commit queue buffer  # 56
    socket handle index remote blob branch token handle handle socket delta  # 57
    index header header pack stream  # 58
    merge tree token remote queue handle queue pack rebase merge window  # 59
    worker queue tree ref token buffer window  # 60
    stream handle commit ref queue header token window queue remote merge  # 61
    socket token tree delta rebase  # 62
    parser merge pack worker window pack object socket token worker socket socket  # 63
    window delta blob branch tree handle ref cache delta  # 64
    header blob object object ref  # 65
    remote token branch pack  # 66
    buffer buffer commit object blob ref rebase header branch pack tree worker  # 67
    buffer socket ref commit index queue commit ref  # 68
    socket cache remote worker delta blob  # 69
    window branch buffer blob commit worker ref ref  # 70
    header commit handle object ref rebase  # 71
    index header cache window queue branch blob stream  # 72
    ref rebase commit ref handle delta  # 73
    window branch cache pack window worker object socket socket buffer cache stream  # 74
    ref remote object blob  # 0
    merge stream window stream window delta  # 1
    worker parser worker ref header  # 2
    worker object header parser window merge commit blob remote  # 3
    branch parser commit worker tree blob  # 4
    parser commit handle remote stream merge pack tree commit ref handle pack  # 5
    tree pack header delta socket object commit cache buffer token  # 6
    cache header rebase delta stream ref delta parser buffer parser socket branch  # 7
    stream object pack rebase delta cache socket worker  # 8
    window ref blob rebase handle ref cache cache stream blob commit worker  # 9
