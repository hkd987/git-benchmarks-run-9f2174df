    remote object delta cache buffer handle worker  # 0
    ref window queue branch remote tree remote branch worker socket handle parser  # 1
    socket object queue header cache cache remote buffer remote tree object  # 2
    remote window tree buffer handle header token  # 3
    commit queue delta branch cache  # 4
    socket worker buffer token stream window stream delta header  # 5
    delta handle socket index delta object queue window blob window queue  # 6
    rebase remote stream branch token parser branch token delta tree delta object  # 7
    worker branch header tree window pack commit socket tree buffer  # 8
    remote branch parser rebase delta index pack socket window merge socket pack  # 9
    blob delta merge handle parser  # 10
    delta cache object tree index stream delta index  # 11
    object index rebase buffer worker cache object merge merge pack token  # 12
    index worker stream parser object rebase parser header stream  # 13
    stream merge branch pack queue commit rebase parser  # 14
    window header rebase object delta token object header index object  # 15
    parser handle handle handle blob  # 16
    branch pack delta parser remote delta commit branch socket worker rebase header  # 17
    ref handle parser header rebase index worker commit handle merge stream  # 18
    branch stream commit index rebase pack  # 19
    handle merge commit token commit ref merge header parser  # 20
    rebase buffer merge index header merge  # 21
    blob socket delta buffer tree index  # 22
    remote parser delta tree remote pack token object ref object cache rebase  # 23
    merge worker object header index tree index  # 24
    pack queue token queue blob socket delta header object remote  # 25
    cache ref remote header remote pack handle ref blob index cache  # 26
    tree queue rebase pack window  # 27
    socket cache header pack cache worker index socket rebase commit  # 28
    index blob object queue stream  # 29
    object tree remote delta branch window  # 30
    queue ref delta cache rebase header buffer worker object ref window header  # 31
    rebase blob index delta header socket socket buffer handle worker  # 32
    token window ref ref tree stream blob tree merge  # 33
    remote handle worker remote ref socket index socket header delta  # 34
    socket worker header index ref ref commit pack object socket merge blob  # 35
    handle worker tree branch branch  # 36
    merge worker queue object window window queue parser commit ref merge  # 37
    merge stream object blob  # 38
    ref parser cache cache stream socket tree worker  # 39
    remote delta cache socket  # 40
    buffer rebase tree index branch object parser stream cache index  # 41
    pack blob window token commit buffer token stream  # 42
    queue parser blob token pack queue header rebase header  # 43
    window queue object tree queue branch commit object buffer ref  # 44
    blob commit branch blob window worker commit  # 45
    parser stream branch object rebase queue worker branch blob queue cache  # 46
    commit pack handle worker remote handle header  # 47
    parser merge queue stream index socket index tree merge buffer  # 48
    merge buffer blob rebase cache parser rebase socket  # 49
    blob blob merge buffer remote ref queue index  # 50
    rebase socket pack remote rebase buffer cache index ref rebase token blob  # 51
    token index rebase pack buffer queue header stream worker stream worker  # 52
    token delta pack pack header rebase socket token pack stream window  # 53
    token pack token socket pack ref handle merge tree parser  # 54
    merge object buffer rebase tree cache buffer buffer rebase token blob  # 55
    blob index cache branch worker socket queue blob  # 56
    token ref blob socket socket window header tree worker header window  # 57
    header ref index delta branch parser socket branch queue rebase  # 58
    handle merge socket tree buffer merge branch  # 59
    header rebase stream buffer pack pack delta object socket  # 60
    blob ref stream tree index header  # 61
    blob rebase queue tree rebase stream  # 62
    remote handle delta index rebase cache  # 63
    worker cache cache commit commit remote remote  # 64
    merge blob delta object header merge ref cache handle  # 65
    commit blob commit branch parser header merge parser queue  # 66
    worker pack token socket merge handle merge token ref rebase rebase window  # 67
    index handle token remote handle parser delta branch worker parser object handle  # 68
    commit header tree branch window buffer  # 69
    index delta socket handle buffer socket handle merge merge queue blob stream  # 70
    queue handle tree commit stream branch cache token worker  # 71
    blob tree handle worker buffer index window header handle pack window  # 72
    header pack commit index stream index blob tree object header token branch  # 73
    queue blob ref commit header blob rebase  # 74
    window remote header socket buffer pack branch ref handle rebase index  # 75
    remote token cache branch token buffer  # 76
    ref buffer delta window commit pack rebase queue buffer merge  # 77
    index commit commit buffer pack index parser commit remote blob  # 78
    handle token queue tree ref cache merge  # 79
    stream stream branch delta socket  # 80
    remote blob tree queue pack merge  # 81
    object index queue index parser branch remote commit branch token ref  # 82
    ref commit stream socket tree  # 0
    blob delta socket window  # 1
    pack commit worker header tree ref  # 2
    object cache tree header object stream commit worker tree worker handle header  # 3
    header socket branch branch parser worker branch pack  # 4
    ref header rebase pack branch commit worker handle commit rebase worker  # 5
    branch worker object handle socket delta  # 6
    queue branch branch blob tree commit rebase worker buffer index  # 7
    merge remote merge ref merge commit tree window tree  # 8
    ref socket cache buffer window socket handle delta worker branch window object  # 9
    index pack queue delta window branch merge remote ref queue object commit  # 10
    delta ref worker blob pack ref blob queue  # 11
    index rebase blob handle  # 12
