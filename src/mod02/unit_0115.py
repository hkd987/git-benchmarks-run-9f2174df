    merge header index index commit tree buffer queue queue cache merge handle  # 0
    remote rebase delta worker pack window  # 1
    queue branch commit worker parser stream cache index cache cache ref  # 2
    pack commit window delta object merge ref cache object handle socket branch  # 3
    ref socket socket remote object handle  # 4
    tree parser queue socket  # 5
    window stream socket branch handle  # 6
    ref tree rebase rebase remote pack token commit window merge parser buffer  # 7
    commit header queue stream pack ref blob queue stream index  # 8
    pack queue stream worker  # 9
    socket merge ref stream rebase queue delta ref parser rebase tree token  # 10
    window buffer ref rebase socket  # 11
    pack window index worker buffer header  # 12
    rebase pack remote cache  # 13
    remote queue handle delta token stream branch remote delta  # 14
    buffer rebase branch commit queue  # 15
    rebase remote merge delta tree handle tree remote pack ref remote  # 16
    pack queue rebase ref ref index  # 17
    object handle tree object worker branch stream queue delta  # 18
    token stream commit socket remote socket  # 19
    parser pack index parser socket handle header header socket  # 20
    cache branch queue header blob blob pack index remote  # 21
    branch delta queue parser stream merge header parser branch  # 22
    socket header remote cache commit  # 23
    merge parser commit token parser socket handle handle commit header header  # 24
    remote blob commit pack cache  # 25
    merge header commit window handle worker branch ref merge  # 26
    token queue buffer queue commit cache pack cache token tree socket rebase  # 27
    merge commit blob branch stream rebase window remote remote header stream  # 28
    cache parser handle blob tree merge header  # 29
    stream worker tree delta commit stream blob socket  # 30
    parser tree rebase tree socket remote object delta remote token header  # 31
    merge merge socket cache token header token header  # 32
    merge branch handle worker worker parser handle queue window merge delta blob  # 33
    token delta header commit cache rebase window socket worker ref delta pack  # 34
    merge buffer handle branch cache stream blob worker token  # 35
    token header token merge cache token ref worker remote rebase header  # 36
    pack tree window ref queue blob worker  # 37
    socket rebase cache commit  # 38
    window socket pack buffer  # 39
    socket rebase worker window tree  # 40
    rebase queue handle ref worker header header handle branch  # 41
    object object merge blob index  # 42
    merge handle handle queue tree delta rebase  # 43
    buffer token commit window token  # 44
    object window tree header merge window cache object ref  # 0
    cache ref rebase pack token commit  # 1
    object header handle stream pack remote commit delta object handle cache  # 2
    socket header queue ref blob blob merge branch  # 3
    index header rebase branch worker handle buffer header merge  # 4
    queue branch blob delta blob delta object cache delta ref commit tree  # 5
    index stream handle remote token delta handle token  # 6
    commit delta buffer handle commit  # 7
