    object delta branch pack  # 0
    header token window blob index handle commit object  # 1
    window object remote blob handle handle blob blob  # 2
    socket commit stream queue header ref index parser  # 3
    merge blob merge stream token index stream commit socket worker merge queue  # 4
    merge cache merge cache buffer socket  # 5
    blob header handle stream token ref branch delta rebase  # 6
    remote handle token window commit cache  # 7
    branch object pack token index queue stream window pack  # 8
    object parser parser delta buffer worker  # 9
    token header socket worker socket  # 10
    delta header stream index delta pack  # 11
    parser socket handle buffer cache object merge delta rebase  # 12
    queue worker remote window commit worker  # 13
    cache commit commit merge stream blob pack worker worker rebase delta  # 14
    index tree cache token rebase stream ref socket cache merge  # 15
    worker commit branch stream  # 16
    header tree rebase socket commit stream pack  # 17
    cache blob window index delta commit queue remote stream header  # 18
    commit handle commit rebase cache tree  # 19
    tree token remote remote socket ref socket buffer handle socket  # 20
    tree stream ref branch worker commit  # 21
    stream pack window buffer object remote merge cache window cache rebase commit  # 22
    header stream token branch stream  # 23
    tree queue cache blob rebase socket pack remote cache token  # 24
    commit cache remote blob stream worker tree  # 25
    index commit socket rebase delta rebase worker blob commit  # 26
    blob stream header rebase ref delta parser rebase commit header  # 27
    buffer commit parser buffer header window remote merge remote cache token socket  # 28
    blob queue handle merge pack window handle ref stream worker merge  # 29
    handle pack pack delta socket delta  # 30
    delta index pack branch window window  # 31
    pack delta pack socket rebase commit rebase handle  # 32
    worker window stream rebase queue rebase window merge  # 33
    handle ref parser header cache commit token queue delta  # 34
    window header delta parser cache header remote  # 35
    stream index cache parser blob stream  # 36
    delta token object delta queue  # 37
    token buffer stream rebase remote commit ref  # 38
    buffer tree branch parser  # 39
    cache index ref window tree  # 40
