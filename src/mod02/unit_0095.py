    pack pack rebase branch buffer tree remote cache token blob  # 0
    index header commit object  # 1
    stream merge parser buffer header token window socket window  # 2
    blob index commit blob handle parser  # 3
    handle socket window buffer remote stream branch stream parser  # 4
    socket header ref pack branch rebase buffer token commit queue cache  # 5
    cache merge buffer stream pack object buffer blob header object handle branch  # 6
    tree parser window worker tree commit delta merge commit index buffer rebase  # 7
    token index token handle index handle socket token token  # 8
    ref cache queue cache remote worker  # 9
    commit handle ref blob worker index ref  # 10
    stream tree delta commit index object  # 11
    index buffer handle worker object cache stream  # 12
    header object blob rebase parser object merge queue delta  # 13
    handle pack blob rebase tree tree  # 14
    socket socket blob handle blob cache socket stream stream object blob  # 15
    blob tree blob commit rebase tree tree header ref object  # 16
    handle socket object parser object commit window  # 17
    ref window stream buffer branch worker branch  # 18
    rebase rebase window rebase merge branch queue  # 19
    buffer buffer queue queue  # 20
    rebase remote socket blob delta queue parser window  # 21
    header object remote buffer handle pack parser  # 22
    stream handle branch worker index queue token  # 23
    queue ref stream index header ref blob window  # 24
    worker token token parser buffer queue pack  # 25
    worker remote rebase remote handle object branch rebase ref header  # 26
    delta object handle stream remote tree commit rebase tree remote merge remote  # 27
    pack token tree cache parser blob branch parser  # 28
    merge token worker parser merge  # 29
    branch branch window tree rebase header cache  # 30
    handle index stream rebase blob  # 31
    token object object tree rebase socket  # 32
    delta pack object window merge remote worker  # 33
    stream parser merge object branch token  # 34
    buffer branch buffer index stream  # 35
    window merge delta cache  # 36
    blob buffer buffer buffer parser pack stream tree pack  # 37
    socket blob worker delta socket worker parser header ref cache tree  # 38
    object socket socket header  # 39
    token parser rebase rebase cache blob ref branch rebase  # 40
    delta object socket cache parser rebase pack ref cache blob token delta  # 41
    buffer ref buffer commit window  # 42
    handle token parser buffer worker stream buffer remote worker queue  # 43
    remote delta ref index object  # 44
    branch commit ref token branch socket  # 45
    header token queue cache queue  # 46
    rebase blob tree parser branch ref token handle worker  # 47
    branch branch delta delta handle blob delta  # 48
    handle rebase rebase worker window worker header commit  # 49
    commit window object blob buffer token socket header  # 50
    ref blob delta header object token index queue tree window  # 51
    worker delta stream index stream object rebase rebase header blob  # 52
    window window buffer window handle window tree handle  # 53
    index remote commit header handle  # 54
    parser cache window token merge object  # 55
    branch parser worker window window branch queue  # 56
    handle stream branch blob token handle tree window commit buffer blob  # 57
    branch merge pack branch buffer merge token queue handle merge token window  # 58
    object ref rebase cache remote stream worker branch buffer handle merge  # 59
    buffer commit buffer window parser  # 60
    rebase handle commit branch socket  # 61
    remote token token socket index tree index socket cache cache branch blob  # 62
    header token object merge remote  # 63
    pack delta parser socket parser window  # 64
    ref delta rebase commit object rebase cache  # 65
    commit rebase blob parser branch index commit ref index window  # 66
    tree object cache worker merge tree  # 67
    worker rebase socket object commit handle merge pack remote  # 68
    branch rebase commit worker socket ref branch header cache pack  # 69
    tree handle stream merge remote ref tree queue remote tree rebase  # 70
    pack stream cache blob commit object ref parser  # 71
    remote handle pack object ref branch branch object cache delta pack pack  # 72
    handle ref socket parser handle index branch handle socket  # 73
    header merge token merge socket commit index index remote  # 74
    rebase cache delta handle queue delta pack stream tree worker window  # 75
    branch buffer ref buffer ref socket queue  # 76
    index delta parser socket worker handle buffer pack ref window  # 77
    stream worker pack stream pack branch token pack remote branch cache cache  # 78
