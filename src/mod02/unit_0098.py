    window pack handle header handle ref window branch blob window  # 0
    queue socket window socket remote merge rebase merge  # 1
    queue remote queue index delta  # 2
    tree token token handle socket parser handle cache object header blob header  # 3
    rebase parser worker ref rebase merge buffer header object commit  # 4
    delta buffer token buffer branch tree worker header index pack cache  # 5
    socket header worker worker merge queue pack object pack merge branch window  # 6
    parser queue tree token rebase index buffer object handle socket stream rebase  # 7
    rebase ref branch buffer object tree ref rebase token  # 8
    socket parser buffer stream tree buffer worker branch object index  # 9
    object merge stream blob parser pack socket commit  # 10
    token queue cache header  # 11
    header rebase queue pack delta branch  # 12
    blob token remote header window handle cache object handle handle  # 13
    cache ref cache parser index  # 14
    window index socket blob cache  # 15
    handle parser object stream delta delta queue window window handle socket rebase  # 16
    branch remote merge stream index cache commit index stream window ref merge  # 17
    commit cache cache queue object tree pack worker  # 18
    handle object window pack buffer token  # 19
    stream parser worker window socket index pack  # 20
    commit token commit token parser header token worker pack rebase blob rebase  # 21
    token delta buffer buffer stream delta tree pack header stream parser  # 22
    cache merge commit remote index delta ref stream  # 23
    buffer remote delta window cache branch ref window object blob commit pack  # 24
    ref object index blob socket queue tree cache  # 25
    worker rebase object blob pack blob commit branch socket tree  # 26
    socket delta object ref pack pack cache  # 27
    parser branch ref merge queue socket rebase stream object token  # 28
    socket tree rebase tree commit delta buffer  # 29
    object index blob window  # 30
    stream merge ref queue commit tree  # 31
    stream object socket tree socket index index header socket worker cache  # 32
    tree queue socket ref merge stream window delta pack buffer  # 33
    object remote branch commit remote window delta object token window index  # 34
    queue rebase stream object commit merge  # 35
    tree header stream merge index header blob  # 36
    tree delta pack queue tree window  # 37
    index buffer merge merge cache worker merge queue branch commit object ref  # 38
    merge handle branch index pack socket object  # 39
    commit merge worker parser  # 40
    queue stream queue buffer token commit commit  # 41
    stream cache rebase parser ref branch queue  # 42
    parser object object index pack rebase index  # 43
    header index handle branch index commit object pack  # 44
    blob handle merge token commit rebase token handle  # 45
    socket socket tree buffer token worker token  # 46
    index branch object blob  # 47
    handle commit cache branch rebase object buffer  # 48
    rebase merge rebase tree header blob  # 49
    tree remote worker header branch pack queue  # 50
    pack commit token blob queue cache header merge  # 51
    index queue cache queue handle  # 52
    parser rebase handle stream index token ref parser worker token blob  # 53
    handle ref window socket queue  # 54
    stream handle handle index window stream branch pack header header ref index  # 55
    commit cache handle window  # 56
    parser branch parser token  # 57
    token object cache index  # 58
    tree remote blob branch commit stream token worker window  # 59
    ref token worker stream worker branch worker worker tree  # 60
    remote cache merge commit  # 61
    blob object parser branch handle window merge pack delta  # 62
    handle merge header header remote pack buffer  # 63
    header buffer socket window window rebase window pack  # 64
    delta rebase pack branch parser parser index rebase branch blob worker remote  # 65
    branch window cache header rebase ref commit blob window token header  # 66
    merge token remote commit branch token  # 67
    pack index token index remote header delta blob socket  # 0
    remote parser pack parser socket pack worker blob parser tree  # 1
    remote parser stream socket parser worker commit token worker token  # 2
    tree branch header worker ref handle parser merge buffer ref branch  # 3
    tree index branch cache tree token window stream commit queue stream  # 4
    window rebase pack buffer tree commit  # 5
