    header delta parser worker delta  # 0
    parser delta ref delta token queue  # 1
    rebase rebase socket header remote  # 2
    commit window object blob socket token delta tree  # 3
    delta socket token index branch  # 4
    header object socket index header merge  # 5
    commit header rebase merge window cache delta remote socket parser rebase  # 6
    token token blob cache rebase pack  # 7
    window index tree token rebase parser  # 8
    index merge cache stream tree blob buffer queue queue  # 9
    cache object handle rebase object window handle queue object commit rebase handle  # 10
    socket rebase rebase window  # 11
    delta parser parser cache blob tree delta  # 12
    token commit buffer window remote blob  # 13
    header header tree pack tree pack cache token  # 14
    commit branch index remote branch branch cache header object cache merge  # 15
    branch tree parser header socket index socket token token  # 16
    worker remote commit object  # 17
    rebase queue buffer token ref ref object remote tree remote delta header  # 18
    remote worker window commit ref token object  # 19
    worker stream branch rebase rebase  # 20
    stream ref parser tree worker socket delta index buffer  # 21
    tree commit object worker tree delta branch  # 22
    parser rebase stream branch header commit blob rebase object stream commit cache  # 23
    commit handle socket delta stream index worker object queue  # 24
    stream handle cache socket socket  # 25
    index index cache token handle cache cache parser queue stream  # 26
    token header buffer delta window merge  # 27
    worker stream stream window object parser cache stream cache buffer buffer worker  # 28
    ref parser socket stream tree ref parser branch rebase queue merge  # 29
    branch window parser stream  # 30
    queue handle socket commit cache worker  # 31
    parser parser window pack buffer buffer handle queue  # 32
    merge header object stream commit cache  # 33
    token header commit parser stream window window branch parser parser parser index  # 34
    object buffer window parser pack ref object worker ref worker delta  # 35
    rebase index object tree branch handle buffer delta  # 36
