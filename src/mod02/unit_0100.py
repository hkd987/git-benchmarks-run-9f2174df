    rebase blob merge pack token branch  # 0
    index branch delta window merge  # 1
    handle commit rebase remote token merge index  # 2
    header commit tree header worker  # 3
    delta commit token index  # 4
    queue blob queue queue branch token token remote object buffer socket  # 5
    header index merge header tree cache socket stream socket blob  # 6
    blob commit header remote object object  # 7
    pack index commit window rebase handle cache merge branch queue queue pack  # 8
    branch tree buffer window rebase delta index  # 9
    branch worker cache header remote ref  # 10
    ref buffer pack buffer handle ref header ref  # 11
    pack index header queue object blob tree object token  # 12
    pack blob buffer stream merge stream  # 13
    window rebase delta index socket stream pack blob  # 14
    socket tree cache ref header stream merge pack window handle queue  # 15
    window index object token delta handle  # 16
    delta merge cache header header blob tree token branch remote buffer  # 17
    parser tree remote token commit ref rebase ref handle header  # 18
    buffer object ref delta socket delta pack worker  # 19
    commit token stream ref header cache blob  # 20
    delta worker pack rebase queue worker pack  # 21
    queue queue blob buffer blob branch  # 22
    tree buffer header stream branch delta parser ref  # 23
    buffer header delta stream  # 24
    blob header window handle cache  # 25
    window token token blob tree cache socket commit  # 26
    window commit cache worker branch stream token merge commit remote delta  # 27
    header ref cache stream queue token rebase merge  # 28
    delta index stream handle token remote queue parser merge header object  # 29
    merge worker parser object ref branch stream object window  # 30
    object handle branch handle remote rebase commit remote delta stream object index  # 31
    index handle index pack commit  # 32
    commit handle stream object  # 33
    queue handle rebase window commit stream header  # 34
    header merge branch object rebase window remote handle branch blob branch blob  # 35
    index index buffer rebase socket pack token header tree stream merge  # 36
    remote merge pack index ref  # 37
    object merge header buffer pack commit queue ref buffer header token  # 38
