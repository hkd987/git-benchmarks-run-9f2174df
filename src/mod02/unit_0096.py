    rebase branch object branch worker commit stream window stream delta stream socket  # 0
    stream commit buffer blob parser branch token rebase stream tree  # 1
    branch branch pack index  # 2
    rebase ref rebase header queue rebase header worker remote index index delta  # 3
    rebase cache commit handle  # 4
    object merge tree stream ref branch socket object  # 5
    rebase socket index worker index cache rebase rebase blob  # 6
    rebase blob blob index buffer header pack buffer blob buffer socket tree  # 7
    header worker object cache merge handle header delta index  # 8
    commit tree buffer ref merge stream commit blob buffer rebase socket  # 9
    socket pack cache worker branch ref socket parser buffer  # 10
    remote worker merge parser delta merge window header tree merge cache  # 11
    commit index commit ref stream tree commit queue branch blob  # 12
    merge token branch remote remote pack object token commit remote merge merge  # 13
    worker parser branch token handle rebase queue pack delta stream  # 14
    worker window tree header branch ref token rebase commit socket stream  # 15
    branch object rebase parser index tree remote handle tree  # 16
    socket header buffer handle blob remote parser  # 17
    object window blob rebase pack object token token blob  # 18
    pack header cache delta blob ref cache window socket blob  # 19
    delta ref stream parser blob remote blob handle header ref parser  # 20
    cache worker parser tree  # 21
    rebase parser ref ref stream delta tree remote buffer queue  # 22
    commit branch commit delta ref remote buffer remote index  # 23
    object tree object handle buffer delta  # 24
    socket buffer cache parser  # 25
    queue delta commit handle tree queue buffer parser token remote ref  # 26
    buffer cache remote handle token worker  # 27
    object rebase merge merge parser commit  # 28
    blob header tree index socket cache delta token cache branch  # 29
    rebase token cache blob object socket branch  # 30
    header cache stream branch socket object object merge  # 31
    commit remote blob queue parser parser socket branch buffer worker index  # 32
    pack parser tree remote  # 33
    header rebase ref parser merge window ref  # 34
    worker index ref worker merge  # 35
    stream parser blob window rebase socket header remote parser rebase object header  # 36
    ref window delta window commit delta  # 37
    cache stream remote index ref merge cache token  # 38
    cache parser stream pack ref object buffer  # 39
    pack tree object parser handle branch blob delta rebase window window  # 40
    pack window buffer header pack remote token socket blob queue worker stream  # 41
    pack delta header window  # 42
    rebase handle remote index parser  # 43
    pack handle ref cache handle  # 44
    parser worker remote token commit token index queue  # 45
    buffer worker handle commit index header object  # 46
    token queue merge parser stream window branch commit pack  # 47
    pack object commit buffer commit token token buffer queue tree pack pack  # 48
    token window parser branch worker pack tree  # 49
    token header buffer pack window  # 50
    queue header merge parser token token ref header rebase handle worker  # 51
    buffer worker delta worker handle merge branch tree  # 52
    handle blob index parser  # 53
    merge queue commit header remote remote header queue buffer index token window  # 54
    remote object merge worker queue header stream remote pack rebase stream  # 55
    object merge handle merge stream  # 56
    object header tree buffer pack header handle socket tree remote queue handle  # 57
    delta commit worker stream object token header rebase delta  # 58
    ref header delta header socket  # 59
    parser commit rebase pack ref parser socket  # 60
    object parser token cache commit parser tree cache delta blob remote queue  # 61
    commit queue queue cache branch merge index commit cache delta cache  # 62
    object branch stream parser rebase parser worker commit  # 63
    merge delta commit handle window buffer header parser remote cache rebase remote  # 64
    socket blob merge delta header stream  # 65
    buffer index cache queue  # 66
    commit rebase cache cache rebase ref delta  # 67
    index index window handle token parser remote index  # 68
    handle branch header window  # 69
    parser tree pack buffer token handle object window parser merge parser header  # 70
    tree merge handle rebase socket handle merge handle cache rebase  # 71
    pack stream merge blob  # 72
    branch parser cache ref  # 73
    delta commit queue stream commit buffer window cache remote  # 74
    stream remote merge window rebase token token header rebase pack handle delta  # 75
    ref cache handle tree token ref token ref commit commit  # 76
    queue merge ref object parser window cache ref tree socket commit branch  # 77
    queue cache pack tree  # 78
    merge object ref rebase parser index header  # 79
