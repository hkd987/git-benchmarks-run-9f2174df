    buffer commit delta cache worker  # 0
    socket delta worker remote window  # 1
    header tree tree cache cache delta remote tree  # 2
    blob branch handle queue window socket delta stream tree blob index  # 3
    branch remote object tree token tree tree  # 4
    parser queue socket delta pack rebase rebase worker  # 5
    stream branch object handle ref token remote worker  # 6
    object token blob worker token  # 7
    token index queue header queue object window  # 8
    window stream index index delta pack ref ref socket  # 9
    queue remote handle blob window blob cache worker ref buffer queue  # 10
    commit cache token pack index  # 11
    cache queue stream header window delta tree pack  # 12
    window window rebase commit header stream rebase header  # 13
    object buffer window parser tree merge ref merge pack commit delta branch  # 14
    pack stream stream object rebase tree queue  # 15
    index remote remote header header stream  # 16
    rebase commit merge parser object ref cache object index handle  # 17
    branch buffer stream branch object queue queue queue rebase merge pack  # 18
    branch buffer pack object blob pack parser blob ref  # 19
    handle window buffer buffer delta  # 20
    ref worker cache handle tree branch branch buffer ref worker worker  # 21
    commit delta blob socket delta  # 22
    stream remote branch worker  # 23
    object branch cache pack index queue ref index rebase  # 24
    buffer delta pack pack ref queue branch blob pack parser index handle  # 25
    parser token stream tree parser token delta branch queue rebase  # 26
    index index cache cache tree ref branch socket branch delta handle queue  # 27
    token cache blob remote socket blob pack delta  # 28
    pack header cache token delta cache commit parser  # 29
    commit branch parser worker queue worker object  # 30
    queue header pack window  # 31
    socket handle buffer merge rebase branch  # 32
    commit branch token branch stream stream parser object token remote buffer  # 33
    worker pack object handle object tree parser delta  # 34
    ref parser worker object  # 35
    commit branch queue parser delta rebase worker  # 36
    parser pack delta pack pack tree token index  # 37
    handle ref tree tree remote stream  # 38
    window blob index window branch blob queue pack cache worker header  # 39
    delta index tree ref ref tree window pack delta branch commit blob  # 40
    rebase branch header merge branch rebase queue token window  # 41
    branch index branch delta merge worker queue object  # 42
    remote cache rebase stream object pack  # 43
    stream branch object worker  # 44
    worker rebase rebase pack  # 45
    index object tree ref commit worker branch window token  # 0
    delta commit tree window  # 1
    header pack parser window rebase ref index  # 2
    cache rebase stream object  # 3
    rebase parser token branch token buffer merge window blob token  # 4
    cache branch token buffer merge blob delta index  # 5
    commit token rebase handle token buffer  # 6
    pack socket queue handle rebase buffer handle worker merge index  # 7
    queue index token delta blob  # 8
    branch handle branch buffer object ref ref queue queue  # 9
    commit tree branch commit socket branch stream  # 10
    header queue object index handle ref commit header  # 11
    token buffer object object queue  # 12
    delta index delta buffer socket commit  # 13
    queue delta merge stream  # 0
    object ref worker merge ref stream socket object merge tree  # 1
    header rebase cache commit parser index remote merge merge delta  # 2
    blob delta queue rebase token  # 3
    queue buffer branch socket rebase socket header delta blob  # 4
    handle queue socket window rebase commit  # 5
