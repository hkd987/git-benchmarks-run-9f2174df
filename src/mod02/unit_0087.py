    merge rebase pack tree window index  # 0
    merge delta blob parser commit delta  # 1
    buffer window remote parser parser token blob object buffer stream  # 2
    worker blob queue buffer socket buffer merge  # 3
    socket tree pack buffer window remote object commit commit rebase ref header  # 4
    remote ref handle branch  # 5
    ref socket queue blob header handle tree ref token  # 6
    blob socket branch handle parser stream pack commit branch tree  # 7
    parser worker parser stream queue index commit merge token socket token rebase  # 8
    ref tree handle header token token header window tree merge  # 9
    commit branch header cache header tree parser  # 10
    buffer object commit branch branch token merge socket socket  # 11
    buffer socket stream worker  # 12
    index token delta queue remote token merge pack commit queue  # 13
    socket token tree branch merge parser parser socket branch index commit  # 14
    blob remote index worker handle merge delta  # 15
    blob rebase parser header object branch  # 16
    parser pack window tree commit socket delta commit blob  # 17
    worker window blob delta remote rebase  # 18
    buffer delta handle rebase cache remote merge branch socket  # 19
    stream pack delta cache parser parser stream handle commit  # 20
    blob index remote delta header stream header window socket delta stream  # 21
    branch token token cache commit stream  # 22
    parser token pack header tree  # 23
    token delta stream index stream stream window handle buffer object delta commit  # 24
    pack pack merge cache remote  # 25
    delta delta remote tree branch merge ref blob worker  # 26
    pack object blob remote  # 27
    blob cache branch index delta  # 28
    tree header pack cache  # 29
    window index worker socket index socket stream blob blob header  # 30
    tree commit handle token commit handle header index header index delta  # 31
    header object buffer commit  # 32
    handle buffer pack header header merge ref buffer ref tree  # 33
    header window token cache blob commit object pack socket  # 34
    tree token stream remote  # 35
    parser merge window tree  # 36
    remote handle rebase remote parser commit  # 37
    window worker tree buffer commit index window token queue remote object commit  # 38
    queue object ref socket blob stream  # 39
    remote queue cache stream index tree header rebase delta token  # 40
    pack commit index pack token stream stream queue  # 0
    token window parser worker object index header header pack  # 1
    rebase parser tree blob parser header commit queue header worker cache  # 2
    remote blob blob rebase index branch  # 3
    pack queue rebase index remote parser worker buffer handle merge token parser  # 4
    token buffer commit stream branch parser header pack  # 5
    window token pack delta blob branch token cache object header queue  # 6
    parser index token commit window worker cache worker  # 7
    object socket remote delta window worker branch remote cache cache remote window  # 8
    stream parser parser queue stream queue branch stream handle socket handle handle  # 0
    parser object tree merge remote branch queue  # 1
    handle parser commit remote handle blob branch  # 2
    header parser worker stream branch object pack  # 3
    ref rebase index cache cache branch buffer blob  # 4
    cache tree header stream window socket  # 5
    stream tree delta ref parser queue stream blob merge window  # 6
    buffer delta rebase rebase  # 7
    header cache ref remote index stream window pack  # 8
    window buffer merge stream worker stream pack worker  # 9
