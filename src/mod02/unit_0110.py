    branch header pack blob delta socket worker remote handle commit  # 0
    buffer object stream delta commit merge branch object merge commit queue remote  # 1
    pack token rebase window delta remote parser handle ref cache window object  # 2
    merge pack queue token blob commit cache window header index commit pack  # 3
    blob remote branch handle  # 4
    pack handle cache worker branch remote branch pack  # 5
    ref branch cache cache blob blob remote object pack delta socket  # 6
    stream queue handle tree socket buffer blob token index  # 7
    parser stream pack tree blob  # 8
    pack rebase remote index commit commit  # 9
    worker socket pack remote queue token rebase object delta index  # 10
    handle handle tree branch header index socket queue merge stream rebase rebase  # 11
    branch commit blob remote remote pack rebase commit index object  # 12
    buffer index stream cache handle queue  # 13
    cache merge pack stream parser merge socket  # 14
    cache remote worker header  # 15
    object remote window cache  # 16
    rebase parser rebase index buffer worker stream pack rebase ref queue stream  # 17
    header object header queue merge handle  # 18
    blob header rebase window token buffer blob parser blob window header  # 19
    cache tree blob rebase stream remote window socket token pack  # 20
    parser buffer cache commit window token queue remote branch window ref  # 21
    cache commit delta delta rebase window parser worker blob worker delta  # 22
    ref parser cache worker token handle  # 23
    queue cache queue socket remote window merge  # 24
    tree blob commit rebase queue index token index object handle  # 25
    tree stream remote merge  # 26
    worker blob cache branch blob blob rebase header remote  # 27
    cache worker worker tree header stream header buffer  # 28
    remote branch socket token queue merge branch object commit  # 29
    delta ref token token worker ref blob parser queue pack  # 30
    pack object remote index  # 31
    object delta parser stream blob queue tree branch blob socket  # 32
    tree branch branch tree stream socket window worker remote header blob merge  # 33
    delta buffer index ref commit cache parser pack window socket  # 34
    cache cache buffer tree branch parser queue buffer  # 35
    token delta worker window  # 36
    branch token cache delta header remote buffer rebase rebase branch stream token  # 37
    pack tree socket commit merge branch delta handle token  # 38
    rebase blob rebase buffer branch worker remote queue handle  # 39
    merge commit cache pack buffer  # 40
    worker merge cache rebase handle blob token commit blob  # 41
    remote buffer cache ref  # 42
    parser parser stream socket parser stream handle cache  # 43
    tree cache tree tree buffer tree remote delta rebase delta remote  # 44
    queue branch ref header remote stream commit worker handle blob rebase  # 45
    header ref buffer queue  # 46
    queue branch cache handle tree tree object  # 47
    parser buffer window cache worker blob cache ref window  # 48
    stream socket token rebase worker index index queue  # 49
    worker window index socket stream ref branch  # 50
    delta ref cache buffer branch pack worker stream  # 51
    blob buffer merge token queue stream blob queue  # 52
    merge handle delta branch stream branch pack parser  # 53
    merge worker index cache buffer blob buffer commit rebase ref  # 54
    pack tree cache delta ref stream delta cache  # 55
    index cache tree pack pack commit buffer branch object blob  # 56
    index index stream rebase rebase stream rebase tree worker tree header  # 57
    token parser parser delta commit socket ref branch  # 58
    tree socket worker object window index ref buffer  # 59
    socket delta window queue blob pack worker worker window  # 60
    parser branch branch remote header tree window worker merge token  # 61
    worker buffer cache parser socket worker socket blob blob worker tree  # 62
    remote socket token cache blob index  # 63
    rebase commit branch delta header cache queue rebase socket  # 64
    branch ref stream object commit ref worker object  # 65
    tree worker remote stream object  # 66
    pack tree handle cache commit remote  # 67
    commit delta parser queue remote queue remote rebase tree queue pack ref  # 68
    blob pack stream branch pack handle index buffer commit delta  # 69
    window token branch commit queue socket index  # 70
    buffer index object token remote header handle token rebase blob  # 71
    worker blob window delta rebase commit stream  # 72
    commit rebase ref branch stream parser window  # 73
    token tree cache buffer object cache token pack window commit parser header  # 74
    merge worker delta tree remote  # 75
    remote merge handle window parser parser pack remote index delta index queue  # 76
    parser buffer buffer commit header queue socket  # 77
    tree buffer header cache  # 78
    commit ref handle rebase parser  # 79
    stream cache object parser buffer stream branch  # 80
