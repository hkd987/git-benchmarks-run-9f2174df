    handle object parser object token token tree parser buffer  # 0
    stream ref remote blob buffer rebase header branch window stream remote rebase  # 1
    stream branch commit delta merge queue ref socket worker index  # 2
    delta window header socket  # 3
    ref branch branch remote merge cache queue parser stream ref cache  # 4
    index handle delta stream socket rebase buffer commit buffer pack commit tree  # 5
    pack cache header ref handle queue tree socket socket  # 6
    branch handle rebase remote ref worker  # 7
    pack delta merge socket blob tree buffer token merge  # 8
    cache tree pack rebase  # 9
    socket pack object token token  # 10
    buffer cache handle token commit object object buffer branch queue tree remote  # 11
    branch merge rebase branch ref commit token branch  # 12
    tree ref header pack queue object  # 13
    window cache stream cache rebase rebase cache handle worker header  # 14
    handle handle rebase remote branch handle stream  # 15
    ref worker cache delta cache socket window token  # 16
    buffer token header commit  # 17
    branch pack queue object  # 18
    queue handle remote commit cache parser merge token header  # 19
    token remote branch worker index blob commit parser  # 20
    worker pack window index delta object branch object ref cache cache  # 21
    tree parser object cache token cache blob  # 22
    window blob socket tree delta worker tree  # 23
    cache handle buffer object ref cache worker window  # 24
    pack cache delta blob ref ref socket object cache blob  # 25
    ref pack header merge rebase socket token delta socket commit ref remote  # 26
    pack blob index index index ref  # 27
    delta cache parser handle remote handle window worker rebase tree window  # 28
    socket merge stream rebase token buffer cache worker commit parser branch socket  # 29
    tree object header stream worker worker worker socket branch commit  # 30
    delta header pack parser handle window parser merge  # 31
    ref queue stream token delta socket tree tree cache worker  # 32
    commit branch merge index index stream worker socket pack commit  # 33
    delta buffer stream rebase remote ref  # 34
    header branch queue window merge merge worker stream window pack  # 35
    handle remote remote token index blob  # 36
    socket commit parser token worker delta socket commit object tree index  # 37
    commit queue handle header remote token delta token queue merge  # 38
    header queue commit commit rebase merge parser handle  # 39
    socket handle queue index delta object remote ref merge window ref object  # 40
    cache queue delta header commit delta branch  # 41
    parser handle header rebase handle tree ref blob tree  # 42
    tree remote worker window index socket parser parser  # 43
    buffer pack header socket worker index remote delta header  # 44
    branch merge header delta blob buffer stream  # 45
    delta handle ref object commit window handle  # 46
    socket blob delta merge delta buffer index remote stream window merge  # 47
    blob delta remote tree handle index handle  # 48
    rebase buffer branch socket delta window parser merge worker rebase blob  # 49
    handle window blob worker  # 50
    delta remote header blob socket remote blob  # 51
    header stream parser stream  # 52
    window stream delta branch ref merge branch index buffer merge  # 53
    handle rebase ref delta blob ref remote buffer header worker handle  # 54
    handle tree remote commit pack delta tree  # 55
    buffer header branch header token  # 56
    stream token commit parser remote handle merge handle index  # 57
    header stream buffer rebase branch remote window pack token handle socket branch  # 58
    token branch socket branch merge window delta handle window handle blob blob  # 59
    token remote index pack token pack delta merge tree delta  # 60
    object blob buffer socket ref parser header tree ref rebase  # 61
