    tree remote blob queue handle socket merge cache socket window queue  # 0
    header queue header ref  # 1
    commit rebase cache queue token merge parser parser handle remote worker merge  # 2
    token stream parser queue blob pack tree token commit buffer  # 3
    blob commit branch parser pack branch token commit blob rebase  # 4
    worker delta index buffer delta commit cache socket stream window parser handle  # 5
    worker ref tree token parser branch  # 6
    pack blob queue window pack token index  # 7
    rebase merge cache branch queue queue cache pack  # 8
    blob queue stream handle branch buffer  # 9
    remote branch object object delta window merge remote index delta  # 10
    pack window ref window  # 11
    merge commit queue ref pack buffer tree branch branch  # 12
    commit cache cache commit buffer merge merge parser  # 13
    branch pack parser commit handle rebase  # 14
    buffer commit worker pack cache delta tree remote parser worker window rebase  # 15
    object queue header worker remote delta branch rebase worker delta queue  # 16
    ref handle tree pack blob index remote delta tree merge  # 17
    commit object parser index  # 18
    parser tree remote remote parser tree remote  # 19
    delta index stream cache handle socket merge window token  # 20
    tree worker tree remote parser cache commit  # 21
    object queue pack remote index token buffer  # 22
    buffer remote socket pack header delta queue queue  # 23
    stream token remote socket  # 24
    window token parser rebase token tree index socket buffer buffer  # 25
    ref remote worker queue branch  # 26
    socket parser merge rebase handle  # 27
    socket parser blob rebase ref pack delta object remote delta  # 28
    cache header buffer stream buffer index window object tree stream  # 29
    ref socket parser delta window rebase handle index socket commit object merge  # 30
    object blob ref remote remote ref handle  # 31
    branch blob header branch window  # 32
    branch pack window delta buffer rebase  # 33
    commit tree delta token object handle index commit header  # 34
    blob queue handle object commit buffer stream merge remote ref blob  # 35
    object socket merge token commit rebase  # 0
    queue header queue handle delta merge stream  # 1
    commit parser stream delta branch cache buffer index blob worker commit window  # 2
    object pack worker ref branch handle blob remote  # 3
    tree ref header ref merge token tree ref queue branch  # 4
    token parser blob parser tree commit merge  # 5
    cache token commit index  # 6
    delta tree parser remote commit merge buffer rebase token  # 7
    object handle parser window tree stream ref index handle  # 8
    pack commit parser ref handle  # 9
    ref commit blob socket stream remote cache queue rebase  # 10
    tree merge worker worker  # 0
    branch merge header buffer ref window pack queue handle handle buffer  # 1
    delta blob handle index merge ref branch queue delta pack token  # 2
