    header tree handle token remote queue delta pack rebase rebase  # 0
    token object buffer stream object stream socket  # 1
    cache ref blob tree  # 2
    blob token pack object buffer stream window header buffer  # 3
    buffer merge blob cache blob socket parser  # 4
    commit handle pack queue token merge  # 5
    worker tree queue rebase commit cache worker worker  # 6
    worker worker buffer index blob cache  # 7
    worker blob ref window token queue window stream token buffer rebase  # 8
    rebase queue delta delta token rebase header token buffer tree buffer  # 9
    ref handle remote blob buffer ref handle buffer commit  # 10
    remote buffer blob worker merge tree cache object delta ref worker stream  # 11
    header remote stream index queue merge parser commit  # 12
    handle blob blob merge  # 13
    commit tree object header buffer tree commit parser tree branch buffer  # 14
    header token ref handle handle worker header merge remote delta remote  # 15
    token queue queue blob window index pack merge  # 16
    delta stream window handle branch pack worker object remote  # 17
    socket cache delta blob queue merge  # 18
    commit object rebase remote tree socket tree rebase tree  # 19
    ref rebase index delta commit handle index queue commit ref parser  # 20
    buffer rebase index tree socket buffer index cache parser  # 21
    token window queue commit delta stream delta header index index blob  # 22
    handle header stream pack buffer remote cache parser socket delta remote rebase  # 23
    header commit remote parser  # 24
    object merge stream window rebase parser cache  # 25
    tree queue header queue  # 26
    branch parser stream tree  # 27
    header tree buffer stream window worker handle handle token  # 28
    pack window rebase window handle stream header  # 29
    commit branch index ref tree pack header stream buffer queue queue  # 30
    blob remote buffer socket handle cache ref parser window socket worker commit  # 31
    commit stream window rebase window rebase rebase buffer handle rebase blob parser  # 32
    buffer header socket object socket object queue parser remote object parser  # 33
    header worker buffer rebase  # 34
    ref rebase pack commit rebase buffer header socket index rebase  # 35
    commit handle queue commit buffer window cache header  # 36
    index object worker buffer tree pack  # 37
    worker parser parser queue blob blob worker parser header branch  # 38
    handle token handle cache  # 39
    buffer remote socket delta merge rebase  # 40
    token worker commit delta remote blob handle tree cache parser  # 41
    handle token merge object handle stream handle ref stream queue  # 42
    queue handle branch stream window blob ref commit  # 43
    remote parser merge merge index pack parser index socket rebase branch branch  # 44
    commit branch tree worker parser window worker delta cache worker commit  # 45
    worker worker delta worker merge  # 46
    buffer queue branch rebase branch cache object worker queue token delta cache  # 47
    socket header pack remote tree queue delta merge rebase pack  # 48
    token commit tree pack queue merge pack  # 49
    token handle header stream window buffer window  # 50
    parser header rebase index branch remote merge socket merge cache token token  # 51
    object handle header parser window window window  # 52
    merge worker queue stream buffer worker parser  # 53
    commit rebase object parser handle delta cache commit queue socket ref  # 54
    handle pack branch commit rebase handle remote rebase  # 55
    buffer index index worker worker ref  # 56
    token blob tree stream ref token object pack worker window token  # 57
    parser worker parser buffer commit merge remote worker parser token cache header  # 58
    buffer stream merge commit  # 59
    handle header rebase queue blob window ref cache token object parser  # 60
    ref merge merge worker queue remote worker index rebase ref stream object  # 61
    parser index remote buffer socket rebase cache  # 62
    blob queue tree parser index branch cache  # 63
    commit parser branch object stream token  # 64
    tree token header remote  # 65
    parser blob branch rebase merge queue socket stream  # 66
    worker remote delta blob index stream remote blob merge  # 67
    remote handle delta object branch stream  # 68
    blob worker commit object delta socket rebase window delta window  # 69
    pack index stream header worker rebase branch token branch  # 70
    parser index ref branch token header index pack  # 71
    socket ref ref branch parser tree socket object parser stream window  # 72
    remote rebase token parser buffer blob worker  # 0
    socket delta delta index socket index index cache  # 1
    rebase rebase header ref remote  # 2
    merge merge token header index worker remote queue  # 3
    delta window delta pack ref stream stream tree queue merge worker pack  # 0
    rebase window worker delta handle ref object queue worker  # 1
    worker ref delta token handle tree handle object ref  # 2
    pack header index pack ref merge header  # 3
    socket cache pack merge pack index header queue delta commit merge socket  # 4
    header branch socket ref pack branch delta merge delta worker index  # 5
    parser worker branch tree  # 6
    token merge delta header stream blob cache token queue  # 7
    branch token branch window worker socket branch  # 8
