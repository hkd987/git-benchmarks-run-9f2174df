    header merge merge blob blob merge worker header buffer queue  # 0
    branch index object cache token header cache cache  # 1
    index merge window commit window worker cache  # 2
    remote cache token ref header remote  # 3
    token stream delta blob worker parser handle tree ref blob socket  # 4
    index parser handle ref worker queue object commit queue index header  # 5
    index stream remote pack rebase rebase object  # 6
    commit pack blob branch window branch handle merge index token blob  # 7
    branch branch commit branch pack  # 8
    worker stream header buffer  # 9
    queue stream merge worker cache buffer header socket remote parser socket stream  # 10
    pack queue socket handle handle commit blob  # 11
    delta buffer queue worker pack index parser parser  # 12
    buffer socket header handle worker ref merge object pack handle merge pack  # 13
    stream remote merge worker cache queue rebase  # 14
    handle socket header blob token  # 15
    window queue object merge  # 16
    pack rebase merge remote commit  # 17
    commit worker tree queue delta stream window index socket  # 18
    buffer window tree cache merge socket pack stream token parser  # 19
    cache buffer stream socket remote index object commit remote  # 20
    handle blob cache rebase merge rebase  # 21
    branch branch object object stream  # 22
    remote handle worker queue blob commit handle rebase merge  # 23
    parser parser token window parser object header remote commit  # 24
    handle remote merge header window header handle window ref stream  # 25
    tree queue stream buffer header header delta object rebase worker handle  # 26
    window remote socket commit index  # 27
    delta window branch window rebase pack index pack  # 28
    parser remote socket rebase object  # 29
    buffer remote token ref commit pack  # 30
    delta blob socket socket cache blob pack branch cache window tree stream  # 31
    queue branch tree object stream socket handle blob stream object  # 32
    tree branch handle ref stream token parser window worker merge parser remote  # 33
    queue stream index index blob blob rebase object rebase  # 34
    ref stream cache object branch tree parser header handle stream remote token  # 35
    commit buffer object header parser window  # 36
    delta stream merge delta  # 37
    handle ref delta buffer  # 38
    tree rebase handle socket header remote pack buffer index worker handle  # 39
    stream queue worker worker header window  # 40
    object delta rebase buffer buffer buffer blob  # 41
    branch commit remote branch blob header merge token handle tree  # 42
    queue window pack queue ref stream merge blob rebase  # 43
    worker window queue worker  # 44
    stream rebase object header socket branch branch branch header handle header  # 45
    branch delta index tree merge queue merge blob pack token  # 46
    blob worker branch worker pack ref  # 47
    socket remote rebase header token stream handle rebase  # 48
    stream window commit pack stream remote buffer  # 49
    parser cache remote object rebase worker queue cache remote ref  # 50
    cache stream remote socket blob header header token  # 51
    parser pack queue handle header commit ref header branch object  # 52
    stream commit queue handle queue worker delta tree header object  # 53
    buffer branch stream index buffer merge commit  # 54
    remote worker tree object token header delta merge  # 55
    delta delta window header remote ref token ref worker  # 56
    branch handle worker branch merge blob parser rebase ref  # 57
    buffer pack token window delta handle queue queue merge  # 58
    index handle tree tree worker  # 59
    buffer blob tree rebase buffer token object stream branch pack ref object  # 60
    tree branch ref worker pack handle worker  # 61
    buffer socket handle branch remote index merge queue cache socket  # 62
    token object branch merge blob merge  # 63
    header cache delta merge index buffer stream  # 0
    remote blob stream rebase handle handle blob token branch  # 1
    tree stream delta queue queue remote branch worker parser object  # 2
    branch cache stream commit delta header merge merge header worker queue tree  # 3
    header object window branch remote parser index queue  # 4
    queue blob branch ref  # 5
    cache remote remote index stream merge  # 6
    object worker header worker delta  # 7
    token cache queue queue object index window remote parser worker header  # 8
    delta tree branch commit pack remote cache socket cache object ref  # 9
    tree queue blob remote token pack delta handle remote header object branch  # 0
    socket delta merge socket delta tree remote index worker socket token socket  # 1
    delta socket rebase pack tree queue handle window parser stream parser window  # 2
    header header worker token window object object handle  # 3
    index remote remote handle queue ref parser commit token object object stream  # 4
    header delta buffer handle socket cache  # 5
    socket pack worker merge cache  # 6
    merge buffer blob stream merge  # 7
    cache window window handle header index delta queue delta parser  # 8
