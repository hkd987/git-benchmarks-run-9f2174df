    window stream merge ref buffer handle window delta branch  # 0
    commit branch commit header blob blob rebase pack ref stream ref  # 1
    pack socket commit buffer window blob delta remote buffer worker remote  # 2
    header socket cache window parser queue socket buffer socket index blob blob  # 3
    commit cache remote index header  # 4
    cache remote token branch merge buffer  # 5
    window index merge stream pack object queue worker blob  # 6
    blob parser window stream pack  # 7
    header parser buffer object  # 8
    tree handle tree delta object rebase index merge handle pack cache  # 9
    token rebase stream commit socket handle  # 10
    delta ref stream buffer tree commit  # 11
    buffer rebase stream cache tree buffer delta rebase  # 12
    delta handle ref handle remote window rebase token  # 13
    rebase pack remote pack parser rebase handle blob  # 14
    merge ref merge cache object buffer  # 15
    worker delta delta pack window token merge queue rebase worker  # 16
    ref blob token index pack ref ref  # 17
    branch rebase blob tree ref worker blob object socket pack  # 18
    parser header header handle worker token  # 19
    queue merge rebase window stream commit branch queue  # 20
    handle object stream remote object parser parser worker token queue cache remote  # 21
    handle merge delta object stream queue ref delta  # 22
    branch remote merge stream cache branch token pack blob  # 23
    cache rebase delta object buffer header branch  # 24
    merge window socket blob pack parser commit parser token pack buffer parser  # 25
    object blob stream tree rebase  # 26
    buffer window buffer commit window  # 27
    object delta handle buffer header window  # 28
    remote object token remote handle branch  # 29
    parser delta header remote branch branch  # 30
    blob blob stream delta parser rebase branch cache tree merge handle header  # 31
    delta delta parser index buffer object  # 32
    buffer ref index blob branch stream socket buffer  # 33
    token merge merge remote socket buffer remote token socket remote tree  # 34
    blob queue window ref header rebase rebase token handle delta index  # 35
    worker ref token header header tree window pack commit blob object pack  # 36
    tree worker worker tree ref blob token socket  # 37
    merge branch rebase commit commit parser rebase ref handle window buffer  # 38
    pack buffer worker merge tree stream commit tree commit remote queue tree  # 39
    window blob parser handle header cache branch buffer cache  # 0
    commit object buffer object index  # 1
    tree worker tree commit object window header merge merge rebase object  # 2
    tree merge commit tree merge commit socket merge rebase commit branch tree  # 3
    window window delta rebase object ref  # 4
    index rebase tree worker delta remote merge branch merge remote branch cache  # 5
    delta parser rebase queue window ref blob tree tree blob  # 6
    branch branch branch index parser pack handle commit token blob remote handle  # 7
    handle queue pack buffer worker header commit pack branch  # 8
    commit stream handle remote  # 9
    handle queue index blob parser blob  # 0
    cache queue rebase stream buffer blob queue handle queue object blob  # 1
    rebase blob parser pack buffer token cache merge pack  # 2
    buffer rebase stream queue branch  # 3
    queue handle delta ref token queue blob window  # 4
    remote delta worker index queue  # 5
    parser remote delta rebase buffer rebase buffer queue rebase header tree worker  # 6
    remote ref token socket parser delta pack socket  # 7
    remote parser socket queue tree stream buffer socket index header  # 8
