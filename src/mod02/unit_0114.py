    buffer remote object remote merge object remote  # 0
    cache token handle rebase remote index handle socket stream object  # 1
    merge token commit ref worker queue index buffer buffer worker  # 2
    header worker stream cache buffer commit ref header parser stream commit  # 3
    stream window object handle rebase queue cache  # 4
    index object cache rebase blob  # 5
    pack token handle commit buffer blob handle  # 6
    rebase tree token pack stream merge  # 7
    commit header blob delta handle branch commit queue queue  # 8
    commit merge tree blob delta pack  # 9
    stream stream ref stream socket ref tree ref window blob queue  # 10
    worker header blob remote pack window parser cache delta ref  # 11
    socket socket header socket  # 12
    object token token object handle handle pack branch delta worker rebase  # 13
    tree parser tree token queue object remote remote window buffer commit  # 14
    queue index handle merge merge tree socket index  # 15
    parser header remote parser  # 16
    cache handle parser header parser header rebase pack pack buffer  # 17
    window object token index header pack  # 18
    blob stream commit cache ref merge header commit  # 19
    socket token merge token parser socket rebase pack ref  # 20
    parser buffer queue tree index handle commit socket delta branch stream queue  # 21
    rebase remote remote merge worker tree queue branch blob object  # 22
    buffer tree delta commit  # 23
    cache branch branch commit remote buffer socket token cache merge worker stream  # 24
    header worker branch branch handle commit header header cache merge  # 25
    remote merge handle commit pack pack  # 26
    remote commit rebase commit object stream buffer  # 27
    cache stream buffer buffer object queue index queue handle socket rebase  # 28
    branch object parser commit handle index tree branch window stream  # 29
    delta window tree worker worker branch commit  # 30
    buffer worker window header remote blob  # 31
    handle stream blob parser parser ref  # 32
    parser handle socket object remote branch worker cache object worker window remote  # 33
    stream pack token commit worker window pack handle tree  # 34
    commit header tree object  # 35
    merge merge merge header  # 36
    parser handle tree branch remote ref remote handle  # 37
    rebase stream header window blob window object object merge commit  # 38
    queue buffer cache parser object object pack blob  # 39
    pack blob delta delta tree index  # 40
    worker ref rebase commit remote commit cache index merge window  # 41
    handle header header commit branch tree handle  # 42
    buffer handle index stream merge merge blob delta  # 43
    header stream cache token socket rebase merge handle header window rebase tree  # 44
    socket branch tree header parser blob header branch handle stream queue branch  # 45
    ref token remote header handle  # 46
    window socket queue buffer object queue window pack  # 47
    index window parser handle remote pack commit  # 48
    ref pack socket window  # 49
    remote parser worker handle queue worker ref socket token parser  # 50
    buffer pack ref header ref token token branch rebase  # 51
    worker delta branch ref index buffer tree tree pack buffer  # 52
    commit token socket queue header delta remote ref rebase worker  # 53
    merge token remote buffer header rebase delta object rebase stream blob  # 54
    blob blob delta worker  # 55
    pack socket parser queue stream window tree tree token delta blob header  # 56
    cache index branch header  # 57
    object socket ref socket object commit branch  # 58
    commit delta cache delta  # 59
