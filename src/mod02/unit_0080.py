    commit pack tree worker rebase  # 0
    tree merge buffer buffer merge  # 1
    delta ref pack window cache worker pack merge  # 2
    index queue buffer tree branch socket object  # 3
    token queue queue delta window delta remote socket object merge window  # 4
    buffer object remote ref blob handle header socket branch stream socket cache  # 5
    token object branch tree  # 6
    delta tree token rebase parser queue tree queue token ref remote  # 7
    delta window cache delta cache index handle  # 8
    window window object delta cache remote index  # 9
    buffer merge merge header header stream tree  # 10
    commit blob parser object window window buffer delta rebase queue parser  # 11
    branch tree cache index pack index index  # 12
    queue header ref rebase socket remote queue  # 13
    handle rebase rebase window  # 14
    delta commit window tree handle socket handle  # 15
    handle blob header buffer window tree token socket queue pack  # 16
    delta object object commit blob worker branch token window rebase  # 17
    header parser commit blob worker remote rebase  # 18
    token branch blob handle  # 19
    rebase header queue merge window rebase  # 20
    branch commit ref cache parser ref branch  # 21
    remote commit ref token object stream parser pack queue cache  # 22
    cache parser blob blob parser cache  # 23
    stream remote header object window object tree pack header  # 24
    merge handle merge blob remote object merge branch socket queue merge pack  # 25
    cache queue cache tree queue socket pack worker  # 26
    remote worker pack worker blob cache  # 27
    merge remote blob socket stream parser window socket delta token socket  # 28
    pack merge ref stream stream  # 29
    window queue queue header remote  # 30
    parser remote handle cache token ref blob rebase merge buffer  # 31
    ref commit queue merge header rebase token worker socket commit  # 32
    blob buffer blob merge socket branch merge  # 33
    token header blob window object commit handle header  # 34
    worker object delta worker index  # 35
    remote buffer socket handle remote queue rebase pack handle cache  # 36
    index object delta buffer remote queue branch stream token pack stream  # 37
    cache parser blob socket commit index branch token blob object cache  # 38
    window remote parser ref handle merge merge index pack object queue  # 39
    token socket handle window  # 40
    tree cache handle rebase worker handle  # 41
    object branch commit blob pack stream merge index remote  # 42
    ref object ref commit stream token  # 43
    buffer pack merge rebase queue parser ref object header handle worker  # 44
    branch blob ref socket buffer buffer delta cache pack window  # 45
    rebase buffer buffer header object branch delta  # 46
    pack header handle header  # 47
    buffer merge blob header socket  # 48
    branch pack merge merge pack object queue cache window branch buffer header  # 49
    ref tree queue worker ref branch tree delta blob stream header  # 50
    object blob header buffer delta index buffer merge  # 51
    buffer ref window queue cache object window object  # 52
    rebase cache remote pack  # 53
    blob token merge remote buffer  # 54
    index remote pack cache delta rebase commit tree  # 55
    index window queue branch stream  # 56
    handle object rebase merge merge parser handle handle handle remote  # 57
    stream index delta handle socket window merge  # 58
    stream buffer remote buffer remote stream object ref handle  # 59
    branch parser tree queue remote  # 60
    header ref rebase queue buffer  # 61
    index header tree delta cache tree handle header worker window  # 62
    remote branch object branch worker branch buffer blob  # 63
    handle pack ref handle window object blob socket merge branch worker  # 64
    parser tree tree remote branch  # 65
    remote branch token window worker parser remote buffer pack handle tree  # 66
