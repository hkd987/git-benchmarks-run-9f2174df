    token parser header merge stream tree rebase stream remote  # 0
    blob socket index window  # 1
    object index remote ref parser branch window pack merge commit  # 2
    branch branch tree pack object buffer cache handle commit ref pack header  # 3
    cache parser parser token socket  # 4
    cache ref parser pack tree header buffer pack tree  # 5
    remote worker window rebase branch cache socket rebase index rebase blob  # 6
    delta delta commit ref window socket index  # 7
    token pack tree buffer handle remote parser branch ref  # 8
    rebase commit stream worker blob worker queue  # 9
    tree pack socket handle worker pack tree header  # 10
    index handle delta pack commit token pack cache queue  # 11
    ref pack header parser buffer pack blob parser delta buffer  # 12
    object commit window commit rebase worker header stream blob handle buffer  # 13
    remote header branch parser socket queue socket  # 14
    object commit blob header index rebase pack queue handle remote token handle  # 15
    object rebase commit rebase index header index queue  # 16
    token worker buffer object header tree handle handle buffer header worker  # 17
    handle token worker ref queue index buffer socket parser  # 18
    merge blob object buffer queue commit queue window rebase token pack branch  # 19
    merge delta window index header parser buffer blob stream delta  # 20
    header tree pack socket token header commit header header merge handle  # 21
    rebase stream branch window worker object parser pack blob index stream object  # 22
    object index handle index stream handle cache  # 23
    branch ref socket object pack  # 24
    ref queue pack branch commit queue handle blob merge tree socket  # 25
    blob delta header header parser commit parser remote  # 26
    object remote index merge commit tree remote ref cache worker  # 27
    commit token stream worker blob buffer merge pack token header  # 28
    branch queue rebase queue delta token worker  # 29
    pack buffer cache tree socket queue cache ref cache branch index worker  # 30
    pack handle object worker merge header parser header  # 31
    pack handle buffer buffer worker branch  # 32
    merge commit buffer index socket worker index window commit header branch  # 33
    queue parser ref cache rebase token  # 34
    handle handle stream remote pack header commit window  # 35
    delta blob rebase delta commit merge parser cache merge rebase delta  # 36
    merge queue object blob ref window  # 37
    branch blob worker socket object branch handle blob  # 38
    rebase blob branch merge pack queue parser remote index rebase socket  # 39
    tree merge merge blob blob delta rebase stream branch token  # 40
    commit handle token header  # 41
    ref cache header delta parser  # 42
    handle worker buffer socket remote header branch blob stream merge remote  # 43
    window object branch parser  # 44
    header buffer tree parser remote  # 45
    commit blob object parser buffer worker delta socket ref  # 46
    branch rebase tree object token merge object branch tree socket queue  # 47
    tree blob cache tree pack socket pack queue merge cache remote pack  # 48
    tree tree ref blob pack merge rebase rebase merge worker  # 49
    remote token branch ref  # 50
    header token object socket buffer parser branch object window header remote  # 51
    buffer ref branch header object worker tree  # 52
