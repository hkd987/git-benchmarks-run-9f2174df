    branch remote handle ref ref cache stream token socket delta commit handle  # 0
    delta token commit header handle  # 1
    buffer branch buffer branch socket  # 2
    commit parser socket index  # 3
    worker pack queue remote tree pack commit rebase pack buffer commit  # 4
    handle tree rebase index token tree rebase window  # 5
    buffer parser header window parser  # 6
    remote object parser delta handle stream merge ref parser branch tree  # 7
    parser pack blob window parser ref socket  # 8
    cache blob queue delta branch queue delta  # 9
    worker worker window worker worker rebase  # 10
    object stream commit cache pack merge header blob  # 11
    handle queue remote parser  # 12
    commit socket window branch object delta blob pack remote blob handle  # 13
    handle blob handle socket token buffer blob tree merge delta  # 14
    object socket token commit queue remote delta object  # 15
    index index object index  # 16
    commit socket commit tree remote socket branch remote worker  # 17
    worker rebase ref tree token buffer ref cache window  # 18
    queue cache rebase delta parser token  # 19
    commit branch commit index handle cache stream header socket object commit pack  # 20
    index socket worker queue worker socket index header header commit commit  # 21
    window tree worker pack socket token rebase  # 22
    blob worker commit cache index commit cache rebase  # 23
    worker window token tree  # 24
    branch object tree cache handle ref commit rebase index rebase object worker  # 25
    commit rebase blob pack header blob handle handle token ref tree ref  # 26
    object worker cache delta stream stream delta parser tree branch socket tree  # 27
    tree header header token header blob ref object commit branch socket  # 28
    rebase cache cache tree socket delta pack object remote branch pack cache  # 29
    branch queue rebase remote queue  # 30
    socket header header rebase commit  # 31
