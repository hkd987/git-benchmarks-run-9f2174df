    cache window remote parser cache parser commit  # 0
    remote tree branch index socket parser tree handle cache pack  # 1
    handle object object handle stream window window header  # 2
    merge parser branch queue stream index handle merge queue index  # 3
    commit worker buffer delta header stream index blob  # 4
    token token index blob parser cache worker worker branch  # 5
    remote branch ref socket queue cache  # 6
    commit cache branch handle blob commit  # 7
    worker socket handle queue window tree object buffer buffer object rebase  # 8
    blob buffer handle token token worker stream token cache index handle window  # 9
    queue window branch rebase merge blob token handle header handle token cache  # 10
    worker cache tree merge  # 11
    delta blob rebase parser parser buffer object tree header  # 12
    token merge handle worker ref  # 13
    cache pack buffer blob delta  # 14
    token parser token buffer branch window index tree  # 15
    pack parser parser stream  # 16
    branch rebase stream worker parser socket cache  # 17
    merge window rebase window remote header  # 18
    pack parser commit worker cache window  # 19
    cache object socket queue stream ref commit branch  # 20
    socket pack buffer socket worker index rebase buffer  # 21
    socket rebase tree commit commit handle handle window token buffer pack  # 22
    commit tree blob object queue cache remote rebase worker  # 23
    commit object queue pack  # 24
    blob delta remote remote object delta merge header cache cache worker branch  # 25
    buffer header ref worker branch commit branch  # 26
    branch ref worker delta commit parser ref cache remote cache header  # 27
    parser object index delta  # 28
    commit object buffer worker blob parser delta header worker index  # 29
    queue buffer worker branch commit token  # 30
    stream cache worker branch rebase handle stream socket merge  # 31
    tree remote socket token rebase queue  # 32
    stream worker delta handle handle worker tree  # 33
    rebase delta remote worker stream merge  # 34
    socket delta merge blob  # 35
    socket worker index rebase handle window handle socket pack merge  # 36
    merge stream commit queue merge cache token  # 37
    blob worker parser queue object parser tree socket worker pack  # 38
    worker commit remote handle blob queue branch socket header tree branch delta  # 39
    merge merge delta stream worker delta queue tree branch  # 40
    delta handle blob parser queue blob buffer commit  # 41
    delta object index window object commit window  # 42
    socket object merge delta  # 43
    window queue tree buffer object  # 44
    object rebase socket queue object  # 45
    blob window header queue merge window branch remote  # 46
    header tree index header remote buffer pack  # 47
    queue stream window parser pack window object object cache  # 48
    commit window header index socket object token object  # 49
    socket queue index delta queue stream buffer ref handle stream window  # 50
    remote ref delta socket delta object blob delta cache worker tree  # 51
    worker branch object buffer remote index merge header handle  # 52
    socket socket branch object handle queue merge window index worker  # 53
    stream commit remote queue header parser handle stream  # 54
    parser pack ref window stream commit parser window  # 55
    token commit queue queue branch remote remote tree tree tree cache blob  # 56
    parser index delta worker branch branch object handle  # 57
    object window commit ref window tree blob queue commit index worker rebase  # 58
    index index parser pack parser merge queue blob tree socket merge  # 59
    cache header token remote parser  # 60
    rebase object cache rebase token remote rebase stream pack  # 61
    stream buffer tree merge commit index ref remote cache pack  # 62
    pack cache index branch stream cache pack blob  # 63
    pack handle socket remote blob buffer branch  # 64
    window cache tree cache branch index delta cache stream pack  # 65
    cache branch pack buffer header commit buffer window commit rebase  # 66
    handle merge stream buffer parser window header branch queue header blob parser  # 67
    blob handle remote queue handle header index object rebase delta commit cache  # 68
    remote pack pack pack merge object  # 69
    queue buffer window object  # 70
    cache delta object rebase commit cache socket stream parser delta  # 71
    blob pack socket parser parser blob token pack commit handle parser window  # 72
    remote index ref stream stream cache parser stream index header handle  # 73
    header branch ref parser blob window ref tree  # 0
    worker header object socket object queue rebase handle buffer merge socket tree  # 1
    remote queue tree worker delta buffer parser merge rebase tree  # 2
    pack remote tree socket  # 3
