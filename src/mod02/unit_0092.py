    remote index remote parser index parser queue header tree commit  # 0
    token token commit branch remote index buffer  # 1
    index ref remote parser token pack queue stream header object window rebase  # 2
    ref branch commit worker delta cache header  # 3
    worker ref remote delta  # 4
    blob tree header queue object queue buffer stream stream  # 5
    branch handle stream tree branch queue index socket object  # 6
    merge blob queue commit pack handle  # 7
    remote socket cache pack worker commit ref object header socket stream  # 8
    window window object header token ref ref ref  # 9
    branch blob rebase remote cache worker blob index worker window  # 10
    buffer blob token delta merge pack stream blob worker header  # 11
    worker cache handle worker buffer  # 12
    handle header stream delta pack rebase pack window buffer token commit  # 13
    stream header parser object  # 14
    object handle socket commit window merge remote header branch remote cache object  # 15
    token delta branch stream object tree index branch  # 16
    pack blob index ref remote branch token  # 17
    header buffer commit object commit blob tree header blob pack pack  # 18
    stream handle socket parser stream window cache socket buffer tree cache  # 19
    merge pack worker merge remote delta rebase ref  # 20
    header window delta merge  # 21
    index remote buffer queue index commit worker object  # 22
    branch object remote cache ref worker branch remote  # 23
    token merge queue merge token worker buffer commit  # 24
    parser rebase index branch socket  # 25
    parser pack socket object index ref worker token  # 26
    buffer object worker handle header index stream window buffer  # 27
    pack commit rebase token stream window  # 28
    tree ref window cache cache buffer pack remote socket object worker worker  # 29
    cache index rebase remote header header commit object branch  # 30
    branch index socket object  # 31
    cache pack stream index buffer socket buffer handle tree handle token buffer  # 32
    handle header window pack index header header cache merge  # 33
    socket handle tree stream stream window pack header  # 34
    cache index delta merge  # 35
    delta buffer delta stream pack socket index worker worker delta parser  # 36
    merge socket buffer rebase object token ref worker queue commit  # 37
    branch tree parser stream blob delta tree window merge commit queue object  # 38
    blob object token cache ref buffer parser  # 39
    token worker delta remote socket object rebase blob tree ref delta socket  # 40
