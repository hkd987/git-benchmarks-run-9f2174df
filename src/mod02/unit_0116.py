    ref rebase index delta  # 0
    pack queue cache buffer object socket window window pack pack  # 1
    window window ref blob cache tree object handle worker  # 2
    pack tree object index handle pack commit ref stream queue parser rebase  # 3
    commit buffer index commit object pack cache blob window queue buffer blob  # 4
    window branch token index index  # 5
    buffer remote rebase header handle window tree window queue  # 6
    merge handle stream window index ref blob worker  # 7
    branch queue rebase delta token  # 8
    header worker buffer window cache handle ref index delta ref object  # 9
    index window object socket remote tree rebase  # 10
    ref token merge ref token pack pack stream commit token commit buffer  # 11
    cache rebase handle parser stream header index commit  # 12
    parser object rebase remote ref branch header stream ref handle  # 13
    queue queue parser window  # 14
    token rebase pack handle  # 15
    token socket stream pack queue queue worker branch  # 16
    buffer window object ref commit handle rebase  # 17
    token buffer handle remote cache merge cache commit buffer header rebase  # 18
    delta worker commit header commit branch ref buffer  # 19
    parser stream blob handle worker window rebase  # 20
    branch queue buffer tree token socket ref rebase stream  # 21
    blob delta handle token  # 22
    branch commit cache pack merge buffer worker buffer remote handle pack index  # 23
    parser handle branch delta commit socket  # 24
    cache branch commit rebase handle pack worker remote queue socket merge handle  # 25
    header branch worker branch tree merge index rebase worker buffer window  # 26
    buffer blob buffer buffer stream token merge pack merge pack  # 27
    worker remote window pack window parser stream token  # 28
    ref ref worker worker object remote token socket parser  # 29
    blob commit handle branch merge ref queue socket stream blob token merge  # 30
    parser remote object worker stream commit tree worker index parser buffer  # 31
    ref window index cache handle stream object socket commit rebase  # 32
    remote handle index blob delta tree rebase handle buffer rebase handle header  # 33
    handle worker blob queue index delta worker blob  # 0
    stream merge worker rebase header buffer rebase  # 1
    token buffer commit ref remote  # 2
    token blob index index header socket  # 3
    merge window cache tree branch handle queue rebase window buffer token  # 4
    branch remote cache window index queue merge object remote delta  # 5
    window ref remote header token merge cache rebase  # 6
    tree window socket ref queue  # 7
    delta parser merge token header rebase ref remote object blob tree socket  # 8
    stream window parser commit commit ref tree parser commit stream  # 9
    window ref blob cache header cache  # 10
    worker parser handle parser queue socket socket blob merge ref branch buffer  # 11
    header header worker merge pack buffer object  # 12
    buffer cache object queue index  # 13
    pack rebase tree window  # 14
