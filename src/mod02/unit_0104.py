    socket window token queue merge tree tree  # 0
    stream queue window token stream pack rebase tree tree  # 1
    blob rebase window rebase worker pack blob  # 2
    rebase header parser header stream commit blob queue window tree parser  # 3
    rebase commit object cache remote  # 4
    commit object commit socket stream stream  # 5
    blob socket queue buffer merge stream index  # 6
    rebase token cache remote buffer buffer header  # 7
    handle object queue queue header worker delta worker index  # 8
    handle index pack worker commit window worker queue queue header  # 9
    commit tree delta window window blob object rebase window buffer pack  # 10
    worker token cache rebase parser rebase queue  # 11
    stream parser blob socket socket stream  # 12
    index object worker cache index rebase tree stream buffer  # 13
    worker ref merge remote rebase tree  # 14
    blob parser stream stream parser tree branch merge stream  # 15
    tree object tree branch  # 16
    branch merge stream window tree  # 17
    commit rebase header object commit parser buffer handle parser handle header token  # 18
    handle header blob socket window token header index window branch buffer  # 19
    pack remote token delta object index cache ref commit rebase branch remote  # 20
    blob buffer object delta  # 21
    cache remote rebase index parser token delta buffer pack buffer socket  # 22
    rebase stream commit branch header merge socket token  # 23
    merge remote socket buffer branch pack cache rebase delta handle window  # 24
    merge pack delta index queue branch commit rebase socket merge header cache  # 25
    stream window rebase merge socket token rebase cache  # 26
    object tree ref buffer cache buffer  # 27
    rebase tree cache rebase socket window handle pack commit  # 28
    delta blob queue window window handle merge window  # 29
    index index remote pack object window blob handle buffer delta tree  # 30
    buffer index stream queue ref merge stream window remote parser ref  # 31
    blob cache remote blob window  # 32
    delta branch window tree blob tree blob  # 33
    pack queue index ref  # 34
    handle socket socket ref buffer object  # 35
    rebase ref object rebase queue worker pack  # 36
    index header worker worker object worker  # 37
    remote queue cache worker tree ref delta ref cache  # 38
    index pack ref handle blob token window  # 39
    ref parser header remote handle object socket commit pack remote commit branch  # 40
    handle buffer token socket pack ref socket buffer merge index branch delta  # 41
    window window worker remote rebase index queue window queue  # 42
    stream blob remote buffer index branch worker  # 43
    rebase token blob header delta stream worker buffer socket  # 44
    worker tree buffer merge token merge  # 45
    header commit index delta socket blob handle tree parser delta pack rebase  # 46
    rebase delta pack token buffer delta tree delta buffer window  # 47
    merge delta stream token stream window tree  # 48
    object branch window worker parser header  # 49
    remote branch ref socket commit pack parser object worker cache socket queue  # 50
    ref merge merge delta rebase buffer rebase tree rebase merge window index  # 51
    remote worker object handle ref queue  # 52
    object parser worker stream delta cache ref commit delta ref ref delta  # 53
    worker socket stream delta buffer index parser ref delta buffer  # 54
    worker stream stream handle object  # 55
    blob object ref rebase socket header handle  # 56
    worker branch delta buffer token ref  # 57
    merge handle ref buffer queue tree tree worker rebase worker cache merge  # 58
    pack queue merge rebase delta buffer cache merge queue queue  # 59
    index tree token object  # 60
    ref remote pack parser commit tree token worker tree blob cache  # 61
    ref object queue merge commit rebase worker object commit tree  # 62
    window ref delta token worker socket stream branch  # 63
    blob ref branch queue buffer ref header token stream branch delta  # 64
    cache commit stream queue token merge header commit  # 65
    tree tree window index merge  # 66
    header header ref branch queue socket  # 67
    rebase delta remote remote token index  # 68
    queue worker blob delta blob merge rebase object  # 69
    rebase delta token socket window remote pack header pack cache  # 70
    socket pack parser ref delta commit  # 71
    index handle tree header remote worker index  # 72
    pack stream buffer parser window branch socket buffer blob commit  # 73
    delta header ref remote  # 74
    handle commit merge queue merge queue merge ref rebase  # 75
    token remote remote pack index branch branch remote handle object delta buffer  # 76
    index rebase socket merge token remote delta object  # 77
    tree ref branch window delta parser tree delta token delta window  # 78
    handle socket handle rebase commit merge window worker delta  # 79
    blob object remote buffer worker queue ref stream  # 80
    merge pack stream merge header merge parser merge pack token object header  # 81
    rebase delta branch tree stream index window header  # 82
    buffer blob rebase window socket remote  # 83
    rebase tree object window worker window remote  # 84
    stream rebase tree object ref handle  # 85
    object header ref tree rebase remote header window window parser worker pack  # 86
    object pack blob socket  # 87
    buffer delta buffer header queue queue remote branch  # 88
    merge branch pack merge worker window queue tree  # 89
    handle header ref parser commit parser handle cache header  # 0
    header branch cache tree remote branch buffer tree  # 1
    ref index rebase handle token header  # 2
    merge rebase token parser header remote branch  # 3
