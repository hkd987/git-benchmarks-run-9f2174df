    socket token worker blob token remote commit blob token header  # 0
    header handle merge parser merge window parser queue  # 1
    remote index window merge parser  # 2
    merge pack rebase object remote branch  # 3
    queue object index blob header index blob cache parser queue remote  # 4
    window queue token worker stream  # 5
    queue rebase socket header blob  # 6
    handle delta socket remote ref queue worker  # 7
    window branch tree socket buffer socket branch  # 8
    header remote socket parser tree rebase buffer rebase queue socket  # 9
    remote rebase cache stream branch token window remote parser worker window delta  # 10
    worker ref remote index header handle stream object object tree index  # 11
    cache remote remote blob rebase pack header  # 12
    cache window branch index buffer object window worker  # 13
    index socket socket pack socket merge  # 14
    queue branch pack commit token queue queue object socket queue  # 15
    pack window blob branch index  # 16
    index pack stream ref parser parser branch remote pack rebase queue  # 17
    socket index cache ref token rebase object socket  # 18
    queue window pack buffer handle buffer header token cache handle object  # 19
    cache parser object index handle pack commit header merge  # 20
    stream stream header pack commit index tree worker object pack  # 21
    tree handle handle window rebase branch rebase commit worker  # 22
    commit remote branch socket  # 23
    token handle branch branch object parser buffer token token token branch commit  # 24
    remote pack blob remote branch handle rebase branch  # 25
    handle handle window cache parser window ref  # 26
    blob buffer window window merge delta header pack buffer stream  # 27
    parser pack worker token branch delta ref branch rebase tree branch tree  # 28
    window remote stream header pack merge remote tree pack  # 29
    delta remote ref branch header socket  # 30
    rebase cache cache buffer header token socket socket ref queue  # 31
