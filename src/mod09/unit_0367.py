    parser header commit tree token ref cache commit commit remote buffer branch  # 0
    commit cache object pack parser object  # 1
    parser ref token window remote remote header cache remote tree  # 2
    ref cache merge cache blob tree parser object  # 3
    token rebase object header window stream commit worker  # 4
    commit blob blob object  # 5
    pack remote buffer blob delta  # 6
    socket header window object commit index commit rebase delta worker object  # 7
    merge queue worker pack commit  # 8
    handle header rebase rebase blob queue branch  # 9
    stream handle blob pack index delta worker cache commit  # 10
    remote branch tree pack worker parser stream merge cache  # 11
    blob cache ref index blob rebase socket object  # 12
    cache cache pack handle rebase stream window cache delta  # 13
    cache index blob blob delta  # 14
    stream rebase ref token parser merge remote remote blob delta parser remote  # 15
    buffer commit window window commit tree tree rebase delta commit blob remote  # 16
    token branch cache buffer remote queue buffer object rebase object handle  # 17
    queue branch remote parser queue window pack blob  # 18
    queue tree commit socket stream parser  # 19
    index worker cache remote queue parser delta tree window merge commit queue  # 20
    commit remote remote commit merge blob stream  # 21
    commit socket branch blob cache window commit tree commit  # 22
    commit index token worker branch queue header header ref worker  # 23
    parser remote merge queue blob delta blob delta handle token index  # 24
    pack pack token window branch  # 25
    ref tree merge delta stream socket  # 26
    buffer blob handle rebase commit window worker branch socket token  # 27
    merge window parser handle commit remote commit  # 28
    ref tree object remote  # 29
    branch merge merge handle branch pack index pack index ref handle  # 30
    index pack tree ref branch worker branch socket handle rebase delta  # 31
    stream token object branch token header pack  # 32
    remote header pack token cache window handle index queue branch  # 33
    parser handle delta delta  # 34
    tree stream token header merge  # 35
    blob queue queue handle buffer  # 36
    stream remote stream header  # 37
    tree stream queue blob socket buffer merge stream pack parser  # 38
    worker stream rebase token blob ref handle index  # 39
    token window buffer handle window tree tree  # 40
    cache window queue tree handle commit ref stream tree buffer  # 41
    cache rebase merge parser  # 42
    handle ref buffer stream branch merge handle blob commit buffer rebase  # 43
