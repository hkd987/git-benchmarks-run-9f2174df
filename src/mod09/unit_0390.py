    parser socket index index delta  # 0
    delta commit delta header merge ref object ref  # 1
    ref header socket tree window blob delta socket object  # 2
    delta rebase rebase tree rebase blob worker blob  # 3
    worker socket cache delta commit handle header delta socket header token  # 4
    token index delta tree  # 5
    token header remote window header header  # 6
    worker worker index socket tree branch merge buffer  # 7
    blob worker socket blob  # 8
    socket merge window object cache tree  # 9
    remote parser buffer blob buffer remote queue stream handle remote  # 10
    header rebase commit blob commit  # 11
    delta ref object handle pack socket merge merge window  # 12
    tree buffer object stream socket cache tree pack cache header tree pack  # 13
    commit commit tree tree  # 14
    parser merge blob commit stream  # 15
    window remote commit object  # 16
    commit remote ref ref rebase object branch window stream blob pack index  # 17
    merge merge branch header  # 18
    tree branch tree object merge delta token branch delta ref index  # 19
    window socket window token cache commit index  # 20
    commit queue index queue pack  # 21
    merge handle cache queue worker ref token worker queue branch  # 22
    worker index parser blob header stream branch cache ref pack worker socket  # 23
    queue tree ref window remote branch worker tree tree stream  # 24
    commit pack index branch  # 25
    ref branch branch tree commit remote parser cache socket branch pack window  # 26
    header merge cache branch handle index stream  # 27
    stream branch cache header index rebase header window pack  # 28
    merge branch parser socket window branch tree parser worker  # 29
    rebase merge header window blob queue pack header  # 30
    commit rebase cache pack  # 31
    merge object queue queue index cache parser header  # 32
    socket cache tree object header remote parser worker buffer worker branch remote  # 33
    ref delta queue parser tree tree  # 34
    parser cache remote tree rebase  # 35
    worker commit blob delta merge window handle socket cache commit blob  # 36
    pack header tree socket object merge token buffer delta  # 37
    token stream header token index delta cache merge  # 38
    queue header pack index queue tree worker buffer branch stream worker commit  # 39
    branch index delta worker worker merge  # 40
    branch commit remote stream  # 41
    object tree parser worker merge object buffer  # 42
    ref header handle remote blob parser window  # 43
    blob index delta stream socket tree merge remote  # 44
    parser window socket tree socket merge socket pack token handle object  # 45
    token header header header token  # 46
    delta object pack buffer branch header token delta ref handle remote  # 47
    tree handle window socket  # 48
    handle queue rebase parser object commit blob header object  # 49
    token index parser blob index parser tree  # 50
    queue blob branch object blob tree stream header  # 51
    pack buffer cache merge  # 52
    blob rebase parser tree  # 53
    branch index handle merge delta window merge token object tree  # 54
    merge buffer socket commit stream delta header index ref worker  # 55
    window worker handle tree parser  # 56
    token object socket header parser token worker remote worker  # 57
    delta handle rebase rebase  # 58
    token token buffer commit buffer token header tree tree cache header delta  # 59
    delta socket ref handle queue socket pack parser queue branch branch buffer  # 60
