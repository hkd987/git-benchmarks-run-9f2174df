    tree ref delta queue ref rebase  # 0
    delta blob window queue tree  # 1
    header cache buffer socket blob window object commit delta worker rebase  # 2
    queue pack handle window token merge token stream  # 3
    index pack parser branch ref buffer blob window  # 4
    commit stream blob rebase merge queue tree index pack  # 5
    worker object delta tree ref branch object  # 6
    index rebase ref queue worker branch commit  # 7
    parser tree commit object socket parser socket handle parser  # 8
    index ref remote pack stream  # 9
    handle header rebase parser worker stream branch cache worker index handle tree  # 10
    handle blob commit buffer handle object merge window stream index commit  # 11
    blob index rebase ref blob ref token remote object  # 12
    header delta token cache tree handle pack pack parser worker token socket  # 13
    header parser cache index remote  # 14
    window tree tree worker remote handle cache pack remote blob cache token  # 15
    buffer cache remote buffer worker queue token merge queue object rebase  # 16
    socket cache handle cache worker pack merge parser handle  # 17
    remote object merge pack index remote token commit buffer buffer commit parser  # 18
    queue buffer delta pack parser  # 19
    handle remote pack queue header delta queue merge  # 20
    pack worker commit window tree object header queue cache branch ref worker  # 21
    pack buffer ref token queue window rebase commit worker rebase branch  # 22
    blob parser ref worker tree object tree remote merge window socket  # 23
    rebase parser token commit queue pack merge parser buffer handle branch handle  # 24
    remote stream stream delta  # 25
    worker branch socket ref rebase ref merge blob  # 26
    header socket pack cache header window commit  # 27
    tree ref queue rebase  # 28
    handle index token socket  # 29
    ref pack ref queue remote worker branch stream queue parser header  # 30
    window handle index handle handle header ref buffer  # 31
    rebase rebase header stream worker cache stream stream socket  # 32
    delta merge remote header stream cache  # 33
    socket tree worker header object parser merge header window socket pack object  # 34
    branch buffer token remote remote token merge remote commit rebase tree  # 35
    commit cache buffer ref stream buffer tree socket tree commit buffer merge  # 36
    remote remote pack worker branch branch cache ref parser commit object handle  # 37
    parser token commit window  # 38
    delta header blob window branch  # 0
    remote buffer delta index cache socket  # 1
    header branch token window object  # 2
    pack commit token remote token object ref worker merge object pack rebase  # 3
