    worker merge index worker token merge delta rebase  # 0
    tree delta index worker header tree worker rebase  # 1
    ref object window queue tree parser cache socket handle header commit window  # 2
    buffer pack tree merge blob parser stream object stream window tree handle  # 3
    rebase window handle ref index merge stream  # 4
    worker token worker object queue stream header rebase token header window  # 5
    handle token blob handle handle rebase parser index  # 6
    rebase window tree delta delta stream remote parser token  # 7
    blob stream blob tree remote stream  # 8
    buffer pack queue handle delta queue parser ref branch object  # 9
    cache parser rebase remote index window  # 10
    buffer worker rebase queue window header pack rebase blob commit token  # 11
    worker buffer delta blob merge index  # 12
    stream queue parser handle header object  # 13
    index cache socket index pack window branch stream blob  # 14
    commit parser parser pack commit object blob rebase ref parser cache commit  # 15
    token branch blob buffer remote stream worker worker header object merge  # 16
    socket blob handle handle handle blob  # 17
    worker worker remote worker token  # 18
    branch merge socket blob remote handle token handle  # 19
    worker header commit handle buffer tree  # 20
    remote cache remote cache stream cache index socket  # 21
    window ref pack remote commit branch  # 22
    stream handle branch queue  # 23
    handle token worker stream stream index token buffer  # 24
    worker branch handle header queue merge cache queue worker token  # 25
    blob commit pack handle ref ref header tree ref remote merge  # 26
    queue parser commit rebase token  # 27
    stream merge stream token object socket parser rebase cache  # 28
    commit index cache merge rebase window window delta token  # 29
    remote header object window handle handle branch  # 30
    blob buffer handle stream ref rebase  # 31
    buffer tree ref merge header header commit object worker rebase ref  # 32
    socket buffer pack rebase  # 33
    remote branch index branch socket queue rebase blob rebase cache  # 34
    pack branch tree cache handle  # 35
    pack remote merge merge window queue queue worker window worker  # 36
    tree header ref branch blob merge pack  # 37
    handle cache handle remote window worker  # 38
    pack index index commit blob token commit branch window handle pack  # 39
    commit delta blob rebase  # 40
    queue ref tree socket commit remote  # 41
    delta token commit worker parser delta socket  # 42
    cache tree remote worker blob delta  # 43
    commit handle token object queue branch parser handle branch worker token merge  # 44
    worker worker remote stream merge ref object  # 45
    delta cache rebase window  # 46
    merge index buffer branch stream queue buffer buffer commit object socket queue  # 47
    blob queue tree tree merge remote handle queue buffer pack queue  # 48
    buffer socket buffer rebase commit index rebase remote parser header header  # 49
    pack worker pack token  # 50
    merge ref parser remote commit buffer window  # 51
    socket ref merge index socket pack ref index merge tree  # 52
    rebase queue window window token  # 53
    window window socket commit  # 54
    merge header tree tree cache parser cache tree ref worker commit  # 55
    ref object stream object queue parser tree buffer tree object rebase  # 56
    queue delta ref index queue blob ref stream rebase cache  # 57
    pack handle handle index merge tree index  # 58
    rebase merge blob queue ref header stream worker merge commit window  # 59
    commit socket object handle  # 60
    buffer socket rebase parser pack  # 61
    ref socket ref token buffer  # 62
    parser ref handle object stream merge remote header rebase socket  # 63
    remote stream index remote queue parser branch index  # 64
    window blob worker ref worker cache  # 65
    stream worker ref pack handle rebase  # 66
    stream parser ref ref remote  # 67
    socket socket commit merge  # 68
    rebase handle token cache cache header parser cache  # 69
    pack handle pack header header worker cache merge pack header  # 70
    blob header branch ref tree header tree cache worker  # 71
    ref merge header worker  # 72
    buffer buffer socket branch delta pack delta stream branch  # 73
    queue rebase buffer queue queue stream  # 74
    socket socket delta buffer worker window buffer socket header object  # 75
    blob buffer parser buffer window window  # 76
    stream pack index socket socket remote index handle blob rebase  # 77
    rebase delta tree pack index queue stream blob commit queue  # 78
    ref commit pack worker branch token tree blob  # 79
    index header merge ref  # 80
