    window index socket index header index socket queue worker  # 0
    buffer blob queue parser delta  # 1
    parser merge blob socket  # 2
    remote object worker socket  # 3
    token worker cache socket  # 4
    token merge blob remote merge  # 5
    blob socket header commit object worker tree tree window commit header buffer  # 6
    branch socket merge socket cache parser  # 7
    blob worker parser window blob handle token  # 8
    stream blob object worker parser ref object queue worker  # 9
    pack index cache header branch index header object handle buffer index commit  # 10
    branch merge tree socket buffer  # 11
    token blob stream rebase merge handle parser token delta merge  # 12
    stream ref delta buffer tree delta handle  # 13
    cache handle header rebase worker merge window tree merge  # 14
    worker queue parser cache token  # 15
    delta pack token queue cache header worker rebase object branch object  # 16
    header rebase branch index handle object  # 17
    worker buffer queue buffer pack token index rebase object token  # 18
    branch socket tree queue header socket worker cache commit socket handle  # 19
    ref parser worker merge token branch rebase window remote header branch  # 20
    merge header remote socket buffer pack header blob cache  # 21
    header token token branch parser blob queue token queue pack ref commit  # 22
    index blob token delta buffer worker parser delta token queue  # 23
    object parser index header tree remote token token window worker window  # 24
    socket delta remote token cache object  # 25
    remote token header socket delta parser pack  # 26
    branch parser blob delta  # 27
    cache branch parser blob handle  # 28
    object remote blob object stream remote merge handle  # 29
    commit pack token header object ref header  # 30
    branch buffer delta pack buffer parser stream index header index remote  # 31
    socket remote window branch rebase merge worker merge window merge handle  # 32
    parser queue parser merge object  # 33
