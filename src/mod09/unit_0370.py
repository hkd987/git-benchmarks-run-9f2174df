    blob commit handle ref window remote buffer buffer pack delta stream  # 0
    buffer remote tree rebase handle  # 1
    object stream branch handle remote socket window buffer branch window tree token  # 2
    buffer index tree stream index merge tree handle  # 3
    object commit delta worker delta merge token  # 4
    branch commit token token queue window object  # 5
    index queue tree blob token token handle buffer  # 6
    object worker rebase header index parser buffer remote cache  # 7
    handle socket remote cache delta remote rebase object object handle  # 8
    handle pack header merge remote rebase  # 9
    header pack remote pack delta socket cache object ref tree  # 10
    window blob delta stream window remote header merge merge  # 11
    header parser merge stream remote handle window object  # 12
    object commit buffer index window tree cache queue pack  # 13
    handle blob header branch stream index object commit  # 14
    socket handle object socket ref header  # 15
    index window blob delta pack handle cache  # 16
    pack handle token blob tree cache blob socket header commit handle ref  # 17
    ref blob header blob token stream ref  # 18
    socket socket handle delta  # 19
    token merge worker window blob worker  # 20
    stream remote window commit object cache window index  # 21
    tree delta window handle  # 22
    parser rebase window pack tree ref token merge  # 23
    tree header stream tree  # 24
    remote merge commit commit token index object token  # 25
    delta rebase tree ref queue parser remote handle merge merge worker  # 26
    ref socket socket blob ref cache delta index  # 27
    queue branch object handle merge header commit branch pack remote handle  # 28
    queue delta stream merge delta header socket buffer worker remote  # 29
    stream commit window rebase object branch stream window rebase  # 30
    buffer merge handle queue object ref header  # 31
    blob socket remote branch pack delta ref queue header header stream  # 32
    pack header remote rebase commit blob header handle object  # 33
