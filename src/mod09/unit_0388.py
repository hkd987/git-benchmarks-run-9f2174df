    buffer queue index object header handle merge queue worker commit branch remote  # 0
    merge pack token parser stream commit handle header  # 1
    blob handle remote commit delta  # 2
    object window window commit  # 3
    ref remote parser object pack delta  # 4
    token blob blob commit pack blob remote rebase handle  # 5
    socket pack delta delta worker  # 6
    ref handle rebase ref buffer queue ref rebase remote handle stream worker  # 7
    socket remote ref handle remote window buffer queue  # 8
    blob socket handle handle  # 9
    remote ref stream buffer index index rebase window worker buffer  # 10
    socket index object socket commit window handle tree stream blob merge  # 11
    cache queue delta cache remote socket delta stream index tree  # 12
    parser header handle window window parser header buffer socket  # 13
    buffer header branch branch object  # 14
    blob delta token worker queue worker index  # 15
    blob queue blob pack ref window rebase cache branch  # 16
    remote token blob remote header window merge token commit commit queue stream  # 17
    pack queue index commit delta  # 18
    pack window parser queue tree  # 19
    delta ref delta delta buffer socket branch merge pack object header queue  # 20
    window branch queue pack ref blob worker tree  # 21
    branch branch token blob token commit pack handle stream  # 22
    worker cache commit stream token header branch rebase tree ref pack token  # 23
    header pack parser merge worker buffer parser ref pack branch rebase branch  # 24
    rebase header pack branch window  # 25
    index rebase index merge pack rebase socket handle delta handle merge  # 26
    ref queue remote merge merge cache stream handle socket tree  # 27
    socket token window handle index delta index  # 28
    blob merge buffer object  # 29
    worker tree merge socket parser token delta rebase  # 30
    token window token queue parser parser stream merge queue tree queue worker  # 31
    buffer index object socket pack buffer rebase socket  # 32
    window merge parser socket delta token handle stream stream  # 33
    parser ref window header commit stream  # 34
    parser merge worker pack pack tree handle socket blob  # 35
    token ref delta queue stream delta buffer header blob queue cache  # 36
    blob window token tree worker parser handle handle worker parser queue  # 37
    rebase remote header branch socket socket header merge  # 38
    ref commit ref blob handle pack merge ref object pack window socket  # 39
    buffer buffer queue rebase parser window queue merge merge remote blob  # 40
    buffer index worker cache handle worker socket merge window  # 41
    pack parser delta index delta blob commit commit socket branch socket stream  # 42
    cache queue merge branch delta delta window rebase blob  # 43
    tree header delta worker tree branch delta handle delta queue  # 44
    socket buffer token buffer token token object rebase parser buffer  # 45
    merge ref index index  # 46
    rebase stream window tree remote handle cache ref index handle merge window  # 47
    merge ref branch parser rebase handle commit  # 48
    cache index rebase tree token merge worker cache branch cache commit tree  # 49
    blob branch index handle buffer window token ref rebase queue stream branch  # 50
    object remote ref remote pack handle delta handle delta window  # 51
    delta buffer window stream window index remote cache commit worker commit  # 52
    index tree stream merge parser delta branch stream token  # 53
    ref pack stream queue commit ref handle worker rebase ref commit  # 54
    buffer ref ref index index commit handle buffer delta  # 55
    branch buffer queue token  # 56
    commit stream delta merge merge parser ref  # 57
    merge delta socket remote tree handle object cache branch cache  # 58
    index remote remote ref worker ref rebase cache  # 59
    token tree header handle queue buffer parser queue  # 60
    blob buffer delta handle handle merge branch commit ref worker cache stream  # 61
    buffer stream delta merge index object stream commit blob pack  # 62
    merge remote tree header  # 63
    header window object blob rebase cache handle commit rebase window branch handle  # 64
    ref pack rebase index ref header branch socket header  # 65
    ref buffer commit worker delta remote index delta  # 66
    socket parser branch queue remote worker socket  # 67
    index remote handle pack handle rebase rebase  # 68
