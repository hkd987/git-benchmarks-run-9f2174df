    blob socket branch merge header header object commit  # 0
    parser cache ref parser  # 1
    delta remote token delta tree buffer commit parser window  # 2
    stream blob merge socket  # 3
    parser tree blob delta cache handle token socket parser rebase  # 4
    index queue blob remote index header parser window socket token  # 5
    commit token buffer queue stream window token cache commit remote  # 6
    header branch index header handle handle stream worker socket  # 7
    index ref merge rebase rebase window stream  # 8
    parser handle rebase branch stream worker merge object  # 9
    object cache handle stream stream commit blob  # 10
    handle handle window buffer handle object cache object  # 11
    header branch ref buffer buffer rebase delta socket delta branch blob queue  # 12
    delta queue parser buffer stream socket  # 13
    pack object ref socket socket socket cache socket rebase  # 14
    index index ref blob  # 15
    window ref index parser commit socket blob ref buffer blob  # 16
    queue remote merge stream buffer  # 17
    handle merge index remote merge delta index cache blob commit  # 18
    handle merge cache rebase  # 19
    ref branch queue merge index pack pack handle commit window worker  # 20
    window worker merge merge delta stream remote header socket header pack  # 21
    queue merge remote socket queue tree rebase socket branch merge window remote  # 22
    socket remote handle buffer ref  # 23
    token ref header pack commit handle  # 24
    header worker queue socket branch pack worker merge index  # 25
    object rebase buffer buffer merge stream delta merge remote stream handle worker  # 26
    commit rebase merge worker queue  # 27
    window header tree buffer remote commit branch commit object  # 28
    branch pack index window rebase branch merge commit rebase index  # 29
    rebase stream buffer merge window  # 30
    socket delta branch tree merge token cache queue blob socket branch merge  # 31
    commit token delta branch object token handle index commit socket  # 32
    parser ref token index ref blob cache commit window queue rebase  # 33
    merge pack worker merge merge  # 34
    token buffer token header window socket worker rebase buffer ref header branch  # 35
    object commit window merge token branch window tree  # 36
    handle ref stream ref rebase delta tree pack  # 37
    ref worker remote merge rebase token  # 38
    parser rebase blob parser cache handle object remote  # 39
    index rebase remote commit  # 40
    buffer header commit branch buffer window  # 41
    ref pack token remote buffer header delta  # 42
    stream tree stream queue header remote socket  # 43
    rebase ref cache merge tree  # 44
    window queue stream merge  # 45
    delta header handle branch tree remote token pack cache delta object  # 46
    stream header delta stream  # 47
    stream object index blob parser worker  # 48
    merge worker blob rebase worker blob merge socket  # 49
    token rebase buffer merge socket ref index rebase  # 50
    pack ref branch object  # 0
    header cache remote stream delta socket  # 1
    header worker window index ref ref socket worker merge socket buffer  # 2
    blob tree tree blob parser index tree  # 3
    queue index remote handle socket queue worker window cache token parser stream  # 4
    token delta rebase index cache  # 5
    window socket tree remote parser window rebase cache rebase  # 6
    branch parser buffer commit tree pack handle index index remote  # 0
    parser worker rebase token window buffer rebase header token worker  # 1
    worker handle ref delta delta remote  # 2
