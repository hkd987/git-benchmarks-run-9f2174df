    commit cache header worker cache  # 0
    cache commit branch stream blob tree merge blob handle pack  # 1
    handle commit tree handle commit branch header object  # 2
    ref cache token ref merge window handle window  # 3
    commit buffer delta pack worker delta worker window handle worker index  # 4
    remote token delta handle ref branch worker cache  # 5
    blob branch remote parser  # 6
    delta stream ref rebase object index cache pack token merge  # 7
    rebase commit worker stream window branch pack cache index index tree  # 8
    parser branch buffer rebase rebase token object window remote  # 9
    token tree parser handle delta buffer cache  # 10
    ref tree commit buffer pack  # 11
    index object stream handle merge blob parser  # 12
    queue delta delta buffer  # 13
    pack commit remote blob handle  # 14
    queue branch ref handle remote handle token queue index  # 15
    rebase rebase token queue socket buffer parser handle blob ref ref pack  # 16
    merge commit cache window remote parser merge tree header remote buffer window  # 17
    header commit tree pack merge blob queue buffer tree  # 18
    branch buffer blob remote parser worker merge remote buffer  # 19
    branch branch tree object ref socket commit pack remote  # 20
    remote buffer socket commit  # 21
    rebase tree tree tree commit  # 22
    handle tree cache blob delta buffer header buffer  # 23
    header branch handle worker remote handle blob tree pack commit index remote  # 24
    remote index ref handle index object stream header header queue merge ref  # 25
    tree merge remote object worker  # 26
    branch object index handle stream pack commit handle  # 27
    delta stream branch buffer buffer merge socket cache queue socket  # 28
    stream commit header index token queue tree parser remote socket  # 29
    window window object ref parser  # 30
    blob branch merge buffer remote commit merge parser window  # 31
    stream blob tree remote token  # 32
    handle remote cache ref index object remote tree  # 33
    rebase token stream tree cache pack remote worker pack  # 34
    handle socket rebase header queue delta worker tree buffer socket handle ref  # 35
    handle delta merge tree  # 36
    blob ref stream branch header object  # 37
    parser stream merge parser buffer header delta queue rebase  # 38
    window socket rebase pack token header rebase handle remote socket handle  # 39
    parser token commit tree handle socket index handle  # 40
    token worker header parser  # 41
    socket cache ref stream socket merge merge  # 42
    pack header queue object  # 43
    merge worker index header object index  # 44
    rebase header blob worker tree  # 45
    handle parser worker header pack header handle queue  # 46
    branch merge token object branch window remote pack  # 47
    blob blob header queue token handle window  # 48
    header header delta handle parser socket queue remote merge  # 49
    index window branch index buffer branch socket handle  # 50
    header commit ref commit blob commit buffer pack pack ref remote  # 51
    window merge stream tree socket object remote queue  # 52
    pack index remote header merge buffer window socket  # 53
    object blob rebase socket object queue rebase ref  # 54
    pack queue delta token ref branch worker  # 55
    socket merge socket window  # 56
    ref branch delta ref queue ref stream  # 57
    socket rebase handle blob handle socket token branch stream commit cache  # 58
    window ref queue rebase object queue branch parser stream  # 59
    parser commit blob remote merge blob rebase rebase socket blob commit token  # 60
    queue worker tree object header header blob branch cache token queue tree  # 61
    header commit merge buffer rebase cache index  # 62
    pack token buffer parser branch socket remote branch stream header  # 63
    cache merge token worker index rebase object ref socket ref  # 64
    tree queue ref stream ref cache window window parser object  # 65
    tree commit remote blob ref worker parser index blob cache  # 66
    pack buffer delta blob commit delta pack merge  # 67
    parser parser buffer stream pack queue  # 68
    index token cache pack  # 69
    parser socket worker commit blob parser stream cache object ref  # 70
    worker index blob buffer handle  # 71
    rebase stream handle object worker buffer buffer window header cache ref blob  # 72
    handle blob object object buffer  # 73
    cache worker header index header ref cache window queue header parser object  # 74
    stream token handle token  # 75
    parser remote object worker  # 76
    pack cache remote window queue socket tree commit  # 77
    index blob handle rebase worker token merge delta queue remote worker  # 78
    blob handle handle index  # 79
    pack parser cache header token handle ref  # 80
    worker index ref stream token delta  # 81
    handle socket merge queue merge delta cache stream  # 82
    handle parser branch stream blob tree blob commit pack branch  # 83
    delta socket rebase cache rebase index rebase  # 84
    token stream delta object remote  # 85
    queue remote pack header socket header window pack index ref  # 0
    blob rebase buffer rebase branch branch window rebase tree buffer delta  # 1
    queue parser worker handle index object handle socket object delta  # 2
    queue window ref stream stream remote ref branch  # 3
    blob header socket object window  # 4
    tree tree merge window  # 5
    buffer parser blob ref worker commit merge header ref rebase parser  # 6
    cache delta remote header  # 7
