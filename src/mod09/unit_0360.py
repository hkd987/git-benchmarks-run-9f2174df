    branch cache queue branch merge window worker commit pack parser socket  # 0
    commit index parser index token object header  # 1
    delta queue commit parser cache header stream pack  # 2
    worker blob merge tree commit ref  # 3
    remote tree ref object window blob handle pack handle delta branch  # 4
    cache object cache pack pack rebase worker commit merge  # 5
    tree index object token  # 6
    merge handle merge merge queue commit object remote merge tree  # 7
    commit worker header handle pack parser commit  # 8
    delta token cache queue parser worker index commit token object delta tree  # 9
    commit queue merge rebase merge token stream ref stream branch tree  # 10
    index rebase index parser object queue token ref  # 11
    pack object blob parser token index  # 12
    blob object merge parser index worker parser  # 13
    queue ref blob blob index remote object  # 14
    queue header blob window  # 15
    merge rebase branch blob remote cache header commit socket delta branch rebase  # 16
    token cache token handle tree parser object cache worker parser  # 17
    buffer blob queue merge commit queue parser buffer socket queue parser socket  # 18
    stream window socket ref  # 19
    worker stream ref stream handle pack remote handle  # 20
    delta window stream window tree  # 21
    commit branch cache blob delta tree index branch parser socket cache  # 22
    buffer remote tree window  # 23
    token token branch merge token buffer header parser parser pack token buffer  # 24
    buffer remote handle parser cache parser  # 25
    handle branch cache header remote cache header parser index tree delta merge  # 26
    header worker object window index worker merge  # 27
    remote token buffer stream object commit stream branch index  # 28
    stream index remote delta header  # 29
    window object buffer queue merge queue  # 30
    branch header window object worker socket merge pack index  # 31
    rebase commit blob merge buffer rebase rebase commit  # 32
    worker handle merge socket ref ref index index commit socket  # 33
