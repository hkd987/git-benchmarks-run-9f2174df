    ref branch blob parser  # 0
    commit pack cache tree token handle  # 1
    pack handle branch blob worker ref pack branch cache delta  # 2
    window remote delta stream blob pack stream window queue pack  # 3
    parser window handle rebase delta window socket queue  # 4
    cache token window delta worker handle tree merge queue rebase  # 5
    branch cache ref parser rebase buffer parser commit remote  # 6
    index rebase object cache object  # 7
    index pack window header merge index handle parser  # 8
    branch parser branch cache buffer object object rebase rebase  # 9
    header worker rebase window remote cache queue buffer delta tree header  # 10
    header object branch worker handle remote  # 11
    blob handle delta parser token  # 12
    branch branch index rebase token object object rebase handle ref commit  # 13
    merge stream remote parser handle  # 14
    object pack delta handle queue queue  # 15
    index tree tree index stream parser merge buffer  # 16
    tree tree commit handle remote tree ref stream  # 17
    cache parser branch handle header commit remote  # 18
    handle cache cache merge branch blob object pack window token  # 19
    delta merge buffer delta handle header  # 20
    remote tree queue worker cache rebase index pack worker merge branch remote  # 21
    header blob stream ref index blob  # 22
    cache header commit object commit merge buffer commit  # 23
    delta parser rebase tree commit token ref remote  # 24
    cache tree blob queue socket handle  # 25
    ref rebase commit delta pack worker object queue rebase  # 26
    delta object index stream tree parser object remote socket branch  # 27
    cache tree commit commit handle delta socket header queue handle rebase ref  # 28
    merge remote header parser object header ref window buffer index blob delta  # 29
    buffer commit blob delta socket pack delta  # 30
    header header pack merge stream merge merge merge buffer stream header  # 31
    rebase queue blob branch branch delta branch rebase buffer object tree branch  # 32
    merge queue tree pack branch tree object window rebase  # 33
    worker tree cache merge branch queue token  # 34
    branch merge window queue token buffer remote queue  # 35
    window index worker handle handle object branch  # 36
    commit pack stream object header cache pack commit remote  # 37
    handle remote blob stream queue parser index  # 38
    window remote worker token tree handle ref cache  # 39
    remote window rebase blob stream  # 40
    commit ref object blob  # 41
    header cache tree commit window  # 42
    token socket tree stream window queue commit merge  # 43
    ref buffer tree delta queue window object parser stream  # 44
    remote cache worker header handle queue buffer commit  # 45
    cache blob window worker ref blob socket pack  # 46
    pack stream worker pack  # 47
    tree buffer tree worker ref merge  # 48
    cache queue buffer parser buffer blob handle  # 49
    socket header buffer merge remote branch handle  # 50
    worker blob branch branch token queue cache  # 51
    handle window commit tree object worker  # 52
    buffer header stream branch window  # 53
    commit queue object window remote socket buffer merge remote worker parser  # 54
    delta cache socket socket  # 55
    header pack cache socket header parser remote rebase delta  # 56
    stream branch cache cache remote  # 57
    tree object object commit parser  # 58
    rebase tree window queue handle handle commit queue  # 59
    remote window buffer header merge rebase blob handle worker  # 60
    pack blob commit object remote index tree  # 61
    branch pack rebase merge  # 62
    header merge queue buffer remote index delta  # 63
    handle blob header pack ref index stream socket tree cache header  # 64
    delta index window window tree object  # 65
    socket cache merge object delta parser pack tree header  # 66
    delta parser delta remote header branch ref object worker merge socket delta  # 67
    socket queue handle window  # 68
