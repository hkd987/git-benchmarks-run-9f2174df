    handle handle rebase handle parser ref branch  # 0
    queue delta object parser merge delta blob remote  # 1
    stream token rebase parser rebase rebase commit cache pack branch stream remote  # 2
    queue merge window remote remote cache  # 3
    queue ref header commit queue window  # 4
    rebase commit cache tree delta merge remote socket index tree stream stream  # 5
    branch delta merge merge socket  # 6
    header commit socket stream header token  # 7
    buffer stream blob commit handle  # 8
    object buffer delta token queue queue window token tree branch merge  # 9
    token rebase tree delta pack ref parser branch delta  # 10
    rebase rebase worker index  # 11
    header tree merge handle ref handle rebase  # 12
    merge buffer index cache blob branch remote queue socket tree cache blob  # 13
    blob stream ref window cache remote handle pack index blob blob  # 14
    socket buffer ref tree parser  # 15
    blob ref tree merge  # 16
    object queue stream object stream rebase parser stream tree  # 17
    window branch parser socket ref rebase buffer tree remote tree merge queue  # 18
    merge stream index socket object blob handle commit  # 19
    tree object worker object  # 20
    pack index rebase window handle index header ref cache ref  # 21
    object tree pack tree cache delta  # 22
    index cache queue remote handle token socket window handle queue handle  # 23
    branch rebase socket branch header index buffer  # 24
    tree cache merge object window blob index merge socket  # 25
    object token worker index window header rebase cache  # 26
    stream object index parser cache parser branch tree cache stream rebase  # 27
    token rebase blob commit socket ref cache tree commit  # 28
    queue handle token handle handle worker header rebase socket queue  # 29
    buffer parser ref ref remote  # 30
    object window window socket delta buffer blob parser merge branch commit branch  # 31
    index stream delta ref object socket window object pack window delta commit  # 32
    parser rebase queue parser delta socket socket merge branch  # 33
    window worker cache token handle merge cache rebase remote handle commit  # 34
    ref remote branch token cache cache object object merge  # 35
    handle ref socket tree delta buffer merge socket token  # 36
    token stream handle blob blob commit  # 37
    cache header commit queue header header tree window handle worker delta  # 38
    object merge delta cache socket object pack buffer  # 39
    worker token worker cache blob blob tree worker index pack  # 40
    remote ref header ref rebase socket queue delta tree remote header  # 41
    handle remote parser merge parser window tree delta header  # 42
    delta parser cache ref object branch stream pack branch  # 43
    buffer socket index token  # 44
    buffer commit window parser parser worker delta buffer  # 45
    commit rebase rebase buffer cache ref  # 46
    buffer window remote object object worker socket ref parser merge  # 47
    remote worker merge ref buffer stream pack cache  # 48
    remote remote tree token worker blob blob remote tree  # 49
    tree cache header branch header socket tree  # 50
    merge merge rebase handle remote blob queue handle  # 51
    token delta commit socket socket branch commit blob window remote rebase  # 52
    handle ref header pack parser index token merge branch rebase token  # 53
    object object blob handle tree buffer  # 54
    pack buffer branch merge delta worker window socket  # 55
    ref cache queue socket buffer merge delta cache parser commit  # 56
    header commit cache queue queue header handle header  # 57
    socket cache object branch tree token delta worker  # 58
    blob pack socket ref delta  # 59
    socket rebase blob socket window branch queue worker header handle commit  # 60
    ref ref remote window socket commit rebase  # 61
    pack delta index worker socket socket index header window rebase buffer  # 62
    header handle tree stream rebase token token  # 63
    queue parser parser cache buffer stream delta window index ref socket  # 64
    parser remote commit worker branch blob header handle rebase index  # 65
    blob worker stream stream token cache handle parser worker  # 66
    delta rebase blob rebase  # 0
    pack merge remote index handle buffer worker handle parser  # 1
    handle window stream stream handle  # 2
    object handle buffer branch socket window blob pack  # 3
    delta header handle commit pack handle socket tree  # 4
    merge socket blob tree remote  # 5
    object merge rebase object merge object pack object window  # 6
    worker window merge ref remote window cache worker socket  # 7
    parser socket delta handle remote  # 8
    remote stream rebase commit branch object merge window window socket token  # 9
