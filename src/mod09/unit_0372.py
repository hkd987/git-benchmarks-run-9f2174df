    cache handle queue cache blob  # 0
    merge handle stream tree  # 1
    handle remote buffer commit  # 2
    merge window index socket parser cache header branch header token  # 3
    worker object index cache stream window ref worker queue pack  # 4
    remote cache rebase token header delta token token  # 5
    delta stream remote socket stream remote  # 6
    branch remote handle parser commit rebase  # 7
    worker pack header delta tree cache rebase worker  # 8
    handle handle delta handle parser stream handle  # 9
    tree pack rebase buffer object  # 10
    pack commit socket ref ref  # 11
    branch buffer socket socket token commit stream ref handle  # 12
    remote rebase window ref remote queue branch tree window  # 13
    ref commit remote parser token header token object blob  # 14
    queue handle handle stream worker object tree window  # 15
    header ref merge blob index buffer commit  # 16
    window cache delta header  # 17
    handle header token remote handle blob ref token token object rebase  # 18
    cache blob rebase header  # 19
    stream merge queue ref stream pack stream stream ref  # 20
    buffer delta window remote header remote tree  # 21
    window header ref token header stream parser window index tree stream  # 22
    commit commit commit tree delta index stream  # 23
    worker header queue remote  # 24
    rebase cache buffer queue pack  # 25
    blob delta branch ref branch delta remote worker handle  # 26
    stream merge rebase index index tree tree index blob index  # 27
    index tree commit queue parser object merge socket commit branch commit  # 28
    window ref object cache cache delta queue ref worker rebase  # 29
    delta delta worker branch delta handle  # 30
    remote parser header blob worker parser blob buffer blob  # 31
    worker parser pack stream pack delta index  # 32
    cache handle cache merge index merge header merge window  # 33
    handle buffer buffer tree index cache window merge queue index delta  # 34
    worker pack queue ref remote worker header pack merge stream rebase worker  # 35
    branch queue header tree stream pack remote window queue  # 36
    stream index header parser object ref  # 37
    queue branch rebase tree token stream buffer pack token  # 38
    handle worker tree blob handle cache handle blob socket object branch parser  # 39
    pack remote queue socket object  # 40
    blob object worker remote index pack handle blob socket header token  # 41
    merge ref commit queue stream blob socket tree commit token  # 42
    delta pack header remote branch buffer buffer pack queue merge buffer  # 43
    buffer socket queue handle rebase branch queue index remote socket  # 44
    queue window delta parser buffer index remote  # 45
    index header ref token blob stream commit index ref queue  # 46
    remote queue worker buffer commit parser parser token parser header  # 47
    worker blob buffer queue  # 48
    blob parser handle cache  # 49
    merge index parser handle handle stream rebase remote token  # 50
    token pack socket parser buffer socket handle queue pack  # 51
    queue branch object parser worker header cache worker merge blob  # 52
    socket handle token worker object token  # 53
    delta delta commit merge tree merge worker worker handle  # 54
    handle object ref commit object parser pack handle window  # 55
    stream remote delta delta index  # 56
    buffer tree rebase parser rebase merge cache token rebase socket merge  # 57
    commit pack commit socket parser pack pack blob tree rebase stream worker  # 58
    delta remote handle delta worker token window  # 0
    socket index queue pack delta remote index buffer token worker queue  # 1
    tree ref branch queue header delta merge handle  # 2
    buffer blob window delta merge cache blob branch  # 3
    object object tree header remote branch queue worker pack merge socket branch  # 4
