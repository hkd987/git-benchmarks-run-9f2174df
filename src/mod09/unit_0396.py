    remote tree object stream blob buffer socket handle worker handle  # 0
    cache cache stream socket remote object  # 1
    socket header commit index token blob queue buffer object commit  # 2
    pack header socket queue worker  # 3
    handle tree cache merge token worker stream tree  # 4
    remote window index delta cache rebase parser handle token cache object  # 5
    branch queue window branch object window branch socket header cache  # 6
    object parser token header header token commit parser branch index  # 7
    rebase worker branch socket socket object worker buffer socket  # 8
    rebase tree cache worker blob window worker stream worker header  # 9
    buffer blob socket pack ref rebase  # 10
    pack socket blob ref cache  # 11
    parser socket socket index queue token header  # 12
    header ref window merge ref pack parser  # 13
    ref remote socket handle branch token cache queue object  # 14
    index queue ref window header parser handle stream queue  # 15
    tree token buffer stream remote  # 16
    cache object cache merge delta queue socket rebase buffer window  # 17
    tree pack buffer branch worker cache worker stream window buffer ref  # 18
    window cache queue merge delta blob blob pack branch queue  # 19
    tree commit object index rebase ref cache branch cache  # 20
    remote object rebase token object blob  # 21
    rebase ref parser rebase worker pack stream commit stream stream ref token  # 22
    commit stream socket handle index handle handle blob worker worker cache  # 23
    rebase parser socket window socket worker merge ref window  # 24
    header object handle stream blob pack stream rebase  # 25
    merge parser parser token  # 26
    ref remote remote index  # 27
    handle merge branch buffer tree object rebase header buffer queue branch  # 28
    window branch pack cache index window cache  # 29
    buffer index object blob ref rebase window  # 30
    parser cache commit index token merge header socket  # 31
    blob worker commit window parser rebase  # 32
    buffer parser object header blob index delta  # 33
    window window window merge delta branch queue  # 34
    handle queue window tree ref token blob  # 35
    header delta worker handle  # 36
    blob queue index delta cache stream object  # 37
    cache commit tree branch queue blob socket delta stream stream window header  # 38
    merge remote token ref buffer queue ref  # 39
    index stream branch handle queue  # 40
    parser rebase merge stream parser  # 41
    object ref rebase queue worker index blob  # 42
