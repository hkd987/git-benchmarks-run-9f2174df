    handle merge buffer header cache queue parser queue  # 0
    queue tree index handle object parser  # 1
    window token window object object header cache queue  # 2
    ref socket window parser stream pack worker object object  # 3
    remote token worker delta  # 4
    merge queue branch parser  # 5
    pack handle token socket remote  # 6
    index index token delta tree buffer  # 7
    cache window parser cache  # 8
    index object ref tree rebase stream worker  # 9
    object rebase branch window socket blob blob queue token object commit buffer  # 10
    commit handle parser ref parser delta branch remote  # 11
    parser branch tree rebase merge  # 12
    window ref handle parser handle rebase cache  # 13
    cache merge parser object ref header branch header worker token tree delta  # 14
    tree rebase ref token header buffer buffer cache branch  # 15
    delta ref tree header handle rebase parser remote buffer merge branch pack  # 16
    remote index remote delta  # 17
    handle pack blob worker token socket buffer stream stream  # 18
    ref delta rebase worker header object  # 19
    parser ref queue tree pack stream  # 20
    queue pack handle branch window object tree remote blob queue  # 21
    branch rebase rebase token object  # 22
    merge merge tree window pack rebase delta tree delta parser buffer  # 23
    delta blob merge buffer merge tree worker cache blob socket handle window  # 24
    ref window commit blob rebase  # 25
    tree rebase blob remote  # 26
    remote index token blob object branch parser socket remote socket  # 27
    parser blob queue ref delta delta delta window token cache object tree  # 28
    header blob token merge  # 29
    blob merge stream index stream queue parser worker token  # 30
    rebase rebase socket buffer branch queue cache window merge object remote worker  # 31
    merge token header merge window index worker stream buffer delta  # 32
    rebase queue index index blob merge pack tree branch branch worker remote  # 33
    handle tree index ref index blob ref tree ref pack  # 34
    object handle rebase ref branch tree header remote  # 35
    index queue socket rebase delta rebase rebase buffer header rebase  # 36
    pack cache ref buffer object stream queue remote  # 37
    buffer index delta token rebase stream pack index  # 38
    pack header tree pack handle  # 39
    parser worker header blob  # 40
    tree queue index header ref parser merge tree token branch  # 41
    header handle socket delta ref blob  # 42
    remote buffer cache handle merge worker cache  # 43
    buffer cache blob blob  # 44
    token merge worker ref token handle token socket header worker object  # 45
    index handle header remote object object blob  # 46
    remote merge rebase index object tree worker parser index  # 47
    blob pack merge socket stream stream token ref remote index  # 48
    tree index header token worker parser window  # 49
    token token queue header blob  # 50
    worker commit socket queue blob  # 51
    tree branch merge commit queue object pack handle queue buffer  # 52
    tree cache delta cache  # 53
    rebase tree merge object branch commit window delta header parser index merge  # 54
    rebase parser cache ref remote merge  # 55
    index handle rebase socket stream merge object remote commit rebase  # 56
    window remote blob stream worker delta  # 57
    window branch worker socket branch parser worker socket pack header handle token  # 58
    index branch ref rebase merge blob rebase queue index handle  # 59
    branch parser tree stream cache rebase handle blob merge merge  # 60
    window branch rebase remote rebase tree  # 61
    worker stream buffer worker  # 62
    header header buffer token index window tree socket blob ref header parser  # 63
    merge stream blob handle stream socket  # 64
    index token cache blob stream merge rebase pack stream worker  # 65
    parser object merge socket header handle window tree pack header object index  # 66
    commit token commit tree  # 67
    delta window header object token parser buffer queue ref handle commit  # 68
    branch cache stream cache cache socket stream merge merge delta  # 69
    delta handle delta queue index ref cache token rebase buffer  # 70
