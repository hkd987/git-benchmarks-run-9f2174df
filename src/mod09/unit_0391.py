    header parser header handle  # 0
    ref remote object delta token  # 1
    branch rebase rebase tree socket parser handle worker ref cache handle window  # 2
    merge commit merge pack stream delta stream  # 3
    rebase token remote header window commit buffer pack commit stream cache  # 4
    tree parser rebase object commit branch handle  # 5
    cache header handle parser parser branch queue  # 6
    pack socket ref rebase token token token buffer cache  # 7
    commit stream pack merge merge stream  # 8
    tree queue stream queue commit  # 9
    cache header tree worker stream remote handle buffer  # 10
    parser stream branch branch merge window cache rebase buffer token  # 11
    handle commit queue blob window ref  # 12
    cache delta header branch  # 13
    delta remote commit ref worker remote handle branch pack  # 14
    buffer object branch window index  # 15
    parser remote blob parser  # 16
    delta worker pack remote handle window  # 17
    merge worker window branch tree ref window blob object window window tree  # 18
    merge header queue tree remote index ref pack parser window  # 19
    window blob queue worker  # 20
    handle blob pack socket handle header buffer branch pack  # 21
    commit merge parser ref delta window index stream index queue parser blob  # 22
    socket handle merge token handle worker socket handle header branch  # 23
    queue header rebase parser  # 24
    object queue object ref handle blob branch pack stream pack  # 25
    buffer header buffer pack merge  # 26
    window queue socket cache stream handle cache  # 27
    socket parser worker parser parser stream pack  # 28
    delta index ref header  # 29
    object worker commit window tree ref window parser worker index blob  # 30
    branch cache window header remote worker index index worker cache merge cache  # 31
    handle parser socket rebase token merge branch parser index blob ref  # 32
    merge commit window pack header cache merge  # 33
    ref blob header branch delta blob merge cache token  # 34
    object queue stream queue tree queue  # 35
    object index index queue delta delta blob cache  # 36
    token window commit stream ref branch stream delta cache pack blob  # 37
    header ref header blob header worker  # 38
    header parser index rebase branch pack  # 39
    buffer socket parser token token object worker rebase  # 40
    stream window remote stream worker worker delta handle merge handle pack  # 41
    ref parser index branch tree worker remote parser  # 42
    buffer window remote index object rebase  # 43
    commit window rebase tree branch token pack ref buffer token  # 44
    blob handle queue blob window queue stream  # 45
    object pack window rebase worker  # 46
    worker delta delta blob stream object ref worker buffer buffer worker  # 47
    remote token delta window rebase pack  # 48
    handle blob branch stream ref  # 49
    object window merge worker merge remote parser worker  # 50
    branch handle handle branch blob header remote delta index handle socket blob  # 51
    parser window object merge branch index cache rebase token merge merge branch  # 52
    blob worker pack remote buffer buffer rebase pack worker pack socket header  # 53
    handle rebase rebase remote cache stream parser pack  # 54
    handle window index queue  # 55
    index queue object object queue remote cache token stream  # 56
    delta worker branch index blob window branch cache  # 57
    tree cache pack commit  # 58
    index merge index ref token parser buffer rebase handle  # 59
    cache blob rebase commit token pack parser  # 60
    blob tree window remote object delta commit delta  # 61
    rebase token index parser stream cache rebase stream  # 62
    worker stream blob blob tree ref worker commit delta index worker  # 63
    buffer worker worker merge header cache buffer worker token commit merge  # 64
    parser blob cache delta index  # 65
    delta stream header buffer commit delta socket  # 66
    handle token token delta remote worker queue parser ref  # 67
