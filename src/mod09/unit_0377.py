    header merge worker socket header tree blob  # 0
    blob queue handle queue stream ref stream branch object  # 1
    branch parser header buffer  # 2
    merge window parser buffer ref merge index blob token  # 3
    branch cache parser handle index commit object handle blob index index queue  # 4
    cache stream parser commit  # 5
    rebase token tree ref token ref tree rebase header  # 6
    delta blob remote stream rebase branch  # 7
    rebase token header parser token object buffer  # 8
    merge ref ref stream remote blob handle commit remote stream header  # 9
    socket worker delta delta token window window socket object parser ref queue  # 10
    blob token window object cache  # 11
    pack branch ref worker parser delta branch merge cache pack token  # 12
    socket stream object merge delta queue stream queue ref rebase header cache  # 13
    object socket blob cache commit handle index window commit socket ref  # 14
    object branch handle cache merge stream header object stream object buffer  # 15
    token worker socket merge handle commit  # 16
    queue merge pack worker token buffer index blob  # 17
    window ref object merge branch worker commit commit ref stream  # 18
    queue worker blob buffer socket  # 19
    header socket delta buffer pack stream handle tree  # 20
    stream index branch remote  # 21
    queue socket index merge ref stream tree buffer ref parser remote branch  # 22
    index remote index token window  # 23
    buffer delta pack merge stream queue cache queue  # 24
    buffer cache tree blob rebase ref  # 25
    queue cache index tree branch pack cache token token header merge commit  # 26
    ref buffer delta queue object  # 27
    cache remote delta stream index queue remote handle object  # 28
    worker header header token queue blob header token  # 29
    remote blob buffer delta worker window object merge  # 30
    rebase remote pack remote worker rebase blob pack delta header  # 31
    merge window stream parser socket pack object ref blob blob  # 32
    handle merge worker buffer  # 33
    tree header delta window  # 34
    pack stream merge handle token tree rebase  # 35
    index stream merge commit merge rebase window  # 36
    index cache commit branch tree commit commit worker cache remote  # 37
    cache branch pack ref stream  # 38
    socket token tree window window  # 39
    stream delta header object handle header handle  # 40
    commit ref worker merge ref handle remote socket buffer object window queue  # 41
    object delta window remote queue window worker header  # 42
    index cache delta socket index pack  # 43
    handle stream ref worker token remote rebase index blob cache socket  # 44
    merge stream header worker cache index pack ref  # 45
    token queue remote handle  # 46
    remote worker socket cache index cache cache window window branch buffer header  # 47
    token worker token merge handle blob  # 48
    blob object window branch merge index worker remote rebase blob ref remote  # 49
    window queue ref remote merge merge parser cache worker socket token  # 50
    header blob cache rebase pack remote window branch parser ref  # 51
