    token token delta pack branch buffer  # 0
    token object pack commit tree socket window handle remote  # 1
    commit cache delta ref ref handle  # 2
    rebase buffer parser tree token ref pack pack tree  # 3
    pack commit buffer worker blob  # 4
    object token worker worker cache queue tree rebase ref cache index  # 5
    socket handle token commit socket  # 6
    window buffer queue ref delta delta ref  # 7
    branch token queue commit rebase window remote worker  # 8
    rebase branch blob stream  # 9
    queue handle rebase merge  # 10
    queue rebase commit header queue pack  # 11
    index stream branch tree branch  # 12
    parser index socket buffer tree  # 13
    header header token window tree commit pack header tree merge  # 14
    buffer delta buffer window  # 15
    blob window queue rebase token window header token token  # 16
    ref remote cache object remote header merge handle  # 17
    cache merge merge delta tree queue pack stream parser merge  # 18
    remote blob merge tree object index branch merge rebase pack window  # 19
    header ref commit parser ref  # 20
    pack delta rebase worker delta remote cache cache stream worker cache ref  # 21
    stream blob window index commit rebase remote merge ref ref pack  # 22
    merge window object buffer pack pack header socket  # 23
    branch buffer window commit cache branch commit  # 24
    header token parser socket  # 25
    socket worker parser object  # 26
    merge branch pack token pack commit queue commit object cache handle  # 27
    rebase worker handle worker parser token worker  # 28
    tree stream blob commit remote rebase remote  # 29
    rebase queue window index delta branch worker handle pack rebase worker tree  # 30
    object commit remote header  # 31
    merge blob header cache index  # 32
    ref object handle queue rebase  # 33
    worker queue header queue index buffer branch remote index buffer buffer handle  # 34
    ref buffer merge token branch rebase ref queue  # 35
    window cache parser pack buffer tree merge  # 36
    token commit handle handle remote handle tree remote  # 37
    ref rebase blob commit pack stream tree queue  # 38
    queue socket remote delta ref  # 39
    delta parser handle blob window queue  # 40
    cache branch index socket handle index stream blob worker blob buffer  # 41
    header header tree buffer  # 42
    handle header worker socket cache socket  # 43
    ref worker branch window tree delta commit object cache index  # 44
    queue remote buffer window  # 45
    header pack queue blob parser handle  # 46
    index stream object queue blob tree handle cache pack buffer header  # 47
    handle handle queue token ref worker pack remote blob  # 48
    branch token token buffer ref tree handle handle tree branch remote  # 49
    queue handle socket cache queue parser delta pack ref handle  # 50
    remote header ref buffer  # 51
    buffer pack blob window tree object object parser socket blob socket token  # 52
    header tree delta delta object remote token object  # 53
    worker window parser buffer window parser  # 54
    blob token delta ref token remote token socket token tree commit  # 55
    object commit cache blob  # 56
    delta buffer rebase remote buffer  # 57
    header parser commit stream rebase worker rebase delta socket rebase  # 58
    tree remote tree commit token remote  # 59
    queue cache window merge window window  # 60
    stream buffer blob rebase delta branch tree socket socket delta  # 61
    remote buffer header handle queue queue blob buffer merge  # 62
    index object blob stream parser buffer tree parser object rebase socket ref  # 63
    header rebase header handle rebase pack  # 64
    token ref ref tree window worker cache queue cache delta socket  # 65
    merge queue delta buffer worker handle delta branch ref handle handle  # 66
    rebase tree window ref pack merge queue branch  # 67
    handle rebase merge cache delta token commit remote buffer rebase parser  # 68
    rebase rebase buffer ref  # 69
    ref pack remote index merge pack  # 70
    stream ref buffer delta token buffer tree  # 71
    handle parser stream window worker cache ref  # 72
    object rebase remote socket delta merge pack  # 73
    token handle remote handle window buffer window remote delta token delta  # 74
    socket merge buffer socket cache worker remote header cache  # 75
    header merge stream queue socket handle parser merge rebase  # 76
    index ref window header cache parser buffer rebase delta  # 77
    cache window commit handle blob stream index  # 78
    remote pack index rebase handle handle  # 79
    cache worker queue buffer  # 80
    handle buffer parser header branch stream blob  # 81
    queue branch handle branch delta  # 82
