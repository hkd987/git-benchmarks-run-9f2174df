    parser window window tree delta tree remote  # 0
    parser index parser handle worker blob  # 1
    socket commit remote rebase  # 2
    tree tree index socket  # 3
    branch header pack cache stream window window commit object commit handle ref  # 4
    pack remote index buffer header object index handle socket  # 5
    socket pack header cache merge buffer pack  # 6
    header merge cache window  # 7
    window token tree ref header pack window  # 8
    object worker token header buffer merge object queue index object merge  # 9
    worker commit blob merge index  # 10
    socket merge header cache blob window buffer delta buffer merge  # 11
    parser blob worker delta rebase  # 12
    merge header buffer remote branch index object buffer stream index  # 13
    delta worker delta pack ref tree blob  # 14
    cache rebase remote merge stream stream  # 15
    queue index tree merge delta commit worker worker header commit  # 16
    socket remote worker window branch handle buffer  # 17
    tree queue branch stream  # 18
    buffer stream remote pack parser socket socket handle  # 19
    socket header header header  # 20
    window ref stream worker commit  # 21
    tree window object rebase header  # 22
    buffer cache window remote  # 23
    worker tree index stream branch worker object  # 24
    pack remote pack rebase parser merge tree stream pack branch  # 25
    ref window commit token parser tree merge tree ref stream  # 26
    worker tree tree buffer index commit rebase object token worker socket socket  # 27
    ref branch stream tree branch pack buffer ref handle parser delta  # 28
    index stream buffer index  # 29
    commit branch token object  # 30
    buffer cache object queue  # 31
    pack blob handle branch ref merge socket worker buffer window token  # 32
    handle header handle parser tree  # 33
    worker token window worker rebase tree ref delta  # 34
    delta ref cache parser blob socket token stream commit index  # 35
    remote window rebase queue commit stream token remote handle commit merge  # 36
    tree object queue blob pack merge delta merge  # 37
    cache header index token window blob socket  # 38
    blob buffer buffer commit header buffer index  # 39
    cache rebase ref header header pack delta queue  # 40
    blob handle commit commit  # 41
    buffer rebase worker index  # 42
    parser token tree worker worker stream branch socket rebase token cache ref  # 43
    merge token parser socket  # 44
    index merge window pack ref remote  # 45
    tree tree buffer delta socket  # 46
    tree window header index handle remote merge commit remote blob worker  # 47
    handle stream buffer socket branch blob  # 48
    cache tree pack header socket delta index parser handle socket stream  # 49
    queue queue stream queue  # 50
    token merge pack ref branch stream window token pack header worker blob  # 51
    stream worker branch remote window stream  # 52
    branch buffer socket cache parser token  # 53
    ref delta blob queue socket branch remote worker merge parser  # 54
    index tree rebase blob stream merge cache tree window  # 55
    object header remote rebase ref  # 56
    pack token socket remote tree pack socket rebase  # 57
    handle blob window cache stream handle token header commit ref object header  # 58
    stream commit remote tree merge parser window  # 59
    rebase rebase rebase token commit  # 60
    merge cache token merge worker  # 61
    token index delta buffer cache merge window worker queue stream buffer queue  # 62
    buffer rebase delta window socket merge pack queue buffer queue tree queue  # 63
