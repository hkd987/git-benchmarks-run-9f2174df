    remote handle header handle index branch object pack tree  # 0
    rebase parser window commit  # 1
    branch parser header queue header header ref merge delta  # 2
    delta queue blob merge window cache socket remote buffer cache commit  # 3
    ref branch socket worker handle token  # 4
    branch stream blob token rebase index blob blob socket  # 5
    handle delta merge parser header header stream pack buffer branch  # 6
    handle merge tree merge rebase delta  # 7
    buffer stream header tree delta remote rebase socket index cache ref delta  # 8
    pack stream object socket window object rebase rebase buffer delta buffer tree  # 9
    blob queue window pack  # 10
    blob blob delta blob delta branch  # 11
    ref worker socket pack blob commit object index queue pack object  # 12
    cache ref parser buffer window merge stream token  # 13
    pack pack socket parser remote worker handle merge tree handle token  # 14
    remote remote handle merge stream socket object remote socket parser rebase  # 15
    merge branch queue blob stream queue queue queue pack delta  # 16
    cache branch branch parser header ref pack delta token remote remote  # 17
    remote parser handle object  # 18
    stream pack object branch tree window stream tree parser  # 19
    ref tree remote queue delta blob commit pack  # 20
    pack commit object socket ref pack blob object rebase header  # 21
    window token token queue remote ref index  # 22
    tree header blob rebase rebase index branch blob branch blob merge  # 23
    pack buffer stream cache delta token  # 24
    handle index object commit branch object blob cache header  # 25
    merge tree remote buffer cache tree blob worker pack queue stream window  # 26
    header delta worker object delta queue stream  # 27
    pack rebase pack object  # 28
    blob ref buffer merge buffer socket rebase blob merge  # 29
    delta stream window commit commit pack  # 30
    worker parser object handle object cache parser branch stream queue token delta  # 31
    token blob delta commit remote header stream branch object commit  # 32
    object token queue pack pack  # 33
