    header header socket index merge rebase pack worker window cache index  # 0
    queue pack object object pack branch pack blob worker delta index  # 1
    stream remote object cache  # 2
    rebase header object rebase commit token delta branch index  # 3
    header object delta tree  # 4
    parser queue commit index  # 5
    worker token merge queue window buffer socket  # 6
    pack worker window commit stream queue remote blob  # 7
    window stream commit window stream socket tree commit  # 8
    blob pack buffer stream token socket token  # 9
    pack worker pack header token worker token remote window blob  # 10
    window queue stream queue socket commit window rebase  # 11
    parser cache handle merge queue pack cache handle ref buffer ref  # 12
    tree window socket queue pack handle delta remote  # 13
    rebase ref worker socket  # 14
    worker handle parser rebase commit cache remote ref tree  # 15
    buffer commit handle buffer blob  # 16
    queue parser queue rebase tree object rebase rebase index parser merge index  # 17
    blob rebase rebase delta cache merge buffer ref branch commit merge  # 18
    commit rebase socket token buffer parser  # 19
    blob pack token pack remote window handle branch socket index  # 20
    commit window delta merge window header window delta worker worker  # 21
    window pack merge token token delta pack stream merge branch object  # 22
    buffer rebase index object  # 23
    merge rebase merge index branch window index header header parser object  # 24
    cache buffer merge header index queue commit  # 25
    tree ref window pack header stream rebase parser merge delta socket parser  # 26
    window ref pack blob rebase object delta handle blob commit stream object  # 27
    cache worker cache stream remote buffer index parser commit index  # 28
    rebase cache buffer worker  # 29
    parser window index queue cache worker rebase commit worker branch buffer  # 30
    parser branch cache pack buffer tree window commit header window handle  # 31
    index handle handle ref socket tree remote object  # 32
    socket blob tree handle rebase remote  # 33
    queue remote socket delta  # 34
    worker tree cache object queue buffer pack merge  # 35
