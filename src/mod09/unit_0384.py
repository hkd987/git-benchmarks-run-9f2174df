    rebase socket header index worker tree tree remote delta blob handle  # 0
    buffer header index parser object object object merge  # 1
    queue remote delta remote ref buffer ref cache parser  # 2
    rebase socket blob window window ref rebase index pack  # 3
    stream rebase ref ref branch index branch  # 4
    ref branch window stream tree socket parser remote window worker cache  # 5
    object rebase worker buffer tree parser index  # 6
    parser buffer remote worker remote blob  # 7
    token remote rebase pack tree worker header tree  # 8
    handle token merge delta queue stream handle delta  # 9
    commit parser stream index pack cache blob header  # 10
    merge token queue parser queue ref remote  # 11
    window window header worker queue socket  # 12
    token socket window token blob parser worker index header  # 13
    tree pack merge window branch pack worker tree  # 14
    index pack merge cache  # 15
    queue commit buffer tree object buffer header pack pack buffer branch pack  # 16
    blob queue merge token merge branch  # 17
    commit buffer blob object window stream queue  # 18
    blob handle blob object remote queue socket blob cache  # 19
    object tree header commit delta ref worker pack commit handle object worker  # 20
    socket branch merge branch buffer cache index socket  # 21
    window cache window stream pack socket stream socket socket queue  # 22
    header delta buffer blob ref commit stream delta commit index index  # 23
    blob delta tree cache window worker delta object remote object  # 24
    rebase pack merge remote rebase header  # 25
    queue cache rebase stream worker header tree stream  # 26
    branch object handle remote index delta  # 27
    queue handle handle index token object socket pack handle remote socket  # 28
    queue object stream queue token branch tree rebase index  # 29
    index ref pack handle  # 30
    commit remote remote blob  # 31
    cache worker blob index queue header stream worker token  # 32
    rebase header token pack window tree  # 33
    window rebase branch branch parser branch queue index parser ref  # 34
    commit remote token stream cache  # 35
    parser socket queue worker header blob delta  # 36
    delta blob index buffer merge queue cache parser  # 37
    socket window window header delta cache  # 38
    stream commit socket delta tree object socket delta queue  # 39
    object queue socket remote buffer object commit handle tree socket  # 40
    ref stream header handle window pack token queue  # 41
    queue branch object cache index ref tree token index parser blob socket  # 42
    cache parser buffer branch worker delta queue branch stream  # 43
    window queue remote merge merge token branch commit  # 44
    parser queue delta header blob buffer cache cache stream delta stream header  # 45
    parser cache token blob parser cache object  # 46
    delta parser queue delta stream blob rebase parser token blob queue  # 47
    pack window object blob object stream commit pack header worker blob remote  # 48
    rebase handle worker window remote merge header  # 49
    rebase socket commit handle  # 50
    worker delta cache object  # 51
    window token commit queue index  # 52
    window rebase commit rebase header header tree pack window rebase  # 53
    object cache commit cache stream cache rebase parser header commit rebase worker  # 54
    queue handle delta token socket pack blob  # 55
    rebase pack pack remote branch object stream socket  # 56
    branch branch remote rebase rebase pack stream queue delta  # 57
    tree remote ref ref  # 58
    branch window remote stream remote index socket token  # 59
    socket queue tree token stream  # 0
    worker tree header delta socket remote buffer object  # 1
    queue stream header header buffer remote  # 2
    buffer pack merge tree branch handle  # 3
    object ref stream token merge ref worker header  # 4
    delta index parser token index token delta worker cache  # 5
    merge window buffer parser index window token delta buffer blob  # 6
    delta buffer token rebase cache remote delta pack cache tree rebase  # 7
    buffer ref handle ref token buffer queue index stream tree  # 8
