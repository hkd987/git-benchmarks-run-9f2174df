    header branch blob token  # 0
    delta parser queue blob  # 1
    socket delta cache rebase stream stream queue socket  # 2
    merge pack pack buffer handle branch  # 3
    token ref ref worker cache worker pack commit  # 4
    token object object blob queue stream  # 5
    window pack header buffer worker parser stream commit queue parser  # 6
    blob merge index token worker  # 7
    branch worker ref token header worker tree header object stream index worker  # 8
    token remote branch worker header cache header  # 9
    header blob worker stream blob delta handle queue  # 10
    pack commit ref blob  # 11
    token stream handle stream pack queue remote rebase  # 12
    socket socket queue window delta buffer cache parser rebase  # 13
    commit object blob stream commit queue worker  # 14
    blob pack stream ref  # 15
    queue delta queue rebase cache commit socket rebase  # 16
    stream ref handle remote header parser branch delta  # 17
    tree socket handle pack object object  # 18
    ref socket parser header rebase remote ref  # 19
    worker handle handle parser buffer commit token  # 20
    buffer token merge token blob rebase token merge window header object  # 21
    object pack delta buffer rebase window blob stream handle branch  # 22
    remote index index token ref  # 23
    branch object tree remote object cache  # 24
    delta branch tree ref commit  # 25
    buffer remote index worker  # 26
    rebase commit commit pack ref blob cache object  # 27
    delta socket commit buffer object handle  # 28
    cache delta socket commit buffer token  # 29
    window pack object remote ref header branch blob handle window  # 30
    window blob rebase header cache buffer object merge  # 31
    delta buffer commit delta blob blob parser socket stream  # 32
    buffer blob tree queue  # 33
    merge token tree queue remote header stream branch merge handle  # 34
    stream commit merge merge remote handle parser ref pack socket pack  # 35
    parser stream header worker blob cache  # 36
    rebase ref commit tree token worker parser token worker token index cache  # 37
    token index worker rebase  # 38
    blob index blob cache index window  # 39
    worker worker remote commit tree buffer commit buffer token  # 40
    delta index handle parser cache rebase blob token handle worker cache buffer  # 41
    blob parser remote parser buffer parser token handle ref index stream  # 42
    window blob handle pack object handle blob stream tree remote  # 43
    tree ref header commit pack remote remote rebase queue stream  # 44
    ref object window queue commit index  # 45
    token object stream window  # 46
    parser worker merge delta socket branch index  # 47
    object token token pack blob remote  # 48
    blob branch remote delta token window merge token  # 49
    rebase merge window pack parser cache ref  # 50
    token token window socket  # 51
    buffer object delta pack  # 52
    rebase stream pack queue token rebase index blob  # 53
    rebase branch parser header object object object header commit commit  # 54
    branch blob socket rebase  # 55
    cache header cache pack  # 56
    worker socket branch header object header commit index branch header  # 57
    socket window blob header delta  # 58
    window index tree parser  # 59
    cache branch header remote window buffer window socket object token window pack  # 60
    index parser buffer queue cache queue token index tree  # 0
    token index parser rebase blob delta window  # 1
    worker queue remote buffer cache merge buffer tree index pack commit  # 2
    blob pack index cache stream window handle  # 3
    worker worker object socket object header buffer index cache pack cache  # 4
    handle delta branch cache  # 5
    branch header token handle ref cache worker socket branch token commit worker  # 6
    blob handle token header remote  # 7
    branch branch window merge merge index blob worker cache cache token worker  # 8
    index pack pack stream header handle rebase branch worker merge branch  # 9
    delta blob blob delta merge ref delta tree remote  # 10
    ref object queue socket header remote  # 11
