    object delta header pack pack socket queue worker  # 0
    socket remote merge ref parser handle object parser token parser  # 1
    handle pack tree parser header header worker buffer handle  # 2
    rebase queue pack header header tree remote index  # 3
    object window pack cache socket socket rebase socket index tree  # 4
    pack merge remote branch buffer  # 5
    header tree parser rebase object  # 6
    object handle delta blob delta token socket tree blob pack remote  # 7
    tree pack buffer window object blob object tree  # 8
    queue tree worker branch  # 9
    worker tree blob remote  # 10
    index pack ref delta  # 11
    blob token ref parser remote socket tree delta queue parser merge  # 12
    pack rebase stream buffer handle socket index  # 13
    queue remote commit buffer merge pack object buffer blob blob cache token  # 14
    index window header parser commit ref remote worker merge  # 15
    branch object rebase header stream token object  # 16
    commit object object delta object window rebase socket index header  # 17
    token commit cache tree commit queue header object rebase window socket  # 18
    pack queue ref token  # 19
    tree parser token blob handle  # 20
    branch buffer branch buffer  # 21
    tree index queue commit socket rebase handle delta token parser ref remote  # 22
    socket window rebase merge delta commit  # 23
    window handle handle parser blob handle window merge window delta branch object  # 24
    blob parser parser index token token tree blob  # 25
    commit object remote ref parser  # 26
    parser window worker tree ref pack  # 27
    parser commit tree stream  # 28
    socket tree remote branch  # 29
    blob branch branch token index ref rebase remote token handle handle  # 30
    rebase pack cache worker rebase token pack parser blob remote branch rebase  # 31
    buffer index tree ref  # 32
    ref rebase worker queue buffer queue commit  # 33
    remote branch buffer parser delta  # 0
    token merge queue merge worker  # 1
    delta token buffer stream worker object token object index worker ref  # 2
    stream worker object branch ref window queue socket buffer  # 3
    delta socket cache merge token header tree socket buffer  # 4
    object stream delta buffer merge cache remote stream rebase  # 5
    header pack commit index parser window worker commit object cache pack stream  # 0
    index cache object worker blob handle window branch  # 1
    rebase pack ref delta commit branch  # 2
    cache commit buffer rebase cache queue tree ref header parser  # 3
    token rebase queue pack remote rebase parser buffer remote  # 4
    commit cache worker worker window merge parser blob window branch tree  # 5
    queue rebase rebase window socket socket tree  # 6
    window tree ref branch token pack queue header  # 7
    window pack commit handle worker handle window branch  # 8
    cache blob tree window stream ref socket blob object commit stream delta  # 9
    socket stream token buffer header tree  # 10
    delta commit pack window remote  # 11
    object rebase remote pack buffer tree  # 12
    delta handle rebase merge ref window stream buffer blob  # 13
