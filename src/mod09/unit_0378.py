    stream worker index token  # 0
    rebase delta worker rebase rebase worker  # 1
    remote header token commit delta object ref  # 2
    header handle token branch branch ref delta blob header delta  # 3
    token commit cache window index worker worker commit index cache cache  # 4
    buffer commit delta object ref  # 5
    delta token rebase socket parser pack token cache socket index  # 6
    worker header window pack pack delta stream  # 7
    rebase index remote handle stream parser  # 8
    window buffer header handle stream  # 9
    blob worker remote socket stream cache merge commit token merge object branch  # 10
    blob socket parser worker  # 11
    remote remote blob stream pack branch pack header  # 12
    worker parser buffer delta remote ref worker parser  # 13
    index merge worker window commit blob branch ref commit remote  # 14
    tree merge rebase object branch branch buffer  # 15
    ref window buffer handle window pack  # 16
    handle parser tree object buffer pack token token remote  # 17
    delta cache socket queue object  # 18
    branch stream token handle ref pack blob  # 19
    branch pack socket ref remote window  # 20
    handle stream rebase parser cache  # 21
    ref branch parser rebase parser delta delta stream worker window blob  # 22
    queue worker window buffer stream buffer token delta token commit socket  # 23
    index parser blob buffer window header merge object delta queue queue worker  # 24
    index header queue pack merge header socket handle remote window  # 25
    remote queue window parser pack branch queue stream rebase remote stream  # 26
    commit socket remote parser ref delta  # 27
    parser object merge rebase  # 28
    parser branch handle buffer remote cache remote index stream ref  # 29
    pack index queue commit worker object branch handle ref  # 30
    socket token worker stream cache window  # 31
    stream cache window remote worker blob commit  # 32
    window merge token branch queue rebase rebase tree worker  # 33
    blob socket pack tree index merge  # 34
    socket buffer stream commit blob  # 35
    object commit worker handle index  # 36
    merge handle index token  # 37
    cache parser queue socket pack token header socket tree  # 38
    stream ref ref remote queue window  # 39
    ref worker header remote merge socket  # 40
    index pack cache blob stream blob stream index merge index  # 41
    object pack queue merge worker window commit object handle  # 42
    commit object queue queue pack token branch stream merge socket blob  # 43
    worker remote window buffer handle buffer stream  # 44
    socket commit socket merge stream handle  # 45
    handle delta stream tree pack  # 46
    index socket header stream token index worker  # 47
    queue parser window remote handle index queue ref handle  # 48
    ref ref pack merge commit stream object remote ref stream ref branch  # 49
    ref branch branch branch blob tree buffer parser token buffer  # 50
    stream rebase stream blob  # 51
    parser blob remote header worker object  # 52
    commit pack object buffer header tree tree delta commit window  # 53
    cache header window token header buffer queue object handle parser header  # 54
    worker handle remote delta handle delta token window worker merge  # 55
    rebase branch window tree window branch tree queue index queue worker buffer  # 56
    buffer delta handle buffer pack remote commit header merge ref worker socket  # 57
    pack parser header cache blob buffer parser rebase tree header token  # 58
    token worker merge object handle handle tree delta index pack pack  # 59
    stream buffer rebase delta queue index branch queue socket cache pack object  # 60
    window token buffer branch remote index buffer socket window window  # 61
    cache commit token delta merge merge handle pack delta  # 62
    delta index commit delta delta index  # 63
    token stream object worker tree worker merge  # 64
    token buffer queue window branch  # 65
    window window buffer queue stream cache merge cache  # 66
    rebase handle pack blob pack ref  # 67
    object branch cache stream rebase index queue buffer socket branch  # 68
    rebase object header parser index header buffer cache  # 69
    socket blob object delta  # 70
    blob pack cache queue token delta cache  # 71
    ref ref handle window  # 72
    stream worker parser socket branch  # 73
    commit queue stream merge branch  # 74
    buffer cache stream merge delta  # 75
    ref buffer rebase commit branch delta delta rebase  # 76
    pack delta handle merge tree handle stream window cache branch  # 77
    commit ref remote socket remote window blob tree commit delta rebase socket  # 78
    blob commit cache delta index  # 0
    object token handle queue ref commit worker buffer  # 1
    cache rebase handle commit worker buffer branch ref blob delta token delta  # 2
    handle cache stream rebase commit  # 3
    window pack branch ref handle ref buffer stream queue rebase  # 4
    ref remote buffer buffer commit parser merge header worker token commit  # 5
    rebase ref window object rebase delta buffer stream handle window  # 6
    header rebase queue handle header pack tree token worker ref pack parser  # 7
    object delta stream header index handle  # 8
    ref pack parser header pack queue header parser stream stream blob index  # 9
    merge rebase pack delta remote buffer  # 10
    delta index index parser commit  # 11
    tree blob object handle queue rebase queue rebase delta queue tree socket  # 12
