    token window cache parser  # 0
    socket delta handle token index  # 1
    ref object index worker rebase ref  # 2
    tree index window remote  # 3
    blob header parser pack stream  # 4
    queue ref window delta remote blob window  # 5
    remote token merge remote stream parser pack  # 6
    token tree worker cache ref merge object stream remote token blob  # 7
    window branch stream delta cache blob  # 8
    cache branch buffer queue  # 9
    token token object token merge rebase branch commit header  # 10
    index header object merge blob pack blob  # 11
    merge cache parser parser tree index tree queue cache socket tree tree  # 12
    queue stream buffer token delta index socket stream  # 13
    ref delta pack tree window header parser header parser delta ref  # 14
    index parser tree header stream delta object ref cache merge pack  # 15
    delta ref buffer tree index  # 16
    worker token window branch remote token token tree stream queue  # 17
    branch blob tree rebase merge queue object socket  # 18
    remote delta delta object pack branch cache branch handle window window parser  # 19
    socket remote stream cache  # 20
    rebase queue cache header cache delta stream header rebase handle commit  # 21
    parser cache worker branch worker commit merge pack tree commit branch  # 22
    handle header parser commit branch parser buffer object buffer rebase  # 23
    socket blob remote blob pack pack token header  # 24
    stream queue index index socket merge window branch  # 25
    queue socket handle parser blob blob index buffer  # 26
    buffer cache blob pack worker header socket worker index socket socket  # 27
    remote tree header merge worker cache socket cache worker buffer  # 28
    window buffer delta merge  # 29
    worker stream commit object  # 30
    token worker branch window delta commit parser tree blob ref  # 31
    pack blob handle pack pack socket token buffer remote token rebase  # 32
    buffer header worker worker delta index  # 33
    branch header index commit cache delta tree ref socket socket  # 34
    remote rebase commit stream branch queue delta remote tree token socket  # 35
    branch queue socket queue buffer buffer rebase parser handle token pack  # 36
    index merge worker commit token commit  # 37
    socket queue handle tree window handle delta socket token  # 38
    token queue queue socket buffer token queue queue token merge pack  # 39
    object worker blob remote cache ref remote rebase buffer branch  # 40
    object remote object parser ref cache stream branch header ref  # 41
    delta cache header delta branch  # 42
    header socket buffer pack socket delta cache cache commit merge  # 43
    tree commit tree socket window socket ref  # 44
    blob index ref rebase remote ref stream index worker  # 45
    branch buffer branch cache rebase merge socket handle ref  # 46
    delta merge token handle worker buffer index queue remote socket object  # 47
    pack branch ref object parser remote parser parser remote  # 48
    token cache blob queue  # 49
    window cache branch pack window pack handle stream window window worker  # 50
    blob socket delta cache parser pack delta blob commit ref cache worker  # 51
    branch branch buffer worker socket branch worker remote delta handle  # 52
    branch ref pack buffer pack header token object handle parser blob buffer  # 53
    remote merge ref object window parser socket commit queue remote tree merge  # 54
    queue handle window rebase socket delta remote queue pack blob pack commit  # 55
    object merge tree branch  # 56
    socket branch pack remote token  # 57
    parser stream tree cache parser tree header socket commit  # 58
    header ref token socket rebase  # 59
    merge buffer ref delta stream tree blob branch pack tree parser  # 60
    queue object ref ref index queue window delta worker merge token stream  # 61
    buffer remote blob rebase header object socket header remote  # 62
    delta pack object handle parser index buffer merge token  # 63
    buffer window parser queue  # 64
    index cache pack stream  # 65
    merge commit branch index cache ref queue window cache merge  # 66
    commit window socket branch  # 67
    object parser index branch ref  # 68
    ref delta header tree  # 69
    pack stream branch stream pack tree commit cache token merge remote  # 70
    pack stream remote remote ref buffer worker branch merge ref  # 71
