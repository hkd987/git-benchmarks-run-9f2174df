    socket blob commit cache token pack  # 0
    window cache cache object cache window buffer header pack buffer  # 1
    tree blob branch socket rebase window worker worker  # 2
    blob tree remote merge buffer merge window merge tree delta tree  # 3
    branch merge merge remote ref branch pack tree buffer  # 4
    branch stream ref remote rebase blob queue token  # 5
    socket rebase parser buffer parser  # 6
    parser index commit cache worker merge ref commit parser  # 7
    tree handle rebase branch token delta  # 8
    header cache stream rebase remote stream queue worker  # 9
    queue ref worker buffer remote window stream window cache header worker remote  # 10
    merge window handle stream ref remote commit delta tree parser commit branch  # 11
    merge rebase index pack delta socket commit ref queue branch stream pack  # 12
    window tree branch socket handle cache cache  # 13
    token blob parser delta delta header branch  # 14
    commit socket socket socket branch stream  # 15
    queue header cache object socket header worker tree commit branch blob tree  # 16
    stream merge rebase tree object worker parser header index  # 17
    window commit queue worker ref merge branch rebase remote  # 18
    rebase queue commit handle cache queue merge worker window  # 19
    header cache buffer tree tree queue token queue delta  # 20
    pack header header object parser branch  # 21
    handle ref blob buffer  # 22
    ref branch handle socket index handle pack  # 23
    parser token buffer handle delta token  # 24
    token queue tree remote cache rebase  # 25
    queue socket ref worker  # 26
    merge delta blob stream queue parser index tree header rebase  # 27
    ref commit cache handle pack stream socket buffer remote parser  # 28
    window commit queue window delta  # 29
    stream stream index socket socket  # 30
    remote object remote window pack socket remote  # 31
    blob header tree commit parser parser tree  # 32
    header remote token ref commit queue token commit token buffer parser window  # 33
    delta tree window stream  # 34
    queue pack handle parser blob rebase delta  # 35
    queue buffer window parser cache  # 36
    cache parser remote worker ref  # 37
    remote worker object queue window socket handle handle  # 38
    merge blob remote socket cache object remote  # 39
    merge merge buffer socket merge  # 40
    ref stream token remote pack pack  # 41
    buffer socket commit window ref parser delta window token delta branch  # 42
    commit blob ref rebase branch socket  # 43
    branch branch ref parser object parser branch buffer delta  # 44
    tree buffer cache queue pack parser  # 45
    merge remote queue socket branch pack worker socket  # 46
    rebase buffer commit queue  # 47
    merge queue ref header handle socket branch  # 48
    cache blob ref queue index delta  # 49
    cache window blob token worker socket socket tree header commit  # 50
    delta remote stream header cache window commit object branch remote  # 51
    merge token tree window tree remote branch rebase header cache  # 52
    stream socket pack object cache rebase remote window socket  # 53
    index queue commit parser  # 54
    remote tree queue stream cache buffer rebase window  # 55
    token buffer blob rebase queue queue ref branch socket delta merge  # 56
    branch ref remote blob worker buffer window socket queue branch delta  # 57
    delta merge worker index branch branch window pack commit  # 58
    ref delta queue queue token token remote  # 59
    worker socket stream remote object index delta header branch socket blob socket  # 60
    delta window parser token  # 61
    object parser parser object  # 62
    blob stream token queue window socket tree index  # 63
    socket blob delta commit commit parser  # 64
    rebase parser queue handle branch delta object  # 65
    tree pack branch ref  # 66
    ref queue token header branch  # 67
    stream rebase parser buffer branch remote header  # 68
    socket queue cache header object branch remote  # 69
    socket queue delta tree branch object index merge header  # 70
    ref blob merge parser cache handle index  # 71
    queue worker buffer window  # 72
    header handle parser merge branch ref rebase token handle  # 73
    blob object tree rebase tree delta pack stream object branch  # 74
    parser blob delta remote  # 75
    blob blob remote window remote header  # 76
    queue rebase branch object commit ref  # 77
    object branch cache pack queue merge blob cache ref token blob object  # 78
