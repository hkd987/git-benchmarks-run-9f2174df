    object handle queue branch object tree merge delta header remote window  # 0
    window blob window header buffer index  # 1
    stream ref object index token token stream delta cache commit remote  # 2
    index blob delta ref queue queue blob buffer index header  # 3
    tree remote socket blob object socket blob ref blob  # 4
    ref branch socket tree blob window worker  # 5
    window delta tree branch socket commit  # 6
    branch blob merge worker cache  # 7
    socket tree rebase token cache queue header cache token socket handle  # 8
    handle tree buffer ref cache merge index index cache  # 9
    parser header socket header  # 10
    socket parser delta branch buffer handle buffer commit remote blob cache  # 11
    commit blob commit worker token pack  # 12
    rebase buffer pack token header ref window token window worker window ref  # 13
    window rebase pack tree  # 14
    queue queue window buffer merge window token  # 15
    window blob blob rebase blob rebase queue queue  # 16
    cache socket parser commit blob index  # 17
    token worker blob rebase commit queue branch buffer ref socket index  # 18
    buffer merge commit worker merge rebase header delta stream worker socket  # 19
    blob handle pack cache tree socket delta  # 20
    delta ref object ref socket branch  # 21
    queue blob buffer delta buffer header blob rebase  # 22
    tree parser buffer socket  # 23
    remote queue socket pack remote stream parser header index  # 24
    pack index rebase socket pack branch delta pack parser parser commit  # 25
    rebase queue remote pack branch queue rebase index buffer  # 26
    socket remote rebase token handle token buffer cache  # 27
    tree buffer header index index handle  # 28
    header branch window rebase header stream cache tree handle object  # 29
    blob rebase worker buffer  # 30
    parser blob queue rebase window commit  # 31
    merge merge buffer remote ref buffer remote object socket cache worker worker  # 32
    cache index worker handle  # 33
    tree merge handle header token tree parser tree worker object commit  # 34
    cache handle index tree buffer token ref cache branch  # 35
    ref header branch cache buffer stream index stream pack object buffer socket  # 36
    stream stream buffer tree worker tree  # 37
    blob buffer socket queue  # 38
    tree handle tree object token window index index handle  # 39
    cache handle object commit  # 40
    cache handle stream stream pack buffer  # 41
    blob parser ref worker merge pack  # 42
    rebase object queue handle branch pack blob header  # 43
    index branch tree stream buffer handle  # 44
    object tree header object  # 45
    handle blob blob ref tree  # 46
    buffer merge rebase stream worker handle  # 47
    socket tree ref parser pack buffer commit parser  # 48
    commit delta branch commit cache worker branch  # 49
    rebase pack cache ref parser queue header  # 50
    remote buffer header header  # 51
    branch worker branch pack commit pack cache header ref  # 52
    merge rebase parser index branch merge rebase  # 53
    blob stream pack window window socket queue commit commit worker index buffer  # 54
    branch cache index worker delta merge buffer  # 55
    buffer commit object commit rebase header blob merge queue  # 56
    branch handle blob worker object merge window queue tree remote  # 57
    commit queue pack index  # 58
    ref tree cache stream pack  # 59
    merge socket queue socket merge parser queue  # 60
    ref merge merge socket stream handle header delta tree window stream  # 61
    stream window worker delta stream stream  # 62
    commit object merge object  # 63
    merge queue commit buffer pack stream handle ref  # 64
    branch commit index queue queue worker buffer blob  # 65
    token object delta worker rebase handle queue stream merge index token  # 66
    buffer blob tree ref header pack commit pack queue  # 67
    rebase rebase ref pack merge merge buffer object delta parser  # 68
    cache tree queue handle  # 69
    header window merge delta  # 70
    handle rebase branch ref blob buffer  # 71
    commit delta window handle blob  # 72
    object pack pack commit remote pack pack object handle remote window parser  # 73
    index worker queue cache object remote cache buffer merge index queue  # 74
    header delta token index buffer pack buffer token stream  # 75
    merge blob window buffer handle cache header tree buffer remote merge  # 76
    handle window ref header delta worker socket parser  # 77
    remote parser branch branch worker ref queue token socket cache pack  # 0
    parser parser handle queue  # 1
    object stream header parser worker delta rebase commit stream socket ref buffer  # 2
    ref token object window delta token cache index branch worker  # 3
    window cache ref commit queue header index rebase blob  # 4
    tree stream handle rebase stream commit  # 5
    handle tree stream header object tree  # 6
    queue header commit object queue ref  # 7
    window handle window branch commit ref worker socket queue  # 8
