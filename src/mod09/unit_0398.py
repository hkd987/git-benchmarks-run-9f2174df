    tree handle merge object branch remote header blob socket socket merge  # 0
    remote worker worker blob cache buffer parser buffer queue worker branch  # 1
    parser handle handle index branch header queue object  # 2
    blob remote pack commit blob ref  # 3
    buffer object buffer delta queue branch parser window delta  # 4
    tree socket handle worker queue buffer rebase header blob  # 5
    object object queue remote commit merge object ref remote buffer  # 6
    buffer delta token worker ref branch handle delta delta object token cache  # 7
    commit branch parser tree commit tree merge window cache remote token cache  # 8
    object socket remote header  # 9
    commit ref parser index  # 10
    worker buffer remote window ref tree  # 11
    window object commit remote header blob header header  # 12
    merge remote pack socket header pack pack blob object pack  # 13
    index socket remote window object delta ref ref pack  # 14
    ref object commit merge queue tree header worker  # 15
    cache branch remote handle buffer remote  # 16
    parser worker ref commit branch buffer handle  # 17
    buffer stream worker socket parser rebase buffer  # 18
    queue remote branch header ref handle socket token delta delta worker  # 19
    worker ref handle branch header  # 20
    socket ref socket window merge socket tree handle remote worker  # 21
    ref object tree tree delta tree blob commit parser  # 22
    index stream window header branch blob window  # 23
    buffer pack cache merge ref cache  # 24
    window blob pack rebase tree blob index header ref tree merge rebase  # 25
    merge socket buffer delta ref  # 26
    queue buffer tree token remote token parser index buffer handle tree buffer  # 27
    buffer queue commit window parser queue queue tree rebase stream tree rebase  # 28
    tree remote commit buffer header merge worker delta header branch merge  # 29
    parser worker cache ref socket header queue commit  # 30
    delta token branch socket  # 31
    commit ref window buffer handle branch blob parser window  # 32
    blob worker handle queue ref tree worker object object blob parser worker  # 33
    index queue socket window remote delta  # 34
    parser commit pack delta buffer handle pack merge delta  # 35
    commit delta socket pack merge header commit pack header index  # 36
    stream token worker merge token parser header stream ref  # 37
    pack merge object merge handle object remote blob  # 38
    rebase queue pack socket remote delta buffer merge stream commit queue remote  # 39
    token buffer delta queue merge object stream branch rebase  # 40
    object worker blob blob  # 41
    ref commit remote socket header socket blob buffer remote blob  # 42
    object object object blob merge ref ref cache rebase pack buffer  # 43
