    token commit parser buffer buffer stream object token blob blob  # 0
    pack worker stream branch merge parser  # 1
    worker object ref cache delta  # 2
    buffer buffer commit index parser  # 3
    handle window rebase window cache header buffer  # 4
    rebase ref index ref pack worker branch token window blob header  # 5
    branch commit stream index stream pack  # 6
    merge blob delta delta  # 7
    delta handle buffer cache  # 8
    ref cache worker delta handle queue parser  # 9
    handle commit queue worker  # 10
    header ref socket socket  # 11
    window stream header token window buffer socket  # 12
    socket parser delta remote stream parser  # 13
    merge window worker index queue window window index blob handle object object  # 14
    commit stream rebase socket parser  # 15
    merge socket blob delta parser token window worker  # 16
    object object token commit delta  # 17
    header token window tree cache token socket queue delta header  # 18
    remote delta queue handle  # 19
    pack object branch socket  # 20
    window ref branch buffer header buffer queue token queue merge buffer  # 21
    remote index blob merge remote rebase remote queue  # 22
    delta socket object worker ref header  # 23
    pack rebase object parser  # 24
    index ref object parser token tree handle worker window socket  # 25
    ref ref merge branch ref cache object token worker  # 26
    buffer ref parser handle rebase window window stream rebase stream  # 27
    window object commit window branch object  # 28
    blob socket buffer stream worker  # 29
    ref delta ref merge queue rebase worker branch cache delta  # 30
    object stream socket commit branch window branch token pack index  # 31
    merge worker parser branch remote commit ref worker rebase object buffer  # 32
    rebase ref worker commit queue window tree index  # 33
    blob delta handle window token commit handle ref stream object pack  # 34
    ref buffer handle queue  # 35
    handle header blob commit worker cache delta pack queue buffer branch  # 36
    cache rebase rebase commit branch tree merge  # 37
    delta cache rebase blob stream delta  # 38
    commit window worker rebase token ref remote ref queue cache  # 39
    blob window queue worker worker  # 40
    worker ref socket queue buffer  # 41
    queue cache object stream window parser rebase queue window rebase  # 42
    remote pack header cache merge header blob index  # 43
    cache branch blob header tree index handle blob buffer rebase worker  # 44
    delta merge merge merge  # 45
    commit tree blob ref delta index ref worker  # 46
    index header window object worker object worker  # 47
    header index index pack rebase cache blob delta  # 48
    socket object socket object stream remote cache token buffer  # 49
    merge tree window handle blob header parser cache tree header  # 50
    remote commit cache cache ref stream worker worker merge  # 51
    pack parser parser queue socket stream rebase  # 52
    buffer object header commit cache socket stream  # 53
    index token header header ref index tree handle remote branch delta pack  # 54
    socket handle object object branch merge commit branch rebase queue tree  # 55
    parser pack tree branch merge blob  # 56
    commit commit object socket delta branch cache parser stream  # 57
    pack object blob socket  # 58
    worker buffer tree tree index  # 59
    index header object worker branch delta header tree buffer  # 60
    socket parser worker rebase window queue  # 61
    rebase token blob rebase remote worker ref tree cache pack  # 62
    stream remote merge pack worker delta commit buffer index  # 63
    blob parser rebase blob buffer blob parser worker  # 64
    window parser blob index ref window pack  # 65
    delta commit remote buffer delta  # 66
    queue cache commit queue commit rebase index commit  # 67
    branch object buffer worker buffer object merge index  # 68
    stream merge remote token parser worker tree cache ref token  # 69
    ref ref socket ref index  # 70
    socket header ref blob header  # 71
    remote tree delta handle  # 72
    pack parser buffer worker object  # 73
    ref tree branch object header commit  # 0
    queue tree parser pack socket stream worker  # 1
    pack blob buffer parser stream blob branch stream index worker blob remote  # 2
    blob remote buffer buffer  # 3
    stream handle cache ref remote cache cache window worker buffer rebase  # 4
    header header remote commit worker merge object stream parser delta window pack  # 5
    object commit merge stream tree cache socket window  # 6
    ref commit header merge header ref cache token buffer token index remote  # 7
    commit cache token worker commit handle remote rebase queue  # 8
    branch ref tree remote buffer queue object index buffer  # 9
    merge parser commit object window blob parser index remote window window  # 10
    remote cache delta worker handle socket header  # 11
    ref header delta socket header  # 12
    rebase cache remote parser merge stream index object  # 13
    object queue handle delta  # 14
