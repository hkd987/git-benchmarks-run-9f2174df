    ref window header object  # 0
    rebase stream merge delta buffer header handle stream handle delta cache stream  # 1
    pack token queue cache header commit handle  # 2
    delta remote delta rebase branch tree window socket handle object  # 3
    worker object object object object buffer token socket token  # 4
    handle merge object handle rebase  # 5
    stream socket window window merge blob header branch  # 6
    stream window remote ref header header  # 7
    cache token merge handle  # 8
    window tree queue stream pack window  # 9
    remote rebase socket handle merge pack socket remote branch worker remote  # 10
    cache commit socket delta handle header ref index queue rebase blob  # 11
    ref parser socket ref header queue remote remote header blob branch  # 12
    header merge index delta cache parser blob buffer header  # 13
    queue pack ref blob branch buffer object  # 14
    worker ref window tree object queue tree tree header parser  # 15
    commit object handle commit remote remote object pack pack parser  # 16
    tree rebase remote pack object queue index  # 17
    window socket index object merge handle  # 18
    merge commit tree rebase window pack commit buffer  # 19
    parser header parser rebase rebase rebase blob index blob ref tree  # 20
    pack index ref handle worker object  # 21
    handle header queue parser stream  # 22
    tree branch merge pack remote token ref delta handle merge parser header  # 23
    merge token token parser remote tree stream  # 24
    queue parser token socket commit handle index queue queue rebase delta rebase  # 25
    window index cache tree cache  # 26
    rebase worker tree object cache header  # 27
    queue branch branch branch stream ref pack pack delta commit header  # 28
    remote merge rebase queue blob  # 29
    handle tree delta merge header socket rebase socket  # 30
    rebase tree object remote queue branch branch merge ref remote  # 31
    cache ref branch tree rebase buffer  # 32
    handle object object remote token stream handle tree  # 33
    commit cache stream delta header queue  # 34
    delta blob handle handle  # 35
