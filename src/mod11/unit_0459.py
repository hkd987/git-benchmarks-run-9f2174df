    handle ref commit object  # 0
    socket commit header token merge token delta header remote parser index remote  # 1
    branch queue ref buffer remote stream stream  # 2
    index blob tree branch branch remote window stream index handle branch handle  # 3
    token object branch header cache  # 4
    ref cache buffer remote buffer branch worker commit ref  # 5
    handle worker delta queue queue token handle  # 6
    worker buffer branch tree socket  # 7
    worker header commit commit pack handle index window stream window  # 8
    pack stream tree index ref worker worker tree branch  # 9
    buffer window socket tree ref remote stream ref index cache branch stream  # 10
    socket rebase remote rebase index pack  # 11
    stream queue object cache merge parser buffer  # 12
    pack handle stream object rebase rebase branch parser  # 13
    stream cache delta parser window blob branch commit commit  # 14
    ref ref handle socket branch commit worker rebase branch buffer object  # 15
    queue window delta commit rebase ref pack handle handle  # 16
    commit stream delta token delta index buffer  # 17
    branch remote object worker  # 18
    branch object index object ref  # 19
    header ref worker delta window stream index rebase window  # 20
    rebase ref index commit delta remote merge index merge index socket worker  # 21
    header delta ref branch tree cache stream handle blob header header stream  # 22
    worker tree handle token stream queue queue stream token buffer tree object  # 23
    window worker buffer handle pack stream queue token handle socket cache object  # 24
    ref token commit rebase commit tree window parser  # 25
    header queue buffer remote object pack  # 26
    queue blob header commit worker  # 27
    window branch parser stream handle delta ref delta merge window  # 28
    merge header header delta tree  # 29
    blob remote commit ref remote buffer handle  # 30
    queue object stream pack  # 31
    remote delta branch delta  # 32
    buffer header queue header merge socket merge blob handle header queue pack  # 33
