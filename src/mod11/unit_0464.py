    cache rebase window delta socket worker queue  # 0
    delta commit socket queue commit branch index header branch queue blob branch  # 1
    handle merge branch parser index tree token delta  # 2
    ref object buffer tree remote  # 3
    pack tree token parser blob ref branch header object index  # 4
    object handle tree queue commit parser merge index ref  # 5
    delta queue index worker stream parser window merge blob index  # 6
    stream token socket buffer merge blob commit worker  # 7
    worker delta stream object tree token delta  # 8
    delta index token worker ref cache delta remote delta merge  # 9
    cache stream stream queue remote merge window handle  # 10
    parser worker parser parser window queue  # 11
    remote header remote handle window delta rebase commit pack cache header  # 12
    stream commit remote buffer token index branch cache socket blob remote cache  # 13
    cache tree handle merge queue window header socket merge  # 14
    socket tree header remote queue rebase  # 15
    handle token delta window queue blob queue parser  # 16
    socket merge commit header rebase  # 17
    parser buffer commit token stream remote buffer delta rebase delta merge parser  # 18
    token buffer delta pack token header stream  # 19
    merge queue cache index worker token  # 20
    delta token object delta commit delta  # 21
    window worker ref object blob window  # 22
    window queue queue window delta branch header object header token cache index  # 23
    commit handle tree ref worker delta branch  # 24
    buffer cache token tree rebase tree  # 25
    header cache buffer object parser worker handle socket handle stream stream window  # 26
    merge rebase merge cache delta handle rebase stream socket index branch rebase  # 27
    merge buffer parser delta object parser commit queue socket cache  # 28
    cache socket token worker  # 29
    commit delta rebase index window  # 30
    token blob stream worker delta  # 31
    merge index branch delta queue merge index  # 32
    object rebase blob buffer buffer queue ref commit socket index parser  # 33
    tree parser delta header index commit stream handle  # 34
    merge commit merge queue commit token handle cache worker branch index  # 35
    worker socket object stream branch handle stream branch socket object merge  # 36
    merge rebase worker rebase buffer parser blob blob buffer  # 37
    handle parser token delta blob delta handle parser pack  # 38
    queue stream parser commit blob worker  # 39
    buffer rebase socket worker socket blob index ref delta  # 40
    remote handle buffer handle parser parser  # 41
    commit blob commit commit rebase object  # 42
    remote window object index rebase  # 43
    merge pack ref remote merge  # 44
    branch stream index window tree index socket queue  # 45
    token ref merge window index delta pack  # 46
    parser buffer stream delta buffer rebase worker stream worker merge rebase tree  # 47
    pack stream stream queue queue  # 48
