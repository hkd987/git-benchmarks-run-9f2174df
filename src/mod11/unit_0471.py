    token token remote pack delta rebase cache  # 0
    handle stream merge tree window merge header cache queue tree  # 1
    blob commit tree queue object index delta commit socket worker cache  # 2
    blob socket branch remote merge parser pack buffer  # 3
    rebase ref delta blob  # 4
    token tree object blob buffer rebase rebase  # 5
    header window pack handle rebase parser  # 6
    queue header blob socket  # 7
    queue cache stream branch remote merge delta rebase worker token  # 8
    handle delta remote object branch queue branch tree merge rebase  # 9
    socket commit stream ref socket tree handle ref commit  # 10
    token ref parser rebase ref header token blob  # 11
    tree commit tree handle rebase handle token  # 12
    object header tree socket window remote queue worker  # 13
    buffer delta remote header header token object buffer queue  # 14
    handle buffer tree object worker blob object token token token buffer  # 15
    handle merge parser socket  # 16
    remote ref pack parser remote  # 17
    pack queue object commit parser header window index window queue index pack  # 18
    window token ref delta queue cache buffer blob object remote stream queue  # 19
    token pack queue rebase delta  # 20
    branch remote branch remote merge handle  # 21
    socket token delta merge object commit window blob  # 22
    cache header window index handle socket  # 23
    rebase parser commit merge index  # 24
    tree commit stream branch remote buffer  # 25
    object cache ref rebase remote remote worker pack queue merge rebase merge  # 26
    branch queue commit commit delta branch buffer blob rebase queue handle  # 27
    merge delta stream ref merge socket stream branch remote  # 28
    branch handle delta worker queue pack ref index parser object  # 29
    delta header window rebase merge stream pack parser delta handle queue  # 30
    cache queue queue object queue  # 31
    index commit buffer token parser blob parser rebase remote worker header  # 32
    merge socket index token parser commit remote stream token pack  # 33
    worker remote commit buffer worker index merge  # 34
    pack parser tree tree object queue delta delta socket pack  # 35
    handle handle pack buffer handle token branch remote  # 36
    token commit stream parser delta index token stream remote handle pack  # 37
    merge tree branch socket  # 38
    ref header token object buffer merge cache header buffer  # 39
    socket token stream delta merge remote header rebase  # 40
    blob parser socket buffer header branch  # 41
    pack token worker handle pack pack commit queue delta parser  # 42
    worker index index socket window  # 43
    object merge token remote buffer ref commit pack  # 44
    queue buffer parser worker stream blob blob remote  # 45
    commit worker handle blob commit ref remote index token token ref  # 46
    remote cache object token ref index tree  # 47
    cache tree cache worker ref index rebase object  # 48
    cache socket socket pack blob commit  # 49
    tree header rebase rebase commit  # 50
    token merge merge buffer worker pack object pack  # 51
    tree index token branch index cache object window ref rebase  # 52
    parser window cache index parser remote pack commit blob socket  # 53
    socket index rebase buffer merge pack socket  # 54
    cache branch ref header header delta stream merge header socket handle token  # 55
    object cache commit cache worker branch ref  # 56
    buffer branch queue socket branch rebase commit token  # 57
    merge handle parser queue header commit queue ref header blob ref buffer  # 58
    rebase ref pack pack handle window rebase rebase cache  # 59
    tree merge tree window  # 60
    window index merge index handle queue parser tree index object  # 61
