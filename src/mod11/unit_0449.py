    ref remote index token handle object  # 0
    parser index stream index handle rebase  # 1
    commit stream handle delta stream parser remote  # 2
    remote pack ref stream worker remote rebase  # 3
    object cache header header handle commit socket tree commit pack worker  # 4
    header index worker handle worker buffer delta delta parser remote rebase tree  # 5
    merge worker buffer header branch stream branch ref object worker stream  # 6
    handle header ref rebase blob socket worker  # 7
    handle worker handle commit header blob delta commit merge  # 8
    delta merge pack token rebase commit worker remote ref parser ref  # 9
    socket blob queue commit delta  # 10
    header handle index ref worker cache socket pack commit handle stream rebase  # 11
    pack delta socket parser  # 12
    window delta stream rebase commit remote merge  # 13
    index socket merge window branch window rebase queue commit delta ref token  # 14
    stream token socket object stream remote remote rebase tree parser index  # 15
    cache branch token cache branch handle handle worker pack  # 16
    cache index queue commit merge parser index token  # 17
    object handle window index  # 18
    merge parser queue blob handle header  # 19
    pack handle cache handle cache tree  # 20
    index socket merge socket delta index buffer queue parser delta queue rebase  # 21
    cache parser ref handle handle tree stream worker branch  # 22
    header header ref socket stream token header branch object  # 23
    ref index cache merge object  # 24
    parser header queue index stream object header blob index  # 25
    stream header handle remote  # 26
    branch rebase parser header index pack  # 27
    branch pack tree parser  # 28
    remote blob token commit merge ref tree remote socket object remote blob  # 29
    branch pack remote remote rebase rebase remote branch index  # 30
    ref token object object buffer ref ref object cache header stream pack  # 31
    commit merge socket queue cache remote parser window queue  # 32
    window branch merge remote queue stream queue token cache branch branch cache  # 33
    branch ref ref stream rebase index queue blob cache ref ref  # 34
    stream blob branch index tree cache  # 35
    queue header pack stream blob header rebase ref pack pack cache  # 36
    cache stream object delta stream branch parser merge window header  # 37
    cache rebase stream token parser merge socket handle queue queue  # 38
    window window buffer token socket stream remote handle branch tree remote token  # 39
    queue delta token object rebase remote worker  # 40
    pack commit commit header  # 41
    worker tree window tree blob  # 42
    cache token stream stream parser  # 43
    commit merge worker header ref remote remote stream  # 44
    tree ref ref socket ref socket worker blob window token  # 45
    delta delta worker object cache queue object parser index token header  # 46
    header object blob queue parser merge cache rebase stream buffer  # 47
    merge delta handle header  # 48
    pack tree buffer parser token handle rebase window window worker pack rebase  # 49
    pack socket tree token ref parser  # 50
    object rebase token buffer handle stream pack handle merge worker cache  # 51
    merge merge window delta index socket  # 52
    pack ref pack object window queue rebase commit cache  # 53
    index ref buffer handle blob merge parser pack parser delta handle  # 54
    queue window socket window handle remote buffer remote socket  # 55
    handle index token pack parser  # 56
    tree parser token delta handle parser rebase handle blob queue  # 57
    ref rebase handle cache index merge window delta object rebase queue delta  # 58
    merge header branch object delta token pack pack  # 59
    header ref blob delta stream rebase buffer  # 60
    parser buffer blob window handle cache object blob blob rebase commit  # 61
    index remote commit handle cache  # 62
    object queue pack merge stream pack blob  # 63
    remote parser object cache socket  # 64
    branch ref pack merge token delta socket  # 65
    object remote buffer pack  # 66
    commit worker token pack commit cache ref token merge window  # 67
    worker branch ref buffer parser tree tree buffer worker  # 68
    handle parser header buffer rebase remote object  # 69
    cache object handle socket stream rebase queue ref  # 70
    stream object buffer branch  # 71
    tree token ref delta blob rebase parser header buffer worker  # 72
    queue handle handle pack cache blob  # 73
    stream commit queue ref branch handle buffer object  # 74
    parser queue cache parser handle remote window buffer branch pack queue  # 75
    header handle ref rebase window stream  # 76
    worker tree commit blob buffer remote handle  # 77
    ref worker window remote queue handle stream worker  # 78
    branch socket queue index buffer remote  # 79
    stream queue rebase commit ref  # 80
