    handle tree index tree handle remote object rebase queue stream  # 0
    blob worker worker buffer commit tree header index  # 1
    token blob cache delta  # 2
    window buffer ref cache ref merge queue stream object commit  # 3
    socket commit commit queue parser worker pack worker handle delta ref commit  # 4
    window socket remote cache commit object handle rebase blob stream tree  # 5
    commit parser parser cache header queue rebase index cache branch pack  # 6
    pack commit cache handle commit queue  # 7
    parser remote object branch window  # 8
    rebase blob ref remote worker queue merge handle object window commit delta  # 9
    buffer merge ref commit blob object header token queue header commit  # 10
    ref commit buffer delta window token pack tree  # 11
    blob stream ref token header stream  # 12
    pack header merge header commit rebase index  # 13
    ref branch cache header  # 14
    header ref parser branch cache merge queue buffer  # 15
    ref socket buffer parser  # 16
    tree remote ref header blob  # 17
    queue ref index tree  # 18
    header index remote rebase  # 19
    token pack buffer object merge parser queue object object header index  # 20
    index queue cache rebase window stream ref merge window buffer  # 21
    blob rebase pack blob header pack parser queue remote  # 22
    object blob token delta merge branch queue cache parser header  # 23
    object socket buffer index queue queue blob tree  # 24
    index remote header handle worker queue handle  # 25
    pack ref index blob branch handle buffer index cache  # 26
    buffer ref merge window window socket commit cache stream  # 27
    index stream header socket cache header stream  # 28
    tree header index header object  # 29
    parser worker pack pack tree parser index ref buffer commit blob  # 30
    pack queue blob object header branch queue worker pack header  # 31
    window tree commit delta merge buffer delta index cache remote  # 32
    header token socket pack parser merge token merge parser window buffer  # 33
    window object rebase blob tree token merge tree tree commit ref parser  # 34
    socket token object blob index rebase  # 35
    queue object remote remote delta commit worker header commit cache ref  # 36
    cache buffer remote cache commit queue stream  # 37
    remote queue header branch token queue object rebase handle  # 38
    ref rebase cache buffer cache object token token pack blob ref parser  # 39
    rebase ref pack parser object remote  # 40
