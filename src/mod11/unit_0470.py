    stream token pack delta queue remote remote queue buffer socket token  # 0
    branch queue delta branch window token merge stream  # 1
    index parser index ref branch parser worker socket commit parser tree rebase  # 2
    token worker parser queue object token buffer token token queue remote  # 3
    branch rebase merge window rebase worker tree rebase socket  # 4
    buffer stream merge merge stream token  # 5
    handle merge blob blob buffer window index  # 6
    commit parser pack parser worker handle  # 7
    blob parser delta token blob window pack commit parser stream socket  # 8
    header socket worker window commit header pack buffer buffer  # 9
    header handle buffer window branch socket token delta object  # 10
    handle rebase pack ref index  # 11
    socket commit index queue handle  # 12
    pack buffer commit parser token parser socket commit cache index cache header  # 13
    stream header rebase handle rebase object  # 14
    buffer index header worker commit  # 15
    delta remote delta token token window stream  # 16
    rebase token cache worker cache rebase cache object merge  # 17
    header remote object cache parser  # 18
    merge delta socket token buffer worker rebase handle  # 19
    worker handle object commit object rebase pack  # 20
    object handle object worker queue ref worker header queue commit  # 21
    token object queue commit handle queue buffer socket stream tree  # 22
    buffer parser worker stream cache ref index handle pack  # 23
    cache handle worker queue remote  # 24
    object socket header window handle delta rebase pack worker remote branch object  # 25
    blob blob window tree delta pack branch index branch remote branch  # 26
    rebase object index cache token window pack worker remote merge worker  # 27
    cache stream window branch socket blob blob handle tree socket  # 28
    pack cache parser tree commit buffer worker handle  # 29
    merge ref commit rebase parser delta window delta  # 30
    index ref pack ref pack ref window branch index window branch tree  # 31
    merge socket merge object pack  # 32
    token blob remote remote tree remote remote socket window tree  # 33
    queue window window stream index buffer cache stream commit pack  # 34
    commit token socket delta cache blob branch header handle cache socket window  # 35
    cache tree cache branch header queue  # 36
    token queue stream parser index worker buffer  # 37
    remote remote ref remote delta tree parser  # 38
    header handle window pack blob stream  # 39
    commit handle buffer cache ref buffer window blob handle parser remote  # 40
    remote object tree header  # 41
    window buffer tree blob  # 42
    pack remote header merge branch buffer commit header object buffer rebase pack  # 43
    socket token object commit parser commit socket header  # 44
    tree header handle merge token window blob window merge branch token  # 45
    header commit buffer blob branch window token delta branch handle window tree  # 46
    token window rebase ref queue remote stream  # 47
    handle token socket object parser delta  # 48
    rebase pack ref pack tree remote cache branch pack queue  # 49
    pack header rebase stream pack  # 50
    queue worker blob object branch blob branch handle index merge merge  # 51
    handle cache delta commit token ref rebase index parser  # 52
    index handle handle worker cache token merge buffer  # 53
    commit queue blob header buffer buffer window  # 54
    ref pack queue header header stream  # 55
    stream object pack delta header buffer pack  # 56
    commit tree delta worker commit  # 57
    parser tree blob cache token window queue  # 58
    socket window delta branch merge commit token handle parser index  # 59
    object ref commit remote remote  # 60
    queue queue branch ref pack socket pack token stream token branch  # 61
    queue object queue token buffer queue pack  # 62
    object stream merge commit parser  # 63
    worker merge handle queue commit branch branch ref  # 64
    tree buffer buffer header delta  # 65
    window parser stream cache token rebase tree worker worker socket parser socket  # 66
