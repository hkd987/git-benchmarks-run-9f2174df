    socket pack branch branch socket rebase parser  # 0
    queue header rebase blob branch  # 1
    index header header commit worker stream rebase branch parser socket  # 2
    parser branch merge pack commit socket index pack handle  # 3
    cache branch object parser index token delta header object ref  # 4
    window object branch parser cache header object branch  # 5
    commit header tree index parser delta blob stream remote window cache worker  # 6
    ref object tree cache delta token token  # 7
    worker worker rebase tree socket stream commit commit queue stream worker  # 8
    worker remote delta tree handle  # 9
    stream ref branch branch tree remote merge merge header socket  # 10
    pack cache cache token merge  # 11
    object stream delta parser parser delta socket tree tree  # 12
    merge delta parser token commit index queue blob cache  # 13
    worker rebase token pack buffer commit stream  # 14
    remote cache object tree queue handle queue ref index  # 15
    index merge handle commit header  # 16
    merge socket parser buffer token merge stream ref  # 17
    queue parser queue commit object window index token object  # 18
    ref stream index header object  # 19
    rebase queue header pack queue  # 20
    queue pack stream ref object window blob index merge index  # 21
    parser buffer parser blob header merge delta  # 22
    merge blob ref token branch delta cache cache rebase parser  # 23
    commit rebase remote header worker buffer socket  # 24
    window rebase pack socket token  # 25
    commit token merge parser delta pack cache pack queue object  # 26
    parser stream parser socket blob object remote queue blob header  # 27
    branch buffer blob parser  # 28
    rebase rebase worker tree header blob commit remote rebase branch  # 29
    delta stream branch rebase header window stream  # 30
    ref branch remote window delta  # 31
    buffer remote rebase delta socket buffer branch branch parser merge pack  # 32
    pack socket queue cache tree ref  # 33
    window object queue token worker  # 34
    branch pack parser token branch index  # 35
    socket branch blob index window object  # 36
    worker worker buffer socket handle  # 37
    blob ref commit socket tree index worker handle  # 38
    index header object rebase parser buffer tree branch buffer pack  # 39
    commit blob commit object tree worker header  # 40
    worker pack ref token tree  # 41
    object header index token window index queue window stream delta  # 42
    ref blob worker cache blob  # 43
    buffer window stream ref header merge pack  # 44
    parser parser header delta ref  # 45
    worker pack cache object branch commit  # 46
    tree cache merge commit stream merge  # 47
    delta window rebase pack stream  # 48
    index cache rebase token blob  # 49
    index cache tree delta header handle stream socket merge parser  # 50
    object ref window header remote  # 51
    parser header socket index ref header branch header cache  # 52
    parser buffer parser branch object queue handle cache queue rebase merge  # 53
    cache handle queue object merge index parser  # 54
    delta worker token stream remote blob index token  # 55
    remote socket branch branch ref cache cache worker index commit window  # 56
    token token worker buffer rebase index stream blob ref stream socket rebase  # 57
    ref buffer rebase rebase branch merge  # 58
    merge buffer tree socket socket object tree ref parser object tree  # 59
    worker buffer commit cache ref  # 60
    socket tree token handle branch token socket  # 61
    socket stream pack socket socket handle parser merge tree tree socket  # 62
    worker index branch ref ref  # 63
    buffer window object queue delta merge merge  # 64
    parser buffer branch queue pack stream object pack buffer branch  # 65
    stream buffer remote commit blob  # 66
    delta handle commit delta blob ref object worker commit buffer  # 67
    delta index branch cache queue pack  # 68
    index branch ref commit handle parser rebase stream handle socket branch  # 69
    rebase rebase parser branch buffer header worker tree rebase stream object  # 70
    token ref commit branch parser header parser index commit index  # 71
    merge queue delta branch cache delta window tree queue rebase ref  # 72
    tree worker buffer merge parser delta blob header handle object pack ref  # 73
    remote token cache index header ref ref ref queue queue  # 74
    tree rebase pack worker socket  # 75
    rebase branch index handle delta buffer merge  # 76
    commit buffer cache object cache handle socket header cache  # 77
    token socket worker delta parser socket stream object buffer buffer  # 78
    window ref worker ref handle commit rebase  # 79
    remote object pack queue worker cache  # 80
    handle pack commit object queue handle branch  # 81
    blob object pack index token parser pack worker  # 0
    merge object branch token  # 1
    header header branch socket pack  # 2
    object delta blob delta  # 3
    object object parser branch parser  # 4
    branch handle window stream  # 5
    branch index buffer worker blob pack delta queue  # 6
    window branch token tree cache token rebase window remote queue  # 7
