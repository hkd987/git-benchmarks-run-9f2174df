    worker index parser token  # 0
    tree worker buffer index merge remote handle  # 1
    queue handle delta object socket tree cache  # 2
    header header blob delta  # 3
    ref pack rebase window remote token  # 4
    window blob socket object stream window index commit header header  # 5
    token token worker remote token  # 6
    token index token header  # 7
    blob remote window buffer index tree merge commit queue  # 8
    queue socket worker socket token rebase header  # 9
    ref remote branch window stream remote blob index delta delta rebase window  # 10
    tree index token window header cache buffer remote socket parser index remote  # 11
    header header queue tree tree buffer buffer remote socket  # 12
    blob object socket object queue window tree  # 13
    pack cache merge merge window ref header index stream stream blob buffer  # 14
    cache queue worker ref commit merge handle merge blob delta object handle  # 15
    stream ref header queue worker token stream tree token  # 16
    parser parser buffer blob  # 17
    cache rebase index queue  # 18
    rebase ref stream branch index rebase  # 19
    pack worker header socket rebase buffer window rebase socket  # 20
    rebase ref handle queue index worker ref stream worker commit object  # 21
    merge remote parser cache worker delta socket stream socket  # 22
    parser worker merge buffer stream  # 23
    ref worker branch branch handle delta remote merge  # 24
    remote queue stream worker  # 25
    branch remote delta blob object pack tree queue commit  # 26
    socket pack index merge worker socket branch merge object  # 27
    index remote delta branch remote remote pack tree tree worker ref parser  # 28
    blob commit socket rebase index parser window index blob branch delta  # 29
    cache object worker stream object pack handle index token index blob  # 30
    header socket buffer tree  # 31
    cache worker cache buffer rebase handle tree rebase remote token  # 32
    merge handle stream socket rebase merge pack header ref rebase queue  # 33
    merge handle handle delta worker handle cache branch handle object  # 34
    cache index worker tree blob remote remote socket handle stream  # 35
    remote branch queue tree cache stream pack  # 36
    branch index delta delta  # 37
    merge merge queue merge buffer  # 38
    parser header stream blob  # 39
    buffer socket socket token commit token buffer buffer token stream index  # 40
    tree blob queue index index pack socket delta branch rebase  # 41
    buffer socket pack remote  # 42
    window header worker rebase blob cache worker delta pack remote window cache  # 43
    rebase ref remote index remote object index socket  # 44
    ref ref blob merge cache tree blob parser stream buffer branch ref  # 45
    token index branch commit queue handle commit merge parser  # 46
    merge header socket object queue rebase tree tree  # 47
    commit token token commit commit handle buffer  # 48
    token cache ref delta delta  # 49
    branch queue branch delta  # 50
    delta branch object blob delta  # 51
    queue worker header remote branch cache buffer parser ref branch header delta  # 52
    worker remote worker rebase remote worker stream branch window buffer commit  # 53
    object branch buffer header cache blob handle ref rebase queue merge delta  # 54
    rebase cache delta rebase object  # 55
    commit ref handle index buffer worker  # 56
    commit blob rebase tree delta rebase index header parser cache  # 57
    object blob merge remote rebase blob parser queue worker  # 58
    index pack handle stream pack index blob worker handle merge remote index  # 59
    merge socket pack worker merge parser cache ref queue object  # 60
    handle index worker commit blob object blob commit socket worker worker rebase  # 61
    stream token delta cache buffer  # 62
    queue worker token tree blob socket handle  # 63
    window header socket pack buffer handle  # 64
    stream socket merge ref buffer socket  # 65
    parser socket commit cache handle pack merge  # 66
    tree cache stream tree socket cache  # 67
    commit remote stream rebase tree  # 68
