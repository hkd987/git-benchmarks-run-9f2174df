    buffer worker handle object header  # 0
    worker commit header tree stream token stream token cache merge  # 1
    cache tree object blob branch rebase rebase worker buffer  # 2
    object stream merge object commit handle parser remote parser window socket  # 3
    commit worker blob cache  # 4
    remote rebase branch stream remote token blob worker branch merge  # 5
    handle rebase token commit handle socket header header index handle rebase queue  # 6
    socket buffer rebase delta branch  # 7
    branch commit worker handle merge delta parser queue blob worker handle  # 8
    remote header header queue worker window branch delta index window  # 9
    token tree worker parser queue parser buffer stream socket queue remote  # 10
    index queue header index rebase rebase remote token  # 11
    remote remote delta tree blob blob blob worker socket index  # 12
    branch worker ref queue ref merge object rebase pack cache  # 13
    window delta remote stream ref remote blob queue cache blob tree blob  # 14
    rebase handle remote blob remote ref index queue socket pack  # 15
    handle tree stream buffer queue stream queue blob index index  # 16
    token ref branch branch tree delta ref buffer window window queue index  # 17
    buffer delta socket ref tree  # 18
    token worker delta blob socket socket  # 19
    queue branch stream token token buffer parser handle worker worker  # 20
    blob rebase token branch  # 21
    parser branch parser token  # 22
    cache commit token remote pack queue ref  # 23
    queue rebase commit window blob object socket object  # 24
    object commit rebase buffer branch ref object window commit window merge  # 25
    ref socket pack merge handle remote stream merge worker socket  # 26
    index socket merge queue branch queue buffer index ref  # 27
    header delta object object index handle index delta index parser branch stream  # 28
    handle stream tree branch  # 29
    queue window ref remote tree buffer  # 30
    ref tree header commit delta worker branch worker delta window  # 31
    index token pack ref header window  # 32
    branch handle rebase window socket buffer stream commit object  # 33
    blob header stream queue  # 34
    blob commit tree pack parser stream  # 35
    stream branch delta buffer buffer pack window queue  # 36
    rebase index parser merge blob rebase stream branch queue socket socket window  # 37
    stream handle rebase commit blob  # 38
    token token commit header remote commit socket queue worker handle  # 39
    tree header token cache buffer object branch window  # 40
    buffer handle branch token socket handle delta  # 41
    object window remote socket window window parser parser  # 42
    rebase merge cache token object object  # 43
    parser window queue queue socket worker delta pack handle  # 44
    buffer object worker remote buffer tree header pack index  # 45
    socket branch token ref stream handle  # 46
    token object token ref rebase cache delta worker parser commit merge ref  # 47
    tree handle token handle blob  # 48
    merge blob commit worker object queue  # 49
    branch token object merge handle ref delta window  # 50
    ref blob worker window branch worker handle  # 51
    index handle blob worker worker pack pack cache cache index window queue  # 52
    blob queue stream commit cache header header  # 53
    socket branch delta buffer tree parser  # 54
