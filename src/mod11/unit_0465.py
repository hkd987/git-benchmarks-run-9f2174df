    worker merge parser commit  # 0
    socket ref stream parser stream pack  # 1
    parser delta token merge  # 2
    delta blob worker header commit worker stream stream rebase tree  # 3
    merge ref stream remote rebase object handle rebase index blob commit  # 4
    branch pack cache handle  # 5
    delta socket ref rebase window queue header  # 6
    object rebase handle ref socket branch parser  # 7
    header branch worker handle remote  # 8
    merge cache pack commit cache handle queue parser ref buffer remote index  # 9
    worker index header pack branch remote stream tree worker pack handle worker  # 10
    remote object object handle delta branch remote blob object delta cache  # 11
    buffer queue window remote delta buffer window socket socket  # 12
    ref object index object handle stream  # 13
    blob object header stream window handle worker  # 14
    buffer delta header delta stream index ref parser pack cache cache remote  # 15
    blob worker parser blob buffer socket header blob cache  # 16
    header rebase buffer blob token branch commit token branch delta tree  # 17
    merge buffer cache commit remote queue commit tree window tree  # 18
    merge token stream token tree  # 19
    token token token object token delta delta index commit handle index  # 20
    token remote buffer index ref parser parser handle  # 21
    branch merge token commit socket buffer token stream cache handle  # 22
    commit queue window window rebase ref worker commit  # 23
    worker blob token socket  # 24
    rebase token remote parser  # 25
    merge buffer header stream index stream parser delta rebase tree  # 26
    stream cache cache worker  # 27
    tree window delta cache parser  # 28
    remote remote stream delta ref  # 29
    object pack blob parser header token  # 30
    merge socket tree tree remote tree buffer buffer worker tree  # 31
    blob merge cache object cache parser  # 32
    remote branch stream blob branch pack tree socket remote worker  # 33
    branch header blob blob object tree buffer token merge branch parser  # 34
    remote cache parser token cache merge  # 35
    rebase index merge parser cache  # 36
    handle queue cache remote rebase remote  # 37
