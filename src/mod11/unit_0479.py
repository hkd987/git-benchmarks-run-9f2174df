    handle parser token handle parser object socket  # 0
    merge pack delta socket object object remote commit handle stream  # 1
    remote remote merge rebase delta tree tree token blob  # 2
    header socket merge blob tree header remote  # 3
    object token blob index token cache index commit  # 4
    blob ref cache header header window handle commit cache object header remote  # 5
    queue ref ref queue rebase window handle ref merge token commit object  # 6
    remote worker queue index index pack header  # 7
    pack rebase remote token tree commit worker queue  # 8
    merge parser pack remote remote tree  # 9
    queue merge queue header  # 10
    stream rebase stream token window  # 11
    buffer token tree socket rebase token socket commit merge  # 12
    header token object branch buffer  # 13
    rebase delta commit blob token commit  # 14
    token merge commit cache header index handle  # 15
    stream merge ref pack commit pack merge tree stream buffer  # 16
    socket worker rebase remote worker delta branch blob  # 17
    socket branch merge branch  # 18
    commit tree cache worker branch buffer parser  # 19
    object tree commit remote window window  # 20
    worker tree stream commit  # 21
    rebase stream blob delta  # 22
    commit remote token buffer queue tree cache  # 23
    token queue window cache blob branch worker tree worker stream  # 24
    ref handle merge handle branch parser worker pack object window  # 25
    handle rebase handle rebase tree index commit parser queue ref  # 26
    delta queue handle pack ref worker  # 27
    delta window ref commit buffer parser  # 28
    index rebase blob delta commit socket token rebase branch header  # 29
    window branch buffer branch  # 30
    merge rebase branch merge  # 31
    delta object rebase commit branch delta  # 32
    parser queue header branch parser token remote index handle ref handle  # 33
    stream queue object header cache commit handle socket token stream  # 34
    worker socket worker tree worker commit token index  # 35
    ref buffer branch token rebase rebase  # 36
    worker index parser handle delta buffer parser  # 37
    merge pack window header commit delta commit window  # 38
    token blob stream header worker index branch cache branch header token  # 39
    commit merge commit socket socket  # 40
    ref branch token worker queue buffer  # 41
    rebase socket token buffer commit  # 42
    queue header branch socket socket handle remote header object commit queue commit  # 43
    queue socket token ref rebase  # 44
    tree index rebase parser ref commit pack header rebase socket stream commit  # 45
    stream queue merge handle handle pack blob branch  # 46
    blob ref commit index branch token window branch remote  # 47
    merge ref handle cache parser buffer  # 48
    ref handle merge merge blob  # 49
    remote index delta index object branch ref blob  # 50
    header blob window window socket stream stream blob queue worker buffer  # 51
    tree parser ref rebase pack  # 52
    blob socket buffer ref blob header commit handle  # 53
    rebase stream blob worker delta worker rebase header  # 54
    parser worker tree tree buffer stream branch pack commit socket handle  # 55
    branch blob tree handle pack header socket remote  # 56
    worker merge socket rebase  # 57
    socket buffer commit cache buffer remote merge window tree  # 58
    rebase branch delta rebase header delta tree  # 59
    buffer blob window ref  # 60
    queue branch cache object ref delta buffer  # 61
    ref tree commit cache blob commit blob stream buffer tree delta header  # 62
    worker queue remote worker tree  # 63
    header remote worker cache socket worker remote header socket header remote header  # 64
    ref queue buffer buffer  # 65
    branch delta window rebase rebase buffer handle index rebase blob  # 66
    object socket ref rebase rebase commit worker worker commit object pack  # 67
    ref commit token window queue socket stream handle  # 68
    branch cache stream blob ref header buffer  # 69
    window merge window index branch pack branch parser window blob blob  # 70
    rebase tree buffer stream pack handle  # 71
    window pack merge queue branch blob handle  # 72
    tree remote remote cache handle parser stream parser worker  # 73
    stream parser tree worker header header branch commit delta header header buffer  # 74
    queue token parser commit commit token handle header parser commit token socket  # 75
    buffer remote cache socket pack index pack handle ref cache tree  # 76
    buffer branch parser window commit index remote cache delta commit delta  # 77
    token stream stream merge  # 78
    queue queue stream parser worker tree  # 79
    commit queue delta buffer remote blob token  # 80
