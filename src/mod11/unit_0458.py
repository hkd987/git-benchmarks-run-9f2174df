    socket merge delta tree worker stream merge remote delta  # 0
    socket merge worker object branch blob commit  # 1
    parser parser cache merge header remote cache pack  # 2
    commit delta branch rebase socket remote socket index rebase cache merge branch  # 3
    stream parser token parser index queue  # 4
    token socket queue rebase  # 5
    remote rebase blob socket socket delta pack stream queue parser worker queue  # 6
    object stream remote tree worker socket buffer socket  # 7
    index socket blob commit stream rebase header window  # 8
    header socket cache remote  # 9
    commit tree stream queue queue queue queue rebase object tree worker  # 10
    merge commit remote token worker worker branch stream stream  # 11
    remote token ref remote pack branch stream buffer  # 12
    index token handle blob merge handle buffer  # 13
    commit object rebase socket queue buffer  # 14
    parser object rebase window blob  # 15
    cache cache buffer worker  # 16
    window worker index socket merge window pack merge stream  # 17
    blob pack ref buffer parser parser rebase index  # 18
    tree ref cache rebase commit blob index header header rebase  # 19
    branch token queue token delta blob pack socket parser branch worker merge  # 20
    merge pack branch header buffer  # 21
    cache socket header cache remote  # 22
    rebase token header commit branch queue parser queue pack worker commit token  # 23
    commit handle worker queue parser header handle window  # 24
    branch object token worker pack buffer commit window handle rebase commit handle  # 25
    token branch rebase index handle ref header token commit socket header parser  # 26
    worker stream tree window worker worker worker  # 27
    token header buffer pack stream index rebase token pack  # 28
    merge remote buffer pack  # 29
    socket ref delta token delta blob parser buffer pack  # 30
    object cache merge token blob parser socket token object worker pack merge  # 31
    rebase object blob tree index pack delta handle worker  # 32
    parser delta rebase ref socket  # 33
    worker stream ref token index delta parser token  # 34
    merge handle window pack pack commit cache  # 35
    branch stream cache pack tree parser rebase delta  # 36
    delta socket stream window cache  # 37
    stream window branch parser queue stream commit stream  # 38
    blob remote handle pack header parser stream remote tree handle  # 39
    object object index commit worker buffer token merge remote tree blob socket  # 40
    tree worker handle buffer ref  # 41
    buffer rebase buffer buffer branch buffer blob window rebase queue ref  # 42
    pack blob buffer buffer header blob merge  # 43
    merge blob handle index ref stream cache index window socket cache  # 44
    buffer commit buffer buffer remote tree queue window socket handle worker stream  # 45
    queue parser token buffer index cache ref socket ref stream pack  # 46
    rebase socket window pack token pack worker pack index commit merge ref  # 47
    remote pack ref header  # 48
    buffer pack header worker ref remote object  # 49
    parser cache tree object object tree  # 50
    rebase window commit stream pack branch  # 51
