    branch parser queue index merge remote socket  # 0
    ref handle commit parser window pack index tree  # 1
    token header pack cache socket token pack queue  # 2
    commit delta window commit pack index tree branch branch branch  # 3
    queue stream tree buffer window index socket  # 4
    remote blob pack token pack remote  # 5
    token branch pack merge remote buffer  # 6
    window cache commit stream object ref window commit  # 7
    merge tree queue worker window handle rebase header  # 8
    worker commit stream commit branch blob rebase commit rebase  # 9
    pack header branch parser merge ref delta header branch index object pack  # 10
    ref blob commit token branch  # 11
    merge tree merge window socket merge worker socket pack index pack branch  # 12
    pack socket ref commit buffer object token token commit buffer remote object  # 13
    parser blob tree queue  # 14
    tree pack window stream index tree socket merge  # 15
    buffer socket rebase worker token  # 16
    blob handle window cache cache header  # 17
    index buffer parser index branch worker merge tree  # 18
    object handle stream rebase worker delta object token branch  # 19
    pack object window worker cache socket  # 20
    index pack handle cache commit merge object ref merge stream merge window  # 21
    merge merge window handle rebase object socket index blob tree  # 22
    ref rebase stream stream stream index socket  # 23
    worker tree delta tree  # 24
    commit window cache index branch  # 25
    merge socket index socket  # 26
    socket index handle cache socket window  # 27
    parser commit blob handle window  # 28
    cache object index ref  # 29
    object branch worker blob pack delta remote worker worker index header  # 30
    parser pack delta buffer parser header index header parser header buffer  # 31
    blob window token tree rebase parser ref buffer rebase remote token  # 32
    tree remote delta window pack queue header token socket rebase  # 33
    merge ref commit delta rebase parser tree commit commit rebase merge  # 34
    branch delta parser token blob worker rebase queue window cache worker  # 35
    worker commit handle worker stream index buffer parser  # 36
    cache queue stream tree branch handle commit  # 37
    header handle header pack tree window  # 38
    tree token stream socket  # 39
    remote handle merge index index queue parser index worker delta delta queue  # 40
    commit queue parser token rebase parser header branch tree object  # 41
    cache ref socket handle delta ref queue token blob  # 42
    branch merge rebase socket header merge cache index index delta  # 43
    commit token branch merge window index token index blob  # 44
    handle handle rebase pack  # 45
    queue merge handle delta branch token window queue header merge  # 46
    tree socket pack remote  # 47
    stream merge tree remote  # 48
    object stream branch commit merge parser object commit token  # 49
    index blob stream token window remote window pack  # 50
    ref socket ref handle worker socket delta buffer window pack merge parser  # 51
    ref merge handle cache ref token buffer window delta handle cache handle  # 52
    socket cache worker parser cache remote header stream  # 53
    blob parser index cache handle buffer header blob worker  # 54
    worker commit remote commit  # 55
    queue buffer ref object object pack cache window  # 56
    index pack queue handle remote ref delta pack  # 57
    worker remote delta worker window  # 58
    socket object handle index header ref  # 59
    stream merge cache handle tree pack tree object  # 60
    handle queue rebase delta handle merge rebase  # 61
    socket remote socket branch remote  # 62
    token merge cache object object blob  # 63
    remote remote parser pack rebase remote worker branch blob queue index  # 0
    buffer tree object commit stream object rebase index object remote tree stream  # 1
    window tree rebase merge index stream buffer rebase blob buffer socket index  # 2
    buffer merge token token delta window buffer handle  # 3
    commit branch index merge handle index commit rebase remote branch  # 4
    ref socket merge parser parser  # 5
    handle socket delta tree worker handle token delta  # 6
    tree delta ref tree branch blob blob header  # 7
    remote merge parser handle delta object remote branch  # 8
    branch cache object window  # 9
    buffer handle tree object commit token  # 0
    buffer delta parser merge merge delta stream socket branch  # 1
    cache pack remote remote socket object object  # 2
