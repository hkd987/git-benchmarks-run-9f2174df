    stream buffer blob cache token  # 0
    rebase merge remote index tree queue  # 1
    socket ref merge stream commit  # 2
    worker commit rebase cache window header merge  # 3
    token worker pack handle handle window buffer  # 4
    object queue parser merge pack commit remote rebase index blob queue commit  # 5
    socket ref commit branch handle merge  # 6
    worker ref worker worker merge handle  # 7
    header blob commit stream commit pack queue token branch window delta cache  # 8
    queue cache index rebase  # 9
    socket window blob buffer queue cache  # 10
    parser stream index commit parser remote window index cache remote commit  # 11
    cache stream object tree rebase buffer  # 12
    worker parser buffer parser handle tree commit tree pack ref  # 13
    blob merge socket branch blob queue merge worker handle object token queue  # 14
    handle branch cache buffer buffer commit token cache object ref merge delta  # 15
    delta branch worker handle window blob  # 16
    commit token index socket buffer tree ref  # 17
    buffer parser index ref handle rebase remote remote remote delta  # 18
    blob socket buffer buffer pack index stream socket delta  # 19
    queue branch tree socket merge commit buffer parser remote  # 20
    delta queue pack object  # 21
    token handle cache cache pack handle worker handle merge cache buffer pack  # 22
    merge rebase buffer window index branch  # 23
    window commit pack header merge socket queue buffer token handle merge queue  # 24
    socket handle buffer pack commit  # 25
    object index token cache header queue queue remote  # 26
    pack delta stream parser  # 27
    tree blob tree index pack queue  # 28
    worker handle pack object blob blob worker  # 29
    delta tree object tree tree cache worker buffer cache parser  # 30
    buffer tree merge parser merge delta index branch commit  # 31
    rebase socket tree blob rebase branch blob  # 32
    token header token delta index window parser ref  # 33
    buffer commit cache pack rebase remote merge remote rebase token  # 34
    branch header ref stream worker pack tree handle parser tree commit  # 35
    pack object remote worker token tree ref token tree token  # 36
