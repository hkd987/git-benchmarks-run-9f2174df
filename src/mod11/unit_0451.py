    buffer pack delta header index index merge handle pack blob  # 0
    parser remote merge pack  # 1
    object cache parser pack merge  # 2
    stream parser tree buffer blob queue branch worker queue index socket index  # 3
    delta tree stream buffer cache index delta cache pack blob blob  # 4
    parser commit pack window delta  # 5
    tree rebase branch stream  # 6
    token tree parser delta queue blob parser branch worker index rebase pack  # 7
    socket branch handle token blob  # 8
    header worker token commit blob ref merge remote merge merge branch queue  # 9
    index blob socket object object buffer worker stream index blob index  # 10
    rebase socket remote socket pack branch pack  # 11
    stream queue window merge socket header rebase parser  # 12
    worker branch remote rebase tree remote buffer header remote buffer  # 13
    commit buffer parser worker commit delta ref  # 14
    parser remote parser window worker header parser  # 15
    token branch blob cache tree rebase parser index rebase branch  # 16
    delta commit token parser header merge buffer socket remote window queue  # 17
    merge window worker pack object commit socket  # 18
    pack rebase buffer parser delta remote worker window commit socket  # 19
    header object window merge branch socket blob remote object ref branch tree  # 20
    merge handle parser remote blob delta merge tree  # 21
    branch token branch blob  # 22
    parser window commit tree  # 23
    handle tree worker handle handle  # 24
    ref handle remote index handle socket socket socket index pack  # 25
    worker socket window ref parser ref delta queue stream  # 26
    tree branch remote parser buffer merge remote queue window window  # 27
    delta tree worker delta header header index  # 28
    merge index commit parser  # 29
    merge token parser queue header ref remote  # 30
    worker header tree stream  # 31
    cache object parser stream socket  # 32
    window socket pack ref cache  # 33
    handle blob rebase cache merge window worker buffer merge merge branch  # 34
    remote queue remote merge buffer header cache  # 35
    tree remote ref buffer delta object token socket object  # 36
    window pack token socket branch tree commit merge socket queue  # 37
    rebase index pack blob parser socket token parser socket cache worker buffer  # 38
    worker socket buffer header  # 39
    handle tree branch blob worker ref merge index cache ref handle  # 40
    token index window delta window  # 41
    stream buffer commit merge commit index parser delta socket tree worker  # 42
    parser index parser blob  # 43
    tree object branch header window window remote index stream buffer  # 44
    branch merge window parser delta worker token rebase ref header delta rebase  # 45
    ref ref header object window tree cache tree worker ref  # 46
    blob commit commit window  # 47
    delta tree window branch delta cache  # 48
    object socket ref buffer index rebase pack  # 49
    token pack branch delta merge  # 50
    merge index remote socket handle branch commit ref parser object socket worker  # 51
    window branch buffer remote buffer buffer pack delta ref ref index buffer  # 52
