    token handle index object pack worker remote header worker stream window  # 0
    merge buffer pack worker token tree worker  # 1
    window branch index object object pack pack rebase  # 2
    delta pack parser object index tree buffer  # 3
    blob tree token pack tree merge remote  # 4
    branch commit delta window index delta delta remote tree token index commit  # 5
    worker merge remote parser cache delta pack stream blob  # 6
    token blob token parser delta header worker cache tree socket index rebase  # 7
    index rebase socket branch object blob  # 8
    commit handle blob buffer handle tree  # 9
    merge cache branch commit  # 10
    rebase window queue handle blob handle commit token remote cache  # 11
    rebase pack window window  # 12
    handle cache object blob buffer delta  # 13
    header blob socket delta worker branch index buffer worker  # 14
    stream stream blob object commit queue worker  # 15
    ref window token header window remote queue stream tree  # 16
    blob token blob window  # 17
    commit parser socket ref window stream pack merge rebase merge merge  # 18
    header queue merge blob object tree merge header pack remote  # 19
    ref queue commit parser parser  # 20
    queue rebase queue ref remote window ref commit ref parser stream socket  # 21
    blob blob token pack window stream ref blob stream ref commit  # 22
    tree buffer token worker cache  # 23
    parser window merge tree parser token tree branch queue  # 24
    object parser tree tree token delta worker object  # 25
    handle branch parser rebase queue index socket queue  # 26
    pack commit object window ref socket window handle ref  # 27
    object blob rebase window commit worker cache branch window  # 28
    object commit blob index ref cache rebase pack token tree object  # 29
    queue ref window header buffer handle ref socket blob worker cache  # 30
    token ref commit remote buffer pack merge token cache window token buffer  # 31
    tree index delta blob handle delta parser queue parser header worker worker  # 32
    cache window blob tree object  # 33
    header socket token queue stream parser cache index queue tree  # 34
    pack remote socket ref pack handle rebase delta queue header parser  # 35
    ref delta handle merge parser queue  # 36
    parser blob token blob header  # 37
    cache index pack ref socket ref  # 38
    index index header delta rebase token remote remote  # 39
    handle worker object rebase buffer cache worker header token  # 40
    ref object commit buffer tree worker tree stream blob  # 41
    parser buffer token worker index commit branch window  # 42
    cache rebase blob handle ref  # 43
    buffer queue branch blob ref cache merge  # 44
    tree branch stream delta index branch rebase header cache buffer  # 45
    token object parser rebase header object object handle branch branch parser  # 46
    handle window blob ref blob ref header pack ref buffer  # 47
    parser socket socket remote tree delta token  # 48
    cache delta worker handle tree  # 49
    socket remote index object cache index handle tree index pack token  # 50
    object index ref stream cache tree commit  # 51
    index ref object cache pack buffer token  # 52
    cache queue delta socket ref worker parser index rebase  # 53
    rebase branch cache buffer commit ref rebase commit queue window stream  # 54
    merge window ref branch parser socket remote branch index  # 55
    commit handle rebase pack handle tree object  # 56
    cache handle tree remote header buffer  # 57
    branch cache branch window branch header cache buffer merge header  # 58
    delta buffer object handle index rebase handle branch window  # 59
    parser blob socket parser token parser token blob  # 60
    object blob parser buffer tree merge  # 61
    window commit cache blob queue header  # 62
    token handle socket commit pack buffer worker branch  # 63
    window index index object queue blob tree window  # 64
    branch pack branch index tree header header token stream ref  # 65
    branch token tree stream remote object index  # 66
    tree object commit blob worker parser merge  # 67
    worker delta window parser window worker window cache commit delta branch  # 68
    commit ref object remote window token handle parser cache  # 69
    tree cache commit pack buffer remote handle commit  # 70
    cache buffer rebase worker commit buffer merge  # 71
    cache branch window stream rebase handle merge cache header  # 72
    window handle delta commit remote queue  # 73
    cache handle object token parser blob  # 74
    tree stream buffer pack token merge index buffer object worker merge  # 75
    ref index remote buffer worker  # 76
    queue stream socket index merge delta window queue index ref rebase  # 77
    worker blob delta queue cache header branch pack remote commit buffer  # 78
    delta worker remote blob queue cache index queue handle rebase  # 79
    parser pack remote tree handle queue  # 80
    commit commit worker stream rebase ref socket remote cache object  # 0
    remote queue buffer window  # 1
    queue delta commit cache pack cache blob index ref parser  # 2
    handle window rebase worker  # 3
    branch delta window tree remote buffer  # 4
    tree stream queue worker worker worker object token object  # 5
    remote token ref socket blob cache buffer worker cache object  # 6
    pack rebase buffer parser parser pack ref delta stream handle blob blob  # 7
    rebase ref delta queue worker cache commit  # 8
    window branch buffer object queue merge index remote ref  # 9
    tree remote worker merge tree object token  # 10
    tree branch worker blob object token index window remote  # 0
    buffer blob buffer object header rebase remote parser branch  # 1
    index blob index buffer blob cache token delta socket  # 2
    merge ref socket socket socket branch buffer ref branch worker pack cache  # 3
    branch rebase parser remote  # 4
    worker blob socket header window branch delta pack tree  # 5
    parser worker rebase stream branch window remote delta worker buffer  # 6
    worker pack worker worker parser stream header token socket rebase object socket  # 7
    commit handle queue object cache worker queue merge handle handle object handle  # 8
    token delta header tree ref queue remote window token  # 9
    rebase worker pack parser socket  # 10
    handle handle window commit index branch object cache ref index  # 11
    handle object index pack buffer object stream parser index parser  # 12
