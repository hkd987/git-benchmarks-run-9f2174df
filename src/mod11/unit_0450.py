    token remote queue socket worker tree tree blob worker merge  # 0
    object parser ref parser blob ref parser header  # 1
    remote cache header blob buffer  # 2
    buffer pack buffer tree  # 3
    buffer handle rebase queue pack window token header  # 4
    parser blob object commit merge worker handle blob cache ref  # 5
    queue ref merge commit worker  # 6
    parser rebase handle remote  # 7
    parser rebase parser socket buffer object token object branch  # 8
    worker merge queue buffer socket header  # 9
    worker parser worker index merge window index parser ref remote tree index  # 10
    buffer token merge socket buffer delta cache header parser stream buffer ref  # 11
    token branch window delta token header buffer window ref  # 12
    queue handle handle tree remote handle  # 13
    tree commit window pack window blob  # 14
    pack ref object handle token object index ref socket merge rebase cache  # 15
    header object object stream  # 16
    remote parser merge window remote tree remote  # 17
    ref branch ref stream  # 18
    parser object worker blob rebase  # 19
    remote blob window index worker commit window delta worker worker handle object  # 20
    blob token branch index  # 21
    window blob header buffer stream  # 22
    cache rebase token ref  # 23
    pack cache object rebase header index worker blob  # 24
    token delta parser header rebase window  # 25
    ref stream rebase object merge blob worker socket  # 26
    queue window socket remote commit window delta  # 27
    rebase parser branch commit tree stream pack  # 28
    stream blob buffer object remote handle blob index window rebase blob queue  # 29
