    cache stream object window ref worker merge stream window remote  # 0
    handle token socket commit rebase  # 1
    index blob object rebase index header pack cache tree socket  # 2
    pack ref delta commit handle window parser blob window branch token  # 3
    stream ref delta branch window remote window index rebase  # 4
    delta ref merge socket window merge object buffer  # 5
    token rebase worker merge parser window token index header  # 6
    worker token handle cache header object delta rebase blob index parser  # 7
    socket object window token handle tree handle object delta cache  # 8
    remote header queue ref token worker merge parser  # 9
    cache stream cache object token merge rebase delta parser branch merge  # 10
    stream worker parser object window object token  # 11
    handle handle pack index blob tree token  # 12
    buffer merge buffer branch ref branch  # 13
    parser cache delta header cache branch  # 14
    blob parser handle rebase buffer commit  # 15
    rebase handle parser header window commit  # 16
    tree window window token stream index ref index pack header stream  # 17
    header object handle tree handle token rebase  # 18
    header buffer handle index delta object header blob stream delta worker  # 19
    window tree commit handle buffer  # 20
    delta blob rebase merge window token socket  # 21
    token blob object delta blob pack cache blob queue blob merge  # 22
    worker rebase remote branch stream pack header window ref queue  # 23
    pack token socket socket merge merge branch handle delta queue header  # 24
    window merge pack token queue blob merge tree ref stream window token  # 25
    window handle blob token commit window  # 26
    parser token socket delta remote worker buffer pack pack  # 27
    commit rebase queue handle  # 28
    tree rebase buffer socket blob delta  # 29
    header stream pack branch object cache  # 30
    branch worker pack socket  # 31
    delta worker tree window index branch  # 32
    merge tree branch delta  # 33
    pack merge tree object handle pack branch window  # 34
    object branch rebase token remote object tree merge  # 35
    object object worker ref  # 36
    handle handle header remote queue commit  # 37
    cache parser handle tree commit window pack  # 38
    token token pack remote handle handle handle branch window  # 39
    queue worker buffer worker tree  # 40
    buffer worker window ref parser commit worker header object blob object socket  # 41
    pack index socket commit handle  # 42
    window pack parser blob ref parser parser delta merge window commit  # 43
    object object tree index  # 44
