    cache commit parser index  # 0
    ref tree ref cache cache token socket remote queue  # 1
    buffer merge rebase worker token ref object branch tree blob remote socket  # 2
    socket remote queue socket cache  # 3
    socket branch header delta buffer  # 4
    object header index tree remote object pack index commit rebase buffer remote  # 5
    object commit parser ref parser  # 6
    remote commit object index header parser token buffer tree  # 7
    queue delta rebase buffer merge rebase worker worker delta delta  # 8
    parser socket worker token buffer window socket blob commit  # 9
    header commit merge delta cache rebase commit token worker remote  # 10
    buffer header buffer token delta worker stream  # 11
    stream rebase commit header  # 12
    branch token commit object queue handle worker merge  # 13
    merge rebase pack window  # 14
    parser object index stream branch header pack window  # 15
    pack commit stream delta commit rebase rebase  # 16
    commit parser index worker ref pack cache index branch branch merge  # 17
    header index stream token parser pack handle pack  # 18
    token cache merge blob merge  # 19
    index remote merge header pack cache  # 20
    token object ref buffer object merge queue cache token  # 21
    parser index branch token buffer object  # 22
    commit delta queue socket header buffer remote window header branch  # 23
    remote parser buffer delta window socket index parser header  # 24
    branch buffer worker window socket  # 25
    header blob blob pack object commit  # 26
    socket socket object commit index  # 27
    worker delta buffer parser tree branch  # 28
    delta rebase socket buffer pack remote merge branch cache  # 29
    commit branch socket commit blob worker rebase token  # 30
    commit header index branch merge index stream token delta  # 31
    index rebase remote token cache parser rebase blob  # 32
    ref stream handle pack queue header remote commit token ref  # 33
    remote cache blob merge queue queue object  # 34
    object merge blob ref branch parser commit remote rebase merge queue cache  # 35
    buffer cache buffer remote object queue tree window buffer handle buffer ref  # 36
    buffer object blob delta tree blob blob tree remote ref  # 37
    delta blob remote cache  # 38
    rebase branch merge remote  # 39
    socket parser stream stream buffer blob ref handle parser socket handle  # 40
    queue handle window remote queue commit  # 41
    cache branch merge stream commit branch  # 42
    parser index socket buffer  # 43
    handle parser remote worker  # 44
    pack branch cache remote worker parser commit object  # 45
    delta header cache delta merge commit delta  # 46
    ref delta remote buffer object  # 47
    buffer index token tree object delta socket parser  # 48
    token object ref queue parser  # 49
    tree buffer token blob merge tree object  # 50
    buffer remote window cache cache header header blob worker parser  # 51
    rebase merge object commit branch rebase object buffer delta  # 52
    cache worker window parser branch branch branch commit merge token  # 53
    tree stream delta object socket  # 54
    handle merge merge pack  # 55
    stream delta worker merge delta index queue branch index merge merge  # 56
    cache stream tree object branch token header buffer  # 57
    stream blob buffer branch index rebase blob  # 58
    worker socket stream delta tree  # 59
    header queue window cache worker merge queue delta  # 60
    stream commit remote stream pack delta object commit  # 61
    object pack blob window index rebase cache stream remote ref remote  # 62
    pack branch stream token buffer socket token header worker branch tree  # 63
    branch tree pack merge handle ref rebase merge  # 64
    window worker stream socket cache index socket cache parser  # 65
    parser remote buffer blob ref pack commit token tree object  # 66
    merge remote header delta object tree delta header stream commit  # 67
    cache tree delta commit buffer rebase object queue socket commit  # 68
    cache blob index header blob window blob commit window parser  # 69
    branch rebase stream ref tree handle  # 70
    rebase blob socket socket cache socket window cache object  # 71
    commit index tree parser handle tree merge queue parser cache blob  # 72
    token tree rebase token  # 73
    worker remote ref index stream tree queue socket buffer  # 74
    handle parser commit branch pack  # 75
    merge worker handle worker parser remote index queue ref cache socket  # 76
    branch branch handle parser blob  # 77
    delta stream token buffer ref header index blob  # 78
    commit merge header stream  # 79
