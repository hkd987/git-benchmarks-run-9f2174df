    ref token handle token branch remote parser merge  # 0
    cache cache blob tree ref queue  # 1
    stream window rebase worker  # 2
    pack object rebase pack tree socket cache branch  # 3
    window queue queue blob buffer delta  # 4
    commit index socket delta index token token commit branch  # 5
    token worker blob object merge token merge window  # 6
    index branch remote index tree branch remote  # 7
    ref buffer handle handle stream ref blob parser buffer socket  # 8
    header socket rebase token  # 9
    window queue socket buffer buffer  # 10
    object token header queue buffer remote  # 11
    token index branch blob queue commit tree merge object  # 12
    index rebase cache branch pack ref token object object stream blob  # 13
    rebase rebase branch pack socket delta object buffer cache ref parser handle  # 14
    worker commit branch commit cache token queue tree index window handle  # 15
    index commit pack queue rebase worker header delta merge  # 16
    index cache cache stream  # 17
    worker remote remote handle token index socket remote ref  # 18
    token pack stream stream parser commit buffer parser object  # 19
    ref tree merge delta worker commit parser parser  # 20
    parser pack handle tree branch rebase blob  # 21
    rebase object buffer rebase cache queue delta queue rebase  # 22
    queue index branch branch blob commit cache ref  # 23
    merge tree queue merge remote cache blob index queue branch cache  # 24
    parser parser socket commit  # 25
    header index buffer socket  # 26
    commit worker parser queue tree  # 27
    ref socket merge worker remote queue buffer  # 28
    cache remote tree branch index worker remote parser socket tree ref delta  # 29
    commit merge buffer parser token merge header parser delta  # 30
    delta cache handle index buffer queue parser branch stream delta socket  # 31
    remote stream blob object  # 32
    buffer worker cache handle object worker cache delta  # 33
    cache worker rebase blob window pack  # 34
    stream object merge delta ref  # 35
    queue object token buffer pack worker index pack stream object commit  # 36
    commit cache cache worker buffer  # 37
    blob object merge commit  # 38
    parser pack branch object commit index index pack socket tree branch remote  # 39
    index tree rebase stream handle pack merge remote branch  # 40
    branch handle pack window  # 41
    window commit tree merge handle window queue remote buffer header remote blob  # 42
    buffer delta ref socket socket pack cache merge rebase handle  # 43
    queue tree cache parser header  # 44
    merge handle index worker remote object tree pack merge  # 45
    cache rebase delta object  # 46
    socket blob parser index pack buffer header merge object tree stream handle  # 47
    header remote pack commit window  # 48
    branch delta rebase header header index merge  # 49
    pack merge token window token worker  # 50
    worker worker delta remote branch  # 51
    parser blob remote cache merge tree cache socket delta remote  # 52
    tree blob worker delta header commit object  # 53
    header token parser index parser  # 54
    delta queue rebase stream window ref  # 55
    cache blob stream tree buffer window handle  # 56
    token remote worker pack remote  # 57
    stream ref parser merge remote header branch worker parser index  # 58
    handle blob worker merge cache header delta header blob cache  # 59
    delta rebase index commit  # 60
    buffer index tree remote index index cache token rebase  # 61
    worker branch queue delta stream worker index  # 62
    branch pack stream blob object pack blob pack ref tree index buffer  # 63
    blob ref window blob remote buffer commit  # 64
    object worker cache parser window remote merge handle  # 65
    stream handle index socket commit remote header buffer  # 66
    branch blob rebase token merge token delta  # 67
    object delta tree index  # 68
    object buffer tree branch delta socket blob worker index socket  # 69
    pack worker pack parser stream  # 70
    stream branch blob cache branch index pack ref  # 71
    index token commit cache socket socket ref object  # 72
    window object blob ref pack worker  # 73
    commit buffer stream delta delta header  # 74
    window cache token worker handle commit parser handle merge  # 75
    object blob ref header handle  # 76
    tree parser tree pack  # 77
    token parser stream window window  # 78
    parser socket merge delta buffer  # 79
    rebase remote blob ref stream worker blob object worker ref delta  # 80
    cache delta merge header parser tree tree blob blob rebase  # 81
    object cache blob index  # 82
    queue object worker commit pack index cache  # 83
    queue ref rebase pack buffer header  # 84
    delta branch worker buffer tree token worker object commit parser  # 85
    commit cache parser window stream  # 86
    window object stream object  # 87
    socket token worker handle merge  # 88
