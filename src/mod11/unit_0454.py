    delta ref queue stream branch rebase token remote  # 0
    window tree branch token blob stream parser  # 1
    stream ref header header index socket  # 2
    tree ref socket object parser object blob ref ref pack  # 3
    header stream branch stream  # 4
    worker socket parser cache socket ref blob queue  # 5
    header queue index index cache parser buffer remote  # 6
    header pack object handle header stream  # 7
    cache tree window token  # 8
    buffer token delta index window  # 9
    pack branch ref pack  # 10
    remote tree object branch blob pack worker  # 11
    handle queue cache merge token buffer header  # 12
    parser pack header stream object worker rebase rebase parser  # 13
    window index remote rebase index ref tree window  # 14
    tree header header header buffer blob ref  # 15
    rebase worker handle object branch ref index commit handle ref handle  # 16
    worker window tree blob  # 17
    stream socket blob stream  # 18
    token queue handle header worker queue token object pack  # 19
    index buffer cache handle commit worker window blob token window  # 20
    cache worker cache queue buffer pack delta cache parser handle buffer  # 21
    branch commit token buffer parser socket commit stream branch  # 22
    rebase blob index token ref window queue index worker merge  # 23
    merge stream worker socket delta cache index ref object window  # 24
    queue commit token object object commit merge blob worker window branch  # 25
    commit ref object ref index worker stream  # 26
    buffer blob cache blob worker commit tree delta merge stream  # 27
    stream ref merge window branch stream stream  # 28
    merge window header tree branch rebase stream  # 29
    index index handle handle pack branch handle ref window tree  # 30
    handle ref remote header merge parser  # 31
    commit delta branch ref ref branch socket  # 32
    token header index object queue window blob cache handle queue delta  # 33
    tree rebase cache parser merge object remote  # 34
    rebase branch socket stream tree parser  # 35
    commit token object socket parser socket token index pack cache  # 36
    rebase remote cache merge tree buffer index ref ref  # 37
    parser branch ref window ref handle token  # 38
    remote window header commit stream window buffer cache header cache  # 39
    socket queue buffer window parser socket worker ref merge  # 40
