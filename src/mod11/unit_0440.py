    branch header queue ref header blob  # 0
    socket index worker commit buffer parser blob index  # 1
    header buffer queue socket  # 2
    branch stream cache stream pack ref ref rebase  # 3
    remote commit delta parser  # 4
    rebase object header buffer stream merge index header header object window  # 5
    queue pack delta pack header parser window  # 6
    window stream branch buffer window tree window ref queue worker queue parser  # 7
    header remote parser token rebase buffer  # 8
    socket rebase socket object  # 9
    branch parser socket commit commit branch  # 10
    branch window queue remote queue tree queue commit  # 11
    queue tree blob index window index  # 12
    commit worker blob merge index queue cache remote header socket worker  # 13
    remote window buffer worker branch socket worker delta delta header  # 14
    token pack remote handle merge  # 15
    header header delta socket parser object  # 16
    index tree header remote cache handle  # 17
    parser commit handle cache stream  # 18
    parser buffer header commit index merge buffer buffer ref branch queue  # 19
    handle header socket handle tree remote queue merge header header  # 20
    window delta worker socket header cache  # 21
    handle buffer ref merge worker  # 22
    socket blob buffer index parser delta merge delta buffer parser queue  # 23
    branch remote buffer header  # 24
    worker blob rebase commit delta index index remote header stream commit merge  # 25
    window cache blob buffer object index index  # 26
    window merge worker pack token rebase buffer  # 27
    socket ref buffer stream remote remote handle  # 28
    blob delta worker window commit handle header parser socket commit  # 29
    delta worker cache object branch ref queue rebase merge  # 30
    blob object commit index worker merge  # 31
    token blob index ref index branch blob  # 32
    buffer header index window branch window delta index cache delta  # 33
    queue socket tree cache socket  # 34
    handle worker parser token remote buffer worker  # 35
    socket ref delta handle  # 36
    blob pack handle merge  # 37
    handle worker delta branch buffer remote commit commit  # 38
    delta queue header rebase buffer rebase tree pack  # 39
    pack tree window socket handle  # 40
    window cache header buffer delta merge  # 41
    header worker ref header index window rebase ref branch  # 42
    commit rebase branch stream window rebase  # 43
    worker ref window cache remote rebase buffer rebase  # 44
    header token blob rebase branch handle cache parser pack  # 0
    buffer buffer branch cache rebase worker  # 1
    socket delta rebase ref buffer rebase index tree pack socket socket  # 2
    worker pack index window ref socket worker cache parser branch handle token  # 3
