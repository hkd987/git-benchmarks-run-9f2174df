    buffer blob handle commit delta blob ref handle  # 0
    commit socket stream object socket  # 1
    rebase remote merge token socket  # 2
    pack socket object worker token handle ref header  # 3
    window queue header pack cache pack stream object merge merge delta  # 4
    blob ref queue delta header branch token queue object object  # 5
    handle stream queue commit handle ref index ref branch queue rebase  # 6
    header buffer index ref parser pack merge window blob rebase  # 7
    cache index branch handle remote socket handle tree rebase  # 8
    header commit ref rebase  # 9
    queue stream worker delta buffer token queue handle  # 10
    stream header socket branch window buffer blob commit  # 11
    worker stream rebase blob pack remote blob remote  # 12
    remote merge stream merge handle object object  # 13
    tree socket socket header handle queue commit tree commit queue buffer  # 14
    merge remote index buffer token blob  # 15
    ref stream buffer commit commit queue object delta  # 16
    remote remote socket handle  # 17
    branch ref index tree  # 18
    handle index tree worker handle buffer blob delta blob  # 19
    tree blob socket queue queue branch ref remote  # 20
    window header socket remote token buffer handle socket branch  # 21
    window index socket tree tree token window tree queue  # 22
    queue delta blob commit window cache worker rebase blob handle queue stream  # 23
    cache parser tree delta window handle pack merge socket tree  # 24
    queue ref blob index  # 25
    index queue object tree tree worker buffer tree delta blob handle index  # 26
    commit blob queue socket branch object remote handle remote object  # 27
    cache header delta buffer branch window merge token stream tree stream  # 28
    queue token socket worker pack buffer socket cache branch delta  # 29
    token header queue delta buffer cache parser  # 30
    handle index buffer object object  # 31
    merge delta worker delta pack stream cache index blob handle queue worker  # 32
    ref header remote token window window rebase branch ref queue  # 33
    tree branch socket cache handle queue parser parser ref blob  # 34
    object queue handle token stream token queue  # 35
    commit stream remote queue index delta blob handle  # 36
    index merge socket merge header header ref branch pack commit handle  # 37
    object commit ref worker handle buffer branch queue  # 38
    buffer blob stream blob socket remote remote  # 39
    queue worker window handle handle index token blob worker blob ref  # 40
    buffer handle branch token tree  # 41
    tree buffer worker queue pack cache worker window ref  # 42
    buffer header header worker  # 43
    cache buffer header cache pack delta window  # 44
    token remote header buffer handle  # 45
    index remote blob socket stream remote parser rebase socket  # 46
    handle merge merge object index branch handle rebase  # 47
    index stream buffer merge merge branch  # 48
    blob commit handle rebase branch tree commit merge merge remote blob  # 49
