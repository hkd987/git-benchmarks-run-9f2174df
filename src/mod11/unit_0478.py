    object blob header delta object  # 0
    worker rebase queue index queue merge buffer header pack index rebase merge  # 1
    window window remote worker header  # 2
    remote handle cache parser ref rebase token ref  # 3
    pack token branch stream remote queue  # 4
    cache worker pack queue queue delta handle rebase pack stream  # 5
    buffer buffer pack handle delta  # 6
    token index branch cache tree ref delta index commit rebase  # 7
    queue ref socket rebase branch worker token tree handle remote  # 8
    socket window object parser socket buffer parser buffer  # 9
    cache token blob window tree queue commit commit commit  # 10
    header object socket worker parser merge rebase cache header  # 11
    header token blob commit ref blob branch  # 12
    commit handle ref rebase handle  # 13
    header stream ref remote  # 14
    delta handle pack stream buffer  # 15
    delta header cache tree remote cache  # 16
    worker object merge blob index blob handle parser  # 17
    socket header pack commit object  # 18
    cache handle merge handle remote queue window object remote header  # 19
    remote parser token pack rebase handle buffer stream window branch  # 20
    remote buffer delta header token blob  # 21
    window object merge object worker parser object cache  # 22
    remote delta merge commit branch buffer socket object  # 23
    ref socket handle handle stream index  # 24
    blob buffer blob rebase blob merge  # 25
    blob pack commit socket remote header buffer token tree worker stream  # 26
    ref ref buffer index window worker  # 27
    branch remote commit blob delta  # 28
    socket socket queue worker cache ref rebase remote delta delta  # 29
    window delta buffer merge worker window socket worker  # 30
    worker stream rebase object queue pack token commit pack queue tree  # 31
    commit ref socket cache branch window tree  # 32
    window blob token tree delta  # 33
    socket parser index branch window header object remote  # 34
    tree blob remote worker worker  # 35
    rebase tree worker merge rebase handle branch token  # 36
    index window queue commit merge buffer object pack  # 37
    queue window queue merge stream branch header branch  # 38
    stream window cache buffer ref header commit pack  # 39
    tree socket delta pack header object stream header commit remote window  # 40
    index worker queue token ref tree handle merge remote rebase  # 41
    queue object blob stream socket  # 42
    tree cache object tree pack index pack window stream token delta  # 43
    buffer socket window queue remote header object parser object branch  # 44
    index socket delta handle rebase tree commit  # 45
    worker remote stream tree  # 46
    object pack cache queue tree cache  # 47
    socket pack branch commit pack socket pack branch  # 48
    branch window commit commit delta ref handle parser blob parser  # 49
    index remote tree tree merge  # 50
    remote worker ref object rebase handle merge queue handle  # 51
    stream stream commit header header worker  # 52
    index blob ref worker merge branch tree  # 53
    cache rebase remote window  # 54
    ref worker window commit  # 55
    handle stream cache branch parser window object pack  # 56
    stream handle commit window object queue cache ref worker blob  # 57
    rebase stream branch index header parser delta parser socket stream  # 58
    stream stream delta remote pack queue pack branch delta pack index delta  # 59
    blob object queue merge header cache  # 60
    rebase header handle tree token token  # 61
    remote handle index buffer worker cache blob commit queue pack object remote  # 62
    handle token handle pack pack socket queue cache  # 63
    delta socket delta queue cache pack header delta branch remote token rebase  # 64
    ref buffer branch remote delta worker merge parser remote pack handle  # 65
    token token commit header tree  # 66
    branch commit remote branch token branch  # 67
    merge object blob socket rebase window merge handle header remote parser  # 68
    ref blob merge remote branch blob cache delta ref buffer window ref  # 69
