    header branch socket parser token  # 0
    branch rebase stream buffer stream blob blob rebase cache  # 1
    remote queue header cache delta worker buffer stream blob header index  # 2
    branch worker blob commit token parser queue  # 3
    stream merge socket token tree worker queue blob index cache branch token  # 4
    token socket token tree queue token token header  # 5
    queue delta worker token stream queue commit delta commit tree branch  # 6
    branch commit merge pack  # 7
    commit parser parser pack token branch tree  # 8
    tree merge branch cache merge buffer window index stream  # 9
    branch index blob parser token commit buffer window token cache  # 10
    parser object socket blob delta commit parser pack socket  # 11
    buffer header ref pack delta header branch cache  # 12
    token header stream remote rebase  # 13
    socket cache queue buffer worker queue stream blob index object  # 14
    object handle header token merge header index remote window delta object blob  # 15
    object ref worker remote commit header  # 16
    commit handle buffer delta parser blob  # 17
    handle stream stream queue merge  # 18
    queue cache tree handle queue remote  # 19
    worker branch header handle stream parser buffer  # 20
    delta worker delta window worker branch tree token handle remote object  # 21
    branch delta window delta  # 22
    remote buffer merge commit  # 23
    tree delta tree commit commit ref  # 24
    buffer window queue index  # 25
    token socket merge remote token socket commit pack pack blob  # 26
    cache worker ref cache  # 27
    blob object delta index index cache pack branch buffer header  # 28
    merge rebase cache merge object ref worker  # 29
    stream commit cache handle object blob header socket queue  # 30
    merge token window delta parser rebase  # 31
