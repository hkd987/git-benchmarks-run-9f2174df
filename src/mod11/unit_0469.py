    stream cache merge cache branch socket buffer parser commit worker  # 0
    pack socket pack socket worker commit window object socket blob remote cache  # 1
    queue parser rebase window  # 2
    remote ref ref queue header index commit buffer stream object object  # 3
    queue index merge worker stream parser parser  # 4
    stream queue window tree stream handle window parser stream delta  # 5
    token header window window queue tree worker worker  # 6
    queue buffer window socket commit parser index commit header  # 7
    header branch buffer ref  # 8
    buffer cache object pack index  # 9
    delta merge tree handle index handle socket handle object  # 10
    pack buffer queue object ref index commit blob window window index  # 11
    object merge object ref rebase branch cache blob tree  # 12
    queue ref handle socket branch stream commit remote parser rebase pack  # 13
    token commit socket header header object branch window commit index handle object  # 14
    buffer commit branch rebase socket tree remote tree window commit  # 15
    socket ref header stream merge parser  # 16
    rebase merge blob delta parser handle worker socket  # 17
    commit buffer pack queue handle  # 18
    rebase remote tree ref rebase header commit commit commit blob token  # 19
    handle queue blob object  # 20
    buffer remote pack parser buffer branch rebase handle delta ref  # 21
    object merge token worker index  # 22
    parser blob parser blob tree tree remote buffer cache buffer header  # 23
    remote tree tree cache window  # 24
    token stream ref ref object delta cache handle window index queue  # 25
    parser stream object delta blob delta socket window window blob window branch  # 26
    branch cache blob index stream  # 27
    header index handle object object stream object socket pack object  # 28
    merge commit token cache commit object tree delta branch  # 29
    worker rebase token ref tree  # 30
    rebase index index delta token window rebase handle  # 31
    pack token stream token worker ref token pack handle cache tree  # 32
    pack header buffer pack  # 33
    buffer queue commit stream pack socket handle handle buffer queue worker  # 34
    parser index delta blob index branch  # 35
    window buffer socket blob stream parser tree parser remote  # 36
    branch ref buffer stream queue branch blob ref queue  # 37
    stream blob blob window tree  # 38
    cache window branch queue handle  # 39
    queue queue remote queue  # 40
    header queue index worker object delta ref remote  # 41
    stream object remote pack header token header branch  # 42
    header cache remote tree remote object  # 43
    object index header buffer buffer object stream commit index worker worker  # 44
    pack worker index merge socket merge queue delta branch branch  # 45
    window pack tree ref worker delta rebase header window  # 46
    pack remote tree cache parser token header window  # 47
    cache blob stream handle socket tree token  # 48
    socket handle socket rebase worker  # 49
    window buffer handle remote blob worker buffer merge ref token delta commit  # 50
    header merge object branch blob stream commit stream  # 51
    rebase ref window socket pack header branch commit  # 52
    commit socket handle parser token branch stream  # 53
    token stream rebase rebase  # 54
    commit token rebase stream stream worker ref remote  # 55
    tree delta token branch delta stream commit window branch remote buffer parser  # 56
    window branch socket remote worker index branch token queue  # 57
    merge branch merge remote parser handle commit worker object  # 58
    buffer blob queue header rebase  # 59
    header cache remote blob handle handle worker rebase  # 60
    stream token object buffer  # 61
    pack delta branch handle cache commit pack buffer rebase commit blob  # 62
    cache token delta remote buffer socket pack  # 63
    merge parser object blob rebase worker socket token blob object  # 64
    parser cache window pack stream branch remote ref  # 65
    blob blob ref worker header delta socket socket object ref  # 66
    worker parser merge blob tree window token header rebase pack  # 67
    buffer worker queue object rebase header object buffer delta worker  # 68
    blob header handle tree branch header  # 69
    stream remote pack delta handle header branch token  # 70
    queue socket header handle delta commit queue header branch buffer worker  # 71
    buffer index worker tree object object branch worker blob branch socket  # 72
    stream socket rebase stream object index cache index socket index socket  # 73
    object index buffer rebase remote window window rebase merge tree rebase  # 0
    stream handle blob delta worker header delta delta parser  # 1
    parser merge cache merge token  # 2
    object blob handle branch pack  # 3
    commit token index object branch window window blob tree  # 4
    ref socket merge cache socket cache delta pack  # 5
    branch rebase branch object worker index branch buffer parser delta  # 6
    ref remote tree pack pack  # 7
    branch parser cache worker pack ref worker commit  # 8
    token rebase ref index commit remote merge object ref header  # 9
    buffer tree window window delta parser object parser token worker  # 10
    worker branch cache object branch branch pack pack blob merge merge  # 11
    parser queue window socket socket merge blob tree  # 12
