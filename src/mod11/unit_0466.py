    merge window socket queue header handle blob socket branch window  # 0
    index token handle buffer queue window commit worker object cache window remote  # 1
    delta merge pack index index socket pack stream remote rebase  # 2
    ref commit stream commit worker token stream commit worker parser ref handle  # 3
    tree object window merge stream rebase token header rebase window queue  # 4
    parser parser commit handle pack merge  # 5
    commit object header object  # 6
    remote cache worker merge  # 7
    cache buffer rebase cache buffer parser delta header worker  # 8
    rebase pack rebase handle parser  # 9
    parser buffer blob window pack rebase handle remote window  # 10
    branch branch buffer parser  # 11
    socket branch remote commit token merge stream ref  # 12
    pack tree tree merge ref tree parser remote pack tree commit  # 13
    buffer commit socket header worker ref pack delta socket token remote ref  # 14
    blob queue pack parser  # 15
    cache object queue token rebase branch handle queue worker token  # 16
    socket queue index branch commit merge branch buffer parser token parser worker  # 17
    stream cache header buffer remote branch remote socket handle branch  # 18
    header cache pack blob socket  # 19
    handle handle rebase queue header header header blob  # 20
    remote tree parser commit pack ref  # 21
    window queue pack handle  # 22
    pack queue ref queue ref socket window  # 23
    ref object worker merge stream socket worker header commit tree socket window  # 24
    object remote buffer buffer rebase  # 25
    buffer header blob queue index queue  # 26
    blob blob rebase object queue delta blob queue rebase  # 27
    ref parser token token stream merge rebase queue rebase delta  # 28
    socket pack header stream blob queue commit queue  # 29
    tree token ref delta remote  # 30
    rebase parser window socket window buffer ref remote pack worker queue  # 31
    pack stream ref handle index delta handle buffer  # 32
    object handle merge merge  # 33
    cache header worker pack token token queue handle  # 34
    worker blob buffer delta stream  # 35
    remote delta merge index rebase window parser rebase tree  # 36
    stream worker tree socket merge worker stream pack object worker delta remote  # 37
    handle ref blob blob index cache blob tree  # 38
    index index socket buffer buffer commit delta token cache stream  # 39
