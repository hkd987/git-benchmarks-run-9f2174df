    queue stream header tree ref ref handle worker  # 0
    remote commit queue token merge index remote buffer remote buffer tree merge  # 1
    handle parser merge parser parser object merge ref parser tree commit commit  # 2
    window worker rebase branch blob index header stream merge queue  # 3
    commit tree stream token delta blob handle buffer  # 4
    merge rebase rebase cache token token commit remote  # 5
    rebase index stream handle  # 6
    merge stream parser blob merge handle pack queue index header commit  # 7
    buffer cache socket window parser remote buffer handle socket  # 8
    header remote ref stream parser cache window cache object pack  # 9
    pack object cache rebase  # 10
    worker rebase header object blob  # 11
    remote parser cache parser handle merge token ref merge  # 12
    merge stream cache tree ref parser blob ref  # 13
    socket buffer merge ref  # 14
    ref tree object blob pack token token commit merge socket  # 15
    blob blob cache queue stream header buffer  # 16
    tree rebase queue cache socket window token  # 17
    worker buffer object token commit  # 18
    queue parser window buffer window handle branch  # 19
    handle token merge tree merge branch pack tree queue header  # 20
    token token object blob buffer object delta queue object  # 21
    tree commit queue handle window window rebase index tree ref handle worker  # 22
    pack buffer blob header parser branch  # 23
    socket tree window delta  # 24
    stream rebase handle parser pack  # 25
    queue tree window socket remote worker  # 26
    cache blob stream commit merge pack socket worker index  # 27
    stream branch ref socket stream socket queue token stream  # 28
    object parser cache delta delta pack ref worker cache stream parser cache  # 29
    merge ref rebase window stream cache  # 30
    parser tree remote window parser stream rebase branch queue socket parser blob  # 31
    tree cache remote pack  # 32
    stream index index commit stream ref delta rebase header socket branch  # 33
    object tree blob queue cache header rebase branch window cache window merge  # 34
    remote header queue remote object  # 35
    parser index merge token socket  # 36
    pack remote merge handle blob buffer worker buffer tree socket index  # 37
    cache rebase rebase socket parser parser branch handle ref  # 38
    worker handle socket merge buffer blob branch  # 39
    token object remote pack tree index queue delta branch  # 40
    stream object buffer header  # 41
    pack parser remote buffer index  # 42
