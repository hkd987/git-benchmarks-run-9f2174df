    header rebase remote branch delta pack delta merge cache pack handle tree  # 0
    header branch branch branch remote  # 1
    index buffer handle token token worker branch queue socket window branch  # 2
    worker remote delta stream handle socket worker pack tree merge  # 3
    window handle ref buffer window handle parser blob rebase index branch  # 4
    branch delta object index  # 5
    handle delta ref buffer window socket header  # 6
    rebase header buffer cache stream branch commit branch remote  # 7
    stream delta ref delta tree tree worker index ref queue  # 8
    queue branch stream delta index stream rebase handle ref cache  # 9
    blob merge stream branch handle object parser buffer ref token rebase  # 10
    buffer commit remote header commit blob remote  # 11
    handle branch stream parser commit tree merge parser commit tree handle  # 12
    window ref pack branch index delta worker delta header handle socket rebase  # 13
    buffer ref pack handle  # 14
    window queue tree header blob worker token  # 15
    delta stream remote commit header  # 16
    object delta commit index merge merge stream pack window delta branch  # 17
    stream parser remote tree merge remote object blob commit delta window  # 18
    pack ref header window  # 19
    object blob blob delta worker cache header  # 20
    pack header queue remote ref stream  # 21
    handle blob object remote pack  # 22
    merge rebase commit token commit worker handle tree token rebase buffer  # 23
    socket stream token ref delta window token object socket handle handle branch  # 24
    blob buffer handle merge object cache stream blob stream token  # 25
    queue commit pack parser socket ref window handle rebase window  # 26
    handle window delta pack commit branch token  # 27
    parser merge handle stream window tree parser branch pack socket socket  # 28
    rebase index header branch header ref parser queue stream merge index rebase  # 29
    cache pack tree branch branch  # 30
    header token parser header remote rebase token pack  # 31
    ref stream blob ref cache remote index header  # 32
    ref blob blob blob token tree socket object pack commit pack object  # 33
    worker token blob remote pack handle handle worker  # 34
    worker queue commit header window token ref socket merge buffer remote  # 35
    cache commit header handle object rebase blob merge socket token ref  # 36
    commit handle object queue tree cache  # 37
    object token merge cache cache token index  # 38
    cache window worker commit commit  # 39
    delta delta branch commit token rebase buffer socket  # 40
    stream object blob branch ref worker queue ref ref socket  # 41
    parser window header delta index blob parser branch pack socket queue  # 0
    handle worker cache rebase worker  # 1
    delta queue blob remote  # 2
    parser tree tree cache delta parser tree queue window  # 3
    remote header header commit  # 4
    cache delta cache index tree header branch pack blob  # 5
    worker tree branch merge remote  # 6
    token branch merge cache  # 7
    socket stream buffer commit  # 0
    cache window pack socket delta window window commit buffer branch cache header  # 1
    rebase worker commit delta  # 2
    queue tree blob socket token  # 3
