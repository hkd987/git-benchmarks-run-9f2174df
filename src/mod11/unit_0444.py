    worker queue buffer handle delta token commit window index window worker  # 0
    handle ref rebase handle merge header ref index blob handle merge queue  # 1
    socket rebase worker tree rebase branch branch object branch header  # 2
    token object header cache branch object object stream  # 3
    token buffer branch header rebase buffer  # 4
    ref queue token commit merge commit rebase ref  # 5
    remote ref stream rebase delta object window window socket  # 6
    window cache branch stream  # 7
    index rebase window queue ref ref token blob remote  # 8
    commit delta commit token buffer object delta delta  # 9
    pack handle tree window  # 10
    worker rebase header index blob buffer pack buffer  # 11
    merge object tree pack token branch handle pack tree worker  # 12
    header pack buffer pack tree queue index socket pack commit  # 13
    header buffer stream rebase window cache ref token rebase delta window  # 14
    buffer merge window ref pack merge ref pack object socket stream  # 15
    blob buffer branch branch  # 16
    stream commit cache ref  # 17
    ref cache header worker blob parser pack merge merge  # 18
    object pack token token delta  # 19
    remote object index remote rebase header parser cache handle branch rebase  # 20
    header token token blob tree merge branch queue  # 21
    stream buffer blob rebase ref  # 22
    pack cache socket object  # 23
    buffer rebase ref window socket object socket worker  # 24
    buffer handle queue header ref pack index  # 25
    cache buffer buffer worker branch  # 26
    queue cache remote header token merge stream stream socket  # 27
    commit parser ref token parser socket  # 28
    window handle tree ref rebase  # 29
    stream socket buffer handle worker pack worker blob  # 30
    object window delta object worker buffer blob rebase window rebase  # 31
    cache stream worker ref socket  # 32
    index blob rebase window stream commit ref handle commit socket  # 33
    token rebase queue pack tree merge stream  # 34
    socket worker socket merge  # 35
    object index socket cache token object handle blob worker remote parser object  # 36
    handle object object cache rebase token buffer socket ref  # 37
    handle pack remote branch header index header tree  # 38
    delta ref index delta cache  # 39
    commit handle object buffer handle ref token  # 40
    worker merge window handle socket window buffer  # 41
    branch blob blob remote  # 42
    branch cache ref index header branch rebase  # 43
    ref header pack branch merge object cache  # 44
    ref token object window branch stream  # 45
    rebase pack worker blob window parser buffer  # 46
    cache socket remote window stream object remote object  # 47
    stream branch index delta  # 48
    token remote object pack index tree  # 49
    stream worker handle parser blob stream  # 50
    delta tree ref remote cache commit  # 51
    buffer token tree worker handle index  # 52
    tree pack stream queue tree queue blob remote handle token tree  # 53
    pack stream object cache  # 54
    pack ref token handle merge remote  # 55
    buffer worker branch worker remote stream  # 56
    token delta rebase remote parser rebase header handle buffer header index tree  # 57
    stream branch ref object  # 58
    header stream handle socket header worker  # 59
    remote buffer merge parser cache parser buffer pack  # 60
    merge tree worker buffer  # 61
    delta token object commit window commit parser rebase ref blob socket header  # 62
    handle pack blob cache remote header tree  # 63
    parser merge remote blob token ref pack merge buffer object merge queue  # 64
    cache stream delta window queue worker remote merge handle cache delta  # 65
    delta worker commit remote stream pack buffer parser index handle buffer  # 66
    queue parser commit handle tree merge commit parser worker cache object socket  # 67
    merge stream handle branch blob object token socket object remote handle buffer  # 68
    handle queue socket ref rebase  # 69
    queue handle commit cache pack remote tree rebase blob handle handle merge  # 0
    commit ref pack branch  # 1
    tree parser socket ref window delta branch handle parser header ref  # 2
    parser buffer stream ref buffer header buffer  # 3
    object pack stream pack  # 4
    token window worker delta merge handle object merge branch  # 5
    rebase pack queue socket delta parser parser token rebase socket parser  # 6
    remote merge parser pack  # 7
    merge object handle commit merge ref merge handle delta rebase object  # 8
    worker commit delta remote blob handle stream remote commit  # 9
