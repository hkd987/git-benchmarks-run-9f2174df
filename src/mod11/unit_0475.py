    parser queue queue cache  # 0
    merge rebase branch stream queue delta pack delta parser  # 1
    merge rebase remote blob remote merge blob stream commit tree tree  # 2
    merge cache buffer parser tree cache delta branch queue pack  # 3
    stream worker ref worker object pack pack merge parser socket rebase cache  # 4
    index pack socket queue token  # 5
    index stream commit buffer  # 6
    socket header remote commit queue token  # 7
    object blob pack cache  # 8
    tree delta handle cache handle socket cache rebase pack blob index  # 9
    token merge delta object index buffer merge object pack pack buffer index  # 10
    rebase cache object index parser queue  # 11
    stream socket stream worker index socket merge  # 12
    buffer remote delta rebase  # 13
    blob commit worker socket branch branch object blob  # 14
    header tree pack header worker blob worker parser branch stream  # 15
    header pack handle remote ref ref ref rebase parser buffer header commit  # 16
    header object handle tree token remote queue  # 17
    object object queue index worker merge commit blob handle parser  # 18
    worker queue header rebase buffer object merge ref worker  # 19
    queue buffer handle handle cache  # 20
    ref delta parser stream header commit worker worker object merge  # 21
    blob queue token header remote blob delta merge token  # 22
    handle buffer handle socket tree remote window  # 23
    blob tree tree merge object delta branch  # 24
    object pack handle ref merge  # 25
    socket socket buffer buffer parser tree commit buffer pack  # 26
    parser blob buffer merge socket socket rebase handle object cache index rebase  # 27
    cache buffer cache stream buffer merge stream token remote branch branch worker  # 28
    cache delta tree handle delta remote remote object token worker cache ref  # 29
    commit commit remote cache ref pack token  # 30
    header remote queue stream  # 31
    remote tree parser merge tree blob  # 32
    index branch stream object pack queue  # 33
    pack socket merge token commit object  # 34
    parser branch cache header commit parser tree token merge worker ref  # 35
    buffer rebase cache branch remote queue window handle socket  # 36
    stream worker object buffer object delta token  # 37
    commit worker index header object  # 0
    merge rebase remote branch  # 1
    commit socket index token branch object branch header buffer handle stream  # 2
    pack pack pack worker branch branch worker delta merge commit  # 3
    socket pack remote blob branch cache commit  # 4
    socket stream remote rebase pack header handle worker queue object pack header  # 5
    window merge cache token buffer token  # 6
    delta header delta ref branch worker merge tree stream token parser  # 7
