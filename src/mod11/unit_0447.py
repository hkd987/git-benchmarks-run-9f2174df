    token delta object blob ref worker worker header handle index token token  # 0
    parser window cache pack tree commit ref  # 1
    buffer pack handle object  # 2
    ref token cache buffer queue  # 3
    branch token index buffer header remote tree rebase socket token token  # 4
    handle worker header buffer window merge socket parser  # 5
    remote pack parser queue blob ref commit handle buffer cache handle object  # 6
    parser rebase parser remote stream window queue  # 7
    delta window worker cache token token stream stream  # 8
    rebase worker merge socket remote object rebase  # 9
    tree tree window remote queue remote merge  # 10
    worker header buffer merge stream ref rebase buffer tree stream  # 11
    parser commit index worker cache object  # 12
    index object pack handle header stream  # 13
    pack object commit handle handle  # 14
    stream window rebase pack cache rebase handle handle handle cache delta parser  # 15
    delta socket delta buffer window socket index rebase tree  # 16
    merge token cache index queue window commit  # 17
    remote pack buffer rebase pack cache header window index stream parser  # 18
    blob remote index remote buffer handle  # 19
    buffer queue window remote cache object blob  # 20
    ref window merge stream tree delta delta cache  # 21
    socket token remote index  # 22
    blob window commit delta remote buffer  # 23
    object blob handle ref blob cache cache merge worker commit  # 24
    cache delta ref object window stream  # 25
    window parser token queue branch socket queue window window merge  # 26
    rebase object queue rebase blob handle  # 27
    object tree queue queue rebase commit parser header cache  # 28
    buffer branch buffer buffer  # 29
    remote branch commit rebase window  # 30
    socket token remote buffer commit remote blob index branch token token buffer  # 31
    tree commit queue tree buffer delta merge tree remote tree delta object  # 32
    token socket token parser socket blob window token commit branch parser  # 33
    worker token buffer window  # 34
    rebase tree window object stream window handle cache  # 35
    buffer tree stream tree remote tree buffer buffer index  # 36
    parser handle pack rebase socket socket worker cache  # 37
    remote branch ref index object branch ref index delta token  # 38
    token token pack ref handle buffer index  # 39
    cache branch pack handle blob worker buffer remote delta blob  # 40
    commit parser branch tree cache delta token socket commit object merge  # 41
    commit header handle header handle index blob  # 42
    cache handle parser branch object  # 43
    branch delta remote delta remote stream window  # 44
    branch merge socket socket branch cache commit rebase cache  # 45
    branch cache remote rebase index  # 46
    buffer pack worker branch merge branch object  # 47
    rebase rebase pack socket window remote object  # 48
    index rebase object queue merge parser ref stream  # 49
    handle index commit window object object  # 50
    rebase tree remote object object handle blob pack header cache object  # 51
    socket commit buffer token socket buffer remote socket rebase  # 52
    ref ref tree object remote branch socket socket ref pack blob buffer  # 53
    ref branch window handle object worker ref token object  # 54
    remote branch rebase queue merge worker branch  # 55
    delta merge parser pack  # 56
    socket window cache header index parser queue branch blob branch stream index  # 57
    worker queue ref window stream pack  # 58
    pack buffer merge blob tree delta index  # 59
    queue branch remote object stream parser remote branch object  # 60
    remote delta ref parser tree delta index  # 61
    worker index commit header parser handle stream parser tree  # 62
    index header blob commit queue pack commit index merge tree  # 63
    tree socket remote window delta delta rebase tree  # 64
    index queue window branch window parser commit branch  # 65
