    merge worker object branch parser object remote remote header delta pack index  # 0
    rebase socket handle delta remote handle tree merge header tree merge parser  # 1
    rebase remote header merge socket handle token  # 2
    index queue header cache index tree object buffer buffer  # 3
    ref token buffer ref object parser socket commit header  # 4
    parser branch parser index blob handle cache merge tree blob  # 5
    ref tree queue blob merge delta buffer  # 6
    cache stream rebase socket  # 7
    header header queue delta index cache queue delta  # 8
    object stream handle buffer  # 9
    delta worker commit socket  # 10
    index queue queue tree branch  # 11
    buffer parser remote worker parser ref merge ref handle object  # 12
    commit parser queue window handle cache  # 13
    cache merge window token window parser commit  # 14
    cache header rebase ref socket ref queue commit  # 15
    stream handle pack token  # 16
    tree delta handle branch window  # 17
    handle merge worker pack object  # 18
    queue cache worker tree  # 19
    branch parser index branch tree buffer  # 20
    handle cache pack worker ref ref worker buffer  # 21
    ref commit window handle index queue tree window header index window commit  # 22
    branch remote parser queue window handle ref commit window ref  # 23
    blob handle blob queue  # 24
    branch blob object stream header commit merge merge  # 25
    handle remote index merge stream object parser token token worker  # 26
    ref tree delta pack remote stream queue window object cache  # 27
    worker object commit queue pack  # 28
    token remote index parser header delta buffer blob parser delta token  # 29
    tree ref stream worker merge pack handle  # 30
    merge buffer rebase socket handle index queue queue commit remote  # 31
    ref branch socket window object  # 32
    index pack commit index header stream object pack worker socket object header  # 33
    window cache pack handle window buffer stream socket ref merge parser  # 34
    token stream header rebase remote parser object ref pack rebase delta pack  # 35
    object delta pack token ref header buffer parser  # 36
    window ref queue handle stream  # 37
    branch commit delta ref stream window handle tree remote  # 38
    merge ref queue worker  # 39
    window ref rebase delta rebase merge  # 40
    object header commit handle handle branch worker stream stream delta merge  # 41
    socket delta ref cache window blob window index header  # 42
    socket object cache stream token worker ref branch handle stream  # 43
    remote header parser parser index parser worker remote  # 44
    tree window pack pack index index header pack  # 45
