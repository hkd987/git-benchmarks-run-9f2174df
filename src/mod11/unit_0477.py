    index ref header pack rebase delta delta  # 0
    parser blob buffer commit stream branch handle tree  # 1
    cache queue rebase delta commit cache commit object ref socket stream  # 2
    commit object ref header  # 3
    cache queue header parser rebase branch index  # 4
    header pack token worker worker rebase queue queue cache parser cache  # 5
    object delta parser parser pack queue stream  # 6
    window merge socket index worker blob stream delta object delta buffer header  # 7
    blob worker branch queue stream pack commit remote ref queue ref  # 8
    commit header socket stream delta tree cache  # 9
    tree delta merge ref worker  # 10
    branch token commit ref object branch window rebase cache token  # 11
    ref remote handle worker ref object delta  # 12
    window cache tree ref commit header socket blob tree rebase header  # 13
    queue parser token queue pack  # 14
    delta blob header ref header tree pack branch merge  # 15
    queue stream delta object rebase delta header branch cache  # 16
    remote cache blob cache tree tree merge token  # 17
    handle worker remote index merge branch handle pack blob branch  # 18
    handle branch stream rebase merge  # 19
    window tree window delta pack object  # 20
    handle stream remote cache object  # 21
    handle blob merge parser object token queue  # 22
    parser ref blob delta commit commit buffer cache  # 23
    window branch index window worker commit commit cache merge  # 24
    queue queue stream pack header handle buffer tree tree  # 25
    delta rebase merge merge cache rebase commit  # 26
    token tree branch socket merge  # 27
    buffer rebase buffer ref token blob window parser  # 28
    merge ref branch cache header remote  # 29
    window delta window buffer commit socket remote  # 30
    branch commit token rebase  # 31
    handle token pack delta window pack branch commit pack  # 32
    rebase index ref merge rebase queue queue cache worker  # 33
    queue pack merge object queue token blob  # 34
    window index commit cache cache merge blob branch remote window stream  # 0
    object merge window queue socket branch index cache commit cache commit token  # 1
    window window window commit commit socket cache  # 2
    pack blob rebase socket queue cache pack pack queue socket index delta  # 3
    cache pack remote queue cache remote index tree queue stream stream stream  # 0
    queue remote pack window branch blob pack  # 1
    worker handle ref pack branch  # 2
    stream window socket merge rebase cache handle handle pack  # 3
    branch queue window object rebase  # 4
    handle tree header buffer index commit tree delta  # 5
    delta cache pack cache index  # 6
    remote tree cache parser handle stream branch window parser  # 0
    handle handle remote index header queue tree socket  # 1
    buffer object object stream object window stream token remote window token  # 2
    worker stream merge stream header remote socket ref rebase parser index  # 3
    ref pack commit remote object commit index remote  # 4
    queue merge cache cache buffer blob merge socket buffer  # 5
    remote cache commit merge pack index merge tree blob stream worker window  # 6
    header token ref queue parser token branch ref rebase socket rebase  # 7
    remote ref stream handle merge remote buffer branch window queue  # 8
    buffer socket token socket token object ref tree branch  # 9
    parser header branch parser cache buffer header rebase ref stream  # 10
    queue object handle blob  # 11
    tree socket tree blob socket worker worker delta branch index tree remote  # 12
    worker header index parser token object buffer commit cache branch window  # 13
    object token delta buffer window handle socket blob worker  # 0
    delta stream window window blob handle  # 1
    merge rebase index stream token handle queue pack handle rebase  # 2
    remote worker buffer stream queue  # 3
    token parser window ref  # 4
    pack stream object queue delta ref pack  # 5
    index object parser token buffer worker queue  # 6
