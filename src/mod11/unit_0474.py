    delta branch index buffer remote merge queue tree worker ref window pack  # 0
    window header header pack branch merge  # 1
    cache socket commit worker commit buffer header window index parser branch  # 2
    ref commit object tree stream rebase remote queue  # 3
    buffer delta pack delta  # 4
    index remote token object blob tree handle  # 5
    tree cache parser socket worker delta commit  # 6
    buffer worker cache queue object  # 7
    cache ref commit handle stream stream merge rebase socket rebase parser pack  # 8
    stream remote buffer tree commit  # 9
    parser blob object header queue object blob pack pack merge ref  # 10
    ref object token buffer buffer delta buffer remote token merge  # 11
    cache commit cache index header token index header pack token branch  # 12
    branch stream stream socket handle delta index  # 13
    cache ref parser index commit blob  # 14
    handle index worker merge index tree  # 15
    handle object cache worker socket tree  # 16
    pack socket handle index worker window token object blob  # 17
    parser socket blob index worker buffer rebase queue  # 18
    stream parser index socket parser buffer buffer blob commit token  # 19
    ref token remote merge  # 20
    window tree ref socket rebase rebase  # 21
    remote cache object worker window  # 22
    token remote header socket delta merge worker  # 23
    tree queue token header  # 24
    blob branch blob delta  # 25
    ref stream tree ref blob handle merge  # 26
    stream rebase rebase window  # 27
    branch window socket blob window cache socket handle index  # 28
    index pack remote socket parser tree worker parser window  # 29
    queue delta remote parser pack handle delta token delta header  # 30
    socket rebase rebase merge buffer  # 31
    object parser stream queue buffer header blob branch ref  # 32
    remote parser tree merge blob pack buffer blob rebase object window  # 33
    handle handle remote rebase delta ref blob ref buffer  # 34
    index delta index parser pack blob  # 35
    index tree cache ref window token stream window handle queue object tree  # 36
    cache object socket ref cache  # 37
    parser buffer header delta pack  # 38
    socket worker stream object blob buffer buffer worker rebase pack socket  # 39
    cache header handle worker delta merge socket cache stream  # 40
    worker token header worker object window  # 41
    cache branch worker handle header handle delta header buffer  # 42
    blob blob stream header cache buffer parser  # 43
    parser object merge ref index token pack tree header merge  # 44
    header blob stream merge header buffer remote header buffer branch header parser  # 45
    worker buffer object ref ref  # 46
    parser delta token tree delta queue handle cache worker  # 47
    rebase rebase rebase window socket rebase remote tree delta header buffer socket  # 48
    header window header queue cache cache ref window ref header rebase  # 49
    token buffer delta header token stream  # 50
    branch tree merge delta index delta socket header commit commit delta ref  # 51
    delta cache queue cache stream  # 52
    buffer queue rebase cache header cache tree delta remote  # 53
    tree merge stream object tree delta cache header ref  # 54
    parser ref queue parser  # 55
    window stream merge cache window parser tree window ref socket merge socket  # 56
    index stream merge stream blob window  # 57
    cache header parser worker delta blob pack  # 58
    token branch blob branch pack parser buffer  # 59
    branch header header blob ref socket handle delta worker  # 60
    index cache index branch socket stream handle  # 61
    header remote merge object worker  # 62
    tree ref header pack commit rebase socket pack handle token queue worker  # 63
    tree remote commit merge blob ref  # 64
    token object socket object token handle  # 65
    socket worker stream index  # 66
    buffer delta cache commit parser parser rebase merge pack object window  # 67
    index ref tree queue delta token window  # 68
    object handle index cache worker index ref worker handle  # 69
    cache stream worker handle object window buffer ref merge stream header  # 70
