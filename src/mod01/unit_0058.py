    cache commit buffer queue merge handle branch handle blob window index buffer  # 0
    parser stream pack window window ref buffer remote cache  # 1
    buffer rebase commit socket cache branch blob  # 2
    rebase worker pack token merge object  # 3
    header tree branch delta handle socket header handle tree  # 4
    tree blob handle buffer handle tree buffer branch blob  # 5
    header handle tree cache  # 6
    ref window merge socket object ref worker commit  # 7
    stream merge socket worker  # 8
    rebase header delta tree  # 9
    parser socket object worker token buffer window cache object parser tree  # 10
    pack worker worker worker window parser  # 11
    delta index remote pack token ref commit parser rebase branch  # 12
    ref delta buffer ref branch tree cache handle ref  # 13
    ref worker remote cache header token buffer branch header index  # 14
    index socket rebase queue socket commit object merge  # 15
    header delta pack handle socket queue  # 16
    tree commit delta token  # 17
    handle header commit cache queue parser object  # 18
    socket ref merge remote ref merge queue blob  # 19
    pack index commit pack stream merge ref queue remote  # 20
    parser queue header index  # 21
    window worker worker rebase window remote merge socket buffer  # 22
    index remote tree pack stream parser remote tree handle socket  # 23
    index queue cache stream buffer stream queue index  # 24
    remote cache queue delta rebase socket handle commit handle object header remote  # 25
    object pack object cache  # 26
    merge object buffer index blob object merge ref merge ref index index  # 27
    remote socket remote stream index  # 28
    rebase queue blob socket parser  # 29
    stream header stream delta delta parser header pack merge window buffer  # 30
    buffer window parser cache  # 31
    commit socket commit object stream worker pack commit  # 32
    worker worker cache pack commit branch cache cache queue handle  # 33
    header pack stream worker  # 34
    handle branch merge remote remote ref tree branch parser worker  # 35
    merge merge cache stream blob tree parser header pack commit object tree  # 36
    ref index delta stream merge worker commit object blob queue  # 37
    delta blob delta delta  # 38
    commit remote parser parser token tree ref parser handle stream  # 39
    worker stream branch remote cache object socket socket pack worker buffer rebase  # 40
    parser header parser cache queue queue token blob  # 41
    tree ref index socket tree  # 42
    worker parser branch commit commit pack rebase stream  # 43
    index queue parser object object commit window window socket  # 44
    merge worker queue index delta socket stream parser token  # 45
    token handle delta merge ref merge delta commit header tree worker  # 46
    blob worker socket remote rebase socket stream socket pack stream  # 47
    branch index rebase remote token blob window worker  # 48
    remote blob buffer branch queue header blob handle  # 49
    branch index merge handle tree token handle window parser  # 50
    handle index worker buffer handle  # 51
    buffer commit header buffer tree delta commit parser buffer buffer commit  # 52
    token worker token tree blob  # 53
    cache rebase rebase stream delta  # 54
    tree delta worker tree rebase object commit ref tree socket remote handle  # 55
    delta stream blob merge queue window buffer pack  # 56
    blob socket parser cache cache object window handle window stream blob queue  # 57
    blob commit socket ref rebase  # 58
    pack socket ref buffer token parser branch socket  # 59
    branch index rebase socket branch stream token parser object handle header tree  # 60
    object index stream delta socket index rebase cache commit remote cache header  # 61
    buffer handle queue index handle cache pack tree  # 62
    header parser worker token window ref tree blob buffer  # 63
    worker commit tree rebase ref remote index ref  # 64
    delta worker blob token queue  # 65
    parser index worker ref stream buffer blob worker  # 66
    commit stream merge branch window cache parser tree buffer stream  # 67
    socket header buffer blob object stream  # 68
    blob tree token socket stream stream blob stream pack  # 69
    worker token tree index handle handle stream object blob tree worker  # 70
    header header ref blob index parser index blob  # 71
    object worker handle header worker blob cache delta  # 72
    window token buffer index token  # 73
    merge merge object delta ref  # 74
    rebase remote delta pack handle header ref buffer rebase  # 75
    ref ref remote handle token pack parser merge header window remote  # 76
    remote tree socket blob stream socket header socket ref  # 77
    queue header queue header cache token remote object  # 78
    parser token index worker token header tree cache  # 79
