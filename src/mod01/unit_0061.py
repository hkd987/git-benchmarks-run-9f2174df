    object index pack blob tree worker commit blob merge  # 0
    ref worker commit tree index branch  # 1
    stream stream token window index  # 2
    ref socket token object header worker  # 3
    delta worker pack queue socket ref  # 4
    window queue parser cache branch remote handle  # 5
    stream stream handle buffer socket worker parser blob pack  # 6
    tree parser branch delta  # 7
    pack branch token branch merge window header socket socket buffer buffer window  # 8
    commit parser token buffer index merge queue handle  # 9
    header object merge merge  # 10
    pack cache cache commit worker branch header remote  # 11
    tree header delta blob header remote worker handle cache blob tree  # 12
    worker socket handle token worker window worker rebase delta parser cache  # 13
    handle blob rebase header ref socket commit ref index tree parser blob  # 14
    socket buffer index header  # 15
    remote header queue tree stream cache delta parser pack worker  # 16
    worker handle pack remote queue header rebase socket  # 17
    worker buffer merge object merge object window commit pack token  # 18
    token token cache rebase parser  # 19
    queue window buffer remote worker blob  # 20
    token delta blob blob tree  # 21
    rebase window queue worker handle cache object pack tree index  # 22
    object buffer pack stream remote merge ref buffer  # 23
    queue commit blob ref tree queue remote merge object  # 24
    worker rebase token window blob buffer buffer stream blob queue  # 25
    commit stream rebase buffer token commit window token worker pack handle pack  # 26
    header pack tree branch ref delta buffer stream  # 27
    window delta object worker window  # 28
    buffer tree delta queue  # 29
    commit stream commit stream index handle  # 30
    handle commit worker remote  # 31
    stream stream queue merge object cache  # 32
    rebase pack ref worker commit header merge remote  # 33
    index delta remote object commit object rebase parser merge buffer commit  # 34
    handle window delta queue ref window object merge parser rebase handle merge  # 35
    pack window delta cache  # 36
    token worker remote buffer parser  # 37
    rebase branch commit remote header  # 38
    delta merge handle commit commit header  # 39
    parser header rebase token delta tree rebase  # 40
    object rebase delta index delta header remote pack queue index  # 41
    header merge cache cache blob queue worker branch blob socket rebase  # 42
    remote queue token socket parser pack pack pack commit ref remote  # 43
    pack delta window worker delta branch pack rebase  # 44
    ref header delta blob rebase commit header buffer buffer  # 45
    commit worker tree commit  # 46
    window parser header blob tree delta queue commit branch cache worker  # 47
    tree blob pack queue branch merge merge stream socket window  # 48
    delta blob remote socket handle pack blob header window  # 49
    delta delta blob window stream  # 50
    buffer rebase commit stream stream remote cache rebase parser  # 51
    pack buffer window delta pack object  # 52
    index merge cache pack tree  # 53
    tree cache merge merge  # 54
    blob index buffer socket parser  # 55
    stream delta window rebase socket header buffer stream handle queue branch  # 56
    remote branch merge merge worker stream remote buffer worker header  # 57
    header window remote cache worker  # 58
    handle stream commit tree window cache cache blob commit merge pack handle  # 59
    branch tree tree object  # 60
    parser socket blob token  # 61
    worker worker handle header pack pack stream delta object  # 62
    token object branch stream tree  # 63
    socket object parser branch window header commit ref worker  # 64
