    buffer tree delta rebase delta stream  # 0
    index index branch handle pack  # 1
    queue pack window token cache parser handle handle object  # 2
    stream branch header socket remote  # 3
    handle token pack parser token token branch stream pack  # 4
    index delta socket commit merge commit header merge cache window  # 5
    socket parser buffer socket buffer socket  # 6
    stream cache stream blob rebase  # 7
    index handle tree object rebase remote handle rebase parser worker socket remote  # 8
    remote cache object rebase branch object  # 9
    merge delta parser handle token parser parser blob blob token remote  # 10
    buffer worker rebase branch  # 11
    window delta token handle  # 12
    cache pack tree delta queue window queue object  # 13
    cache handle branch token buffer worker  # 14
    token blob branch tree buffer window object socket index blob commit token  # 15
    blob cache socket stream tree cache object  # 16
    blob cache worker handle socket buffer  # 17
    object blob rebase ref cache socket queue rebase pack buffer  # 18
    worker object queue remote pack object queue rebase  # 19
    commit window parser socket rebase object delta  # 20
    remote parser pack remote stream worker header handle  # 21
    header stream merge handle ref pack commit blob stream queue pack object  # 22
    remote handle index delta window stream rebase  # 23
    delta ref window branch remote worker buffer token remote rebase blob handle  # 24
    blob rebase queue stream  # 25
    delta stream merge parser token parser  # 26
    blob rebase ref parser  # 27
    token branch cache stream  # 28
    ref merge window merge queue blob queue branch token object remote  # 29
    tree socket handle object branch window  # 30
    queue tree merge buffer  # 31
    handle rebase object token cache header  # 32
    merge ref tree object cache window header handle object stream tree  # 33
    index handle cache socket window socket stream  # 34
    rebase socket parser ref ref token worker rebase worker  # 35
    token queue branch blob merge socket merge window window  # 36
    remote stream pack delta handle stream  # 37
    window cache index remote rebase  # 38
    pack delta ref pack queue cache  # 39
    header ref buffer queue worker handle stream index tree  # 40
    branch header handle header header queue window  # 41
    stream object stream merge handle blob  # 42
    buffer header delta ref queue tree object remote blob stream  # 43
    cache merge window window buffer stream remote handle object cache handle buffer  # 44
    pack cache cache tree blob index branch  # 45
    cache ref token rebase  # 46
    delta parser branch delta pack  # 47
    tree cache cache delta rebase header worker object object  # 48
    index window ref rebase handle ref  # 49
    ref ref branch merge merge pack branch header header worker  # 50
    token rebase header rebase parser ref rebase object object handle pack token  # 51
    socket commit delta pack merge remote tree branch token pack blob rebase  # 52
    queue index branch tree  # 53
    ref rebase merge worker object delta ref rebase buffer  # 54
    delta token object index queue rebase index index object stream index remote  # 55
    index stream delta merge  # 56
    tree ref worker ref tree  # 57
    commit ref commit worker buffer commit remote worker index  # 58
    branch branch token socket stream parser branch buffer stream branch  # 59
    object commit window queue buffer pack pack  # 60
    pack cache token stream parser window header ref branch token handle  # 61
    ref window parser parser ref  # 62
    header cache ref remote  # 63
    pack ref stream token header stream token cache tree remote rebase  # 64
    window remote pack buffer pack remote handle  # 65
    ref object worker window remote ref blob buffer merge parser pack  # 66
    branch worker blob delta blob merge blob cache delta parser stream remote  # 67
    branch commit pack ref  # 68
    remote remote cache delta delta queue window buffer  # 0
    token delta branch index token queue branch window pack  # 1
    rebase worker remote remote branch pack ref cache worker index header  # 2
