    branch stream buffer window parser  # 0
    blob index object tree cache rebase stream commit parser token pack socket  # 1
    index blob buffer pack queue handle commit token remote worker  # 2
    parser ref tree tree ref tree delta pack rebase  # 3
    buffer index delta socket commit stream handle merge remote  # 4
    rebase merge object queue  # 5
    ref remote delta cache handle index cache tree handle header branch rebase  # 6
    window handle delta tree merge  # 7
    buffer pack rebase commit stream branch cache branch merge branch  # 8
    remote pack buffer parser worker commit handle handle delta cache merge  # 9
    remote header ref handle  # 10
    blob merge handle queue parser pack header header queue socket pack header  # 11
    header parser header header index queue socket object stream commit tree  # 12
    window buffer branch index commit ref stream delta stream stream blob  # 13
    remote object header stream blob delta blob branch blob cache  # 14
    header parser window header merge index queue  # 15
    branch branch handle token  # 16
    window socket commit branch header tree ref ref  # 17
    tree remote ref token remote  # 18
    remote tree window buffer index header cache delta worker  # 19
    delta merge handle window branch window worker  # 20
    branch remote buffer token  # 21
    blob socket handle queue pack worker stream parser queue  # 22
    queue rebase remote parser  # 23
    handle remote merge commit  # 24
    window token remote merge branch commit tree window rebase blob stream  # 25
    stream parser socket socket  # 26
    buffer rebase commit buffer  # 27
    rebase buffer token buffer queue stream object commit stream  # 28
    merge handle pack commit commit  # 29
    pack parser token header index handle token parser token stream rebase  # 30
    window commit tree worker  # 31
    header window delta tree blob object delta header socket  # 32
    socket merge header token remote  # 33
    index worker worker window delta ref object blob blob  # 34
    handle stream index branch window handle window worker merge socket pack remote  # 35
    delta branch object index rebase remote branch commit rebase token branch token  # 36
    pack socket object ref header window commit pack merge window  # 37
    blob cache buffer window  # 38
    pack blob queue remote rebase handle index delta merge merge buffer index  # 39
    delta blob tree window token handle branch worker window  # 40
    handle delta merge pack worker branch socket delta worker  # 41
    ref merge remote commit queue cache commit buffer token rebase  # 42
    handle parser delta worker rebase socket stream stream worker blob  # 43
    rebase index token index token tree delta branch queue cache remote cache  # 44
    delta queue merge socket ref delta handle branch  # 45
    remote remote index token token ref  # 46
    tree pack remote stream parser remote rebase remote  # 47
    pack socket index remote tree socket queue tree  # 48
    queue blob merge handle handle buffer ref queue ref delta pack  # 49
    branch window buffer token blob buffer pack blob merge ref cache  # 50
    tree branch object socket branch remote buffer remote socket token cache  # 51
    index branch parser cache object delta handle branch socket  # 52
    header worker commit pack object  # 53
    token queue commit pack remote worker  # 54
    blob cache rebase object rebase blob handle commit buffer delta ref  # 55
    rebase merge socket rebase header parser merge buffer object handle parser socket  # 56
    merge blob cache pack tree socket queue pack index tree cache  # 57
    delta object stream window buffer  # 58
    branch blob cache index index remote pack window socket  # 59
    cache index rebase tree socket index blob  # 60
    delta header pack pack tree tree branch cache cache blob header  # 61
    window rebase parser index object tree pack blob token commit buffer branch  # 62
    remote index queue ref remote stream blob object buffer commit  # 63
    pack socket header header object remote parser object tree delta  # 64
    index stream stream handle worker object queue worker index socket  # 65
    merge remote remote handle branch branch queue queue buffer cache handle ref  # 66
    merge object window merge cache socket merge ref pack header branch socket  # 67
    delta stream header header stream window parser merge merge  # 68
    socket socket rebase object header parser blob header  # 69
    worker token window ref index socket object tree stream socket  # 70
    buffer pack cache window cache ref socket window cache buffer rebase window  # 71
    parser branch ref rebase object index cache stream  # 72
    socket merge rebase delta remote token socket merge  # 73
    remote pack handle cache tree index worker delta token  # 74
    queue socket rebase remote token merge token queue window worker window  # 75
    index blob header branch delta header handle merge rebase merge  # 76
    header socket token ref socket queue worker blob remote pack index ref  # 77
    window buffer merge object buffer commit queue  # 78
    index remote window socket commit ref tree tree commit cache  # 79
    branch delta ref blob  # 80
    parser ref handle rebase parser buffer rebase index header buffer  # 81
    cache cache blob socket  # 82
    buffer cache window queue socket queue header remote worker delta  # 0
    stream header branch rebase socket branch worker merge tree  # 1
    object worker merge pack merge parser  # 2
    remote tree window worker header merge  # 3
    ref branch tree commit index pack ref token token  # 4
    ref ref index socket  # 5
    delta rebase object handle parser socket  # 6
    header window delta cache  # 7
    ref index window blob index remote queue branch queue  # 8
    branch parser rebase socket delta remote parser commit  # 9
    parser token index stream merge buffer merge buffer parser parser merge  # 10
    pack merge rebase queue queue commit blob header delta  # 11
