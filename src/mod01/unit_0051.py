    socket object parser object stream cache stream handle  # 0
    delta socket tree object merge queue tree parser buffer buffer branch  # 1
    token ref remote delta object branch branch merge  # 2
    tree buffer header delta socket merge handle worker stream  # 3
    token object commit header object buffer queue cache  # 4
    queue ref tree merge parser cache  # 5
    commit pack rebase queue index cache pack pack rebase handle header  # 6
    handle buffer cache rebase rebase socket window  # 7
    commit commit remote cache pack buffer token window  # 8
    ref cache index tree merge rebase branch buffer  # 9
    handle merge commit buffer queue merge branch blob token socket remote  # 10
    blob stream delta object token blob index commit branch blob token socket  # 11
    blob tree queue branch socket stream  # 12
    index remote token branch pack worker socket  # 13
    tree parser socket cache commit cache header handle  # 14
    object branch tree token parser object rebase blob  # 15
    branch stream buffer branch worker merge blob queue  # 16
    delta socket delta object queue remote blob branch  # 17
    handle buffer blob parser commit stream worker  # 18
    buffer ref index merge socket object token socket socket token ref object  # 19
    handle token header branch socket stream  # 20
    pack remote pack branch header branch queue object socket pack ref  # 21
    handle handle pack delta worker  # 22
    handle blob queue queue buffer ref stream stream merge rebase header merge  # 23
    ref object header delta socket delta pack  # 24
    header rebase window commit remote socket parser pack buffer object pack ref  # 25
    buffer buffer tree window pack  # 26
    remote merge window commit object header pack  # 27
    commit merge parser blob branch remote remote queue token socket ref  # 28
    stream merge buffer ref parser  # 29
    pack header header token index blob  # 30
    window handle index token delta header handle  # 31
    token merge blob buffer window index object header  # 32
    token worker branch header header queue commit worker cache index object  # 33
    cache index cache handle delta blob branch merge worker  # 34
    cache token commit branch header cache  # 35
    delta worker socket window object  # 36
    index branch index worker header header window rebase branch blob parser header  # 37
    tree pack cache remote remote cache cache stream pack token commit  # 38
    branch object cache socket tree buffer ref blob  # 39
    branch header tree buffer merge  # 40
    tree index stream window delta handle ref queue  # 41
    delta pack stream worker branch  # 42
    stream index remote socket  # 43
