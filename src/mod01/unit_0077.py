    rebase header rebase handle rebase buffer  # 0
    worker remote buffer window blob header tree buffer index buffer  # 1
    ref delta worker tree  # 2
    object branch blob object pack pack token rebase token remote rebase tree  # 3
    branch tree index blob branch commit token token  # 4
    queue commit blob queue branch  # 5
    header header buffer merge commit token merge pack merge worker merge  # 6
    queue queue buffer merge socket cache  # 7
    handle buffer delta blob  # 8
    socket socket socket remote blob worker worker stream ref header commit token  # 9
    merge delta ref tree ref remote parser object ref delta rebase  # 10
    header pack object header window buffer merge window  # 11
    parser socket queue buffer blob worker  # 12
    stream header token commit index rebase object  # 13
    remote socket tree cache  # 14
    delta window parser parser commit queue  # 15
    branch rebase cache cache queue handle  # 16
    handle commit index worker queue merge  # 17
    ref socket commit blob rebase pack  # 18
    stream pack index socket  # 19
    socket stream branch header cache token rebase header handle index header  # 20
    parser worker remote handle merge blob  # 21
    socket queue socket pack  # 22
    handle ref buffer index  # 23
    handle token pack rebase cache  # 24
    parser parser ref stream object blob buffer parser branch tree buffer  # 25
    handle handle header blob window delta queue window header blob buffer ref  # 26
    rebase index stream ref token worker parser buffer window remote token  # 27
    header token blob header merge socket  # 28
    merge handle ref worker handle token object  # 29
    stream merge queue blob merge object rebase commit  # 30
    tree remote ref blob blob  # 31
    cache blob cache stream worker cache  # 32
    handle tree remote worker window header parser queue rebase  # 33
    object buffer header merge  # 34
    header branch pack stream  # 35
    merge queue parser stream tree rebase delta buffer remote commit branch  # 36
    buffer merge tree queue index ref delta parser token rebase  # 37
    socket commit ref handle branch branch token parser blob handle pack  # 38
    delta delta index object branch  # 39
    blob buffer stream buffer header  # 40
    object parser header stream tree buffer  # 41
    worker index object tree handle handle  # 0
    window pack parser tree parser merge pack branch merge  # 1
    parser buffer ref rebase delta token worker merge header buffer header  # 2
    blob socket merge token blob token commit blob tree merge  # 3
    pack handle rebase token commit handle delta pack remote window  # 4
