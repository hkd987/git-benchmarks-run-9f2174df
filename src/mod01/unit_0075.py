    remote delta handle stream pack commit remote header parser token queue stream  # 0
    remote worker pack ref object socket delta worker index  # 1
    branch remote pack cache cache branch rebase queue index socket merge pack  # 2
    branch pack branch parser object  # 3
    handle index queue delta index pack index index tree  # 4
    tree merge index index handle merge worker branch merge  # 5
    pack pack merge branch object tree blob parser socket  # 6
    window branch buffer branch object merge buffer handle ref parser token  # 7
    remote tree delta merge rebase cache buffer blob handle header  # 8
    stream pack queue parser handle header branch ref pack handle tree worker  # 9
    pack delta window parser  # 10
    header commit worker handle ref socket tree buffer  # 11
    buffer branch merge ref tree delta queue ref merge blob  # 12
    header merge cache commit pack  # 13
    parser queue commit ref cache remote header parser pack  # 14
    handle cache window remote ref index  # 15
    rebase handle queue index  # 16
    pack delta handle socket delta buffer buffer  # 17
    header commit rebase token ref stream blob branch tree  # 18
    merge commit socket ref commit worker pack  # 19
    window cache merge delta  # 20
    window parser branch cache ref index remote stream window  # 21
    commit cache parser queue ref token  # 22
    queue worker cache branch  # 23
    worker object remote index  # 24
    window window queue handle header remote rebase branch pack  # 25
    blob delta queue blob remote  # 26
    delta cache remote delta pack branch window tree branch tree  # 27
    cache queue queue blob tree  # 28
    worker stream tree window socket queue  # 29
    stream delta remote cache rebase commit delta buffer socket  # 30
    blob handle queue object commit remote object  # 31
    tree socket queue object window parser  # 32
    buffer socket ref buffer ref commit stream merge delta  # 33
    queue remote rebase handle  # 34
    branch commit blob handle object index stream  # 35
    parser ref window merge remote  # 36
    stream object parser tree blob cache pack  # 37
    buffer header branch parser ref cache worker commit socket  # 38
    buffer handle delta branch commit socket blob branch commit buffer  # 39
    buffer header commit pack blob ref tree socket blob index merge worker  # 40
    merge handle merge branch merge stream socket branch buffer window  # 41
    worker blob socket parser handle  # 42
