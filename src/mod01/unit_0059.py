    socket socket handle blob object socket  # 0
    ref rebase rebase index buffer buffer stream  # 1
    tree branch tree stream commit branch socket parser delta handle ref delta  # 2
    branch header stream buffer ref worker merge cache  # 3
    buffer header worker buffer merge rebase merge  # 4
    token window worker pack stream header commit branch  # 5
    window rebase delta blob delta branch window merge stream header  # 6
    buffer socket queue index header queue window pack remote worker buffer merge  # 7
    merge pack stream blob commit token window token  # 8
    worker cache worker ref pack window object index cache window  # 9
    handle parser handle rebase queue handle remote branch tree commit  # 10
    worker queue ref index worker  # 11
    delta token delta stream  # 12
    parser header header parser remote window rebase  # 13
    cache header pack token ref token window merge merge  # 14
    queue stream pack queue window pack tree index parser blob tree  # 15
    buffer queue header token index branch header commit  # 16
    handle stream delta pack pack queue rebase ref cache stream parser merge  # 17
    worker cache index index  # 18
    worker socket delta handle tree tree parser stream pack  # 19
    tree queue queue socket object socket header  # 20
    object stream commit queue parser pack cache branch index window merge index  # 21
    ref delta handle socket branch commit handle object tree token tree commit  # 22
    rebase handle handle socket handle cache index  # 23
    branch blob socket remote remote object header remote token queue  # 24
    socket header parser worker queue delta branch  # 25
    object window commit stream window index queue index  # 26
    buffer merge object window delta cache header object cache ref commit stream  # 27
    token pack tree object worker commit  # 28
    stream pack socket parser queue stream merge delta  # 29
    header commit socket ref remote buffer  # 30
    rebase ref pack token branch header handle delta worker socket index  # 31
    socket remote queue socket queue  # 32
    window branch header window window  # 33
    delta tree window commit handle index branch stream remote  # 34
    rebase object index header window stream buffer pack  # 35
    index pack header merge merge ref token remote worker rebase  # 36
    commit merge ref stream parser delta merge buffer window object  # 37
    ref commit tree commit worker pack index parser parser handle worker rebase  # 38
    delta ref ref worker stream cache handle cache  # 39
    cache buffer pack worker blob window cache worker branch window  # 40
    pack object queue token delta cache index index  # 0
    pack commit cache cache merge cache window pack  # 1
    parser cache commit window buffer stream commit buffer socket  # 2
    cache merge parser tree worker delta merge  # 3
    parser parser stream queue merge rebase commit ref ref socket  # 0
    parser ref rebase object merge blob header worker remote index rebase  # 1
    merge cache stream blob header tree delta parser  # 2
    ref blob header token stream merge blob handle pack window object handle  # 3
