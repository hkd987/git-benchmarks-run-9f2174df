    header worker stream window parser ref  # 0
    buffer handle socket index merge  # 1
    index index ref merge socket pack tree pack header handle window  # 2
    blob queue object buffer commit blob cache  # 3
    rebase branch delta pack cache buffer socket cache delta pack tree  # 4
    remote pack index pack stream commit handle  # 5
    parser delta index pack buffer branch cache header rebase  # 6
    token tree merge delta tree window index  # 7
    pack merge socket buffer ref pack commit merge remote remote  # 8
    remote pack branch header token commit branch ref queue  # 9
    remote buffer remote handle cache handle cache header queue merge  # 10
    delta handle cache remote object remote  # 11
    branch tree tree rebase handle handle ref pack queue rebase  # 12
    commit pack delta object buffer ref  # 13
    worker parser pack ref worker index socket parser queue window index  # 14
    ref blob buffer window tree tree branch socket index stream rebase  # 15
    pack stream branch pack  # 16
    token cache blob remote handle blob  # 17
    commit worker token remote blob handle blob  # 18
    handle pack object cache stream stream socket socket ref tree  # 19
    object remote tree remote buffer handle merge  # 20
    buffer pack token commit object rebase commit object cache pack  # 21
    stream rebase header header socket worker queue delta  # 22
    buffer delta token ref cache queue ref socket socket remote queue  # 23
    merge buffer delta pack token  # 24
    commit queue blob rebase cache remote index object commit cache worker object  # 25
    commit stream blob token merge  # 26
    ref ref blob window token  # 27
    rebase stream delta merge parser window blob socket handle handle  # 28
    handle blob delta blob window tree buffer token cache index  # 29
