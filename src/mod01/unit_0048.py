    queue buffer merge parser handle merge  # 0
    delta socket worker stream worker header header branch stream  # 1
    commit socket blob pack blob header worker branch window stream  # 2
    branch parser socket buffer merge  # 3
    stream socket pack socket cache  # 4
    branch commit pack stream worker header stream cache  # 5
    remote ref window socket cache merge commit blob pack tree tree index  # 6
    blob stream header worker blob  # 7
    rebase stream commit worker pack commit buffer delta pack rebase tree header  # 8
    cache socket socket buffer rebase parser  # 9
    merge worker merge header tree pack branch worker  # 10
    index queue blob tree queue buffer window rebase header object cache  # 11
    rebase tree pack rebase ref delta  # 12
    delta window queue blob blob merge buffer buffer  # 13
    delta socket rebase token branch merge window tree index header cache  # 14
    window buffer socket remote  # 15
    worker branch object object parser commit  # 16
    header merge commit pack worker blob index branch object header header  # 17
    commit ref worker handle header cache socket merge blob token  # 18
    stream pack queue object queue worker tree blob rebase pack cache  # 19
    window socket cache branch remote header token index object  # 20
    socket token token object token commit header buffer buffer  # 21
    stream queue rebase branch object rebase rebase worker header  # 22
    remote pack parser socket  # 23
    worker cache buffer header delta blob handle parser rebase tree stream  # 24
    cache cache buffer socket window cache merge object object commit window token  # 25
    commit header queue commit index merge index commit pack tree handle cache  # 26
    buffer stream ref queue index branch buffer socket socket branch  # 27
    handle ref window buffer socket pack tree tree  # 28
    ref tree rebase object header worker header buffer remote branch branch blob  # 29
    remote window header token commit ref  # 30
    pack queue delta rebase merge token commit queue delta  # 31
    commit header parser queue cache  # 32
    window blob window socket tree buffer worker parser token  # 33
    token stream socket ref handle stream  # 34
    handle header tree object queue socket rebase token token blob token worker  # 35
    delta object index handle parser window header pack handle blob buffer socket  # 36
    parser pack blob cache blob object branch ref remote  # 37
    index index object rebase delta  # 38
    object socket index window queue handle token window parser worker  # 39
    object window buffer merge  # 40
    merge ref buffer tree parser  # 41
    buffer queue object rebase stream window token  # 42
    pack remote cache object pack commit delta handle parser  # 43
    index worker buffer pack parser ref commit remote branch remote blob socket  # 44
    buffer blob socket buffer handle stream pack socket worker rebase commit stream  # 45
    stream commit socket index  # 46
    token pack handle index remote token handle blob worker parser stream  # 47
    parser commit parser worker delta window rebase worker header blob cache delta  # 48
    parser merge cache parser pack tree pack  # 49
    branch index queue ref socket  # 50
    merge queue tree tree object tree tree  # 51
    cache commit rebase rebase window socket pack pack stream  # 52
    blob stream branch remote buffer commit window socket worker rebase commit  # 53
    token buffer blob branch blob buffer worker merge cache cache blob buffer  # 54
    worker delta cache window header parser object  # 55
    commit ref object blob socket parser blob delta rebase  # 56
    commit merge object pack pack  # 57
    worker index commit queue socket header buffer parser remote  # 58
    commit window blob header remote header buffer blob  # 59
    cache cache object stream handle  # 60
    branch stream cache parser object ref worker queue branch buffer header  # 61
    merge object worker window queue remote parser buffer delta queue queue merge  # 62
    commit window index handle commit  # 63
    handle delta rebase cache handle object cache object  # 64
    branch tree index cache queue index buffer  # 65
    queue ref buffer pack header pack commit handle ref window  # 66
    index cache branch remote queue queue stream header branch buffer commit  # 67
    buffer delta window tree rebase merge parser handle index branch tree  # 68
    remote worker worker merge handle worker buffer  # 69
    rebase queue queue window index rebase parser blob handle ref  # 70
    queue stream window ref  # 71
