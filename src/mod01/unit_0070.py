    branch branch object cache header stream  # 0
    worker merge header buffer socket  # 1
    commit parser buffer queue socket rebase stream remote token  # 2
    buffer rebase queue remote window  # 3
    stream worker tree parser merge rebase socket buffer pack merge  # 4
    branch buffer branch object tree worker  # 5
    ref blob tree header rebase merge socket remote token buffer  # 6
    stream worker rebase header  # 7
    ref ref ref remote window  # 8
    stream queue handle object parser  # 9
    rebase handle parser handle blob worker handle ref commit  # 10
    buffer blob remote ref worker handle ref socket blob object index  # 11
    worker queue blob token handle  # 12
    worker merge pack ref  # 13
    commit worker blob blob remote tree handle pack buffer merge header  # 14
    queue parser delta handle parser cache commit socket  # 15
    cache commit socket handle  # 16
    remote handle buffer handle token pack token index buffer cache remote remote  # 17
    delta blob ref queue handle worker socket  # 18
    header buffer tree buffer ref window commit  # 19
    pack ref blob branch  # 20
    socket parser window header  # 21
    rebase parser handle socket queue tree rebase  # 22
    commit blob stream worker merge branch merge token branch branch  # 23
    worker merge handle branch queue delta window socket  # 24
    delta tree pack index queue stream delta blob  # 25
    delta parser branch worker parser tree  # 26
    tree queue blob window rebase  # 27
    pack remote token pack window merge index rebase merge handle stream header  # 28
    delta handle window rebase commit branch  # 29
    socket worker merge remote delta object handle handle worker rebase  # 30
    delta queue rebase buffer branch commit buffer handle  # 31
    token rebase object stream  # 32
    cache buffer blob remote handle cache queue stream queue tree parser  # 33
    window pack buffer window  # 34
    parser blob queue remote worker tree tree rebase stream socket commit  # 35
    cache cache delta parser token  # 36
    parser queue remote buffer commit socket delta stream  # 37
    object queue buffer queue branch remote worker object cache pack remote window  # 38
    remote index delta buffer buffer merge commit  # 39
    merge blob buffer socket delta socket merge  # 40
    delta queue token handle  # 41
    commit merge window stream token handle blob  # 42
    stream window socket commit commit socket index handle blob  # 43
    worker commit handle parser commit handle  # 44
    index stream commit index handle delta window parser delta commit  # 45
    socket merge cache commit remote token queue object  # 46
    merge delta header index cache blob pack pack  # 47
    rebase delta index worker merge pack commit queue buffer  # 48
    handle stream rebase handle index  # 49
    blob cache merge commit worker handle stream pack parser  # 50
    token cache remote pack  # 51
    commit ref rebase pack pack remote remote rebase delta  # 52
    rebase parser header token socket index ref rebase  # 53
    commit token stream handle rebase window object window ref rebase queue  # 54
    pack delta cache handle worker branch window remote header  # 55
    cache buffer handle socket window merge window worker tree remote remote  # 56
    delta merge commit ref  # 57
    parser ref remote ref remote cache remote worker socket  # 58
    token handle header remote branch window queue socket  # 59
    index buffer worker delta blob  # 60
    rebase remote stream header header parser rebase worker rebase parser socket  # 61
    rebase blob queue header stream index ref branch ref queue  # 62
    queue branch window ref blob parser remote worker object window object object  # 63
    rebase remote tree merge rebase worker object rebase ref object  # 64
    window stream rebase rebase cache  # 65
    rebase tree tree remote buffer cache rebase parser parser  # 66
    branch tree stream worker handle commit socket branch stream window  # 67
