    blob ref tree delta worker queue token header  # 0
    rebase parser pack window rebase  # 1
    object worker handle tree pack stream blob handle socket  # 2
    ref queue handle object pack queue parser commit remote object parser  # 3
    buffer token pack index branch header merge index buffer window window index  # 4
    token delta header merge header socket worker worker handle  # 5
    parser object branch token rebase parser buffer delta  # 6
    merge merge branch handle cache index cache object header  # 7
    rebase token ref pack pack object merge buffer rebase handle  # 8
    token buffer commit merge blob tree remote  # 9
    queue commit header ref tree handle  # 10
    queue remote branch pack queue stream pack window queue branch  # 11
    merge branch object stream merge delta cache delta  # 12
    token branch object worker pack socket parser pack ref delta  # 13
    cache buffer stream blob rebase  # 14
    rebase token remote remote blob index pack merge handle  # 15
    worker rebase pack commit rebase stream index queue blob header header queue  # 16
    ref cache window delta cache  # 17
    header parser remote tree remote parser  # 18
    queue queue merge queue pack queue branch worker  # 19
    worker worker ref pack merge  # 20
    worker commit tree remote  # 21
    remote cache tree delta handle socket worker worker window socket parser remote  # 22
    queue cache worker object cache index header  # 23
    rebase socket delta buffer stream buffer  # 24
    merge object buffer branch blob index socket blob branch header parser  # 25
    object object parser object merge commit blob  # 26
    pack buffer remote worker index buffer header object tree worker  # 27
    delta cache window window parser rebase blob delta header buffer object  # 28
    index merge buffer blob  # 29
    stream buffer queue stream socket queue object rebase cache rebase  # 30
    tree buffer pack commit pack tree ref branch  # 31
    tree merge window pack remote branch commit  # 32
    buffer delta token branch branch rebase commit buffer stream tree tree ref  # 33
    index index cache queue handle remote  # 34
    socket delta window index stream handle window rebase header window object socket  # 35
    object socket branch tree token blob cache  # 36
    rebase window parser rebase branch token merge tree  # 37
    rebase delta tree queue worker  # 38
    ref window handle worker branch worker buffer branch  # 39
    buffer pack blob commit object worker  # 40
    cache ref socket buffer delta  # 41
    index handle header ref tree  # 42
    remote branch branch worker  # 43
    parser commit worker header stream blob merge ref remote commit  # 44
    header branch queue tree commit queue buffer  # 45
