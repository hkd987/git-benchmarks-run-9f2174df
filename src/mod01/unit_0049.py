    ref commit remote pack rebase window stream cache handle  # 0
    ref delta cache worker  # 1
    rebase header remote socket commit worker ref header commit  # 2
    token delta token branch window  # 3
    index remote ref blob  # 4
    pack index merge handle stream object  # 5
    merge worker tree header  # 6
    socket token stream ref cache  # 7
    stream merge cache parser queue object queue worker token branch ref  # 8
    queue index queue merge queue queue commit header  # 9
    queue buffer index cache  # 10
    socket rebase parser blob worker remote handle blob rebase  # 11
    handle blob buffer delta handle parser blob blob blob remote branch cache  # 12
    ref token remote token branch  # 13
    stream commit parser queue merge tree buffer ref rebase tree  # 14
    delta handle tree pack tree window branch delta socket stream  # 15
    buffer socket pack handle buffer object commit  # 16
    ref merge window remote delta worker index handle stream queue  # 17
    ref index header index rebase blob ref header merge delta merge  # 18
    remote pack index parser window pack ref header window handle tree  # 19
    blob cache token merge index queue token tree parser  # 20
    cache queue tree token parser queue queue object parser  # 21
    buffer cache buffer ref ref pack window branch worker buffer  # 22
    ref parser buffer window stream commit commit object buffer buffer delta  # 23
    object tree buffer header pack stream delta  # 24
    buffer queue handle token socket ref worker worker window  # 25
    window parser cache remote queue commit rebase stream object index token  # 26
    buffer cache token queue ref tree  # 27
    remote rebase object pack  # 28
    rebase buffer pack branch index blob ref header delta branch  # 29
    tree delta worker parser merge stream index commit queue stream  # 30
    object object queue handle remote merge object branch  # 31
    blob object object token index rebase socket parser index remote buffer  # 32
    cache object stream token commit pack queue stream object index window merge  # 33
    parser delta commit header  # 34
    buffer token worker object window branch buffer handle  # 35
    remote pack queue index delta commit  # 36
    object commit parser rebase buffer remote  # 37
    ref pack buffer pack handle  # 38
    index ref worker tree worker stream worker commit  # 39
    worker blob pack cache  # 40
    queue parser stream queue tree commit branch queue merge pack branch object  # 41
    window rebase remote ref pack delta handle token pack object worker stream  # 42
    socket tree ref remote socket worker socket pack handle token queue  # 43
    parser cache parser rebase blob cache blob  # 44
    tree socket token queue worker object  # 45
    cache branch pack blob delta commit index socket handle  # 46
    handle header cache token merge cache tree token remote  # 47
    index stream token header window branch stream  # 48
    cache token branch merge  # 49
    handle index ref blob delta stream object  # 50
    token worker remote window  # 51
    parser branch blob remote socket delta  # 52
    merge commit blob pack worker object pack window window index worker  # 53
    token token rebase queue window index handle delta branch window remote  # 54
    header index commit buffer index merge cache index object  # 55
    remote ref parser handle parser handle branch ref header object  # 56
    commit window token branch remote  # 57
    remote delta parser blob merge rebase  # 58
    queue branch socket branch cache handle ref  # 59
    commit socket blob commit branch  # 60
    remote rebase object header  # 61
    object tree commit worker pack  # 62
    remote rebase window blob branch commit parser object queue  # 63
    object commit ref rebase branch branch handle ref delta delta  # 64
    branch object branch handle object pack socket queue queue  # 65
