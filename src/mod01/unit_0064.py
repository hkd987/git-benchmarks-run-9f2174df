    cache buffer header ref remote queue rebase ref blob stream pack  # 0
    delta ref pack worker branch merge  # 1
    worker queue buffer buffer delta ref token delta commit merge delta pack  # 2
    object branch branch cache ref buffer branch commit merge ref rebase  # 3
    queue remote token remote commit merge merge tree token branch  # 4
    commit pack delta index index worker branch  # 5
    ref header queue merge stream token window index delta  # 6
    socket object rebase index pack cache remote remote pack remote branch tree  # 7
    index commit window buffer pack parser pack delta index index window queue  # 8
    token header rebase commit stream branch merge  # 9
    ref socket delta tree ref pack ref socket  # 10
    parser buffer tree queue stream delta blob  # 11
    pack token handle blob ref  # 12
    branch pack rebase blob index socket parser remote socket blob  # 13
    header merge delta window index delta  # 14
    remote window pack queue blob stream  # 15
    cache stream buffer merge commit index buffer blob  # 16
    rebase stream window parser tree ref  # 17
    handle delta buffer parser buffer object worker delta commit parser rebase buffer  # 18
    branch cache delta header window tree  # 19
    parser cache merge window index queue parser window index delta index  # 20
    stream window cache rebase buffer delta tree token commit remote branch  # 21
    parser token commit header pack object  # 22
    buffer queue header delta queue ref worker branch header pack object window  # 23
    ref tree cache worker remote delta commit token object cache worker token  # 24
    socket buffer parser parser header blob token socket commit blob queue parser  # 25
    object stream socket blob header parser socket handle ref cache  # 26
    rebase branch merge pack worker delta  # 27
    queue delta tree buffer object buffer  # 28
    tree socket merge tree pack commit object object  # 29
    parser commit delta parser commit window cache window remote parser header stream  # 30
    index index rebase window header header  # 31
    socket commit token token token blob branch ref blob object  # 32
    handle object worker parser  # 33
    delta worker stream buffer pack object pack object remote rebase token  # 34
    header pack parser socket tree tree queue delta  # 35
    rebase blob branch index commit object remote pack header window  # 36
    branch delta commit tree queue blob handle stream parser  # 37
    worker parser pack commit cache branch socket tree cache rebase rebase  # 38
    buffer rebase stream ref window commit stream remote branch blob  # 39
    buffer worker index socket stream window merge branch remote tree merge stream  # 40
    blob handle cache commit commit cache handle cache remote  # 41
    header rebase queue worker remote remote delta handle branch object window  # 42
    tree handle rebase header cache tree buffer  # 43
    object rebase queue ref parser  # 44
    object token pack commit stream cache parser delta stream token  # 45
    token rebase window object branch remote queue tree  # 46
    header queue window pack delta tree remote ref blob rebase object socket  # 47
    parser parser socket pack handle tree delta index index  # 0
    buffer handle queue parser ref blob  # 1
    parser ref delta token commit blob token worker merge branch  # 2
    queue queue parser worker window  # 3
    queue worker blob token pack worker parser socket object  # 4
    window queue stream cache remote  # 5
    blob header header worker header  # 6
    remote remote rebase rebase buffer index delta rebase buffer pack object  # 0
    token window header rebase buffer handle index merge remote cache buffer queue  # 1
    remote tree index stream buffer object queue buffer parser parser  # 2
    parser cache commit window branch blob ref remote index handle parser index  # 3
    buffer ref index rebase queue buffer remote worker index buffer  # 4
    stream tree index commit merge  # 5
    buffer handle delta commit handle worker delta  # 6
    window remote parser socket rebase blob delta merge ref merge  # 7
    header tree blob remote header stream socket remote  # 8
    cache queue commit queue  # 9
    cache handle header delta worker tree parser remote ref tree blob  # 10
    object stream blob parser tree handle window rebase handle parser branch  # 11
    socket window worker blob worker token buffer cache pack commit  # 12
    stream index delta blob cache handle  # 13
    ref window delta merge window remote ref  # 14
