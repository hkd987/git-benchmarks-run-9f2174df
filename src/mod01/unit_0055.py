    rebase delta queue header branch branch  # 0
    header worker merge queue header  # 1
    handle buffer delta index  # 2
    handle blob socket tree cache object blob merge buffer  # 3
    cache socket buffer handle pack commit pack remote ref buffer  # 4
    handle header window header queue stream header header pack  # 5
    object parser worker socket  # 6
    blob pack handle branch  # 7
    worker commit socket window delta worker queue ref  # 8
    socket object socket stream cache worker delta tree remote object  # 9
    index buffer handle delta token  # 10
    rebase parser worker stream rebase parser remote  # 11
    branch commit delta tree rebase  # 12
    token buffer pack remote stream tree parser blob token buffer  # 13
    remote ref tree header index object ref  # 14
    window pack handle delta  # 15
    tree parser token stream cache ref  # 16
    ref pack commit parser ref commit socket ref  # 17
    index handle socket remote cache buffer merge remote  # 18
    index blob branch pack handle merge  # 19
    commit branch merge queue object pack remote buffer worker parser  # 20
    queue header object blob  # 21
    branch parser handle commit object parser rebase cache  # 22
    tree remote ref worker header merge tree object  # 23
    ref index delta header  # 24
    buffer object cache object  # 25
    pack socket ref buffer cache pack index window  # 26
    merge delta branch index cache branch tree stream header handle stream  # 27
    rebase header worker object index header socket socket branch buffer window commit  # 28
    worker object commit ref handle remote stream index header  # 29
    pack blob window handle commit handle header socket header remote  # 30
    queue object stream pack window merge index stream pack queue rebase remote  # 31
    token handle handle buffer worker index worker branch token header index  # 32
    stream queue header worker token worker pack cache token branch remote  # 33
    ref handle index window remote  # 34
    worker branch stream commit pack rebase remote merge tree object  # 35
    object remote remote cache object object worker merge  # 36
    worker worker parser buffer rebase  # 37
    commit buffer ref commit remote stream delta tree object rebase blob  # 38
    worker handle window stream tree tree queue queue index  # 39
    delta stream cache queue buffer header token  # 40
    handle cache queue remote ref header branch socket token socket stream  # 41
    delta stream delta stream ref socket  # 42
    token commit ref merge  # 43
    merge commit header cache parser buffer handle  # 44
    rebase tree buffer commit handle token worker cache commit  # 45
    queue remote buffer merge handle queue header handle  # 46
    socket cache stream ref  # 47
    ref branch blob window  # 48
    tree delta handle cache handle merge stream tree  # 49
    object pack queue buffer queue  # 50
    blob object remote header  # 51
    socket queue socket worker pack  # 52
    handle parser stream commit branch queue branch branch socket  # 53
    blob blob ref index blob blob commit blob blob object merge branch  # 54
    stream blob stream worker window rebase index window handle  # 55
    handle ref parser object queue cache token parser  # 56
    token cache socket parser  # 57
    token remote merge commit parser object pack queue ref remote pack  # 58
    rebase window queue index merge stream handle socket ref header  # 59
    token blob rebase token branch queue parser socket socket socket blob  # 60
    index stream ref tree header handle buffer socket object stream commit  # 61
    stream pack blob header pack remote  # 62
    buffer cache delta pack window  # 63
    queue tree handle socket branch branch parser object index  # 64
    tree commit merge header blob buffer blob tree  # 65
    window branch object header branch delta handle delta index parser worker  # 66
    index ref index object delta window window  # 67
    window header worker rebase stream cache blob buffer pack  # 68
    socket buffer stream remote  # 69
    ref tree index branch branch delta stream merge remote token  # 70
    queue stream pack stream  # 71
    merge header commit pack object ref header socket merge socket merge  # 72
    rebase object handle header ref handle header pack  # 73
    buffer parser object remote  # 74
    tree rebase delta branch  # 75
    object stream index blob parser queue commit parser  # 76
    buffer header blob cache object index token pack socket buffer ref  # 0
    index worker delta index  # 1
    object handle branch delta buffer commit socket merge handle remote tree merge  # 2
    ref token index pack cache handle branch  # 3
    header header delta buffer rebase branch rebase blob commit cache token  # 4
    parser object remote cache  # 5
    parser cache handle rebase merge blob remote blob  # 6
    token ref ref merge handle socket token remote blob queue branch  # 7
    cache object header rebase commit header object pack socket  # 8
    cache token blob buffer header  # 9
    handle queue worker pack object index tree rebase  # 10
    pack pack delta parser  # 11
