    token socket blob merge object tree commit remote header pack tree delta  # 0
    buffer cache buffer object handle remote  # 1
    commit blob worker queue blob rebase cache parser  # 2
    window handle tree queue token handle blob buffer  # 3
    remote token index tree pack delta buffer  # 4
    blob rebase object tree branch token buffer branch socket stream commit cache  # 5
    parser pack commit buffer remote handle cache buffer tree window  # 6
    cache branch queue socket handle  # 7
    rebase tree object stream blob commit merge ref rebase  # 8
    stream token token handle index object remote buffer window parser rebase  # 9
    handle buffer parser handle index branch commit  # 10
    token buffer tree stream blob  # 11
    token commit window delta  # 12
    worker pack rebase worker queue socket header delta  # 13
    object socket merge stream token socket worker remote  # 14
    queue pack pack stream parser buffer  # 15
    remote ref commit delta header handle  # 16
    pack rebase pack stream  # 17
    ref object object worker cache  # 18
    window index tree socket ref merge  # 19
    header rebase cache tree index pack object parser socket  # 20
    branch header remote socket commit socket rebase object socket commit  # 21
    stream index queue parser window cache object  # 22
    blob index delta merge object queue  # 23
    blob remote buffer tree ref merge socket merge pack tree pack remote  # 24
    blob tree token object worker  # 25
    cache queue merge blob  # 26
    stream branch tree index buffer  # 27
    header branch parser queue  # 28
    socket blob cache window handle commit delta ref commit worker stream  # 29
    object stream commit pack buffer worker  # 30
    parser window worker buffer index stream parser queue parser merge tree stream  # 31
    ref cache cache commit commit parser handle stream merge  # 32
    socket blob token token object remote header  # 33
    token tree rebase object rebase parser branch ref delta index worker cache  # 34
    window index stream socket remote commit buffer pack pack merge branch handle  # 35
    token rebase delta remote object queue tree cache  # 36
    buffer parser buffer buffer blob cache stream object merge rebase  # 37
    cache blob pack token worker  # 38
    commit socket blob cache  # 39
    window merge ref delta blob token object merge  # 40
    tree index queue tree buffer delta parser pack stream  # 41
    buffer handle cache window tree index object handle stream  # 42
    blob tree rebase stream buffer  # 43
    object queue pack header window buffer stream rebase cache window ref commit  # 44
    delta parser token rebase tree  # 45
    window merge remote rebase commit merge token branch  # 46
    token header parser pack header object window  # 47
    ref stream commit handle buffer queue delta  # 48
    delta handle token commit queue buffer handle buffer tree  # 49
    branch object branch branch branch buffer  # 50
    cache remote stream object remote parser ref remote index  # 51
    token window header queue blob parser queue  # 52
    cache blob stream window remote merge index blob rebase handle  # 53
    index window queue buffer socket queue  # 54
    index cache remote queue merge delta  # 55
    index buffer ref handle tree ref queue buffer blob  # 56
    buffer parser branch object cache  # 57
    object buffer worker pack window  # 58
    window header socket rebase remote buffer window socket socket handle parser  # 59
    delta token token parser index  # 60
    tree parser stream blob queue blob tree index  # 61
    index buffer tree ref merge pack stream window blob commit merge  # 62
    buffer header delta commit header  # 63
    delta cache queue cache parser socket cache worker stream parser header  # 64
    ref index rebase queue tree branch  # 65
    ref index handle header commit socket object index ref delta tree  # 66
    delta header queue delta remote index window  # 67
    handle blob parser branch header branch tree window merge blob  # 68
    delta cache blob branch delta socket index pack token worker remote  # 69
    blob blob queue header  # 70
    buffer tree commit header stream window handle commit parser  # 71
    ref handle header commit remote rebase rebase buffer  # 72
    window token blob rebase window buffer handle blob blob tree merge token  # 73
    tree delta window cache  # 74
    delta ref merge tree ref blob rebase pack delta worker ref  # 75
    stream handle buffer ref socket commit branch ref rebase worker cache  # 76
    worker ref rebase merge blob parser  # 77
    tree tree ref ref socket handle buffer cache handle  # 78
    queue worker object buffer ref token header pack  # 79
    remote branch worker worker stream merge queue pack handle branch token merge  # 80
    handle window object header header pack ref  # 81
    window cache delta handle rebase handle remote index commit socket index commit  # 82
