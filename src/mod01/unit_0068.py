    stream ref cache header tree stream buffer  # 0
    index handle pack object rebase pack window branch worker queue handle  # 1
    worker merge rebase worker parser  # 2
    pack window queue object delta cache  # 3
    header ref remote blob worker stream  # 4
    queue object index stream  # 5
    buffer token handle remote ref object parser socket pack rebase branch parser  # 6
    tree object index delta  # 7
    stream token object parser window queue remote stream index remote commit  # 8
    pack delta blob buffer handle branch window worker index worker token  # 9
    socket object index window cache rebase tree  # 10
    branch commit ref socket token stream tree cache header pack commit  # 11
    stream token window delta parser worker token queue socket  # 12
    rebase token handle branch socket buffer buffer merge buffer  # 13
    commit merge buffer index stream stream window  # 14
    queue socket socket buffer delta header  # 15
    tree index tree header ref window branch merge commit window tree  # 16
    index merge socket rebase buffer blob remote index ref  # 17
    tree branch branch parser socket commit commit parser pack  # 18
    pack stream queue window ref rebase object token ref window parser delta  # 19
    remote header parser worker window delta  # 20
    commit tree handle header socket parser blob  # 21
    pack delta buffer token  # 22
    queue parser rebase object remote remote socket index handle handle cache  # 23
    remote worker queue parser object object commit remote  # 24
    handle rebase pack remote tree rebase  # 25
    parser token remote merge window merge remote buffer blob tree branch  # 26
    tree rebase rebase pack pack token queue branch delta commit  # 27
    buffer merge buffer commit branch ref delta cache worker socket ref  # 28
    index window queue index  # 29
    worker parser delta parser  # 30
    merge parser header parser merge parser  # 31
    handle index branch delta header merge ref merge commit socket window remote  # 32
    parser merge ref pack handle index queue ref  # 33
    blob handle token blob stream header merge branch tree  # 34
    buffer socket pack commit window worker cache stream rebase worker  # 35
    branch parser worker index delta header window branch parser worker index  # 36
    branch tree header socket delta rebase window window handle handle index  # 37
    object buffer blob pack commit tree delta merge socket branch rebase cache  # 38
    tree window ref header handle blob branch socket  # 39
    pack worker queue remote rebase branch  # 40
    handle token blob ref commit window branch commit  # 41
    ref merge queue commit pack  # 42
    blob branch delta stream queue stream remote blob cache  # 43
    parser index header header stream rebase  # 44
    object token socket parser tree delta handle  # 45
    blob buffer stream parser blob token cache handle socket  # 46
    cache stream blob delta window  # 47
    merge pack index merge commit tree ref blob object index remote pack  # 48
    blob pack cache tree worker handle worker stream  # 49
    commit buffer handle token header remote object  # 50
    queue window blob stream delta cache handle pack tree parser branch  # 51
    worker handle commit queue worker  # 52
    cache window merge token index delta rebase object  # 53
    commit stream blob index stream branch  # 54
    object handle object token parser  # 55
