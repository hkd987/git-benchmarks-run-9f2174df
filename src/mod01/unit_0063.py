    parser socket window parser remote ref handle merge queue  # 0
    commit remote ref branch pack delta index index commit  # 1
    rebase branch tree ref token stream commit index object header  # 2
    delta parser queue index  # 3
    pack commit socket index buffer queue worker merge handle worker tree worker  # 4
    ref rebase tree remote worker commit tree tree remote  # 5
    delta object commit stream rebase cache  # 6
    blob parser commit socket token blob rebase stream object  # 7
    tree cache buffer window parser object parser window token  # 8
    ref ref handle delta branch index object rebase remote  # 9
    parser pack token object blob stream index cache pack pack parser index  # 10
    socket window ref ref buffer parser merge branch merge  # 11
    buffer blob socket merge header worker object header commit delta pack branch  # 12
    queue cache index merge commit branch  # 13
    ref branch socket window socket queue buffer object header branch worker  # 14
    remote buffer branch pack window parser handle  # 15
    delta parser worker object rebase index  # 16
    index handle branch object header merge tree token  # 17
    object queue socket header branch  # 18
    stream worker commit queue tree commit  # 19
    ref window parser commit header queue pack rebase socket header  # 20
    merge socket parser parser remote window handle  # 21
    commit queue blob cache branch branch  # 22
    cache tree delta cache ref window stream header parser queue  # 23
    cache merge remote socket  # 24
    window commit cache cache object merge queue remote tree  # 25
    rebase token merge window commit socket  # 26
    pack ref rebase window  # 27
    blob parser socket branch tree socket handle tree rebase ref queue index  # 28
    blob blob branch header commit parser commit cache merge  # 29
    window parser socket delta tree parser stream header buffer parser branch  # 30
    index ref queue queue stream branch worker delta object  # 31
    queue tree cache object cache handle cache tree queue remote ref object  # 32
    object rebase rebase window handle commit socket header branch socket index  # 33
    blob rebase pack object stream handle header worker cache merge  # 34
    merge handle socket remote  # 35
    handle pack merge commit commit handle object buffer queue  # 36
    header socket tree tree  # 37
    commit parser object object pack tree stream branch remote  # 38
    object parser branch index rebase queue buffer cache  # 39
    commit parser header commit tree rebase delta merge ref rebase parser buffer  # 40
    worker remote delta socket delta commit  # 41
    stream branch rebase window parser blob rebase object stream worker  # 42
    queue index stream socket parser tree queue  # 43
    token socket socket tree  # 44
    token remote delta token rebase tree  # 45
    remote header blob delta  # 46
    delta token parser socket window rebase pack commit object  # 47
    object rebase token stream window token delta index  # 48
    ref handle parser handle branch ref index commit ref pack  # 49
    cache pack handle cache pack merge socket header parser window  # 50
    token handle cache rebase blob stream parser  # 51
    remote remote index branch object index ref tree  # 52
    token buffer merge cache worker queue  # 53
    worker parser queue pack  # 54
    parser ref commit branch cache header window cache commit delta rebase worker  # 55
    tree tree handle blob header index ref remote delta stream token pack  # 56
    window queue tree merge stream blob  # 57
    token delta rebase handle delta branch worker  # 58
    stream pack socket delta buffer stream ref blob tree cache header  # 59
    parser tree merge merge object worker tree header rebase token rebase  # 60
    token handle index token queue rebase merge remote handle  # 61
    buffer handle stream cache socket token merge blob  # 62
    tree object window object queue object worker window handle cache  # 63
    window remote buffer delta  # 64
    buffer ref pack object window merge token  # 65
    token pack queue delta object socket index buffer index window commit  # 66
    branch queue header handle delta parser window branch pack header commit socket  # 67
    branch socket tree token queue  # 68
    merge socket remote merge window tree object  # 0
    tree buffer branch buffer token  # 1
    window rebase stream index delta parser header parser stream  # 2
    merge window handle stream handle remote worker token remote window  # 3
    blob index object worker tree token handle cache rebase parser branch  # 4
    queue merge stream buffer  # 5
    tree queue window worker ref pack merge window index header queue tree  # 6
    worker merge socket tree pack socket object buffer buffer queue merge cache  # 7
    blob stream object branch handle commit merge token stream  # 8
    merge header socket buffer remote branch header pack  # 9
    object header remote worker  # 10
    rebase token delta ref  # 11
    token socket worker stream socket buffer tree handle object  # 12
    buffer window delta cache merge  # 13
