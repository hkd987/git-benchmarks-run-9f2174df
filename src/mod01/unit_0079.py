    merge parser pack buffer  # 0
    handle merge parser window delta parser socket buffer  # 1
    tree cache commit object header header header  # 2
    pack rebase cache ref ref index stream  # 3
    merge pack delta queue merge header  # 4
    delta branch index token socket worker ref  # 5
    header socket blob rebase rebase ref parser socket blob  # 6
    stream blob buffer blob object rebase pack worker  # 7
    merge worker blob window merge window blob merge index window  # 8
    header merge remote rebase worker  # 9
    token merge ref socket handle cache worker object  # 10
    branch queue window remote object header buffer merge socket worker cache queue  # 11
    index branch tree tree pack header blob  # 12
    buffer worker commit ref merge blob merge  # 13
    queue buffer header handle branch pack branch cache  # 14
    window handle rebase merge handle rebase commit cache  # 15
    remote delta socket index header object rebase index  # 16
    window blob rebase buffer buffer delta pack tree  # 17
    remote blob rebase cache pack  # 18
    handle cache buffer remote buffer remote buffer blob  # 19
    blob merge window delta pack commit queue window socket  # 20
    buffer index header rebase blob queue handle index  # 21
    tree object delta remote blob handle token merge branch merge  # 22
    remote delta token blob remote delta token handle index ref  # 23
    window ref commit cache object remote object window cache parser branch merge  # 24
    ref tree worker object socket object commit rebase blob blob cache handle  # 25
    merge branch commit rebase handle ref  # 26
    stream ref socket delta token handle blob  # 27
    branch window worker commit ref object branch  # 28
    socket cache blob object commit remote buffer worker ref buffer tree  # 29
    cache tree cache header index commit handle delta branch cache  # 30
    handle ref branch index queue window handle worker token index  # 31
    queue header pack cache rebase token remote queue parser  # 32
    rebase cache socket branch ref pack merge token  # 33
    rebase pack blob merge header window  # 34
    cache branch window pack buffer index blob  # 35
    delta remote pack pack branch commit index header buffer parser ref parser  # 36
    socket stream branch blob worker  # 37
    ref delta buffer object object buffer socket tree blob  # 38
    index index pack commit worker parser pack pack socket commit handle window  # 39
    object buffer tree handle queue socket  # 40
    pack branch commit cache object  # 41
    header worker blob queue header queue token branch header delta handle queue  # 42
    socket window object window ref window window queue queue  # 43
