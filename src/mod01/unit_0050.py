    stream token window delta object token worker merge token cache branch tree  # 0
    ref ref window buffer buffer queue tree object blob blob  # 1
    stream delta commit delta worker pack queue pack  # 2
    commit commit delta object ref stream buffer blob socket remote socket  # 3
    handle stream token stream parser queue pack blob commit pack  # 4
    commit parser handle handle remote delta index socket merge object  # 5
    worker branch worker commit parser worker worker buffer  # 6
    commit commit header socket handle buffer merge cache header delta ref  # 7
    remote ref delta header  # 8
    pack pack index delta branch blob  # 9
    rebase merge object branch pack index handle blob branch pack pack object  # 10
    pack handle object queue worker branch handle queue tree branch ref  # 11
    parser buffer buffer rebase merge header  # 12
    delta cache parser queue delta handle branch  # 13
    queue delta ref pack handle pack socket rebase  # 14
    blob window remote remote remote  # 15
    parser ref rebase window worker delta header index token object worker  # 16
    queue index ref object socket socket  # 17
    queue socket rebase pack token handle  # 18
    remote parser index delta  # 19
    commit blob delta stream window rebase rebase queue delta  # 20
    handle pack token remote handle merge blob buffer commit socket window  # 21
    parser index stream pack header object buffer  # 22
    delta tree rebase object rebase queue handle object  # 23
    header delta token token delta ref header pack  # 24
    delta socket worker token parser delta header remote stream  # 25
    object remote index tree branch cache  # 26
    parser buffer index queue  # 27
    cache header object branch commit blob worker commit window merge rebase  # 28
    stream parser merge ref rebase worker commit  # 29
    rebase stream branch handle socket queue window queue  # 30
    parser worker window queue ref  # 31
    worker delta pack token handle header  # 32
    remote ref branch branch header branch parser buffer rebase parser window stream  # 33
    delta pack socket token parser worker merge  # 34
    parser handle queue merge socket index branch ref  # 35
    header merge commit commit cache token blob  # 36
    branch parser branch blob cache ref socket parser merge delta worker  # 37
    object window queue index queue  # 38
    ref branch ref rebase socket merge blob header  # 39
    commit header tree parser token queue merge ref object tree blob  # 40
