    worker object index cache worker  # 0
    token window window parser handle header  # 1
    index window cache rebase worker rebase cache delta cache  # 2
    cache merge worker queue tree handle  # 3
    queue worker stream queue blob parser buffer delta window branch  # 4
    object blob tree queue cache handle stream commit ref  # 5
    stream ref pack tree handle rebase remote tree merge pack  # 6
    socket queue delta header token pack index ref commit window  # 7
    rebase branch ref stream pack object merge commit buffer window ref  # 8
    branch buffer delta header ref cache cache blob  # 9
    rebase remote rebase pack object header remote window token  # 10
    handle socket pack socket branch queue  # 11
    commit token handle delta commit parser index queue delta blob parser token  # 12
    rebase ref worker stream rebase pack header  # 13
    handle buffer pack pack window header remote  # 14
    delta buffer merge merge object  # 15
    header object window buffer window merge parser delta buffer worker object rebase  # 16
    token index commit socket buffer window remote blob index  # 17
    queue branch pack worker delta rebase object blob window commit cache  # 18
    ref handle cache parser commit header socket blob commit delta  # 19
    blob queue rebase branch rebase cache  # 20
    worker header object buffer branch remote worker tree index rebase object  # 21
    socket cache index stream queue parser worker window commit token merge merge  # 22
    tree header tree remote token parser queue delta  # 23
    worker object pack index delta branch handle blob handle branch  # 24
    cache delta ref blob rebase handle tree  # 25
    object token buffer remote branch tree merge header parser handle handle handle  # 26
    tree stream window index token delta token  # 27
    rebase delta branch window object index  # 28
    handle branch pack object delta buffer window queue queue ref remote  # 29
    cache window parser commit cache worker object token worker cache  # 30
    cache stream handle handle blob handle stream index socket tree branch  # 31
    index commit window header remote token header remote cache  # 32
    cache ref ref stream branch rebase pack  # 33
    handle tree worker ref ref parser  # 34
    window object queue stream  # 35
    object window rebase header rebase blob rebase  # 36
    branch parser pack branch object socket parser commit commit  # 37
    rebase merge commit header stream cache  # 38
    stream index ref rebase stream stream queue socket header cache queue  # 39
    worker window index cache  # 40
    merge parser token socket index token token branch stream parser window stream  # 41
    parser stream cache token window stream tree object tree header object  # 42
    handle index parser socket pack remote blob pack handle remote blob handle  # 43
    cache socket header ref index delta object queue handle token pack commit  # 44
    header branch tree merge queue rebase object index index socket token  # 45
    index stream cache cache ref buffer token stream header rebase  # 46
    stream blob blob object cache ref parser branch stream ref  # 47
    commit blob rebase merge worker  # 48
    pack ref handle blob ref blob  # 49
    window pack delta tree token rebase parser buffer rebase window window cache  # 50
    object rebase commit index branch token parser cache socket  # 51
    stream stream token branch cache remote cache remote queue commit  # 52
    socket worker handle commit parser handle commit ref merge socket  # 53
    worker header window handle index object handle remote buffer pack  # 54
    merge commit cache window merge  # 55
    delta blob pack tree token buffer commit delta index branch token rebase  # 56
    handle stream index worker cache worker socket object  # 57
    cache worker handle socket header queue parser  # 58
    remote cache delta cache commit tree socket remote remote delta index  # 59
    branch handle header queue token remote token rebase cache merge tree  # 60
    pack commit cache commit cache  # 61
    index branch blob token token  # 62
    delta worker token handle merge  # 63
    rebase buffer pack window rebase header header rebase pack token  # 64
    header worker branch handle merge  # 65
    object ref cache parser rebase commit pack handle index queue queue worker  # 66
    tree remote header window worker object token parser rebase cache blob  # 67
    cache window commit remote object token merge socket parser parser cache merge  # 68
