    worker blob remote branch blob delta socket header commit window  # 0
    buffer pack branch rebase index  # 1
    blob window index branch branch  # 2
    window index cache branch queue buffer stream branch delta  # 3
    queue index handle token worker commit window  # 4
    branch cache index remote token pack socket  # 5
    buffer index object stream handle buffer  # 6
    queue ref worker index window object  # 7
    token rebase rebase object worker blob object commit  # 8
    remote parser commit branch commit delta  # 9
    rebase index queue cache buffer socket pack index buffer window rebase  # 10
    header cache tree worker branch queue worker token  # 11
    parser rebase header handle commit handle branch  # 12
    header branch buffer remote rebase handle queue merge  # 13
    queue index remote queue header branch  # 14
    remote index handle window blob parser remote rebase delta token delta  # 15
    delta blob object tree queue cache handle  # 16
    socket queue token ref window buffer header stream blob header  # 17
    pack parser branch delta token blob blob pack token socket  # 18
    socket remote worker socket remote header worker socket blob worker header  # 19
    window remote socket blob cache index header object token delta  # 20
    remote stream delta ref rebase rebase header socket index queue ref branch  # 21
    queue stream blob pack socket queue cache token window  # 22
    handle handle branch commit window tree  # 23
    handle queue parser tree remote buffer commit header pack handle delta  # 24
    delta window window index tree  # 25
    commit pack token queue worker window merge merge cache  # 26
    cache pack index tree ref branch handle  # 27
    branch buffer rebase merge ref handle index socket socket socket worker parser  # 28
    stream cache parser rebase buffer stream socket  # 29
    index buffer handle branch merge buffer delta  # 30
    queue branch blob buffer commit object parser parser commit rebase index  # 31
