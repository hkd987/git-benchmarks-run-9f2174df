    merge handle tree stream blob socket pack blob blob pack  # 0
    pack parser delta object remote branch object commit merge buffer  # 1
    delta delta queue stream queue window stream  # 2
    handle handle queue socket stream buffer socket object blob queue  # 3
    queue stream socket worker buffer window rebase blob pack  # 4
    remote index delta worker ref index  # 5
    blob commit buffer queue  # 6
    tree socket blob parser blob index window delta cache  # 7
    rebase commit merge pack branch object token stream token rebase handle  # 8
    ref handle stream worker branch parser socket parser parser merge delta socket  # 9
    index pack window pack remote merge queue stream cache merge worker worker  # 10
    token pack merge handle merge socket object queue parser header token  # 11
    remote index merge branch token parser rebase tree window header cache blob  # 12
    delta socket token branch tree  # 13
    pack worker parser pack ref stream worker remote  # 14
    worker worker pack queue stream window remote socket  # 15
    stream pack tree tree rebase tree ref parser stream buffer  # 16
    object blob buffer handle  # 17
    pack queue remote buffer cache buffer worker queue handle commit merge  # 18
    parser blob stream object remote rebase ref ref remote  # 19
    object ref parser branch stream delta branch  # 20
    cache delta blob ref delta header cache commit  # 21
    branch buffer cache branch tree buffer window worker tree header pack  # 22
    token commit delta parser window socket queue merge rebase  # 23
    token token object buffer rebase ref pack ref stream header worker  # 24
    handle header token delta  # 25
    token merge merge parser pack ref handle ref worker tree buffer tree  # 26
    commit index blob parser remote worker pack header pack branch branch commit  # 27
    socket parser token parser blob object delta queue  # 28
    stream queue tree tree queue tree commit token  # 29
    rebase tree stream commit merge index  # 30
    merge buffer cache rebase parser merge pack socket index remote  # 31
    buffer commit branch rebase socket delta remote rebase parser worker ref ref  # 32
    token socket window delta remote window branch parser  # 33
    delta rebase window index queue cache branch remote buffer  # 34
    ref parser index object delta worker  # 35
    ref tree tree remote  # 36
    token remote object index socket delta  # 37
    buffer handle queue pack delta header commit token  # 38
    token parser remote index parser remote  # 39
    handle merge index merge buffer window merge window index branch  # 40
    rebase tree handle commit ref pack buffer ref  # 41
    object tree ref blob branch socket delta  # 42
    token tree header cache object index ref tree  # 43
    merge delta blob window  # 44
    buffer branch token socket buffer handle cache rebase pack buffer  # 45
    blob commit socket rebase merge header merge handle  # 46
    commit object parser buffer pack object  # 47
    delta handle merge socket socket blob commit queue branch blob branch  # 48
    merge handle token header tree  # 49
    merge worker window delta queue remote blob handle index  # 50
    parser buffer window branch header queue commit worker  # 51
    token ref cache cache  # 52
    branch ref branch rebase branch parser token  # 53
    stream window branch tree tree stream window blob header  # 54
    index cache token blob queue socket rebase window merge  # 55
    socket blob window socket cache merge remote token window  # 56
    merge stream window cache stream  # 57
    cache commit delta rebase object buffer worker handle socket stream  # 58
    buffer stream index delta  # 59
    object worker handle merge cache parser  # 60
    socket commit cache ref remote tree merge commit handle  # 61
    window handle remote stream  # 62
    stream cache buffer delta worker ref  # 63
    merge worker rebase remote delta index  # 64
    remote merge window buffer ref  # 65
    commit ref handle parser socket ref  # 66
    handle ref tree cache queue tree pack branch  # 67
    worker token object parser buffer cache worker socket remote remote merge rebase  # 68
    token branch rebase handle object handle stream token  # 69
    ref pack commit stream blob tree index token rebase worker  # 70
    socket worker stream object ref parser tree tree cache merge delta index  # 71
    pack blob buffer worker socket cache stream object pack stream tree  # 72
