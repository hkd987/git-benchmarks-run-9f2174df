    delta branch branch pack parser  # 0
    delta blob socket rebase pack branch token handle blob  # 1
    window object queue token  # 2
    commit remote object header index rebase branch pack  # 3
    token window stream branch index merge commit parser branch merge  # 4
    commit rebase pack merge blob buffer ref header rebase buffer  # 5
    rebase commit rebase handle delta index ref queue buffer  # 6
    token window queue handle token window tree remote object  # 7
    window remote cache delta token window  # 8
    queue object worker cache queue handle worker queue  # 9
    handle handle window window token cache  # 10
    worker tree delta pack queue cache token remote  # 11
    object tree stream buffer cache commit tree window ref branch stream  # 12
    header worker worker branch branch handle header socket ref ref ref  # 13
    window buffer merge merge tree blob merge header commit commit cache delta  # 14
    branch remote queue tree commit buffer header header  # 15
    window socket tree ref  # 16
    blob stream handle index cache ref  # 17
    blob tree blob stream remote handle header merge window index  # 18
    token branch pack pack branch commit buffer ref merge blob window object  # 19
    header queue merge tree rebase commit branch buffer remote  # 20
    merge ref socket pack buffer parser merge window window pack window parser  # 21
    rebase queue pack cache header commit header delta cache cache socket parser  # 22
    delta buffer parser index buffer socket delta  # 23
    stream buffer tree cache queue stream window  # 24
    index merge token token socket stream pack handle  # 25
    buffer worker parser merge window handle index header remote cache header remote  # 26
    handle commit index rebase handle handle stream  # 27
    ref window ref tree parser ref merge remote stream worker  # 28
    blob object cache ref delta header ref branch branch remote  # 29
    ref tree pack worker  # 30
    buffer parser delta cache index cache object rebase index  # 31
    blob object stream pack remote blob token token  # 32
    socket rebase object index socket ref  # 33
    branch remote queue socket blob handle object branch buffer token  # 34
    branch rebase object rebase pack object index branch window parser  # 35
    remote window window blob tree stream stream  # 36
    token branch blob pack buffer header commit cache rebase object window  # 37
    blob cache parser tree merge handle window ref header parser object commit  # 38
    handle merge queue pack tree tree  # 39
    pack object ref parser  # 40
    worker commit remote object socket branch socket window buffer rebase  # 41
    index cache pack index  # 42
    commit stream object object queue  # 43
    delta branch merge header socket rebase commit index rebase pack parser  # 44
    worker delta worker buffer remote  # 45
    blob header buffer remote blob pack  # 46
    merge worker cache handle delta ref branch cache  # 47
    object socket window socket ref worker cache  # 48
    ref merge buffer buffer blob socket stream index commit worker tree  # 49
    remote window parser handle commit delta parser branch worker cache rebase  # 50
    tree socket rebase object index token cache  # 51
    socket remote blob cache  # 52
    ref token worker rebase queue index  # 53
    handle pack cache tree  # 54
    cache token worker buffer handle parser rebase socket  # 55
    window merge socket delta parser queue tree socket branch buffer stream  # 56
    merge cache token socket queue object branch window object delta delta  # 57
    stream parser worker object blob buffer blob index socket header token  # 58
    blob header branch cache commit remote remote index queue  # 59
    header rebase pack header object delta  # 0
    header worker remote blob handle window handle  # 1
    handle header handle buffer worker header stream handle  # 2
    blob window worker handle queue delta tree socket index buffer tree  # 3
    handle token token header token delta pack tree ref ref  # 4
    window object cache branch blob window remote queue cache  # 0
    header delta buffer commit  # 1
    queue tree index queue commit queue rebase cache ref  # 2
    worker blob pack rebase buffer rebase tree  # 3
    socket stream delta delta worker merge  # 4
    token buffer queue worker token merge rebase tree pack stream delta  # 5
    rebase index parser branch token header  # 6
    queue stream buffer handle worker queue socket parser  # 7
    index ref stream token pack pack ref  # 8
    header parser ref remote ref object pack commit blob object pack  # 9
    handle socket index queue tree blob token worker  # 10
    index pack merge commit header index  # 11
    stream remote tree index socket token ref queue socket  # 12
