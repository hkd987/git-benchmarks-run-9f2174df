    header handle object object blob index  # 0
    worker header stream header ref index delta merge handle index header object  # 1
    cache worker ref worker buffer ref cache cache pack ref  # 2
    commit pack rebase rebase window parser  # 3
    worker rebase socket token worker header socket  # 4
    object header remote cache merge stream  # 5
    blob object socket stream stream parser tree ref  # 6
    ref branch buffer ref ref header branch object commit  # 7
    worker ref token cache queue header tree  # 8
    parser stream ref stream ref window socket  # 9
    tree merge header worker queue delta window token branch ref worker window  # 10
    worker rebase stream remote cache  # 11
    branch branch merge stream remote remote commit rebase  # 12
    header queue merge window commit window window  # 13
    delta remote branch ref branch commit header  # 14
    token object object pack merge ref cache merge rebase  # 15
    window index index header queue branch header  # 16
    index queue blob handle window  # 17
    delta token remote handle queue stream pack worker buffer  # 18
    index delta handle blob blob blob ref  # 19
    handle queue buffer delta tree branch rebase object  # 20
    pack pack stream buffer merge  # 21
    parser window delta buffer parser handle handle  # 22
    handle object ref queue  # 23
    stream ref ref tree buffer commit  # 24
    handle ref blob blob tree ref rebase stream window worker window pack  # 25
    worker commit commit header  # 26
    commit token pack pack blob  # 27
    window ref commit parser remote buffer token delta merge merge parser  # 28
    socket commit parser window branch worker commit header queue tree worker queue  # 29
    commit ref stream commit window object  # 30
    remote stream commit rebase blob  # 31
    queue buffer object token object header socket buffer rebase delta object  # 32
    buffer socket header commit object queue  # 33
    branch token delta token merge remote window ref ref index delta worker  # 34
    worker worker worker socket object window delta ref  # 35
    commit stream branch commit window handle delta blob ref cache pack  # 36
    buffer merge token header merge pack queue header rebase pack token  # 37
