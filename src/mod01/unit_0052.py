    handle branch socket object branch token stream socket delta buffer  # 0
    buffer queue token token worker token window token ref handle socket  # 1
    merge branch merge parser blob blob handle stream parser  # 2
    commit index object socket worker stream queue buffer index remote  # 3
    window queue stream pack object commit token index merge merge pack  # 4
    header socket socket buffer stream  # 5
    socket delta window index remote parser blob  # 6
    branch window parser commit socket header buffer header ref worker  # 7
    socket token stream merge branch rebase socket delta queue  # 8
    handle commit tree cache  # 9
    commit delta parser tree rebase merge buffer  # 10
    ref blob worker socket rebase blob queue delta token  # 11
    token token object object stream token blob rebase  # 12
    merge socket token object stream branch  # 13
    header pack blob tree socket worker index buffer index socket branch branch  # 14
    commit header branch token  # 15
    handle cache parser rebase token buffer parser worker  # 16
    header token branch object  # 17
    worker parser index tree worker ref  # 18
    stream delta rebase header ref token index queue merge parser socket socket  # 19
    tree socket pack pack rebase object blob parser remote handle  # 20
    socket blob header blob index rebase stream window  # 21
    buffer window remote object branch tree index worker commit header  # 22
    rebase header parser blob branch queue ref  # 23
    merge window stream ref ref  # 24
    index parser token rebase blob blob  # 25
    token parser window stream tree parser merge  # 26
    queue remote commit cache tree queue worker ref socket delta  # 27
    socket commit branch tree delta branch remote remote blob ref  # 28
    stream index blob delta delta object buffer cache commit pack ref remote  # 29
    worker merge worker blob remote remote stream handle window queue blob  # 30
    delta token ref object worker index rebase stream delta branch pack pack  # 31
    remote token remote commit buffer delta index commit ref token branch  # 32
    handle remote window object ref delta tree index  # 33
    worker cache socket branch token buffer tree  # 34
    blob blob tree ref cache rebase  # 35
    branch token ref remote  # 36
    merge header commit pack ref tree socket cache handle blob remote tree  # 37
