    index cache parser ref ref  # 0
    branch commit stream ref ref branch  # 1
    ref merge socket token index  # 2
    parser cache buffer window handle cache  # 3
    object parser remote queue  # 4
    token branch buffer queue stream pack  # 5
    ref object merge object socket pack header rebase blob worker  # 6
    commit delta handle object remote pack parser header remote commit delta  # 7
    buffer blob cache stream socket header object parser  # 8
    commit cache tree buffer  # 9
    token blob header socket header window  # 10
    tree remote blob queue parser socket queue index object socket  # 11
    remote object index object  # 12
    object stream object pack branch  # 13
    ref pack ref socket cache commit commit  # 14
    pack object branch stream merge stream rebase commit queue  # 15
    tree socket commit cache branch queue stream header socket parser remote  # 16
    token pack window handle object header object index index worker blob commit  # 17
    blob remote header merge handle  # 18
    buffer object queue object stream index rebase handle index window header blob  # 19
    pack blob object merge pack header  # 20
    remote branch stream socket index object index branch tree cache  # 21
    header pack pack parser queue pack header branch token  # 22
    delta tree index remote commit queue object stream header buffer remote commit  # 23
    rebase worker cache worker cache ref blob  # 24
    buffer commit blob queue  # 25
    token queue index cache stream rebase  # 26
    parser buffer queue worker  # 27
    handle header merge cache  # 28
    handle cache queue pack parser token handle commit branch queue token  # 29
    index ref token ref header header delta  # 30
    branch worker merge header merge buffer pack tree  # 31
    handle parser pack merge rebase socket buffer merge  # 32
    blob socket blob token pack pack commit header rebase remote  # 33
    pack tree commit delta remote handle  # 34
    parser handle stream blob pack  # 35
    blob token parser pack remote stream worker parser cache socket  # 36
    window parser queue window object remote tree cache parser worker  # 37
    cache header queue header  # 38
    socket commit token socket  # 39
    cache object commit branch cache ref object socket  # 40
    commit rebase branch window stream parser delta  # 41
    commit pack handle blob  # 42
    ref buffer ref worker buffer ref delta buffer worker rebase tree tree  # 43
    socket token rebase object remote socket queue blob cache  # 44
    window commit header remote tree socket buffer object blob  # 45
    delta handle window rebase parser socket  # 46
    tree merge commit window blob index token header rebase blob queue  # 47
    merge delta delta object tree queue  # 48
    branch ref rebase blob blob stream worker commit header socket  # 49
    ref parser tree ref tree buffer buffer handle object object index ref  # 50
    queue blob object handle header handle  # 51
    window queue delta merge index tree merge branch  # 52
    handle window cache commit worker handle commit header object  # 53
    buffer delta queue cache socket  # 54
    merge ref stream commit rebase window header ref cache  # 55
    queue merge buffer remote tree remote parser header commit socket stream stream  # 56
    remote index parser worker merge buffer tree merge queue delta blob  # 57
    token parser token buffer pack delta ref  # 58
    socket buffer cache buffer merge buffer  # 59
    object remote stream cache index merge  # 60
    parser object pack index branch queue delta delta delta socket  # 61
    token merge handle parser pack parser  # 62
    cache header merge merge rebase header  # 63
    branch remote handle commit buffer branch pack cache tree stream header delta  # 64
    object parser delta cache  # 65
    tree commit window token tree rebase  # 66
    blob header header window tree index merge stream blob  # 67
    handle remote handle delta worker remote worker  # 68
    queue blob tree merge queue stream handle stream  # 69
    commit window delta rebase  # 70
    stream buffer remote cache window header queue tree stream cache token  # 71
    cache parser object buffer delta worker  # 72
    merge buffer index stream buffer branch cache  # 73
    buffer delta object header rebase token tree delta merge token socket  # 74
    worker header socket blob remote merge handle ref token branch cache buffer  # 75
    queue buffer tree tree worker cache queue  # 76
    token parser buffer worker object ref  # 77
    commit object merge blob pack parser cache socket stream  # 78
    branch ref window blob commit object  # 79
    branch commit remote buffer index window merge window worker rebase cache tree  # 80
