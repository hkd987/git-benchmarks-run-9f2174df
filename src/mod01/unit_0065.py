    blob object pack handle buffer window handle  # 0
    commit index pack remote  # 1
    queue window buffer ref delta index parser rebase parser token  # 2
    ref token cache pack cache commit worker tree branch  # 3
    tree token merge handle rebase index header parser token stream branch  # 4
    header blob token header  # 5
    ref index commit rebase blob socket pack blob pack  # 6
    window cache branch delta window  # 7
    stream worker merge rebase ref window  # 8
    parser header parser blob rebase blob buffer queue cache token  # 9
    tree worker delta ref remote stream queue  # 10
    handle queue buffer worker blob blob tree rebase header socket handle ref  # 11
    pack window delta delta queue stream token merge blob branch remote  # 12
    handle parser cache rebase parser blob ref branch  # 13
    socket parser index buffer commit tree token stream rebase  # 14
    object tree rebase worker header token socket rebase token delta pack handle  # 15
    remote cache blob parser blob remote index socket buffer  # 16
    branch socket index window cache stream worker tree socket socket  # 17
    socket tree buffer parser queue  # 18
    merge blob remote tree  # 19
    socket cache socket delta commit  # 20
    remote branch rebase object buffer ref cache branch  # 21
    parser object handle window parser delta queue parser header commit remote token  # 22
    token socket parser window tree object stream socket ref rebase token parser  # 23
    parser handle cache merge socket header  # 24
    remote tree worker window header rebase object tree blob ref  # 25
    token branch queue window remote index header buffer header merge delta  # 26
    buffer merge handle delta remote index merge pack tree stream  # 27
    buffer token remote pack  # 28
    tree parser ref cache ref header merge object  # 29
    socket object worker blob rebase tree window cache  # 30
    merge object parser commit index blob stream commit merge  # 31
    index blob pack handle pack  # 32
    parser queue ref parser  # 33
    remote commit worker blob delta window remote blob rebase socket  # 34
    parser handle socket socket  # 35
