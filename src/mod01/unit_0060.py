    object blob blob token window buffer branch buffer ref  # 0
    pack ref object token object blob remote parser delta  # 1
    socket rebase parser socket handle tree header parser window cache  # 2
    blob commit ref branch stream token ref delta branch worker rebase delta  # 3
    token index remote stream token pack remote socket branch  # 4
    header pack remote cache worker remote window  # 5
    handle ref stream buffer token parser queue  # 6
    merge remote rebase commit merge blob  # 7
    merge header cache handle rebase ref  # 8
    remote cache header queue pack queue cache header stream tree  # 9
    cache merge commit branch queue  # 10
    worker branch token buffer tree  # 11
    tree cache parser branch delta stream handle blob socket queue socket  # 12
    rebase ref blob rebase window pack queue queue pack ref commit  # 13
    merge socket buffer buffer worker header token object queue parser branch header  # 14
    delta remote window cache blob delta rebase  # 15
    socket stream remote header header parser rebase socket rebase cache  # 16
    merge window socket merge  # 17
    queue branch delta socket rebase remote object header  # 18
    parser pack ref buffer stream parser  # 19
    blob commit header delta buffer tree token socket handle  # 20
    merge merge blob socket  # 21
    rebase cache ref ref rebase tree queue  # 22
    stream ref queue ref  # 23
    worker branch cache handle ref tree token object pack ref remote  # 24
    socket object header tree stream rebase cache token branch  # 25
    merge queue stream header parser rebase token buffer token stream merge  # 26
    object parser branch commit object rebase window pack  # 27
    tree handle commit pack header header  # 28
    window queue pack token  # 29
    tree window handle stream cache merge window pack  # 30
    rebase commit buffer header index commit  # 31
    parser header branch remote blob  # 32
    stream header worker window ref socket worker token buffer merge tree  # 33
    branch blob queue delta merge  # 34
    window worker buffer stream window token handle commit index  # 35
    rebase object branch cache handle token pack index pack cache rebase header  # 36
    remote ref delta rebase parser token pack socket token remote token rebase  # 37
    socket branch worker header tree pack pack index window object buffer token  # 38
    token parser header ref header branch  # 39
    parser rebase rebase queue buffer cache tree blob ref  # 40
    pack rebase parser handle branch blob worker ref  # 41
    branch buffer worker object handle commit pack  # 42
    parser handle ref cache branch queue ref  # 43
