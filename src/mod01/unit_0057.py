    parser handle stream object queue  # 0
    index tree buffer index worker socket parser header window ref  # 1
    header header header stream merge branch handle merge  # 2
    buffer ref stream commit merge object token parser  # 3
    token header rebase rebase worker  # 4
    stream stream rebase window rebase branch stream parser  # 5
    window remote parser cache delta  # 6
    handle ref commit queue stream window worker  # 7
    window index ref merge rebase pack branch ref  # 8
    delta ref token object ref object  # 9
    header remote socket tree blob header buffer socket window  # 10
    queue index buffer merge object worker buffer token stream token cache  # 11
    stream rebase worker delta commit  # 12
    socket pack handle object  # 13
    branch token worker merge socket queue commit  # 14
    blob delta buffer window  # 15
    object header stream buffer parser window buffer buffer  # 16
    socket worker cache window header rebase  # 17
    commit ref cache blob parser parser pack ref  # 18
    stream index stream tree token index token worker  # 19
    stream cache stream delta token  # 20
    ref delta header remote stream  # 21
    rebase ref commit parser handle ref header header stream object  # 22
    header buffer parser index commit parser remote tree  # 23
    handle cache index handle socket parser cache rebase header  # 24
    header remote index worker index remote index stream commit delta  # 25
    worker worker queue delta ref token pack index cache socket token socket  # 26
    merge remote header remote tree index object token  # 27
    pack worker buffer rebase header branch ref commit window pack merge tree  # 28
    header index branch remote  # 29
    index delta merge remote pack cache window object commit  # 30
    commit delta buffer window merge object  # 31
    tree worker object window window parser object socket tree queue buffer  # 32
    commit tree tree header cache remote handle object stream rebase  # 33
    delta merge handle tree delta handle socket blob  # 34
    blob remote tree handle token worker delta  # 35
    commit window pack delta token handle stream commit  # 36
    index buffer ref index buffer cache delta ref window buffer  # 37
    handle token delta parser blob stream object  # 38
    ref object window window object  # 39
