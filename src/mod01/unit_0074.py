    window window socket handle rebase tree queue branch buffer cache remote token  # 0
    delta socket token window branch ref  # 1
    window header cache object merge stream queue parser branch delta merge commit  # 2
    merge blob branch ref worker delta stream  # 3
    pack queue pack pack remote pack queue  # 4
    blob rebase remote handle  # 5
    window object window commit parser commit merge delta commit header remote index  # 6
    header stream token header buffer header  # 7
    branch parser token queue worker worker buffer parser ref branch object  # 8
    header rebase commit handle commit queue header  # 9
    branch buffer merge ref branch  # 10
    delta object cache window commit stream token queue stream index  # 11
    remote socket socket handle ref stream ref buffer blob  # 12
    worker commit socket ref object queue window worker blob buffer object  # 13
    tree stream rebase ref index rebase index ref  # 14
    stream blob blob branch header  # 15
    parser commit stream delta remote index  # 16
    index buffer blob header  # 17
    blob object window parser parser header rebase rebase blob  # 18
    delta handle rebase branch blob rebase stream cache  # 19
    tree socket handle parser cache token branch object token merge remote handle  # 20
    rebase socket merge handle  # 21
    blob header tree merge token index buffer header handle queue  # 22
    token delta index parser  # 23
    cache queue queue tree blob merge worker commit ref  # 24
    index remote cache blob parser ref token parser  # 25
    tree parser ref index index remote stream pack remote  # 26
    socket socket commit object  # 27
    queue branch window stream commit socket branch object handle merge  # 28
    branch worker delta branch token  # 29
    token token buffer header object parser cache window handle delta remote  # 30
    socket rebase window header blob commit socket commit  # 31
    token token buffer worker header remote pack blob  # 32
    cache blob delta parser index window blob object index buffer merge  # 33
    socket index merge delta remote parser  # 34
    handle worker cache object queue stream worker commit pack stream  # 35
    parser object stream cache  # 36
    pack socket rebase token worker stream buffer  # 37
    header handle header stream socket rebase remote  # 38
    buffer token token worker worker socket queue token worker  # 39
    header window object socket queue rebase  # 40
    merge header worker queue  # 41
    socket socket object token handle rebase rebase  # 42
    handle pack ref merge blob tree pack  # 43
    parser window cache tree  # 44
    socket window ref commit socket socket queue token tree remote rebase  # 45
    ref stream window queue  # 46
    header queue blob remote ref remote pack rebase parser object buffer rebase  # 47
    remote worker parser merge branch cache  # 48
    header rebase stream cache header branch  # 49
    socket worker socket handle rebase delta worker buffer rebase tree handle  # 50
    delta index parser token buffer queue worker pack pack pack stream  # 51
    token blob rebase queue remote remote pack merge  # 52
    delta pack socket ref rebase ref window token  # 53
    socket worker window pack parser index commit handle merge blob  # 54
    queue parser pack branch pack parser branch  # 55
    delta branch parser rebase token delta delta window token stream worker window  # 0
    pack parser branch token ref object pack socket token window buffer  # 1
    tree cache handle branch stream worker cache merge cache token parser  # 2
    remote delta remote socket merge parser cache worker stream header object socket  # 3
    delta window socket blob blob rebase  # 4
