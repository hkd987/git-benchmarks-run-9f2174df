    header cache window socket object ref index stream pack token branch  # 0
    worker commit handle merge worker delta  # 1
    buffer object object delta  # 2
    delta parser rebase merge  # 3
    ref remote parser handle stream  # 4
    commit queue index window  # 5
    worker window header cache  # 6
    merge tree ref handle object buffer cache rebase  # 7
    window header pack queue queue window rebase ref  # 8
    cache blob branch merge object window blob  # 9
    buffer ref pack window handle queue  # 10
    stream ref header index socket  # 11
    commit queue window blob socket  # 12
    socket branch delta blob buffer header token pack index worker tree  # 13
    worker tree cache merge branch object index index blob token blob  # 14
    commit header socket commit blob ref stream  # 15
    commit blob tree delta object  # 16
    pack commit commit buffer worker token queue commit remote  # 17
    commit parser branch socket stream buffer branch stream cache buffer index  # 18
    handle rebase handle cache  # 19
    stream branch branch buffer index  # 20
    cache remote remote object handle  # 21
    handle object buffer rebase cache blob  # 22
    object handle blob branch branch ref blob  # 23
    stream cache tree pack queue  # 24
    queue branch token delta window delta rebase tree branch object buffer  # 25
    tree merge delta cache object cache  # 26
    remote delta socket merge handle handle header cache stream blob ref parser  # 27
    rebase cache header pack handle rebase remote cache worker  # 28
    branch queue index cache queue  # 29
    tree window token object commit commit tree handle parser index merge  # 30
    socket token token parser delta pack buffer ref  # 31
    cache index blob socket worker delta delta  # 32
    tree index window pack token rebase queue buffer parser pack token  # 33
    delta rebase remote buffer  # 34
    token delta tree parser branch  # 35
    buffer buffer merge branch rebase blob ref ref queue cache  # 36
    index socket cache socket header merge window queue  # 37
    remote parser token delta window  # 38
    buffer tree rebase delta pack queue  # 39
    token blob object ref window rebase rebase stream stream commit  # 40
    index handle merge window buffer rebase ref rebase commit header worker merge  # 41
    token cache parser handle stream blob blob  # 42
    parser pack merge token stream  # 43
    header token buffer worker  # 44
    token window ref window  # 45
    cache header object index token pack  # 46
    remote parser object remote stream blob  # 47
    window pack socket blob parser cache  # 48
    handle index branch handle  # 49
    rebase rebase rebase ref delta  # 50
    cache branch stream queue token handle window branch parser remote  # 51
    remote worker rebase socket  # 52
    blob index header index window stream object parser  # 53
    socket socket commit rebase queue  # 54
    socket queue header rebase  # 55
    blob index object rebase index tree  # 56
    branch handle queue handle branch  # 57
    blob commit stream parser branch rebase socket delta blob  # 58
    commit queue socket cache rebase object header object commit cache window stream  # 59
    parser parser worker blob commit window handle cache handle  # 60
    merge merge index window rebase  # 61
    queue remote cache rebase pack  # 62
    tree object handle socket window rebase cache object delta  # 63
    index blob merge socket stream ref worker  # 64
    pack buffer branch commit window header  # 65
    window delta branch remote remote worker object token merge rebase  # 66
