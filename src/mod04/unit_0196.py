    pack buffer worker buffer queue merge merge index rebase  # 0
    worker queue rebase index pack pack worker delta stream merge  # 1
    rebase rebase rebase socket branch index index handle handle stream ref  # 2
    queue header header ref merge blob buffer stream  # 3
    ref window cache tree object pack handle tree merge index index  # 4
    merge cache window cache stream branch index  # 5
    object object worker buffer blob header branch rebase tree object index  # 6
    rebase merge queue index commit token header ref header delta  # 7
    branch ref socket header index merge header object  # 8
    delta remote merge rebase merge queue parser buffer commit branch rebase window  # 9
    parser socket branch commit  # 10
    parser token queue handle rebase object parser object index token tree  # 11
    header buffer object blob  # 12
    header object socket ref  # 13
    rebase header window socket rebase handle remote queue branch parser ref index  # 14
    handle queue worker remote  # 15
    cache window buffer commit buffer  # 16
    worker token commit index delta cache  # 17
    window handle branch token object ref buffer object tree worker tree cache  # 18
    worker buffer queue tree blob pack tree stream  # 19
    commit delta socket header index tree commit buffer ref  # 20
    header tree commit queue queue delta queue buffer socket ref header  # 21
    merge token buffer buffer window cache cache  # 22
    buffer tree socket tree merge index socket merge stream token commit pack  # 23
    ref queue pack blob queue tree branch  # 24
    tree merge window header pack index remote  # 25
    worker blob handle remote pack commit worker queue handle stream  # 26
    cache merge delta delta handle worker  # 27
    ref tree ref ref commit rebase handle  # 28
    socket socket parser delta remote object rebase buffer worker  # 29
    queue rebase pack cache pack pack rebase  # 30
    pack delta buffer queue socket header index remote socket blob  # 31
    queue rebase handle branch commit merge  # 32
    stream window token rebase commit socket window object worker header socket delta  # 33
    handle cache delta pack merge worker stream header queue delta delta remote  # 34
    remote commit index pack commit branch cache merge ref  # 35
    buffer socket token commit merge parser socket  # 36
    remote tree blob handle index ref ref branch object worker  # 37
    commit ref window cache index commit blob buffer header  # 38
    ref delta merge branch  # 39
    cache queue object blob socket rebase token buffer  # 40
    buffer tree pack ref delta object socket object  # 41
    index parser token window buffer merge window buffer window  # 42
    merge parser remote window merge header blob parser buffer  # 43
    cache worker stream object  # 44
    merge rebase worker queue worker parser pack branch header  # 45
    blob delta handle pack commit merge window buffer  # 46
    rebase index parser index merge ref token window token rebase  # 47
    handle delta worker cache token socket  # 48
    remote socket token window  # 49
    tree header queue queue object  # 50
    commit branch object header tree handle ref ref cache  # 51
    pack handle commit index worker worker queue ref parser  # 52
    header delta commit merge worker queue rebase window queue parser  # 53
    window ref worker index queue tree object  # 54
    handle stream token socket branch delta object object header branch  # 55
    pack pack blob window pack merge  # 56
    remote ref cache ref  # 57
    index handle ref header queue token merge window blob queue worker queue  # 58
    queue rebase object blob handle  # 59
    blob rebase stream header  # 0
    socket socket pack header header parser socket object tree index remote blob  # 1
    buffer rebase worker token remote  # 2
    rebase merge parser branch commit  # 3
    rebase pack buffer pack  # 4
    worker buffer socket handle ref  # 5
    commit parser parser stream handle queue remote commit  # 6
    stream window delta tree branch worker queue index  # 7
    stream rebase tree branch stream handle cache header token  # 8
