    ref stream index object delta  # 0
    worker branch socket window remote  # 1
    token socket ref commit socket socket commit stream index ref commit parser  # 2
    window tree socket header  # 3
    pack socket buffer ref delta  # 4
    rebase handle rebase buffer parser object delta buffer token stream token  # 5
    rebase merge stream commit tree object queue index object remote  # 6
    branch socket socket tree ref blob socket  # 7
    handle tree object worker tree  # 8
    pack index remote merge commit worker handle socket window token cache  # 9
    queue header pack tree parser  # 10
    pack tree token stream index token  # 11
    merge rebase header delta commit header  # 12
    rebase buffer tree index tree token  # 13
    merge queue merge commit tree object token blob merge remote stream socket  # 14
    tree handle stream token window pack  # 15
    parser queue socket branch token header  # 16
    pack delta ref merge index ref  # 17
    delta token object cache queue  # 18
    buffer merge delta socket tree object handle  # 19
    index handle buffer object rebase handle  # 20
    socket buffer rebase stream index window branch token commit  # 21
    cache branch window blob tree pack parser token blob cache token rebase  # 22
    parser commit handle queue cache queue rebase  # 23
    tree cache header worker header queue stream index  # 24
    tree blob tree socket  # 25
    blob socket blob blob remote buffer cache socket tree  # 26
    index token delta queue stream token window handle buffer  # 27
    header token rebase token token  # 28
    branch cache header pack remote tree remote ref parser ref  # 29
    rebase merge worker delta parser cache  # 30
    rebase object buffer socket blob merge branch ref  # 31
    socket commit token window handle queue ref blob  # 32
    pack blob worker tree index remote buffer  # 33
    commit tree remote window pack worker merge  # 34
    queue cache queue rebase stream index remote blob pack branch stream  # 35
    worker buffer remote token pack blob index socket worker blob worker  # 36
    header ref cache parser merge tree merge handle rebase worker object  # 37
    branch branch cache window window blob stream remote parser  # 38
    header window stream tree rebase worker queue object socket stream worker commit  # 39
    stream ref handle branch window token rebase handle  # 40
    commit worker worker buffer blob stream  # 41
    token worker blob pack  # 42
    pack tree blob handle handle  # 43
    delta stream token stream header  # 44
    rebase window pack commit merge parser pack index  # 45
    object stream buffer object worker ref buffer handle pack blob parser  # 46
    socket pack socket token commit blob cache buffer cache socket queue rebase  # 47
    ref branch header buffer tree  # 48
    index rebase parser window  # 49
    cache pack blob index commit  # 50
    object tree token handle blob blob handle cache blob pack  # 51
    pack tree queue stream tree socket pack window  # 52
    worker stream stream header merge index remote header  # 53
    socket parser socket tree blob pack queue window parser commit  # 54
    delta blob stream ref pack stream stream object remote  # 55
    tree pack blob parser branch branch commit branch buffer socket cache  # 56
    worker worker index worker delta parser delta window cache  # 57
    object tree token branch merge commit handle tree cache stream queue worker  # 58
    token remote index blob ref blob  # 59
    cache object merge delta rebase worker object queue branch ref token  # 60
    rebase window socket window branch  # 61
    stream index commit window remote header delta pack merge blob socket tree  # 62
    object window object queue token pack merge delta cache merge branch blob  # 63
    queue queue worker window index ref delta pack delta rebase token  # 64
    socket rebase header socket commit delta remote  # 65
    stream pack queue branch merge blob tree cache  # 66
    rebase ref pack ref window queue index ref window delta token  # 67
    merge ref merge socket stream commit token  # 68
    stream tree index parser header  # 69
    parser worker rebase header socket  # 70
    index pack commit object socket remote pack handle object window  # 71
    header blob queue queue rebase worker parser remote remote rebase merge  # 72
    commit pack handle tree object merge  # 0
    ref window pack index  # 1
    stream remote branch rebase buffer branch ref blob index object token  # 2
    index handle token worker rebase delta tree window  # 3
    blob blob buffer commit parser delta  # 4
    blob cache window parser window header  # 5
    object window remote token index commit delta handle index delta  # 6
    index worker rebase branch socket parser window  # 7
    index index worker buffer delta merge rebase queue worker rebase  # 8
    tree ref socket delta token remote remote buffer socket  # 9
    handle buffer buffer window object  # 10
    stream queue token delta branch object branch socket  # 11
    index parser cache object branch pack merge commit tree rebase stream worker  # 12
    commit worker window queue branch remote tree  # 13
    branch ref delta object window token window parser parser stream index tree  # 14
