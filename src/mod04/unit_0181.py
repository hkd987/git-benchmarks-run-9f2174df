    ref buffer commit index cache handle queue  # 0
    parser branch object merge queue socket window worker  # 1
    branch header blob index socket cache index  # 2
    header index commit tree index stream token rebase window queue index pack  # 3
    socket tree blob index rebase tree stream socket index  # 4
    token branch blob socket object branch worker stream object queue handle  # 5
    rebase pack stream tree branch tree stream handle commit token queue window  # 6
    tree socket handle branch index index remote commit  # 7
    header handle merge blob  # 8
    branch window window handle branch queue worker object  # 9
    ref tree object stream socket cache header pack buffer  # 10
    branch index handle socket ref index delta merge window  # 11
    socket ref cache worker token tree pack handle index rebase pack  # 12
    commit delta blob ref merge ref  # 13
    commit buffer socket delta window index cache worker blob worker remote pack  # 14
    stream socket cache parser merge queue buffer cache socket commit rebase queue  # 15
    queue buffer token ref parser blob  # 16
    socket parser pack token cache  # 17
    cache queue branch cache window  # 18
    rebase buffer handle pack merge header token index  # 19
    window handle ref socket handle cache cache stream ref  # 20
    stream commit branch tree tree  # 21
    rebase merge pack remote branch buffer delta  # 22
    merge delta tree cache blob blob window header  # 23
    branch commit window branch rebase queue window object pack  # 24
    tree stream blob tree commit remote blob tree buffer branch buffer  # 25
    socket index window object ref delta parser buffer ref merge  # 26
    buffer pack pack queue delta commit buffer merge handle  # 27
    token tree index object worker rebase queue header window commit token  # 28
    queue ref queue pack header  # 29
    queue branch ref token socket object  # 30
    cache rebase header merge pack remote branch queue handle delta queue  # 31
    token object object worker merge index queue rebase cache socket header header  # 32
    index object parser socket ref blob pack ref header queue  # 33
    pack socket commit branch cache remote tree rebase rebase pack window cache  # 34
