    remote parser stream window stream  # 0
    blob commit stream merge index stream pack rebase ref tree  # 1
    handle worker buffer stream blob branch  # 2
    index index remote buffer rebase tree pack  # 3
    buffer buffer ref buffer index  # 4
    stream commit socket ref blob queue merge  # 5
    queue pack buffer remote merge  # 6
    object remote stream stream  # 7
    commit branch handle worker pack ref handle window window tree queue buffer  # 8
    commit remote parser pack buffer stream stream delta  # 9
    socket remote stream header commit merge parser index branch socket object  # 10
    object buffer object parser socket pack stream  # 11
    index buffer buffer parser commit socket window delta ref delta token commit  # 12
    index token handle delta socket worker  # 13
    stream cache ref delta parser header token blob pack cache window  # 14
    commit blob buffer object stream  # 15
    worker rebase remote pack queue token  # 16
    token parser buffer rebase  # 17
    branch window tree socket merge handle object merge stream  # 18
    cache stream branch pack stream socket ref  # 19
    stream index token handle blob cache delta object object ref merge  # 20
    queue stream branch rebase object parser blob pack remote object merge  # 21
    delta index token stream tree socket rebase  # 22
    buffer remote worker handle object blob delta window remote worker worker  # 23
    parser buffer handle token cache blob rebase parser delta pack stream  # 24
    pack window commit pack handle pack cache commit  # 25
    token stream index delta delta worker socket socket  # 26
    cache cache commit commit window  # 27
    parser tree ref blob handle parser index header index ref delta  # 28
    window token stream remote socket commit merge worker remote  # 29
    blob window cache handle window cache worker  # 30
    merge handle worker stream index pack worker tree branch queue  # 31
    handle delta tree tree window delta token socket  # 32
    blob pack index handle socket merge remote remote  # 33
    parser worker queue blob buffer token blob  # 34
    window stream cache rebase rebase queue  # 35
    rebase rebase buffer index  # 36
    branch queue queue queue token  # 37
    commit pack pack commit commit branch cache worker  # 38
    header delta queue buffer token blob cache remote delta stream  # 39
    pack merge object queue tree rebase worker  # 40
    window ref header ref object rebase remote ref token pack tree  # 41
    cache cache window buffer token pack socket token header merge merge  # 42
    object ref buffer remote queue tree branch merge  # 43
    worker header handle merge parser merge branch  # 44
    token rebase index remote remote window merge ref  # 45
    merge rebase delta rebase object handle header commit  # 46
    merge parser window header commit tree worker socket token header queue commit  # 47
    parser branch rebase branch delta  # 48
    buffer rebase commit merge queue pack tree socket index blob tree  # 49
    worker token commit header blob token object  # 50
    stream delta cache queue buffer blob  # 51
    tree header queue cache handle  # 52
    handle ref branch blob commit  # 53
    stream branch merge token  # 54
    tree object branch pack pack buffer queue delta stream buffer rebase delta  # 55
    socket stream rebase window window pack queue handle queue blob socket  # 56
    object merge cache rebase ref worker handle worker worker  # 57
    ref merge tree buffer parser  # 58
    branch handle buffer worker object object delta  # 59
    token worker pack worker merge header branch pack delta delta rebase branch  # 60
    cache buffer queue index pack  # 61
    socket worker cache cache handle queue commit blob delta blob stream commit  # 62
    socket parser queue stream stream commit rebase rebase index remote object socket  # 63
    tree socket stream window socket  # 64
    delta window object socket commit merge index  # 65
    object delta queue pack queue stream socket delta cache socket handle queue  # 66
    window blob remote stream  # 67
    blob token object window delta  # 68
    tree buffer window merge stream tree rebase queue  # 69
    object buffer queue socket delta  # 70
    commit token parser tree buffer  # 71
    commit remote delta ref pack  # 72
    branch commit branch merge object stream tree window worker remote tree ref  # 73
    delta delta stream header worker object worker  # 74
    rebase rebase rebase stream queue window blob  # 75
    pack delta buffer tree blob rebase buffer branch buffer header token worker  # 76
    branch window worker object commit buffer remote rebase merge  # 77
    buffer delta pack socket cache  # 78
    remote window window delta object commit  # 79
    parser object merge socket token ref worker  # 80
    handle remote blob parser blob remote  # 81
    tree token blob ref window stream worker index remote  # 82
    remote buffer commit buffer parser stream queue queue buffer stream  # 83
    commit window token header  # 84
    cache buffer tree branch  # 85
    branch queue rebase cache tree delta index remote socket buffer ref socket  # 86
    object token stream worker  # 87
