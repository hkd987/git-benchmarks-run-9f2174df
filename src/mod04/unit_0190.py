    blob token tree parser index handle stream  # 0
    tree blob header stream commit remote worker object parser commit  # 1
    pack stream parser pack merge parser  # 2
    window blob token queue object stream header token socket index object  # 3
    cache blob ref worker cache remote commit  # 4
    token worker token blob ref index rebase parser branch stream queue  # 5
    remote handle ref queue ref parser buffer  # 6
    buffer tree parser commit branch remote buffer tree  # 7
    remote index stream stream  # 8
    blob pack buffer index object remote tree commit stream header commit  # 9
    worker commit window object  # 10
    remote parser cache buffer remote merge window tree handle cache header  # 11
    rebase buffer pack handle window merge window window  # 12
    remote socket stream object remote index socket branch rebase buffer queue  # 13
    pack tree index merge delta commit delta parser handle ref  # 14
    branch commit worker delta  # 15
    merge ref merge socket commit rebase buffer buffer commit worker index remote  # 16
    object cache delta queue  # 17
    handle index token pack commit blob tree delta  # 18
    queue tree buffer window remote  # 19
    merge pack object merge  # 20
    object rebase handle header window window object parser  # 21
    cache remote pack buffer token merge  # 22
    cache window commit handle  # 23
    stream socket token stream delta object worker socket rebase ref delta blob  # 24
    queue worker blob rebase pack token  # 25
    index tree remote cache blob window token pack merge handle object  # 26
    socket window remote branch tree delta handle  # 27
    pack handle token parser queue parser commit token  # 28
    commit index branch queue tree token  # 29
    index socket parser handle object rebase delta handle buffer worker delta  # 30
    ref stream delta worker commit delta cache  # 31
    cache header header queue header queue  # 32
    cache parser window pack window header index worker pack tree queue handle  # 33
    window handle handle handle handle parser stream blob token  # 34
    socket blob pack stream header blob buffer handle  # 35
    rebase remote remote tree socket queue  # 36
    buffer pack worker queue blob socket delta merge queue merge blob  # 37
    commit socket worker window index remote cache blob ref header delta  # 38
    stream tree rebase worker pack socket object  # 39
    commit blob header cache token token window cache stream pack  # 40
    ref rebase remote merge stream  # 41
    queue buffer tree cache rebase rebase stream worker stream delta pack  # 42
    pack object index buffer handle worker remote merge window object header merge  # 43
    delta cache pack branch  # 44
    object cache delta index branch queue  # 45
    queue handle buffer queue token commit tree  # 46
    blob token socket buffer handle token cache  # 47
    parser header window branch parser delta parser socket object  # 48
    pack cache token worker stream stream remote header parser ref window commit  # 49
    header rebase object remote ref blob  # 50
    parser blob queue queue pack commit rebase worker stream merge pack header  # 51
    parser worker parser tree pack tree blob  # 52
    buffer cache socket header worker  # 53
    buffer merge branch pack  # 54
    blob socket branch stream remote stream merge  # 55
    remote branch cache queue  # 56
    rebase pack merge stream pack handle index stream stream socket  # 57
    commit rebase rebase rebase parser stream queue delta stream ref remote index  # 58
    header branch handle cache commit queue index ref ref  # 59
    merge tree cache commit  # 60
    worker socket remote window socket worker tree index  # 61
    remote branch handle handle ref parser rebase tree  # 62
    parser pack queue delta ref handle handle ref socket handle index  # 63
    pack remote buffer remote object header stream cache token  # 64
    worker stream index handle parser ref token blob header  # 65
    tree merge object index queue handle worker window stream commit buffer  # 66
    queue index worker commit header buffer queue buffer pack object  # 67
    window index merge worker object commit  # 68
    commit commit cache pack tree branch remote pack blob commit queue  # 69
    rebase delta worker token  # 70
    merge rebase index index commit stream commit blob branch  # 71
