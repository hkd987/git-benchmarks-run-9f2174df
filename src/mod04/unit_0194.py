    header ref ref stream index token worker pack header worker buffer queue  # 0
    window token index merge worker commit tree blob branch branch  # 1
    header commit index index buffer  # 2
    worker pack delta cache commit index stream worker header  # 3
    header queue blob object worker socket pack merge parser worker  # 4
    pack merge commit header blob commit socket commit  # 5
    token cache pack blob socket delta  # 6
    queue tree window buffer branch index delta commit object index  # 7
    socket tree index worker ref index window buffer worker buffer window index  # 8
    buffer buffer queue queue socket worker handle delta socket  # 9
    delta parser tree socket blob queue remote header  # 10
    index branch merge index  # 11
    branch pack rebase queue stream cache tree header index stream token window  # 12
    remote branch cache pack window blob index  # 13
    branch pack merge socket buffer token stream handle blob  # 14
    delta merge blob window blob header blob  # 15
    pack branch merge token rebase cache merge cache  # 16
    header commit handle parser  # 17
    index commit window cache rebase delta parser remote queue token buffer object  # 18
    cache window branch branch queue  # 19
    token stream delta stream  # 20
    stream remote branch queue object window cache  # 21
    remote object worker delta queue buffer remote delta cache handle pack  # 22
    handle parser branch tree object ref stream branch remote  # 23
    pack stream delta parser tree cache branch parser ref remote token  # 24
    delta object commit buffer cache tree  # 25
    pack blob worker queue buffer handle blob branch stream pack header delta  # 26
    pack object commit merge branch  # 27
    queue rebase handle object  # 28
    remote queue cache branch buffer window worker queue ref stream merge object  # 29
    handle header parser worker rebase header socket handle cache branch merge tree  # 30
    branch worker branch header object merge window remote blob buffer  # 31
    index delta token stream token token window commit socket commit  # 32
    header buffer merge object handle delta ref stream  # 33
    ref branch handle window  # 34
    merge parser delta remote token branch worker  # 35
    object stream remote remote token parser cache remote buffer  # 36
    delta tree handle blob index stream  # 37
    stream cache remote delta merge  # 38
    delta token branch buffer  # 39
    header index rebase merge stream stream stream parser delta pack  # 40
    index parser cache branch merge cache tree handle blob pack  # 41
    ref handle queue token pack  # 42
    handle token index buffer header branch  # 43
    worker ref buffer cache index worker handle commit blob remote worker  # 44
    window ref handle index merge buffer socket  # 45
    merge header remote window  # 46
    handle socket delta socket parser window blob delta queue rebase cache tree  # 47
    buffer object worker remote buffer handle socket token handle socket stream remote  # 48
    window parser stream tree  # 49
    rebase buffer header socket merge remote object header parser queue  # 50
    token buffer window object queue merge header  # 51
    handle parser ref blob cache blob window buffer tree pack merge commit  # 52
    parser header commit socket window window object index buffer delta token object  # 53
    blob worker handle parser window socket worker queue  # 54
    queue worker handle blob remote object  # 55
    remote index stream object cache blob  # 56
    cache pack stream socket blob stream pack parser stream  # 57
    remote ref object ref object delta merge token  # 58
    delta handle window socket object blob parser queue tree  # 59
    index delta stream worker token delta merge token worker ref  # 60
    token worker handle stream header pack cache blob cache  # 61
    object window commit index parser ref remote  # 62
    buffer worker commit pack stream buffer merge socket stream buffer tree socket  # 63
    merge object remote index rebase tree buffer  # 64
    cache rebase socket branch stream  # 65
    rebase header header delta  # 66
    worker commit index header blob merge delta parser tree object commit  # 67
    header blob merge window ref cache header index delta blob parser  # 68
    pack socket buffer delta rebase branch remote  # 69
    stream buffer remote delta  # 70
    blob socket buffer window tree window index buffer  # 71
    pack cache branch blob queue parser  # 72
    branch remote parser index delta merge queue buffer worker commit header  # 73
    ref tree worker merge blob blob cache socket branch ref handle buffer  # 74
    branch token stream blob  # 75
    token handle handle rebase delta tree delta  # 76
    branch header cache worker index stream handle window object worker stream window  # 77
    socket pack handle socket tree commit ref token blob queue token stream  # 78
