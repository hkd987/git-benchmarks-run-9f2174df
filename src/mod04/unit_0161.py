    pack queue ref rebase  # 0
    buffer socket delta remote handle ref object buffer remote stream  # 1
    window header branch header delta commit remote  # 2
    merge worker parser tree worker header commit buffer parser token merge  # 3
    remote object buffer header buffer commit merge stream branch branch object  # 4
    worker tree branch socket ref tree window worker  # 5
    header delta socket buffer queue stream stream  # 6
    rebase parser buffer pack handle blob pack tree handle buffer branch merge  # 7
    object socket queue cache index parser pack buffer tree  # 8
    worker tree window cache parser worker token stream buffer  # 9
    buffer commit branch queue commit delta socket ref merge branch cache socket  # 10
    pack rebase window blob buffer tree token object delta delta queue  # 11
    tree window buffer stream index queue remote commit merge header  # 12
    cache pack handle delta queue handle pack delta object cache worker  # 13
    stream header handle rebase pack window socket delta remote merge  # 14
    stream blob header pack rebase worker  # 15
    token queue worker branch branch index queue window rebase socket blob handle  # 16
    index handle token cache  # 17
    merge header pack tree remote handle rebase branch parser rebase tree  # 18
    pack token header queue delta rebase merge handle commit  # 19
    remote commit buffer merge cache commit header delta worker handle cache token  # 20
    object header index index rebase worker cache token buffer cache remote  # 21
    buffer ref stream worker stream index pack  # 22
    merge handle index branch rebase parser remote socket rebase tree socket  # 23
    cache tree queue merge socket blob token  # 24
    cache buffer index socket tree cache handle pack  # 25
    delta token blob socket window merge window ref handle remote  # 26
    remote delta object buffer merge  # 27
    token socket object stream ref merge stream tree  # 28
    parser stream merge cache rebase socket  # 29
    parser token index merge socket  # 30
    handle cache parser window handle window  # 31
    rebase stream pack window object pack  # 32
    remote pack stream window header rebase cache blob  # 33
    handle rebase remote worker window ref index delta token queue buffer  # 34
    rebase delta header tree index branch  # 35
    token rebase pack rebase socket delta stream rebase ref stream  # 36
    object header object delta token handle object header cache  # 37
    stream rebase object delta index buffer object token blob  # 38
    rebase stream merge index  # 39
    ref tree delta ref index object merge token worker pack header  # 40
    commit tree parser object ref token ref rebase handle merge header stream  # 41
    buffer delta commit commit object object rebase ref remote parser parser  # 42
    ref token queue index pack  # 43
    parser pack object rebase window tree  # 44
    header branch handle blob tree merge socket  # 45
    blob blob object token cache remote token socket header ref  # 46
    pack commit rebase object merge  # 47
    buffer worker token object delta handle delta  # 48
    index index remote parser ref  # 49
    socket stream socket remote  # 50
    buffer buffer socket window rebase parser ref parser window  # 51
    buffer window commit token rebase window parser rebase object  # 52
    pack buffer index parser delta worker ref pack socket  # 53
    window queue merge pack delta object merge ref header  # 54
    tree header merge handle merge worker remote queue buffer handle index object  # 55
    buffer header ref delta token remote branch socket remote index branch stream  # 56
    index handle delta window pack stream cache merge queue  # 57
    rebase rebase queue branch commit commit index parser cache  # 58
    tree delta pack queue commit index merge index delta token  # 59
    delta tree object worker parser commit branch pack  # 60
    object tree remote delta merge cache commit  # 61
    tree token tree branch commit commit commit tree parser merge commit  # 62
    window rebase socket queue  # 63
    commit header handle pack merge handle buffer socket worker delta token object  # 64
    delta remote pack delta index socket branch stream  # 65
    ref window tree handle token pack stream ref remote rebase token cache  # 66
    token ref object parser index  # 67
