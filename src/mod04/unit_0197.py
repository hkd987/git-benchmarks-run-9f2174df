    worker window socket rebase object  # 0
    merge stream worker pack index  # 1
    merge token stream socket rebase  # 2
    handle worker ref ref branch token index pack buffer branch remote  # 3
    tree stream delta token window queue object  # 4
    header commit stream ref stream index worker window  # 5
    branch ref buffer token blob parser queue branch ref buffer  # 6
    queue window parser parser tree buffer buffer handle rebase  # 7
    queue branch handle index queue blob queue queue tree  # 8
    handle pack ref token buffer cache tree rebase tree token  # 9
    socket stream commit cache branch window  # 10
    header window buffer buffer ref index branch parser pack  # 11
    commit ref tree commit  # 12
    window parser window worker index  # 13
    header index index cache object token tree  # 14
    buffer pack branch queue parser  # 15
    object branch socket pack rebase window handle rebase delta remote remote rebase  # 16
    queue header queue buffer tree window index delta blob object buffer  # 17
    blob commit socket stream window token commit tree parser rebase handle socket  # 18
    stream ref delta branch tree cache tree window branch  # 19
    merge branch queue remote branch pack parser handle window ref  # 20
    commit parser pack token cache  # 21
    window index blob rebase parser stream header object worker tree socket stream  # 22
    remote merge branch object delta worker buffer buffer  # 23
    index ref buffer blob parser worker branch buffer header object  # 24
    remote parser rebase token  # 25
    branch socket blob token stream ref  # 26
    handle merge token blob delta delta  # 27
    cache pack rebase commit handle tree token branch parser parser  # 28
    handle object stream merge object commit header branch index cache stream  # 29
    cache tree buffer tree remote ref branch index token  # 30
    token remote pack merge handle queue delta  # 31
    queue window cache remote  # 32
    token rebase cache header  # 33
    ref header window socket rebase  # 34
    handle worker handle window window remote branch  # 35
    blob queue merge worker socket queue ref commit delta handle  # 36
    index index buffer header stream blob tree header  # 37
    remote delta handle stream  # 38
    buffer index window tree  # 39
    cache buffer delta index  # 40
    remote ref object merge worker cache index remote  # 41
    pack delta rebase token rebase  # 42
    index tree object branch commit window stream delta  # 43
    rebase cache header object merge  # 44
    queue handle queue tree socket commit commit  # 45
    parser worker remote worker buffer branch worker header handle branch queue delta  # 46
    parser tree pack buffer worker rebase merge handle tree buffer  # 47
