    index remote merge cache header rebase worker token index token remote  # 0
    branch handle ref window socket object object parser merge  # 1
    header handle queue window stream commit header parser object queue branch  # 2
    delta branch queue object pack index stream socket blob queue pack  # 3
    window buffer queue stream  # 4
    index cache window rebase ref  # 5
    commit object commit parser queue socket parser pack  # 6
    buffer queue buffer remote queue blob  # 7
    pack socket branch token stream parser branch  # 8
    queue merge object buffer remote parser worker window socket ref  # 9
    socket tree remote object socket branch branch remote  # 10
    buffer cache rebase ref socket branch remote tree  # 11
    index commit cache handle ref header cache buffer pack index  # 12
    parser socket tree remote token handle blob rebase token commit merge  # 13
    delta commit tree socket ref buffer remote index object ref  # 14
    cache buffer index commit delta commit merge handle queue window worker  # 15
    parser object pack pack window  # 16
    blob branch pack stream stream commit  # 17
    delta parser merge tree token rebase tree token stream queue window  # 18
    parser branch header handle stream branch tree remote delta window ref queue  # 19
    handle queue buffer socket queue ref buffer  # 20
    tree ref commit parser window tree worker  # 21
    socket cache token window index  # 22
    tree delta delta header merge ref  # 23
    ref window index remote token socket branch tree buffer index merge  # 24
    pack remote stream token tree stream delta delta blob pack tree window  # 25
    tree handle branch tree object buffer token  # 26
    stream blob header ref window tree header header queue ref socket  # 27
    header parser queue branch delta stream pack  # 28
    stream buffer cache pack  # 29
    tree remote rebase commit blob pack blob window remote  # 30
    commit cache buffer blob cache stream index  # 31
    index cache blob stream  # 32
    socket window index branch header stream socket delta commit worker  # 33
    window parser parser stream buffer  # 34
    commit remote window socket merge socket window window buffer parser  # 35
    handle index blob merge object rebase header cache  # 36
    parser delta branch rebase header tree object cache handle cache index header  # 37
    parser tree handle index cache header queue handle tree parser queue  # 38
    rebase header branch stream tree  # 39
    merge object pack tree pack blob  # 40
    token worker object commit merge branch branch handle delta pack queue  # 41
    stream object tree blob remote  # 42
    ref delta ref parser parser header tree  # 43
    worker stream worker worker handle socket remote worker handle worker  # 44
    branch ref buffer cache remote stream  # 45
    delta header header branch  # 46
    branch token queue merge index index  # 47
    blob token delta stream index commit remote parser token  # 48
    blob delta rebase blob pack  # 49
    token commit tree token buffer blob commit header buffer parser  # 50
    socket delta index header delta worker pack  # 51
    pack worker delta stream parser index token branch object delta ref  # 52
    token socket index handle handle socket object socket  # 53
    pack commit blob buffer parser index merge  # 54
    merge window merge ref token branch blob buffer  # 55
    commit token header socket socket handle object token handle  # 56
    delta window cache object buffer object index tree commit worker worker  # 57
    rebase remote header pack window ref blob delta queue object pack  # 58
    cache window blob blob token header index header  # 59
    stream worker header merge stream cache object  # 60
    delta queue window header  # 61
    merge handle tree worker header worker  # 62
    worker merge buffer window queue  # 63
    queue window stream handle  # 64
    socket socket index header remote pack index queue  # 65
    window buffer cache token remote stream stream  # 66
    window handle cache socket  # 67
    merge rebase merge commit buffer parser worker branch cache  # 68
    tree window branch queue cache commit socket index header socket  # 69
    tree commit buffer header  # 70
    stream index rebase parser worker stream window window remote delta tree  # 71
    window socket token merge rebase delta token remote window pack remote  # 72
    window tree commit ref branch delta blob  # 73
    cache commit remote handle queue handle commit index stream  # 74
    object parser queue index  # 75
    tree cache tree tree token branch object parser commit blob  # 76
    stream header cache worker ref header pack token  # 77
    window socket parser buffer object cache object remote ref token  # 78
    pack object stream tree header object window socket handle pack  # 79
    header ref index cache delta parser  # 80
    window merge cache object parser blob ref cache  # 81
