    rebase socket buffer tree index merge token token stream  # 0
    token commit token blob queue branch cache buffer queue  # 1
    header delta tree parser object rebase  # 2
    ref cache queue branch stream index token index  # 3
    header parser window delta token object token  # 4
    branch queue header tree branch ref worker stream stream  # 5
    object ref worker tree merge delta socket  # 6
    socket blob branch socket queue  # 7
    window header remote object socket commit stream  # 8
    blob object header socket  # 9
    blob object tree worker tree branch branch rebase tree parser  # 10
    pack stream branch parser token queue cache commit  # 11
    rebase stream tree stream object object token worker  # 12
    merge queue cache branch object rebase remote merge merge  # 13
    branch cache window index rebase commit object window object header merge  # 14
    stream worker token ref handle object buffer cache  # 15
    worker parser queue delta delta worker rebase object rebase  # 16
    token merge ref cache branch delta ref delta buffer rebase  # 17
    cache cache remote pack window rebase buffer tree window pack token parser  # 18
    rebase ref delta header merge handle header  # 19
    ref blob ref rebase commit socket worker branch branch handle  # 20
    tree handle buffer handle stream object rebase blob branch remote  # 21
    tree tree remote commit branch token buffer stream  # 22
    remote header branch header stream remote stream ref commit  # 23
    worker delta header merge index index socket rebase ref pack stream  # 24
    parser worker remote commit tree blob  # 25
    branch queue blob branch  # 26
    delta worker ref buffer handle cache tree  # 27
    delta parser branch branch queue  # 28
    buffer worker token token tree index merge worker handle blob handle  # 29
    remote object ref buffer rebase  # 30
    stream cache window token  # 31
    parser buffer delta delta pack buffer commit queue window token ref  # 32
    index cache tree socket  # 33
    delta queue blob rebase object  # 34
    parser commit cache branch socket commit worker header socket  # 35
    stream pack pack branch  # 36
    tree index ref buffer  # 37
    worker socket ref commit token token token  # 38
    header branch header pack remote parser parser parser commit remote merge  # 39
    blob parser parser worker cache  # 40
    header socket socket parser pack  # 41
    delta pack header worker worker  # 42
    window rebase handle worker token tree blob object commit branch remote  # 43
    rebase index queue handle branch blob branch stream branch parser ref  # 44
    cache index cache queue  # 45
    remote blob parser worker  # 46
    token remote merge header buffer  # 47
    parser queue remote queue header branch tree ref ref blob merge  # 48
    handle socket branch worker window  # 49
    object pack pack merge branch  # 50
    buffer header token remote token branch  # 51
    handle object token pack commit stream ref buffer blob rebase commit buffer  # 52
    cache merge object token merge delta stream window  # 53
    remote header pack branch object ref tree queue worker stream blob  # 54
    ref object tree pack index ref delta  # 55
    parser cache object queue queue  # 56
    stream token remote parser blob window remote queue merge  # 0
    parser cache queue buffer branch blob  # 1
    socket window object stream merge tree cache object delta index queue  # 2
    buffer tree branch index worker header header stream cache  # 0
    stream ref merge buffer branch index remote window  # 1
    rebase handle window delta tree handle token socket stream pack pack queue  # 2
    window worker handle merge cache delta tree cache buffer header  # 3
