    pack stream pack cache  # 0
    blob worker remote header branch  # 1
    branch token tree worker window rebase  # 2
    object stream header merge handle window header buffer remote token buffer object  # 3
    handle merge delta pack worker stream parser  # 4
    delta socket buffer socket commit  # 5
    delta ref queue rebase stream queue index tree  # 6
    pack commit commit socket blob ref window cache index token cache  # 7
    handle window tree index cache window  # 8
    index stream blob handle ref  # 9
    window socket token delta branch  # 10
    index handle buffer remote blob blob  # 11
    object queue pack branch token  # 12
    cache parser queue stream  # 13
    delta object buffer pack delta tree header index worker merge window  # 14
    queue queue window object token rebase parser delta cache  # 15
    window header header blob object delta  # 16
    handle worker worker delta token merge  # 17
    ref object handle socket object queue remote  # 18
    handle branch tree parser ref  # 19
    socket token object stream branch  # 20
    queue rebase commit tree blob header queue delta worker parser  # 21
    index merge window rebase blob tree window tree  # 22
    queue worker blob branch queue remote index pack token  # 23
    window parser ref stream buffer remote delta window worker queue queue  # 24
    commit merge stream socket index delta socket object queue token  # 25
    object buffer branch buffer  # 26
    worker delta blob token rebase buffer queue  # 27
    commit blob object commit socket token cache socket delta socket queue  # 28
    branch commit blob handle delta merge header header branch ref header  # 29
    object delta commit parser socket  # 30
    buffer window pack handle index worker remote commit handle index index object  # 31
    remote queue tree handle delta  # 32
    rebase object merge rebase queue cache ref queue stream rebase pack  # 33
    token token ref header blob stream buffer rebase rebase  # 34
    blob worker merge tree index object queue merge blob  # 35
    tree merge object commit queue commit rebase  # 36
    buffer commit header window handle object window  # 37
    window window worker cache  # 38
    header remote pack header remote window tree tree object blob  # 39
    remote token token window token branch index cache worker cache branch remote  # 40
    cache window buffer delta commit branch branch  # 41
    parser window socket stream tree remote object token pack branch handle  # 42
    queue cache window cache queue delta  # 43
    blob window pack index window branch delta index window queue  # 44
    buffer branch cache object commit index queue  # 45
    merge token branch remote blob stream delta tree queue delta stream  # 46
    index parser tree token tree  # 47
    cache window header ref  # 48
    commit buffer delta tree buffer cache  # 49
    commit cache socket ref pack  # 50
    cache remote index stream header branch  # 51
    delta cache delta buffer merge window pack delta rebase socket ref commit  # 52
    handle stream remote queue handle  # 53
    delta pack commit handle  # 54
    stream pack ref branch parser token window buffer queue rebase stream blob  # 55
    branch index queue remote object stream socket tree index  # 56
    token token delta token tree parser pack handle merge worker header delta  # 57
    index object tree parser blob  # 58
    index delta worker stream  # 59
    header delta window stream parser merge merge parser object cache pack token  # 60
    handle socket delta token ref handle handle remote remote commit pack stream  # 61
    socket pack blob remote  # 0
    blob index commit header remote pack token object token cache  # 1
    window token pack blob  # 2
    parser tree header delta stream pack parser merge commit  # 3
    stream token token pack worker merge remote  # 4
    stream handle tree socket worker queue header branch queue pack delta  # 5
    delta merge window blob delta blob parser blob parser delta  # 6
    branch worker window stream  # 0
    parser window blob buffer stream buffer delta stream queue index tree handle  # 1
    stream token index header worker cache token pack  # 2
    commit parser pack rebase remote merge  # 3
    handle parser worker queue commit tree socket tree buffer buffer branch blob  # 4
    window cache window ref blob remote object index tree pack header object  # 5
    branch branch pack object object socket  # 6
    handle cache rebase object ref  # 7
    blob blob rebase parser merge merge rebase queue  # 8
    window stream handle cache cache token worker pack buffer delta  # 9
    delta commit blob remote  # 10
    parser ref parser worker branch header  # 11
    cache buffer tree tree token pack handle  # 12
