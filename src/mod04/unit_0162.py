    worker object socket handle delta stream token  # 0
    worker queue delta queue cache merge worker window blob window header parser  # 1
    object branch stream socket window parser worker header window queue parser token  # 2
    stream ref worker remote  # 3
    tree merge merge object window branch buffer buffer branch  # 4
    object commit tree branch object merge branch  # 5
    parser pack rebase socket pack rebase ref  # 6
    socket stream pack tree  # 7
    branch rebase window queue index window rebase remote  # 8
    delta commit stream stream tree pack delta worker  # 9
    socket merge merge blob object worker tree  # 10
    queue parser index pack  # 11
    stream socket remote pack tree stream branch  # 12
    delta stream commit handle worker branch buffer object handle  # 13
    stream remote blob header branch buffer  # 14
    pack cache buffer blob rebase blob parser rebase token queue handle  # 15
    worker socket rebase cache header  # 16
    worker tree window branch pack tree worker  # 17
    pack branch ref worker socket parser ref  # 18
    queue index header header window merge  # 19
    parser parser pack commit object  # 20
    merge token object socket rebase queue commit cache tree merge pack rebase  # 21
    index index delta blob cache remote  # 22
    blob index blob tree merge stream pack header delta blob worker  # 23
    stream object commit delta ref delta commit queue header  # 24
    merge window merge commit parser socket token delta handle window buffer  # 25
    stream blob stream remote socket  # 26
    delta queue index tree handle cache parser socket delta object  # 27
    cache merge object parser merge commit window socket  # 28
    blob blob handle queue rebase window queue cache queue header branch  # 29
    token index ref delta rebase buffer  # 30
    stream pack socket worker index delta stream index commit delta rebase tree  # 31
    header queue cache index  # 32
    object socket merge object  # 33
    remote merge blob remote worker tree worker window index pack queue worker  # 34
    header header ref token buffer remote  # 35
    index header cache tree header window delta socket delta parser handle  # 36
    buffer parser blob rebase  # 37
    buffer merge object handle window parser  # 38
    object buffer index remote cache header window delta pack socket stream  # 39
    rebase worker ref commit delta rebase token stream delta cache object worker  # 40
    worker index header handle queue  # 41
    pack buffer token remote delta  # 42
    tree merge ref index ref stream pack remote commit parser commit  # 43
    window window parser commit worker rebase  # 44
    worker buffer queue queue handle blob object branch  # 45
    rebase buffer rebase commit parser  # 46
    token handle commit ref  # 47
    buffer socket tree tree buffer blob  # 48
    blob branch tree delta merge socket socket header  # 49
    queue queue stream parser header window  # 50
    commit delta ref pack ref rebase queue pack  # 51
    stream rebase commit blob blob buffer cache  # 52
    parser socket queue remote  # 53
    handle buffer queue cache ref socket rebase ref branch  # 54
    stream index branch window token commit ref tree  # 55
    blob merge index cache object  # 56
    delta buffer stream token tree commit blob window delta blob handle  # 57
    token index rebase handle object branch token  # 58
    socket ref worker branch  # 59
    token pack commit socket blob branch commit  # 60
    ref index socket commit commit object  # 61
    delta cache token token worker commit worker rebase  # 62
    parser tree commit index cache remote window worker  # 63
    rebase parser cache cache branch merge  # 64
    buffer queue cache rebase window worker  # 65
    commit worker parser header branch handle ref token  # 66
    stream merge window queue parser ref token delta branch branch delta  # 67
    rebase object commit cache parser index tree rebase ref  # 68
    delta object tree stream tree  # 69
    pack delta pack handle buffer stream  # 70
    remote stream ref parser rebase handle pack  # 71
    worker window worker object remote token handle object parser merge  # 72
    window header merge buffer header branch commit window worker worker  # 73
    blob buffer header branch cache index commit header delta socket branch  # 74
    worker merge token commit worker index socket rebase  # 75
    handle tree header buffer remote rebase index  # 76
    handle parser socket rebase stream handle merge  # 77
    stream queue branch token  # 78
    header branch queue parser pack tree parser header commit commit  # 79
    socket buffer tree cache blob parser buffer header  # 80
    window parser pack index worker branch  # 81
