    token buffer merge queue remote header merge queue  # 0
    token worker queue pack ref socket cache pack stream branch handle  # 1
    blob rebase parser remote commit pack socket index  # 2
    merge worker worker ref  # 3
    queue worker worker object delta pack rebase blob header delta merge stream  # 4
    merge worker socket commit ref ref handle commit branch remote  # 5
    token header blob branch queue branch commit tree index index  # 6
    delta window stream tree ref header token socket pack header  # 7
    pack parser index delta worker  # 8
    stream parser cache cache handle  # 9
    token worker ref ref queue branch pack delta  # 10
    parser ref stream index cache rebase index parser branch merge parser  # 11
    header header branch branch tree header  # 12
    branch token ref branch worker tree  # 13
    handle buffer stream handle tree cache ref window delta object  # 14
    delta merge merge socket pack worker index commit branch  # 15
    index buffer ref handle parser socket rebase buffer window parser delta header  # 16
    remote worker tree cache commit queue tree cache worker delta cache  # 17
    handle index tree blob tree branch header handle  # 18
    commit socket tree header token buffer  # 19
    commit cache object index window header parser pack rebase  # 20
    worker pack stream rebase  # 21
    header blob blob parser worker tree branch blob blob remote remote  # 22
    object branch window queue cache ref handle branch window  # 23
    rebase window remote header object parser cache parser worker  # 24
    tree blob merge token ref  # 25
    commit socket index remote branch pack  # 26
    object remote header queue merge branch queue window  # 27
    parser commit ref header remote commit socket buffer  # 28
    window pack stream token stream rebase branch branch worker window pack  # 29
    blob worker branch header window  # 30
    commit commit parser pack commit pack index ref window socket object handle  # 31
    merge tree parser merge pack blob worker commit  # 32
    cache header index ref socket blob index pack object  # 33
    buffer queue index buffer commit branch buffer object token  # 34
    worker parser stream remote header tree queue  # 35
    branch commit rebase object tree worker commit commit index ref  # 36
    object pack pack blob socket blob queue parser ref index tree  # 37
    pack delta token rebase  # 38
    ref object object remote parser pack rebase header window  # 39
    remote commit handle token tree  # 40
    socket tree index object tree token cache header socket queue rebase  # 41
    queue merge merge rebase buffer handle merge  # 42
    delta delta cache header ref delta token remote rebase  # 43
    branch blob stream branch rebase  # 44
    remote header pack token stream queue header handle branch delta stream  # 45
    object delta worker commit index object socket window  # 46
    ref object queue buffer remote worker commit pack tree socket  # 47
    socket queue stream cache blob remote object queue branch header object  # 48
    remote rebase rebase ref ref buffer queue token rebase stream  # 49
    socket commit branch socket ref remote tree window cache blob object header  # 50
    ref pack token remote socket cache index index blob socket pack  # 0
    stream tree parser stream delta delta ref  # 1
    remote tree pack merge pack cache  # 2
    socket branch branch worker rebase header ref pack object  # 3
    object object merge delta header commit object  # 4
    parser rebase cache token pack  # 5
