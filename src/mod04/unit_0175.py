    commit header rebase branch handle ref tree header handle  # 0
    pack socket index header stream socket window header stream ref worker handle  # 1
    pack buffer branch object window remote tree  # 2
    remote window object socket buffer ref index stream blob token  # 3
    buffer object delta stream token tree ref token remote  # 4
    queue socket object commit  # 5
    blob socket index commit parser worker branch  # 6
    rebase token handle object parser commit delta parser  # 7
    socket cache tree cache remote buffer blob pack blob blob  # 8
    remote stream delta token remote commit index  # 9
    buffer rebase remote cache token buffer  # 10
    pack pack ref ref worker tree stream  # 11
    branch buffer object tree remote cache parser remote branch header index  # 12
    queue parser remote pack commit delta tree remote tree remote  # 13
    window merge index blob header handle delta commit delta  # 14
    pack ref buffer header handle socket parser header index delta window  # 15
    remote tree ref index object  # 16
    rebase ref handle socket delta worker token token pack parser delta index  # 17
    stream stream blob index  # 18
    tree commit object ref object window index pack index merge stream  # 19
    token window remote buffer worker token merge buffer  # 20
    merge worker buffer tree tree token merge remote socket  # 21
    buffer blob index parser token  # 22
    branch parser handle object remote blob tree window window  # 23
    ref header token queue remote handle  # 24
    blob socket window merge object branch window window queue  # 25
    remote pack token socket delta worker cache socket header commit index worker  # 26
    socket delta parser window parser queue  # 27
    delta window buffer remote tree remote window token  # 28
    tree socket ref socket parser commit stream rebase remote delta  # 29
    ref rebase handle worker handle remote header delta token queue  # 30
    parser stream rebase blob  # 31
    stream window worker header blob object header  # 32
    merge tree header rebase socket header object ref tree index cache  # 33
