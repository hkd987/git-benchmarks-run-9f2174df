    socket socket ref merge branch delta header ref  # 0
    pack pack worker parser  # 1
    socket stream delta rebase cache blob tree pack delta worker index delta  # 2
    stream delta tree handle index queue branch object cache branch header  # 3
    cache merge buffer window queue commit token  # 4
    remote parser header branch pack  # 5
    socket queue ref buffer merge buffer rebase remote  # 6
    commit pack window header remote  # 7
    blob worker handle tree token worker header branch ref rebase ref  # 8
    delta window blob remote pack handle rebase branch commit handle buffer parser  # 9
    branch tree branch queue header cache object ref tree  # 10
    header cache queue parser pack rebase branch remote object parser branch pack  # 11
    branch blob delta rebase rebase blob  # 12
    stream stream parser remote stream merge parser handle buffer blob  # 13
    object ref stream commit window  # 14
    queue stream window remote ref  # 15
    index header token branch cache cache worker socket delta handle worker  # 16
    window commit worker handle blob queue tree pack branch  # 17
    commit remote remote branch  # 18
    token token token handle cache header object header ref delta  # 19
    handle worker token ref commit worker commit remote  # 20
    buffer cache commit object stream window blob index ref pack merge pack  # 21
    remote index handle commit  # 22
    branch socket commit rebase pack worker index cache  # 23
    commit tree merge window socket queue rebase parser rebase header branch  # 24
    ref delta socket handle remote queue stream window  # 25
    pack buffer blob remote merge token queue socket merge remote parser worker  # 26
    worker pack parser pack commit branch delta object merge parser cache  # 27
    blob socket parser blob tree blob index rebase remote token  # 28
    rebase blob remote remote socket  # 29
    pack branch handle queue remote index delta branch token handle pack  # 30
    window delta pack ref ref delta  # 31
    ref cache window ref branch stream blob  # 32
    remote buffer window object buffer token object  # 33
    object merge blob blob header socket  # 34
    handle pack worker token object  # 35
    tree object remote ref branch commit queue parser worker parser  # 36
    remote buffer merge commit queue delta  # 37
    cache stream socket remote window queue branch rebase  # 38
    pack token window token pack window  # 39
    delta window worker remote  # 40
    remote blob socket commit commit tree merge socket socket token stream blob  # 41
    stream pack window stream socket delta queue blob handle  # 42
    handle tree buffer socket window rebase delta buffer  # 43
    commit commit socket tree ref index delta delta worker pack tree  # 44
    delta token parser delta cache socket blob delta parser branch commit pack  # 45
    cache buffer queue index window buffer commit queue socket branch  # 46
    token buffer pack header index stream object merge worker stream  # 47
    object header branch tree delta queue blob delta  # 48
    worker socket remote stream commit rebase  # 49
    commit token blob stream ref queue  # 50
    rebase remote merge worker object cache  # 51
    header branch pack blob stream header handle blob header queue  # 52
    buffer tree buffer delta object delta window  # 53
    window socket worker ref buffer cache socket merge  # 54
    buffer tree queue branch merge window index socket window ref branch header  # 55
    worker header tree token delta stream queue remote rebase  # 56
    delta commit merge queue merge parser remote  # 57
    index ref stream parser tree blob object commit rebase cache rebase object  # 58
    header object tree delta token worker merge commit parser  # 59
    window blob tree parser cache parser header  # 60
    socket header branch merge token socket  # 61
    stream parser pack stream buffer  # 62
    queue merge socket cache queue remote remote queue  # 63
    token cache worker token window  # 64
    socket remote remote token  # 65
    delta stream object rebase blob worker rebase commit parser rebase stream  # 66
    handle queue index remote buffer window remote parser cache ref remote socket  # 67
    blob commit merge parser index  # 68
    remote merge merge commit parser merge queue token header queue tree  # 69
    merge delta ref index index branch window  # 70
    delta blob commit stream delta queue rebase branch  # 71
    merge parser window pack buffer  # 72
    delta merge blob pack window blob delta delta  # 73
    blob rebase pack token handle pack index queue tree  # 74
    window merge branch blob buffer  # 75
    remote commit header commit header parser branch window header index  # 76
    object header rebase blob window commit socket object rebase handle blob  # 77
    queue worker queue commit  # 78
    delta ref ref remote blob socket handle queue parser  # 79
    ref merge rebase index blob  # 80
    remote rebase delta buffer delta worker tree commit buffer queue buffer  # 81
    handle parser branch token object header window rebase  # 82
    rebase merge queue cache parser merge parser  # 83
    header cache stream header window token object pack window buffer branch buffer  # 84
    queue queue ref ref cache cache ref  # 85
    blob token token merge header queue stream worker token parser handle  # 86
    cache branch tree buffer  # 87
    queue blob socket delta  # 88
    object cache merge commit  # 0
    delta blob handle commit merge queue buffer index header commit cache  # 1
    ref cache buffer tree index token worker branch  # 2
