    tree index object token delta branch header delta cache  # 0
    window socket stream socket queue ref buffer  # 1
    blob handle buffer window cache  # 2
    stream delta rebase handle object queue rebase pack handle buffer blob  # 3
    index merge commit remote  # 4
    delta token queue merge worker parser  # 5
    cache buffer buffer parser socket object socket branch commit ref remote  # 6
    delta branch pack blob pack cache ref object index buffer buffer  # 7
    stream branch object index index index  # 8
    parser buffer commit token  # 9
    tree header branch rebase delta commit blob buffer object parser delta remote  # 10
    branch header remote branch tree tree parser blob branch  # 11
    rebase cache header object  # 12
    blob branch index queue stream cache parser blob window tree index tree  # 13
    header branch header socket token delta  # 14
    cache handle rebase merge  # 15
    worker buffer window rebase queue worker  # 16
    ref remote branch socket branch token rebase handle handle index  # 17
    object rebase window tree worker commit worker  # 18
    pack merge handle token  # 19
    buffer tree merge buffer  # 20
    window rebase handle ref pack ref socket  # 21
    stream index worker stream commit parser rebase  # 22
    buffer parser merge cache parser commit header worker ref remote socket  # 23
    buffer cache object rebase handle parser queue object  # 24
    worker cache queue pack window socket handle  # 25
    object rebase buffer delta pack blob handle buffer commit ref socket  # 26
    merge window worker buffer commit merge blob stream remote  # 27
    window worker delta tree  # 28
    socket buffer ref window delta cache  # 29
    blob rebase handle blob  # 30
    object worker delta worker cache worker commit blob blob header  # 31
    header object worker cache  # 32
    buffer handle buffer token blob commit object delta buffer  # 33
    queue queue parser handle tree merge ref ref worker parser  # 34
    socket handle queue commit  # 35
    header pack worker token blob branch cache  # 36
    pack index merge tree remote rebase index buffer  # 37
    ref worker merge stream  # 38
    pack handle parser socket handle  # 39
    branch header merge handle token tree socket token remote rebase header  # 40
    remote remote stream buffer socket rebase stream pack object cache  # 0
    branch queue index branch queue merge tree token branch  # 1
    header header tree branch socket merge index stream  # 2
    rebase handle pack remote buffer buffer ref  # 3
    worker index rebase rebase  # 4
    branch rebase merge queue cache window queue worker  # 5
    pack merge tree handle ref rebase window merge  # 6
    branch rebase blob cache worker  # 7
    window buffer socket cache  # 8
    window token pack token rebase index rebase branch  # 9
    header token window branch tree branch  # 10
