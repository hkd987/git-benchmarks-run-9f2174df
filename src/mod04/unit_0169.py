    queue handle merge handle  # 0
    rebase header socket socket remote commit commit window rebase rebase  # 1
    buffer stream header buffer pack stream pack parser rebase token remote  # 2
    ref blob buffer worker stream  # 3
    pack remote pack pack parser index merge header delta branch buffer merge  # 4
    merge ref worker cache index branch pack object  # 5
    window queue handle merge branch index commit merge parser  # 6
    index token delta object token socket stream object  # 7
    worker commit blob stream cache  # 8
    ref queue token socket tree tree header handle object  # 9
    worker blob header branch handle index  # 10
    worker index merge stream header tree  # 11
    worker parser parser remote handle  # 12
    window token worker index queue header stream buffer stream  # 13
    object pack branch delta  # 14
    merge cache remote index cache delta header blob window remote  # 15
    remote delta pack worker branch  # 16
    rebase header stream queue ref header  # 17
    tree buffer delta commit handle rebase index socket cache  # 18
    object handle tree cache socket  # 19
    stream blob commit object blob rebase cache index header remote pack  # 20
    parser branch queue index header merge cache index merge token  # 21
    parser rebase parser remote blob tree worker header queue tree  # 22
    handle parser socket rebase handle  # 23
    ref object worker token parser index window  # 24
    cache cache socket rebase branch token merge header socket  # 25
    rebase token header tree worker blob  # 26
    worker socket parser ref  # 27
    header header parser worker ref branch delta ref  # 28
    index worker index rebase object  # 29
    pack socket socket branch  # 30
    remote index ref object ref header parser  # 31
    merge commit header pack object stream  # 32
    handle parser branch buffer blob  # 33
    branch handle delta rebase  # 34
    remote rebase pack socket ref window handle window  # 35
    parser worker rebase header handle pack merge branch rebase  # 36
    ref socket blob ref window buffer  # 37
    pack pack header token cache pack  # 38
    branch index index pack worker parser ref handle blob handle socket branch  # 39
    token pack pack window tree  # 40
    stream stream buffer index commit window header tree branch remote buffer cache  # 41
    parser cache delta pack blob object worker stream token pack merge header  # 42
    remote socket handle blob remote queue tree header socket token header  # 43
    branch remote stream blob queue stream pack header  # 44
    queue ref stream commit header socket token token window object object ref  # 45
    rebase object merge handle queue remote header pack  # 46
    stream rebase token parser merge merge pack pack object  # 47
    remote queue commit token rebase cache parser  # 48
    index buffer parser stream  # 49
    worker commit stream branch index handle branch  # 50
    handle stream commit merge pack header stream buffer stream object  # 51
    commit token header index commit token buffer token commit header delta delta  # 52
    commit ref ref blob index header pack parser handle rebase buffer ref  # 53
    object buffer cache commit parser parser handle merge worker socket index  # 0
    worker token parser index tree  # 1
    merge queue commit worker queue object object branch handle cache  # 2
    parser token blob object parser header parser pack blob  # 3
    queue socket header socket  # 4
    delta commit header worker  # 5
    delta ref tree handle merge merge  # 6
