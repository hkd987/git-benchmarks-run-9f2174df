    tree worker stream blob branch object  # 0
    header tree token token blob ref window remote stream queue  # 1
    worker handle merge tree blob parser  # 2
    tree stream ref cache remote branch index blob  # 3
    worker header merge blob merge stream object  # 4
    window stream pack window commit delta index header branch commit worker  # 5
    commit stream stream window index buffer merge  # 6
    header ref branch object branch commit token worker  # 7
    token rebase buffer pack window handle  # 8
    token stream blob merge blob worker cache pack object branch token branch  # 9
    socket queue tree header  # 10
    handle index tree ref remote rebase cache socket queue  # 11
    rebase socket worker handle branch parser ref commit branch rebase index cache  # 12
    blob handle rebase token worker merge  # 13
    queue queue stream index remote branch window rebase parser socket delta socket  # 14
    ref object tree pack buffer  # 15
    rebase remote delta window  # 16
    branch socket remote remote rebase header  # 17
    cache commit blob pack remote socket tree  # 18
    pack header queue window worker cache merge  # 19
    delta handle rebase commit branch window worker worker ref tree handle  # 20
    parser commit worker delta tree merge  # 21
    branch object queue index blob index index branch header handle delta worker  # 22
    rebase object pack ref remote socket  # 23
    remote commit ref delta window blob header pack  # 24
    header object parser cache header stream socket branch handle delta token  # 25
    socket index ref token delta tree commit  # 26
    parser commit header merge socket stream object  # 27
    index buffer ref window handle index  # 28
    header tree delta cache commit tree queue  # 29
    remote branch buffer window index worker index pack index remote socket queue  # 30
    object worker commit object  # 31
    delta tree parser token blob parser queue remote pack header  # 32
    cache token index rebase cache cache tree blob window parser index index  # 33
    parser delta delta buffer buffer blob remote branch ref ref ref  # 34
    stream socket branch socket branch  # 35
    parser blob branch delta delta  # 36
    window parser socket socket parser commit merge remote header buffer tree  # 37
    object stream tree merge header socket stream  # 38
    remote pack commit index blob delta  # 39
    branch merge token object blob stream window remote cache  # 40
    stream header delta buffer buffer merge merge socket socket tree  # 41
    parser cache commit cache cache  # 42
    handle token blob cache header branch handle window object ref  # 43
    branch socket index commit  # 44
    handle tree tree merge socket  # 45
    object handle rebase queue parser ref  # 46
    window object header queue parser delta ref object blob  # 47
    rebase ref stream ref remote token  # 48
    pack handle pack handle buffer remote  # 49
    rebase header remote token index remote worker worker delta object cache  # 50
    pack branch blob tree delta commit pack index tree ref  # 51
    blob cache queue parser socket commit socket rebase index  # 52
    parser worker token tree window window queue  # 53
    object object cache cache commit blob merge  # 54
    commit stream rebase index worker window merge socket  # 55
    commit stream remote window parser  # 56
    tree handle buffer cache worker handle blob blob token window  # 57
    parser object index handle commit delta object rebase header tree  # 58
    branch cache window index branch pack  # 59
    ref branch stream blob branch blob object commit commit worker merge  # 60
    stream merge buffer merge rebase commit  # 61
    blob ref ref delta rebase  # 62
    merge parser branch parser worker tree commit tree tree buffer tree parser  # 63
    queue object tree buffer tree stream cache delta handle  # 64
    pack queue tree cache tree rebase blob  # 65
    blob merge branch rebase branch object header index queue delta merge  # 66
    buffer blob parser index blob branch index queue buffer pack  # 67
    socket commit blob index object rebase cache branch worker socket socket tree  # 68
    queue ref queue parser object buffer merge delta window token object  # 69
    socket index rebase tree worker  # 70
    cache object blob stream merge buffer branch commit blob  # 71
    delta rebase branch worker cache object parser commit socket parser  # 72
    window branch blob index tree merge commit object delta parser remote token  # 0
    buffer stream ref index tree parser delta worker socket object  # 1
    merge buffer branch object window branch object handle  # 2
