    buffer cache branch ref remote branch object rebase stream branch buffer token  # 0
    merge blob handle stream delta buffer parser remote window tree  # 1
    worker tree parser worker socket branch object commit parser stream  # 2
    parser merge queue index merge  # 3
    parser handle object queue  # 4
    delta handle handle parser queue  # 5
    window stream remote buffer socket window object delta  # 6
    object remote parser header buffer rebase window branch header  # 7
    buffer handle ref delta object ref commit stream remote  # 8
    worker buffer pack handle worker commit rebase socket stream rebase  # 9
    blob merge blob commit token  # 10
    remote socket worker stream object socket rebase  # 11
    queue header rebase commit worker  # 12
    tree cache remote ref queue pack parser socket queue socket  # 13
    branch remote socket token index socket ref  # 14
    index blob handle header window parser header rebase blob  # 15
    stream remote window handle rebase  # 16
    index header socket index buffer  # 17
    parser worker commit branch branch  # 18
    queue header parser delta worker buffer rebase queue worker token cache  # 19
    delta rebase index queue socket  # 20
    ref token index window parser stream  # 21
    remote stream handle index buffer pack rebase parser merge token  # 22
    remote merge delta stream object header token  # 23
    parser worker ref pack index cache object commit token tree worker remote  # 24
    index remote buffer commit  # 25
    rebase socket blob stream commit  # 26
    commit socket header window  # 27
    remote rebase branch socket parser object tree branch handle  # 28
    delta tree branch blob token  # 29
    cache commit index parser header rebase stream ref object window buffer  # 30
    socket queue delta remote queue delta merge worker  # 31
    tree window header cache ref token rebase queue blob window ref  # 32
    delta ref pack branch worker tree handle token index blob cache token  # 33
    parser commit remote header  # 34
    worker merge cache socket worker token index  # 35
    pack header socket ref merge buffer  # 36
    pack worker tree tree commit delta queue blob delta worker commit  # 37
    parser handle blob delta queue token  # 38
    handle branch remote object buffer stream stream branch queue  # 39
    stream blob cache socket buffer header header ref commit branch socket  # 40
    buffer parser object index remote stream ref  # 41
    queue worker socket commit window stream merge  # 42
    commit merge commit token parser buffer branch tree  # 43
    worker parser queue index merge token ref tree branch blob  # 44
    branch header worker merge window rebase socket parser rebase  # 45
