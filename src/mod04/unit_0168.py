    header window index header socket pack remote  # 0
    blob token window remote window token  # 1
    parser branch branch ref buffer  # 2
    remote branch cache index buffer pack tree  # 3
    pack pack worker object parser delta object tree buffer remote queue parser  # 4
    header remote buffer merge blob  # 5
    header token header window merge parser stream blob worker  # 6
    token cache handle stream object index handle socket object  # 7
    rebase merge blob stream blob buffer remote stream token  # 8
    index buffer branch blob commit delta remote commit commit commit tree rebase  # 9
    commit object index handle blob handle token  # 10
    queue ref merge merge buffer  # 11
    queue tree tree cache window socket blob commit  # 12
    stream blob handle pack tree socket delta merge tree pack parser buffer  # 13
    merge cache stream object commit  # 14
    header stream queue ref  # 15
    window queue cache merge delta stream worker ref tree pack  # 16
    parser cache window index branch stream  # 17
    parser stream index header parser ref worker merge  # 18
    token stream buffer header pack ref  # 19
    header rebase worker token  # 20
    queue merge blob pack handle token parser  # 21
    delta index delta worker remote merge ref header pack index remote window  # 22
    delta socket queue blob queue socket commit  # 23
    header delta merge cache delta header parser branch parser ref ref delta  # 24
    rebase pack window merge  # 25
    socket queue ref object handle socket cache token  # 26
    object rebase delta pack blob header parser window blob  # 27
    ref window index window tree socket handle index  # 28
    cache ref queue cache commit handle remote rebase ref token parser delta  # 29
    rebase parser branch pack ref buffer  # 30
    parser worker remote handle remote token token object queue  # 31
    merge queue merge stream rebase cache index worker object blob delta stream  # 32
    cache blob handle delta parser ref pack worker token blob  # 33
    handle handle token blob object stream buffer  # 34
    ref blob index index worker token header token  # 35
    parser merge merge pack index commit handle  # 36
    buffer remote blob token index worker branch handle worker stream  # 37
    token parser rebase token  # 38
    handle remote object pack socket rebase handle handle rebase  # 39
    delta tree stream queue queue merge rebase branch branch ref  # 40
    token token queue index parser  # 41
    ref cache header index handle ref handle rebase  # 42
    socket object pack index commit parser merge cache window worker socket  # 43
    window delta parser header cache stream  # 44
    merge delta blob commit tree rebase index parser tree token handle ref  # 45
    tree merge pack worker remote ref socket rebase worker delta merge  # 46
    parser merge window ref  # 47
    commit merge merge worker token delta branch blob buffer  # 48
    commit parser window tree index token merge rebase  # 49
    cache queue branch stream socket rebase buffer stream tree index token branch  # 50
    rebase pack rebase index worker rebase buffer token buffer  # 51
    window delta stream parser index branch merge branch stream  # 52
    tree remote commit socket branch buffer rebase branch index worker parser  # 53
    header worker object buffer stream cache  # 54
    buffer commit queue window parser  # 55
    parser header window window parser window index token delta buffer  # 56
    socket index cache parser handle  # 57
    branch commit socket stream parser tree branch queue delta worker  # 58
