    ref cache socket rebase parser  # 0
    delta blob pack tree window buffer remote  # 1
    commit header worker tree queue token ref header branch queue tree index  # 2
    merge worker window cache delta queue parser header delta  # 3
    socket delta token object window header merge delta pack queue  # 4
    queue buffer rebase index object stream blob ref  # 5
    buffer ref pack tree branch commit parser tree window tree merge token  # 6
    pack tree cache rebase blob cache merge index tree handle token  # 7
    window stream blob token pack rebase parser tree token  # 8
    merge handle index blob  # 9
    handle parser ref branch socket pack object handle index window pack socket  # 10
    index delta commit branch blob  # 11
    object stream cache stream remote remote queue  # 12
    ref buffer pack object token remote window token  # 13
    pack cache tree commit branch stream rebase worker branch  # 14
    cache branch rebase merge stream delta cache commit pack window buffer buffer  # 15
    merge cache blob stream handle parser worker rebase queue remote  # 16
    parser tree object tree window parser  # 17
    header blob token parser socket  # 18
    rebase header pack blob cache  # 19
    buffer buffer ref delta tree handle queue object token  # 20
    index branch index tree ref  # 21
    window cache index object tree ref  # 22
    queue handle commit window stream handle delta  # 23
    merge branch commit worker  # 24
    parser parser delta remote handle blob  # 25
    queue pack commit tree tree  # 26
    token pack header remote worker index handle  # 27
    worker buffer stream window queue ref  # 28
    stream merge delta worker window remote buffer cache rebase cache worker  # 29
    blob object window pack blob tree tree ref object commit worker  # 30
    object buffer object pack remote commit index socket delta merge parser window  # 31
    handle rebase header delta commit tree  # 32
    commit rebase ref worker worker token handle queue header window index branch  # 33
    buffer index cache branch socket socket buffer stream parser queue commit commit  # 34
    tree header merge stream handle tree window index parser  # 35
    branch blob commit window merge branch commit stream commit  # 36
    branch worker queue blob window worker handle  # 37
    handle buffer cache token cache header buffer  # 38
    parser merge index token pack parser rebase remote remote parser  # 39
    rebase parser worker pack merge worker stream header blob stream tree  # 40
    tree socket buffer blob commit tree handle remote merge rebase  # 41
    queue delta stream tree handle blob token commit  # 42
    ref window window cache  # 43
    pack stream tree ref socket queue remote token cache buffer branch  # 44
    ref ref parser window pack object blob pack cache handle  # 45
    queue delta parser cache commit socket  # 46
    cache rebase worker parser parser parser tree  # 47
    ref token blob window header worker token object  # 48
    worker object delta buffer branch  # 49
    remote parser stream ref  # 50
    handle blob stream branch tree queue merge token socket commit tree tree  # 51
    parser blob window delta cache  # 52
    blob tree ref rebase handle pack object  # 53
    commit pack tree socket  # 54
    token delta branch token rebase branch socket  # 55
    ref object parser worker object object  # 56
    stream window index object window header queue header  # 57
    handle merge tree buffer  # 58
    branch blob index branch delta ref commit blob commit ref buffer  # 59
    socket token delta queue stream delta stream socket pack token remote remote  # 60
    branch buffer socket buffer  # 61
    object remote delta branch window commit handle stream object  # 62
    merge window handle pack ref rebase index ref  # 63
    header buffer socket remote index socket queue token ref handle  # 64
    object token pack cache merge queue parser stream socket parser tree  # 65
    stream worker remote header worker cache pack merge commit commit parser  # 66
    merge merge token socket header buffer branch worker commit pack cache handle  # 67
    tree pack handle worker ref  # 68
    socket object handle cache worker delta cache branch  # 69
    token branch worker delta header cache commit socket queue  # 70
    commit header token blob stream tree queue header window index index  # 71
