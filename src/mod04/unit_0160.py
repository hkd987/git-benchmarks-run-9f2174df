    branch merge pack token cache parser handle object  # 0
    handle header header token blob cache commit pack  # 1
    handle tree remote delta buffer blob branch handle buffer socket blob rebase  # 2
    worker merge branch index delta window worker ref ref handle  # 3
    handle merge rebase object ref index  # 4
    merge socket blob handle remote rebase remote branch merge branch ref  # 5
    header rebase queue object ref handle cache branch window parser index queue  # 6
    window header token commit tree  # 7
    blob queue worker branch merge header  # 8
    queue buffer remote object handle object object header worker object commit commit  # 9
    socket stream cache index ref  # 10
    merge index commit commit  # 11
    merge rebase socket socket cache token pack tree window  # 12
    parser commit index header worker  # 13
    window rebase remote worker object rebase worker  # 14
    branch merge socket index cache branch index  # 15
    branch parser branch socket ref worker remote  # 16
    merge handle index rebase blob queue ref worker index parser  # 17
    parser object delta buffer delta header pack ref cache  # 18
    delta index rebase index parser header socket window token merge  # 19
    worker worker remote parser merge token object index tree stream  # 20
    window header header parser header  # 21
    merge tree stream tree handle object rebase window delta index tree  # 22
    ref parser remote tree  # 23
    cache remote tree parser stream object blob stream worker queue  # 24
    object tree ref socket token pack parser worker ref  # 25
    pack commit remote blob handle  # 26
    pack merge token blob handle commit token window commit index blob tree  # 27
    token header index ref tree rebase  # 28
    tree commit pack pack blob parser rebase merge tree index blob  # 29
    index object ref worker parser window token socket window object stream merge  # 30
    pack tree ref header pack branch header window handle object cache  # 31
    header buffer pack socket parser delta branch  # 32
    stream token cache cache merge blob stream stream buffer remote buffer  # 33
    tree socket socket index worker ref commit tree handle pack delta  # 34
    window cache object delta index window tree worker pack handle  # 35
    worker handle header buffer worker branch queue remote header parser ref  # 36
    buffer window queue worker token header object merge stream rebase token tree  # 37
    handle branch merge rebase object cache handle object parser  # 38
    window header rebase object pack delta index  # 39
    ref token token object handle  # 40
    delta token handle handle pack branch buffer blob cache token  # 41
    token socket parser index index token parser ref parser window index  # 42
    header worker delta window  # 43
    merge rebase object header remote commit parser blob  # 44
    blob merge branch index ref merge blob stream handle  # 45
    delta cache handle cache parser window commit  # 46
    parser parser queue worker branch object delta index  # 47
    object token parser object delta  # 48
    worker ref handle delta parser token socket window handle socket queue parser  # 49
    cache delta merge delta worker worker pack commit handle  # 50
    parser handle blob queue cache header branch parser socket  # 51
    cache buffer handle parser branch blob cache queue blob pack cache  # 52
    rebase token merge parser ref blob handle commit window tree window index  # 53
    ref parser cache branch window commit pack pack merge parser window cache  # 54
    rebase object merge merge worker tree commit token  # 55
    buffer object index rebase buffer index parser queue worker  # 0
    window branch socket rebase tree worker remote header token queue header header  # 1
    branch pack stream commit  # 2
    blob socket commit tree pack merge  # 3
    blob window queue rebase window worker index socket parser  # 4
    cache branch handle token pack delta handle buffer cache branch  # 5
    delta remote index socket worker tree merge  # 0
    commit merge cache header ref tree stream rebase index cache header parser  # 1
    rebase commit parser merge branch  # 2
    token merge merge object header branch branch ref  # 3
    handle window tree tree  # 4
    parser remote tree header tree remote cache buffer commit  # 5
