    header handle cache header cache handle header  # 0
    handle queue ref branch rebase parser cache blob  # 1
    ref parser branch header  # 2
    cache commit parser window token token parser queue stream  # 3
    pack object parser branch delta  # 4
    ref queue buffer buffer handle rebase pack delta delta worker  # 5
    object header token delta blob worker socket queue  # 6
    worker worker delta rebase branch branch header  # 7
    tree merge socket worker stream remote branch commit blob merge window stream  # 8
    queue cache worker tree index stream tree  # 9
    token remote stream socket blob token delta remote handle commit window window  # 10
    rebase index rebase rebase index merge handle commit tree  # 11
    rebase stream rebase rebase  # 12
    delta token delta rebase window  # 13
    parser object queue stream stream remote parser cache tree header  # 14
    window rebase merge cache index blob blob header  # 15
    commit blob window window ref window ref blob  # 16
    window buffer merge branch object index object merge ref rebase index object  # 17
    commit handle token rebase  # 18
    cache handle worker branch handle pack commit ref branch  # 19
    ref queue parser pack stream ref  # 20
    buffer remote ref index index index merge merge delta  # 21
    merge tree rebase index tree window buffer worker header worker  # 22
    ref remote parser object rebase branch commit commit  # 23
    queue tree delta pack  # 24
    object token buffer stream queue socket cache tree blob buffer buffer object  # 25
    queue socket pack socket blob socket stream buffer delta index  # 26
    cache stream remote handle  # 27
    window stream index rebase stream rebase cache merge rebase merge  # 28
    cache index queue token merge delta object rebase rebase handle header merge  # 29
    parser buffer parser queue tree queue  # 30
    pack stream stream remote cache  # 31
    delta window cache tree commit cache stream parser blob blob handle ref  # 32
    token index queue window header handle handle  # 33
    handle tree commit index branch socket remote stream  # 34
    merge queue window rebase  # 35
    socket pack ref ref  # 36
    merge branch index stream queue socket buffer pack token  # 37
    commit pack merge cache index branch parser cache  # 38
    commit remote worker token pack window handle token merge  # 39
    stream ref branch tree cache delta delta tree parser cache commit merge  # 40
    blob ref token pack stream cache buffer branch blob queue stream queue  # 41
    tree window token handle commit ref remote header  # 42
    parser blob buffer index rebase queue window object handle pack parser index  # 43
    rebase queue ref object delta pack header token  # 44
    branch queue cache cache delta commit remote  # 45
    merge header socket blob window parser object worker branch delta ref  # 46
    buffer index token ref handle stream  # 47
    remote queue handle parser queue index worker  # 48
    queue rebase ref handle stream socket index window queue cache  # 49
    rebase stream merge pack window queue cache  # 50
    branch index window delta delta worker stream merge  # 51
    tree ref stream cache branch stream remote header token cache  # 52
    remote remote buffer remote delta  # 53
    commit stream cache ref  # 54
    object socket remote buffer handle buffer object parser blob  # 55
    index cache ref merge parser pack ref blob worker socket  # 56
    window tree object delta parser index  # 57
    tree branch ref header index  # 58
    handle index merge object pack branch  # 59
    delta pack stream stream tree  # 60
    index index delta blob socket stream stream index  # 61
    remote commit delta merge merge worker token  # 62
    object branch header remote ref branch commit socket window header object window  # 63
    worker stream blob remote  # 64
    tree buffer tree cache buffer rebase queue index socket  # 65
    header queue tree merge token window tree handle buffer tree  # 66
    tree pack socket token worker cache object object  # 67
    handle buffer window merge parser remote buffer blob ref  # 68
    stream window handle worker delta header ref window window cache  # 69
    merge parser window branch branch queue queue queue worker merge index  # 70
    pack buffer object socket cache ref branch token buffer  # 71
    delta rebase merge ref blob remote tree blob window rebase header token  # 72
    branch delta stream worker  # 73
    buffer object handle branch worker stream object buffer window cache  # 74
    remote branch delta buffer pack buffer index queue buffer ref cache  # 75
