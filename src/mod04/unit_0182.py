    cache worker stream parser  # 0
    handle header cache window parser commit pack commit token pack  # 1
    merge merge token worker  # 2
    pack merge branch index blob tree  # 3
    pack object handle pack object  # 4
    ref buffer parser merge token tree  # 5
    header handle commit queue tree queue  # 6
    cache header token rebase buffer queue queue rebase  # 7
    worker window commit handle remote pack ref window pack  # 8
    token queue merge index queue commit buffer  # 9
    rebase buffer tree merge rebase delta socket worker delta header  # 10
    parser socket ref queue delta queue  # 11
    buffer object socket ref socket remote parser parser index index  # 12
    queue object parser parser handle  # 13
    header stream handle window  # 14
    token rebase parser handle window merge pack  # 15
    delta rebase handle buffer  # 16
    tree branch header queue delta pack remote stream  # 17
    queue object parser index window merge stream remote  # 18
    buffer socket handle rebase socket rebase commit  # 19
    window handle commit rebase delta object pack cache window ref socket  # 20
    blob commit window cache  # 21
    commit token buffer ref queue stream rebase tree remote commit  # 22
    object socket cache token delta cache token worker cache rebase  # 23
    window merge tree branch token token remote rebase ref socket delta stream  # 24
    queue pack queue ref  # 25
    blob stream index merge handle cache ref index delta  # 26
    pack remote queue window ref merge queue index buffer object pack  # 27
    header window handle queue commit rebase  # 28
    object buffer stream commit rebase stream object remote ref ref  # 29
    merge commit delta ref token queue handle window  # 30
    object window commit blob blob cache stream  # 31
    cache window handle window object header cache window buffer parser  # 32
    delta queue socket parser delta merge branch queue merge tree ref  # 33
    token index merge commit parser header  # 34
    worker token stream delta blob blob merge handle index socket branch rebase  # 35
    index handle commit handle cache ref window buffer commit commit blob  # 36
    socket parser queue stream worker token blob socket  # 37
    commit tree delta branch buffer queue cache remote object queue buffer  # 38
    rebase stream window parser rebase header token stream buffer  # 39
    worker index remote handle buffer commit window ref socket  # 40
    remote worker header blob parser ref  # 41
    branch rebase ref queue branch tree handle buffer token pack parser  # 42
    socket handle remote remote object branch header blob  # 43
    rebase worker cache object tree rebase header handle index commit window cache  # 44
    parser cache header blob ref buffer queue rebase rebase blob branch  # 45
    cache header object worker cache ref tree delta blob  # 46
    token queue index merge worker commit  # 47
    handle rebase queue tree cache  # 48
    cache socket object cache blob branch pack commit window commit  # 49
    buffer index delta buffer commit blob  # 50
    worker socket header token stream merge rebase pack buffer ref buffer cache  # 51
    branch ref branch merge ref branch  # 52
    buffer commit window stream parser rebase object blob parser commit tree index  # 53
    buffer remote queue buffer token stream  # 54
    socket header tree branch  # 55
    branch cache blob remote delta pack stream  # 56
    branch ref branch pack blob cache rebase handle cache handle  # 57
    socket handle merge branch header rebase socket remote object  # 58
    window commit blob commit index tree index tree cache queue parser  # 59
    tree index token handle ref object cache queue window branch branch cache  # 60
    queue buffer handle parser stream token merge  # 61
    commit handle cache remote ref  # 62
    token token remote header  # 63
    remote index header index socket header tree  # 64
    header cache merge worker tree index stream buffer remote header tree parser  # 65
    blob index ref header merge  # 66
    merge socket cache tree header handle remote  # 67
    header header index object commit rebase merge  # 68
