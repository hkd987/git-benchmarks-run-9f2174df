    rebase merge header pack worker queue ref branch queue  # 0
    branch worker socket buffer blob rebase object ref  # 1
    remote queue object buffer rebase blob merge token blob stream object header  # 2
    token header queue handle object remote worker header  # 3
    index buffer delta buffer stream  # 4
    cache queue header commit merge branch  # 5
    worker index stream cache cache window branch  # 6
    header merge stream queue buffer  # 7
    delta buffer object queue stream  # 8
    header window buffer ref  # 9
    stream object ref blob commit  # 10
    pack header tree window header worker header tree stream commit merge pack  # 11
    object token pack handle parser header index stream  # 12
    cache cache socket rebase token header merge  # 13
    commit rebase worker queue cache rebase  # 14
    handle window header ref merge  # 15
    merge header remote header queue socket ref ref object ref ref  # 16
    handle tree blob token object stream commit remote buffer  # 17
    merge branch pack socket handle index token socket delta buffer tree index  # 18
    worker branch pack remote pack pack header queue index queue parser  # 19
    rebase index commit buffer  # 20
    worker socket merge pack branch buffer pack tree handle handle  # 21
    pack pack window delta queue branch handle worker branch merge worker  # 22
    ref remote window handle worker merge header socket  # 23
    branch socket commit remote ref pack worker  # 24
    delta pack handle handle worker buffer token commit  # 25
    queue parser ref branch  # 26
    buffer pack token cache parser index window queue object remote  # 27
    worker merge ref pack ref blob  # 28
    token commit parser merge object ref remote  # 29
    commit ref header token  # 30
    queue delta token socket remote tree handle  # 31
    cache object parser ref object handle cache object delta branch token token  # 32
    object window handle stream handle window worker  # 33
    blob rebase merge tree remote pack  # 34
    delta commit branch blob socket merge merge tree  # 35
    socket tree pack queue branch cache delta  # 36
    worker ref delta window buffer branch object stream socket  # 37
    tree header pack parser  # 38
    merge pack token delta buffer delta handle ref worker window cache  # 39
    stream remote handle pack rebase ref header handle window queue  # 40
    header parser queue merge buffer worker parser delta blob socket buffer  # 41
    blob worker token buffer delta parser parser queue index index handle merge  # 42
    object header commit parser object blob stream socket worker  # 43
    tree object worker index queue  # 44
    parser ref remote tree worker rebase buffer window tree  # 45
    stream handle branch buffer window cache window  # 46
    token blob socket branch merge handle remote stream buffer  # 47
    socket branch merge object parser stream window rebase tree branch index  # 48
    handle commit cache queue tree branch pack ref token  # 49
    tree socket delta stream pack  # 50
    blob buffer delta header window delta commit queue  # 51
    ref blob object token index branch  # 52
    buffer index merge window remote ref handle  # 53
    object token remote delta stream pack header branch blob commit parser  # 54
    commit pack rebase rebase worker object  # 55
    parser pack object tree remote parser cache index worker delta  # 56
    ref merge socket merge cache tree  # 57
    ref cache token stream commit index blob buffer  # 58
    token merge cache parser commit parser queue stream window remote rebase  # 59
    branch token token branch tree object  # 60
    handle window buffer commit  # 61
    worker window branch worker merge queue blob object stream blob branch  # 62
    buffer branch branch index branch rebase parser  # 63
    handle ref pack object parser  # 64
    stream socket window socket ref worker header stream handle remote socket blob  # 65
    rebase window window delta  # 66
    header cache buffer cache commit branch index commit queue delta ref merge  # 67
    tree index commit commit ref ref delta index token buffer  # 68
    worker handle header worker ref window tree ref token  # 69
    commit blob remote branch handle tree delta  # 70
    ref header stream branch token header cache window remote  # 71
    blob window ref object queue branch token buffer  # 72
    object cache object branch buffer  # 73
    ref object buffer tree stream commit token tree tree stream blob window  # 74
    branch merge pack remote stream  # 75
    window index index token worker index queue queue delta rebase  # 76
    tree parser merge rebase socket queue object merge tree merge  # 77
    token ref delta buffer buffer remote  # 78
    delta merge handle branch token queue object commit header stream blob  # 79
    index socket token stream object merge window buffer stream window  # 80
    token blob pack queue branch merge window commit ref cache  # 81
    remote rebase delta commit ref rebase pack buffer delta queue branch  # 82
    buffer header merge blob handle  # 83
    commit stream rebase socket  # 84
    worker queue tree worker merge buffer  # 85
    parser pack handle token branch delta queue worker  # 86
    header token remote remote tree rebase handle index blob  # 0
    rebase pack handle token queue merge merge  # 1
    object worker blob buffer remote token rebase remote buffer  # 2
