    worker remote token handle  # 0
    commit stream rebase object tree token object socket rebase token parser  # 1
    branch commit cache queue queue commit header  # 2
    index worker index object cache handle branch  # 3
    buffer pack object commit  # 4
    stream window parser stream merge index blob branch cache pack  # 5
    token parser worker window handle queue  # 6
    window header stream worker queue stream  # 7
    parser pack window branch blob  # 8
    object index handle worker  # 9
    buffer parser header header tree stream header merge object  # 10
    blob window stream pack commit  # 11
    object tree index token index blob remote blob commit commit socket  # 12
    cache pack queue pack blob rebase  # 13
    remote index tree delta cache window  # 14
    merge stream commit stream parser token window window blob  # 15
    ref delta pack window delta parser remote branch rebase token  # 16
    pack cache parser handle window socket cache blob stream window  # 17
    header blob worker object merge  # 18
    parser rebase index window cache  # 19
    merge tree window socket merge  # 20
    handle parser cache merge stream delta blob tree buffer token rebase  # 21
    tree index token pack buffer pack handle socket cache  # 22
    queue queue tree handle  # 23
    rebase stream ref remote queue parser remote token stream buffer window  # 24
    cache socket tree worker commit cache tree  # 25
    handle delta buffer remote socket handle stream token parser header window  # 26
    buffer rebase ref buffer  # 27
    worker object window parser delta  # 28
    commit remote header worker object stream queue  # 29
    buffer socket stream window  # 30
    token object remote rebase worker  # 31
    cache tree rebase commit object tree ref worker tree ref tree  # 32
    token blob window commit handle header merge  # 0
    worker ref handle parser queue  # 1
    buffer merge rebase rebase blob cache pack cache buffer socket  # 2
    rebase merge delta branch tree delta commit merge parser  # 3
    index ref tree ref token  # 4
    queue ref socket cache branch blob cache rebase ref  # 5
    object stream pack window tree handle index  # 6
    merge tree blob queue blob handle  # 7
    rebase handle object merge pack merge  # 8
    stream tree branch window rebase  # 9
    rebase tree ref worker header buffer token handle index merge commit token  # 10
    object buffer header cache remote worker  # 11
    queue buffer commit header parser socket token merge object remote  # 12
    pack socket window tree merge stream blob commit rebase socket stream  # 13
