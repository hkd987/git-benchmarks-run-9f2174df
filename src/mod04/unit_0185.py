    ref cache remote pack object remote window  # 0
    socket handle blob queue object token object buffer  # 1
    handle commit worker worker tree socket rebase merge  # 2
    merge socket remote remote token token index rebase ref  # 3
    socket blob pack pack socket parser delta commit branch parser  # 4
    blob buffer header socket delta window handle parser token  # 5
    token index header tree parser  # 6
    queue remote pack commit header index branch blob stream object handle  # 7
    commit window merge stream stream object  # 8
    merge commit token socket branch  # 9
    rebase object object token rebase parser  # 10
    remote rebase blob merge  # 11
    worker remote index pack rebase window buffer worker header worker blob  # 12
    object queue parser branch remote index stream object header remote socket blob  # 13
    index buffer handle stream header tree remote remote remote branch  # 14
    pack socket rebase blob stream  # 15
    stream worker ref buffer  # 16
    window remote cache handle header  # 17
    cache blob header cache cache branch worker  # 18
    header header tree branch commit blob handle merge index commit tree  # 19
    window commit index socket handle ref queue object object  # 20
    index socket tree header merge branch window buffer parser pack tree  # 21
    token commit token queue blob header queue pack token cache  # 22
    rebase token socket parser commit merge stream  # 23
    worker token index merge index ref buffer socket worker rebase ref  # 24
    index ref header parser parser remote pack token window header  # 25
    remote cache parser socket  # 26
    handle commit object merge parser handle pack pack header commit  # 27
    tree commit commit rebase commit  # 28
    cache queue pack merge delta token index  # 29
    branch commit delta pack queue tree buffer buffer merge  # 30
    worker stream rebase header queue blob token cache merge stream  # 31
