    stream buffer merge queue tree merge ref worker blob  # 0
    merge index parser branch branch tree commit commit pack cache token buffer  # 1
    header stream commit pack  # 2
    merge index queue handle window queue tree delta token  # 3
    parser handle ref tree socket cache  # 4
    index pack remote window blob token object socket socket stream stream  # 5
    tree ref delta socket socket tree tree socket remote stream stream worker  # 6
    index window window branch  # 7
    object ref ref cache delta stream  # 8
    window socket delta branch window  # 9
    token parser tree merge worker window index window  # 10
    cache window index worker buffer token remote parser parser window commit  # 11
    window socket cache index tree buffer stream delta worker  # 12
    window rebase commit object queue  # 13
    cache cache branch socket ref remote ref stream branch window header  # 14
    handle window pack buffer socket token branch merge header rebase cache object  # 15
    branch buffer commit object  # 16
    socket pack blob object header blob queue parser  # 17
    worker blob rebase window window queue stream index header  # 18
    remote branch ref buffer branch  # 19
    parser index pack header queue socket  # 20
    header branch delta rebase worker queue handle token  # 21
    handle branch branch rebase  # 22
    worker handle commit token stream stream parser ref  # 23
    merge handle handle tree commit pack ref token rebase worker remote window  # 24
    object stream buffer parser delta cache stream  # 25
    pack window merge buffer queue pack  # 26
    worker stream buffer ref branch socket blob index buffer blob parser cache  # 27
    handle socket parser header index  # 28
    remote token stream blob branch  # 29
    remote rebase handle queue socket  # 30
    cache header remote tree socket header branch blob branch handle parser window  # 31
    stream window branch ref buffer rebase commit  # 32
    queue buffer handle stream buffer socket cache remote cache merge pack header  # 33
    commit merge stream tree  # 34
    buffer buffer buffer window worker handle blob tree queue pack worker parser  # 35
    blob cache cache remote blob header object queue  # 36
    commit blob remote queue delta buffer cache ref parser tree socket merge  # 37
    worker pack stream header header ref  # 38
    rebase rebase commit socket rebase  # 39
    parser socket window tree pack  # 40
    header rebase buffer handle window pack buffer commit  # 41
    index token queue header object remote ref window ref ref socket stream  # 42
    stream pack stream buffer delta remote commit blob  # 43
    index tree token buffer object  # 44
    token token stream blob remote index index socket socket index  # 45
    ref worker socket index  # 46
    stream remote commit cache rebase buffer window  # 47
    commit cache commit buffer cache merge pack merge remote index  # 48
    worker tree stream index tree commit tree  # 49
    stream remote index blob stream queue tree rebase  # 50
    delta remote stream window queue branch queue pack handle  # 51
    remote buffer header buffer object handle ref window stream token socket pack  # 52
    object pack remote stream header remote buffer ref handle branch  # 53
    tree tree token commit object  # 54
    blob tree remote index blob  # 55
    object handle header buffer delta  # 56
    header ref token socket merge branch handle buffer object handle remote  # 57
    queue token object parser rebase worker parser cache commit blob  # 58
    ref remote header rebase  # 59
    header worker ref queue commit handle worker object queue merge  # 60
    cache token commit merge delta parser  # 61
    delta worker queue queue rebase header commit  # 62
    buffer token buffer object merge window stream  # 63
    ref socket worker rebase  # 64
    socket worker object cache worker branch merge commit window  # 65
    stream window stream stream merge  # 66
    cache worker rebase index tree parser index pack  # 67
    cache ref cache commit worker branch cache  # 68
    socket tree window buffer remote socket ref merge token pack  # 69
    branch merge rebase queue ref pack index parser blob worker branch header  # 70
    header pack rebase parser token window header stream window buffer commit  # 71
    stream branch delta handle remote parser cache delta  # 0
    worker buffer worker ref worker  # 1
    parser merge window object  # 2
