    index branch index delta handle token tree stream  # 0
    cache stream header remote object stream cache commit buffer window  # 1
    stream buffer merge token object delta queue rebase blob header handle  # 2
    buffer remote cache branch parser buffer delta token cache window rebase  # 3
    token tree stream queue rebase ref buffer branch pack  # 4
    queue cache commit merge queue object rebase merge header parser blob cache  # 5
    remote tree parser delta object header handle window  # 6
    cache worker merge ref header parser pack parser queue blob commit stream  # 7
    queue tree tree queue  # 8
    tree socket rebase commit commit commit remote branch worker  # 9
    remote token object cache  # 10
    blob remote blob parser merge commit index socket window socket socket  # 11
    stream rebase pack cache handle remote merge delta remote delta queue  # 12
    commit commit handle branch commit object delta  # 13
    worker index rebase rebase cache  # 14
    queue cache pack blob  # 15
    branch worker window branch queue header merge  # 16
    delta index remote cache  # 17
    branch branch commit handle window object buffer cache window token object  # 18
    pack blob blob worker worker handle  # 19
    worker object socket buffer remote  # 20
    index ref tree commit stream commit cache token rebase  # 21
    cache parser stream remote merge worker buffer commit  # 22
    remote parser remote parser header handle handle window socket  # 23
    rebase delta buffer merge socket branch stream index merge delta cache  # 24
    parser stream buffer branch tree socket delta window socket branch socket  # 25
    merge stream delta merge parser buffer socket pack parser queue stream tree  # 26
    token branch cache parser window queue rebase buffer  # 27
    blob parser branch merge window window branch cache blob  # 28
    ref pack merge token pack handle header token cache window buffer handle  # 29
    worker merge tree delta window blob token cache stream  # 30
    merge delta buffer index merge branch handle tree remote worker ref header  # 31
    socket merge cache handle worker merge delta blob index handle  # 32
    object window commit object remote cache  # 33
    stream header merge cache parser cache merge tree parser parser  # 34
    queue merge queue tree stream parser  # 35
    parser buffer parser delta queue parser  # 36
    ref remote merge ref ref commit token blob socket socket worker index  # 37
    socket blob pack window queue delta window header object stream tree window  # 38
    parser blob handle remote worker tree index  # 39
    delta worker rebase commit  # 40
    queue queue queue header rebase pack window  # 41
    tree buffer blob remote header ref merge worker ref worker  # 42
    delta handle commit ref ref remote object remote  # 43
    merge window token handle  # 44
    pack tree buffer rebase cache pack delta  # 45
    object merge object worker header branch object handle  # 46
    handle index ref blob commit index ref merge header worker  # 47
    window socket rebase branch commit commit merge worker rebase  # 48
    socket branch branch blob merge ref index object index branch pack  # 49
    rebase parser buffer socket  # 50
    socket merge cache remote blob  # 51
    queue header header cache worker window pack pack  # 52
    window branch merge buffer socket remote stream worker  # 53
    token socket tree header queue index worker token parser rebase  # 54
    stream commit socket rebase pack stream index object object pack  # 55
    parser pack merge blob handle blob cache remote ref header  # 56
    window stream tree object ref rebase  # 57
    token pack merge ref blob delta worker rebase  # 58
    pack header header rebase delta merge object stream worker queue  # 59
    queue ref token token commit stream queue  # 60
    remote parser stream tree merge tree  # 61
    queue pack buffer object ref remote window stream pack  # 62
    blob delta parser cache ref window token object index tree handle  # 63
    tree socket remote header tree  # 64
    parser index merge rebase  # 65
    socket window index index  # 66
    buffer index index pack remote rebase commit merge token rebase commit object  # 67
