    parser index handle stream token delta stream socket  # 0
    branch remote branch object blob handle stream branch pack buffer socket worker  # 1
    remote stream branch queue cache window index worker  # 2
    rebase cache object commit window rebase worker  # 3
    pack ref commit pack blob branch  # 4
    handle token ref token remote blob  # 5
    blob remote worker worker socket index merge stream window stream queue index  # 6
    stream socket worker remote parser queue socket index parser  # 7
    header worker window branch cache rebase commit  # 8
    window merge index header rebase buffer ref  # 9
    queue window window delta branch  # 10
    window queue token window token stream blob worker queue  # 11
    queue parser branch remote window delta branch parser token handle  # 12
    window blob handle parser socket header window pack  # 13
    socket cache tree token tree remote index  # 14
    stream commit remote parser pack merge parser  # 15
    ref socket remote window parser object  # 16
    tree merge index tree queue handle worker ref parser pack socket  # 17
    tree ref socket pack index  # 18
    socket header window ref handle branch blob ref rebase token pack index  # 19
    branch parser cache branch token window rebase  # 20
    branch window rebase remote  # 21
    queue remote parser queue queue header tree  # 22
    rebase delta branch index remote object header  # 23
    pack rebase remote remote handle merge cache  # 24
    remote branch object buffer  # 25
    parser cache commit index cache rebase header  # 26
    pack stream tree worker branch socket header object stream socket stream  # 27
    index header commit tree socket delta handle header socket object tree pack  # 28
    worker blob commit buffer window handle  # 29
    socket pack remote merge delta worker header parser parser delta tree buffer  # 30
    rebase handle stream buffer  # 31
    rebase ref tree worker buffer cache pack queue parser  # 32
    branch socket header parser object worker merge  # 33
    blob ref header commit socket token  # 34
    queue token socket blob tree queue  # 35
    rebase worker socket token worker header index  # 36
    merge tree branch stream index ref queue header delta delta  # 37
    remote commit buffer queue socket header handle tree  # 38
    buffer object queue ref token ref cache parser tree ref  # 39
    cache index worker ref handle blob index object token buffer  # 40
    stream object commit parser window cache queue ref  # 41
    branch token merge branch parser blob queue index worker  # 42
    parser pack merge remote ref branch object cache stream object  # 43
    pack tree blob handle worker token stream worker  # 44
    queue worker cache index handle branch worker stream  # 45
    pack stream worker rebase worker blob object tree object blob branch  # 46
    window stream branch cache branch window header buffer index window handle  # 47
    commit commit remote branch buffer  # 48
    handle header branch window remote cache branch token commit  # 49
    commit stream token blob pack merge cache queue remote handle  # 50
    handle parser ref ref buffer handle index  # 51
    object socket cache rebase ref delta header  # 52
    delta remote rebase pack merge pack parser  # 53
    window buffer index parser window parser queue branch commit  # 54
    branch tree ref ref header index parser object merge  # 55
    socket ref cache stream ref blob remote worker  # 56
    ref merge header object tree parser queue delta object delta  # 57
    header merge blob window object  # 58
    merge parser queue blob parser cache header pack token commit queue  # 59
    commit token stream cache ref blob tree branch blob token  # 60
