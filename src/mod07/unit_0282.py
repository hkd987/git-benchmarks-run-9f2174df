    tree buffer window index ref parser rebase index token header  # 0
    ref token merge stream  # 1
    branch stream pack window tree  # 2
    handle remote window ref  # 3
    index merge branch buffer pack queue delta cache buffer  # 4
    ref parser commit window commit cache queue blob commit blob  # 5
    commit ref stream delta worker handle pack blob  # 6
    ref cache buffer remote branch worker rebase object tree  # 7
    token parser parser buffer ref  # 8
    delta merge worker header token commit window ref remote worker socket buffer  # 9
    commit rebase blob index tree handle commit token pack pack handle rebase  # 10
    remote blob ref commit tree socket cache  # 11
    token worker socket commit handle remote  # 12
    socket remote worker object cache buffer pack pack  # 13
    object handle window socket socket header  # 14
    commit worker remote remote  # 15
    branch header commit remote stream pack object parser remote commit  # 16
    cache buffer queue delta object stream handle  # 17
    parser socket ref worker commit delta index buffer merge remote  # 18
    merge socket window handle rebase branch buffer ref blob  # 19
    object remote delta header header socket buffer remote worker stream ref  # 20
    buffer pack merge queue delta handle handle worker ref  # 21
    queue handle header merge token buffer  # 22
    queue blob pack socket delta worker handle  # 23
    rebase queue cache queue remote header token blob remote blob  # 24
    worker ref cache index  # 25
    header handle blob remote branch tree tree blob branch stream  # 26
    worker token remote cache tree queue handle  # 27
    pack token parser token ref header blob stream rebase buffer buffer token  # 28
    pack blob rebase branch rebase buffer  # 29
