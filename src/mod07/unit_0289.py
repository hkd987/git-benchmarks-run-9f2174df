    window token blob blob pack delta token window window commit merge  # 0
    ref remote cache delta window parser  # 1
    window handle parser handle socket stream branch  # 2
    delta commit queue stream commit index index remote  # 3
    ref commit token parser pack ref worker rebase delta rebase socket  # 4
    cache remote blob index handle  # 5
    merge remote remote parser object ref  # 6
    tree delta commit branch queue delta worker token handle  # 7
    tree rebase branch tree merge rebase object blob parser handle  # 8
    tree window buffer socket  # 9
    index handle buffer remote index rebase worker ref ref  # 10
    buffer token object rebase branch window object remote merge queue index  # 11
    parser index branch blob branch window object blob tree  # 12
    blob delta pack tree rebase  # 13
    pack buffer worker ref ref parser cache remote merge pack merge header  # 14
    parser header index merge cache header rebase branch merge remote worker cache  # 15
    blob window buffer rebase cache window stream index index  # 16
    branch parser rebase parser tree commit delta branch delta branch index  # 17
    header cache window tree token worker  # 18
    header tree parser merge header branch header pack rebase commit header  # 19
    commit ref stream blob stream index ref header remote stream buffer  # 20
    branch branch commit rebase parser  # 21
    index token branch object  # 22
    index queue delta commit remote  # 23
    worker rebase delta ref queue rebase remote pack index handle  # 24
    parser worker object ref socket pack  # 25
    header object remote cache merge  # 26
    remote delta socket parser remote  # 27
    remote queue index object  # 28
    pack blob tree socket  # 29
    commit rebase pack remote merge  # 30
    tree queue stream object tree pack socket cache blob branch  # 31
    rebase worker remote tree stream socket buffer buffer  # 32
    pack socket commit parser cache window  # 33
    handle queue index buffer  # 34
    worker tree worker parser object parser worker parser tree worker blob tree  # 35
    window object ref handle  # 36
    queue delta rebase parser  # 37
    tree stream socket object  # 38
    parser blob handle remote tree stream blob window handle queue  # 39
    stream rebase handle ref parser rebase object index cache index merge delta  # 40
    stream token object socket window queue handle  # 41
    branch branch merge commit index merge merge handle token cache parser index  # 42
    merge header queue token ref socket tree  # 43
    tree remote stream remote stream handle socket worker merge queue token  # 44
    socket merge blob socket header object blob commit worker stream branch  # 45
    blob stream delta handle cache cache socket worker tree  # 46
    delta ref parser handle handle commit  # 47
    object rebase commit queue  # 48
    commit buffer token handle tree parser socket delta queue  # 49
    ref index index socket queue buffer handle index object branch index  # 50
    blob cache blob index pack worker tree  # 51
    delta index buffer queue  # 52
    header socket queue index merge ref  # 53
    socket remote parser pack object blob  # 54
    ref object handle tree ref merge delta  # 55
    merge header pack delta remote queue buffer ref tree queue  # 56
    handle stream socket cache merge  # 57
    parser header header tree window stream blob cache  # 58
    header parser object handle socket tree index index buffer  # 59
    queue worker socket pack rebase  # 60
    handle object ref pack pack rebase tree socket ref handle blob socket  # 61
    ref blob remote handle blob merge  # 62
    window buffer blob buffer cache ref  # 63
    remote blob commit object rebase  # 64
    rebase delta ref buffer branch worker worker  # 65
    branch tree merge branch token header ref blob  # 66
    handle commit blob index  # 67
    blob parser index remote cache cache pack blob remote header  # 68
    stream delta object stream blob parser  # 69
    stream token tree window merge window object branch branch stream cache  # 70
    rebase index pack branch token branch  # 0
    merge handle buffer branch parser  # 1
    window queue buffer rebase remote  # 2
    blob cache rebase commit index delta worker branch window branch  # 3
    blob tree window parser header socket rebase merge pack socket  # 4
    index merge handle cache  # 5
    commit delta token handle index stream rebase queue header worker rebase tree  # 6
    token header merge index  # 0
    ref branch commit blob delta index  # 1
    token index queue rebase commit buffer token queue queue remote window  # 2
    rebase merge delta window cache handle  # 3
    blob branch token remote cache ref queue buffer  # 4
    rebase cache cache buffer worker worker parser rebase commit  # 5
    buffer buffer socket index object handle token  # 6
    cache index remote header delta handle  # 7
    cache stream pack header  # 8
