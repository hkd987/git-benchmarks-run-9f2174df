    delta delta pack window  # 0
    socket tree merge object stream token pack token commit tree buffer  # 1
    buffer commit branch window header rebase stream rebase queue blob  # 2
    branch queue buffer merge remote tree window remote  # 3
    pack merge branch remote pack token commit cache tree index ref delta  # 4
    merge ref worker merge cache blob branch  # 5
    cache token ref window token stream token token  # 6
    stream stream worker rebase remote socket blob  # 7
    blob stream buffer remote buffer blob token tree cache  # 8
    rebase handle parser token header parser token queue rebase commit handle  # 9
    object cache rebase buffer object parser stream delta branch cache rebase  # 10
    buffer socket index window stream  # 11
    queue delta delta pack  # 12
    merge window rebase socket blob tree commit stream window object  # 13
    rebase index delta queue buffer handle merge remote blob  # 14
    blob worker header rebase socket merge commit socket  # 15
    tree blob header tree index remote  # 16
    blob tree parser blob tree blob window object header stream socket  # 17
    tree buffer stream cache  # 18
    socket handle commit window  # 19
    cache tree index window handle object queue commit tree merge index blob  # 20
    blob worker handle parser  # 21
    window cache cache rebase worker token handle window token commit handle index  # 22
    token window socket token stream object queue socket parser  # 23
    branch tree remote commit  # 24
    rebase ref pack worker  # 25
    commit stream worker socket merge handle  # 26
    branch parser header parser object  # 27
    worker cache pack branch  # 28
    pack handle handle remote  # 29
    buffer ref rebase remote blob delta branch  # 30
    blob buffer buffer queue delta  # 31
    branch ref commit ref header handle buffer window parser  # 32
    blob rebase blob window  # 33
    window pack remote buffer branch buffer parser merge  # 34
    remote object object rebase index branch window stream  # 35
    pack delta buffer token cache socket branch queue delta remote socket pack  # 36
    blob pack delta stream worker blob commit tree buffer  # 37
    object tree window blob tree remote worker header pack rebase branch  # 38
    ref ref object socket cache rebase token  # 39
    object queue object handle object pack cache queue branch delta  # 40
    handle object queue queue socket window parser  # 41
    stream token pack buffer buffer stream header header  # 42
    object socket delta remote index header parser stream ref  # 43
    index cache ref buffer buffer merge remote  # 44
    queue delta window worker socket  # 45
    handle queue ref branch tree commit  # 46
    object commit token rebase branch token remote queue merge worker ref  # 47
    queue merge merge socket tree queue header index stream  # 48
    ref queue parser header stream worker window ref object  # 49
    blob ref tree merge remote  # 50
    tree ref delta pack branch commit remote  # 51
    window stream parser queue buffer  # 52
    rebase parser header ref blob delta remote window branch  # 53
    branch object ref parser index worker worker  # 54
    handle queue tree index ref ref object commit  # 55
    merge buffer header socket rebase index tree window buffer ref buffer  # 56
    worker remote queue window handle tree queue remote tree object worker  # 57
    blob buffer stream header ref remote rebase delta commit queue  # 58
    ref stream worker queue delta blob token stream blob  # 0
    window remote branch token index delta queue blob ref blob  # 1
    branch object stream socket socket commit stream  # 2
    ref worker tree window merge commit object remote object queue delta  # 3
    queue handle socket socket ref header handle remote token pack  # 4
    cache rebase parser buffer cache token cache delta  # 5
    cache merge buffer commit object window stream pack blob blob  # 6
    remote token handle token object buffer merge window handle header branch  # 7
    stream remote parser buffer socket queue token buffer buffer pack  # 8
    pack window index handle remote blob index handle ref commit  # 9
    cache delta parser stream stream stream  # 10
    index pack blob window index cache delta commit parser merge object  # 11
    ref cache handle stream branch index tree ref  # 12
