    handle blob parser worker parser parser worker pack ref  # 0
    token stream queue delta  # 1
    cache cache token handle window delta window buffer token cache  # 2
    tree commit branch object queue object stream object socket cache merge  # 3
    remote tree stream blob socket parser index token header rebase  # 4
    socket object token tree  # 5
    buffer ref index blob index  # 6
    blob handle delta object tree header blob worker  # 7
    queue pack header parser  # 8
    rebase pack socket object handle queue merge tree ref pack token rebase  # 9
    handle token queue socket parser index cache handle merge  # 10
    token parser stream buffer branch parser tree queue branch  # 11
    window object delta stream object blob ref buffer header handle handle  # 12
    merge remote branch blob  # 13
    socket buffer index index socket stream  # 14
    parser delta commit remote merge  # 15
    pack ref cache delta buffer rebase remote index remote  # 16
    cache ref merge remote  # 17
    queue socket parser blob pack buffer window socket commit merge branch commit  # 18
    merge header header socket branch object stream token parser merge token  # 19
    remote tree index pack commit  # 20
    index buffer cache cache cache index header cache  # 21
    object cache parser commit commit branch  # 22
    stream cache merge stream  # 23
    buffer header pack object socket branch cache worker window  # 24
    worker commit worker rebase worker stream buffer branch branch  # 25
    queue queue remote worker index header delta pack token merge remote header  # 26
    blob pack queue queue token worker blob remote socket  # 27
    worker object rebase handle commit tree socket  # 28
    header index window queue ref buffer ref  # 29
    queue tree delta tree pack stream cache token cache ref index  # 30
    tree ref header ref queue index pack queue tree queue token  # 31
    pack blob token socket blob rebase index socket delta queue handle merge  # 32
    ref header delta remote buffer stream stream merge queue delta tree blob  # 33
    ref pack token stream  # 34
    branch ref commit socket blob rebase token queue  # 35
    parser stream buffer index delta object  # 36
    cache blob branch blob handle stream commit remote  # 0
    parser object stream object tree worker  # 1
    stream handle blob worker object commit queue parser pack cache rebase cache  # 2
    remote delta blob blob pack cache ref merge buffer  # 3
    parser token buffer blob handle rebase header branch branch  # 4
    socket cache index tree buffer object  # 5
    header remote header header pack merge  # 6
    blob worker header token parser object cache header token delta index  # 7
    stream branch window window pack branch merge commit worker token  # 8
    branch stream tree ref socket  # 9
    rebase window parser rebase tree queue object socket parser delta  # 10
    header token remote remote commit handle remote blob  # 11
    remote tree pack tree branch header branch  # 0
    cache blob index object pack  # 1
    delta header queue index socket token worker buffer cache  # 2
    commit blob tree delta buffer pack ref  # 3
    socket tree parser header pack rebase token header  # 4
    stream stream stream cache stream tree commit branch buffer  # 5
    handle commit tree tree window parser pack  # 6
    branch remote merge header window pack parser branch handle branch merge  # 7
    buffer tree branch pack queue socket object handle cache ref header token  # 8
    commit token stream delta branch queue  # 9
    parser merge header buffer  # 10
    header token branch rebase branch delta tree index buffer parser ref index  # 11
    tree ref parser window blob pack  # 12
    ref delta pack rebase commit handle commit object blob stream worker rebase  # 0
    pack stream commit rebase  # 1
    buffer blob header handle  # 2
    parser worker pack index header  # 3
    parser queue queue handle socket ref worker buffer  # 4
    rebase commit cache delta queue commit branch index pack  # 5
    rebase remote socket object tree ref  # 6
    socket tree remote merge socket handle token window  # 7
