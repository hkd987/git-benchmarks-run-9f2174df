    commit queue merge parser parser socket merge socket handle worker  # 0
    merge remote branch merge  # 1
    rebase worker socket parser cache  # 2
    branch object index pack buffer handle tree index tree handle  # 3
    buffer pack delta blob cache ref pack delta pack  # 4
    remote object object socket stream delta stream  # 5
    token object stream ref merge index cache worker queue commit  # 6
    parser socket handle queue window delta token header index parser token delta  # 7
    blob merge buffer cache commit remote tree token socket branch  # 8
    pack remote tree buffer queue delta window index pack  # 9
    queue stream rebase worker  # 10
    parser socket rebase delta tree remote merge ref parser index queue header  # 11
    header tree merge parser ref window index rebase  # 12
    window parser queue token rebase index branch cache object worker handle  # 13
    handle index ref delta ref object socket remote  # 14
    tree merge object cache delta remote  # 15
    blob branch queue commit buffer remote blob header  # 16
    socket blob tree handle remote delta ref buffer socket tree index rebase  # 17
    branch token buffer commit stream handle object cache object delta handle queue  # 18
    blob ref merge index remote ref remote handle handle rebase buffer window  # 19
    rebase tree parser blob rebase stream worker token blob  # 20
    cache branch handle index merge index rebase socket remote tree pack worker  # 21
    handle merge queue cache branch  # 22
    stream buffer index token handle token blob worker  # 23
    object remote worker socket stream worker buffer buffer tree handle  # 24
    window index cache token token worker ref socket buffer header  # 25
    delta object header header token object worker object token  # 26
    token buffer remote tree  # 27
    index object tree remote pack object blob blob pack queue handle stream  # 28
    pack window cache cache blob cache  # 29
    stream delta header remote merge buffer  # 30
