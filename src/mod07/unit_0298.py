    blob queue parser pack pack header buffer cache cache object queue  # 0
    index object header object  # 1
    branch worker rebase branch rebase commit commit  # 2
    buffer queue socket rebase rebase ref  # 3
    index delta handle handle socket blob  # 4
    token object worker delta tree pack delta rebase header index merge  # 5
    ref delta commit branch remote rebase remote socket remote cache cache commit  # 6
    cache header rebase worker delta  # 7
    header queue cache queue index blob  # 8
    queue tree queue branch socket buffer queue object  # 9
    socket ref tree rebase parser object socket header queue  # 10
    merge object token index token window worker buffer rebase handle window blob  # 11
    ref delta index window merge  # 12
    pack parser parser tree object blob  # 13
    remote rebase index cache  # 14
    delta header buffer socket object commit  # 15
    remote delta remote commit branch queue merge ref remote tree parser socket  # 16
    token buffer window remote stream rebase  # 17
    rebase commit handle delta ref  # 18
    index window object token cache remote delta tree ref  # 19
    queue remote stream index parser  # 20
    index pack window handle  # 21
    object rebase branch remote parser header delta header buffer remote stream merge  # 22
    worker buffer cache pack socket buffer cache socket remote delta cache tree  # 23
    buffer delta index branch stream commit queue  # 24
    header object handle branch buffer blob pack ref handle index stream remote  # 25
    cache delta ref parser  # 26
    worker header remote buffer blob  # 27
    header ref tree parser delta worker remote tree blob  # 28
    ref delta ref index remote header ref blob object remote handle commit  # 29
    remote stream object merge delta stream delta  # 30
    rebase branch merge socket ref queue parser stream  # 31
    pack remote token worker parser header header worker index  # 32
    worker stream merge commit token queue stream tree queue window handle token  # 33
    window delta index ref blob  # 34
    ref branch buffer queue index stream blob tree pack stream  # 35
    cache header blob remote buffer  # 36
    worker worker commit tree socket blob socket pack buffer header stream  # 37
    rebase queue handle commit pack  # 38
    merge merge header handle header buffer delta parser buffer remote rebase  # 39
    blob parser rebase tree  # 40
    cache cache ref object queue tree stream worker rebase worker  # 41
    commit token rebase delta merge cache index remote queue rebase branch  # 42
    ref cache branch window stream header ref  # 43
    window socket merge window tree  # 44
    delta stream window token queue object stream commit  # 45
    object delta window delta socket  # 46
    object ref remote stream pack rebase delta delta blob socket pack  # 47
    object rebase stream merge remote remote pack delta blob index merge  # 48
    handle rebase worker queue ref socket queue merge merge worker  # 49
    cache tree index buffer ref index delta header tree  # 50
    window window blob index commit commit merge header stream  # 51
    ref parser buffer window index rebase  # 52
    branch delta branch token  # 53
    index cache commit queue stream delta index rebase merge rebase  # 54
    blob token branch index branch header delta parser tree handle  # 55
    token index object token tree merge blob  # 56
    token window cache branch pack  # 57
    branch pack object window stream  # 58
    blob token tree merge blob worker rebase  # 59
    handle window blob queue commit ref remote queue  # 60
    delta ref handle token  # 61
    blob handle pack parser buffer tree token token stream merge  # 62
