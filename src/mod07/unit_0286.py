    queue handle ref commit cache socket buffer worker queue  # 0
    rebase worker handle stream  # 1
    rebase branch pack branch  # 2
    blob rebase ref handle merge window parser cache index buffer buffer  # 3
    parser commit delta tree ref remote object buffer stream ref  # 4
    header object queue header merge commit queue branch merge token  # 5
    branch header index tree pack pack ref tree index queue  # 6
    handle buffer merge window delta merge rebase blob rebase  # 7
    header object index index parser ref ref blob parser blob socket  # 8
    branch pack tree queue tree blob queue merge object blob rebase branch  # 9
    header header window queue remote remote token tree  # 10
    tree merge socket worker  # 11
    handle rebase buffer parser rebase handle pack delta token rebase cache  # 12
    commit handle window buffer tree commit  # 13
    token buffer ref branch index handle object cache rebase rebase commit pack  # 14
    worker branch handle commit header pack window merge cache index  # 15
    worker commit delta pack remote cache delta commit remote cache  # 16
    worker cache index rebase merge blob branch  # 17
    stream socket ref socket index index pack delta token parser token branch  # 18
    tree token blob merge ref cache buffer delta blob parser blob tree  # 19
    parser ref blob pack delta worker parser delta index  # 20
    window object worker header blob buffer branch stream pack pack delta delta  # 21
    index handle parser delta index branch  # 22
    rebase ref window ref queue blob header delta  # 23
    index commit parser handle  # 24
    commit merge stream cache  # 25
    commit delta ref ref remote remote  # 26
    handle cache pack window token header cache object  # 27
    pack worker remote commit merge cache buffer commit blob pack merge ref  # 28
    worker buffer branch socket queue stream cache index parser parser socket ref  # 29
    merge tree socket delta delta blob buffer window branch  # 30
    merge token commit queue merge socket window tree  # 31
    buffer parser parser delta  # 32
    stream delta tree commit remote  # 33
    socket branch queue socket tree remote index rebase buffer window  # 34
    commit index pack delta stream tree parser buffer worker handle socket  # 35
    socket commit buffer delta cache header  # 36
    stream handle merge handle handle parser token buffer merge token index token  # 37
    ref buffer stream commit socket index commit commit buffer socket branch  # 38
    delta header commit tree pack header blob branch branch cache delta  # 39
    worker blob handle branch header socket token socket stream socket  # 40
    socket handle worker branch pack handle ref socket token  # 41
    window object queue socket queue handle delta  # 42
    parser merge commit object ref socket remote parser token cache  # 43
    worker token blob object  # 44
    tree blob pack object branch tree tree worker socket index commit buffer  # 45
    stream rebase delta window merge  # 46
    rebase parser pack worker  # 47
    ref header commit merge cache header  # 48
    delta handle worker cache object tree parser merge  # 49
    index token parser queue delta ref handle commit merge  # 50
    remote delta parser buffer commit worker  # 51
    socket remote header index stream remote merge object stream blob stream  # 52
    tree stream queue buffer tree ref object commit queue  # 53
    object delta remote blob token blob ref window pack  # 54
    tree remote blob header socket rebase queue branch  # 55
    header branch buffer queue  # 56
    worker commit header queue merge delta blob buffer window socket window  # 57
    ref pack object buffer  # 58
    delta pack token remote  # 59
    socket merge stream commit branch object header object  # 60
    stream delta merge header cache  # 61
    remote tree merge stream  # 62
    parser parser commit branch token remote rebase pack  # 63
    window branch merge queue pack delta blob  # 64
    token parser branch cache ref worker header blob stream commit delta  # 65
    cache queue ref worker token object ref object branch rebase branch  # 66
    window worker tree parser pack buffer tree window window handle branch  # 67
    socket socket queue branch delta buffer  # 68
    object token token parser worker buffer  # 69
    queue socket branch window merge object handle commit blob pack socket cache  # 70
    token worker commit queue  # 71
    pack buffer delta tree object tree commit stream commit blob pack  # 72
    socket object worker index  # 73
    rebase socket pack tree branch  # 74
    handle index blob header object stream delta branch  # 75
    parser merge handle blob parser header worker header delta tree blob  # 76
    worker parser cache rebase index queue handle cache  # 77
    queue ref rebase parser queue  # 78
    delta queue object tree index window  # 79
    rebase rebase blob commit blob rebase window remote  # 80
    handle delta stream handle pack stream index remote tree queue queue handle  # 81
    worker queue queue index delta  # 82
    token blob stream branch  # 83
    queue token stream window index queue parser token rebase  # 84
    stream stream window branch remote header rebase window ref commit  # 85
    handle handle stream commit commit window tree socket commit header  # 0
    window parser blob tree buffer ref index rebase commit token  # 1
    cache blob window stream queue object delta stream rebase commit tree queue  # 2
    delta object header object handle window index buffer handle branch  # 3
    branch merge token socket token rebase parser  # 4
    header rebase buffer worker ref commit commit rebase remote socket  # 5
    window handle commit handle header header ref pack buffer handle  # 6
    queue parser branch stream  # 7
    cache handle commit stream blob tree pack handle queue parser  # 8
