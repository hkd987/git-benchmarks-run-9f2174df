    queue socket handle object pack header handle rebase  # 0
    cache object blob tree blob tree pack object  # 1
    socket token window buffer tree stream branch token index parser index window  # 2
    delta window window handle delta window queue commit blob  # 3
    remote stream worker queue  # 4
    commit worker stream commit branch object blob blob queue  # 5
    object rebase stream header branch buffer window commit window  # 6
    delta commit index window index header pack merge  # 7
    cache handle worker branch object handle  # 8
    merge buffer object worker handle  # 9
    object blob handle socket parser  # 10
    rebase object pack delta pack socket token buffer merge  # 11
    tree cache stream blob handle buffer socket  # 12
    socket rebase merge window merge merge ref socket  # 13
    buffer blob object token  # 14
    token cache stream cache  # 15
    buffer parser delta ref  # 16
    worker queue pack rebase rebase buffer blob  # 17
    ref parser commit tree blob window ref object remote pack handle  # 18
    worker pack rebase handle branch index queue handle ref branch rebase  # 19
    queue delta worker merge merge ref pack worker  # 20
    branch pack index window queue token pack delta  # 21
    parser index ref header queue delta worker delta token delta  # 22
    worker queue handle cache  # 23
    remote socket worker index stream window ref tree socket  # 24
    socket merge merge stream blob  # 25
    cache rebase socket pack window stream handle cache  # 26
    worker socket remote branch buffer stream token merge handle merge window  # 27
    window pack pack index header cache rebase  # 28
    buffer object branch index blob tree buffer worker commit  # 29
    remote worker token ref cache buffer branch header cache tree token  # 30
    branch ref branch index branch  # 31
    commit index remote index header queue commit buffer stream rebase parser  # 32
    blob branch worker socket queue socket pack worker buffer  # 33
    delta ref index token delta rebase remote ref delta  # 34
    worker rebase blob branch handle branch  # 35
