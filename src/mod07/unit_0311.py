    tree handle merge delta rebase socket commit socket  # 0
    branch worker worker rebase cache ref worker parser  # 1
    queue stream ref buffer ref object window  # 2
    header blob buffer queue branch tree buffer pack header  # 3
    ref object tree token  # 4
    remote queue tree remote branch cache tree blob tree  # 5
    blob remote stream delta parser delta window object queue pack remote parser  # 6
    worker rebase worker header  # 7
    blob socket index blob merge delta rebase handle socket object  # 8
    header index pack merge window header token socket branch rebase window pack  # 9
    commit handle rebase pack buffer socket blob header pack pack  # 10
    socket cache commit object  # 11
    stream commit buffer commit header tree tree  # 12
    cache branch header socket remote branch parser blob buffer branch  # 13
    rebase delta buffer blob header object commit branch stream remote  # 14
    window blob remote window header object token tree  # 15
    branch branch blob object handle ref worker merge  # 16
    index merge handle merge socket index branch ref  # 17
    window object cache tree  # 18
    index merge tree parser pack  # 19
    header socket object merge delta handle merge blob token tree  # 20
    socket token window tree delta remote worker rebase stream commit ref  # 21
    commit rebase header object  # 22
    window blob handle handle merge branch rebase  # 23
    rebase object index object  # 24
    blob parser index pack rebase token rebase  # 25
    merge tree delta header ref  # 26
    rebase token commit remote commit stream stream tree token tree index  # 27
    worker blob buffer buffer blob  # 28
    tree ref socket branch rebase  # 29
    blob queue token index header merge remote worker  # 30
    merge index token token commit object delta  # 31
    stream socket object remote tree object  # 32
    ref tree index ref header remote header pack  # 33
    header stream socket handle queue index rebase window  # 34
    queue parser commit commit object rebase handle  # 35
    queue socket token token header remote stream worker  # 36
    pack pack handle index pack buffer blob index object rebase stream  # 37
    worker delta remote queue ref buffer window token delta delta parser window  # 38
    remote remote object object window header worker queue commit index pack  # 39
    index pack object queue tree cache parser queue queue queue merge parser  # 40
    cache pack handle window header index stream tree ref  # 41
    worker cache commit token delta commit delta pack pack ref  # 42
    rebase branch parser worker  # 43
    object blob header remote handle  # 44
    queue buffer cache socket rebase buffer socket tree worker delta  # 45
    header worker pack parser buffer object header commit worker merge queue  # 46
    merge parser handle branch header handle window object delta merge header header  # 47
    object window buffer object rebase merge merge header  # 48
    object buffer queue socket stream  # 49
    rebase tree branch remote blob merge pack tree  # 50
    socket ref header socket index ref  # 51
    socket stream token cache handle cache blob index  # 52
    handle merge remote buffer merge  # 53
    index buffer blob window socket header branch cache handle  # 54
    pack commit socket buffer worker cache header commit  # 55
    header rebase tree index merge merge worker  # 56
    pack index remote header cache rebase ref object queue pack cache  # 57
    cache commit token stream  # 58
    branch rebase tree buffer ref commit delta  # 59
    branch cache handle buffer  # 60
    buffer stream merge worker header window stream worker  # 61
    stream ref stream merge buffer handle pack handle tree pack token header  # 62
    worker socket stream handle rebase queue header queue cache index stream  # 63
    index rebase tree buffer socket handle merge  # 64
    branch object header header remote commit cache commit stream  # 65
    buffer rebase object buffer object parser  # 66
    token token delta cache rebase handle handle object remote parser stream  # 67
    cache socket branch queue header remote pack tree stream delta branch  # 68
    buffer ref index commit delta  # 69
    worker branch pack delta window blob parser merge blob stream merge ref  # 70
    branch pack index header rebase object rebase rebase rebase pack ref window  # 71
    index window parser window delta buffer  # 72
    rebase stream header object  # 73
    rebase window worker queue rebase worker tree rebase delta  # 74
    commit cache window token window  # 75
    remote handle remote pack window token  # 76
    object buffer queue index handle socket cache queue window index socket  # 77
    pack socket commit parser commit merge parser delta blob header  # 78
    index branch parser token commit delta blob parser token parser pack  # 79
    handle object parser delta worker ref commit delta  # 80
    header blob socket blob buffer window  # 81
    header blob pack window parser rebase worker ref stream remote commit header  # 82
    queue socket pack stream remote cache pack buffer window  # 83
    object pack ref tree blob pack rebase  # 84
    header delta pack tree tree buffer rebase stream parser merge  # 85
    remote object cache stream remote queue worker merge cache  # 86
