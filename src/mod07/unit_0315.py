    index blob window merge tree index commit tree  # 0
    blob tree index cache  # 1
    token worker queue index  # 2
    index index worker delta  # 3
    index worker object queue index rebase worker header index queue  # 4
    tree tree pack remote pack commit pack token ref  # 5
    delta window handle index delta  # 6
    rebase cache blob handle tree socket merge  # 7
    rebase object stream header pack queue  # 8
    delta merge remote commit ref merge index branch window  # 9
    remote buffer stream buffer branch blob  # 10
    branch merge worker queue stream ref branch rebase queue  # 11
    worker ref queue merge  # 12
    ref header index queue branch handle tree parser queue cache parser  # 13
    commit tree remote parser remote worker  # 14
    rebase remote tree parser ref worker rebase socket object merge  # 15
    parser delta parser stream ref branch commit  # 16
    remote pack cache branch  # 17
    commit pack object parser delta header token handle merge remote  # 18
    blob handle merge handle commit parser buffer rebase header stream merge index  # 19
    socket header index commit object commit cache cache  # 20
    blob pack index window parser blob  # 21
    index token queue rebase ref header ref commit delta queue queue token  # 22
    handle blob token object commit branch pack header  # 23
    window tree queue header object ref rebase parser index window  # 24
    rebase token merge ref cache cache ref handle  # 25
    worker worker window token buffer ref window tree  # 26
    stream cache queue delta merge cache stream pack queue token tree  # 27
    ref remote object commit branch commit rebase rebase  # 28
    branch header object remote tree remote blob header commit branch  # 29
    window token pack parser handle object stream cache remote commit queue blob  # 30
    blob token stream cache blob tree index remote stream queue ref object  # 31
    buffer delta parser blob tree rebase stream buffer  # 32
    pack object handle socket delta worker handle  # 33
    parser buffer branch ref  # 34
    pack commit ref handle delta  # 35
    cache tree merge cache tree window merge parser  # 36
    stream socket parser remote cache token commit buffer window  # 37
    merge parser rebase pack  # 38
    queue window pack branch token cache buffer buffer  # 39
    tree handle rebase handle tree merge cache queue remote  # 40
    queue delta merge object buffer remote  # 41
    handle parser commit window branch header branch index rebase stream ref stream  # 42
    pack parser index merge parser remote blob object  # 43
    index window window index handle remote socket parser socket  # 44
    queue ref commit stream header stream worker tree  # 45
    worker blob merge rebase token  # 46
    remote stream queue rebase object branch rebase  # 47
    buffer branch commit token token window cache tree handle object remote handle  # 48
    commit pack blob stream buffer ref queue pack buffer ref branch header  # 49
    cache branch socket commit stream queue queue header window token pack cache  # 50
    remote window object buffer tree token token tree delta object tree  # 51
    window queue queue buffer ref  # 52
    pack buffer index rebase pack remote parser tree cache branch token delta  # 53
    index blob delta index worker window commit  # 54
    commit ref branch index stream token buffer delta header rebase handle window  # 55
    index tree blob pack token queue  # 56
    pack blob tree branch worker commit queue cache cache header  # 57
    parser window ref blob index merge tree remote  # 58
    object object tree parser index socket object worker remote buffer pack worker  # 59
    ref handle blob header branch buffer delta  # 60
    stream merge rebase tree commit blob commit parser delta pack stream  # 61
    worker index merge header  # 62
    window buffer commit ref  # 63
    index remote rebase worker remote  # 64
    cache pack delta merge stream token  # 65
    buffer blob commit buffer  # 66
    merge branch window remote queue index ref socket commit commit socket branch  # 67
    worker socket object rebase branch object worker rebase window cache queue  # 68
    branch queue buffer commit delta rebase header header  # 69
    delta tree ref rebase  # 70
    worker index token window tree worker pack parser ref ref index socket  # 71
    queue delta blob blob remote stream queue delta buffer  # 72
    delta token handle branch socket worker queue branch rebase pack  # 73
    header header stream delta parser pack stream cache  # 74
    rebase branch handle pack merge merge branch buffer ref stream  # 75
    branch socket cache index ref rebase queue cache pack delta  # 76
    handle token delta delta remote buffer rebase stream pack worker stream worker  # 77
    object branch ref commit stream merge object branch tree  # 78
    header buffer queue remote delta  # 79
    commit queue object pack token header cache  # 80
    tree remote cache ref object tree object queue merge  # 81
