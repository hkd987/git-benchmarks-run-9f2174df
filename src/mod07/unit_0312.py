    handle handle buffer parser buffer queue parser socket ref worker  # 0
    delta blob worker buffer ref rebase stream header buffer header  # 1
    blob remote queue handle parser header header stream  # 2
    index commit ref pack index pack merge pack buffer delta blob cache  # 3
    socket object parser ref commit  # 4
    commit cache header branch worker  # 5
    branch branch index handle token rebase socket object parser merge  # 6
    worker rebase object ref parser handle merge queue token delta window ref  # 7
    parser header token header  # 8
    object handle blob merge  # 9
    index commit header tree window  # 10
    socket socket worker cache delta rebase token  # 11
    ref token handle pack tree cache window blob queue  # 12
    parser socket delta blob pack queue index ref  # 13
    handle pack rebase header object stream tree  # 14
    worker pack handle socket handle parser commit handle buffer remote commit  # 15
    pack socket socket stream header branch buffer handle stream index header  # 16
    token cache cache ref  # 17
    socket window worker token  # 18
    parser buffer ref index pack  # 19
    remote merge ref stream stream merge worker remote  # 20
    delta pack header worker merge pack blob token cache  # 21
    window pack window pack buffer blob index  # 22
    socket cache index buffer commit tree delta branch cache  # 23
    worker worker tree object branch  # 24
    worker worker buffer handle worker socket  # 25
    pack header tree merge worker merge worker cache worker  # 26
    header rebase handle buffer rebase buffer token  # 27
    header branch merge buffer header index ref  # 28
    pack branch object cache blob worker tree blob  # 29
    window parser pack remote worker token tree  # 30
    handle window worker queue parser  # 31
    window handle commit index queue remote queue pack  # 32
    worker stream object worker  # 33
    commit cache ref header delta branch  # 34
    pack ref tree index delta object blob cache parser merge merge handle  # 35
    branch index token branch token window header commit pack parser remote socket  # 36
    cache branch remote branch rebase handle  # 37
    buffer pack object object index queue ref object commit token  # 38
    handle parser token blob  # 39
    branch buffer cache token stream delta socket handle pack  # 40
    remote socket token blob  # 41
    branch worker index ref commit token blob rebase delta stream token window  # 42
    stream worker rebase ref socket commit  # 43
    stream pack handle pack branch header pack stream blob handle token  # 44
    worker delta queue branch socket stream object  # 45
    blob token commit parser index pack merge object queue  # 46
    queue index queue tree  # 47
    branch handle tree commit queue token handle  # 48
    blob commit rebase delta parser buffer commit delta window  # 49
    worker header worker buffer stream commit merge branch blob blob parser  # 50
    delta queue merge header  # 51
    handle object blob token queue header handle  # 52
    handle branch branch tree handle index stream tree parser  # 53
    index object branch branch stream delta pack parser tree remote token  # 54
    blob header commit parser parser socket header token  # 55
    commit index pack index commit index socket socket commit  # 56
    cache buffer header handle  # 57
    delta worker handle buffer handle token window cache  # 58
    socket branch commit socket index worker object commit merge  # 59
    buffer ref remote window  # 60
    stream header remote rebase delta remote branch ref queue  # 61
    buffer object ref handle worker delta object blob  # 62
    branch handle object stream  # 63
    merge window tree delta stream remote handle  # 64
    branch header socket cache buffer handle delta remote buffer ref stream  # 65
    buffer tree remote parser parser socket merge worker token  # 66
    merge blob token token cache cache stream merge ref  # 67
    worker remote tree stream blob parser stream header socket  # 68
    queue queue index queue stream pack  # 69
    buffer index token delta window  # 70
    stream cache ref pack header window stream window buffer object commit object  # 71
    rebase window merge queue tree ref blob ref cache header ref commit  # 72
