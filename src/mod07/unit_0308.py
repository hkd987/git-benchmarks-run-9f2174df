    commit remote cache handle window  # 0
    window commit header ref rebase socket buffer  # 1
    pack pack remote ref index object cache pack socket cache index  # 2
    stream object object queue cache cache  # 3
    window cache queue window parser stream blob blob parser merge  # 4
    pack token tree index  # 5
    stream index worker queue parser index queue blob pack  # 6
    header tree rebase tree token worker socket ref blob  # 7
    queue branch buffer merge buffer worker tree  # 8
    queue commit object rebase handle object branch  # 9
    index object rebase branch stream blob ref socket index blob  # 10
    queue queue stream delta  # 11
    token branch ref handle handle parser delta merge token  # 12
    queue queue delta pack remote ref worker socket ref worker handle  # 13
    object object pack ref remote remote handle  # 14
    index stream tree tree cache object commit handle  # 15
    commit cache worker branch tree branch  # 16
    merge delta buffer delta delta cache worker commit  # 17
    queue ref parser pack commit queue delta parser header branch commit  # 18
    window header ref tree header header header window  # 19
    object stream object window  # 20
    cache cache buffer blob tree stream commit cache pack handle buffer parser  # 21
    token pack header commit object buffer handle buffer merge ref queue parser  # 22
    cache token stream token object window remote index  # 23
    parser remote pack token pack pack branch commit token delta window pack  # 24
    remote merge worker socket  # 25
    pack handle header socket  # 26
    cache window cache queue stream index stream ref worker cache merge delta  # 27
    merge handle parser delta delta remote blob delta ref remote rebase  # 28
    cache handle pack remote blob  # 29
    pack token rebase index delta buffer token  # 30
    worker queue queue object stream token queue  # 31
    queue branch socket delta tree token commit tree merge header  # 32
    handle stream stream pack tree commit token rebase parser  # 33
    handle parser object worker ref remote socket parser index pack ref  # 34
    window merge delta token merge window commit blob ref blob cache  # 35
    remote ref tree remote token handle ref socket header branch  # 36
    queue worker parser pack buffer  # 37
    buffer window object handle blob delta delta remote tree blob header  # 38
    blob handle worker commit rebase handle handle  # 39
    ref branch delta remote parser object socket ref stream merge token  # 40
    buffer tree blob socket queue token remote parser pack commit rebase queue  # 41
    delta ref blob pack blob blob remote delta  # 42
    branch window merge token  # 43
    window rebase branch rebase pack rebase  # 44
    stream token object ref socket token  # 45
