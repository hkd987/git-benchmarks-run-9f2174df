    pack delta index commit buffer token remote token index worker  # 0
    stream tree cache header  # 1
    buffer delta rebase token parser  # 2
    window parser socket socket ref ref  # 3
    rebase header tree queue queue token  # 4
    parser cache cache cache parser queue parser buffer handle pack tree  # 5
    queue delta worker object merge socket blob cache  # 6
    header remote pack parser pack index object queue blob  # 7
    header socket merge rebase worker window object socket pack ref commit  # 8
    worker header branch index  # 9
    tree index ref ref rebase cache branch index  # 10
    rebase token ref branch window stream  # 11
    socket merge stream cache object buffer header  # 12
    tree queue token buffer token merge cache tree window socket token rebase  # 13
    merge tree handle handle  # 14
    object remote cache commit queue merge pack queue  # 15
    handle queue window socket worker commit pack window worker  # 16
    merge header tree handle blob worker  # 17
    header blob object ref ref pack handle ref  # 18
    worker index object stream  # 19
    queue header merge object cache handle commit object buffer object handle cache  # 20
    tree cache commit handle index buffer  # 21
    parser rebase buffer branch commit queue blob socket parser commit pack  # 22
    index commit parser buffer stream cache queue rebase stream blob worker  # 23
    worker ref branch socket token ref header worker stream  # 24
    pack header handle buffer header cache stream merge cache worker  # 25
    pack rebase blob stream delta window buffer queue blob tree  # 26
    queue worker window object window pack handle merge header delta stream  # 27
    pack tree remote delta delta rebase remote token cache parser  # 28
    token header delta tree  # 29
    handle token delta index index pack buffer  # 30
    rebase cache delta queue pack object blob index window blob commit  # 31
    socket commit header socket handle socket delta delta ref buffer  # 32
    tree worker handle tree  # 33
    blob buffer header worker merge tree cache header merge buffer rebase  # 34
    object ref window delta token socket delta rebase object tree rebase  # 35
    blob queue token cache token merge token index pack branch blob  # 36
    branch rebase remote ref worker  # 37
    remote socket tree token commit  # 0
    blob tree commit object commit handle index ref parser remote blob parser  # 1
    handle commit delta socket ref token parser merge commit pack cache branch  # 2
    ref cache window stream pack branch merge parser window parser  # 3
