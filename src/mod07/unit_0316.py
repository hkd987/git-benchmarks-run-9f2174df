    cache header buffer blob stream header worker  # 0
    merge window window delta header index ref queue delta  # 1
    cache handle blob remote socket  # 2
    parser merge rebase stream  # 3
    cache index blob header header parser handle stream  # 4
    stream parser object worker ref tree buffer  # 5
    merge buffer handle buffer blob stream branch branch remote commit  # 6
    index branch tree queue queue handle socket parser stream buffer queue  # 7
    worker parser blob pack parser index  # 8
    index remote tree window buffer merge delta  # 9
    rebase delta blob handle branch stream worker commit stream delta index  # 10
    index window pack ref  # 11
    pack remote ref commit  # 12
    worker delta object delta remote handle stream  # 13
    token stream object worker merge  # 14
    blob blob parser blob worker window blob  # 15
    header branch blob merge  # 16
    worker parser index socket cache  # 17
    remote queue parser commit  # 18
    parser worker window rebase blob tree header queue ref index  # 19
    queue handle buffer remote buffer  # 20
    remote delta socket object pack merge tree  # 21
    merge stream stream blob socket  # 22
    window rebase queue token  # 23
    token header merge ref commit worker socket socket window blob header  # 24
    worker delta queue stream queue worker header socket  # 25
    cache object cache token  # 26
    cache ref window buffer parser stream object queue rebase  # 27
    pack token commit handle commit socket header ref  # 28
    remote tree parser commit header header cache queue branch handle  # 29
    branch stream parser tree delta parser  # 30
    queue rebase handle pack buffer delta tree  # 31
    worker commit header remote socket  # 32
    header stream socket buffer  # 33
    tree pack remote index commit worker header pack  # 34
    handle worker tree token blob  # 35
    ref handle window branch index handle commit blob ref ref window remote  # 36
    delta token index cache merge  # 37
    handle ref token cache  # 38
    window handle rebase branch header token branch commit ref header socket  # 39
    handle stream cache socket ref merge delta stream queue handle remote  # 40
    window token window tree window buffer index parser ref handle merge worker  # 41
    ref pack rebase remote  # 42
    header window commit merge rebase handle buffer delta  # 43
    handle pack worker socket ref token blob object merge index tree  # 44
    index rebase delta window cache token index stream rebase stream cache stream  # 45
    worker buffer delta branch socket index blob remote  # 46
    cache ref window commit blob remote remote socket  # 47
    token remote cache worker branch index merge buffer worker worker handle  # 48
    rebase rebase stream header blob header pack object cache pack parser  # 49
