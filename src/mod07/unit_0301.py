    stream remote buffer window cache rebase tree delta  # 0
    ref index merge parser queue rebase stream parser header  # 1
    handle cache ref branch parser  # 2
    worker socket window pack pack queue tree pack remote tree  # 3
    pack branch stream blob object tree  # 4
    commit worker object cache object parser queue buffer pack branch cache socket  # 5
    delta blob cache header worker window header branch socket ref worker  # 6
    merge blob ref queue ref parser queue cache merge handle  # 7
    object stream cache socket parser index  # 8
    blob pack merge socket  # 9
    handle blob parser delta branch object buffer  # 10
    object merge rebase buffer stream blob pack cache index queue  # 11
    token rebase ref handle header commit handle queue cache handle  # 12
    merge socket branch rebase window index rebase commit  # 13
    window object remote remote worker  # 14
    buffer pack blob header rebase ref ref blob queue tree rebase  # 15
    index index stream header  # 16
    branch blob header merge token cache branch window merge buffer window  # 17
    window queue blob branch token window object socket handle  # 18
    worker token rebase window  # 19
    token tree merge blob cache window buffer queue window  # 20
    stream object blob parser worker object tree  # 21
    window stream tree parser delta  # 22
    stream parser buffer header branch object delta handle tree  # 23
    tree blob pack cache merge delta buffer blob delta ref parser  # 24
    rebase pack ref branch rebase rebase  # 25
    token socket object socket  # 26
    queue buffer header handle  # 27
    window pack blob tree token merge cache  # 28
    branch handle merge blob index remote remote parser  # 29
    stream handle tree buffer commit ref worker stream  # 30
    rebase remote buffer socket window stream window object  # 31
    header queue queue ref buffer  # 32
    buffer rebase blob index branch index index worker index socket buffer tree  # 33
    pack branch handle object rebase worker delta  # 34
    rebase pack stream rebase pack object window rebase cache handle remote commit  # 35
    parser socket window socket pack remote header window ref  # 36
    merge ref tree pack merge  # 37
    window remote index commit stream token branch queue  # 38
    token merge queue object index cache index  # 39
    branch rebase token commit  # 40
    handle parser token ref  # 41
    queue window ref tree  # 42
    branch pack tree buffer  # 43
    socket commit header pack socket remote object  # 44
    rebase worker commit tree cache delta header rebase header  # 45
    token tree delta ref rebase remote index tree index  # 46
    worker header delta stream remote queue rebase socket header ref  # 47
    header buffer queue window handle  # 48
    header remote ref buffer stream  # 49
    parser rebase socket merge remote header object branch tree remote cache  # 50
    commit token worker rebase cache  # 51
    merge worker token object  # 52
    header parser ref queue stream branch handle  # 53
    index blob token branch handle delta rebase delta commit blob parser  # 54
    socket queue parser buffer  # 55
    parser delta parser branch index window  # 56
    header delta commit tree remote queue delta remote  # 57
    pack rebase cache parser parser rebase worker header commit object  # 58
    queue object handle queue remote stream handle commit index pack commit delta  # 59
    index blob token window blob remote window  # 60
    ref index token commit token queue buffer rebase  # 61
    ref blob tree merge handle pack parser merge tree parser  # 62
    socket index pack window pack socket  # 63
    header tree stream window branch stream stream object  # 64
    merge handle pack socket queue queue remote socket index window  # 65
    branch handle tree window header stream branch queue tree remote  # 66
    window header tree rebase buffer cache  # 67
    queue handle object stream rebase index pack worker merge branch delta index  # 68
    object index window object object queue parser remote commit  # 69
    handle token tree remote  # 70
    merge blob pack stream delta  # 71
    parser merge buffer window  # 72
    commit object socket delta pack socket  # 73
    buffer pack pack pack token  # 74
    queue tree buffer blob ref delta branch pack window commit commit  # 75
    buffer tree window delta blob delta queue ref token  # 0
    branch merge handle branch ref worker index worker parser token window socket  # 1
    commit pack header handle socket header queue header token commit  # 2
    socket rebase queue worker token object  # 3
