    socket pack rebase handle queue merge  # 0
    branch socket blob stream buffer handle cache commit branch token merge  # 1
    commit commit buffer tree cache rebase rebase merge merge tree ref  # 2
    remote queue delta delta parser pack delta parser buffer header  # 3
    header buffer socket pack delta socket ref queue  # 4
    parser commit parser index token window parser stream branch handle object cache  # 5
    object cache token ref index commit parser pack token merge worker  # 6
    parser branch branch cache buffer blob tree cache pack window blob  # 7
    stream header ref remote  # 8
    queue stream pack blob remote ref  # 9
    buffer token cache handle ref handle  # 10
    pack header socket ref header parser socket handle cache  # 11
    ref object queue header window index buffer commit queue token window  # 12
    commit header header queue remote cache socket branch cache window  # 13
    header stream delta pack tree socket parser queue parser tree  # 14
    object branch token handle parser token window socket tree queue ref blob  # 15
    remote buffer index rebase socket branch queue cache  # 16
    pack parser socket commit remote cache header ref window  # 17
    remote queue pack socket remote queue stream delta handle queue tree header  # 18
    parser worker parser rebase  # 19
    cache header blob merge  # 20
    pack tree rebase stream window parser stream socket remote buffer rebase token  # 21
    ref buffer header rebase rebase  # 22
    handle blob branch socket tree  # 23
    remote ref socket ref window header object  # 24
    remote queue index queue window ref remote commit delta rebase tree  # 25
    handle object object socket ref tree delta pack parser merge blob  # 26
    branch merge cache buffer  # 27
    worker queue worker merge object handle commit index cache rebase pack header  # 28
    stream pack object socket blob socket  # 29
    blob parser pack blob merge header  # 30
    queue remote remote tree queue  # 31
    header remote object branch pack  # 32
    header remote buffer rebase worker header remote  # 33
    worker stream queue parser  # 34
    header ref delta parser handle remote  # 35
    token remote window rebase window  # 36
    branch index handle cache cache delta stream object cache index queue  # 37
    worker worker blob cache socket  # 38
    cache delta window branch remote tree object buffer index  # 39
    tree queue merge tree header merge window header delta  # 40
    merge parser ref token rebase header handle  # 41
    branch header commit ref blob queue delta header delta  # 42
    worker window cache handle branch index queue window  # 43
    index socket buffer object object delta blob buffer  # 44
    object blob ref buffer cache blob index token  # 45
    buffer tree socket rebase object pack delta delta  # 46
    parser branch index buffer pack object remote  # 47
    rebase object ref remote parser buffer stream tree ref  # 48
    ref socket stream pack parser parser tree window branch object  # 49
    index header merge queue object handle cache rebase rebase tree merge cache  # 50
    ref header stream commit token queue delta rebase delta  # 51
    token window tree worker ref commit delta socket pack rebase  # 52
    rebase pack pack socket stream buffer pack  # 53
    header branch object queue blob blob delta window  # 54
    handle delta socket pack parser delta ref parser index delta stream  # 55
    branch remote buffer blob parser  # 56
    ref merge header handle worker stream parser  # 57
    rebase blob merge rebase worker stream tree header blob delta  # 58
    worker token cache pack branch queue window token delta buffer  # 59
    blob stream blob worker token tree window window cache ref  # 60
    object cache parser rebase window merge buffer token  # 61
    queue cache tree remote delta worker tree  # 62
    remote parser tree branch queue  # 63
    ref socket cache pack branch  # 64
    merge ref rebase header index cache commit blob  # 65
    branch commit buffer handle index handle rebase cache rebase cache  # 66
    delta remote buffer remote ref ref merge  # 67
    socket ref token merge handle buffer  # 68
    tree token commit token object  # 69
    parser worker rebase token index buffer  # 70
    ref queue queue index blob object branch socket  # 71
    header object buffer window socket commit queue socket branch handle  # 72
    stream handle socket header commit blob index worker rebase handle commit  # 73
    handle window branch token rebase handle window buffer buffer cache  # 74
    worker ref socket window socket cache branch  # 75
    index blob merge tree commit  # 76
