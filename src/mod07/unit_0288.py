    queue parser index object header rebase header  # 0
    handle pack stream token  # 1
    buffer queue blob stream window blob object  # 2
    ref commit cache delta socket index branch stream tree  # 3
    remote remote queue commit  # 4
    ref buffer pack commit worker merge cache parser header tree  # 5
    ref merge token queue handle blob blob ref object window delta  # 6
    pack ref stream stream parser branch stream object worker  # 7
    token merge parser delta header socket tree commit blob object delta  # 8
    ref ref ref tree stream worker stream stream  # 9
    pack remote branch remote parser queue  # 10
    pack stream tree parser token branch queue index remote remote branch stream  # 11
    commit rebase merge delta worker header tree cache socket buffer  # 12
    rebase rebase branch commit token commit commit header token tree window header  # 13
    remote object index merge commit  # 14
    delta delta window header  # 15
    object pack remote pack window delta header remote  # 16
    socket rebase pack token  # 17
    parser pack header remote  # 18
    queue delta object handle cache merge index handle rebase header token buffer  # 19
    branch queue ref handle ref branch commit delta  # 20
    commit branch delta pack index token handle buffer branch worker  # 21
    parser remote commit window handle window index  # 22
    rebase pack window object worker index  # 23
    branch ref token token pack stream worker queue blob merge  # 24
    tree handle socket buffer  # 25
    remote remote socket token socket remote merge buffer  # 26
    remote delta stream parser commit commit pack branch queue queue  # 27
    commit object commit worker header tree pack handle token  # 28
    commit branch index worker parser branch tree pack handle  # 29
    socket merge index token  # 30
    window header worker worker worker token header pack buffer token delta token  # 31
    handle delta queue pack ref ref window index tree delta  # 32
    cache merge cache index token cache object  # 33
    worker window pack delta blob object tree ref worker  # 34
    header cache delta cache window ref  # 35
    merge blob tree pack window handle worker  # 36
    buffer merge token tree header window merge  # 37
    socket branch commit stream tree worker cache tree window pack object buffer  # 38
    pack cache buffer merge  # 39
    branch cache branch blob queue rebase worker tree commit handle delta  # 40
    token delta queue queue pack tree window queue buffer merge handle  # 41
    header branch pack branch object worker remote index  # 42
    token branch delta merge tree token index stream window tree stream branch  # 43
    delta tree remote pack rebase worker queue header token handle  # 44
    buffer blob delta handle commit token index delta merge queue socket pack  # 45
    index socket socket index window ref branch merge  # 46
    header commit parser commit merge token socket remote blob  # 0
    worker branch parser socket parser  # 1
    pack rebase branch token commit buffer index commit worker  # 2
    cache socket commit ref pack handle token object cache  # 3
    worker buffer token delta tree window commit socket stream blob ref rebase  # 4
    branch ref header pack commit token rebase  # 5
    queue window handle index queue remote window delta queue queue  # 6
    header socket worker cache commit worker worker token ref worker window  # 7
    ref remote rebase token commit rebase parser remote delta buffer header  # 8
    handle pack merge socket tree queue pack branch tree  # 9
    queue pack tree handle  # 10
    buffer object ref delta queue object token worker commit queue  # 11
    rebase rebase window object ref rebase  # 12
    delta blob header tree  # 13
