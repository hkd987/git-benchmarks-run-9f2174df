    parser buffer queue stream buffer tree index handle handle  # 0
    delta pack parser header branch cache worker commit handle blob commit  # 1
    handle ref token blob pack socket delta  # 2
    rebase parser socket handle branch cache index branch remote window stream parser  # 3
    handle branch queue stream  # 4
    window merge branch queue merge  # 5
    ref ref cache blob socket buffer queue blob  # 6
    remote stream object commit tree commit buffer ref header rebase  # 7
    blob cache stream remote blob ref socket handle  # 8
    index rebase queue tree tree delta header delta pack  # 9
    tree cache commit header branch delta parser socket  # 10
    socket window rebase queue token socket handle parser object header commit object  # 11
    blob remote token handle socket stream remote branch merge branch commit queue  # 12
    handle merge window branch tree commit blob window pack  # 13
    worker cache handle token stream cache remote buffer tree queue tree handle  # 14
    rebase buffer branch merge header tree queue header queue worker buffer branch  # 15
    blob ref branch queue cache queue branch  # 16
    merge token header merge rebase handle remote buffer commit index queue pack  # 17
    queue buffer stream window handle tree pack header pack worker token branch  # 18
    window parser remote branch delta buffer queue  # 19
    blob window stream socket worker token  # 20
    parser blob tree ref buffer handle ref cache stream buffer  # 21
    cache header merge commit stream delta worker tree token object socket stream  # 22
    socket merge stream remote object  # 23
    parser token rebase index  # 24
    socket ref index branch remote cache window tree  # 25
    worker buffer window delta blob cache buffer commit queue remote  # 26
    commit branch remote header cache blob cache  # 27
    rebase handle handle window window token token merge branch  # 28
    stream ref merge queue buffer queue window parser worker buffer buffer  # 29
    cache tree buffer cache tree handle rebase ref tree stream  # 30
    queue object tree ref handle tree blob cache socket  # 31
    queue parser header index queue token tree rebase delta pack branch  # 32
    branch buffer branch branch merge window merge ref remote  # 33
    blob token commit buffer queue  # 34
    worker tree blob blob rebase window tree tree buffer merge blob  # 35
    token socket object remote object pack merge stream  # 36
    worker buffer remote object window delta index merge remote socket  # 37
    merge window socket commit merge object rebase ref header ref  # 38
    stream ref object window header ref index merge  # 39
    rebase header handle merge ref window blob ref  # 40
    index header object pack  # 41
