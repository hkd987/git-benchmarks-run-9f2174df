    delta merge branch ref rebase branch  # 0
    window queue branch ref blob branch rebase header merge  # 1
    branch branch pack handle ref stream blob buffer branch  # 2
    delta index rebase index socket tree  # 3
    parser remote ref window remote index handle object worker  # 4
    stream header worker merge object parser cache delta  # 5
    merge parser token socket blob branch cache rebase parser  # 6
    ref blob branch worker ref delta object  # 7
    buffer blob window remote  # 8
    header socket merge parser socket branch  # 9
    commit buffer commit remote token queue object queue tree parser  # 10
    worker queue delta socket blob stream cache blob tree  # 11
    window handle handle header handle worker worker rebase merge  # 12
    header worker pack window tree handle  # 13
    parser pack blob merge cache commit parser remote buffer header object tree  # 14
    buffer rebase header index window  # 15
    merge stream merge ref handle pack queue rebase stream cache index  # 16
    object worker cache ref tree queue rebase pack tree queue  # 17
    commit cache parser stream worker branch cache header cache  # 18
    buffer rebase queue rebase buffer tree merge socket  # 19
    header worker branch merge remote branch socket window stream worker merge ref  # 20
    cache rebase window merge token  # 21
    parser object token object merge tree remote stream pack  # 22
    pack worker pack stream index delta window merge queue ref buffer  # 23
    parser merge branch stream object parser tree  # 24
    worker branch ref stream delta  # 25
    rebase cache merge remote window socket  # 26
    cache header object stream  # 27
    remote blob delta worker cache  # 28
    buffer handle buffer remote socket delta socket  # 29
    parser buffer cache cache token token window pack branch buffer header queue  # 30
    tree stream rebase cache blob remote socket  # 31
