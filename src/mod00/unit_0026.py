    merge commit delta stream parser window queue remote handle rebase  # 0
    commit object buffer delta token object blob commit remote tree  # 1
    tree cache branch merge ref pack window blob  # 2
    token stream pack index  # 3
    blob window blob token pack merge handle tree rebase handle  # 4
    worker ref ref ref socket  # 5
    branch rebase worker tree commit stream parser commit  # 6
    rebase delta pack cache header delta parser buffer window branch delta worker  # 7
    token branch parser rebase socket tree remote branch handle remote handle  # 8
    commit blob window parser token commit rebase queue delta  # 9
    object remote token worker stream rebase merge  # 10
    rebase token socket queue socket socket branch  # 11
    cache token queue parser ref remote header object object  # 12
    socket socket token rebase queue object rebase handle  # 13
    window index index stream stream socket tree tree merge pack socket  # 14
    cache merge commit ref rebase stream index branch rebase stream rebase parser  # 15
    branch merge header pack commit handle rebase remote branch queue header token  # 16
    branch window socket header token delta handle  # 17
    merge parser stream commit commit handle  # 18
    blob delta token delta header blob worker parser  # 19
    cache pack stream buffer ref rebase header buffer  # 20
    token stream token worker  # 21
    header handle delta remote branch cache object remote  # 22
    commit blob object remote cache object cache token queue header remote ref  # 23
    remote token merge window  # 24
    stream delta queue cache  # 25
    header stream window window object merge parser blob ref stream socket  # 26
    socket ref window blob commit remote commit index rebase stream cache tree  # 27
    buffer remote cache blob index tree  # 28
    parser blob index rebase buffer index  # 29
    queue index blob socket rebase token  # 30
    ref stream parser stream  # 31
    header token commit tree object delta cache header ref cache  # 32
    handle pack header worker branch window commit remote header  # 33
    rebase blob window queue object token parser socket  # 34
    pack branch commit remote parser pack socket blob tree object  # 35
    worker remote worker ref parser object window  # 36
    window commit index branch socket branch  # 37
    branch tree index stream commit window commit socket  # 38
    handle blob pack branch ref merge worker pack stream merge merge cache  # 39
    blob commit delta token delta handle delta merge buffer  # 40
    commit remote tree worker merge object pack socket  # 41
    remote remote socket cache cache worker rebase  # 42
    merge merge ref merge merge socket ref rebase remote rebase  # 43
    cache header token remote  # 44
    worker ref object parser branch  # 45
    merge ref parser queue rebase commit rebase window blob parser tree delta  # 46
    token pack buffer index tree buffer ref  # 47
    queue worker token commit socket worker pack  # 0
    token ref merge blob socket  # 1
    stream window tree rebase worker merge stream handle queue blob pack index  # 2
    delta worker delta object queue branch ref ref window cache commit  # 3
    window delta token tree socket index commit ref ref stream  # 4
    worker handle rebase index cache worker remote cache merge pack  # 5
    branch queue commit remote token ref rebase tree blob  # 6
    worker object delta parser cache stream tree queue buffer header token parser  # 7
    commit header queue stream tree  # 8
    buffer buffer commit stream  # 0
    pack window object ref blob stream header merge commit  # 1
    rebase merge queue ref token object  # 2
    tree remote worker window index ref buffer socket index  # 3
    handle stream tree handle merge merge ref tree ref window  # 4
    delta handle branch handle commit token tree window worker  # 5
    worker cache pack window token  # 6
    handle stream remote handle branch pack parser pack socket window  # 7
    delta stream header stream worker  # 8
    merge remote remote blob token socket  # 9
    queue cache handle header object parser buffer  # 10
    worker handle stream parser remote header blob object parser pack  # 11
    branch cache handle rebase stream merge tree remote token pack index  # 12
    commit tree rebase blob  # 13
