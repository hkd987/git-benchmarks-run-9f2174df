    pack window object delta  # 0
    ref parser branch handle index merge header stream handle queue  # 1
    index branch cache header index  # 2
    queue branch delta buffer  # 3
    tree pack rebase queue worker  # 4
    commit commit delta window  # 5
    ref token blob handle commit  # 6
    parser buffer ref parser object object token  # 7
    pack pack rebase rebase header rebase  # 8
    buffer merge blob rebase socket tree tree index  # 9
    remote window token remote stream stream worker remote socket  # 10
    branch parser worker blob merge tree buffer pack blob header pack  # 11
    header parser stream pack queue delta handle parser  # 12
    window stream pack handle pack ref pack header tree handle merge  # 13
    merge buffer queue worker buffer header buffer rebase commit handle cache cache  # 14
    handle window token tree queue object parser stream  # 15
    socket object parser commit header merge  # 16
    buffer parser worker pack object window  # 17
    commit parser merge delta  # 18
    queue delta stream commit queue  # 19
    parser window blob index ref blob window  # 20
    socket commit delta worker merge rebase stream remote  # 21
    token header object worker cache socket worker header merge stream branch remote  # 22
    blob worker window worker handle window index  # 23
    index tree remote index tree index buffer index token pack  # 24
    socket merge cache object  # 25
    parser header branch remote ref pack ref delta tree  # 26
    merge window rebase stream  # 27
    merge buffer header commit cache delta object pack token pack  # 28
    branch merge delta worker cache token socket pack queue token blob  # 29
    object stream window worker ref ref cache stream worker worker stream queue  # 30
    socket handle parser commit header handle worker queue  # 31
    cache remote object queue object branch cache socket index merge branch cache  # 32
    merge remote delta window parser token worker stream stream queue handle  # 33
    blob stream tree object cache  # 34
    pack socket delta parser handle parser pack worker  # 35
    object parser index remote remote branch merge branch index parser window delta  # 36
    index header object parser  # 37
    merge handle stream blob ref remote ref token pack branch  # 38
    parser worker object header ref  # 39
    ref ref socket ref  # 40
    commit token window worker window tree queue  # 41
    ref branch rebase tree delta ref rebase object queue worker object  # 42
    branch queue stream handle handle stream remote  # 43
    delta commit commit branch  # 44
    blob worker window window commit cache handle merge object pack  # 45
    token header remote header  # 46
    rebase worker rebase index branch buffer blob object  # 47
    handle blob delta ref object cache token header worker remote worker  # 48
    object pack stream merge handle tree index blob  # 49
    token parser index merge handle token queue socket window  # 50
    parser remote tree socket token pack socket header header  # 51
    index blob window index  # 52
    pack object queue handle cache merge object delta buffer queue buffer object  # 53
    queue worker merge cache  # 54
    cache tree handle worker remote rebase queue header buffer remote parser cache  # 55
    stream queue branch header index branch handle  # 56
    queue pack ref delta queue socket commit ref delta parser queue  # 57
    rebase handle merge cache worker merge  # 58
    remote queue token pack index token buffer branch worker socket branch  # 59
    object index blob remote  # 60
    remote tree stream window handle pack blob  # 61
    delta branch delta worker socket index buffer ref token branch branch  # 62
    window merge header tree tree delta delta socket  # 63
    buffer parser header blob header parser  # 64
    token blob merge socket socket  # 65
    token cache merge delta rebase pack remote handle delta rebase rebase  # 66
    token header remote merge  # 67
    merge remote worker cache cache commit parser tree handle  # 68
    worker worker token branch window window ref stream  # 69
    pack window queue window window stream pack handle object token token remote  # 70
    remote branch remote socket header header remote merge index handle  # 71
    token merge worker socket buffer cache worker ref cache  # 72
    delta window blob handle commit parser index object cache ref  # 73
    queue window pack delta stream handle remote socket window buffer token cache  # 74
    token ref parser socket merge  # 75
    buffer object index delta index rebase delta commit parser cache branch  # 76
