    commit remote pack tree delta buffer commit merge index worker pack merge  # 0
    handle window cache cache blob  # 1
    header header stream worker merge  # 2
    rebase handle merge header branch delta delta buffer  # 3
    stream ref queue cache  # 4
    branch blob handle object merge header remote window object  # 5
    parser delta delta ref parser blob socket branch  # 6
    cache token branch delta merge worker object remote token commit  # 7
    pack buffer cache branch delta window buffer handle object cache  # 8
    buffer cache stream object queue rebase handle stream remote worker  # 9
    queue buffer socket header blob stream stream cache  # 10
    header token pack cache stream commit rebase tree worker token object stream  # 11
    parser worker index branch tree queue index rebase handle queue  # 12
    merge object index ref delta cache branch index tree commit tree stream  # 13
    index object parser index commit remote handle commit handle pack window  # 14
    tree delta merge queue delta header remote  # 15
    blob merge stream socket object delta header remote window commit ref  # 16
    buffer header branch delta ref branch handle merge  # 17
    index parser pack header blob object queue rebase  # 18
    index buffer ref cache delta stream commit worker parser blob index  # 19
    remote cache rebase merge commit branch parser  # 20
    buffer queue pack ref socket  # 21
    tree worker worker pack blob window remote  # 22
    branch worker rebase buffer merge merge token index delta  # 23
    stream branch merge header worker token buffer  # 24
    branch socket branch branch  # 25
    pack handle buffer cache commit stream queue commit buffer token pack  # 26
    worker buffer pack object  # 27
    cache pack commit token buffer header token parser queue  # 28
    blob branch parser tree token tree ref queue blob header delta  # 29
    cache stream cache ref branch  # 30
    buffer rebase ref merge rebase pack stream socket remote ref  # 31
    cache object socket socket delta object branch cache  # 32
    tree queue commit worker object branch worker commit remote  # 33
    handle merge remote remote tree cache index  # 34
    blob object merge handle index rebase cache delta  # 35
    queue header buffer rebase remote window socket stream rebase pack ref buffer  # 36
    window worker ref queue blob parser branch index branch remote index  # 37
    pack rebase merge delta index delta worker ref object commit stream stream  # 38
    stream remote object rebase branch queue queue handle  # 39
    buffer header stream tree tree object handle queue window  # 40
    delta blob queue blob delta  # 41
    parser handle ref ref worker branch object stream parser delta remote merge  # 42
    merge cache window merge remote parser pack parser remote index socket remote  # 43
    stream tree tree handle buffer cache header buffer  # 44
    index parser delta index token pack  # 45
    worker token buffer rebase tree window  # 46
    object header token handle delta blob branch tree rebase worker index  # 47
    tree stream buffer stream queue cache handle branch  # 48
    merge index socket blob  # 49
    tree header cache handle object handle parser rebase socket pack branch  # 50
    parser remote delta buffer token  # 51
    delta handle cache remote tree merge  # 0
    queue token header socket cache handle blob worker commit window  # 1
    branch stream pack blob socket commit socket merge remote queue remote  # 2
    token socket queue cache header index cache ref object  # 3
    socket rebase worker tree  # 4
    socket blob branch rebase index window blob socket index  # 5
    parser handle delta header worker object pack socket worker token  # 6
    index commit ref remote buffer worker cache cache stream index object  # 7
    object window worker commit buffer merge remote delta ref  # 8
    worker token tree handle header  # 9
    merge stream worker delta blob queue worker delta buffer stream  # 10
    buffer cache buffer tree  # 11
    rebase index cache object socket tree branch merge ref  # 12
