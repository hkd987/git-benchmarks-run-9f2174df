    stream branch socket buffer blob blob stream handle remote queue index remote  # 0
    blob object buffer header socket socket queue ref tree index object stream  # 1
    window cache window remote handle buffer window blob index  # 2
    pack handle delta blob header remote object window  # 3
    worker object ref ref object  # 4
    worker window ref handle index remote queue commit  # 5
    buffer socket index window socket window rebase token buffer  # 6
    branch rebase parser pack handle tree header ref delta parser  # 7
    remote merge branch branch pack blob delta index handle index object ref  # 8
    token window socket parser merge header delta handle object stream socket  # 9
    stream pack blob parser  # 10
    handle delta token parser blob commit branch header branch blob parser  # 11
    rebase ref buffer merge pack stream token  # 12
    buffer ref handle branch rebase  # 13
    delta queue stream blob pack merge  # 14
    commit ref commit branch worker object ref worker handle queue  # 15
    handle tree worker tree token parser  # 16
    window branch ref window cache  # 17
    rebase queue branch branch blob window header socket queue delta  # 18
    buffer index worker queue token remote queue remote  # 19
    token object tree handle commit worker merge token window commit ref parser  # 20
    worker header header ref stream token index  # 21
    rebase ref blob buffer rebase merge blob rebase stream index ref  # 22
    blob delta handle index stream handle ref  # 23
    cache stream header worker  # 24
    worker tree window index socket rebase remote merge index merge  # 25
    cache stream remote branch  # 26
    tree parser handle header object merge branch rebase merge  # 27
    branch handle ref object buffer socket object remote index buffer token ref  # 28
    worker pack rebase cache buffer branch token  # 29
    object tree parser stream header stream commit blob commit  # 30
    branch index parser cache tree token rebase cache object stream cache  # 31
    branch handle branch cache  # 32
    handle stream merge index delta index stream parser object worker  # 33
    parser buffer blob socket token delta socket  # 34
    buffer token index buffer  # 35
    commit window header cache object header  # 36
    branch handle pack buffer parser buffer stream parser branch  # 37
    remote queue commit socket delta tree pack handle  # 38
    pack commit pack handle pack commit tree socket window  # 39
    rebase handle index ref handle cache branch cache delta handle  # 40
    tree token merge branch header window stream worker tree socket worker pack  # 41
    tree object token merge tree branch index socket index  # 42
    commit header handle branch queue token pack stream buffer blob window  # 43
    queue worker worker worker worker queue blob header buffer parser tree  # 44
    merge object window parser object branch index ref token ref  # 45
    handle branch tree handle tree index queue buffer window ref  # 46
    delta rebase merge header merge rebase delta object parser commit token  # 47
    worker branch tree object blob object pack buffer branch tree index token  # 48
    handle remote blob commit  # 49
    cache worker socket blob stream ref header delta  # 50
    rebase parser object object branch remote blob token object blob  # 51
    pack token cache pack worker rebase queue queue window worker stream header  # 52
    header blob object delta index worker blob stream stream  # 53
    pack cache delta delta queue cache ref index rebase rebase  # 54
    queue queue blob tree parser socket socket socket branch pack  # 55
    header parser handle delta cache  # 56
    delta commit cache queue stream object tree ref  # 57
    handle remote parser pack  # 58
    queue commit pack buffer merge  # 59
    remote token index token branch cache stream  # 60
    blob rebase window branch stream tree parser tree  # 61
    buffer worker window blob rebase commit commit parser  # 62
    socket branch header object queue token index header worker pack  # 63
    blob remote delta handle commit index token  # 64
    cache branch rebase branch delta index window token token commit ref  # 65
    window parser cache remote tree  # 66
    blob remote worker ref token worker merge buffer commit  # 67
