    branch blob delta index worker object token worker  # 0
    branch pack queue index stream  # 1
    commit buffer merge stream tree blob window parser rebase object commit remote  # 2
    delta remote delta pack blob remote worker commit  # 3
    worker tree commit header window  # 4
    pack window header branch object branch object rebase socket  # 5
    cache token merge remote  # 6
    buffer cache parser branch cache  # 7
    pack object index token parser blob stream object stream queue pack remote  # 8
    header parser rebase buffer header pack handle tree token window blob blob  # 9
    cache tree stream rebase merge merge parser merge rebase commit merge remote  # 10
    worker window parser pack socket index buffer header socket worker  # 11
    pack handle socket remote blob worker buffer ref merge cache object pack  # 12
    socket tree cache handle window stream buffer blob  # 13
    ref tree worker merge worker window stream window stream socket blob pack  # 14
    tree remote ref queue cache blob cache object remote merge pack  # 15
    cache socket cache tree merge branch rebase  # 16
    pack index ref token remote  # 17
    stream blob branch header branch  # 18
    ref token merge index remote buffer pack blob token rebase delta branch  # 19
    tree cache ref token commit cache ref stream ref stream queue tree  # 20
    queue pack token window header ref token  # 21
    worker buffer window worker commit tree queue socket header  # 22
    buffer remote token pack tree delta  # 23
    blob index token buffer blob ref index socket worker worker worker branch  # 24
    socket worker tree remote merge header  # 25
    buffer blob remote remote ref stream branch window  # 26
    object branch blob blob ref index blob header  # 27
    object cache handle ref delta token parser  # 28
    stream socket delta merge object index  # 29
    blob cache rebase handle commit  # 30
    pack stream branch token delta stream tree branch rebase  # 31
    delta worker index pack merge remote  # 32
    rebase commit branch blob cache ref worker window remote object  # 33
    ref token blob remote object object branch delta buffer header  # 34
    worker handle header buffer cache handle index delta index window queue  # 35
    buffer worker merge buffer pack index commit pack buffer  # 36
    parser buffer stream remote object pack token delta  # 37
    object pack parser remote merge token  # 38
    index queue commit handle queue handle cache worker stream handle buffer object  # 39
    delta handle index branch  # 40
    ref merge window worker blob buffer rebase worker pack rebase rebase buffer  # 41
    rebase merge index tree token  # 42
    socket remote tree object  # 43
    tree stream index stream buffer remote branch stream ref handle token stream  # 44
    delta commit tree blob socket delta delta blob commit  # 45
    buffer branch ref pack queue commit socket pack commit token tree commit  # 46
    object queue rebase delta socket ref ref header buffer delta  # 47
    blob handle index parser merge stream  # 48
    buffer tree token rebase rebase buffer branch buffer  # 49
    worker queue merge ref blob socket queue window delta remote parser token  # 50
    handle rebase pack parser handle parser cache  # 51
    remote handle commit object blob token  # 52
    merge rebase index blob  # 53
    tree pack header buffer socket window delta branch  # 54
    index cache tree window delta parser tree handle rebase pack  # 55
    handle tree remote tree delta  # 56
    tree branch parser header header remote merge ref remote remote merge  # 57
    handle socket blob merge ref token  # 58
    header socket queue ref index tree blob handle stream index token buffer  # 59
    ref cache object tree delta  # 60
    merge window index index socket pack remote queue socket header buffer header  # 61
    ref blob ref commit  # 62
    header remote parser object socket merge socket queue merge ref stream rebase  # 63
    remote parser delta stream merge blob handle commit  # 64
    remote object rebase blob token pack stream token branch  # 65
    window buffer rebase commit socket blob worker queue  # 66
    cache handle parser object buffer window buffer ref window  # 67
