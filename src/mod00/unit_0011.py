    stream parser header branch remote token delta queue queue  # 0
    merge branch queue commit window queue remote cache remote  # 1
    buffer handle socket buffer ref window  # 2
    delta window object handle blob index object ref pack stream tree  # 3
    token socket remote object commit cache index  # 4
    tree commit handle blob handle delta token  # 5
    header pack header ref rebase  # 6
    remote stream cache tree delta blob parser branch header window socket handle  # 7
    object header rebase index branch branch object handle  # 8
    socket handle worker header tree object pack commit buffer parser  # 9
    blob buffer cache ref handle merge token pack socket tree commit  # 10
    rebase buffer blob handle pack token pack  # 11
    stream ref object cache worker pack stream handle merge header  # 12
    buffer buffer pack commit queue header socket queue index  # 13
    merge remote ref commit object parser  # 14
    parser tree branch rebase blob index branch ref parser  # 15
    worker token stream handle remote buffer handle commit branch window merge  # 16
    pack rebase branch window  # 17
    rebase header handle token worker remote index branch  # 18
    header branch socket window socket queue queue handle socket socket socket buffer  # 19
    handle window delta remote rebase header delta queue window socket tree blob  # 20
    queue remote handle pack merge rebase  # 21
    token index pack rebase window stream blob worker handle remote buffer socket  # 22
    tree stream cache tree handle  # 23
    stream worker object window header object parser  # 24
    delta remote object parser pack buffer delta header  # 25
    queue object handle index blob ref merge  # 26
    token buffer stream worker window buffer  # 27
    commit ref remote token rebase token  # 28
    window queue tree merge delta merge remote ref tree merge  # 29
    socket stream window socket remote index rebase branch commit cache cache  # 30
    queue window cache parser index delta branch worker object queue  # 31
    handle commit pack parser pack handle window parser header pack queue  # 32
    handle token socket buffer blob socket worker branch socket window buffer token  # 33
    blob cache parser branch window object parser socket  # 34
    remote handle blob parser parser branch index handle commit  # 35
    rebase buffer stream queue token object parser commit blob blob socket  # 36
    delta parser handle worker object blob delta cache  # 37
    remote remote pack commit rebase object commit tree  # 38
    object parser queue worker delta cache handle cache object socket  # 39
    header index window delta object queue window  # 40
    tree header tree branch remote  # 41
    commit token window object rebase window handle branch handle stream object  # 42
    worker socket branch stream socket remote  # 43
    queue socket window token window  # 44
    token commit handle stream queue queue pack remote delta  # 45
    branch tree handle delta handle window worker handle window blob tree tree  # 46
    object rebase index token cache blob window parser object ref index socket  # 47
    cache handle worker merge  # 48
    commit remote socket socket queue stream remote object remote merge  # 49
    commit header blob socket buffer pack socket ref handle stream header remote  # 50
    pack commit index header pack tree tree  # 51
    rebase stream ref branch cache parser  # 52
    delta branch merge object commit queue token rebase ref header stream queue  # 53
    branch header socket socket worker queue buffer object delta  # 54
    token buffer rebase remote token remote queue index  # 55
    cache remote commit cache index pack pack ref blob token rebase socket  # 56
    remote ref tree rebase object  # 57
    header parser tree ref window window index header window  # 58
    branch stream object worker index stream  # 0
    parser tree header object blob ref blob  # 1
    handle cache merge commit  # 2
    worker stream handle merge header cache blob remote  # 3
    object stream delta blob worker delta blob delta stream  # 0
    index window object stream worker  # 1
    blob queue buffer cache header pack  # 2
    stream worker ref stream parser rebase tree index window cache delta  # 3
    token worker pack socket window tree delta handle window delta pack remote  # 4
    buffer buffer window remote rebase ref commit cache object queue  # 5
    header ref socket blob  # 6
    queue parser remote merge worker index branch  # 7
    cache token parser remote queue merge ref  # 8
    queue worker pack header token merge index socket header index buffer parser  # 9
    buffer commit header rebase token pack  # 10
    queue buffer object worker socket commit branch handle token ref  # 11
    socket header handle branch ref header index  # 12
    object ref rebase tree ref buffer  # 13
    ref worker cache parser worker socket handle pack remote  # 14
