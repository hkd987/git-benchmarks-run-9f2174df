    header pack worker object buffer handle window pack worker window branch  # 0
    ref remote branch worker object worker header worker branch  # 1
    queue index remote stream window queue cache blob queue parser merge rebase  # 2
    parser stream blob delta worker remote index object commit  # 3
    tree cache header parser socket socket rebase ref ref worker  # 4
    parser window parser token rebase queue worker object stream rebase object socket  # 5
    socket queue cache token token window delta rebase pack  # 6
    stream cache tree queue  # 7
    cache rebase window window  # 8
    queue header worker rebase queue parser socket parser remote rebase branch handle  # 9
    commit blob header ref header buffer index branch cache branch  # 10
    pack branch object tree stream  # 11
    merge object rebase parser merge object ref commit remote object  # 12
    object stream buffer merge cache parser window index buffer rebase  # 13
    cache worker rebase handle  # 14
    blob buffer socket token ref blob header socket delta cache cache  # 15
    header rebase remote tree delta commit queue  # 16
    rebase branch delta parser window remote header token  # 17
    socket cache stream stream queue pack stream stream handle  # 18
    merge socket remote object stream branch header buffer header  # 19
    blob pack tree socket socket ref object worker cache  # 20
    object rebase branch tree remote socket  # 21
    queue parser token window buffer parser stream stream worker  # 22
    window parser worker header remote tree cache commit index worker worker pack  # 23
    buffer parser blob ref header delta cache delta stream cache index  # 24
    stream worker socket merge token object socket blob branch handle branch  # 25
    stream handle blob merge stream merge queue parser queue ref cache  # 26
    window cache parser ref window buffer blob worker blob  # 27
    tree index tree tree worker commit parser pack blob buffer  # 28
    handle blob blob rebase  # 29
    handle token worker branch socket stream index  # 30
