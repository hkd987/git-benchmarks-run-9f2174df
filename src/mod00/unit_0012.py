    cache handle buffer remote stream worker object cache  # 0
    worker window remote delta delta branch tree cache  # 1
    token object object ref stream rebase socket header object  # 2
    stream queue stream object tree socket object  # 3
    commit queue queue branch delta pack  # 4
    queue header branch token worker tree  # 5
    tree window cache commit handle merge queue header merge  # 6
    stream delta stream tree window stream commit branch index  # 7
    buffer delta merge tree ref  # 8
    parser worker token header tree parser  # 9
    commit remote index delta socket index index ref worker  # 10
    parser worker commit buffer queue header worker blob delta queue  # 11
    merge socket merge branch cache buffer header socket window buffer token  # 12
    worker rebase pack header token remote handle token  # 13
    branch parser pack delta delta  # 14
    merge remote handle branch queue  # 15
    index buffer window merge tree worker merge window branch parser remote branch  # 16
    buffer ref delta commit commit pack  # 17
    index index blob parser  # 18
    window worker ref branch tree buffer  # 19
    pack blob commit socket socket pack ref  # 20
    buffer worker tree window  # 21
    stream blob merge blob  # 22
    queue buffer branch tree parser token merge stream ref queue stream  # 23
    worker branch commit rebase queue buffer stream window commit blob  # 24
    rebase object object worker queue stream branch  # 25
    tree tree socket merge cache token blob socket remote buffer cache  # 26
    cache worker index remote delta commit cache cache rebase merge parser commit  # 27
    handle commit index object merge delta handle pack buffer index commit  # 28
    buffer tree remote ref cache  # 29
    ref parser delta index tree  # 30
    commit stream queue queue  # 31
    branch parser handle token worker index worker commit ref  # 32
    branch buffer worker tree blob index stream  # 33
    blob stream index queue index  # 34
    object branch worker remote handle cache commit parser buffer header  # 35
    branch worker cache index tree worker parser commit parser window handle queue  # 36
    stream tree ref branch rebase stream  # 37
    parser token parser tree commit worker object cache  # 38
    delta socket stream rebase queue token handle remote handle token  # 39
    buffer blob blob stream pack window tree queue  # 40
    token socket queue remote merge  # 41
    object stream ref cache worker merge handle delta tree commit ref window  # 42
    handle pack pack header socket token object queue parser parser worker worker  # 43
    token cache socket tree commit  # 44
    merge handle rebase cache  # 45
    token pack queue parser remote tree worker commit socket  # 46
    ref queue ref header remote ref commit token queue blob stream  # 47
    index tree worker socket queue handle object header  # 48
    worker queue rebase queue ref stream parser tree  # 49
    parser parser handle delta header delta buffer rebase header object  # 50
    ref parser buffer cache ref rebase index delta remote parser window  # 51
    handle window branch blob blob stream  # 52
    merge worker object header queue window tree parser pack  # 53
    rebase buffer cache buffer index blob remote index branch worker  # 54
    buffer index header socket branch blob index buffer pack header  # 55
    rebase buffer rebase header pack worker buffer object handle  # 56
    pack blob worker window rebase cache cache parser branch worker  # 57
    buffer rebase parser rebase pack queue header pack window  # 58
    handle stream pack socket worker header merge delta worker pack  # 59
    blob blob merge buffer merge parser commit worker cache worker  # 60
    branch socket tree commit pack  # 0
    stream parser commit delta token index index buffer queue window merge cache  # 1
    handle stream rebase branch  # 2
    commit socket ref buffer worker index ref worker cache socket  # 3
    header token delta cache index  # 4
    worker socket rebase socket  # 5
    cache socket commit object header remote parser buffer remote  # 6
    cache commit rebase branch window parser socket  # 7
    blob pack ref object worker branch index branch  # 8
    tree remote header parser delta token  # 9
    worker pack merge commit tree window token parser token  # 0
    index index tree pack merge handle cache window pack branch  # 1
    window rebase handle handle delta tree tree cache ref  # 2
    ref stream window tree delta socket worker branch socket  # 3
    cache socket index index buffer tree socket object  # 4
    branch header stream delta cache blob tree rebase commit  # 5
    handle queue tree remote branch branch delta handle delta commit cache socket  # 6
    handle tree socket stream header pack stream  # 7
    blob branch ref stream token buffer tree pack index parser ref worker  # 8
