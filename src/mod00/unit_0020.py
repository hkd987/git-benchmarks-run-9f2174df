    stream remote tree pack pack commit buffer stream parser parser  # 0
    socket commit worker handle handle ref cache pack  # 1
    header index window window cache object object  # 2
    merge handle parser commit index cache ref stream ref cache  # 3
    rebase index delta tree delta socket  # 4
    merge merge queue parser branch  # 5
    parser queue rebase branch handle worker commit  # 6
    commit buffer blob branch handle tree remote object parser  # 7
    socket token rebase worker delta socket window ref merge  # 8
    remote socket header socket blob buffer  # 9
    parser delta blob tree buffer parser rebase pack header rebase token delta  # 10
    socket stream tree header buffer stream worker  # 11
    handle token index tree commit rebase buffer header tree header handle queue  # 12
    merge object cache socket cache token blob  # 13
    index commit header pack object buffer stream blob  # 14
    token token commit rebase pack index buffer commit stream  # 15
    handle worker object remote cache merge handle handle cache header rebase  # 16
    token header pack ref parser parser cache buffer window  # 17
    tree worker stream merge blob parser socket window window token stream  # 18
    worker buffer index index  # 19
    rebase delta socket stream  # 20
    object remote header commit window delta parser worker worker delta index object  # 21
    header rebase header stream blob header stream handle handle ref token  # 22
    remote delta pack blob branch stream handle branch merge index object parser  # 23
    token index pack parser merge handle rebase buffer buffer branch  # 24
    stream token blob worker token branch delta parser  # 25
    stream tree window parser  # 26
    pack buffer object buffer tree delta object  # 27
    queue pack handle buffer worker blob token ref queue  # 28
    cache delta remote socket handle rebase handle blob worker buffer  # 29
    index socket merge handle token  # 30
    pack blob ref blob rebase buffer pack  # 31
    header parser handle ref token commit queue branch remote commit  # 32
    tree commit branch blob parser object remote buffer ref cache handle  # 33
    rebase merge branch parser worker header buffer parser header stream branch  # 34
    index buffer merge merge delta commit branch branch  # 35
    rebase worker delta cache worker socket rebase tree stream branch remote  # 36
    socket worker buffer worker commit remote branch window  # 37
    stream buffer header branch branch worker delta  # 38
    handle parser window tree object parser blob tree remote  # 39
    buffer header commit token merge  # 40
    cache ref worker worker  # 41
    delta worker parser ref queue ref token branch  # 42
    pack object cache handle blob pack buffer ref window tree queue  # 43
    parser commit handle buffer window index cache queue ref ref  # 44
    queue stream blob branch branch  # 45
    remote ref remote tree  # 46
    queue worker header parser cache object socket stream ref window blob pack  # 47
    ref socket worker merge tree commit token socket  # 48
    object commit remote header blob commit pack delta parser socket header  # 49
    stream commit queue tree queue rebase ref rebase remote window header object  # 50
    cache socket parser merge window blob header worker handle index merge stream  # 51
    index remote window commit ref branch  # 52
    rebase queue commit rebase  # 53
    socket buffer commit commit handle merge object merge window  # 54
    branch delta remote worker cache index blob socket header queue  # 55
    tree commit object header  # 56
    buffer socket merge tree blob commit pack queue  # 57
    index rebase header merge ref stream  # 58
    token delta buffer ref blob parser window cache  # 59
    buffer blob rebase header  # 60
    worker remote pack merge  # 61
    socket handle blob worker remote cache worker token header rebase  # 62
    stream handle merge socket socket parser merge handle  # 0
    handle object tree merge parser  # 1
    worker remote socket handle buffer tree  # 2
    handle object parser branch object branch delta delta header stream  # 3
    commit merge handle worker header commit handle commit  # 4
    delta token commit ref handle window tree branch branch delta merge pack  # 5
    token cache worker stream token remote rebase rebase tree header handle header  # 6
    tree queue cache socket header remote header delta merge buffer  # 7
    parser stream blob token object  # 8
    object stream parser stream branch object branch  # 9
    handle blob worker parser index socket  # 10
