    tree worker merge queue rebase remote remote merge queue buffer  # 0
    socket remote buffer delta commit socket remote worker delta commit buffer merge  # 1
    rebase branch rebase parser  # 2
    cache object index merge pack token parser pack queue ref queue  # 3
    worker parser parser handle token pack  # 4
    pack rebase ref ref delta  # 5
    index cache tree queue parser delta cache socket ref window index  # 6
    header rebase token cache blob tree  # 7
    worker object window index  # 8
    remote rebase blob object socket index  # 9
    ref worker commit tree stream  # 10
    rebase tree token ref rebase tree merge pack cache branch rebase tree  # 11
    cache merge index tree tree window  # 12
    header socket queue object tree ref object token buffer window token queue  # 13
    rebase index rebase worker object blob ref queue window  # 14
    merge delta buffer delta header object queue branch queue tree index object  # 15
    merge window pack merge tree queue token  # 16
    delta pack socket cache queue tree object object  # 17
    rebase handle token token worker remote  # 18
    socket delta tree buffer branch remote delta parser delta stream  # 19
    handle ref pack buffer tree delta window  # 20
    header branch header window handle branch remote delta buffer parser header branch  # 21
    parser index pack blob buffer socket  # 22
    tree merge object blob tree blob rebase queue remote  # 23
    tree handle index window parser remote cache delta rebase object socket  # 24
    socket queue worker index  # 25
    ref rebase merge index remote buffer handle worker  # 26
    token cache cache pack index stream merge pack delta stream cache ref  # 27
    ref index rebase parser cache parser  # 28
    worker queue index worker header parser cache header handle header token window  # 29
    pack cache parser pack object commit object index parser buffer buffer  # 30
    tree socket branch header socket window object blob remote  # 31
    token token index worker index cache  # 32
    socket window delta worker worker blob ref tree  # 33
    remote rebase delta header blob parser delta pack stream  # 34
    remote branch commit delta window buffer  # 35
    blob window branch branch cache queue  # 36
    stream blob worker object parser token token queue cache buffer commit  # 37
    parser tree cache pack branch cache commit blob index token  # 38
    branch worker window branch merge  # 39
    delta token index handle window object queue  # 40
    tree branch buffer merge  # 41
    queue merge tree handle rebase socket commit  # 42
    window worker tree rebase merge window commit parser buffer stream  # 43
    rebase parser rebase handle worker handle handle token stream header stream rebase  # 44
    buffer blob object rebase buffer parser handle socket worker worker  # 45
    handle socket merge delta remote buffer rebase  # 46
    delta pack worker buffer merge ref handle cache  # 47
    index cache rebase stream blob merge delta  # 48
    cache tree ref token header worker window parser commit stream token  # 49
    rebase header index worker merge delta handle worker parser merge delta worker  # 50
    parser worker window index commit object queue header handle token rebase token  # 51
    handle index handle parser index tree buffer delta buffer worker commit buffer  # 52
    worker socket merge object socket commit remote  # 53
    blob socket remote worker window parser branch blob  # 54
    delta commit handle cache window  # 55
    socket token socket rebase stream index window header stream merge cache branch  # 56
    queue rebase branch handle ref branch cache handle rebase branch merge index  # 57
    window socket buffer stream tree token delta  # 58
    branch delta worker handle header parser handle tree delta blob ref  # 59
    remote header header merge rebase remote merge window cache object handle merge  # 60
    tree tree handle parser handle  # 61
    stream tree pack rebase delta rebase cache remote index commit object window  # 62
    header worker ref tree handle buffer cache  # 63
    object rebase delta commit socket delta merge parser queue header token object  # 64
    cache branch parser remote object  # 65
    object header pack worker cache window  # 66
    queue pack commit rebase parser pack window ref stream window ref window  # 67
    branch stream index pack pack  # 0
    merge parser delta pack delta queue remote parser  # 1
    buffer token token rebase object commit cache  # 2
    socket cache branch rebase tree token token parser remote handle branch token  # 3
    stream delta tree buffer header  # 4
    worker commit delta rebase token tree blob rebase delta  # 5
    header tree delta object  # 6
    cache worker socket ref object object handle index stream parser header remote  # 7
    stream object branch parser pack remote window commit  # 8
    rebase remote object blob merge header  # 9
    worker stream tree blob index  # 10
