    blob branch index window header handle buffer socket cache pack parser window  # 0
    socket index worker rebase ref queue queue blob merge  # 1
    ref branch ref stream commit buffer  # 2
    remote window queue queue header ref  # 3
    cache remote commit worker remote delta header token header cache queue tree  # 4
    ref token blob token rebase  # 5
    socket cache handle header index handle cache stream blob branch rebase  # 6
    rebase object parser pack buffer commit worker  # 7
    object socket socket worker token worker header  # 8
    rebase pack buffer rebase cache parser buffer ref tree  # 9
    remote cache cache merge ref pack queue delta  # 10
    rebase window blob merge remote cache blob index delta pack branch stream  # 11
    stream parser worker delta buffer  # 12
    worker buffer worker queue commit index pack token handle buffer remote remote  # 13
    index commit index blob socket worker cache window branch  # 14
    token token queue stream delta buffer parser branch handle blob tree  # 15
    socket commit merge commit header pack branch delta tree commit remote  # 16
    pack handle socket remote parser buffer worker tree window tree ref index  # 17
    window object rebase queue token merge cache blob commit handle delta merge  # 18
    object branch commit buffer rebase index commit worker blob merge rebase  # 19
    commit parser worker delta object object socket token  # 20
    merge stream ref parser worker token merge socket stream  # 21
    ref window stream blob  # 22
    delta worker blob stream index pack parser queue worker  # 23
    commit commit handle object queue header tree stream buffer  # 24
    parser index socket object  # 25
    window buffer cache ref queue token ref header stream cache worker remote  # 26
    delta branch rebase stream buffer window  # 27
    object stream socket handle pack pack  # 28
    branch header stream remote header merge socket delta branch  # 29
    delta header cache handle commit merge  # 30
    remote delta merge handle stream  # 31
    socket merge commit token rebase ref token rebase tree  # 32
    index delta remote pack window  # 33
    tree buffer blob parser remote branch parser branch window token stream stream  # 34
