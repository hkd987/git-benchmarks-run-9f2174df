    merge object worker commit window  # 0
    token worker parser cache merge delta  # 1
    merge header pack commit branch worker pack delta  # 2
    socket handle merge ref window delta commit worker window tree  # 3
    header token ref remote stream stream window handle object  # 4
    buffer buffer worker tree commit index queue  # 5
    blob socket tree token handle socket pack queue remote commit  # 6
    remote object blob socket branch handle  # 7
    cache worker stream remote parser index buffer ref worker  # 8
    socket buffer queue socket parser queue socket object tree  # 9
    index object remote cache ref handle ref stream blob header branch  # 10
    pack queue handle parser worker  # 11
    token stream commit object  # 12
    delta window rebase delta tree buffer header blob  # 13
    tree index ref parser token blob tree branch ref socket blob rebase  # 14
    delta cache window remote window tree delta  # 15
    commit header index cache parser stream header  # 16
    token parser worker merge cache socket header tree remote parser cache  # 17
    handle stream window remote delta tree delta cache cache window stream ref  # 18
    header index ref commit socket buffer cache index  # 19
    buffer branch worker delta worker branch worker delta  # 20
    pack buffer tree branch ref commit tree delta parser rebase index  # 21
    delta parser worker index commit rebase pack  # 22
    ref cache pack worker buffer token stream header  # 23
    rebase parser queue merge ref object pack window  # 24
    header object blob ref branch queue socket window buffer token delta token  # 25
    socket delta rebase remote index cache token token stream merge index queue  # 26
    object cache commit remote queue cache window object socket  # 27
    socket handle commit header  # 28
    remote merge header merge worker  # 29
    token blob rebase ref cache handle remote token tree ref parser  # 30
    pack rebase branch window ref queue branch stream object remote buffer worker  # 31
    token buffer buffer header window object token branch ref window  # 32
    parser branch delta blob blob  # 33
    ref socket commit index  # 34
    index rebase merge window blob remote  # 35
    header socket delta tree merge socket  # 36
    merge stream remote remote token cache tree stream buffer worker branch stream  # 37
    rebase delta socket pack ref branch  # 38
    merge window object window  # 39
    merge parser handle branch header handle buffer  # 40
    tree rebase object remote ref worker index  # 41
    rebase delta ref queue index token  # 0
    pack ref commit branch delta queue stream rebase  # 1
    commit stream buffer buffer socket parser  # 2
    remote buffer index commit  # 3
    buffer branch buffer handle window  # 4
    index tree worker delta blob tree commit stream  # 5
    socket window parser token pack remote  # 6
    pack stream worker header rebase header queue  # 7
    index parser index index rebase remote rebase blob rebase socket queue  # 8
    worker header parser socket window header delta worker  # 9
    worker stream merge window remote worker socket branch commit commit window  # 10
