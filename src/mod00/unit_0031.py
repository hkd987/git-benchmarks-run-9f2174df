    index index worker window token worker cache blob window pack tree merge  # 0
    object parser branch delta header socket socket queue buffer  # 1
    socket rebase stream handle handle worker ref header token window  # 2
    branch commit ref cache header queue tree branch commit  # 3
    socket ref buffer buffer object commit worker socket blob worker header  # 4
    merge delta ref socket pack index tree merge rebase  # 5
    merge window worker token handle  # 6
    header buffer delta tree pack rebase branch blob blob window stream  # 7
    buffer remote handle window commit blob rebase parser blob stream rebase  # 8
    remote worker pack merge parser remote parser tree parser  # 9
    cache delta pack cache blob  # 10
    stream stream buffer remote cache parser queue buffer rebase window buffer blob  # 11
    ref worker pack remote  # 12
    socket pack ref pack merge rebase object object  # 13
    window worker index socket cache header  # 14
    window worker window queue remote handle branch handle  # 15
    cache pack index merge tree worker rebase header rebase branch handle  # 16
    ref index blob blob branch blob window object handle commit cache  # 17
    merge ref blob socket cache buffer  # 18
    merge parser window handle window index token socket  # 19
    merge ref blob parser merge worker object handle cache handle token  # 20
    handle window branch worker  # 21
    socket parser pack cache  # 22
    stream cache handle token handle index blob worker object merge  # 23
    cache parser cache rebase remote  # 24
    pack worker buffer queue pack socket header  # 25
    parser header pack branch queue pack queue window  # 26
    tree index tree window ref branch handle buffer commit  # 27
    buffer token cache remote commit  # 28
    handle stream branch token rebase parser  # 29
    commit delta window parser delta buffer index buffer  # 30
    delta window branch merge ref remote delta buffer socket  # 31
    worker tree stream commit remote  # 32
    merge pack rebase handle commit pack parser token rebase header  # 33
    stream index delta commit  # 34
    cache cache delta merge  # 35
    ref handle header index buffer blob  # 36
    rebase merge socket header window  # 37
    rebase index pack parser  # 38
    delta buffer queue ref queue ref window handle handle  # 39
    merge merge blob blob branch remote token parser  # 40
    ref tree buffer commit  # 41
    blob header delta blob stream pack token  # 42
    delta window tree socket branch handle index branch ref buffer  # 43
    blob object merge tree  # 44
    parser buffer handle worker parser handle commit header pack branch  # 45
    tree merge buffer buffer remote header tree ref token  # 46
    handle blob stream delta remote index  # 47
    object object merge object buffer pack commit socket window delta ref  # 48
