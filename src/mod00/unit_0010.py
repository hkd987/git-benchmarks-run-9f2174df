    ref delta header tree index  # 0
    stream object merge stream pack header window stream ref  # 1
    rebase rebase branch merge handle socket pack header  # 2
    cache delta ref handle branch  # 3
    window pack branch tree delta object stream handle handle delta object  # 4
    pack socket ref stream delta parser remote object object worker remote tree  # 5
    stream merge stream stream index  # 6
    branch delta queue window rebase remote  # 7
    blob branch cache queue ref header stream blob socket object  # 8
    merge delta worker queue  # 9
    stream buffer remote worker worker token  # 10
    socket queue remote token branch worker worker parser window delta delta handle  # 11
    cache ref remote worker branch socket handle delta window token  # 12
    tree queue tree object delta  # 13
    socket stream index remote object  # 14
    socket cache cache header token token merge  # 15
    socket token branch queue stream branch handle cache queue rebase  # 16
    token object remote handle  # 17
    tree ref stream remote  # 18
    merge remote remote token token blob remote  # 19
    socket socket queue rebase remote rebase commit  # 20
    rebase blob header index pack queue commit socket  # 21
    delta ref queue token header  # 22
    socket ref rebase object object  # 23
    blob tree token pack index  # 24
    index ref remote delta header token remote stream token blob  # 25
    branch stream window stream parser stream object rebase pack  # 26
    pack tree cache header rebase merge worker cache token remote queue  # 27
    stream commit buffer branch buffer branch handle header remote  # 28
    worker branch buffer commit worker blob  # 29
    branch window queue delta ref rebase remote ref tree pack ref  # 30
    worker parser buffer stream socket branch commit worker cache object  # 31
    merge rebase tree stream cache delta stream handle window header  # 32
    socket parser merge buffer ref merge branch branch pack commit  # 33
    delta stream buffer object merge queue parser worker socket  # 34
    tree header remote branch merge  # 35
    merge stream token cache object token pack buffer ref worker  # 36
    index pack stream rebase rebase branch stream tree handle token window commit  # 37
    remote cache commit rebase cache delta merge commit delta handle  # 38
    stream rebase index tree parser token token  # 39
    merge index pack handle object index tree worker  # 40
    delta header worker ref remote delta index tree  # 41
    merge index worker buffer socket remote  # 42
    window tree window remote  # 43
    worker header handle merge remote parser blob  # 44
    window remote delta pack stream  # 45
    tree branch branch remote  # 46
    header worker buffer worker stream parser  # 47
    token merge header pack pack queue header commit remote delta index merge  # 48
    handle header rebase rebase rebase merge pack rebase header handle index buffer  # 49
    handle delta socket handle window buffer merge token rebase handle delta  # 50
    socket queue ref socket rebase remote queue merge token header window header  # 51
    handle rebase cache tree queue cache queue token handle header header  # 52
    queue token branch token worker cache ref socket tree  # 53
    header blob tree branch  # 54
    token parser index ref commit cache branch object  # 55
    remote delta stream worker branch branch  # 56
    ref pack commit commit header ref socket socket  # 57
    rebase cache branch ref queue parser branch  # 58
    index branch token branch merge buffer merge  # 59
    header commit object cache cache cache token  # 60
    commit handle commit parser token  # 61
    pack tree merge queue  # 0
    remote header cache pack merge parser handle queue ref  # 1
    buffer socket token remote cache pack handle branch  # 2
    queue parser blob rebase stream queue cache stream ref stream  # 3
    tree blob object remote branch token  # 4
    worker window blob stream header ref socket  # 5
    handle worker branch socket socket stream handle  # 6
    worker blob blob socket  # 7
    token rebase handle delta branch stream merge  # 8
    parser pack ref pack  # 0
    blob token commit commit commit token remote stream socket tree tree handle  # 1
    delta token queue stream cache ref cache  # 2
    worker handle worker commit header socket  # 3
    delta worker worker commit  # 4
