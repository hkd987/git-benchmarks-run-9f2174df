    token worker window commit handle index  # 0
    stream remote stream tree buffer socket worker handle worker pack index rebase  # 1
    socket pack buffer merge token object object socket worker queue  # 2
    branch object commit delta worker branch merge rebase  # 3
    commit remote stream window socket socket merge  # 4
    remote ref handle rebase delta commit  # 5
    rebase parser branch ref queue cache token delta window merge pack cache  # 6
    window stream token remote remote buffer token  # 7
    rebase parser stream index worker tree ref handle  # 8
    header tree window buffer commit tree tree  # 9
    object ref socket token worker parser token branch commit delta  # 10
    worker stream ref window blob tree index window window pack delta  # 11
    handle remote tree header object remote header token  # 12
    pack object object cache parser  # 13
    parser window stream branch  # 14
    delta stream merge stream remote  # 15
    pack merge merge rebase stream index  # 16
    merge worker ref window token handle parser socket ref parser header  # 17
    merge commit index parser branch handle tree ref ref token queue  # 18
    ref delta parser branch cache index token header tree  # 19
    parser merge rebase commit blob tree rebase  # 20
    rebase socket cache merge object queue index merge header token delta delta  # 21
    merge commit worker remote  # 22
    delta queue socket worker delta  # 23
    merge pack queue cache pack delta object merge parser  # 24
    cache handle token ref header token merge object branch parser window buffer  # 25
    stream buffer handle blob handle socket rebase remote window buffer  # 26
    socket buffer tree branch blob remote handle branch merge object  # 27
    commit rebase tree ref window worker index window  # 28
    tree parser object tree  # 29
    merge parser delta blob window  # 30
    index remote merge blob window queue branch ref header token token index  # 31
    header index blob token  # 32
    cache cache stream rebase index cache commit queue parser index cache token  # 33
    stream token worker buffer handle branch queue window token branch  # 34
    parser stream delta blob rebase  # 35
    remote tree merge buffer object  # 36
    token ref commit object worker token stream  # 37
    buffer worker rebase header branch queue delta parser object  # 38
    worker index tree merge  # 0
    socket stream token socket socket delta commit commit  # 1
    token socket tree parser merge handle rebase  # 2
    parser token pack handle remote branch header ref delta branch window rebase  # 3
    buffer pack object index blob worker  # 4
    blob stream stream stream ref blob queue rebase commit blob header tree  # 5
    pack ref buffer tree commit header socket rebase  # 6
    window commit parser worker  # 7
    handle blob remote window commit window worker  # 8
