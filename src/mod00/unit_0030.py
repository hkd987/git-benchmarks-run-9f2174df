    branch blob branch buffer ref rebase buffer handle parser  # 0
    token cache ref pack rebase branch cache delta commit  # 1
    delta blob token rebase object delta cache handle  # 2
    window buffer rebase window queue  # 3
    socket worker buffer token  # 4
    tree delta pack queue  # 5
    delta buffer object tree  # 6
    handle socket index header worker worker index  # 7
    object buffer ref token  # 8
    parser pack commit merge  # 9
    token queue queue token ref commit  # 10
    rebase rebase merge header blob worker index  # 11
    tree stream merge parser header branch index handle buffer pack branch index  # 12
    token commit socket delta blob  # 13
    ref header ref buffer parser parser merge index commit pack  # 14
    blob pack remote merge token  # 15
    socket object queue header window delta  # 16
    socket delta delta blob stream delta handle cache worker pack  # 17
    object rebase delta commit stream ref header parser  # 18
    cache socket queue header merge header socket queue handle commit socket  # 19
    pack rebase cache object ref object worker worker remote cache  # 20
    socket blob header merge remote window  # 21
    socket cache pack stream  # 22
    cache token cache header ref delta  # 23
    commit rebase ref pack handle  # 24
    rebase pack remote tree  # 25
    parser branch buffer rebase delta commit  # 26
    pack remote pack handle delta stream remote token buffer cache  # 27
    token commit blob handle worker index merge  # 28
    cache branch buffer branch index buffer buffer cache token commit header  # 29
    branch stream buffer queue rebase parser queue cache buffer handle socket  # 30
    parser branch commit stream remote merge ref merge merge  # 31
    delta delta socket socket remote rebase branch token cache  # 32
    index blob tree worker queue tree window  # 33
    delta token handle stream queue  # 34
    index ref queue queue commit  # 35
    window remote header ref blob delta remote queue object branch branch  # 36
    merge stream commit ref delta header socket stream  # 37
    commit merge rebase merge rebase handle socket buffer object rebase  # 38
    merge header header commit queue commit handle  # 39
    stream window remote commit branch worker cache parser  # 40
    header parser buffer remote handle index header branch remote token delta parser  # 41
    stream handle delta socket blob  # 42
    buffer header token branch token parser buffer stream  # 43
    queue header index index socket  # 44
    tree index socket stream ref remote tree worker tree  # 45
    queue object stream commit socket branch ref remote tree token tree  # 46
    parser worker tree commit object worker  # 47
    blob buffer cache stream remote  # 0
    object socket commit queue header token parser ref object queue  # 1
    handle tree tree window branch parser commit pack window socket  # 2
    handle cache cache stream parser index  # 3
    worker object commit pack stream blob remote ref index remote queue worker  # 4
    socket queue rebase pack ref  # 5
    merge merge window delta remote ref  # 6
    stream remote header token parser tree  # 7
    object stream rebase window window stream commit parser pack  # 8
    tree handle merge delta socket header remote buffer cache pack  # 9
    worker parser index worker rebase  # 10
    queue remote socket header branch pack tree stream worker  # 0
    tree branch cache remote worker queue worker buffer branch header ref stream  # 1
    handle ref remote tree blob cache pack ref token socket  # 2
    blob token buffer parser branch commit blob stream branch header object token  # 3
    parser blob commit remote window buffer  # 4
    ref pack handle cache commit tree  # 5
    delta parser worker ref tree object  # 6
    header buffer handle blob ref window index blob blob merge handle cache  # 7
    index tree handle token header window object pack  # 8
    merge commit index token remote token tree merge  # 9
    socket object branch socket tree pack  # 10
    handle commit merge pack object tree handle ref tree blob rebase  # 11
    stream pack queue ref tree worker  # 12
