    cache cache parser branch window rebase  # 0
    branch window index rebase commit blob commit branch pack window tree commit  # 1
    stream index merge handle cache branch index window cache rebase  # 2
    rebase worker ref buffer worker  # 3
    handle rebase ref index handle parser worker cache  # 4
    branch branch ref header tree socket blob worker commit worker window  # 5
    parser pack worker remote handle parser worker index remote tree  # 6
    rebase queue ref parser window handle ref remote worker  # 7
    object window tree queue token merge buffer handle ref socket remote worker  # 8
    token stream window window handle pack window ref socket parser  # 9
    stream token stream token merge remote socket  # 10
    parser header ref blob commit pack blob index socket stream commit pack  # 11
    cache merge header cache pack  # 12
    delta rebase cache delta  # 13
    delta worker socket rebase ref tree pack parser index stream parser  # 14
    header queue cache cache queue ref  # 15
    header stream blob object merge object token  # 16
    parser pack ref index tree queue token  # 17
    merge ref commit pack tree socket pack pack  # 18
    delta header cache buffer pack  # 19
    index parser header tree object  # 20
    blob parser handle parser commit  # 21
    delta branch handle index  # 22
    window remote queue queue queue delta parser worker buffer worker window  # 23
    object pack parser merge ref stream worker  # 24
    handle ref header remote header blob worker merge  # 25
    pack token window handle rebase pack token pack header pack  # 26
    window worker parser remote socket merge worker index object  # 27
    object object queue ref window socket  # 28
    index window tree commit window ref socket header token  # 29
    socket merge window ref delta socket remote  # 30
    commit parser ref rebase handle window buffer commit stream window socket  # 31
    queue pack cache remote window header cache  # 32
    handle index branch ref worker queue cache delta ref tree ref socket  # 33
    merge socket branch ref socket blob buffer cache cache rebase index commit  # 34
    index delta buffer window ref  # 35
    remote index object cache buffer stream merge pack stream branch  # 36
    merge object index socket parser worker  # 37
    index ref commit pack delta ref socket delta stream  # 38
    merge remote rebase ref stream window buffer token  # 39
    buffer socket stream merge  # 40
    token cache commit handle worker cache blob rebase object  # 41
    handle ref window rebase socket  # 42
    window object cache buffer  # 43
    cache merge handle merge header object blob socket  # 44
    window stream cache token commit branch delta branch stream remote parser merge  # 45
    merge buffer delta blob merge  # 46
    worker socket parser blob  # 47
    parser handle tree delta  # 48
    commit ref pack blob ref handle  # 49
    header merge cache commit tree  # 50
    window pack cache window  # 51
    index rebase remote remote object index  # 52
    pack remote window object commit delta parser worker rebase index parser pack  # 53
    object tree branch tree branch handle token ref window  # 0
    commit blob header delta rebase parser header blob handle header cache  # 1
    branch socket parser worker socket merge ref socket socket  # 2
    window token pack blob index rebase buffer  # 3
    pack commit pack cache ref blob  # 4
    stream tree object cache handle window pack worker handle socket  # 5
    merge token handle branch parser pack buffer buffer object  # 6
    cache commit socket worker cache pack  # 7
    socket ref cache token  # 8
    stream parser merge remote  # 9
