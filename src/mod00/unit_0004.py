    branch commit stream queue parser rebase window parser queue remote  # 0
    index blob pack merge  # 1
    window cache commit stream rebase merge  # 2
    index buffer pack delta parser token merge rebase delta delta  # 3
    handle merge queue branch worker blob pack branch ref  # 4
    cache remote queue queue handle index buffer token merge  # 5
    pack tree stream queue index remote cache  # 6
    commit remote handle pack object commit pack queue queue  # 7
    buffer queue index window delta branch delta  # 8
    index rebase socket parser worker commit branch ref worker  # 9
    rebase buffer token window ref queue worker ref stream socket pack  # 10
    buffer index cache window  # 11
    buffer cache stream index  # 12
    rebase merge ref parser worker index commit index  # 13
    socket parser cache queue merge merge branch rebase rebase pack  # 14
    branch pack merge commit window token worker handle tree  # 15
    branch delta pack socket  # 16
    merge blob index ref stream object  # 17
    object commit cache worker cache stream cache merge index worker header  # 18
    ref queue buffer index object blob branch handle  # 19
    queue socket tree branch merge merge cache  # 20
    pack merge tree rebase pack buffer object tree header branch delta delta  # 21
    delta blob window header merge delta buffer  # 22
    remote delta worker rebase blob cache  # 23
    cache remote window delta stream queue delta remote merge remote merge  # 24
    socket stream merge handle ref blob  # 25
    socket commit queue branch header handle ref remote header tree cache ref  # 26
    worker ref handle buffer cache buffer  # 27
    commit index blob parser buffer ref delta remote parser  # 28
    pack buffer remote queue index merge index buffer stream  # 29
    delta buffer tree commit ref index blob header delta worker buffer stream  # 30
    object token cache blob object commit  # 31
    handle delta ref pack handle socket ref stream  # 32
    token commit delta object cache queue pack  # 33
    parser rebase pack cache rebase stream branch  # 34
    worker window stream stream  # 35
    pack header delta rebase header parser pack  # 36
    socket branch queue blob socket  # 37
    worker window buffer pack parser merge token branch object  # 38
    delta parser tree rebase parser index  # 39
    delta header header parser index rebase queue  # 40
    object ref index branch  # 41
    parser tree parser remote window queue pack commit commit  # 42
    window tree stream merge window merge pack  # 43
    ref worker token stream tree  # 44
    index token merge rebase commit parser index socket commit buffer  # 45
    index index blob buffer  # 46
    object rebase buffer blob commit rebase  # 47
    ref tree parser branch tree  # 48
    window token cache header cache pack index  # 49
    delta stream branch rebase token pack  # 50
    parser worker branch queue window object buffer  # 51
    commit rebase merge stream  # 52
    pack worker parser handle delta object worker object  # 53
    commit stream blob delta  # 54
