    remote pack token tree handle header blob remote  # 0
    blob remote handle queue handle stream blob cache tree rebase pack remote  # 1
    index tree rebase rebase handle delta  # 2
    index merge buffer parser tree worker index header socket  # 3
    queue index header remote window buffer delta buffer  # 4
    token pack queue token buffer pack parser rebase ref window handle  # 5
    parser header worker buffer window tree object rebase tree tree tree  # 6
    tree parser remote object  # 7
    delta cache stream merge handle tree  # 8
    branch merge parser blob socket  # 9
    socket rebase parser ref tree cache token delta queue branch delta  # 10
    pack delta cache token branch remote parser ref object token parser delta  # 11
    delta delta buffer queue blob object window  # 12
    socket stream tree pack blob queue remote remote stream object blob socket  # 13
    commit remote commit index parser branch ref  # 14
    branch cache commit commit tree  # 15
    stream token handle blob handle token blob index delta buffer stream queue  # 16
    blob stream header parser window tree  # 17
    queue blob socket object parser blob stream  # 18
    rebase remote handle handle socket commit object pack branch cache ref merge  # 19
    object pack socket ref buffer object window pack queue branch  # 20
    worker tree tree delta tree blob worker  # 21
    blob parser rebase ref  # 22
    buffer cache header blob commit  # 23
    buffer remote index cache object parser stream stream  # 24
    stream header index object socket parser delta merge index buffer window  # 25
    branch handle index window merge merge  # 26
    stream delta token handle stream branch  # 27
    parser stream queue queue branch tree handle blob queue  # 28
    remote blob remote header rebase delta tree handle merge branch cache index  # 29
    pack header commit index handle rebase  # 30
    branch cache header index merge  # 31
    buffer tree stream handle socket tree tree delta header queue delta  # 32
    ref pack handle ref  # 33
    commit merge rebase ref  # 34
    socket parser buffer queue ref  # 35
    handle merge queue branch merge  # 36
    socket token index delta ref queue  # 37
    object blob parser delta  # 38
    worker stream pack remote pack rebase  # 39
    header blob queue stream cache stream rebase stream commit tree  # 40
    pack socket handle delta tree stream socket merge index token  # 41
    worker socket rebase worker buffer delta  # 42
    worker header cache worker ref cache blob pack  # 43
    header tree handle stream buffer pack commit cache header  # 44
    branch handle object index blob handle socket index stream remote  # 45
    cache pack index commit  # 46
    pack commit header delta branch merge rebase queue header index  # 47
    merge cache blob socket token  # 48
    pack rebase blob token merge merge remote header pack pack buffer cache  # 49
    window handle object delta window handle index branch handle commit worker  # 50
    rebase worker merge object  # 51
    handle index ref ref queue  # 52
    index commit window remote pack blob remote  # 53
    handle ref window commit index rebase socket socket merge header buffer  # 54
    header buffer object branch buffer worker stream commit index buffer  # 55
    header stream cache socket worker merge branch ref window cache  # 56
    cache header object tree socket commit tree buffer rebase tree  # 57
    window token branch header  # 58
    queue blob object rebase object parser socket  # 59
    tree tree buffer queue object parser token  # 60
    window worker parser queue token  # 61
    handle queue ref token branch branch parser  # 62
    rebase window commit window branch object cache pack  # 63
    socket branch window remote object delta  # 64
    branch branch index queue token window queue queue branch parser  # 65
    stream header handle buffer object object pack remote  # 66
    commit merge cache buffer queue socket cache cache blob index  # 67
    ref cache commit cache rebase ref remote  # 68
    delta merge delta handle header branch blob  # 69
    branch window parser index object handle index handle buffer  # 70
    buffer remote cache rebase merge index delta tree  # 71
    merge merge index index token branch tree branch stream ref worker index  # 0
    tree socket delta header socket tree parser header ref index header  # 1
    index header worker remote index parser delta ref blob handle pack  # 2
    tree cache window pack blob header token object pack worker ref buffer  # 3
    remote pack branch delta branch blob merge remote header  # 4
    blob queue buffer index window merge socket blob blob socket object  # 5
    merge queue branch worker index  # 6
