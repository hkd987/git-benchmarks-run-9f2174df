    buffer handle window worker socket object token socket commit merge tree tree  # 0
    merge queue stream token  # 1
    window rebase index worker merge  # 2
    delta pack worker worker blob  # 3
    queue rebase window stream delta remote ref delta socket index  # 4
    header token cache remote merge buffer socket object cache stream  # 5
    header token ref tree branch remote branch commit token commit  # 6
    handle remote stream rebase worker tree  # 7
    socket blob cache cache rebase pack cache index  # 8
    socket pack token handle object  # 9
    queue queue stream handle object branch queue token blob worker  # 10
    rebase remote index branch index pack pack stream ref pack branch socket  # 11
    parser header header worker  # 12
    handle token token token socket commit token token handle  # 13
    pack socket index header delta delta socket handle delta rebase blob handle  # 14
    commit socket cache header branch  # 15
    worker blob ref blob token rebase token object parser  # 16
    index stream ref blob branch rebase stream parser worker branch  # 17
    socket header merge remote queue parser tree tree merge ref buffer header  # 18
    pack delta merge buffer parser stream delta index remote tree rebase ref  # 19
    commit blob handle window  # 20
    worker worker delta queue queue tree blob buffer  # 21
    pack delta object commit tree queue ref window stream rebase delta buffer  # 22
    rebase handle handle cache pack queue token ref buffer  # 23
    worker merge worker merge delta rebase  # 24
    merge blob pack stream handle header branch token merge branch ref  # 25
    token parser ref pack  # 26
    token blob remote blob ref  # 27
    tree object object pack token rebase tree rebase buffer stream index delta  # 28
    stream handle worker remote commit parser socket parser token remote delta  # 29
    remote branch parser worker cache  # 30
    tree commit tree rebase queue handle delta  # 31
    stream worker remote branch index remote tree header pack  # 32
    handle branch commit handle  # 33
    object branch branch tree delta tree branch branch handle worker index handle  # 34
    delta ref merge blob header commit remote handle merge merge stream  # 35
    worker stream branch header buffer remote ref ref commit buffer ref handle  # 36
    rebase rebase tree commit merge worker merge commit merge socket  # 37
    socket buffer cache handle buffer worker  # 38
    delta stream queue pack tree object delta stream  # 39
    stream branch rebase parser remote parser object worker pack  # 40
    ref blob cache branch window rebase stream handle pack  # 41
    token handle commit tree window blob commit branch handle socket pack rebase  # 42
    window window token remote token delta queue object ref token buffer index  # 43
    ref window object parser queue  # 44
    stream queue socket branch buffer parser window  # 45
    commit blob merge merge merge rebase buffer commit branch worker rebase  # 46
    cache window parser socket queue stream index blob tree index  # 47
    delta commit queue header stream branch  # 48
    handle ref ref blob window socket merge socket parser cache cache  # 49
    delta tree header window merge pack token parser  # 50
    buffer delta queue delta  # 51
    tree object branch remote  # 52
    branch socket blob stream delta socket  # 53
    handle delta delta index  # 54
    merge commit ref object header socket object token tree commit  # 55
    tree stream delta index worker header window blob buffer queue parser blob  # 56
    buffer pack handle header stream  # 57
    remote worker buffer delta merge delta branch header stream remote  # 58
    pack handle header header  # 59
    blob cache remote worker object rebase rebase  # 60
    ref rebase merge socket token object window  # 61
    blob index branch rebase ref  # 62
    blob header header handle  # 63
    window worker token ref cache header parser socket  # 64
    stream cache delta object remote delta token worker branch  # 65
    token pack merge handle tree handle delta stream delta  # 66
    remote blob handle worker buffer worker delta buffer blob queue  # 67
    token pack socket cache branch blob blob window tree header  # 68
    branch branch header remote rebase token  # 69
    remote cache index merge token queue pack worker remote  # 0
    branch handle parser queue merge worker ref  # 1
    commit remote ref object  # 2
    blob rebase remote parser object parser tree ref blob  # 3
    parser pack cache queue ref pack cache rebase  # 4
    parser stream ref buffer branch blob  # 5
    header delta object queue  # 6
    remote socket buffer worker merge buffer  # 7
    socket parser object tree blob parser  # 8
    rebase token cache commit worker remote cache queue rebase  # 9
    stream token worker delta remote commit stream remote merge worker tree  # 10
