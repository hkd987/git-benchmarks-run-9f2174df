    handle queue queue cache branch cache  # 0
    delta index pack branch ref  # 1
    delta index queue stream rebase remote buffer delta object index  # 2
    blob index branch object index remote tree index merge  # 3
    handle stream parser blob handle merge cache stream pack token  # 4
    branch buffer stream ref rebase socket  # 5
    merge rebase tree delta tree stream delta buffer remote handle  # 6
    tree pack rebase blob remote socket remote cache  # 7
    handle parser socket cache remote worker socket window remote socket  # 8
    merge ref delta queue  # 9
    index tree object buffer commit  # 10
    cache handle stream commit commit parser  # 11
    stream handle tree socket remote token branch  # 12
    header header object queue rebase delta  # 13
    object buffer remote index rebase buffer remote  # 14
    index handle socket tree remote header header  # 15
    handle handle stream commit token merge stream commit pack pack  # 16
    rebase commit merge index  # 17
    blob handle branch worker commit token  # 18
    queue object branch index handle buffer window cache pack stream window  # 19
    queue remote handle rebase socket pack ref  # 20
    pack worker delta stream ref ref branch buffer tree queue branch  # 21
    worker tree window pack object blob merge index rebase  # 22
    merge window blob buffer pack pack index index header stream  # 23
    remote pack ref tree pack  # 24
    delta parser merge rebase  # 25
    token ref blob worker remote handle stream commit blob tree header  # 26
    buffer pack window index rebase  # 27
    parser parser parser pack window handle remote commit parser pack stream  # 28
    tree socket window worker tree header rebase window pack pack  # 29
    remote cache handle commit  # 30
    commit buffer branch cache remote remote tree  # 31
    blob token blob tree delta merge socket commit handle queue remote  # 32
    delta queue ref token  # 33
    worker object token blob handle commit  # 34
    blob merge queue worker delta branch queue window cache queue  # 35
    socket object socket stream rebase ref socket queue cache cache buffer index  # 36
    worker buffer socket parser  # 37
    ref merge socket ref parser rebase branch stream tree delta  # 38
    handle parser socket remote handle queue index  # 39
    queue parser merge merge socket header buffer tree tree  # 40
    index window ref token token blob rebase object remote object  # 41
    delta tree object header object socket remote  # 42
    remote cache rebase rebase stream pack cache remote blob  # 43
    commit handle index rebase handle blob queue window header pack worker cache  # 44
    cache queue ref header delta worker delta buffer socket branch  # 45
    socket ref window remote delta  # 46
    queue buffer token pack buffer commit blob ref window remote queue  # 47
    worker window stream index remote delta stream  # 48
    stream tree index socket remote blob merge socket header index window socket  # 49
    merge queue pack commit branch window index  # 50
    delta handle ref rebase delta cache buffer header  # 51
    rebase object stream object handle object worker socket buffer delta merge  # 52
    parser branch stream worker commit  # 53
    token ref rebase stream merge delta parser branch worker token pack  # 54
    remote tree delta index delta rebase branch  # 55
    pack pack stream index  # 56
    handle worker queue buffer index blob index merge stream ref parser  # 57
    merge tree merge remote commit  # 58
    parser cache index merge cache merge handle socket worker  # 59
    parser worker parser token remote cache rebase index merge socket object  # 60
    remote commit header queue object blob index header  # 61
    rebase worker merge rebase window worker window pack  # 62
    stream branch tree tree window ref  # 63
    stream header branch stream pack handle  # 64
    blob rebase cache tree pack handle ref merge remote  # 65
    index remote socket parser tree stream parser window delta queue commit  # 66
    remote pack commit commit token  # 67
    ref token ref index commit token window merge  # 68
    token merge window ref delta  # 69
    cache delta pack stream tree delta object cache object queue  # 70
    index pack window tree object remote stream tree object  # 71
    pack blob branch parser object  # 72
    object object branch remote tree buffer merge worker blob worker parser blob  # 73
    blob header commit handle rebase  # 74
    object parser buffer object worker token delta commit  # 75
    commit ref tree buffer rebase pack pack window remote  # 76
    blob ref handle socket pack  # 77
    ref pack index commit header tree merge object worker  # 78
    worker socket cache cache rebase ref merge  # 79
    rebase rebase stream branch header token remote rebase  # 80
    socket commit blob remote worker index worker token parser pack queue handle  # 81
    rebase delta delta rebase tree index rebase tree handle  # 0
    merge handle parser object  # 1
    handle ref tree cache socket merge tree  # 2
    branch merge commit object pack remote parser pack queue worker cache ref  # 3
    token index stream remote tree  # 4
    branch blob ref token ref index window socket remote ref blob rebase  # 5
