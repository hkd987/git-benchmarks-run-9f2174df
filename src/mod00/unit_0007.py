    delta branch ref merge buffer delta  # 0
    token handle merge branch buffer object ref branch index worker  # 1
    object pack remote rebase ref socket branch handle  # 2
    stream window index pack  # 3
    queue merge delta worker remote ref pack worker  # 4
    stream merge stream parser parser  # 5
    pack parser merge worker buffer parser commit blob window queue cache remote  # 6
    branch window tree rebase branch pack window  # 7
    token branch header object buffer remote rebase stream  # 8
    socket stream object index tree ref branch pack delta object  # 9
    buffer queue handle pack  # 10
    tree cache merge merge merge parser worker queue blob  # 11
    branch token buffer parser handle ref  # 12
    worker header pack commit tree branch blob  # 13
    object queue delta rebase branch parser window  # 14
    tree blob socket tree cache object index branch delta branch stream stream  # 15
    cache blob ref branch token buffer buffer  # 16
    queue stream parser remote handle buffer ref parser blob blob header  # 17
    remote handle buffer queue object tree queue branch token blob index window  # 18
    parser window rebase merge ref  # 19
    object tree stream socket  # 20
    worker window socket blob header ref  # 21
    handle merge tree parser token queue buffer object branch commit token rebase  # 22
    index cache delta header blob pack  # 23
    parser handle token socket tree  # 24
    delta rebase header blob  # 25
    queue object pack header branch merge  # 26
    remote token blob queue buffer token  # 27
    delta cache parser index header stream delta blob blob blob blob  # 28
    pack pack index branch delta ref buffer parser token index tree window  # 29
    delta header parser blob tree window queue header tree  # 30
    handle remote worker handle  # 31
    rebase worker rebase remote header object handle  # 32
    buffer worker socket commit tree stream handle  # 33
    ref stream remote branch window buffer  # 34
    delta queue window socket index index token buffer token  # 35
    token token object worker parser handle branch header  # 36
    window commit handle delta queue socket remote  # 37
    parser worker token buffer pack buffer  # 38
    header ref remote merge stream index parser handle branch tree merge  # 39
    stream index ref window queue tree tree header queue cache worker  # 40
    parser worker remote window buffer pack pack queue  # 41
    buffer worker blob window header blob tree commit blob tree branch  # 42
    index queue ref stream cache  # 43
    socket blob cache buffer token parser delta object queue buffer rebase  # 44
    index object header delta ref cache blob worker delta remote queue queue  # 45
    queue ref pack buffer socket buffer pack rebase delta  # 46
    ref buffer object buffer handle worker rebase branch ref pack parser index  # 47
    window blob cache pack buffer token  # 48
    parser queue remote blob  # 49
    handle handle queue object remote pack  # 50
    buffer queue branch socket parser  # 51
    pack delta pack parser object remote delta queue header  # 52
    cache branch delta pack handle token header  # 53
    delta queue pack parser blob buffer window blob branch  # 54
    handle delta commit cache window stream rebase handle remote worker delta  # 0
    blob remote socket tree worker header delta pack worker  # 1
    worker remote stream parser commit tree worker handle tree index  # 2
    object cache stream buffer  # 3
    queue branch worker handle rebase commit handle worker branch cache header  # 4
    delta merge worker rebase parser cache object window header  # 5
