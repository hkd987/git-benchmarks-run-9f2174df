    socket stream buffer token ref cache remote rebase worker window cache blob  # 0
    object delta tree header ref worker merge header  # 1
    object buffer parser pack buffer  # 2
    delta merge tree parser handle parser remote  # 3
    remote merge header object delta delta parser worker blob  # 4
    delta handle header socket header commit queue  # 5
    buffer socket commit stream ref queue merge branch  # 6
    rebase commit socket buffer index blob tree commit delta  # 7
    delta merge socket remote stream worker rebase  # 8
    index ref index window handle handle cache delta commit merge socket  # 9
    blob remote delta blob socket handle merge object cache commit window  # 10
    object token commit worker token token header  # 11
    tree token stream worker buffer header delta pack  # 12
    index handle window blob handle pack object blob cache  # 13
    buffer token pack header stream  # 14
    commit delta remote index handle index commit buffer cache buffer handle cache  # 15
    buffer ref blob cache pack socket remote pack pack delta rebase  # 16
    token header rebase handle socket  # 17
    blob blob ref ref window tree header object worker queue socket  # 18
    blob tree merge handle parser parser queue merge  # 19
    pack cache delta delta stream worker worker remote  # 20
    socket ref object worker worker  # 21
    rebase queue remote remote buffer rebase object commit  # 22
    blob cache tree handle  # 23
    ref parser index handle merge delta ref  # 24
    token cache buffer merge  # 25
    queue merge blob object buffer delta branch ref blob handle header parser  # 26
    parser worker socket stream remote commit buffer commit rebase buffer queue  # 27
    handle handle worker blob merge branch window blob commit  # 28
    index merge ref stream stream queue rebase token worker worker token blob  # 29
    buffer stream window window queue index window branch tree object window  # 30
    pack socket buffer socket object stream delta window merge commit blob commit  # 31
    pack buffer handle blob worker  # 32
    delta token rebase parser queue cache socket merge remote  # 33
    handle buffer queue commit rebase  # 34
    delta merge remote delta rebase handle  # 35
    socket token token header  # 36
    index blob parser blob  # 37
    worker window cache cache branch stream merge index token  # 38
    parser tree window rebase  # 39
    pack object remote header cache delta merge rebase header rebase  # 40
    stream tree worker merge  # 41
    window object pack index commit header cache  # 42
    stream socket pack index header  # 43
    commit blob stream queue pack tree  # 44
    ref pack ref queue  # 45
    header ref commit token blob  # 46
    handle index socket pack parser stream socket remote delta object window handle  # 47
    handle buffer branch header blob  # 48
    window pack worker commit handle blob pack ref merge parser pack index  # 49
    ref window pack rebase window object delta parser window queue parser  # 50
    buffer commit window object  # 51
    merge index handle ref delta branch worker pack blob delta  # 52
    worker queue blob header  # 53
    window commit rebase worker pack branch  # 54
    handle rebase cache delta token  # 55
    queue object index commit buffer rebase rebase socket rebase index merge  # 56
    token socket window rebase buffer branch handle  # 57
    buffer index token rebase tree queue blob buffer buffer blob queue stream  # 58
    pack window commit header rebase queue buffer blob  # 59
    socket blob pack ref blob worker handle  # 60
    merge header tree object token merge window delta worker delta worker  # 61
    pack socket object index  # 62
    queue delta object commit cache delta  # 63
    socket queue blob window  # 64
    handle cache object token handle queue stream ref object pack  # 65
    handle object commit delta socket blob socket branch remote  # 66
    token branch branch ref token  # 67
    worker ref worker merge pack socket index index cache remote rebase  # 68
