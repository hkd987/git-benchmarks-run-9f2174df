    branch branch cache token delta parser socket ref delta  # 0
    token window header branch header worker stream index cache merge header  # 1
    tree rebase queue window  # 2
    stream delta cache stream socket socket cache blob branch window index delta  # 3
    rebase window delta window pack branch merge remote stream remote cache  # 4
    rebase socket socket stream rebase ref token object blob commit  # 5
    commit delta remote rebase ref commit stream pack ref  # 6
    handle merge socket blob remote remote queue window ref parser queue  # 7
    object pack delta parser stream queue queue  # 8
    buffer queue delta tree parser pack worker ref token queue rebase merge  # 9
    stream handle merge index  # 10
    pack delta token queue tree  # 11
    stream merge token buffer blob cache  # 12
    delta handle branch rebase tree rebase remote rebase rebase worker  # 13
    object socket commit index window pack rebase cache token  # 14
    header delta rebase remote  # 15
    socket index buffer queue window socket  # 16
    object remote queue commit window remote stream rebase rebase  # 17
    parser tree token index  # 18
    index tree remote delta commit object  # 19
    remote queue handle token  # 20
    commit buffer stream index buffer queue ref  # 21
    tree buffer handle branch commit index blob cache rebase socket window blob  # 22
    ref cache pack buffer handle branch object pack  # 23
    delta token object ref object index object parser cache  # 24
    tree index ref object blob queue remote  # 25
    stream pack merge tree  # 26
    tree remote object remote ref pack blob blob merge parser  # 27
    window buffer stream handle branch worker pack blob stream cache merge  # 28
    stream queue buffer worker index delta tree tree header  # 29
    queue buffer blob remote  # 30
    remote buffer tree header branch rebase merge commit  # 31
    cache stream tree handle  # 32
    rebase delta ref cache  # 33
    object cache queue branch tree worker token  # 34
    handle parser buffer rebase cache  # 35
    buffer stream token stream remote index index handle  # 36
    branch remote stream socket object window commit commit tree pack handle delta  # 37
    cache ref branch rebase parser commit  # 38
    worker blob token tree pack token blob commit buffer commit index  # 39
    buffer token buffer window object  # 40
    remote header blob blob object tree pack delta stream merge handle  # 41
    parser ref merge rebase  # 42
    branch merge tree rebase token merge stream  # 43
    rebase rebase cache remote cache  # 44
    worker blob worker index merge ref delta delta index  # 45
    socket worker rebase commit object commit rebase  # 46
    socket index stream tree handle object token buffer blob queue delta  # 47
    delta worker buffer rebase worker pack window queue delta tree socket  # 48
    window socket token cache remote delta buffer  # 49
    stream buffer buffer cache commit branch commit parser handle branch delta delta  # 50
    cache parser tree token handle branch handle  # 51
    delta header stream merge ref handle queue cache token token stream queue  # 52
    blob cache queue parser token handle worker delta tree buffer token  # 53
    tree buffer queue socket  # 54
    handle token merge index stream pack token object remote token remote branch  # 55
    blob handle socket worker cache object remote  # 56
    token worker delta ref cache parser rebase parser  # 57
    pack object remote token cache window token merge remote queue merge  # 58
    remote ref delta object branch socket merge  # 59
    delta queue ref remote tree token parser handle tree stream worker  # 60
    delta delta object index blob worker rebase  # 61
    stream parser remote branch pack  # 62
    commit commit stream blob tree  # 63
    worker window remote remote merge tree handle rebase header object pack rebase  # 64
    parser stream worker stream commit parser token stream stream worker parser parser  # 65
    window header blob delta  # 66
    socket object socket token header branch commit ref blob  # 67
    branch pack buffer header remote token pack index  # 68
