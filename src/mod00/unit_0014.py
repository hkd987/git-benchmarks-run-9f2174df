    pack parser worker merge queue index window branch commit commit cache  # 0
    pack index branch token  # 1
    delta cache header blob delta cache commit buffer branch index ref  # 2
    window commit handle merge handle remote cache branch pack branch  # 3
    branch commit token parser  # 4
    merge rebase merge token blob socket  # 5
    parser queue cache commit commit pack delta pack queue rebase  # 6
    pack worker object pack handle socket token socket stream  # 7
    merge socket rebase commit  # 8
    window remote delta ref branch ref header tree queue  # 9
    window handle parser pack  # 10
    merge index stream tree buffer token remote remote remote window tree cache  # 11
    stream blob object ref stream  # 12
    parser handle branch rebase cache header object commit cache merge branch stream  # 13
    remote header remote cache stream  # 14
    socket queue delta parser parser remote token stream socket merge window merge  # 15
    parser token index token handle merge merge buffer socket socket  # 16
    handle blob object rebase worker  # 17
    branch queue index blob ref buffer window worker rebase worker pack  # 18
    commit commit worker ref queue token branch remote  # 19
    commit pack parser commit token blob  # 20
    ref cache handle cache token  # 21
    queue cache window stream pack branch stream  # 22
    merge socket socket window blob pack buffer socket buffer remote blob  # 23
    header cache stream merge branch queue branch parser ref delta  # 24
    parser parser parser handle stream token index worker window  # 25
    handle token token index stream  # 26
    stream cache worker stream token buffer window index stream commit socket  # 27
    branch branch stream handle index token worker  # 28
    handle rebase token cache token remote blob cache handle delta  # 29
    branch remote window worker window socket window blob  # 30
    branch socket buffer cache buffer buffer  # 31
    handle socket ref tree socket object header tree  # 32
    index queue blob pack stream socket stream branch header worker object header  # 33
    token branch window worker delta index  # 34
    handle worker ref branch remote  # 35
    commit header window delta ref blob cache ref branch delta  # 36
    buffer commit stream header header  # 37
    index merge buffer rebase  # 38
    blob remote cache header branch merge queue  # 39
    buffer remote buffer delta branch stream handle handle handle  # 40
    token header token rebase delta branch cache blob delta delta parser  # 41
    blob queue token queue stream header socket delta  # 42
    queue window header worker parser  # 43
    blob commit commit queue socket rebase tree blob header  # 44
    cache object ref worker branch merge  # 45
    commit parser remote header remote delta parser rebase  # 46
    buffer worker tree branch header parser index  # 47
    buffer header tree merge ref delta window stream object  # 48
    stream merge object socket ref remote stream object stream ref remote queue  # 49
    header index remote buffer cache stream  # 50
    window token token cache parser stream handle rebase parser merge pack cache  # 51
    tree handle pack delta object tree header delta  # 52
    pack ref parser blob object index worker delta window merge buffer  # 53
    token pack buffer token handle worker blob window window commit handle  # 54
    remote buffer cache tree branch ref header header blob  # 55
    buffer cache commit tree rebase merge  # 56
    token blob cache buffer socket pack buffer  # 57
    index worker object token  # 58
    ref index object blob queue stream index  # 59
    token delta commit blob delta rebase merge merge  # 60
    token commit pack cache cache parser parser tree cache handle ref blob  # 61
