    object queue window commit  # 0
    handle queue merge parser  # 1
    window blob tree header cache parser buffer rebase rebase  # 2
    ref queue ref socket object socket tree socket blob index  # 3
    cache buffer queue ref parser token handle branch remote index cache handle  # 4
    pack index remote queue window cache queue  # 5
    cache remote buffer merge pack  # 6
    ref remote handle token buffer queue  # 7
    parser queue blob handle stream worker branch object worker branch cache  # 8
    object merge queue parser cache handle pack header branch commit object handle  # 9
    token socket merge header remote token commit header  # 10
    socket handle socket window blob merge branch window blob blob ref parser  # 11
    object stream rebase parser window stream  # 12
    rebase commit buffer window stream commit branch rebase header  # 13
    queue handle token socket merge parser blob branch handle  # 14
    remote token parser worker  # 15
    index window commit socket branch socket delta object queue remote  # 16
    commit window blob buffer parser socket remote ref pack buffer  # 17
    object tree stream handle stream  # 18
    handle delta token rebase buffer rebase object delta ref merge delta  # 19
    tree buffer handle blob cache pack parser pack  # 20
    pack tree queue queue window buffer pack cache parser  # 21
    parser merge cache window index branch handle stream object  # 22
    remote remote delta cache rebase  # 23
    branch pack blob token  # 24
    worker rebase token branch socket cache tree socket merge pack header parser  # 25
    worker header socket parser branch queue  # 26
    commit ref ref cache tree object  # 27
    ref socket token handle remote object header  # 28
    token merge branch stream ref  # 29
    branch tree rebase token  # 30
    ref pack tree cache index  # 31
    ref blob object ref tree object  # 32
    buffer index index merge stream window ref  # 33
    delta parser socket tree rebase tree  # 34
    delta header socket stream token delta parser object blob parser header token  # 35
    delta buffer commit token handle  # 36
    stream handle token branch  # 37
    stream blob header cache commit socket index blob window buffer remote  # 38
    blob handle commit tree worker ref ref  # 39
    cache rebase handle ref window handle index merge cache  # 40
    blob socket object worker blob branch worker token queue tree ref header  # 41
    remote socket header cache cache handle queue branch ref worker tree socket  # 42
    stream stream buffer tree window  # 43
    stream queue worker index blob rebase parser branch object pack buffer merge  # 44
    delta stream window handle rebase remote ref window window remote buffer  # 45
    ref branch object parser socket  # 46
    queue blob queue stream cache header header stream buffer parser  # 47
    remote socket pack cache delta rebase tree index ref window blob  # 48
    merge merge queue tree worker commit token socket branch branch commit index  # 49
    buffer merge cache delta worker branch worker commit  # 50
    buffer index pack queue cache  # 51
    worker rebase remote ref pack merge window buffer ref blob handle  # 52
    header index token tree socket buffer blob tree cache header token  # 53
    parser delta handle remote worker  # 54
    handle object index parser branch header rebase rebase buffer branch commit index  # 55
    stream rebase remote handle blob tree tree stream stream  # 56
    handle header window queue parser cache stream cache remote merge  # 57
    blob queue pack tree worker buffer remote buffer  # 58
    token index window queue commit ref merge  # 0
    queue worker stream socket merge  # 1
    token socket merge object blob branch window remote merge handle remote socket  # 2
    handle token token pack rebase window delta  # 3
