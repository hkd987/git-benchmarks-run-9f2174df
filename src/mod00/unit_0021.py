    object header commit socket  # 0
    window window header stream object  # 1
    branch buffer rebase blob blob branch handle handle ref index parser  # 2
    queue cache stream remote stream delta merge queue worker delta  # 3
    worker branch remote tree  # 4
    window delta header socket cache merge buffer socket window window merge  # 5
    blob branch object window delta socket remote remote token worker token merge  # 6
    merge header handle stream object queue parser worker  # 7
    blob parser cache ref  # 8
    cache ref blob tree tree object branch  # 9
    stream tree remote cache rebase header  # 10
    commit blob remote header socket remote header handle queue delta index queue  # 11
    delta queue window worker merge cache  # 12
    cache queue buffer token handle window  # 13
    header handle token queue pack rebase merge token parser parser blob  # 14
    blob window pack tree ref merge rebase header header remote  # 15
    rebase worker object handle  # 16
    cache merge header handle header branch parser pack window header window rebase  # 17
    window merge merge pack branch merge tree object  # 18
    stream tree rebase queue token  # 19
    parser blob ref tree index  # 20
    branch stream parser tree  # 21
    header buffer stream blob branch stream  # 22
    worker socket worker delta token cache remote commit cache object buffer  # 23
    token header token socket merge branch queue branch window token socket  # 24
    token merge token stream queue blob  # 25
    socket remote cache delta remote  # 26
    branch object commit parser token branch worker window commit object  # 27
    buffer branch branch rebase merge index  # 28
    commit commit buffer delta pack worker token object tree stream worker  # 29
    index tree ref rebase branch object remote worker parser pack  # 30
    parser cache window remote  # 31
    blob pack index pack tree branch  # 0
    cache rebase tree queue cache tree buffer index window handle  # 1
    commit index header branch parser stream delta ref  # 2
    header worker header worker  # 3
    buffer socket parser delta cache blob socket  # 4
    rebase object tree ref queue ref ref tree header rebase  # 5
    header delta remote window object socket commit  # 6
    remote tree tree pack queue header  # 7
    handle handle stream remote remote commit index object token buffer  # 8
